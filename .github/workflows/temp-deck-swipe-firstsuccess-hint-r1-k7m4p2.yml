name: TEMP Deck swipe first-success hints R1

on:
  push:
    branches:
      - sol/deck-swipe-firstsuccess-hint-r1-20260905

permissions:
  contents: write

concurrency:
  group: temp-deck-swipe-firstsuccess-hint-r1-k7m4p2
  cancel-in-progress: false

jobs:
  validate-workflow:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Validate executor transport
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse --abbrev-ref HEAD)" = "sol/deck-swipe-firstsuccess-hint-r1-20260905"
          git diff --exit-code 2696d00d23ff84e63de6c3d79c666a1f2c0cf3e6 HEAD -- browser/deck-storage-corner-runtime.mjs tests/deck-storage-corner-runtime.test.mjs
