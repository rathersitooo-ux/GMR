HTML_BYTES=11914066
HTML_LINES=4401

=== LINE PATTERN 'function checkWin' HITS=1 ===
--- line 1827 context 1824:1832 ---
001824: function sideGroups(){if(state.match.mode!=='2v2')return state.match.players.map(p=>({id:p.id,players:[p]}));return['A','B'].map(t=>({id:t,players:state.match.players.filter(p=>p.team===t)}))}
001825: async function replacementBattleCard(p,reason){const opts=p.hand.filter(id=>CARDS[id]?.runtime_status==='playable').map(cardId=>({kind:'hand',cardId,id:cardId,detail:reason}));if(!opts.length)return null;const ch=await chooseAbilityItems('バトルカードを再確認',reason,opts,1,1,p.human);return ch[0]?.cardId||null}
001826: async function resolveBattle(){const m=state.match;if(m.busy)return;if(!m.target)throw Error('target-missing');const active=m.players.find(p=>p.id===m.activeId);if(!active||!legalOpponents(active).some(p=>p.id===m.target.defenderId))throw Error('illegal-target');m.busy=true;m.phase='resolve';const defender=m.players.find(p=>p.id===m.target.defenderId);m.battlePresentation={stage:'focus',round:m.round,mode:m.mode,attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,players:[]};renderBattle();try{if(!await setBattlePresentation('focus',{attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,players:[]}))return;if(active.human)await setBattleRuntimeState('attack');const usedShield=defender.shields[m.target.shield];if(!usedShield)throw Error('shield-missing');let replenish=defender.plan.battleId;if(!defender.hand.includes(replenish)||CARDS[replenish]?.runtime_status!=='playable')replenish=await replacementBattleCard(defender,' …[LINE TRUNCATED]
001827: function checkWin(){if(state.match.mode==='2v2'){const teams=['A','B'].filter(t=>state.match.players.filter(p=>p.team===t).some(p=>Math.max(...Object.values(p.lanes).map(a=>a.length))>=7));return teams}return state.match.players.filter(p=>Math.max(...Object.values(p.lanes).map(a=>a.length))>=7).map(p=>p.id)}
001828: function nextRound(){const m=state.match;m.lastBattleResolution=null;m.round++;m.phase='plan';m.activeId=null;m.target=null;m.battlePresentation=null;m.busy=false;m.players.forEach(p=>p.plan=null);awardRoundStartHoney(m);initRoundRuntime(m);log(`第${m.round}巡。行動を予約します。`);renderBattle()}
001829: function currentPlacementRanks(m=state.match){const ps=m.players.map(p=>({p,depth:Math.max(...Object.values(p.lanes).map(a=>a.length))})).sort((a,b)=>b.depth-a.depth);let prev=null,rank=0;return ps.map((x,i)=>{if(prev===null||x.depth!==prev)rank=i+1;prev=x.depth;return{player:x.p,rank,depth:x.depth}})}
001830: function awardRoundStartHoney(m){for(const row of currentPlacementRanks(m)){const gain=Math.max(1,Number(row.rank)||1);row.player.honey=Math.max(0,Number(row.player.honey)||0)+gain;log(`${row.player.name}：${gain}位のためハニー+${gain}。`)}}
001831: function ranksFFA(){return currentPlacementRanks(state.match)}
001832: function endMatch(winners){const m=state.match;let ranking;if(m.mode==='2v2'){const wt=winners[0];ranking=m.players.map(p=>({player:p,rank:p.team===wt?1:2,depth:Math.max(...Object.values(p.lanes).map(a=>a.length))}))}else ranking=ranksFFA();const me=ranking.find(x=>x.player.human),points=settleBattlePoints(m,me.rank);m.lastResult={ranking,headline:me.rank===1?'勝利':'結果',grade:me.rank,points};state.history.unshift({at:new Date().toISOString(),mode:m.mode,rank:me.rank,rounds:m.round,character:state.playerCharacterId,partner:state.selectedPartnerId,battlePointsEarned:points.earned,quickPoints:points.quick,battlePointsAfter:points.after});save();show('result');renderResult()}

=== LINE PATTERN 'function endMatch' HITS=1 ===
--- line 1832 context 1829:1837 ---
001829: function currentPlacementRanks(m=state.match){const ps=m.players.map(p=>({p,depth:Math.max(...Object.values(p.lanes).map(a=>a.length))})).sort((a,b)=>b.depth-a.depth);let prev=null,rank=0;return ps.map((x,i)=>{if(prev===null||x.depth!==prev)rank=i+1;prev=x.depth;return{player:x.p,rank,depth:x.depth}})}
001830: function awardRoundStartHoney(m){for(const row of currentPlacementRanks(m)){const gain=Math.max(1,Number(row.rank)||1);row.player.honey=Math.max(0,Number(row.player.honey)||0)+gain;log(`${row.player.name}：${gain}位のためハニー+${gain}。`)}}
001831: function ranksFFA(){return currentPlacementRanks(state.match)}
001832: function endMatch(winners){const m=state.match;let ranking;if(m.mode==='2v2'){const wt=winners[0];ranking=m.players.map(p=>({player:p,rank:p.team===wt?1:2,depth:Math.max(...Object.values(p.lanes).map(a=>a.length))}))}else ranking=ranksFFA();const me=ranking.find(x=>x.player.human),points=settleBattlePoints(m,me.rank);m.lastResult={ranking,headline:me.rank===1?'勝利':'結果',grade:me.rank,points};state.history.unshift({at:new Date().toISOString(),mode:m.mode,rank:me.rank,rounds:m.round,character:state.playerCharacterId,partner:state.selectedPartnerId,battlePointsEarned:points.earned,quickPoints:points.quick,battlePointsAfter:points.after});save();show('result');renderResult()}
001833: async function renderResult(){const r=state.match?.lastResult;if(!r)return;const m=state.match,ordered=[...r.ranking].sort((a,b)=>a.rank-b.rank),me=ordered.find(x=>x.player.human),mode=({'2p':'二人','4p':'四人','2v2':'二対二'})[m.mode]||m.mode,content=m.contentId==='honey_hunt'?'ハニーハント':'ロード・シールド';const {projectResultRankPresentation}=await import('./result-presentation-core.mjs'),userPresentation=projectResultRankPresentation(me?.rank);if(!userPresentation?.ok)return;const gradeEl=$('#resultGrade'),headlineEl=$('#resultHeadline');gradeEl.textContent=userPresentation.visibleLabel;headlineEl.textContent=userPresentation.visibleLabel;gradeEl.dataset.formalRank=String(userPresentation.formalRank);headlineEl.dataset.formalRank=String(userPresentation.formalRank);if(userPresentation.rankColorRole){gradeEl.dataset.rankColorRole=userPresentation.rankColorRole;headlineEl.dataset.rankColorRole=userPresentation.rankColorRole}else{delete gradeEl.dataset.rankColorRole;delete headlineEl.dataset.rankColorR …[LINE TRUNCATED]
001834: $('#rematch').onclick=()=>startMatch();$('#leaveMatch').onclick=()=>{state.match=null;navigateRoot('home')};window.__GAMEROAD_QUICK_R14_QA__={version:'FULLGAME-QUICKTIMER-R14',constants:{windowMs:QUICK_WINDOW_MS,perfectRemainMs:QUICK_PERFECT_REMAIN_MS,goodRemainMs:QUICK_GOOD_REMAIN_MS,perfectPoints:QUICK_PERFECT_POINTS,goodPoints:QUICK_GOOD_POINTS,chestTarget:BATTLE_POINTS_CHEST_TARGET},snapshot(){const m=state.match,me=m?.players?.find(p=>p.human),rem=m?quickRemainingMs(m):null;return{screen:state.screen,round:m?.round||null,phase:m?.phase||null,remainingMs:rem,tier:m?quickTierForRemaining(rem):null,quickPoints:me?(m.quickPointsByPlayer?.[me.id]||0):0,awards:me?(m.quickAwards||[]).filter(a=>a.playerId===me.id).map(a=>({...a})):[],battlePoints:state.progression?.battlePoints||0,settlement:m?.battlePointsSettlement?safeClone(m.battlePointsSettlement):null,coilHidden:$('#quickCoil')?.hidden??null,coilText:$('#quickRemain')?.textContent||null,resultText:$('#resultBattlePoints')?.innerText …[LINE TRUNCATED]
001835: window.GAMEROAD_BOARD_REVIEW_QA={version:'BROWSER-BOARD-PLAYTEST-REVIEW-LOOP-001',_rect(e){const b=e?.getBoundingClientRect();return b?{l:b.left,r:b.right,t:b.top,b:b.bottom,w:b.width,h:b.height}:null},_hit(a,b){return!!(a&&b&&Math.min(a.r,b.r)>Math.max(a.l,b.l)&&Math.min(a.b,b.b)>Math.max(a.t,b.t))},snapshot(){const rect=this._rect.bind(this),hit=this._hit.bind(this),nodes=[...document.querySelectorAll('#board .node')].map(e=>({id:e.dataset.pos,rect:rect(e)})),surfaces={publicHud:rect($('#publicTurnHud')),battleInfo:rect(document.querySelector('.battleInfo')),rail:rect(document.querySelector('.battleRail')),top:rect(document.querySelector('.battleTopStatus'))},collisions={};for(const[k,r]of Object.entries(surfaces))collisions[k]=nodes.filter(n=>hit(n.rect,r)).map(n=>n.id);return{screen:state.screen,phase:state.match?.phase||null,viewport:{w:innerWidth,h:innerHeight},profile:fieldCameraState?.kind||null,nodeCount:nodes.length,map:rect($('#battleMap')),collisions,criticalOverlapCount:co …[LINE TRUNCATED]
001836: window.GAMEROAD_VISUAL_SHELL_QA={version:'BROWSER-PLAYTEST-VISUAL-SHELL-REVIEW-001',rect(sel){const e=document.querySelector(sel),r=e?.getBoundingClientRect();return r?{l:r.left,t:r.top,r:r.right,b:r.bottom,w:r.width,h:r.height}:null},inside(sel){const r=this.rect(sel);return!!(r&&r.l>=0&&r.t>=0&&r.r<=innerWidth&&r.b<=innerHeight)},snapshot(){return{screen:state.screen,viewport:{w:innerWidth,h:innerHeight},contextVisible:$('#contextHelp')?.classList.contains('on')||false,home:{primary:this.rect('.codexBattleCta'),character:null,live:this.rect('.codexHomeCenterStage')},setup:{box:this.rect('.setupBox'),start:this.rect('#startMatch'),character:this.rect('#setupRuntime'),goal:this.rect('.setupGoalCard')},result:{rank:this.rect('.resultRank'),body:this.rect('.resultBody'),buttons:this.rect('.resultBtns'),character:this.rect('.resultWorld .grtc-image')}}}};
001837: function positionLabel(id){if(id.startsWith('C:'))return '中央';if(id.startsWith('S:'))return 'シールド境界';if(id.startsWith('R:')){const r=parseRoad(id);return `${r.owner} ${r.lane==='L'?'左':r.lane==='C'?'中央':'右'}列 ${r.depth}`;}return '盤面';}function renderPlayers(){const m=state.match,root=$('#players');root.innerHTML='';m.players.forEach(p=>{const d=document.createElement('div');d.className='player'+(p.human?' you':'')+(p.id===m.activeId?' active':'');d.innerHTML=`<b>${p.name}</b> ${m.mode==='2v2'?`<span>組${p.team}</span>`:''}<div>手札 ${p.hand.length} / 山札 ${p.deck.length} / チップ ${p.chip.length}</div><div class="royalUsed">ロイヤル使用 ${royalUsedLabel(p)}</div><div>位置 ${positionLabel(p.position)}${p.battleBlockedRound===m.round?' / バトルカード提出禁止':''}${p.nextCostReduction?` / 次費用-${p.nextCostReduction}`:''}</div><div class="laneMini"><i>左 ${p.lanes.L.length}</i><i>中 ${p.lanes.C.length}</i><i>右 ${p.lanes.R.length}</i></div>`;root.appendChild(d)})}

=== LINE PATTERN 'function startMatch' HITS=1 ===
--- line 1588 context 1585:1593 ---
001585: const quickTicker=setInterval(()=>{if(state.screen==='battle')renderQuickTimer()},100);window.addEventListener('pagehide',()=>clearInterval(quickTicker),{once:true});
001586: function initRoundRuntime(m){m.participants=new Map(m.players.map(p=>[p.id,[]]));m.participantSeq=0;m.resolvedAbilityKeys=new Set();m.abilityOrderReceipts=new Map();m.testAutoChoices=false;quickStartRound(m)}
001587: 
001588: function startMatch(){const eligibility=deckEligibility(state.savedDeck);if(!eligibility.usable){toast(deckEligibilityMessage(eligibility));show('setup');renderSetupDeckStatus();return null}audioResetForNewMatch();try{if(battleMount&&window.GameRoadThreeCharRuntime?.unmount)window.GameRoadThreeCharRuntime.unmount(battleMount)}catch(e){}battleMount=null;battleMountPromise=null;const battleRuntimeEl=$('#battleRuntime');if(battleRuntimeEl)battleRuntimeEl.replaceChildren();const snapshotFactory=globalThis.GAMEROAD_CREATE_DECK_MATCH_START_SNAPSHOT;let snapshot;try{if(typeof snapshotFactory!=='function')throw new Error('MATCH_START_SNAPSHOT_RUNTIME_UNAVAILABLE');snapshot=snapshotFactory({savedDeck:state.savedDeck,savedDeckRule:state.savedDeckRule,setupMode:state.setupMode,setupContent:state.setupContent,playerCharacterId:state.playerCharacterId,selectedPartnerId:state.selectedPartnerId},{validateDeck})}catch(e){const message=String(e?.message||e),prefix='MATCH_START_DECK_INVALID:';toast(mess …[LINE TRUNCATED]
001589: $('#startMatch').addEventListener('click',startMatch);
001590: function log(t){if(!state.match)return;state.match.log.unshift(t);state.match.log=state.match.log.slice(0,30)}
001591: function refill(p){while(p.hand.length<3&&p.deck.length)p.hand.push(p.deck.shift())}
001592: function laneDepth(p,l){return p.lanes[l].length}
001593: function posType(id){if(id.startsWith('C:'))return'center';if(id.startsWith('S:'))return'shield';if(id.startsWith('R:'))return'road';return'corner'}

=== LINE PATTERN 'GOAL_REACHED' HITS=0 ===

=== LINE PATTERN 'new-base-legacy-seven-win-gate-core.mjs' HITS=0 ===

=== LINE PATTERN 'battle-new-base-live-consumer-adapter.mjs' HITS=0 ===

=== LINE PATTERN 'battle-goal-arrival-live-integration.mjs' HITS=0 ===

=== LINE PATTERN 'type="module"' HITS=9 ===
--- line 1294 context 1291:1299 ---
001291: </script>
001292: <script src="./deck-save-recovery-core.mjs"></script>
001293: <!-- GAMEROAD_LOBBYDECK_R4_MATCHSTART_SNAPSHOT -->
001294: <script type="module" id="gameroad-lobbydeck-r4-matchstart-snapshot">
001295: import { createDeckMatchStartSnapshot } from "./deck-save-ack-core.mjs";
001296: const existingDeckMatchStartSnapshot=globalThis.GAMEROAD_CREATE_DECK_MATCH_START_SNAPSHOT;
001297: if(existingDeckMatchStartSnapshot&&existingDeckMatchStartSnapshot!==createDeckMatchStartSnapshot)throw new Error("GAMEROAD_DECK_MATCH_START_SNAPSHOT_CONFLICT");
001298: if(!existingDeckMatchStartSnapshot)Object.defineProperty(globalThis,"GAMEROAD_CREATE_DECK_MATCH_START_SNAPSHOT",{value:createDeckMatchStartSnapshot,enumerable:false,configurable:false,writable:false});
001299: </script>
--- line 1301 context 1298:1306 ---
001298: if(!existingDeckMatchStartSnapshot)Object.defineProperty(globalThis,"GAMEROAD_CREATE_DECK_MATCH_START_SNAPSHOT",{value:createDeckMatchStartSnapshot,enumerable:false,configurable:false,writable:false});
001299: </script>
001300: <script src="./board-facility-state-core.classic.js"></script>
001301: <script type="module">
001302: import { mountBoardFacilityRuntime } from "./board-facility-runtime-mount.mjs";
001303: await mountBoardFacilityRuntime(globalThis);
001304: </script>
001305: <script type="module">
001306: import { resolveScreenNavigation, createScreenNavigationRuntimeBridge, createScreenTransitionRuntimeAdapter } from "./screen-navigation-core.mjs";
--- line 1305 context 1302:1310 ---
001302: import { mountBoardFacilityRuntime } from "./board-facility-runtime-mount.mjs";
001303: await mountBoardFacilityRuntime(globalThis);
001304: </script>
001305: <script type="module">
001306: import { resolveScreenNavigation, createScreenNavigationRuntimeBridge, createScreenTransitionRuntimeAdapter } from "./screen-navigation-core.mjs";
001307: import { createOrientationProjectionAdapter, ORIENTATION_PROJECTIONS } from "./ui-state-feedback-core.mjs";
001308: const runtimeScreenNavigationBridge=createScreenNavigationRuntimeBridge();
001309: const existingScreenNavigationBridge=globalThis.GAMEROAD_SCREEN_NAVIGATION;
001310: if(existingScreenNavigationBridge && (existingScreenNavigationBridge.resolve!==resolveScreenNavigation || existingScreenNavigationBridge.resolveBackTarget!==runtimeScreenNavigationBridge.resolveBackTarget))throw new Error("GAMEROAD_SCREEN_NAVIGATION is already occupied by an incompatible bridge");
--- line 3824 context 3821:3829 ---
003821: </script>
003822: 
003823: 
003824: <script type="module" id="gameroad-ui-feedback-ready-plan-production-r9">
003825: import { createReadyPlanFeedbackAdapter, bindReadyPlanFeedbackControl } from "./ui-state-feedback-ready-plan-adapter.mjs";
003826: 
003827: const target = document.getElementById("readyPlan");
003828: const commit = globalThis.GAMEROAD_READY_PLAN_COMMIT;
003829: if (!target) throw new Error("UI_FEEDBACK_READY_PLAN_TARGET_MISSING");
--- line 3895 context 3892:3900 ---
003892: .screen.result .resultBody{bottom:120px!important}
003893: }
003894: </style>
003895: <script id="gameroad-post-match-autoqueue-r5" type="module">
003896: (function gameroadPostMatchAutoqueueR5() {
003897:   'use strict';
003898:   (async () => {
003899:     const {
003900:       POST_MATCH_AUTOQUEUE_STATUS: STATUS,
--- line 4146 context 4143:4151 ---
004143: })();
004144: </script>
004145: <!-- GAMEROAD_UI_R45_HOMECARDS_2P5D -->
004146: <script type="module" src="./home-cards-2p5d-presentation.mjs"></script>
004147: 
004148: <!-- GAMEROAD PARTNER ADVICE OBJECT PROJECTION R21B / recovered by BOARD109 R14 -->
004149: <style id="gameroad-partner-advice-object-projection-r21b-style">
004150: [data-partner-advice-role="partner-recommendation"]{position:relative!important;outline:3px dashed rgba(255,238,157,.96)!important;outline-offset:3px;box-shadow:0 0 0 1px rgba(8,18,15,.92),0 0 18px rgba(255,229,132,.42)!important;z-index:18!important}
004151: [data-partner-advice-role="partner-recommendation"]::after{content:attr(data-partner-advice-glyph);position:absolute;left:50%;bottom:calc(100% + 5px);transform:translateX(-50%);min-width:max-content;padding:3px 6px;border:1px dashed rgba(255,242,180,.95);border-radius:999px;background:rgba(5,20,17,.96);color:#fff4bb;font:900 8px/1.15 system-ui,sans-serif;white-space:nowrap;pointer-events:none;box-shadow:0 3px 12px rgba(0,0,0,.48)}
--- line 4179 context 4176:4184 ---
004176: window.__GAMEROAD_PARTNER_ADVICE_BOARD_R21B__=Object.freeze({render,clear,snapshot});document.readyState==='loading'?document.addEventListener('DOMContentLoaded',install,{once:true}):install();
004177: })();
004178: </script>
004179: <script type="module" id="gameroad-r62-home-illustration-runtime">
004180: import { createHomeThemeProfile, createHomePresentationState, resolveHomeProjection } from './home-theme-orientation-core.mjs';
004181: 
004182: (() => {
004183:   const root = document.querySelector('.codexHome');
004184:   if (!root || root.dataset.homeOrientationRuntime === 'r62r') return;
--- line 4312 context 4309:4317 ---
004309: html body:has(.battle.active) .top{display:flex!important}
004310: #boardPlayers .boardPlayerToken.human [data-board-controlled-character]{display:none!important}
004311: </style>
004312: <script type="module" id="gameroad-controlled-character-4p-live-r8">
004313: import {
004314:   createFourParticipantControlledCharacterMotionState,
004315:   applyFourParticipantControlledCharacterEvent,
004316:   projectFourParticipantControlledCharacterMotion,
004317: } from './battle-controlled-character-4p-motion-director.mjs';
--- line 4400 context 4397:4401 ---
004397: globalThis.__GAMEROAD_CONTROLLED_CHARACTER_4P_SYNC__ = syncVisuals;
004398: queueMicrotask(syncVisuals);
004399: </script>
004400: <script type="module" src="./profile-presentation-runtime-mount.mjs"></script>
004401: </body></html>

=== LINE PATTERN "type='module'" HITS=0 ===

=== CHAR PATTERN 'function checkWin(){' HITS_SHOWN=1 ===
--- char 10118388 context 10116588:10125388 ---
players.filter(p=>p.team==='A').reduce((n,p)=>n+scores.get(p.id),0),B:m.players.filter(p=>p.team==='B').reduce((n,p)=>n+scores.get(p.id),0)}:null;m.lastBattleResolution={serial:++m.resolutionSeq,round:m.round,mode:m.mode,attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,winnerIds:[...winningPlayerIds],winningTeam,teamTotals,players:resolutionPlayers,laneGains:m.players.map(p=>({id:p.id,lane:m.target.lane,before:laneBefore.get(p.id),after:p.lanes[m.target.lane].length,added:p.lanes[m.target.lane].length-laneBefore.get(p.id)})).filter(x=>x.added>0),maxLaneProgress:m.players.map(p=>({id:p.id,before:maxLaneBefore.get(p.id),after:Math.max(...Object.values(p.lanes).map(a=>a.length))}))};grBattleReplayAcceptResolution(m,m.lastBattleResolution);const slaykiaAttackEnd=grSlaykiaAttackEndHook(m);m.participants=new Map(m.players.map(p=>[p.id,[]]));m.players.forEach(refill);m.battlePresentation={stage:'settle',round:m.round,mode:m.mode,attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,players:resolutionPlayers,winnerIds:[...winningPlayerIds],teamTotals};renderBattle();await battlePresentationWait(BATTLE_PRESENTATION_RESULT_MS,true);if(state.match!==m||state.screen!=='battle')return;const winners=slaykiaAttackEnd?.specialWinnerTokens?.length?[...slaykiaAttackEnd.specialWinnerTokens]:checkWin();m.battlePresentation=null;if(winners.length){m.busy=false;if(slaykiaAttackEnd?.specialWinnerTokens?.length)log('スレイキア：成功破壊16枚で特殊勝利。');else if(m.contentId==='honey_hunt')log('一列へ実札が七枚以上連続配置され、ハニーハントの勝利条件が成立しました。');grBattleReplayEnd(m,winners);return endMatch(winners)}nextRound()}catch(e){clearBattlePresentationWait();if(state.match===m){m.battlePresentation=null;m.busy=false;log(`戦闘停止：${e.message}`);renderBattle()}throw e}}
function checkWin(){if(state.match.mode==='2v2'){const teams=['A','B'].filter(t=>state.match.players.filter(p=>p.team===t).some(p=>Math.max(...Object.values(p.lanes).map(a=>a.length))>=7));return teams}return state.match.players.filter(p=>Math.max(...Object.values(p.lanes).map(a=>a.length))>=7).map(p=>p.id)}
function nextRound(){const m=state.match;m.lastBattleResolution=null;m.round++;m.phase='plan';m.activeId=null;m.target=null;m.battlePresentation=null;m.busy=false;m.players.forEach(p=>p.plan=null);awardRoundStartHoney(m);initRoundRuntime(m);log(`第${m.round}巡。行動を予約します。`);renderBattle()}
function currentPlacementRanks(m=state.match){const ps=m.players.map(p=>({p,depth:Math.max(...Object.values(p.lanes).map(a=>a.length))})).sort((a,b)=>b.depth-a.depth);let prev=null,rank=0;return ps.map((x,i)=>{if(prev===null||x.depth!==prev)rank=i+1;prev=x.depth;return{player:x.p,rank,depth:x.depth}})}
function awardRoundStartHoney(m){for(const row of currentPlacementRanks(m)){const gain=Math.max(1,Number(row.rank)||1);row.player.honey=Math.max(0,Number(row.player.honey)||0)+gain;log(`${row.player.name}：${gain}位のためハニー+${gain}。`)}}
function ranksFFA(){return currentPlacementRanks(state.match)}
function endMatch(winners){const m=state.match;let ranking;if(m.mode==='2v2'){const wt=winners[0];ranking=m.players.map(p=>({player:p,rank:p.team===wt?1:2,depth:Math.max(...Object.values(p.lanes).map(a=>a.length))}))}else ranking=ranksFFA();const me=ranking.find(x=>x.player.human),points=settleBattlePoints(m,me.rank);m.lastResult={ranking,headline:me.rank===1?'勝利':'結果',grade:me.rank,points};state.history.unshift({at:new Date().toISOString(),mode:m.mode,rank:me.rank,rounds:m.round,character:state.playerCharacterId,partner:state.selectedPartnerId,battlePointsEarned:points.earned,quickPoints:points.quick,battlePointsAfter:points.after});save();show('result');renderResult()}
async function renderResult(){const r=state.match?.lastResult;if(!r)return;const m=state.match,ordered=[...r.ranking].sort((a,b)=>a.rank-b.rank),me=ordered.find(x=>x.player.human),mode=({'2p':'二人','4p':'四人','2v2':'二対二'})[m.mode]||m.mode,content=m.contentId==='honey_hunt'?'ハニーハント':'ロード・シールド';const {projectResultRankPresentation}=await import('./result-presentation-core.mjs'),userPresentation=projectResultRankPresentation(me?.rank);if(!userPresentation?.ok)return;const gradeEl=$('#resultGrade'),headlineEl=$('#resultHeadline');gradeEl.textContent=userPresentation.visibleLabel;headlineEl.textContent=userPresentation.visibleLabel;gradeEl.dataset.formalRank=String(userPresentation.formalRank);headlineEl.dataset.formalRank=String(userPresentation.formalRank);if(userPresentation.rankColorRole){gradeEl.dataset.rankColorRole=userPresentation.rankColorRole;headlineEl.dataset.rankColorRole=userPresentation.rankColorRole}else{delete gradeEl.dataset.rankColorRole;delete headlineEl.dataset.rankColorRole}$('#resultSummary').textContent=`${m.round}巡で決着。 ${content}`;$('#resultLeadText')&&($('#resultLeadText').textContent=r.grade===1?'一位で決着':'最終順位');$('#resultLeadSub')&&($('#resultLeadSub').textContent=`${content} / ${mode}`);$('#resultMode')&&($('#resultMode').textContent=mode);$('#resultRounds')&&($('#resultRounds').textContent=`${m.round}巡`);$('#resultLane')&&($('#resultLane').textContent=`${me?.depth??0}枚`);renderResultBattlePoints(r);const box=$('#resultRanking');box.innerHTML='';ordered.forEach(x=>{const presentation=projectResultRankPresentation(x.rank);if(!presentation?.ok)return;const d=document.createElement('div');d.className='rankLine';d.dataset.formalRank=String(presentation.formalRank);if(presentation.rankColorRole)d.dataset.rankColorRole=presentation.rankColorRole;d.innerHTML=`<b><span class="resultOutcome">${presentation.visibleLabel}</span> ${x.player.human?'あなた':x.player.name}</b><span>最長列 ${x.depth}枚</span>`;box.appendChild(d)});await grBattleReplayRenderResult(m);resultMount=await mountChar('.resultWorld',state.playerCharacterId,'joy')}
$('#rematch').onclick=()=>startMatch();$('#leaveMatch').onclick=()=>{state.match=null;navigateRoot('home')};window.__GAMEROAD_QUICK_R14_QA__={version:'FULLGAME-QUICKTIMER-R14',constants:{windowMs:QUICK_WINDOW_MS,perfectRemainMs:QUICK_PERFECT_REMAIN_MS,goodRemainMs:QUICK_GOOD_REMAIN_MS,perfectPoints:QUICK_PERFECT_POINTS,goodPoints:QUICK_GOOD_POINTS,chestTarget:BATTLE_POINTS_CHEST_TARGET},snapshot(){const m=state.match,me=m?.players?.find(p=>p.human),rem=m?quickRemainingMs(m):null;return{screen:state.screen,round:m?.round||null,phase:m?.phase||null,remainingMs:rem,tier:m?quickTierForRemaining(rem):null,quickPoints:me?(m.quickPointsByPlayer?.[me.id]||0):0,awards:me?(m.quickAwards||[]).filter(a=>a.playerId===me.id).map(a=>({...a})):[],battlePoints:state.progression?.battlePoints||0,settlement:m?.battlePointsSettlement?safeClone(m.battlePointsSettlement):null,coilHidden:$('#quickCoil')?.hidden??null,coilText:$('#quickRemain')?.textContent||null,resultText:$('#resultBattlePoints')?.innerText||''}},setRemainingSeconds(sec){const m=state.match;if(!m?.quickWindow)return false;const v=Math.max(0,Math.min(30,Number(sec)||0));m._quickQaNowMs=m.quickWindow.deadlineAt-v*1000;renderQuickTimer();return true},clearTestClock(){const m=state.match;if(!m)return false;delete m._quickQaNowMs;renderQuickTimer();return true},setBattlePoints(n){state.progression={battlePoints:Math.max(0,Math.floor(Number(n)||0))};return state.progression.battlePoints},persistenceProbe(n){state.progression={battlePoints:Math.max(0,Math.floor(Number(n)||0))};save();state.progression={battlePoints:0};load();return{battlePoints:state.progression.battlePoints,storage:state.storage}}};window.GAMEROAD_3D_WALK_QA={version:'BROWSER-3D-BOARD-WALK-INTEGRATION-001',snapshot(){const m=state.match,me=m?.players?.find(p=>p.human);return{screen:state.screen,phase:m?.phase||null,position:me?.position||null,path:me?.plan?.path?[...me.plan.path]:null,avatarState:battleMount?.root?.dataset?.state||null,avatarFacing:battleMount?.facingName||null,nodeCount:document.querySelectorAll('#board .node').length,canvas:{w:$('#fieldCanvas')?.width||0,h:$('#fieldCanvas')?.height||0}}}};window.GAMEROAD_GRASSLAND3D_QA={version:'BROWSER-BOARD-GRASSLAND-3D-R4',snapshot(){const gl=$('#fieldCanvas')?.getContext('webgl');return{...fieldLastStats,webgl:!!gl,contextLost:!!gl?.isContextLost?.(),projectionCount:fieldProjection.size,nodeCount:document.querySelectorAll('#board .node').length,screen:state.screen}}};
window.GAMEROAD_BOARD_REVIEW_QA={version:'BROWSER-BOARD-PLAYTEST-REVIEW-LOOP-001',_rect(e){const b=e?.getBoundingClientRect();return b?{l:b.left,r:b.right,t:b.top,b:b.bottom,w:b.width,h:b.height}:null},_hit(a,b){return!!(a&&b&&Math.min(a.r,b.r)>Math.max(a.l,b.l)&&Math.min(a.b,b.b)>Math.max(a.t,b.t))},snapshot(){const rect=this._rect.bind(this),hit=this._hit.bind(this),nodes=[...document.querySelectorAll('#board .node')].map(e=>({id:e.dataset.pos,rect:rect(e)})),surfaces={publicHud:rect($('#publicTurnHud')),battleInfo:rect(document.querySelector('.battleInfo')),rail:rec

=== CHAR PATTERN 'checkWin()' HITS_SHOWN=2 ===
--- char 10117950 context 10116150:10124950 ---
es).map(a=>a.length))]));m.players.forEach(p=>{const parts=m.participants.get(p.id)||[];if(winningPlayerIds.includes(p.id)){const lane=p.lanes[m.target.lane];parts.forEach(e=>lane.push(e.cardId));if(p.chip.length)p.chip.splice(0).forEach(id=>lane.push(id))}else parts.forEach(e=>p.chip.push(e.cardId))});const winningTeam=m.mode==='2v2'?(m.players.find(p=>winningPlayerIds.includes(p.id))?.team||null):null,teamTotals=m.mode==='2v2'?{A:m.players.filter(p=>p.team==='A').reduce((n,p)=>n+scores.get(p.id),0),B:m.players.filter(p=>p.team==='B').reduce((n,p)=>n+scores.get(p.id),0)}:null;m.lastBattleResolution={serial:++m.resolutionSeq,round:m.round,mode:m.mode,attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,winnerIds:[...winningPlayerIds],winningTeam,teamTotals,players:resolutionPlayers,laneGains:m.players.map(p=>({id:p.id,lane:m.target.lane,before:laneBefore.get(p.id),after:p.lanes[m.target.lane].length,added:p.lanes[m.target.lane].length-laneBefore.get(p.id)})).filter(x=>x.added>0),maxLaneProgress:m.players.map(p=>({id:p.id,before:maxLaneBefore.get(p.id),after:Math.max(...Object.values(p.lanes).map(a=>a.length))}))};grBattleReplayAcceptResolution(m,m.lastBattleResolution);const slaykiaAttackEnd=grSlaykiaAttackEndHook(m);m.participants=new Map(m.players.map(p=>[p.id,[]]));m.players.forEach(refill);m.battlePresentation={stage:'settle',round:m.round,mode:m.mode,attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,players:resolutionPlayers,winnerIds:[...winningPlayerIds],teamTotals};renderBattle();await battlePresentationWait(BATTLE_PRESENTATION_RESULT_MS,true);if(state.match!==m||state.screen!=='battle')return;const winners=slaykiaAttackEnd?.specialWinnerTokens?.length?[...slaykiaAttackEnd.specialWinnerTokens]:checkWin();m.battlePresentation=null;if(winners.length){m.busy=false;if(slaykiaAttackEnd?.specialWinnerTokens?.length)log('スレイキア：成功破壊16枚で特殊勝利。');else if(m.contentId==='honey_hunt')log('一列へ実札が七枚以上連続配置され、ハニーハントの勝利条件が成立しました。');grBattleReplayEnd(m,winners);return endMatch(winners)}nextRound()}catch(e){clearBattlePresentationWait();if(state.match===m){m.battlePresentation=null;m.busy=false;log(`戦闘停止：${e.message}`);renderBattle()}throw e}}
function checkWin(){if(state.match.mode==='2v2'){const teams=['A','B'].filter(t=>state.match.players.filter(p=>p.team===t).some(p=>Math.max(...Object.values(p.lanes).map(a=>a.length))>=7));return teams}return state.match.players.filter(p=>Math.max(...Object.values(p.lanes).map(a=>a.length))>=7).map(p=>p.id)}
function nextRound(){const m=state.match;m.lastBattleResolution=null;m.round++;m.phase='plan';m.activeId=null;m.target=null;m.battlePresentation=null;m.busy=false;m.players.forEach(p=>p.plan=null);awardRoundStartHoney(m);initRoundRuntime(m);log(`第${m.round}巡。行動を予約します。`);renderBattle()}
function currentPlacementRanks(m=state.match){const ps=m.players.map(p=>({p,depth:Math.max(...Object.values(p.lanes).map(a=>a.length))})).sort((a,b)=>b.depth-a.depth);let prev=null,rank=0;return ps.map((x,i)=>{if(prev===null||x.depth!==prev)rank=i+1;prev=x.depth;return{player:x.p,rank,depth:x.depth}})}
function awardRoundStartHoney(m){for(const row of currentPlacementRanks(m)){const gain=Math.max(1,Number(row.rank)||1);row.player.honey=Math.max(0,Number(row.player.honey)||0)+gain;log(`${row.player.name}：${gain}位のためハニー+${gain}。`)}}
function ranksFFA(){return currentPlacementRanks(state.match)}
function endMatch(winners){const m=state.match;let ranking;if(m.mode==='2v2'){const wt=winners[0];ranking=m.players.map(p=>({player:p,rank:p.team===wt?1:2,depth:Math.max(...Object.values(p.lanes).map(a=>a.length))}))}else ranking=ranksFFA();const me=ranking.find(x=>x.player.human),points=settleBattlePoints(m,me.rank);m.lastResult={ranking,headline:me.rank===1?'勝利':'結果',grade:me.rank,points};state.history.unshift({at:new Date().toISOString(),mode:m.mode,rank:me.rank,rounds:m.round,character:state.playerCharacterId,partner:state.selectedPartnerId,battlePointsEarned:points.earned,quickPoints:points.quick,battlePointsAfter:points.after});save();show('result');renderResult()}
async function renderResult(){const r=state.match?.lastResult;if(!r)return;const m=state.match,ordered=[...r.ranking].sort((a,b)=>a.rank-b.rank),me=ordered.find(x=>x.player.human),mode=({'2p':'二人','4p':'四人','2v2':'二対二'})[m.mode]||m.mode,content=m.contentId==='honey_hunt'?'ハニーハント':'ロード・シールド';const {projectResultRankPresentation}=await import('./result-presentation-core.mjs'),userPresentation=projectResultRankPresentation(me?.rank);if(!userPresentation?.ok)return;const gradeEl=$('#resultGrade'),headlineEl=$('#resultHeadline');gradeEl.textContent=userPresentation.visibleLabel;headlineEl.textContent=userPresentation.visibleLabel;gradeEl.dataset.formalRank=String(userPresentation.formalRank);headlineEl.dataset.formalRank=String(userPresentation.formalRank);if(userPresentation.rankColorRole){gradeEl.dataset.rankColorRole=userPresentation.rankColorRole;headlineEl.dataset.rankColorRole=userPresentation.rankColorRole}else{delete gradeEl.dataset.rankColorRole;delete headlineEl.dataset.rankColorRole}$('#resultSummary').textContent=`${m.round}巡で決着。 ${content}`;$('#resultLeadText')&&($('#resultLeadText').textContent=r.grade===1?'一位で決着':'最終順位');$('#resultLeadSub')&&($('#resultLeadSub').textContent=`${content} / ${mode}`);$('#resultMode')&&($('#resultMode').textContent=mode);$('#resultRounds')&&($('#resultRounds').textContent=`${m.round}巡`);$('#resultLane')&&($('#resultLane').textContent=`${me?.depth??0}枚`);renderResultBattlePoints(r);const box=$('#resultRanking');box.innerHTML='';ordered.forEach(x=>{const presentation=projectResultRankPresentation(x.rank);if(!presentation?.ok)return;const d=document.createElement('div');d.className='rankLine';d.dataset.formalRank=String(presentation.formalRank);if(presentation.rankColorRole)d.dataset.rankColorRole=presentation.rankColorRole;d.innerHTML=`<b><span class="resultOutcome">${presentation.visibleLabel}</span> ${x.player.human?'あなた':x.player.name}</b><span>最長列 ${x.depth}枚</span>`;box.appendChild(d)});await grBattleReplayRenderResult(m);resultMount=await mountChar('.resultWorld',state.playerCharacterId,'joy')}
$('#rematch').onclick=()=>startMatch();$('#leaveMatch').onclick=()=>{state.match=null;navigateRoot('home')};window.__GAMEROAD_QUICK_R14_QA__={version:'FULLGAME-QUICKTIMER-R14',constants:{windowMs:QUICK_WINDOW_MS,perfectRemainMs:QUICK_PERFECT_REMAIN_MS,goodRemainMs:QUICK_GOOD_REMAIN_MS,perfectPoints:QUICK_PERFECT_POINTS,goodPoints:QUICK_GOOD_POINTS,chestTarget:BATTLE_POINTS_CHEST_TARGET},snapshot(){const m=state.match,me=m?.players?.find(p=>p.human),rem=m?quickRemainingMs(m):null;return{screen:state.screen,round:m?.round||null,phase:m?.phase||null,remainingMs:rem,tier:m?quickTierForRemaining(rem):null,quickPoints:me?(m.quickPointsByPlayer?.[me.id]||0):0,awards:me?(m.quickAwards||[]).filter(a=>a.playerId===me.id).map(a=>({...a})):[],battlePoints:state.progression?.battlePoints||0,settlement:m?.battlePointsSettlement?safeClone(m.battlePointsSettlement):null,coilHidden:$('#quickCoil')?.hidden??null,coilText:$('#quickRemain')?.textContent||null,resultText:$('#resultBattlePoints')?.innerText||''}},setRemainingSeconds(sec){const m=state.match;if(!m?.quickWindow)return false;const v=Math.max(0,Math.min(30,Number(sec)||0));m._quickQaNowMs=m.quickWindow.deadlineAt-v*1000;renderQuickTimer();return true},clearTestClock(){const m=state.match;if(!m)return false;delete m._quickQaNowMs;renderQuickTimer();return true},setBattlePoints(n){state.progression={battlePoints:Math.max(0,Math.floor(Number(n)||0))};return state.progression.battlePoints},persistenceProbe(n){state.progression={battlePoints:Math.max(0,Math.floor(Number(n)||0))};save();state.progression={battlePoints:0};load();return{battlePoints:state.progression.battlePoints,storage:state.storage}}};window.GAMEROAD_3D_WALK_QA={version:'BROWSER-3D-BOARD-WALK-INTEGRATION-001',snapshot(){const m=state.match,me=m?.players?.find(p=>p.human);return{screen:state.screen,phase:m?.phase||null,position:me?.position||null,path:me?.plan?.path?[...me.plan.path]:null,avatarState:battleMount?.root?.dataset?.state||null,avatarFacing:battleMount?.facingName||null,nodeCount:document.querySelectorAll('#board .node').length,canvas:{w:$('#fieldCanvas')?.width||0,h:$('#fieldCanvas')?.height||0}}}};window.GAMEROAD_GRASSLAND3D_QA={version:'BROWSER-BOARD-GRASSLAND-3D-R4',snapshot(){const gl=$('#fieldCanvas')?.getContext('webgl');return{...fieldLastStats,webgl:!!gl,contextLost:!!gl?.isContextLost?.(),projectionCount:fieldProjection.size,nodeCount:document.querySelectorAll('#board .node').length,screen:state.screen}}};
window.GAMEROAD_BOARD_REVIEW_QA={version:'BROWSER-BOARD-PLAYTEST-REVIEW-LOOP-001',_rect(e){const b=e?.getBoundingClientRect();return b?{l
--- char 10118397 context 10116597:10125397 ---
ilter(p=>p.team==='A').reduce((n,p)=>n+scores.get(p.id),0),B:m.players.filter(p=>p.team==='B').reduce((n,p)=>n+scores.get(p.id),0)}:null;m.lastBattleResolution={serial:++m.resolutionSeq,round:m.round,mode:m.mode,attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,winnerIds:[...winningPlayerIds],winningTeam,teamTotals,players:resolutionPlayers,laneGains:m.players.map(p=>({id:p.id,lane:m.target.lane,before:laneBefore.get(p.id),after:p.lanes[m.target.lane].length,added:p.lanes[m.target.lane].length-laneBefore.get(p.id)})).filter(x=>x.added>0),maxLaneProgress:m.players.map(p=>({id:p.id,before:maxLaneBefore.get(p.id),after:Math.max(...Object.values(p.lanes).map(a=>a.length))}))};grBattleReplayAcceptResolution(m,m.lastBattleResolution);const slaykiaAttackEnd=grSlaykiaAttackEndHook(m);m.participants=new Map(m.players.map(p=>[p.id,[]]));m.players.forEach(refill);m.battlePresentation={stage:'settle',round:m.round,mode:m.mode,attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,players:resolutionPlayers,winnerIds:[...winningPlayerIds],teamTotals};renderBattle();await battlePresentationWait(BATTLE_PRESENTATION_RESULT_MS,true);if(state.match!==m||state.screen!=='battle')return;const winners=slaykiaAttackEnd?.specialWinnerTokens?.length?[...slaykiaAttackEnd.specialWinnerTokens]:checkWin();m.battlePresentation=null;if(winners.length){m.busy=false;if(slaykiaAttackEnd?.specialWinnerTokens?.length)log('スレイキア：成功破壊16枚で特殊勝利。');else if(m.contentId==='honey_hunt')log('一列へ実札が七枚以上連続配置され、ハニーハントの勝利条件が成立しました。');grBattleReplayEnd(m,winners);return endMatch(winners)}nextRound()}catch(e){clearBattlePresentationWait();if(state.match===m){m.battlePresentation=null;m.busy=false;log(`戦闘停止：${e.message}`);renderBattle()}throw e}}
function checkWin(){if(state.match.mode==='2v2'){const teams=['A','B'].filter(t=>state.match.players.filter(p=>p.team===t).some(p=>Math.max(...Object.values(p.lanes).map(a=>a.length))>=7));return teams}return state.match.players.filter(p=>Math.max(...Object.values(p.lanes).map(a=>a.length))>=7).map(p=>p.id)}
function nextRound(){const m=state.match;m.lastBattleResolution=null;m.round++;m.phase='plan';m.activeId=null;m.target=null;m.battlePresentation=null;m.busy=false;m.players.forEach(p=>p.plan=null);awardRoundStartHoney(m);initRoundRuntime(m);log(`第${m.round}巡。行動を予約します。`);renderBattle()}
function currentPlacementRanks(m=state.match){const ps=m.players.map(p=>({p,depth:Math.max(...Object.values(p.lanes).map(a=>a.length))})).sort((a,b)=>b.depth-a.depth);let prev=null,rank=0;return ps.map((x,i)=>{if(prev===null||x.depth!==prev)rank=i+1;prev=x.depth;return{player:x.p,rank,depth:x.depth}})}
function awardRoundStartHoney(m){for(const row of currentPlacementRanks(m)){const gain=Math.max(1,Number(row.rank)||1);row.player.honey=Math.max(0,Number(row.player.honey)||0)+gain;log(`${row.player.name}：${gain}位のためハニー+${gain}。`)}}
function ranksFFA(){return currentPlacementRanks(state.match)}
function endMatch(winners){const m=state.match;let ranking;if(m.mode==='2v2'){const wt=winners[0];ranking=m.players.map(p=>({player:p,rank:p.team===wt?1:2,depth:Math.max(...Object.values(p.lanes).map(a=>a.length))}))}else ranking=ranksFFA();const me=ranking.find(x=>x.player.human),points=settleBattlePoints(m,me.rank);m.lastResult={ranking,headline:me.rank===1?'勝利':'結果',grade:me.rank,points};state.history.unshift({at:new Date().toISOString(),mode:m.mode,rank:me.rank,rounds:m.round,character:state.playerCharacterId,partner:state.selectedPartnerId,battlePointsEarned:points.earned,quickPoints:points.quick,battlePointsAfter:points.after});save();show('result');renderResult()}
async function renderResult(){const r=state.match?.lastResult;if(!r)return;const m=state.match,ordered=[...r.ranking].sort((a,b)=>a.rank-b.rank),me=ordered.find(x=>x.player.human),mode=({'2p':'二人','4p':'四人','2v2':'二対二'})[m.mode]||m.mode,content=m.contentId==='honey_hunt'?'ハニーハント':'ロード・シールド';const {projectResultRankPresentation}=await import('./result-presentation-core.mjs'),userPresentation=projectResultRankPresentation(me?.rank);if(!userPresentation?.ok)return;const gradeEl=$('#resultGrade'),headlineEl=$('#resultHeadline');gradeEl.textContent=userPresentation.visibleLabel;headlineEl.textContent=userPresentation.visibleLabel;gradeEl.dataset.formalRank=String(userPresentation.formalRank);headlineEl.dataset.formalRank=String(userPresentation.formalRank);if(userPresentation.rankColorRole){gradeEl.dataset.rankColorRole=userPresentation.rankColorRole;headlineEl.dataset.rankColorRole=userPresentation.rankColorRole}else{delete gradeEl.dataset.rankColorRole;delete headlineEl.dataset.rankColorRole}$('#resultSummary').textContent=`${m.round}巡で決着。 ${content}`;$('#resultLeadText')&&($('#resultLeadText').textContent=r.grade===1?'一位で決着':'最終順位');$('#resultLeadSub')&&($('#resultLeadSub').textContent=`${content} / ${mode}`);$('#resultMode')&&($('#resultMode').textContent=mode);$('#resultRounds')&&($('#resultRounds').textContent=`${m.round}巡`);$('#resultLane')&&($('#resultLane').textContent=`${me?.depth??0}枚`);renderResultBattlePoints(r);const box=$('#resultRanking');box.innerHTML='';ordered.forEach(x=>{const presentation=projectResultRankPresentation(x.rank);if(!presentation?.ok)return;const d=document.createElement('div');d.className='rankLine';d.dataset.formalRank=String(presentation.formalRank);if(presentation.rankColorRole)d.dataset.rankColorRole=presentation.rankColorRole;d.innerHTML=`<b><span class="resultOutcome">${presentation.visibleLabel}</span> ${x.player.human?'あなた':x.player.name}</b><span>最長列 ${x.depth}枚</span>`;box.appendChild(d)});await grBattleReplayRenderResult(m);resultMount=await mountChar('.resultWorld',state.playerCharacterId,'joy')}
$('#rematch').onclick=()=>startMatch();$('#leaveMatch').onclick=()=>{state.match=null;navigateRoot('home')};window.__GAMEROAD_QUICK_R14_QA__={version:'FULLGAME-QUICKTIMER-R14',constants:{windowMs:QUICK_WINDOW_MS,perfectRemainMs:QUICK_PERFECT_REMAIN_MS,goodRemainMs:QUICK_GOOD_REMAIN_MS,perfectPoints:QUICK_PERFECT_POINTS,goodPoints:QUICK_GOOD_POINTS,chestTarget:BATTLE_POINTS_CHEST_TARGET},snapshot(){const m=state.match,me=m?.players?.find(p=>p.human),rem=m?quickRemainingMs(m):null;return{screen:state.screen,round:m?.round||null,phase:m?.phase||null,remainingMs:rem,tier:m?quickTierForRemaining(rem):null,quickPoints:me?(m.quickPointsByPlayer?.[me.id]||0):0,awards:me?(m.quickAwards||[]).filter(a=>a.playerId===me.id).map(a=>({...a})):[],battlePoints:state.progression?.battlePoints||0,settlement:m?.battlePointsSettlement?safeClone(m.battlePointsSettlement):null,coilHidden:$('#quickCoil')?.hidden??null,coilText:$('#quickRemain')?.textContent||null,resultText:$('#resultBattlePoints')?.innerText||''}},setRemainingSeconds(sec){const m=state.match;if(!m?.quickWindow)return false;const v=Math.max(0,Math.min(30,Number(sec)||0));m._quickQaNowMs=m.quickWindow.deadlineAt-v*1000;renderQuickTimer();return true},clearTestClock(){const m=state.match;if(!m)return false;delete m._quickQaNowMs;renderQuickTimer();return true},setBattlePoints(n){state.progression={battlePoints:Math.max(0,Math.floor(Number(n)||0))};return state.progression.battlePoints},persistenceProbe(n){state.progression={battlePoints:Math.max(0,Math.floor(Number(n)||0))};save();state.progression={battlePoints:0};load();return{battlePoints:state.progression.battlePoints,storage:state.storage}}};window.GAMEROAD_3D_WALK_QA={version:'BROWSER-3D-BOARD-WALK-INTEGRATION-001',snapshot(){const m=state.match,me=m?.players?.find(p=>p.human);return{screen:state.screen,phase:m?.phase||null,position:me?.position||null,path:me?.plan?.path?[...me.plan.path]:null,avatarState:battleMount?.root?.dataset?.state||null,avatarFacing:battleMount?.facingName||null,nodeCount:document.querySelectorAll('#board .node').length,canvas:{w:$('#fieldCanvas')?.width||0,h:$('#fieldCanvas')?.height||0}}}};window.GAMEROAD_GRASSLAND3D_QA={version:'BROWSER-BOARD-GRASSLAND-3D-R4',snapshot(){const gl=$('#fieldCanvas')?.getContext('webgl');return{...fieldLastStats,webgl:!!gl,contextLost:!!gl?.isContextLost?.(),projectionCount:fieldProjection.size,nodeCount:document.querySelectorAll('#board .node').length,screen:state.screen}}};
window.GAMEROAD_BOARD_REVIEW_QA={version:'BROWSER-BOARD-PLAYTEST-REVIEW-LOOP-001',_rect(e){const b=e?.getBoundingClientRect();return b?{l:b.left,r:b.right,t:b.top,b:b.bottom,w:b.width,h:b.height}:null},_hit(a,b){return!!(a&&b&&Math.min(a.r,b.r)>Math.max(a.l,b.l)&&Math.min(a.b,b.b)>Math.max(a.t,b.t))},snapshot(){const rect=this._rect.bind(this),hit=this._hit.bind(this),nodes=[...document.querySelectorAll('#board .node')].map(e=>({id:e.dataset.pos,rect:rect(e)})),surfaces={publicHud:rect($('#publicTurnHud')),battleInfo:rect(document.querySelector('.battleInfo')),rail:rect(documen

=== CHAR PATTERN 'function startMatch(){' HITS_SHOWN=1 ===
--- char 10036771 context 10034971:10043771 ---
ession?.battlePoints)||0)),earned=base+quick,after=before+earned,awards=(m.quickAwards||[]).filter(a=>a.playerId===me?.id&&a.points>0);state.progression={battlePoints:after};m.battlePointsSettlement={base,quick,earned,before,after,chestTarget:BATTLE_POINTS_CHEST_TARGET,chestLinesBefore:Math.floor(before/BATTLE_POINTS_CHEST_TARGET),chestLinesAfter:Math.floor(after/BATTLE_POINTS_CHEST_TARGET),awards};return m.battlePointsSettlement}
function renderResultBattlePoints(r){const p=r?.points;if(!p)return;$('#resultPointsEarned')&&($('#resultPointsEarned').textContent=`+${p.earned.toLocaleString('ja-JP')}`);const road=p.awards?.filter(a=>a.stage==='road').reduce((n,a)=>n+a.points,0)||0,battle=p.awards?.filter(a=>a.stage==='battle').reduce((n,a)=>n+a.points,0)||0;$('#resultPointsBreakdown')&&($('#resultPointsBreakdown').textContent=`通常 +${p.base.toLocaleString('ja-JP')} / クイック +${p.quick.toLocaleString('ja-JP')}（ロード +${road} / バトル +${battle}）`);const remainder=p.after%BATTLE_POINTS_CHEST_TARGET,reached=p.chestLinesAfter>p.chestLinesBefore,fill=reached?100:(remainder/BATTLE_POINTS_CHEST_TARGET)*100;$('#resultPointsMeterFill')&&($('#resultPointsMeterFill').style.width=`${fill}%`);$('#resultPointsChest')&&($('#resultPointsChest').textContent=reached?`宝箱ライン到達 / 累計 ${p.after.toLocaleString('ja-JP')} BattlePoints`:`宝箱進捗 ${remainder.toLocaleString('ja-JP')} / ${BATTLE_POINTS_CHEST_TARGET.toLocaleString('ja-JP')}`)}
const quickTicker=setInterval(()=>{if(state.screen==='battle')renderQuickTimer()},100);window.addEventListener('pagehide',()=>clearInterval(quickTicker),{once:true});
function initRoundRuntime(m){m.participants=new Map(m.players.map(p=>[p.id,[]]));m.participantSeq=0;m.resolvedAbilityKeys=new Set();m.abilityOrderReceipts=new Map();m.testAutoChoices=false;quickStartRound(m)}

function startMatch(){const eligibility=deckEligibility(state.savedDeck);if(!eligibility.usable){toast(deckEligibilityMessage(eligibility));show('setup');renderSetupDeckStatus();return null}audioResetForNewMatch();try{if(battleMount&&window.GameRoadThreeCharRuntime?.unmount)window.GameRoadThreeCharRuntime.unmount(battleMount)}catch(e){}battleMount=null;battleMountPromise=null;const battleRuntimeEl=$('#battleRuntime');if(battleRuntimeEl)battleRuntimeEl.replaceChildren();const snapshotFactory=globalThis.GAMEROAD_CREATE_DECK_MATCH_START_SNAPSHOT;let snapshot;try{if(typeof snapshotFactory!=='function')throw new Error('MATCH_START_SNAPSHOT_RUNTIME_UNAVAILABLE');snapshot=snapshotFactory({savedDeck:state.savedDeck,savedDeckRule:state.savedDeckRule,setupMode:state.setupMode,setupContent:state.setupContent,playerCharacterId:state.playerCharacterId,selectedPartnerId:state.selectedPartnerId},{validateDeck})}catch(e){const message=String(e?.message||e),prefix='MATCH_START_DECK_INVALID:';toast(message.startsWith(prefix)?'札組を修正: '+message.slice(prefix.length):'対戦開始状態を確認できません');show('setup');renderSetupDeckStatus();return null}const n=playerCount(snapshot.setup.mode),ps=[],diagnosticCpuDeck=[...snapshot.deck.main];for(let i=0;i<n;i++)ps.push(makePlayer(i,i===0,i===0?[...snapshot.deck.main]:diagnosticCpuDeck));if(snapshot.setup.mode==='2v2'){ps[0].team='A';ps[1].team='A';ps[2].team='B';ps[3].team='B'}state.match={id:'M'+Date.now(),mode:snapshot.setup.mode,contentId:snapshot.setup.content,round:1,phase:'plan',players:ps,activeId:null,target:null,log:[],lastResult:null,lastBattleResolution:null,battlePresentation:null,resolutionSeq:0,projection:['N','E','S','W'],abilityRng:(Date.now()^0x9e3779b9)>>>0,busy:false,deckStartSnapshot:snapshot,honey:{byNode:{},receipts:new Set(),generatedRounds:new Set()},playerDeckRule:{id:snapshot.deck.ruleId,revision:snapshot.deck.ruleRevision,mainCount:snapshot.deck.main.length,exCount:snapshot.deck.ex.length}};state.match.selectionLock=grCreateSelectionLock(state.match,'P1');grBattleReplayBegin(state.match);awardRoundStartHoney(state.match);initRoundRuntime(state.match);log(snapshot.setup.content==='honey_hunt'?'第1巡。ハニーハント：一筆移動・終点ハニー全量取得・一列七枚勝利。人物を直接ドラッグして経路を秘密予約します。':'第1巡。全員が行動を予約します。');show('battle');setupBattleDetails();renderBattle();return state.match}
$('#startMatch').addEventListener('click',startMatch);
function log(t){if(!state.match)return;state.match.log.unshift(t);state.match.log=state.match.log.slice(0,30)}
function refill(p){while(p.hand.length<3&&p.deck.length)p.hand.push(p.deck.shift())}
function laneDepth(p,l){return p.lanes[l].length}
function posType(id){if(id.startsWith('C:'))return'center';if(id.startsWith('S:'))return'shield';if(id.startsWith('R:'))return'road';return'corner'}
function parseRoad(id){const [,owner,lane,depth]=id.split(':');return{owner,lane,depth:+depth}}
function projectionFor(viewer){const ids=['P1','P2','P3','P4'];const vi=Math.max(0,ids.indexOf(viewer.id));const arms=['N','E','S','W'];const map={};ids.forEach((id,i)=>map[id]=arms[(i-vi+4)%4]);return map}
function centerIds(){const a=[];for(let r=-1;r<=1;r++)for(let c=-1;c<=1;c++)a.push(`C:${r}:${c}`);return a}
function shieldId(owner,lane){return`S:${owner}:${lane}`};function roadId(owner,lane,d){return`R:${owner}:${lane}:${d}`}
function activeNodes(){const s=new Set(centerIds());['K:NW','K:NE','K:SW','K:SE'].forEach(id=>s.add(id));state.match.players.forEach(p=>{['L','C','R'].forEach(l=>{s.add(shieldId(p.id,l));for(let d=1;d<=p.lanes[l].length&&d<=7;d++)s.add(roadId(p.id,l,d))})});return s}
function centerPort(owner,lane,viewer){const arm=projectionFor(viewer)[owner];const laneIx={L:-1,C:0,R:1}[lane];if(arm==='N')return`C:-1:${laneIx}`;if(arm==='S')return`C:1:${-laneIx}`;if(arm==='W')return`C:${-laneIx}:-1`;return`C:${laneIx}:1`}
function neighbors(id,viewer){const act=activeNodes(),out=[],proj=projectionFor(viewer),ownerFor=arm=>state.match.players.find(p=>proj[p.id]===arm)?.id,cornerDefs={'K:NW':[['N','L'],['W','R']],'K:NE':[['N','R'],['E','L']],'K:SW':[['S','R'],['W','L']],'K:SE':[['S','L'],['E','R']]},cornerRoads=k=>(cornerDefs[k]||[]).map(([arm,lane])=>{const owner=ownerFor(arm);return owner?roadId(owner,lane,1):null}).filter(Boolean);if(id.startsWith('C:')){const [,rs,cs]=id.split(':'),r=+rs,c=+cs;[[r-1,c],[r+1,c],[r,c-1],[r,c+1]].forEach(([rr,cc])=>{const x=`C:${rr}:${cc}`;if(act.has(x))out.push(x)});state.match.players.forEach(p=>['L','C','R'].forEach(l=>{const sh=shieldId(p.id,l);if(centerPort(p.id,l,viewer)===id&&act.has(sh))out.push(sh)}))}else if(id.startsWith('K:')){cornerRoads(id).forEach(x=>{if(act.has(x))out.push(x)})}else if(id.startsWith('S:')){const [,owner,lane]=id.split(':');out.push(centerPort(owner,lane,viewer));const r1=roadId(owner,lane,1);if(act.has(r1))out.push(r1)}else if(id.startsWith('R:')){const {owner,lane,depth}=parseRoad(id);if(depth===1){out.push(shieldId(owner,lane));Object.keys(cornerDefs).forEach(k=>{if(cornerRoads(k).includes(id))out.push(k)})}const a=roadId(owner,lane,depth-1),b=roadId(owner,lane,depth+1);if(depth>1&&act.has(a))out.push(a);if(depth<7&&act.has(b))out.push(b)}return out.filter(x=>act.has(x))}
function shortest(start,end,max,viewer){if(start===end)return[start];const q=[[start]],seen=new Set([start]);while(q.length){const p=q.shift();if(p.length-1>=max)continue;for(const n of neighbors(p[p.length-1],viewer)){if(seen.has(n))continue;const np=[...p,n];if(n===end)return np;seen.add(n);q.push(np)}}return null}
function reachable(p,steps){const q=[[p.position,0]],dist=new Map([[p.position,0]]);while(q.length){const [x,d]=q.shift();if(d>=steps)continue;for(const n of neighbors(x,p)){if(!dist.has(n)){dist.set(n,d+1);q.push([n,d+1])}}}return dist}
/* DOPPELGANGER-NORMAL-RULE-RUNTIME-001 R1
   Recovered user canon (2026-07-26 / 2026-08-06):
   - whole field; road/battle cards only; shields join only after becoming battle cards
   - activates when two or more cards share the same printed number
   - same-number reduction includes the Doppelganger card itself
   - continuous current value affects road movement/order/attack-right/battle value
   - zero does not destroy the card; no recursive modifier loop
   Unknowns remain fail-closed: formal card id/suit/rank, same-name self counting,
   negative values, modifier ordering beyond the recovered examples, hidden-card edge cases. */
const GR_DOPPEL_EFFECT='doppelgangerContinuous';
function grDoppelIs(cardId){return CARDS[cardId]?.effect_type===GR_DOPPEL_EFFECT}
function grDoppelEligibleParticipant(e){return !!e&&['road','active_submission','auto_defense','ability_active_addition'].includes(String(e.origin||''))}
function grDoppelFieldSnapshot(m=state.match){
 const out=[],seen=new Set(); if(!m)return out;
 const add=(key,ownerId,cardId,zone,publicNow=true)=>{const c=CARDS[cardId];if(!c||!publicNow||seen.has(key))return;seen.add(key);out.push(Object.freeze({key,ownerId,cardId,zone,printedValue:Number(c.power),name:String(c.display_name||cardId)}))};
 m.players.forEach(p=>[

=== CHAR PATTERN 'const FR=' HITS_SHOWN=1 ===
--- char 10240230 context 10238430:10247230 ---
;save();return audioSnapshot()},setVolumes:(m,s,p)=>{const a=normalizeAudioSettings();if(m!=null)a.musicVolume=Math.max(0,Math.min(1,Number(m)));if(s!=null)a.sfxVolume=Math.max(0,Math.min(1,Number(s)));if(p!=null)a.partnerVoiceVolume=Math.max(0,Math.min(1,Number(p)));audioUpdateLiveVolumes();save();return audioSnapshot()},clearEvents:()=>audioRuntime.seenEvents.clear(),assetPolicy:{formalDefault:'battle_music_none',previewPack:'unity_candidate_v1',livingForestFormal:false,sfxFormal:false,matchFoundRuntimeHook:false}};
window.__V106_FIELD_TEST__={nodeWorld,fieldProject,renderField3D,beginRouteDrag,moveRouteDrag,endRouteDrag,spawnHoneyFromRoads,collectEndpointHoney,awakeManaFromHoney,honeySourceNodesForPower,honeySourceContract:'lane-card-same-power; shields excluded; player+lane+position retained; endpoint pile pickup=all; win=one lane >=7 real cards',injectHoney:(id,n=1)=>{const m=state.match;if(!m)return false;m.honey.byNode[id]=n;renderBattle();return true},setContent:(id)=>{state.setupContent=id;return id},route:()=>state.match?.players?.[0]?.plan?.path||[],projection:()=>Object.fromEntries(fieldProjection),honey:()=>state.match?.honey?{byNode:{...state.match.honey.byNode},receipts:[...state.match.honey.receipts]}:null};window.__GAMEROAD_BUILD__={version:'10.7',workId:'BROWSER-V107-DECK-BATTLE-INTEGRATION-001',rulings:{honeyPilePickup:'all',win:'one-lane-seven-or-more-real-cards',deckRule:'FIRST_REGULATION@3',mainDesign:40,mainMax:40,exMax:7,sameNameBase:1}};load();initDeckLibrary();renderHome();

/* GAMEROAD FRIEND ROOM R2 CORE INTEGRATION */
(function(){
'use strict';
const BUILD='BROWSERROOM-R32-HATEAUTH-20260810-M7Q4V2N8-R19';
const V=2, ALPHABET='ABCDEFGHJKLMNPQRSTUVWXYZ23456789';
const clone=x=>x==null?x:JSON.parse(JSON.stringify(x));
const now=()=>Date.now();
const FR={role:null,code:'',channel:null,clientId:'',authToken:'',assignedPid:null,playerCount:2,contentId:'road_shield',slots:{},participants:[],roomState:'idle',hostReady:false,guestReady:false,snapshot:null,seq:0,lastReject:'',planValues:{},choiceWait:new Map(),choiceSeq:0,choiceActorPid:null,partnerRules:{},localPartnerRule:null,hateClocks:{},hateBlockers:{},hateLastTick:0,lastMembershipChange:0,matchNo:0,baseProgression:null,baseHistoryLength:null,transport:'broadcastchannel_local_r2'};
const FRIEND_TRANSPORT_PRESENCE_TYPE='transport_presence';
const FRIEND_PRESENCE={states:new Map(),corePromise:null,core:null};
const FRIEND_HATE1000_PRESENTATION={sessionId:null,state:null,timer:null};
function friendHate1000Root(){
  let root=document.getElementById('hate1000Presentation');
  if(root)return root;
  if(!document.getElementById('hate1000PresentationStyle')){
    const style=document.createElement('style');
    style.id='hate1000PresentationStyle';
    style.textContent=`
#hate1000Presentation{position:fixed;inset:0;z-index:84;display:grid;place-items:center;pointer-events:none;contain:layout style paint}
#hate1000Presentation[hidden]{display:none}
#hate1000Presentation .hate1000Burst{position:absolute;width:9rem;height:9rem;border:2px solid rgba(255,112,112,.9);border-radius:50%;box-shadow:0 0 1.8rem rgba(255,78,78,.45),inset 0 0 1.2rem rgba(255,180,112,.18);opacity:.82}
#hate1000Presentation .hate1000Card{position:relative;min-width:10.5rem;padding:.72rem 1rem .68rem;border:1px solid rgba(255,176,132,.74);border-radius:1rem;background:rgba(31,9,14,.88);box-shadow:0 .8rem 2.2rem rgba(0,0,0,.34),0 0 1.2rem rgba(255,84,84,.24);text-align:center;color:#fff7ef;letter-spacing:.06em}
#hate1000Presentation .hate1000Kicker{display:block;font-size:.72rem;font-weight:800;color:#ffb098}
#hate1000Presentation .hate1000Value{display:block;margin-top:.08rem;font-size:1.42rem;font-weight:950;line-height:1}
#hate1000Presentation .hate1000Meaning{display:block;margin-top:.28rem;font-size:.76rem;font-weight:750;color:#ffe5cf}
#hate1000Presentation.play .hate1000Burst{animation:hate1000Burst var(--hate1000-duration,480ms) ease-out both}
#hate1000Presentation.play .hate1000Card{animation:hate1000Card var(--hate1000-duration,480ms) ease-out both}
#hate1000Presentation.static .hate1000Burst,#hate1000Presentation.static .hate1000Card{animation:none;opacity:1;transform:none}
@keyframes hate1000Burst{0%{opacity:0;transform:scale(.72)}12.5%{opacity:1;transform:scale(.96)}25%{opacity:.72;transform:scale(1.08)}54%{opacity:.34;transform:scale(1.18)}100%{opacity:0;transform:scale(1.28)}}
@keyframes hate1000Card{0%{opacity:0;transform:translateY(.5rem) scale(.96)}12.5%{opacity:1;transform:translateY(0) scale(1)}25%{opacity:.96;transform:translateY(0) scale(1)}54%{opacity:.72;transform:translateY(-.06rem) scale(.995)}100%{opacity:0;transform:translateY(-.16rem) scale(.985)}}`;
    document.head.appendChild(style);
  }
  root=document.createElement('div');
  root.id='hate1000Presentation';
  root.hidden=true;
  root.setAttribute('role','status');
  root.setAttribute('aria-live','polite');
  root.setAttribute('aria-atomic','true');
  root.innerHTML='<div class="hate1000Burst" aria-hidden="true"></div><div class="hate1000Card"><span class="hate1000Kicker">HATE LIMIT</span><strong class="hate1000Value">HATE 1000</strong><span class="hate1000Meaning">強制委任</span></div>';
  (document.getElementById('battle')||document.body).appendChild(root);
  return root;
}
function friendHate1000Preferences(){
  const reduced=typeof matchMedia==='function'&&matchMedia('(prefers-reduced-motion: reduce)').matches;
  return{reducedMotion:!!reduced,lowPerf:!!state.settings?.lowPerf,animationEnabled:true};
}
async function friendPresentHate1000(p){
  const sessionId=String(state.match?.id||'');
  if(!p||!sessionId)return false;
  const core=await friendPresenceCore();
  if(FRIEND_HATE1000_PRESENTATION.sessionId!==sessionId||!FRIEND_HATE1000_PRESENTATION.state){
    FRIEND_HATE1000_PRESENTATION.sessionId=sessionId;
    FRIEND_HATE1000_PRESENTATION.state=core.createHate1000PresentationSession({sessionId});
  }
  const eventId=`${sessionId}:${String(p.id||'unknown')}:hate1000`;
  const applied=core.applyHate1000PresentationEvent(FRIEND_HATE1000_PRESENTATION.state,{sessionId,eventId,kind:core.HATE1000_PRESENTATION_MOTION_CORE.kind,authorized:p.hateExploded===true&&Number(p.hateTime)>=1000},friendHate1000Preferences());
  if(applied?.state)FRIEND_HATE1000_PRESENTATION.state=applied.state;
  if(!applied?.accepted||applied.duplicate||!applied.plan)return false;
  const root=friendHate1000Root();
  clearTimeout(FRIEND_HATE1000_PRESENTATION.timer);
  root.hidden=false;
  root.classList.remove('play','static');
  const duration=Number(applied.plan.motion?.durationMs||core.HATE1000_PRESENTATION_MOTION_CORE.timing.durationMs)||480;
  root.style.setProperty('--hate1000-duration',`${duration}ms`);
  void root.offsetWidth;
  root.classList.add(applied.plan.mode==='motion'?'play':'static');
  FRIEND_HATE1000_PRESENTATION.timer=setTimeout(()=>{root.hidden=true;root.classList.remove('play','static')},duration);
  return true;
}
async function friendPresenceCore(){
  if(!FRIEND_PRESENCE.corePromise)FRIEND_PRESENCE.corePromise=import('./hate-peer-presence-core.mjs');
  const core=await FRIEND_PRESENCE.corePromise;FRIEND_PRESENCE.core=core;return core
}
function friendPresenceEligible(cid){
  const peerId=String(cid||''),prior=FRIEND_PRESENCE.states.get(peerId),core=FRIEND_PRESENCE.core;
  if(!prior||!core)return false;
  try{return !!core.projectHateHumanWaitSourceEligibility(prior).eligible}catch{return false}
}
async function friendApplyTransportPresence(m){
  const peerId=String(m?.clientId||''),sessionId=String(m?.sessionId||''),revision=Number(m?.revision),kind=String(m?.kind||'');
  if(!peerId||!sessionId||!Number.isSafeInteger(revision)||revision<=0)return false;
  try{
    const core=await friendPresenceCore();
    let prior=FRIEND_PRESENCE.states.get(peerId);
    if(!prior||prior.sessionId!==sessionId)prior=core.createHatePeerPresenceState({peerId,sessionId,revision:0,connected:false});
    const applied=core.applyHatePeerPresenceEvent(prior,{kind,peerId,sessionId,revision});
    if(!applied.ok){FR.lastReject='hate_presence_'+String(applied.reason||'invalid').toLowerCase();return friendPresenceEligible(peerId)}
    FRIEND_PRESENCE.states.set(peerId,applied.state);
    return !!core.projectHateHumanWaitSourceEligibility(applied.state).eligible
  }catch(e){FR.lastReject='hate_presence_core_unavailable';return false}
}
function normCode(v=''){return String(v||'').toUpperCase().replace(/[\s\-_.]/g,'')}
function validCode(v=''){const x=normCode(v);return x.length===7&&[...x].every(c=>ALPHABET.includes(c))?x:''}
function randomCode(){let s='';for(let i=0;i<7;i++)s+=ALPHABET[Math.floor(Math.random()*ALPHABET.length)];return s}
function pidList(n=FR.playerCount){return Number(n)===4?['P1','P2','P3','P4']:['P1','P2']}
function guestPids(){r

=== CHAR PATTERN 'function sendAllLobby' HITS_SHOWN=1 ===
--- char 10249901 context 10248101:10256901 ---
Id})}catch{}try{FR.channel.close()}catch{}}FR.channel=null}
function reset(sendLeave=true){closeChannel(sendLeave);FRIEND_PRESENCE.states.clear();Object.assign(FR,{role:null,code:'',channel:null,clientId:'',authToken:'',assignedPid:null,playerCount:2,contentId:'road_shield',slots:{},participants:[],roomState:'idle',hostReady:false,guestReady:false,snapshot:null,seq:0,lastReject:'',planValues:{},choiceWait:new Map(),choiceSeq:0,choiceActorPid:null,partnerRules:{},localPartnerRule:null,hateClocks:{},hateBlockers:{},hateLastTick:0,lastMembershipChange:0,matchNo:0,baseProgression:null,baseHistoryLength:null,transport:'broadcastchannel_local_r2'});document.body.classList.remove('friend-room-match');renderLobby()}
function send(msg){if(!FR.channel)return false;const env={v:V,code:FR.code,...msg};if(FR.role==='guest'){env.clientId=FR.clientId;env.authToken=FR.authToken;env.pid=FR.assignedPid;env.seq=++FR.seq}try{FR.channel.postMessage(env);return true}catch{return false}}
function slotByClient(cid){return Object.values(FR.slots).find(s=>s.clientId===String(cid||''))||null}
function slotByPid(pid){return FR.slots[pid]||null}
function allConnected(){return guestPids().every(pid=>slotByPid(pid)?.connected)}
function allReady(){return FR.hostReady&&guestPids().every(pid=>{const s=slotByPid(pid);return s?.connected&&s.ready})}
function participantsHost(){return[{pid:'P1',connected:true,ready:!!FR.hostReady},...guestPids().map(pid=>{const s=slotByPid(pid);return{pid,connected:!!s?.connected,ready:!!s?.ready}})]}
function publicLobby(){return{roomState:FR.roomState,code:FR.code,playerCount:FR.playerCount,contentId:FR.contentId,matchNo:FR.matchNo,participants:participantsHost()}}
function sendLobby(to){const s=slotByClient(to);if(!s)return;send({type:'lobby',to,lobby:publicLobby()})}
function sendAllLobby(){for(const s of Object.values(FR.slots))if(s.connected)sendLobby(s.clientId);renderLobby()}
function reject(to,reason,detail=''){FR.lastReject=reason;send({type:'reject',to,reason,detail})}
function verifyGuest(m,{phase=false}={}){const s=slotByClient(m.clientId);if(!s)return{ok:false,reason:'unknown_guest'};if(String(m.authToken||'')!==String(s.authToken||''))return{ok:false,reason:'auth_mismatch'};if(m.pid&&m.pid!==s.pid)return{ok:false,reason:'not_actor'};if(Number(m.seq)<=Number(s.lastSeq||0))return{ok:false,reason:'duplicate_or_stale_seq'};if(phase&&state.match&&(Number(m.round)!==Number(state.match.round)||String(m.phase)!==String(state.match.phase)))return{ok:false,reason:'stale_round_or_phase'};s.lastSeq=Number(m.seq);return{ok:true,slot:s,pid:s.pid}}
function allocate(cid){let s=slotByClient(cid);if(s)return{s,newMember:false};if(FR.roomState!=='lobby')return{s:null,newMember:false};s=guestPids().map(slotByPid).find(x=>!x.clientId)||guestPids().map(slotByPid).find(x=>!x.connected);if(!s)return{s:null,newMember:false};s.clientId=cid;s.authToken=randomId('auth');s.connected=false;s.ready=false;s.lastSeq=0;return{s,newMember:true}}
function resetReady(){FR.hostReady=false;for(const s of Object.values(FR.slots))s.ready=false;FR.planValues={};FR.lastMembershipChange=now()}
const WS_WIRE='gameroad.wsrelay.v1';
const WS_TRACE=window.__GAMEROAD_WS_TRACE=window.__GAMEROAD_WS_TRACE||[];
function wsTrace(dir,data){try{WS_TRACE.push({dir,type:String(data?.type||''),to:String(data?.to||''),clientId:String(data?.clientId||''),ts:Date.now()});if(WS_TRACE.length>500)WS_TRACE.splice(0,WS_TRACE.length-500)}catch{}}
function wsRelayBase(){const q=new URLSearchParams(location.search);if(window.GAMEROAD_WS_RELAY_URL)return String(window.GAMEROAD_WS_RELAY_URL);if(q.get('grws'))return String(q.get('grws'));const proto=location.protocol==='https:'?'wss:':'ws:';if(location.host)return proto+'//'+location.host+'/ws';return 'ws://127.0.0.1:8765/ws'}
function wsRelayRequested(){const q=new URLSearchParams(location.search);return !!window.GAMEROAD_WS_RELAY_URL||q.get('grrelay')==='ws'||q.has('grws')}
class GAMEROADR21RemoteChannel{
 constructor(name,opts={}){this.name=String(name||'');this.role=String(opts.role||'');this.clientId=String(opts.clientId||'');this.authToken=String(opts.authToken||'');this.onmessage=null;this.onmessageerror=null;this.onopen=null;this.onclose=null;this.onerror=null;this._listeners={message:new Set(),messageerror:new Set(),open:new Set(),close:new Set(),error:new Set()};this._queue=[];this._closed=false;this._ws=null;this._connecting=false;this._connect()}
 _url(){const u=new URL(wsRelayBase(),location.href==='about:blank'?'http://127.0.0.1/':location.href);u.searchParams.set('channel',this.name);u.searchParams.set('role',this.role);u.searchParams.set('clientId',this.clientId);if(this.role==='guest'&&this.authToken)u.searchParams.set('authToken',this.authToken);return u.toString()}
 _emit(type,ev){try{const h=this['on'+type];if(typeof h==='function')h(ev)}catch{}for(const fn of this._listeners[type]||[]){try{fn.call(this,ev)}catch{}}}
 _connect(){if(this._closed||this._connecting||(this._ws&&this._ws.readyState===WebSocket.OPEN))return;this._connecting=true;let ws;try{ws=new WebSocket(this._url())}catch(e){this._connecting=false;this._emit('error',e);return}this._ws=ws;ws.onopen=()=>{this._connecting=false;this._emit('open',{});for(const data of this._queue.splice(0))this._sendNow(data)};ws.onmessage=ev=>{try{const f=JSON.parse(ev.data);if(f&&f.wire===WS_WIRE&&f.op==='data'){wsTrace('in',f.payload);this._emit('message',{data:f.payload});return}if(f&&f.wire===WS_WIRE&&f.op==='transport_reject'){this._emit('messageerror',{reason:f.reason||'transport_reject'});return}}catch(e){this._emit('messageerror',e)}};ws.onerror=e=>this._emit('error',e);ws.onclose=e=>{this._connecting=false;this._emit('close',e)}}
 _sendNow(data){if(!this._ws||this._ws.readyState!==WebSocket.OPEN)return false;try{wsTrace('out',data);this._ws.send(JSON.stringify({wire:WS_WIRE,op:'data',payload:data}));return true}catch(e){this._emit('messageerror',e);return false}}
 postMessage(data){if(this._closed)throw new DOMException('Channel is closed','InvalidStateError');if(this.role==='guest'&&data&&data.authToken)this.authToken=String(data.authToken);if(this._ws&&this._ws.readyState===WebSocket.OPEN)return this._sendNow(data);const raw=JSON.stringify(data);if(!this._queue.length||JSON.stringify(this._queue[this._queue.length-1])!==raw)this._queue.push(data);this._connect();return true}
 close(){this._closed=true;this._queue.length=0;if(this._ws){try{this._ws.close(1000,'channel close')}catch{}}this._ws=null}
 addEventListener(type,fn){if(this._listeners[type]&&typeof fn==='function')this._listeners[type].add(fn)}
 removeEventListener(type,fn){if(this._listeners[type])this._listeners[type].delete(fn)}
}
window.GAMEROADR21RemoteChannel=GAMEROADR21RemoteChannel;
function openChannel(){if(wsRelayRequested()){FR.channel=new GAMEROADR21RemoteChannel(channelName(FR.code),{role:FR.role,clientId:FR.clientId,authToken:FR.authToken||''});FR.transport='ws_relay_recipient_r21'}else{if(typeof BroadcastChannel==='undefined'){FR.lastReject='broadcastchannel_unavailable';renderLobby();return false}FR.channel=new BroadcastChannel(channelName(FR.code));FR.transport='broadcastchannel_local_r2'}FR.channel.onmessage=e=>{Promise.resolve(handleMessage(e.data||{})).catch(err=>{console.error(err);FR.lastReject='handler_error';renderLobby()})};FR.channel.onerror=()=>{FR.lastReject='transport_error';renderLobby()};return true}
function createRoom(count=2){reset(false);FR.role='host';FR.playerCount=Number(count)===4?4:2;FR.code=randomCode();FR.clientId=randomId('host');FR.authToken=randomId('hostauth');FR.assignedPid='P1';FR.contentId=state.setupContent||'road_shield';FR.roomState='lobby';FR.slots=Object.fromEntries(guestPids().map(pid=>[pid,{pid,clientId:null,authToken:null,connected:false,ready:false,lastSeq:0}]));openChannel();show('friendroom');renderLobby();return FR.code}
function joinRoom(raw){const code=validCode(raw);if(!code){toast('七文字の合言葉を確認してください');return false}reset(false);FR.role='guest';FR.code=code;FR.clientId=saved(clientKey(code))||randomId('guest');FR.authToken=saved(authKey(code));FR.roomState='joining';openChannel();show('friendroom');renderLobby();send({type:'join',clientId:FR.clientId,authToken:FR.authToken});return true}
function toggleReady(){if(FR.role==='host'){if(FR.roomState!=='lobby')return false;FR.hostReady=!FR.hostReady;sendAllLobby();return true}if(FR.role==='guest'&&FR.assignedPid&&FR.roomState==='lobby'){send({type:'ready',value:!FR.guestReady});return true}return false}
const FRIEND_PARTNER_RULES=new Set(['left','right','max','min']);
function friendRule(pid){if(pid==='P1')return FRIEND_PARTNER_RULES.has(currentPartnerRule())?currentPartnerRule():null;const r=FR.partnerRules[pid];return FRIEND_PARTNER_RULES.has(r)?r:null}
function friendHumanIsConnected(pid){if(pid===

=== CHAR PATTERN 'function renderLobby(){' HITS_SHOWN=1 ===
--- char 10283363 context 10281563:10290363 ---
R'].includes(t.shield)){reject(cid,'illegal_target');return}mm.target={defenderId:def,lane:t.lane,shield:t.shield};await resolveBattle();broadcastSnapshots();return}if(m.type==='choice_response'){const w=FR.choiceWait.get(String(m.choiceId||''));if(!w||w.pid!==s.pid){reject(cid,'choice_not_actor');return}friendTickHate(performance.now());const actor=state.match?.players?.find(p=>p.id===s.pid);if(actor?.hateExploded){reject(cid,'hate_forced');queueMicrotask(()=>friendForcedDecision(s.pid,'manual-block').catch(e=>{FR.hateBlockers[s.pid]=String(e?.message||e);broadcastSnapshots()}));return}const idx=[...(m.indices||[])].map(Number).filter(i=>Number.isInteger(i)&&i>=0&&i<w.items.length);w.resolve([...new Set(idx)]);return}}
function handleGuestMessage(m){if(m.to&&m.to!==FR.clientId)return;if(m.type==='accept'){FR.assignedPid=m.pid;FR.authToken=m.authToken||'';FR.playerCount=Number(m.playerCount)===4?4:2;FR.contentId=m.contentId||'road_shield';FR.roomState=m.roomState||'lobby';store(clientKey(FR.code),FR.clientId);store(authKey(FR.code),FR.authToken);renderLobby();send({type:'sync'});return}if(m.type==='reject'){FR.lastReject=m.reason||'reject';toast('参加できません: '+FR.lastReject);renderLobby();return}if(m.type==='host_close'){FR.roomState='closed';toast('部屋主が部屋を閉じました');show('friendroom');renderLobby();return}if(m.type==='lobby'){FR.participants=clone(m.lobby?.participants||[]);FR.roomState=m.lobby?.roomState||'lobby';FR.playerCount=Number(m.lobby?.playerCount)===4?4:2;FR.contentId=m.lobby?.contentId||FR.contentId;FR.guestReady=!!FR.participants.find(p=>p.pid===FR.assignedPid)?.ready;if(state.screen!=='friendroom')show('friendroom');renderLobby();return}if(m.type==='snapshot'){applyProjection(m.projection);return}if(m.type==='choice_request'){showRemoteChoice(m.choice);return}}
function renderLobby(){const root=document.getElementById('friendRoomPanel');if(!root)return;const tm=document.getElementById('topMode');if(state.screen==='friendroom'&&tm)tm.textContent='フレンドルーム';const idle=!FR.role,parts=FR.role==='host'?participantsHost():(FR.participants.length?FR.participants:pidList().map(pid=>({pid,connected:pid===FR.assignedPid,ready:false}))),role=FR.role==='host'?'部屋主':FR.role==='guest'?'参加者':'未参加',canReady=FR.roomState==='lobby'&&((FR.role==='host')||(FR.role==='guest'&&FR.assignedPid)),ownReady=FR.role==='host'?FR.hostReady:FR.guestReady;root.innerHTML=`<div class="friendRoomHero"><div><div class="k">FRIEND ROOM</div><div class="h">フレンドルーム</div></div><div class="friendCode"><span>合言葉</span><b>${FR.code||'-------'}</b><small>${role} / ${FR.playerCount}人 / ${FR.transport==='ws_relay_recipient_r21'?'遠隔WS':'同一アドレス'}</small></div></div><div class="friendRoomGrid"><div class="friendRoomCard"><h3>部屋</h3>${idle?`<div class="friendCreateRow"><button class="btn primary" id="friendCreate2">二人部屋を作る</button><button class="btn" id="friendCreate4">四人部屋を作る</button></div><label class="friendJoinLabel">七文字の合言葉<input id="friendJoinCode" maxlength="9" autocomplete="off" inputmode="text" placeholder="ABCDEFG"></label><button class="btn" id="friendJoinBtn">合言葉で入る</button>`:`<div class="friendStatus">状態: ${FR.roomState==='lobby'?'準備中':FR.roomState==='playing'?'対戦中':FR.roomState==='result'?'対戦終了':FR.roomState}</div><button class="btn danger" id="friendLeaveBtn">部屋を出る</button>`}${FR.lastReject?`<div class="friendReject">${FR.lastReject}</div>`:''}</div><div class="friendRoomCard"><h3>参加者</h3><div class="friendParticipants">${parts.map(p=>`<div class="friendParticipant ${p.connected?'connected':''} ${p.ready?'ready':''}"><b>${p.pid}</b><span>${p.connected?'接続':'空き'}</span><em>${p.ready?'準備完了':'待機'}</em></div>`).join('')}</div>${!idle?`<div class="friendActions"><button class="btn ${ownReady?'on':''}" id="friendReadyBtn" ${canReady?'':'disabled'}>${ownReady?'準備を戻す':'準備完了'}</button>${FR.role==='host'?`<button class="btn primary" id="friendStartBtn" ${allReady()?'':'disabled'}>部屋主が開始</button>`:''}</div>`:''}</div></div>`;document.getElementById('friendCreate2')?.addEventListener('click',()=>createRoom(2));document.getElementById('friendCreate4')?.addEventListener('click',()=>createRoom(4));document.getElementById('friendJoinBtn')?.addEventListener('click',()=>joinRoom(document.getElementById('friendJoinCode')?.value));document.getElementById('friendReadyBtn')?.addEventListener('click',toggleReady);document.getElementById('friendStartBtn')?.addEventListener('click',hostStart);document.getElementById('friendLeaveBtn')?.addEventListener('click',()=>{grClearPartnerMatchSession(state.match);reset(true);state.match=null;show('setup')})}
function returnLobby(){if(FR.role!=='host'||!['result','playing'].includes(FR.roomState))return false;FR.roomState='lobby';FR.hostReady=false;for(const s of Object.values(FR.slots))s.ready=false;FR.planValues={};FR.choiceWait.forEach(x=>x.reject?.(Error('rematch_reset')));FR.choiceWait.clear();grClearPartnerMatchSession(state.match);state.match=null;document.body.classList.remove('friend-room-match');show('friendroom');sendAllLobby();return true}
function friendEndMatch(winners){const m=state.match;let ranking;if(m.mode==='2v2'){const wt=winners[0];ranking=m.players.map(p=>({player:p,rank:p.team===wt?1:2,depth:Math.max(...Object.values(p.lanes).map(a=>a.length))}))}else ranking=ranksFFA();const me=ranking.find(x=>x.player.id==='P1');m.lastResult={ranking,headline:me?.rank===1?'勝利':'結果',grade:me?.rank||0,points:null,friendRoom:true};FR.roomState='result';if(FR.baseProgression!=null)state.progression={battlePoints:FR.baseProgression};if(FR.baseHistoryLength!=null&&state.history.length>FR.baseHistoryLength)state.history.splice(0,state.history.length-FR.baseHistoryLength);broadcastSnapshots();show('result');renderResult();const pts=document.getElementById('resultBattlePoints');if(pts)pts.hidden=true;return m.lastResult}
const baseResolveAbilityEntry=resolveAbilityEntry;
resolveAbilityEntry=async function(entry,phase){if(FR.role==='host'&&state.match?.friendRoom){FR.choiceActorPid=entry.ownerId;try{return await baseResolveAbilityEntry(entry,phase)}finally{FR.choiceActorPid=null}}return baseResolveAbilityEntry(entry,phase)};
const baseChooseAbilityItems=chooseAbilityItems;
chooseAbilityItems=async function(title,hint,items,min,max,human){const pid=FR.choiceActorPid;if(FR.role==='host'&&state.match?.friendRoom&&pid&&pid!=='P1'&&slotByPid(pid)?.connected)return requestRemoteChoice(pid,title,hint,items,min,max);return baseChooseAbilityItems(title,hint,items,min,max,human)};
const baseReplacementBattleCard=replacementBattleCard;
replacementBattleCard=async function(p,reason){if(FR.role==='host'&&state.match?.friendRoom&&p?.id!=='P1'&&p?.roomHuman){const opts=p.hand.filter(id=>CARDS[id]?.runtime_status==='playable').map(cardId=>({kind:'hand',cardId,id:cardId,detail:reason}));if(!opts.length)return null;const ch=await requestRemoteChoice(p.id,'バトルカードを再確認',reason,opts,1,1);return ch[0]?.cardId||null}return baseReplacementBattleCard(p,reason)};
const baseNextRound=nextRound;
nextRound=function(){const r=baseNextRound();if(FR.role==='host'&&state.match?.friendRoom){FR.planValues={};friendSyncHateClocks(performance.now());broadcastSnapshots()}return r};
const baseEndMatch=endMatch;
endMatch=function(winners){if(FR.role==='host'&&state.match?.friendRoom)return friendEndMatch(winners);return baseEndMatch(winners)};
const baseRenderBattle=renderBattle;
renderBattle=function(){const r=baseRenderBattle();if(FR.role==='guest'&&state.match?.friendRoom)renderRemoteWait();else if(FR.role==='host'&&state.match?.friendRoom)renderFriendHostBattleState();return r};
const ready=document.getElementById('readyPlan'),baseReady=ready?.onclick;
if(ready)ready.onclick=()=>{if(state.match?.friendRoom){const p=FR.role==='host'?submitHostPlan():submitGuestPlan();return Promise.resolve(p).catch(console.error)}return baseReady?.()};
const target=document.getElementById('confirmTarget'),baseTarget=target?.onclick;
if(target)target.onclick=async()=>{if(state.match?.friendRoom&&FR.role==='guest')return submitGuestTarget();if(FR.role==='host'&&state.match?.friendRoom&&state.match.players?.find(p=>p.id==='P1')?.hateExploded){queueMicrotask(()=>friendForcedDecision('P1','manual-block').catch(e=>{FR.hateBlockers.P1=String(e?.message||e)}));return toast('ヘイト爆発中は必須判断を手動変更できません')}friendTickHate(performance.now());const r=await baseTarget?.();if(FR.role==='host'&&state.match?.friendRoom){friendSyncHateClocks(performance.now());broadcastSnapshots()}return r};
const rem=document.getElementById('rematch'),baseRem=rem?.onclick;
if(rem)rem.onclick=()=>{if(state.match?.friendRoom||FR.roomState==='result'){if(FR.role==='host')return returnLobby();show('friendroom');renderLobby();return}return baseRem?.()};
const leave=document.getElementById('leaveMatch'),baseLeave=leave?.onclick;
if(leave)leave.onc

=== CHAR PATTERN 'friendCreate2' HITS_SHOWN=2 ===
--- char 10284371 context 10282571:10291371 ---
de),FR.clientId);store(authKey(FR.code),FR.authToken);renderLobby();send({type:'sync'});return}if(m.type==='reject'){FR.lastReject=m.reason||'reject';toast('参加できません: '+FR.lastReject);renderLobby();return}if(m.type==='host_close'){FR.roomState='closed';toast('部屋主が部屋を閉じました');show('friendroom');renderLobby();return}if(m.type==='lobby'){FR.participants=clone(m.lobby?.participants||[]);FR.roomState=m.lobby?.roomState||'lobby';FR.playerCount=Number(m.lobby?.playerCount)===4?4:2;FR.contentId=m.lobby?.contentId||FR.contentId;FR.guestReady=!!FR.participants.find(p=>p.pid===FR.assignedPid)?.ready;if(state.screen!=='friendroom')show('friendroom');renderLobby();return}if(m.type==='snapshot'){applyProjection(m.projection);return}if(m.type==='choice_request'){showRemoteChoice(m.choice);return}}
function renderLobby(){const root=document.getElementById('friendRoomPanel');if(!root)return;const tm=document.getElementById('topMode');if(state.screen==='friendroom'&&tm)tm.textContent='フレンドルーム';const idle=!FR.role,parts=FR.role==='host'?participantsHost():(FR.participants.length?FR.participants:pidList().map(pid=>({pid,connected:pid===FR.assignedPid,ready:false}))),role=FR.role==='host'?'部屋主':FR.role==='guest'?'参加者':'未参加',canReady=FR.roomState==='lobby'&&((FR.role==='host')||(FR.role==='guest'&&FR.assignedPid)),ownReady=FR.role==='host'?FR.hostReady:FR.guestReady;root.innerHTML=`<div class="friendRoomHero"><div><div class="k">FRIEND ROOM</div><div class="h">フレンドルーム</div></div><div class="friendCode"><span>合言葉</span><b>${FR.code||'-------'}</b><small>${role} / ${FR.playerCount}人 / ${FR.transport==='ws_relay_recipient_r21'?'遠隔WS':'同一アドレス'}</small></div></div><div class="friendRoomGrid"><div class="friendRoomCard"><h3>部屋</h3>${idle?`<div class="friendCreateRow"><button class="btn primary" id="friendCreate2">二人部屋を作る</button><button class="btn" id="friendCreate4">四人部屋を作る</button></div><label class="friendJoinLabel">七文字の合言葉<input id="friendJoinCode" maxlength="9" autocomplete="off" inputmode="text" placeholder="ABCDEFG"></label><button class="btn" id="friendJoinBtn">合言葉で入る</button>`:`<div class="friendStatus">状態: ${FR.roomState==='lobby'?'準備中':FR.roomState==='playing'?'対戦中':FR.roomState==='result'?'対戦終了':FR.roomState}</div><button class="btn danger" id="friendLeaveBtn">部屋を出る</button>`}${FR.lastReject?`<div class="friendReject">${FR.lastReject}</div>`:''}</div><div class="friendRoomCard"><h3>参加者</h3><div class="friendParticipants">${parts.map(p=>`<div class="friendParticipant ${p.connected?'connected':''} ${p.ready?'ready':''}"><b>${p.pid}</b><span>${p.connected?'接続':'空き'}</span><em>${p.ready?'準備完了':'待機'}</em></div>`).join('')}</div>${!idle?`<div class="friendActions"><button class="btn ${ownReady?'on':''}" id="friendReadyBtn" ${canReady?'':'disabled'}>${ownReady?'準備を戻す':'準備完了'}</button>${FR.role==='host'?`<button class="btn primary" id="friendStartBtn" ${allReady()?'':'disabled'}>部屋主が開始</button>`:''}</div>`:''}</div></div>`;document.getElementById('friendCreate2')?.addEventListener('click',()=>createRoom(2));document.getElementById('friendCreate4')?.addEventListener('click',()=>createRoom(4));document.getElementById('friendJoinBtn')?.addEventListener('click',()=>joinRoom(document.getElementById('friendJoinCode')?.value));document.getElementById('friendReadyBtn')?.addEventListener('click',toggleReady);document.getElementById('friendStartBtn')?.addEventListener('click',hostStart);document.getElementById('friendLeaveBtn')?.addEventListener('click',()=>{grClearPartnerMatchSession(state.match);reset(true);state.match=null;show('setup')})}
function returnLobby(){if(FR.role!=='host'||!['result','playing'].includes(FR.roomState))return false;FR.roomState='lobby';FR.hostReady=false;for(const s of Object.values(FR.slots))s.ready=false;FR.planValues={};FR.choiceWait.forEach(x=>x.reject?.(Error('rematch_reset')));FR.choiceWait.clear();grClearPartnerMatchSession(state.match);state.match=null;document.body.classList.remove('friend-room-match');show('friendroom');sendAllLobby();return true}
function friendEndMatch(winners){const m=state.match;let ranking;if(m.mode==='2v2'){const wt=winners[0];ranking=m.players.map(p=>({player:p,rank:p.team===wt?1:2,depth:Math.max(...Object.values(p.lanes).map(a=>a.length))}))}else ranking=ranksFFA();const me=ranking.find(x=>x.player.id==='P1');m.lastResult={ranking,headline:me?.rank===1?'勝利':'結果',grade:me?.rank||0,points:null,friendRoom:true};FR.roomState='result';if(FR.baseProgression!=null)state.progression={battlePoints:FR.baseProgression};if(FR.baseHistoryLength!=null&&state.history.length>FR.baseHistoryLength)state.history.splice(0,state.history.length-FR.baseHistoryLength);broadcastSnapshots();show('result');renderResult();const pts=document.getElementById('resultBattlePoints');if(pts)pts.hidden=true;return m.lastResult}
const baseResolveAbilityEntry=resolveAbilityEntry;
resolveAbilityEntry=async function(entry,phase){if(FR.role==='host'&&state.match?.friendRoom){FR.choiceActorPid=entry.ownerId;try{return await baseResolveAbilityEntry(entry,phase)}finally{FR.choiceActorPid=null}}return baseResolveAbilityEntry(entry,phase)};
const baseChooseAbilityItems=chooseAbilityItems;
chooseAbilityItems=async function(title,hint,items,min,max,human){const pid=FR.choiceActorPid;if(FR.role==='host'&&state.match?.friendRoom&&pid&&pid!=='P1'&&slotByPid(pid)?.connected)return requestRemoteChoice(pid,title,hint,items,min,max);return baseChooseAbilityItems(title,hint,items,min,max,human)};
const baseReplacementBattleCard=replacementBattleCard;
replacementBattleCard=async function(p,reason){if(FR.role==='host'&&state.match?.friendRoom&&p?.id!=='P1'&&p?.roomHuman){const opts=p.hand.filter(id=>CARDS[id]?.runtime_status==='playable').map(cardId=>({kind:'hand',cardId,id:cardId,detail:reason}));if(!opts.length)return null;const ch=await requestRemoteChoice(p.id,'バトルカードを再確認',reason,opts,1,1);return ch[0]?.cardId||null}return baseReplacementBattleCard(p,reason)};
const baseNextRound=nextRound;
nextRound=function(){const r=baseNextRound();if(FR.role==='host'&&state.match?.friendRoom){FR.planValues={};friendSyncHateClocks(performance.now());broadcastSnapshots()}return r};
const baseEndMatch=endMatch;
endMatch=function(winners){if(FR.role==='host'&&state.match?.friendRoom)return friendEndMatch(winners);return baseEndMatch(winners)};
const baseRenderBattle=renderBattle;
renderBattle=function(){const r=baseRenderBattle();if(FR.role==='guest'&&state.match?.friendRoom)renderRemoteWait();else if(FR.role==='host'&&state.match?.friendRoom)renderFriendHostBattleState();return r};
const ready=document.getElementById('readyPlan'),baseReady=ready?.onclick;
if(ready)ready.onclick=()=>{if(state.match?.friendRoom){const p=FR.role==='host'?submitHostPlan():submitGuestPlan();return Promise.resolve(p).catch(console.error)}return baseReady?.()};
const target=document.getElementById('confirmTarget'),baseTarget=target?.onclick;
if(target)target.onclick=async()=>{if(state.match?.friendRoom&&FR.role==='guest')return submitGuestTarget();if(FR.role==='host'&&state.match?.friendRoom&&state.match.players?.find(p=>p.id==='P1')?.hateExploded){queueMicrotask(()=>friendForcedDecision('P1','manual-block').catch(e=>{FR.hateBlockers.P1=String(e?.message||e)}));return toast('ヘイト爆発中は必須判断を手動変更できません')}friendTickHate(performance.now());const r=await baseTarget?.();if(FR.role==='host'&&state.match?.friendRoom){friendSyncHateClocks(performance.now());broadcastSnapshots()}return r};
const rem=document.getElementById('rematch'),baseRem=rem?.onclick;
if(rem)rem.onclick=()=>{if(state.match?.friendRoom||FR.roomState==='result'){if(FR.role==='host')return returnLobby();show('friendroom');renderLobby();return}return baseRem?.()};
const leave=document.getElementById('leaveMatch'),baseLeave=leave?.onclick;
if(leave)leave.onclick=()=>{if(state.match?.friendRoom){reset(true);state.match=null;document.body.classList.remove('friend-room-match');show('setup');return}return baseLeave?.()};
document.getElementById('partnerRule')?.addEventListener('change',()=>syncFriendPartnerRule(true));
document.getElementById('friendRoomEntry')?.addEventListener('click',()=>{show('friendroom');renderLobby()});
window.addEventListener('beforeunload',()=>closeChannel(true));
const auto=validCode(new URLSearchParams(location.search).get('friend')||'');if(auto)setTimeout(()=>joinRoom(auto),50);
function qaState(){return{build:BUILD,role:FR.role,code:FR.code,pid:FR.assignedPid,clientId:FR.clientId,transport:FR.transport,playerCount:FR.playerCount,roomState:FR.roomState,ready:FR.role==='host'?FR.hostReady:FR.guestReady,participants:FR.role==='host'?participantsHost():clone(FR.participants),lastReject:FR.lastReject,planSubmitted:FR.role==='host'?Object.keys(FR.planValues):FR.snapshot?.private?.planSubmitted||false,hateAuthority:FR.role==='h
--- char 10285547 context 10283747:10292547 ---
=='host'?'部屋主':FR.role==='guest'?'参加者':'未参加',canReady=FR.roomState==='lobby'&&((FR.role==='host')||(FR.role==='guest'&&FR.assignedPid)),ownReady=FR.role==='host'?FR.hostReady:FR.guestReady;root.innerHTML=`<div class="friendRoomHero"><div><div class="k">FRIEND ROOM</div><div class="h">フレンドルーム</div></div><div class="friendCode"><span>合言葉</span><b>${FR.code||'-------'}</b><small>${role} / ${FR.playerCount}人 / ${FR.transport==='ws_relay_recipient_r21'?'遠隔WS':'同一アドレス'}</small></div></div><div class="friendRoomGrid"><div class="friendRoomCard"><h3>部屋</h3>${idle?`<div class="friendCreateRow"><button class="btn primary" id="friendCreate2">二人部屋を作る</button><button class="btn" id="friendCreate4">四人部屋を作る</button></div><label class="friendJoinLabel">七文字の合言葉<input id="friendJoinCode" maxlength="9" autocomplete="off" inputmode="text" placeholder="ABCDEFG"></label><button class="btn" id="friendJoinBtn">合言葉で入る</button>`:`<div class="friendStatus">状態: ${FR.roomState==='lobby'?'準備中':FR.roomState==='playing'?'対戦中':FR.roomState==='result'?'対戦終了':FR.roomState}</div><button class="btn danger" id="friendLeaveBtn">部屋を出る</button>`}${FR.lastReject?`<div class="friendReject">${FR.lastReject}</div>`:''}</div><div class="friendRoomCard"><h3>参加者</h3><div class="friendParticipants">${parts.map(p=>`<div class="friendParticipant ${p.connected?'connected':''} ${p.ready?'ready':''}"><b>${p.pid}</b><span>${p.connected?'接続':'空き'}</span><em>${p.ready?'準備完了':'待機'}</em></div>`).join('')}</div>${!idle?`<div class="friendActions"><button class="btn ${ownReady?'on':''}" id="friendReadyBtn" ${canReady?'':'disabled'}>${ownReady?'準備を戻す':'準備完了'}</button>${FR.role==='host'?`<button class="btn primary" id="friendStartBtn" ${allReady()?'':'disabled'}>部屋主が開始</button>`:''}</div>`:''}</div></div>`;document.getElementById('friendCreate2')?.addEventListener('click',()=>createRoom(2));document.getElementById('friendCreate4')?.addEventListener('click',()=>createRoom(4));document.getElementById('friendJoinBtn')?.addEventListener('click',()=>joinRoom(document.getElementById('friendJoinCode')?.value));document.getElementById('friendReadyBtn')?.addEventListener('click',toggleReady);document.getElementById('friendStartBtn')?.addEventListener('click',hostStart);document.getElementById('friendLeaveBtn')?.addEventListener('click',()=>{grClearPartnerMatchSession(state.match);reset(true);state.match=null;show('setup')})}
function returnLobby(){if(FR.role!=='host'||!['result','playing'].includes(FR.roomState))return false;FR.roomState='lobby';FR.hostReady=false;for(const s of Object.values(FR.slots))s.ready=false;FR.planValues={};FR.choiceWait.forEach(x=>x.reject?.(Error('rematch_reset')));FR.choiceWait.clear();grClearPartnerMatchSession(state.match);state.match=null;document.body.classList.remove('friend-room-match');show('friendroom');sendAllLobby();return true}
function friendEndMatch(winners){const m=state.match;let ranking;if(m.mode==='2v2'){const wt=winners[0];ranking=m.players.map(p=>({player:p,rank:p.team===wt?1:2,depth:Math.max(...Object.values(p.lanes).map(a=>a.length))}))}else ranking=ranksFFA();const me=ranking.find(x=>x.player.id==='P1');m.lastResult={ranking,headline:me?.rank===1?'勝利':'結果',grade:me?.rank||0,points:null,friendRoom:true};FR.roomState='result';if(FR.baseProgression!=null)state.progression={battlePoints:FR.baseProgression};if(FR.baseHistoryLength!=null&&state.history.length>FR.baseHistoryLength)state.history.splice(0,state.history.length-FR.baseHistoryLength);broadcastSnapshots();show('result');renderResult();const pts=document.getElementById('resultBattlePoints');if(pts)pts.hidden=true;return m.lastResult}
const baseResolveAbilityEntry=resolveAbilityEntry;
resolveAbilityEntry=async function(entry,phase){if(FR.role==='host'&&state.match?.friendRoom){FR.choiceActorPid=entry.ownerId;try{return await baseResolveAbilityEntry(entry,phase)}finally{FR.choiceActorPid=null}}return baseResolveAbilityEntry(entry,phase)};
const baseChooseAbilityItems=chooseAbilityItems;
chooseAbilityItems=async function(title,hint,items,min,max,human){const pid=FR.choiceActorPid;if(FR.role==='host'&&state.match?.friendRoom&&pid&&pid!=='P1'&&slotByPid(pid)?.connected)return requestRemoteChoice(pid,title,hint,items,min,max);return baseChooseAbilityItems(title,hint,items,min,max,human)};
const baseReplacementBattleCard=replacementBattleCard;
replacementBattleCard=async function(p,reason){if(FR.role==='host'&&state.match?.friendRoom&&p?.id!=='P1'&&p?.roomHuman){const opts=p.hand.filter(id=>CARDS[id]?.runtime_status==='playable').map(cardId=>({kind:'hand',cardId,id:cardId,detail:reason}));if(!opts.length)return null;const ch=await requestRemoteChoice(p.id,'バトルカードを再確認',reason,opts,1,1);return ch[0]?.cardId||null}return baseReplacementBattleCard(p,reason)};
const baseNextRound=nextRound;
nextRound=function(){const r=baseNextRound();if(FR.role==='host'&&state.match?.friendRoom){FR.planValues={};friendSyncHateClocks(performance.now());broadcastSnapshots()}return r};
const baseEndMatch=endMatch;
endMatch=function(winners){if(FR.role==='host'&&state.match?.friendRoom)return friendEndMatch(winners);return baseEndMatch(winners)};
const baseRenderBattle=renderBattle;
renderBattle=function(){const r=baseRenderBattle();if(FR.role==='guest'&&state.match?.friendRoom)renderRemoteWait();else if(FR.role==='host'&&state.match?.friendRoom)renderFriendHostBattleState();return r};
const ready=document.getElementById('readyPlan'),baseReady=ready?.onclick;
if(ready)ready.onclick=()=>{if(state.match?.friendRoom){const p=FR.role==='host'?submitHostPlan():submitGuestPlan();return Promise.resolve(p).catch(console.error)}return baseReady?.()};
const target=document.getElementById('confirmTarget'),baseTarget=target?.onclick;
if(target)target.onclick=async()=>{if(state.match?.friendRoom&&FR.role==='guest')return submitGuestTarget();if(FR.role==='host'&&state.match?.friendRoom&&state.match.players?.find(p=>p.id==='P1')?.hateExploded){queueMicrotask(()=>friendForcedDecision('P1','manual-block').catch(e=>{FR.hateBlockers.P1=String(e?.message||e)}));return toast('ヘイト爆発中は必須判断を手動変更できません')}friendTickHate(performance.now());const r=await baseTarget?.();if(FR.role==='host'&&state.match?.friendRoom){friendSyncHateClocks(performance.now());broadcastSnapshots()}return r};
const rem=document.getElementById('rematch'),baseRem=rem?.onclick;
if(rem)rem.onclick=()=>{if(state.match?.friendRoom||FR.roomState==='result'){if(FR.role==='host')return returnLobby();show('friendroom');renderLobby();return}return baseRem?.()};
const leave=document.getElementById('leaveMatch'),baseLeave=leave?.onclick;
if(leave)leave.onclick=()=>{if(state.match?.friendRoom){reset(true);state.match=null;document.body.classList.remove('friend-room-match');show('setup');return}return baseLeave?.()};
document.getElementById('partnerRule')?.addEventListener('change',()=>syncFriendPartnerRule(true));
document.getElementById('friendRoomEntry')?.addEventListener('click',()=>{show('friendroom');renderLobby()});
window.addEventListener('beforeunload',()=>closeChannel(true));
const auto=validCode(new URLSearchParams(location.search).get('friend')||'');if(auto)setTimeout(()=>joinRoom(auto),50);
function qaState(){return{build:BUILD,role:FR.role,code:FR.code,pid:FR.assignedPid,clientId:FR.clientId,transport:FR.transport,playerCount:FR.playerCount,roomState:FR.roomState,ready:FR.role==='host'?FR.hostReady:FR.guestReady,participants:FR.role==='host'?participantsHost():clone(FR.participants),lastReject:FR.lastReject,planSubmitted:FR.role==='host'?Object.keys(FR.planValues):FR.snapshot?.private?.planSubmitted||false,hateAuthority:FR.role==='host'?{values:Object.fromEntries((state.match?.players||[]).filter(p=>p.roomHuman).map(p=>[p.id,{hateTime:Number(p.hateTime)||0,hateExploded:!!p.hateExploded,hateExplosionCount:Number(p.hateExplosionCount)||0,signature:friendHateSignature(p.id),rule:friendRule(p.id),blocker:FR.hateBlockers[p.id]||null}])),clockOwner:'host'}:{private:clone(FR.snapshot?.private||null),clockOwner:'host_snapshot'},match:state.match?{mode:state.match.mode,phase:state.match.phase,round:state.match.round,activeId:state.match.activeId,friend:!!state.match.friendRoom,players:state.match.players.map(p=>({id:p.id,human:p.human,roomHuman:!!p.roomHuman,hand:p.hand.length,position:p.position})),historyLength:state.history.length,battlePoints:Number(state.progression?.battlePoints||0)}:null}}
window.GAMEROAD_FRIEND_ROOM_R2={BUILD,create:createRoom,join:joinRoom,ready:toggleReady,start:hostStart,leave:()=>reset(true),returnLobby,status:qaState,projectFor:pid=>FR.role==='host'?project(pid):null,rawSend:o=>send(o),submitPlan:()=>FR.role==='host'?submitHostPlan():submitGuestPlan(),submitTarget:submitGuestTarget,forceSnapshot:()=>{if(FR.role==='host')broadcastSnapshots()},qaSetFast:()=>{battlePr

=== CHAR PATTERN 'friendRoom:true' HITS_SHOWN=2 ===
--- char 10269668 context 10267868:10276668 ---
target:clone(pr.target),log:[...pr.log],lastResult:null,lastBattleResolution:clone(pr.lastBattleResolution),battlePresentation:clone(pr.battlePresentation),resolutionSeq:pr.resolutionSeq,projection:['N','E','S','W'],abilityRng:1,busy:!!pr.busy,honey:{byNode:clone(pr.honey?.byNode||{}),receipts:new Set(),generatedRounds:new Set()},participants:new Map(pr.players.map(p=>[p.id,[]])),participantSeq:0,resolvedAbilityKeys:new Set(),abilityOrderReceipts:new Map(),testAutoChoices:false,quickWindow:null,quickPointsByPlayer:{},quickAwards:[],playerDeckRule:{id:pr.selectionContract?.regulationId||DECK_RULE.id,revision:pr.selectionContract?.regulationRevision||DECK_RULE.revision,mainCount:Number(pr.selectionContract?.mainCount)||Number(pr.players?.find(x=>x.id===pr.viewer)?.sourceDeckSize)||0,exCount:Number(pr.selectionContract?.exCount)||0},friendRoom:{build:BUILD,code:pr.code,playerCount:pr.playerCount,localPid:pr.viewer,host:false,resultPersistenceEnabled:false,noBotFill:true}};m.selectionLock=Object.freeze({version:1,playerId:pr.viewer,mode:m.mode,contentId:m.contentId,regulationId:m.playerDeckRule.id,regulationRevision:m.playerDeckRule.revision,mainCount:m.playerDeckRule.mainCount,exCount:m.playerDeckRule.exCount,sourceDeckDigest:null});const me=m.players.find(x=>x.id===pr.viewer);if(me){me.hateTime=Math.max(0,Math.min(1000,Number(pr.private?.hateTime)||0));me.hateExploded=!!pr.private?.hateExploded;me.hateExplosionCount=Math.max(0,Number(pr.private?.hateExplosionCount)||0)}if(pr.result){const ranking=(pr.result.ranking||[]).map(r=>{const pp=m.players.find(x=>x.id===r.player?.id);return{player:pp,rank:r.rank,depth:r.depth}}).filter(r=>r.player);const me=ranking.find(r=>r.player.id===pr.viewer);m.lastResult={ranking,headline:me?.rank===1?'勝利':'結果',grade:me?.rank||0,points:null,friendRoom:true}}return m}
function applyProjection(pr){if(!pr||pr.viewer!==FR.assignedPid)return;FR.snapshot=pr;FR.roomState=pr.roomState;state.match=projectionToMatch(pr);document.body.classList.add('friend-room-match');const pts=document.getElementById('resultBattlePoints');if(pts)pts.hidden=true;if(pr.roomState==='result'){show('result');renderResult();if(pts)pts.hidden=true;const h=document.getElementById('resultPointsEarned');if(h)h.textContent='フレンド対戦・報酬なし'}else{show('battle');renderBattle();renderRemoteWait();syncFriendPartnerRule()} }
function renderRemoteWait(){if(FR.role!=='guest'||state.screen!=='battle')return;const b=document.getElementById('readyPlan'),pr=FR.snapshot;if(b&&pr?.private?.planSubmitted){b.disabled=true;b.querySelector('.quickReadyText')&&(b.querySelector('.quickReadyText').textContent='予約済み・全員待ち')}const act=state.match?.players?.find(p=>p.id===state.match.activeId);if(state.match?.phase==='target'&&act?.id!==FR.assignedPid){document.getElementById('targetBox')?.classList.remove('on')}}
function renderFriendHostBattleState(){if(FR.role!=='host'||state.screen!=='battle'||!state.match?.friendRoom)return;const b=document.getElementById('readyPlan');if(b&&FR.planValues.P1){b.disabled=true;b.querySelector('.quickReadyText')&&(b.querySelector('.quickReadyText').textContent='予約済み・全員待ち')}}
function legalPlan(p,plan){if(!p||!plan?.roadId||!plan?.battleId)return{ok:false,reason:'plan_missing'};if(plan.roadId===plan.battleId)return{ok:false,reason:'same_card'};if(!p.hand.includes(plan.roadId)||!p.hand.includes(plan.battleId))return{ok:false,reason:'reservation_not_owned'};if(!roadSelectable(plan.roadId)||!roadSelectable(plan.battleId))return{ok:false,reason:'card_not_selectable'};const path=Array.isArray(plan.path)&&plan.path.length?plan.path:[p.position];if(path[0]!==p.position)return{ok:false,reason:'path_origin_mismatch'};const lim=normalMoveLimitForRoad(plan.roadId);if(path.length-1>lim)return{ok:false,reason:'move_limit'};for(let i=1;i<path.length;i++)if(!neighbors(path[i-1],p).includes(path[i])||path.slice(0,i).includes(path[i]))return{ok:false,reason:'path_invalid'};return{ok:true,plan:{roadId:plan.roadId,battleId:plan.battleId,path:[...path]}}}
async function submitHostPlan(){const m=state.match,p=m?.players?.find(x=>x.id==='P1');if(!m?.friendRoom||FR.role!=='host'||m.phase!=='plan'||m.busy)return false;if(p?.hateExploded){queueMicrotask(()=>friendForcedDecision('P1','manual-block').catch(e=>{FR.hateBlockers.P1=String(e?.message||e)}));return toast('ヘイト爆発中は必須判断を手動変更できません')}friendTickHate(performance.now());updatePlanFromSelect();const v=legalPlan(p,p.plan);if(!v.ok){toast('予約内容を確認してください');return false}if(FR.planValues.P1)return false;FR.planValues.P1=v.plan;window.GAMEROAD_PARTNER_COMMENT?.emit?.({eventId:`${m.id}:set:${m.round}:road:${p.id}`,kind:'dialogue',speechIntent:'road_set',cardId:v.plan.roadId,loggable:false,ttlMs:9000});window.GAMEROAD_PARTNER_COMMENT?.emit?.({eventId:`${m.id}:set:${m.round}:battle:${p.id}`,kind:'dialogue',speechIntent:'battle_set',cardId:v.plan.battleId,loggable:false,ttlMs:9000});friendSyncHateClocks(performance.now());broadcastSnapshots();await maybeResolvePlans();return true}
function submitGuestPlan(){const m=state.match,p=m?.players?.find(x=>x.id===FR.assignedPid);if(FR.role!=='guest'||!m?.friendRoom||m.phase!=='plan'||m.busy||FR.snapshot?.private?.planSubmitted)return false;if(FR.snapshot?.private?.hateExploded)return toast('ヘイト爆発中は必須判断を手動変更できません');updatePlanFromSelect();const v=legalPlan(p,p.plan);if(!v.ok){toast('予約内容を確認してください');return false}window.GAMEROAD_PARTNER_COMMENT?.emit?.({eventId:`${m.id}:set:${m.round}:road:${p.id}`,kind:'dialogue',speechIntent:'road_set',cardId:v.plan.roadId,loggable:false,ttlMs:9000});window.GAMEROAD_PARTNER_COMMENT?.emit?.({eventId:`${m.id}:set:${m.round}:battle:${p.id}`,kind:'dialogue',speechIntent:'battle_set',cardId:v.plan.battleId,loggable:false,ttlMs:9000});send({type:'plan',round:m.round,phase:m.phase,payload:v.plan});return true}
async function maybeResolvePlans(){if(FR.role!=='host'||!state.match||state.match.phase!=='plan')return;const need=pidList();if(!need.every(pid=>FR.planValues[pid]))return;const plans=Object.fromEntries(need.map(pid=>[pid,clone(FR.planValues[pid])]));FR.planValues={};await resolveHumanPlans(plans)}
async function resolveHumanPlans(plans){const m=state.match;if(m.busy)return;m.busy=true;try{if(!battleMount)await updateBattleAvatar();for(const p of m.players){const v=legalPlan(p,plans[p.id]);if(!v.ok)throw Error(p.id+':'+v.reason);p.plan=v.plan}m.lastBattleResolution=null;m.players.forEach(p=>removeHand(p,p.plan.roadId));m.activeId=pickActive(m.players).id;const roadEntries=m.players.map(p=>makeParticipant(p,p.plan.roadId,'road'));m.phase='reveal';log('ロードカードを公開。公開時能力を小さい印刷値順で解決します。');renderBattle();broadcastSnapshots();await resolveAbilityBundle(roadEntries,'road');if(m.contentId==='honey_hunt'&&!m.honey.generatedRounds.has(m.round)){spawnHoneyFromRoads();log('公開された1〜6と、列上の同数字ロードカード実体を照合してハニー発生源を更新しました。')}m.phase='move';renderBattle();broadcastSnapshots();const order=randomOrderByRoad(m.players);for(const p of order){await animateResolvedPath(p);log(`${p.name}：${cardLabel(p.plan.roadId)} / ${p.plan.path.length-1}歩`)}const act=m.players.find(p=>p.id===m.activeId);m.phase='target';m.busy=false;friendSyncHateClocks(performance.now());log(`${act.name}が攻撃権を取得。`);renderBattle();broadcastSnapshots()}catch(e){m.busy=false;log(`処理停止：${e.message}`);renderBattle();broadcastSnapshots();throw e}}
async function submitGuestTarget(){const m=state.match,act=m?.players?.find(p=>p.id===m.activeId);if(FR.role!=='guest'||m?.phase!=='target'||act?.id!==FR.assignedPid)return false;if(FR.snapshot?.private?.hateExploded)return toast('ヘイト爆発中は必須判断を手動変更できません');const defenderId=document.getElementById('targetPlayer').value;if(!legalOpponents(act).some(p=>p.id===defenderId))return false;send({type:'target',round:m.round,phase:m.phase,payload:{defenderId,lane:document.getElementById('targetLane').value,shield:document.getElementById('targetShield').value}});return true}
function choicePayload(items){return items.map((it,i)=>({i,label:abilityOptionLabel(it),detail:String(it.detail||'')}))}
function requestRemoteChoice(pid,title,hint,items,min,max){const s=slotByPid(pid);if(!s?.connected)return Promise.reject(Error('room_participant_disconnected:'+pid));const id='choice-'+(++FR.choiceSeq);return new Promise((resolve,reject)=>{const timer=setTimeout(()=>{FR.choiceWait.delete(id);reject(Error('room_choice_timeout:'+pid))},120000);FR.choiceWait.set(id,{pid,items,min,max,resolve:(indices)=>{clearTimeout(timer);FR.choiceWait.delete(id);friendSyncHateClocks(performance.now());resolve(indices.map(i=>items[i]).filter(Boolean))},reject});friendSyncHateClocks(performance.now());const p=state.match?.players?.find(x=>x.id===pid);if(p?.hateExploded){queueMicrotask(()=>friendForcedDecision(pid,'hate-forced-choice').catch(e=>{FR.hateBlockers[pid]=String(e?.message||e);broadcastSnapshots()}));
--- char 10286971 context 10285171:10293971 ---
m>${p.ready?'準備完了':'待機'}</em></div>`).join('')}</div>${!idle?`<div class="friendActions"><button class="btn ${ownReady?'on':''}" id="friendReadyBtn" ${canReady?'':'disabled'}>${ownReady?'準備を戻す':'準備完了'}</button>${FR.role==='host'?`<button class="btn primary" id="friendStartBtn" ${allReady()?'':'disabled'}>部屋主が開始</button>`:''}</div>`:''}</div></div>`;document.getElementById('friendCreate2')?.addEventListener('click',()=>createRoom(2));document.getElementById('friendCreate4')?.addEventListener('click',()=>createRoom(4));document.getElementById('friendJoinBtn')?.addEventListener('click',()=>joinRoom(document.getElementById('friendJoinCode')?.value));document.getElementById('friendReadyBtn')?.addEventListener('click',toggleReady);document.getElementById('friendStartBtn')?.addEventListener('click',hostStart);document.getElementById('friendLeaveBtn')?.addEventListener('click',()=>{grClearPartnerMatchSession(state.match);reset(true);state.match=null;show('setup')})}
function returnLobby(){if(FR.role!=='host'||!['result','playing'].includes(FR.roomState))return false;FR.roomState='lobby';FR.hostReady=false;for(const s of Object.values(FR.slots))s.ready=false;FR.planValues={};FR.choiceWait.forEach(x=>x.reject?.(Error('rematch_reset')));FR.choiceWait.clear();grClearPartnerMatchSession(state.match);state.match=null;document.body.classList.remove('friend-room-match');show('friendroom');sendAllLobby();return true}
function friendEndMatch(winners){const m=state.match;let ranking;if(m.mode==='2v2'){const wt=winners[0];ranking=m.players.map(p=>({player:p,rank:p.team===wt?1:2,depth:Math.max(...Object.values(p.lanes).map(a=>a.length))}))}else ranking=ranksFFA();const me=ranking.find(x=>x.player.id==='P1');m.lastResult={ranking,headline:me?.rank===1?'勝利':'結果',grade:me?.rank||0,points:null,friendRoom:true};FR.roomState='result';if(FR.baseProgression!=null)state.progression={battlePoints:FR.baseProgression};if(FR.baseHistoryLength!=null&&state.history.length>FR.baseHistoryLength)state.history.splice(0,state.history.length-FR.baseHistoryLength);broadcastSnapshots();show('result');renderResult();const pts=document.getElementById('resultBattlePoints');if(pts)pts.hidden=true;return m.lastResult}
const baseResolveAbilityEntry=resolveAbilityEntry;
resolveAbilityEntry=async function(entry,phase){if(FR.role==='host'&&state.match?.friendRoom){FR.choiceActorPid=entry.ownerId;try{return await baseResolveAbilityEntry(entry,phase)}finally{FR.choiceActorPid=null}}return baseResolveAbilityEntry(entry,phase)};
const baseChooseAbilityItems=chooseAbilityItems;
chooseAbilityItems=async function(title,hint,items,min,max,human){const pid=FR.choiceActorPid;if(FR.role==='host'&&state.match?.friendRoom&&pid&&pid!=='P1'&&slotByPid(pid)?.connected)return requestRemoteChoice(pid,title,hint,items,min,max);return baseChooseAbilityItems(title,hint,items,min,max,human)};
const baseReplacementBattleCard=replacementBattleCard;
replacementBattleCard=async function(p,reason){if(FR.role==='host'&&state.match?.friendRoom&&p?.id!=='P1'&&p?.roomHuman){const opts=p.hand.filter(id=>CARDS[id]?.runtime_status==='playable').map(cardId=>({kind:'hand',cardId,id:cardId,detail:reason}));if(!opts.length)return null;const ch=await requestRemoteChoice(p.id,'バトルカードを再確認',reason,opts,1,1);return ch[0]?.cardId||null}return baseReplacementBattleCard(p,reason)};
const baseNextRound=nextRound;
nextRound=function(){const r=baseNextRound();if(FR.role==='host'&&state.match?.friendRoom){FR.planValues={};friendSyncHateClocks(performance.now());broadcastSnapshots()}return r};
const baseEndMatch=endMatch;
endMatch=function(winners){if(FR.role==='host'&&state.match?.friendRoom)return friendEndMatch(winners);return baseEndMatch(winners)};
const baseRenderBattle=renderBattle;
renderBattle=function(){const r=baseRenderBattle();if(FR.role==='guest'&&state.match?.friendRoom)renderRemoteWait();else if(FR.role==='host'&&state.match?.friendRoom)renderFriendHostBattleState();return r};
const ready=document.getElementById('readyPlan'),baseReady=ready?.onclick;
if(ready)ready.onclick=()=>{if(state.match?.friendRoom){const p=FR.role==='host'?submitHostPlan():submitGuestPlan();return Promise.resolve(p).catch(console.error)}return baseReady?.()};
const target=document.getElementById('confirmTarget'),baseTarget=target?.onclick;
if(target)target.onclick=async()=>{if(state.match?.friendRoom&&FR.role==='guest')return submitGuestTarget();if(FR.role==='host'&&state.match?.friendRoom&&state.match.players?.find(p=>p.id==='P1')?.hateExploded){queueMicrotask(()=>friendForcedDecision('P1','manual-block').catch(e=>{FR.hateBlockers.P1=String(e?.message||e)}));return toast('ヘイト爆発中は必須判断を手動変更できません')}friendTickHate(performance.now());const r=await baseTarget?.();if(FR.role==='host'&&state.match?.friendRoom){friendSyncHateClocks(performance.now());broadcastSnapshots()}return r};
const rem=document.getElementById('rematch'),baseRem=rem?.onclick;
if(rem)rem.onclick=()=>{if(state.match?.friendRoom||FR.roomState==='result'){if(FR.role==='host')return returnLobby();show('friendroom');renderLobby();return}return baseRem?.()};
const leave=document.getElementById('leaveMatch'),baseLeave=leave?.onclick;
if(leave)leave.onclick=()=>{if(state.match?.friendRoom){reset(true);state.match=null;document.body.classList.remove('friend-room-match');show('setup');return}return baseLeave?.()};
document.getElementById('partnerRule')?.addEventListener('change',()=>syncFriendPartnerRule(true));
document.getElementById('friendRoomEntry')?.addEventListener('click',()=>{show('friendroom');renderLobby()});
window.addEventListener('beforeunload',()=>closeChannel(true));
const auto=validCode(new URLSearchParams(location.search).get('friend')||'');if(auto)setTimeout(()=>joinRoom(auto),50);
function qaState(){return{build:BUILD,role:FR.role,code:FR.code,pid:FR.assignedPid,clientId:FR.clientId,transport:FR.transport,playerCount:FR.playerCount,roomState:FR.roomState,ready:FR.role==='host'?FR.hostReady:FR.guestReady,participants:FR.role==='host'?participantsHost():clone(FR.participants),lastReject:FR.lastReject,planSubmitted:FR.role==='host'?Object.keys(FR.planValues):FR.snapshot?.private?.planSubmitted||false,hateAuthority:FR.role==='host'?{values:Object.fromEntries((state.match?.players||[]).filter(p=>p.roomHuman).map(p=>[p.id,{hateTime:Number(p.hateTime)||0,hateExploded:!!p.hateExploded,hateExplosionCount:Number(p.hateExplosionCount)||0,signature:friendHateSignature(p.id),rule:friendRule(p.id),blocker:FR.hateBlockers[p.id]||null}])),clockOwner:'host'}:{private:clone(FR.snapshot?.private||null),clockOwner:'host_snapshot'},match:state.match?{mode:state.match.mode,phase:state.match.phase,round:state.match.round,activeId:state.match.activeId,friend:!!state.match.friendRoom,players:state.match.players.map(p=>({id:p.id,human:p.human,roomHuman:!!p.roomHuman,hand:p.hand.length,position:p.position})),historyLength:state.history.length,battlePoints:Number(state.progression?.battlePoints||0)}:null}}
window.GAMEROAD_FRIEND_ROOM_R2={BUILD,create:createRoom,join:joinRoom,ready:toggleReady,start:hostStart,leave:()=>reset(true),returnLobby,status:qaState,projectFor:pid=>FR.role==='host'?project(pid):null,rawSend:o=>send(o),submitPlan:()=>FR.role==='host'?submitHostPlan():submitGuestPlan(),submitTarget:submitGuestTarget,forceSnapshot:()=>{if(FR.role==='host')broadcastSnapshots()},qaSetFast:()=>{battlePresentationRuntime.fast=true;return true},qaSetNoEffectHands:()=>{if(FR.role!=='host'||!state.match?.friendRoom)return null;const out={};for(const p of state.match.players){const pool=[...p.hand,...p.deck],picked=[];for(const id of pool){if(roadSelectable(id)&&CARDS[id]?.runtime_status==='playable'&&CARDS[id]?.effect_type==='none'&&!picked.includes(id)){picked.push(id);if(picked.length===3)break}}if(picked.length<2)throw Error('not-enough-noeffect-'+p.id);const remain=[...pool];for(const id of picked){const i=remain.indexOf(id);if(i>=0)remain.splice(i,1)}p.hand=picked;p.deck=remain;out[p.id]=[...picked]}broadcastSnapshots();renderBattle();return out},qaPlans:()=>state.match?{activeId:state.match.activeId,phase:state.match.phase,round:state.match.round,plans:state.match.players.map(p=>({id:p.id,roadId:p.plan?.roadId||null,battleId:p.plan?.battleId||null,path:[...(p.plan?.path||[])]}))}:null,qaProgress:()=>({battlePoints:Number(state.progression?.battlePoints||0),historyLength:state.history.length}),qaForceEnd:(winner='P1')=>endMatch([winner]),qaStandardStart:(mode='2p')=>{state.match=null;state.setupMode=mode;state.setupContent='road_shield';const m=startMatch();return{mode:m?.mode||null,players:m?.players?.length||0,human:m?.players?.filter(p=>p.human).length||0,friend:!!m?.friendRoom,nodes:document.querySelectorAll('#board .node').length,conservation:cardConservation(),projection:window.__GAMEROAD_TES

=== CHAR PATTERN 'm.friendRoom' HITS_SHOWN=2 ===
--- char 10178120 context 10176320:10185120 ---
adSelect').value=p.plan.roadId;$('#battleSelect').value=p.plan.battleId;$('#endpointText').textContent=p.plan.path.at(-1);pd.advice=`${reason}：${cardLabel(p.plan.roadId)} / ${cardLabel(p.plan.battleId)} / ${p.plan.path.length-1}歩を確定します。`;renderBattle();await commitPlans();return true}
async function autoTargetByPartner(reason){const m=state.match,pd=ensurePartnerRuntime(),act=m?.players.find(p=>p.id===m.activeId);if(!m||m.phase!=='target'||!act?.human)return false;const rule=pd.rule;if(!rule)throw Error('作戦規則の初期値が未裁定です');const t=replayFor(targetPlayerCandidates(),rule);if(!t.ok||!t.selected)throw Error('攻撃対象:'+String(t.error||'候補なし'));if(rule==='max'||rule==='min')throw Error('攻撃対象の最大/最小評価値が未裁定です');const lane=replayFor(fixedLaneCandidates('lane'),rule),shield=replayFor(fixedLaneCandidates('shield'),rule);if(!lane.ok||!shield.ok)throw Error('列/シールド判断:'+String(lane.error||shield.error));m.target={defenderId:t.selected.candidateId,lane:lane.selected.candidateId,shield:shield.selected.candidateId};pd.advice=`${reason}：${t.selected.payload.player.name} / ${m.target.lane}列 / ${m.target.shield}シールドを確定します。`;renderPartnerRuntime();await resolveBattle();return true}
function grPartnerSelectionGuard(reason='delegate'){const m=state.match;if(!m)return false;if(!m.selectionLock){const local=(m.players||[]).find(p=>p.human)||m.players?.[0];m.selectionLock=grCreateSelectionLock(m,local?.id||'P1')}const check=grVerifySelectionLock(m);if(check.ok)return true;const pd=ensurePartnerRuntime(m);if(pd){const msg=`試合開始時の札組・モード保持に失敗(${check.reason})`;if(pd.blocker!==msg){pd.blocker=msg;log(`相棒委任停止：${msg}`)}renderPartnerRuntime()}return false}
async function attemptPartnerAutoDecision(reason='delegate'){
  const m=state.match,pd=ensurePartnerRuntime();if(!m||!partnerDelegated())return false;if(m.friendRoom&&partnerForced())return false;if(!grPartnerSelectionGuard(reason))return false;if(pd.ability){if(typeof pd.ability.autoResolve==='function')return pd.ability.autoResolve(reason);return false}if(m.phase==='plan'&&!m.busy)return autoPlanByPartner(reason);const act=m.players.find(p=>p.id===m.activeId);if(m.phase==='target'&&!m.busy&&act?.human)return autoTargetByPartner(reason);return false
}
function makeAbilityPartnerCandidates(items){return normalizePartnerCandidates(items.map((item,i)=>({ ...item,id:partnerCandidateId(item,i),positionOrder:i,comparisonValue:item?.cardId&&CARDS[item.cardId]?Number(CARDS[item.cardId].power):null,assetAction:PARTNER_ASSET_NONE,publicScope:true,legal:true})),'ability')}
function resolvePartnerMulti(items,min,max,rule){if(min!==max)return{ok:false,error:'VARIABLE_COUNT_UNRESOLVED'};let remain=makeAbilityPartnerCandidates(items),picked=[];for(let n=0;n<min;n++){const r=replayFor(remain,rule);if(!r.ok||!r.selected)return{ok:false,error:r.error||'NO_CANDIDATE'};picked.push(r.selected.payload);remain=remain.filter(c=>c.candidateId!==r.selected.candidateId)}return{ok:true,picked}}
const __grBaseChooseAbilityItems=chooseAbilityItems;
chooseAbilityItems=async function(title,hint,items,min,max,human){if(!human)return __grBaseChooseAbilityItems(title,hint,items,min,max,human);const m=state.match,pd=ensurePartnerRuntime();if(!m)return __grBaseChooseAbilityItems(title,hint,items,min,max,human);const ctxId=`${m.round}:${title}:${++pd.sessionSeq}`;return await new Promise(resolve=>{const veil=$('#abilityVeil'),root=$('#abilityChoices'),confirm=$('#abilityConfirm'),count=$('#abilityCount');$('#abilityTitle').textContent=title;$('#abilityHint').textContent=hint;root.innerHTML='';const selected=new Set(),finish=vals=>{veil.classList.remove('on');veil.setAttribute('aria-hidden','true');confirm.onclick=null;pd.ability=null;syncPartnerDecisionSession();renderPartnerRuntime();resolve(vals)},refresh=()=>{count.textContent=min===max?`選択 ${selected.size} / ${max}`:`選択 ${selected.size}枚 / 必須${min}枚以上・最大${max}枚`;confirm.disabled=selected.size<min||selected.size>max};items.forEach((item,i)=>{const b=document.createElement('button');b.className='abilityChoice';b.innerHTML=`${abilityOptionLabel(item)}${item.detail?`<small>${item.detail}</small>`:''}`;b.onclick=()=>{if(partnerForced())return toast('ヘイト爆発中は必須判断を手動変更できません');if(selected.has(i)){selected.delete(i);b.classList.remove('on')}else if(selected.size<max){selected.add(i);b.classList.add('on')}refresh()};root.appendChild(b)});confirm.onclick=()=>{if(confirm.disabled)return;if(partnerForced())return toast('ヘイト爆発中は必須判断を手動変更できません');finish([...selected].sort((a,b)=>a-b).map(i=>items[i]))};pd.ability={id:ctxId,items,min,max,autoResolve:(why)=>{const rule=pd.rule;if(!rule){pd.blocker='作戦規則の初期値が未裁定です';renderPartnerRuntime();return false}const r=resolvePartnerMulti(items,min,max,rule);if(!r.ok){pd.blocker=`能力自動判断未確定:${r.error}`;pd.advice=min!==max?'任意枚数能力の自動選択枚数が未裁定のため、勝手に枚数を決めません。':`能力候補の${rule}比較値が未裁定です。`;renderPartnerRuntime();return false}pd.advice=`${why}：能力選択を正式候補から${r.picked.length}件確定します。`;finish(r.picked);return true}};veil.classList.add('on');veil.setAttribute('aria-hidden','false');refresh();syncPartnerDecisionSession();renderPartnerRuntime();if(partnerDelegated())queueMicrotask(()=>pd.ability?.autoResolve(partnerForced()?'hate-forced':'voluntary-delegate'))})}
function installPartnerUi(){if($('#hateMeter'))return;const strip=document.querySelector('.battleTopStatus .resourceStrip');if(strip){const h=document.createElement('span');h.id='hateMeter';h.textContent='ヘイト 0/1000';strip.appendChild(h)}const rail=document.querySelector('.battleRail');if(rail){const advice=document.createElement('button');advice.id='partnerAdviceBtn';advice.className='railBtn';advice.textContent='アドバイス';const del=document.createElement('button');del.id='partnerDelegateBtn';del.className='railBtn';del.textContent='まかせた';rail.insertBefore(del,rail.firstChild);rail.insertBefore(advice,rail.firstChild)}const drawer=$('#battleDrawer');if(drawer){const box=document.createElement('section');box.id='partnerDecisionBox';box.innerHTML='<div class="k">PARTNER / HATE</div><label class="partnerRuleLabel">作戦規則 <select id="partnerRule"><option value="">未選択</option><option value="left">左端</option><option value="right">右端</option><option value="max">最大</option><option value="min">最小</option></select></label><div id="partnerDecisionStatus" class="partnerDecisionStatus"></div><div class="sub">最大/最小の対象評価と任意枚数能力は未裁定なら自動化しません。</div>';drawer.insertBefore(box,$('#players'))}const st=document.createElement('style');st.textContent='#partnerDecisionBox{padding:8px;border:1px solid var(--line);background:#071d18;margin-bottom:6px}.partnerRuleLabel{display:grid;grid-template-columns:auto 1fr;gap:6px;align-items:center;font-size:9px}.partnerRuleLabel select{height:44px;background:#0b231d;border:1px solid var(--line);color:inherit}.partnerDecisionStatus{font-size:9px;line-height:1.4;margin-top:6px;white-space:pre-wrap}#hateMeter.hot{border-color:#ff9c86;color:#ffd5ca}#partnerDelegateBtn.forced{border-color:#ff7b66;background:#52251e}';document.head.appendChild(st);$('#partnerRule').onchange=()=>{const pd=ensurePartnerRuntime();pd.rule=$('#partnerRule').value||null;pd.blocker=null;grPersistPartnerMatchSession();renderPartnerRuntime()};$('#partnerAdviceBtn').onclick=partnerAdviceNow;$('#partnerDelegateBtn').onclick=()=>{const pd=ensurePartnerRuntime();if(partnerForced())return toast('ヘイト爆発中は対局終了まで手動へ戻せません');if(!pd.rule)return toast('作戦規則を選んでください');if(!pd.voluntary&&!grPartnerSelectionGuard('voluntary-toggle'))return toast('試合開始時の札組・モードを保持できないため委任できません');pd.voluntary=!pd.voluntary;pd.advice=pd.voluntary?'任意委任を開始。未確定の次判断から同じ合法候補契約を使います。':'次の未確定判断から手動へ戻します。';grPersistPartnerMatchSession();renderPartnerRuntime();if(pd.voluntary)attemptPartnerAutoDecision('voluntary-delegate').catch(e=>{pd.blocker=e.message;renderPartnerRuntime()})}}
function renderPartnerRuntime(){
const publicBox=$('#partnerDecisionBox');if(publicBox){const k=publicBox.querySelector('.k');if(k)k.textContent='パートナーの判断';const label=publicBox.querySelector('.partnerRuleLabel');if(label?.firstChild)label.firstChild.nodeValue='選び方 ';const sub=publicBox.querySelector('.sub');if(sub)sub.textContent='最大・最小を選べない場面では、パートナーは勝手に決めません。'}const telemetry=$('#hudTelemetryBox');if(telemetry){telemetry.hidden=true;telemetry.setAttribute('aria-hidden','true');telemetry.style.display='none'}
installPartnerUi();const m=state.match,p=currentHuman(),pd=m?ensurePartnerRuntime(m):null;if(!p||!pd)return;const h=$('#hateMeter');if(h){h.textContent=`ヘイト ${p.hateTime}/1000`;h.classList.toggle('hot',p.hateTime>=800)}const rule=$('#partnerRule');if(rule&&rule.value!==(pd.rule||''))rule.value=pd.rule||'';const del=$('#partnerDelegateBtn');if(del){del.classList.toggle('forced',partnerForced());del.textContent=partnerForced()?'強制委任':(pd.voluntary?'まかせろ':'まかせた')}const stat=$('#partnerDecisionStatus');if(stat){stat.textConten
--- char 10264873 context 10263073:10271873 ---
signature:sig,lastTick:at,remainderMs:0});if(c.signature!==sig){c.signature=sig;c.lastTick=at;c.remainderMs=0;continue}if(!sig){c.lastTick=at;c.remainderMs=0;continue}const delta=Math.max(0,Math.min(5000,at-c.lastTick));c.lastTick=at;c.remainderMs+=delta;const sec=Math.floor(c.remainderMs/1000);if(sec<=0)continue;c.remainderMs-=sec*1000;const before=Math.max(0,Number(p.hateTime)||0);p.hateTime=Math.min(1000,before+sec);if(p.hateTime!==before)changed=true;if(before<1000&&p.hateTime===1000)changed=friendExplode(p)||changed}FR.hateLastTick=at;if(changed)broadcastSnapshots();return changed}
const friendHateTimer=setInterval(()=>friendTickHate(performance.now()),250);
function syncFriendPartnerRule(explicit=false){let rule=FRIEND_PARTNER_RULES.has(currentPartnerRule())?currentPartnerRule():null;if(FR.role==='guest'){if(explicit)FR.localPartnerRule=rule;else if(!rule&&FRIEND_PARTNER_RULES.has(FR.localPartnerRule)){rule=FR.localPartnerRule;const pd=ensurePartnerRuntime(state.match);if(pd)pd.rule=rule}else if(rule)FR.localPartnerRule=rule}if(FR.role==='host'){FR.partnerRules.P1=rule;if(state.match?.players?.find(p=>p.id==='P1')?.hateExploded)queueMicrotask(()=>friendForcedDecision('P1','rule-update').catch(e=>{FR.hateBlockers.P1=String(e?.message||e)}));return true}if(FR.role==='guest'&&FR.assignedPid){send({type:'partner_rule',rule});renderPartnerRuntime();return true}return false}
function hostStart(){if(FR.role!=='host'||FR.roomState!=='lobby'||!allReady())return false;FR.roomState='playing';FR.matchNo++;FR.planValues={};FR.baseProgression=Number(state.progression?.battlePoints||0);FR.baseHistoryLength=state.history.length;state.setupMode=FR.playerCount===4?'4p':'2p';state.setupContent=FR.contentId||'road_shield';const m=startMatch();if(!m){FR.roomState='lobby';return false}m.friendRoom={build:BUILD,code:FR.code,playerCount:FR.playerCount,localPid:'P1',host:true,resultPersistenceEnabled:false,noBotFill:true};m.players.forEach(p=>{p.roomHuman=true;p.roomConnected=true;if(p.id!=='P1'){p.name=p.id;p.human=false}});FR.partnerRules={P1:FRIEND_PARTNER_RULES.has(currentPartnerRule())?currentPartnerRule():null};FR.hateBlockers={};friendResetHateClocks(performance.now());document.body.classList.add('friend-room-match');const pts=document.getElementById('resultBattlePoints');if(pts)pts.hidden=true;broadcastSnapshots();return true}
function publicPlayer(p,viewer){return{id:p.id,name:p.id===viewer?'あなた':p.name,human:p.id===viewer,roomHuman:true,character:p.character,team:p.team||null,position:p.position,awake:Number(p.awake)||0,honey:Number(p.honey)||0,mana:Array.from({length:p.mana?.length||0},(_,i)=>'M'+i),chip:Array.from({length:p.chip?.length||0},()=> '__PUBLIC_COUNT__'),lanes:clone(p.lanes||{L:[],C:[],R:[]}),deck:Array.from({length:p.deck?.length||0},()=> '__HIDDEN__'),hand:p.id===viewer?[...p.hand]:Array.from({length:p.hand?.length||0},()=> '__HIDDEN__'),shields:p.id===viewer?clone(p.shields):{L:'__HIDDEN__',C:'__HIDDEN__',R:'__HIDDEN__'},plan:p.id===viewer?clone(FR.planValues[p.id]||p.plan||null):null,nextCostReduction:Number(p.nextCostReduction)||0,battleBlockedRound:Number(p.battleBlockedRound)||0,royalUsed:publicRoyalUsed(p),sourceDeckIds:[],sourceDeckSize:Number(p.sourceDeckSize)||0}}
function project(viewer){const m=state.match,p=m?.players?.find(x=>x.id===viewer);if(!m||!p)return null;const ordered=[p,...m.players.filter(x=>x.id!==viewer)].map(x=>publicPlayer(x,viewer));return{schema:'GAMEROAD_FRIEND_ROOM_R2_PROJECTION',build:BUILD,viewer,code:FR.code,matchInstanceId:m.id,roomState:FR.roomState,playerCount:FR.playerCount,round:m.round,phase:m.phase,activeId:m.activeId,target:clone(m.target),mode:m.mode,contentId:m.contentId,log:[...m.log],players:ordered,busy:!!m.busy,honey:{byNode:clone(m.honey?.byNode||{}),receipts:[],generatedRounds:[]},lastBattleResolution:clone(m.lastBattleResolution),battlePresentation:clone(m.battlePresentation),resolutionSeq:Number(m.resolutionSeq)||0,result:FR.roomState==='result'?clone(m.lastResult):null,participants:participantsHost(),selectionContract:grPublicSelectionContract(m,viewer),private:{pid:viewer,planSubmitted:!!FR.planValues[viewer],hateTime:Number(p.hateTime)||0,hateExploded:!!p.hateExploded,hateExplosionCount:Number(p.hateExplosionCount)||0}}}
function sendSnapshot(to){const s=slotByClient(to);if(!s?.connected)return;send({type:'snapshot',to,projection:project(s.pid)})}
function broadcastSnapshots(){if(FR.role!=='host'||!state.match)return;for(const s of Object.values(FR.slots))if(s.connected)sendSnapshot(s.clientId);renderFriendHostBattleState()}
function projectionToMatch(pr){const m={id:'FRIEND-'+pr.code+':'+String(pr.matchInstanceId||'legacy'),mode:pr.mode,contentId:pr.contentId,round:pr.round,phase:pr.phase,players:pr.players.map(x=>clone(x)),activeId:pr.activeId,target:clone(pr.target),log:[...pr.log],lastResult:null,lastBattleResolution:clone(pr.lastBattleResolution),battlePresentation:clone(pr.battlePresentation),resolutionSeq:pr.resolutionSeq,projection:['N','E','S','W'],abilityRng:1,busy:!!pr.busy,honey:{byNode:clone(pr.honey?.byNode||{}),receipts:new Set(),generatedRounds:new Set()},participants:new Map(pr.players.map(p=>[p.id,[]])),participantSeq:0,resolvedAbilityKeys:new Set(),abilityOrderReceipts:new Map(),testAutoChoices:false,quickWindow:null,quickPointsByPlayer:{},quickAwards:[],playerDeckRule:{id:pr.selectionContract?.regulationId||DECK_RULE.id,revision:pr.selectionContract?.regulationRevision||DECK_RULE.revision,mainCount:Number(pr.selectionContract?.mainCount)||Number(pr.players?.find(x=>x.id===pr.viewer)?.sourceDeckSize)||0,exCount:Number(pr.selectionContract?.exCount)||0},friendRoom:{build:BUILD,code:pr.code,playerCount:pr.playerCount,localPid:pr.viewer,host:false,resultPersistenceEnabled:false,noBotFill:true}};m.selectionLock=Object.freeze({version:1,playerId:pr.viewer,mode:m.mode,contentId:m.contentId,regulationId:m.playerDeckRule.id,regulationRevision:m.playerDeckRule.revision,mainCount:m.playerDeckRule.mainCount,exCount:m.playerDeckRule.exCount,sourceDeckDigest:null});const me=m.players.find(x=>x.id===pr.viewer);if(me){me.hateTime=Math.max(0,Math.min(1000,Number(pr.private?.hateTime)||0));me.hateExploded=!!pr.private?.hateExploded;me.hateExplosionCount=Math.max(0,Number(pr.private?.hateExplosionCount)||0)}if(pr.result){const ranking=(pr.result.ranking||[]).map(r=>{const pp=m.players.find(x=>x.id===r.player?.id);return{player:pp,rank:r.rank,depth:r.depth}}).filter(r=>r.player);const me=ranking.find(r=>r.player.id===pr.viewer);m.lastResult={ranking,headline:me?.rank===1?'勝利':'結果',grade:me?.rank||0,points:null,friendRoom:true}}return m}
function applyProjection(pr){if(!pr||pr.viewer!==FR.assignedPid)return;FR.snapshot=pr;FR.roomState=pr.roomState;state.match=projectionToMatch(pr);document.body.classList.add('friend-room-match');const pts=document.getElementById('resultBattlePoints');if(pts)pts.hidden=true;if(pr.roomState==='result'){show('result');renderResult();if(pts)pts.hidden=true;const h=document.getElementById('resultPointsEarned');if(h)h.textContent='フレンド対戦・報酬なし'}else{show('battle');renderBattle();renderRemoteWait();syncFriendPartnerRule()} }
function renderRemoteWait(){if(FR.role!=='guest'||state.screen!=='battle')return;const b=document.getElementById('readyPlan'),pr=FR.snapshot;if(b&&pr?.private?.planSubmitted){b.disabled=true;b.querySelector('.quickReadyText')&&(b.querySelector('.quickReadyText').textContent='予約済み・全員待ち')}const act=state.match?.players?.find(p=>p.id===state.match.activeId);if(state.match?.phase==='target'&&act?.id!==FR.assignedPid){document.getElementById('targetBox')?.classList.remove('on')}}
function renderFriendHostBattleState(){if(FR.role!=='host'||state.screen!=='battle'||!state.match?.friendRoom)return;const b=document.getElementById('readyPlan');if(b&&FR.planValues.P1){b.disabled=true;b.querySelector('.quickReadyText')&&(b.querySelector('.quickReadyText').textContent='予約済み・全員待ち')}}
function legalPlan(p,plan){if(!p||!plan?.roadId||!plan?.battleId)return{ok:false,reason:'plan_missing'};if(plan.roadId===plan.battleId)return{ok:false,reason:'same_card'};if(!p.hand.includes(plan.roadId)||!p.hand.includes(plan.battleId))return{ok:false,reason:'reservation_not_owned'};if(!roadSelectable(plan.roadId)||!roadSelectable(plan.battleId))return{ok:false,reason:'card_not_selectable'};const path=Array.isArray(plan.path)&&plan.path.length?plan.path:[p.position];if(path[0]!==p.position)return{ok:false,reason:'path_origin_mismatch'};const lim=normalMoveLimitForRoad(plan.roadId);if(path.length-1>lim)return{ok:false,reason:'move_limit'};for(let i=1;i<path.length;i++)if(!neighbors(path[i-1],p).includes(path[i])||path.slice(0,i).includes(path[i]))return{ok:false,reason:'path_invalid'};return{ok:true,plan:{roadId:plan.roadId,battleId:plan.battleId,path:[...path]}

=== CHAR PATTERN 'state.match={' HITS_SHOWN=1 ===
--- char 10038179 context 10036379:10045179 ---
g('ja-JP')}`)}
const quickTicker=setInterval(()=>{if(state.screen==='battle')renderQuickTimer()},100);window.addEventListener('pagehide',()=>clearInterval(quickTicker),{once:true});
function initRoundRuntime(m){m.participants=new Map(m.players.map(p=>[p.id,[]]));m.participantSeq=0;m.resolvedAbilityKeys=new Set();m.abilityOrderReceipts=new Map();m.testAutoChoices=false;quickStartRound(m)}

function startMatch(){const eligibility=deckEligibility(state.savedDeck);if(!eligibility.usable){toast(deckEligibilityMessage(eligibility));show('setup');renderSetupDeckStatus();return null}audioResetForNewMatch();try{if(battleMount&&window.GameRoadThreeCharRuntime?.unmount)window.GameRoadThreeCharRuntime.unmount(battleMount)}catch(e){}battleMount=null;battleMountPromise=null;const battleRuntimeEl=$('#battleRuntime');if(battleRuntimeEl)battleRuntimeEl.replaceChildren();const snapshotFactory=globalThis.GAMEROAD_CREATE_DECK_MATCH_START_SNAPSHOT;let snapshot;try{if(typeof snapshotFactory!=='function')throw new Error('MATCH_START_SNAPSHOT_RUNTIME_UNAVAILABLE');snapshot=snapshotFactory({savedDeck:state.savedDeck,savedDeckRule:state.savedDeckRule,setupMode:state.setupMode,setupContent:state.setupContent,playerCharacterId:state.playerCharacterId,selectedPartnerId:state.selectedPartnerId},{validateDeck})}catch(e){const message=String(e?.message||e),prefix='MATCH_START_DECK_INVALID:';toast(message.startsWith(prefix)?'札組を修正: '+message.slice(prefix.length):'対戦開始状態を確認できません');show('setup');renderSetupDeckStatus();return null}const n=playerCount(snapshot.setup.mode),ps=[],diagnosticCpuDeck=[...snapshot.deck.main];for(let i=0;i<n;i++)ps.push(makePlayer(i,i===0,i===0?[...snapshot.deck.main]:diagnosticCpuDeck));if(snapshot.setup.mode==='2v2'){ps[0].team='A';ps[1].team='A';ps[2].team='B';ps[3].team='B'}state.match={id:'M'+Date.now(),mode:snapshot.setup.mode,contentId:snapshot.setup.content,round:1,phase:'plan',players:ps,activeId:null,target:null,log:[],lastResult:null,lastBattleResolution:null,battlePresentation:null,resolutionSeq:0,projection:['N','E','S','W'],abilityRng:(Date.now()^0x9e3779b9)>>>0,busy:false,deckStartSnapshot:snapshot,honey:{byNode:{},receipts:new Set(),generatedRounds:new Set()},playerDeckRule:{id:snapshot.deck.ruleId,revision:snapshot.deck.ruleRevision,mainCount:snapshot.deck.main.length,exCount:snapshot.deck.ex.length}};state.match.selectionLock=grCreateSelectionLock(state.match,'P1');grBattleReplayBegin(state.match);awardRoundStartHoney(state.match);initRoundRuntime(state.match);log(snapshot.setup.content==='honey_hunt'?'第1巡。ハニーハント：一筆移動・終点ハニー全量取得・一列七枚勝利。人物を直接ドラッグして経路を秘密予約します。':'第1巡。全員が行動を予約します。');show('battle');setupBattleDetails();renderBattle();return state.match}
$('#startMatch').addEventListener('click',startMatch);
function log(t){if(!state.match)return;state.match.log.unshift(t);state.match.log=state.match.log.slice(0,30)}
function refill(p){while(p.hand.length<3&&p.deck.length)p.hand.push(p.deck.shift())}
function laneDepth(p,l){return p.lanes[l].length}
function posType(id){if(id.startsWith('C:'))return'center';if(id.startsWith('S:'))return'shield';if(id.startsWith('R:'))return'road';return'corner'}
function parseRoad(id){const [,owner,lane,depth]=id.split(':');return{owner,lane,depth:+depth}}
function projectionFor(viewer){const ids=['P1','P2','P3','P4'];const vi=Math.max(0,ids.indexOf(viewer.id));const arms=['N','E','S','W'];const map={};ids.forEach((id,i)=>map[id]=arms[(i-vi+4)%4]);return map}
function centerIds(){const a=[];for(let r=-1;r<=1;r++)for(let c=-1;c<=1;c++)a.push(`C:${r}:${c}`);return a}
function shieldId(owner,lane){return`S:${owner}:${lane}`};function roadId(owner,lane,d){return`R:${owner}:${lane}:${d}`}
function activeNodes(){const s=new Set(centerIds());['K:NW','K:NE','K:SW','K:SE'].forEach(id=>s.add(id));state.match.players.forEach(p=>{['L','C','R'].forEach(l=>{s.add(shieldId(p.id,l));for(let d=1;d<=p.lanes[l].length&&d<=7;d++)s.add(roadId(p.id,l,d))})});return s}
function centerPort(owner,lane,viewer){const arm=projectionFor(viewer)[owner];const laneIx={L:-1,C:0,R:1}[lane];if(arm==='N')return`C:-1:${laneIx}`;if(arm==='S')return`C:1:${-laneIx}`;if(arm==='W')return`C:${-laneIx}:-1`;return`C:${laneIx}:1`}
function neighbors(id,viewer){const act=activeNodes(),out=[],proj=projectionFor(viewer),ownerFor=arm=>state.match.players.find(p=>proj[p.id]===arm)?.id,cornerDefs={'K:NW':[['N','L'],['W','R']],'K:NE':[['N','R'],['E','L']],'K:SW':[['S','R'],['W','L']],'K:SE':[['S','L'],['E','R']]},cornerRoads=k=>(cornerDefs[k]||[]).map(([arm,lane])=>{const owner=ownerFor(arm);return owner?roadId(owner,lane,1):null}).filter(Boolean);if(id.startsWith('C:')){const [,rs,cs]=id.split(':'),r=+rs,c=+cs;[[r-1,c],[r+1,c],[r,c-1],[r,c+1]].forEach(([rr,cc])=>{const x=`C:${rr}:${cc}`;if(act.has(x))out.push(x)});state.match.players.forEach(p=>['L','C','R'].forEach(l=>{const sh=shieldId(p.id,l);if(centerPort(p.id,l,viewer)===id&&act.has(sh))out.push(sh)}))}else if(id.startsWith('K:')){cornerRoads(id).forEach(x=>{if(act.has(x))out.push(x)})}else if(id.startsWith('S:')){const [,owner,lane]=id.split(':');out.push(centerPort(owner,lane,viewer));const r1=roadId(owner,lane,1);if(act.has(r1))out.push(r1)}else if(id.startsWith('R:')){const {owner,lane,depth}=parseRoad(id);if(depth===1){out.push(shieldId(owner,lane));Object.keys(cornerDefs).forEach(k=>{if(cornerRoads(k).includes(id))out.push(k)})}const a=roadId(owner,lane,depth-1),b=roadId(owner,lane,depth+1);if(depth>1&&act.has(a))out.push(a);if(depth<7&&act.has(b))out.push(b)}return out.filter(x=>act.has(x))}
function shortest(start,end,max,viewer){if(start===end)return[start];const q=[[start]],seen=new Set([start]);while(q.length){const p=q.shift();if(p.length-1>=max)continue;for(const n of neighbors(p[p.length-1],viewer)){if(seen.has(n))continue;const np=[...p,n];if(n===end)return np;seen.add(n);q.push(np)}}return null}
function reachable(p,steps){const q=[[p.position,0]],dist=new Map([[p.position,0]]);while(q.length){const [x,d]=q.shift();if(d>=steps)continue;for(const n of neighbors(x,p)){if(!dist.has(n)){dist.set(n,d+1);q.push([n,d+1])}}}return dist}
/* DOPPELGANGER-NORMAL-RULE-RUNTIME-001 R1
   Recovered user canon (2026-07-26 / 2026-08-06):
   - whole field; road/battle cards only; shields join only after becoming battle cards
   - activates when two or more cards share the same printed number
   - same-number reduction includes the Doppelganger card itself
   - continuous current value affects road movement/order/attack-right/battle value
   - zero does not destroy the card; no recursive modifier loop
   Unknowns remain fail-closed: formal card id/suit/rank, same-name self counting,
   negative values, modifier ordering beyond the recovered examples, hidden-card edge cases. */
const GR_DOPPEL_EFFECT='doppelgangerContinuous';
function grDoppelIs(cardId){return CARDS[cardId]?.effect_type===GR_DOPPEL_EFFECT}
function grDoppelEligibleParticipant(e){return !!e&&['road','active_submission','auto_defense','ability_active_addition'].includes(String(e.origin||''))}
function grDoppelFieldSnapshot(m=state.match){
 const out=[],seen=new Set(); if(!m)return out;
 const add=(key,ownerId,cardId,zone,publicNow=true)=>{const c=CARDS[cardId];if(!c||!publicNow||seen.has(key))return;seen.add(key);out.push(Object.freeze({key,ownerId,cardId,zone,printedValue:Number(c.power),name:String(c.display_name||cardId)}))};
 m.players.forEach(p=>['L','C','R'].forEach(l=>(p.lanes?.[l]||[]).forEach((id,i)=>add(`lane:${p.id}:${l}:${i}:${id}`,p.id,id,'lane',true))));
 if(m.participants)for(const e of participantEntries())if(grDoppelEligibleParticipant(e))add(`participant:${e.key}`,e.ownerId,e.cardId,e.origin,true);
 return out;
}
function grDoppelCensus(cardId,selfKey,snapshot=grDoppelFieldSnapshot(),matchBasis){
 const c=CARDS[cardId],base=Number(c?.power); if(!c||!Number.isFinite(base))return{ok:false,reason:'CARD_NOT_FOUND'};
 if(matchBasis!=='printed')return{ok:false,reason:'DOPPEL_MATCH_BASIS_UNRESOLVED'};
 const self=snapshot.find(x=>x.key===selfKey&&x.cardId===cardId)||snapshot.find(x=>x.cardId===cardId);
 if(!self)return{ok:false,reason:'DOPPEL_SELF_NOT_ON_PUBLIC_FIELD'};
 const sameNumber=snapshot.filter(x=>Number(x.printedValue)===base),sameName=snapshot.filter(x=>x.name===String(c.display_name||cardId));
 return{ok:true,baseValue:base,activation:sameNumber.length>=2,sameNumberCount:sameNumber.length,sameNameCount:sameName.length,selfIncludedInSameNumber:sameNumber.some(x=>x.key===self.key),fieldCount:snapshot.length,matchBasis};
}
function grResolveDoppelgangerValue(cardId,selfKey,options={}){
 const c=CARDS[cardId]; if(!c)return{ok:false,reason:'CARD_NOT_FOUND'};
 if(!grDoppelIs(cardId))return{ok:true,value:Number(c.power),applied:false,reason:'NOT_DOPPELGANGER'};
 const matchBasis=options.matchBasis??c.doppel_match_bas

=== CHAR PATTERN 'function endMatch(' HITS_SHOWN=1 ===
--- char 10119585 context 10117785:10126585 ---
LT_MS,true);if(state.match!==m||state.screen!=='battle')return;const winners=slaykiaAttackEnd?.specialWinnerTokens?.length?[...slaykiaAttackEnd.specialWinnerTokens]:checkWin();m.battlePresentation=null;if(winners.length){m.busy=false;if(slaykiaAttackEnd?.specialWinnerTokens?.length)log('スレイキア：成功破壊16枚で特殊勝利。');else if(m.contentId==='honey_hunt')log('一列へ実札が七枚以上連続配置され、ハニーハントの勝利条件が成立しました。');grBattleReplayEnd(m,winners);return endMatch(winners)}nextRound()}catch(e){clearBattlePresentationWait();if(state.match===m){m.battlePresentation=null;m.busy=false;log(`戦闘停止：${e.message}`);renderBattle()}throw e}}
function checkWin(){if(state.match.mode==='2v2'){const teams=['A','B'].filter(t=>state.match.players.filter(p=>p.team===t).some(p=>Math.max(...Object.values(p.lanes).map(a=>a.length))>=7));return teams}return state.match.players.filter(p=>Math.max(...Object.values(p.lanes).map(a=>a.length))>=7).map(p=>p.id)}
function nextRound(){const m=state.match;m.lastBattleResolution=null;m.round++;m.phase='plan';m.activeId=null;m.target=null;m.battlePresentation=null;m.busy=false;m.players.forEach(p=>p.plan=null);awardRoundStartHoney(m);initRoundRuntime(m);log(`第${m.round}巡。行動を予約します。`);renderBattle()}
function currentPlacementRanks(m=state.match){const ps=m.players.map(p=>({p,depth:Math.max(...Object.values(p.lanes).map(a=>a.length))})).sort((a,b)=>b.depth-a.depth);let prev=null,rank=0;return ps.map((x,i)=>{if(prev===null||x.depth!==prev)rank=i+1;prev=x.depth;return{player:x.p,rank,depth:x.depth}})}
function awardRoundStartHoney(m){for(const row of currentPlacementRanks(m)){const gain=Math.max(1,Number(row.rank)||1);row.player.honey=Math.max(0,Number(row.player.honey)||0)+gain;log(`${row.player.name}：${gain}位のためハニー+${gain}。`)}}
function ranksFFA(){return currentPlacementRanks(state.match)}
function endMatch(winners){const m=state.match;let ranking;if(m.mode==='2v2'){const wt=winners[0];ranking=m.players.map(p=>({player:p,rank:p.team===wt?1:2,depth:Math.max(...Object.values(p.lanes).map(a=>a.length))}))}else ranking=ranksFFA();const me=ranking.find(x=>x.player.human),points=settleBattlePoints(m,me.rank);m.lastResult={ranking,headline:me.rank===1?'勝利':'結果',grade:me.rank,points};state.history.unshift({at:new Date().toISOString(),mode:m.mode,rank:me.rank,rounds:m.round,character:state.playerCharacterId,partner:state.selectedPartnerId,battlePointsEarned:points.earned,quickPoints:points.quick,battlePointsAfter:points.after});save();show('result');renderResult()}
async function renderResult(){const r=state.match?.lastResult;if(!r)return;const m=state.match,ordered=[...r.ranking].sort((a,b)=>a.rank-b.rank),me=ordered.find(x=>x.player.human),mode=({'2p':'二人','4p':'四人','2v2':'二対二'})[m.mode]||m.mode,content=m.contentId==='honey_hunt'?'ハニーハント':'ロード・シールド';const {projectResultRankPresentation}=await import('./result-presentation-core.mjs'),userPresentation=projectResultRankPresentation(me?.rank);if(!userPresentation?.ok)return;const gradeEl=$('#resultGrade'),headlineEl=$('#resultHeadline');gradeEl.textContent=userPresentation.visibleLabel;headlineEl.textContent=userPresentation.visibleLabel;gradeEl.dataset.formalRank=String(userPresentation.formalRank);headlineEl.dataset.formalRank=String(userPresentation.formalRank);if(userPresentation.rankColorRole){gradeEl.dataset.rankColorRole=userPresentation.rankColorRole;headlineEl.dataset.rankColorRole=userPresentation.rankColorRole}else{delete gradeEl.dataset.rankColorRole;delete headlineEl.dataset.rankColorRole}$('#resultSummary').textContent=`${m.round}巡で決着。 ${content}`;$('#resultLeadText')&&($('#resultLeadText').textContent=r.grade===1?'一位で決着':'最終順位');$('#resultLeadSub')&&($('#resultLeadSub').textContent=`${content} / ${mode}`);$('#resultMode')&&($('#resultMode').textContent=mode);$('#resultRounds')&&($('#resultRounds').textContent=`${m.round}巡`);$('#resultLane')&&($('#resultLane').textContent=`${me?.depth??0}枚`);renderResultBattlePoints(r);const box=$('#resultRanking');box.innerHTML='';ordered.forEach(x=>{const presentation=projectResultRankPresentation(x.rank);if(!presentation?.ok)return;const d=document.createElement('div');d.className='rankLine';d.dataset.formalRank=String(presentation.formalRank);if(presentation.rankColorRole)d.dataset.rankColorRole=presentation.rankColorRole;d.innerHTML=`<b><span class="resultOutcome">${presentation.visibleLabel}</span> ${x.player.human?'あなた':x.player.name}</b><span>最長列 ${x.depth}枚</span>`;box.appendChild(d)});await grBattleReplayRenderResult(m);resultMount=await mountChar('.resultWorld',state.playerCharacterId,'joy')}
$('#rematch').onclick=()=>startMatch();$('#leaveMatch').onclick=()=>{state.match=null;navigateRoot('home')};window.__GAMEROAD_QUICK_R14_QA__={version:'FULLGAME-QUICKTIMER-R14',constants:{windowMs:QUICK_WINDOW_MS,perfectRemainMs:QUICK_PERFECT_REMAIN_MS,goodRemainMs:QUICK_GOOD_REMAIN_MS,perfectPoints:QUICK_PERFECT_POINTS,goodPoints:QUICK_GOOD_POINTS,chestTarget:BATTLE_POINTS_CHEST_TARGET},snapshot(){const m=state.match,me=m?.players?.find(p=>p.human),rem=m?quickRemainingMs(m):null;return{screen:state.screen,round:m?.round||null,phase:m?.phase||null,remainingMs:rem,tier:m?quickTierForRemaining(rem):null,quickPoints:me?(m.quickPointsByPlayer?.[me.id]||0):0,awards:me?(m.quickAwards||[]).filter(a=>a.playerId===me.id).map(a=>({...a})):[],battlePoints:state.progression?.battlePoints||0,settlement:m?.battlePointsSettlement?safeClone(m.battlePointsSettlement):null,coilHidden:$('#quickCoil')?.hidden??null,coilText:$('#quickRemain')?.textContent||null,resultText:$('#resultBattlePoints')?.innerText||''}},setRemainingSeconds(sec){const m=state.match;if(!m?.quickWindow)return false;const v=Math.max(0,Math.min(30,Number(sec)||0));m._quickQaNowMs=m.quickWindow.deadlineAt-v*1000;renderQuickTimer();return true},clearTestClock(){const m=state.match;if(!m)return false;delete m._quickQaNowMs;renderQuickTimer();return true},setBattlePoints(n){state.progression={battlePoints:Math.max(0,Math.floor(Number(n)||0))};return state.progression.battlePoints},persistenceProbe(n){state.progression={battlePoints:Math.max(0,Math.floor(Number(n)||0))};save();state.progression={battlePoints:0};load();return{battlePoints:state.progression.battlePoints,storage:state.storage}}};window.GAMEROAD_3D_WALK_QA={version:'BROWSER-3D-BOARD-WALK-INTEGRATION-001',snapshot(){const m=state.match,me=m?.players?.find(p=>p.human);return{screen:state.screen,phase:m?.phase||null,position:me?.position||null,path:me?.plan?.path?[...me.plan.path]:null,avatarState:battleMount?.root?.dataset?.state||null,avatarFacing:battleMount?.facingName||null,nodeCount:document.querySelectorAll('#board .node').length,canvas:{w:$('#fieldCanvas')?.width||0,h:$('#fieldCanvas')?.height||0}}}};window.GAMEROAD_GRASSLAND3D_QA={version:'BROWSER-BOARD-GRASSLAND-3D-R4',snapshot(){const gl=$('#fieldCanvas')?.getContext('webgl');return{...fieldLastStats,webgl:!!gl,contextLost:!!gl?.isContextLost?.(),projectionCount:fieldProjection.size,nodeCount:document.querySelectorAll('#board .node').length,screen:state.screen}}};
window.GAMEROAD_BOARD_REVIEW_QA={version:'BROWSER-BOARD-PLAYTEST-REVIEW-LOOP-001',_rect(e){const b=e?.getBoundingClientRect();return b?{l:b.left,r:b.right,t:b.top,b:b.bottom,w:b.width,h:b.height}:null},_hit(a,b){return!!(a&&b&&Math.min(a.r,b.r)>Math.max(a.l,b.l)&&Math.min(a.b,b.b)>Math.max(a.t,b.t))},snapshot(){const rect=this._rect.bind(this),hit=this._hit.bind(this),nodes=[...document.querySelectorAll('#board .node')].map(e=>({id:e.dataset.pos,rect:rect(e)})),surfaces={publicHud:rect($('#publicTurnHud')),battleInfo:rect(document.querySelector('.battleInfo')),rail:rect(document.querySelector('.battleRail')),top:rect(document.querySelector('.battleTopStatus'))},collisions={};for(const[k,r]of Object.entries(surfaces))collisions[k]=nodes.filter(n=>hit(n.rect,r)).map(n=>n.id);return{screen:state.screen,phase:state.match?.phase||null,viewport:{w:innerWidth,h:innerHeight},profile:fieldCameraState?.kind||null,nodeCount:nodes.length,map:rect($('#battleMap')),collisions,criticalOverlapCount:collisions.publicHud.length+collisions.battleInfo.length,avatar:{state:battleMount?.root?.dataset?.state||null,facing:battleMount?.facingName||null}}},setupSnapshot(){const rect=this._rect.bind(this),hit=this._hit.bind(this),header=rect(document.querySelector('header.top')),controls=['[data-content="road_shield"]','[data-content="honey_hunt"]','[data-mode="2p"]','[data-mode="4p"]','[data-mode="2v2"]','#startMatch'].map(sel=>({sel,rect:rect(document.querySelector(sel))}));return{viewport:{w:innerWidth,h:innerHeight},header,controls,headerCollisions:controls.filter(c=>hit(c.rect,header)).map(c=>c.sel),offscreen:controls.filter(c=>!c.rect||c.rect.t<0||c.rect.l<0||c.rect.r>innerWidth||c.rect.b>innerHeight).map(c=>c.sel)}}};
window.GAMEROAD_VISUAL_SHELL_QA={version:'BR

=== SCRIPT SRC INVENTORY ===
001292: <script src="./deck-save-recovery-core.mjs"></script>
001294: <script type="module" id="gameroad-lobbydeck-r4-matchstart-snapshot">
001300: <script src="./board-facility-state-core.classic.js"></script>
001301: <script type="module">
001305: <script type="module">
003824: <script type="module" id="gameroad-ui-feedback-ready-plan-production-r9">
003895: <script id="gameroad-post-match-autoqueue-r5" type="module">
004146: <script type="module" src="./home-cards-2p5d-presentation.mjs"></script>
004179: <script type="module" id="gameroad-r62-home-illustration-runtime">
004312: <script type="module" id="gameroad-controlled-character-4p-live-r8">
004400: <script type="module" src="./profile-presentation-runtime-mount.mjs"></script>
