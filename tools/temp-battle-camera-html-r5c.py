from pathlib import Path
import json, re

SCRIPT = Path('tools/temp-battle-camera-html-r5c.py')
HTML = Path('browser/GAMEROAD.html')
text = HTML.read_text(encoding='utf-8')
MARK = '# CAMERA_R5C_PROBE='
if any(line.startswith(MARK) for line in SCRIPT.read_text(encoding='utf-8').splitlines()):
    raise SystemExit(0)

def ctx(needle, before=1400, after=4200, limit=8):
    out=[]; pos=0
    while len(out)<limit:
        i=text.find(needle,pos)
        if i<0: break
        out.append({
            'line': text.count('\n',0,i)+1,
            'index': i,
            'text': re.sub(r'\s+',' ',text[max(0,i-before):min(len(text),i+len(needle)+after)])
        })
        pos=i+len(needle)
    return out

result={
  'moduleImports':ctx("from './",800,2200,10),
  'fieldCameraState':ctx('fieldCameraState',1800,5200,8),
  'fieldCameraDef':ctx('function fieldCamera(',2200,6500,2),
  'project3Def':ctx('function project3(',1800,4200,2),
  'renderField3DDef':ctx('function renderField3D(',2200,7000,2),
  'uniformCalls':ctx('uniform',1200,4200,12),
  'battleMapMarkup':ctx('id="battleMap"',1000,5000,2),
  'battleRailMarkup':ctx('class="battleRail"',1000,3000,2),
  'renderBoardDef':ctx('function renderBoard(',1500,5000,2),
  'screenChange':ctx("state.screen='battle'",1200,3500,8),
  'renderBattle':ctx('renderBattle',1200,3200,8),
  'moduleScript':ctx('<script type="module">',300,1600,4),
}
raw=json.dumps(result,ensure_ascii=False,separators=(',',':'))
with SCRIPT.open('a',encoding='utf-8') as f:
    f.write('\n'+MARK+raw+'\n')
print(raw)

# CAMERA_R5C_PROBE={"moduleImports":[{"line":4180,"index":11876169,"text":"utationObserver(schedule).observe(e,{childList:true,subtree:n==='phaseTitle'||n==='partnerDecisionStatus',characterData:n==='phaseTitle'||n==='partnerDecisionStatus',attributes:n==='targetBox',attributeFilter:n==='targetBox'?['class']:undefined})}const b=document.querySelector('section[data-screen=\"battle\"]');if(b)new MutationObserver(schedule).observe(b,{attributes:true,attributeFilter:['class','hidden','aria-hidden']});schedule()} window.__GAMEROAD_PARTNER_ADVICE_BOARD_R21B__=Object.freeze({render,clear,snapshot});document.readyState==='loading'?document.addEventListener('DOMContentLoaded',install,{once:true}):install(); })(); </script> <script type=\"module\" id=\"gameroad-r62-home-illustration-runtime\"> import { createHomeThemeProfile, createHomePresentationState, resolveHomeProjection } from './home-theme-orientation-core.mjs'; (() => { const root = document.querySelector('.codexHome'); if (!root || root.dataset.homeOrientationRuntime === 'r62r') return; const homeAssetRoot = location.pathname.startsWith('/browser/') ? '../assets/visual/home/' : './assets/visual/home/'; const runtime = globalThis.GAMEROAD_ORIENTATION_TRANSITION; const phaseRunner = globalThis.GAMEROAD_RUN_ORIENTATION_TRANSITION_PHASE; if (!runtime || typeof runtime.requestProjection !== 'function' || typeof phaseRunner !== 'function') { root.dataset.homeOrientationRuntime = 'missing-dependency'; return; } const profile = createHomeThemeProfile({ themeId: 'HOME_INITIAL_DEFAULT', displayNameToken: 'home.initial', worldStyleId: 'moonlit_forest_user_illustration', landscapeSceneAsset: `${homeAssetRoot}home-illustration-landscape.webp`, portraitSceneAsset: `${homeAssetRoot}home-illustration-portrait.webp`, landscapeFocalAnchor: { x: 0.58, y: 0.5 }, portraitFocalAnchor: { x: 0.56, y: 0.32 }, safeComposition: { landscape: 'center_16_9', portrait: 'dedicated_art_plate' }, bleed: { landscape: 'full_viewport', portrait: 'full_viewport' }, motionProfileId: 'home.provided_illustration_orientation', ambientLayerIds: ['moon_glow', 'droplets', 'crystal_flow'], transitionBridgeProfileId: 'home.provided_illustration_bridge', reducedMotionFallback: { mode: 'immediate_projection_reanchor' }, lowPerfFallback: { mode: 'reduced_layer_swap' }, availabilityRef: 'UIFEEDBACK-R62R-HOME-ILLUSTRATION-PUBLIC', }); const artImages = [...root.querySelectorAll('.codexHomeArtPlate')]; artImages.forEach((image) => { image.src = `${homeAssetRoot}home-illustration-${image.dataset.homeArtOrientation}.webp`; }); let presentation = createHomePresentationState({ themeId: profile.themeId, orientation: root.dataset.homeOrientation === 'portrait' ? 'portrait' : 'landscape', }); const log = globalThis.__GAMEROAD_HOME_ORIENTATION_LOG__ || []; globalThis.__GAMEROAD_HOME_ORIENTATION_LOG__ = log; const record = (entry) => { const next = { revision: entry.revision ?? null, phase: entry.phase ?? null, "},{"line":4317,"index":11882928,"text":"ord({ status: result?.status || 'unknown', revision: result?.revision, from: result?.from, to: result?.to, superseded: result?.status === 'superseded' }); return result; }), }); })(); </script> <style id=\"gameroad-controlled-character-4p-live-r8-style\"> /* Preserve current Battle chrome and P1's existing continuous #battleRuntime while projecting P2-P4 on authoritative board markers. */ html body:has(.battle.active) .top{display:flex!important} #boardPlayers .boardPlayerToken.human [data-board-controlled-character]{display:none!important} </style> <script type=\"module\" id=\"gameroad-controlled-character-4p-live-r8\"> import { createFourParticipantControlledCharacterMotionState, applyFourParticipantControlledCharacterEvent, projectFourParticipantControlledCharacterMotion, } from './battle-controlled-character-4p-motion-director.mjs'; import './battle-board-naki-4p-visual-binding.mjs'; let directorState = null; let activeMatchId = null; let revision = 0; function readSource() { const reader = globalThis.__GAMEROAD_CONTROLLED_CHARACTER_4P_SOURCE__; if (typeof reader !== 'function') return null; const source = reader(); if (!source || !Array.isArray(source.participants) || source.participants.length !== 4) return null; return source; } function resetDirector(source) { directorState = createFourParticipantControlledCharacterMotionState({ participants: source.participants }); activeMatchId = source.matchId; revision = 0; } function ensureDirector(source) { if (!source) { directorState = null; activeMatchId = null; revision = 0; return false; } const identityChanged = directorState && source.participants.some((row) => ( directorState.stateByParticipant?.[row.participantId]?.characterId !== row.characterId )); if (!directorState || activeMatchId !== source.matchId || identityChanged) resetDirector(source); return true; } function projectCurrent() { const source = readSource(); if (!ensureDirector(source)) return null; for (const row of source.participants) { const current = directorState.stateByParticipant?.[row.participantId]; if (current && current.positionKey !== row.positionKey) { revision += 1; const reconciled = applyFourParticipantControlledCharacterEvent(directorState, row.participantId, { type: 'RECONNECT', revision, controlGeneration: 0, positionKey: row.positionKey, }); directorState = reconciled.state; } } return projectFourParticipantControlledCharacterMotion(directorState, { reducedMotion: source.reducedMotion, lowPerformance: source.lowPerformance, }); } function syncVisuals() { const projection = projectCurrent(); const binding = globalThis.__GAMEROAD_CONTROLLED_CHARACTER_4P_BOARD_VISUAL_BINDING__; if (binding && typeof binding.applyProjection === 'function') binding.applyProjection(projection); return projection; } globalThis.__GAMEROAD_CONTROLLED_CHARACTER_4P_PROJECTION__ = projec"}],"fieldCameraState":[{"line":1683,"index":10052916,"text":"DoppelPolicyReady(id))} function normalMoveLimitForRoad(id){if(!roadSelectable(id))return 0;const n=Number(CARDS[id].power);return Number.isFinite(n)&&n>=1&&n<=6?Math.floor(n):0} function normalMoveLimitForPlayer(p){return normalMoveLimitForRoad(p?.plan?.roadId)} function preferredRoadCandidates(hand){const all=(hand||[]).filter(roadSelectable),movable=all.filter(id=>normalMoveLimitForRoad(id)>0);return movable.length?movable:all} let directPlanRole='road'; function syncDirectPlanRoleUi(){const me=state.match?.players?.[0];if(me){if(!me.plan?.roadId)directPlanRole='road';else if(!me.plan?.battleId)directPlanRole='battle'};[['road','#roadSelect'],['battle','#battleSelect']].forEach(([role,sel])=>{const e=$(sel),box=e?.closest('.planSelect');box?.classList.toggle('directRoleActive',directPlanRole===role);if(e)e.setAttribute('aria-label',`${role==='road'?'ロード':'バトル'}札。手札入力先${directPlanRole===role?'、選択中':''}`)})} function setDirectPlanRole(role,announce=false){if(role!=='road'&&role!=='battle')return directPlanRole;directPlanRole=role;syncDirectPlanRoleUi();if(announce)toast(`${role==='road'?'ロード':'バトル'}を選び直します。手札から札を選択`);return role} function nodeXY(id,viewer){const p=fieldProject(id,viewer);if(p)return[p.x,p.y];if(id.startsWith('C:')){const [,rs,cs]=id.split(':'),r=+rs,c=+cs;return[50+c*5.4,50+r*5.4]};if(id.startsWith('S:')||id.startsWith('R:')){let owner,lane,depth=0;if(id.startsWith('S:')){[,owner,lane]=id.split(':')}else{({owner,lane,depth}=parseRoad(id))}const arm=projectionFor(viewer)[owner],off={L:-5.7,C:0,R:5.7}[lane],base=17+depth*4.2;if(arm==='N')return[50+off,39-base+17];if(arm==='S')return[50-off,61+base-17];if(arm==='W')return[39-base+17,50-off];return[61+base-17,50+off]};return[8,8]} let fieldGL=null,fieldProgram=null,fieldProjection=new Map(),fieldMVP=null,fieldCameraState=null,fieldViewSize={w:1,h:1},routeDrag=null,fieldResizeQueued=false,fieldLastStats={mode:'none',vertices:0,grassTufts:0,lowPerf:false}; function nodeWorld(id,viewer){if(id.startsWith('C:')){const [,rs,cs]=id.split(':'),r=+rs,c=+cs;return{x:+cs*1.35,z:+rs*1.35,y:.24}};if(id.startsWith('S:')||id.startsWith('R:')){let owner,lane,depth=0;if(id.startsWith('S:')){[,owner,lane]=id.split(':');depth=0}else({owner,lane,depth}=parseRoad(id));const arm=projectionFor(viewer)[owner],off={L:-1.45,C:0,R:1.45}[lane],d=2.35+depth*1.25;if(arm==='N')return{x:off,z:-d,y:.24};if(arm==='S')return{x:-off,z:d,y:.24};if(arm==='W')return{x:-d,z:-off,y:.24};return{x:d,z:off,y:.24}};const k={NW:{x:-10,z:-10},NE:{x:10,z:-10},SW:{x:-10,z:10},SE:{x:10,z:10}}[id.split(':')[1]];return{k,y:.2,...k}} function vsub(a,b){return[a[0]-b[0],a[1]-b[1],a[2]-b[2]]}function vdot(a,b){return a[0]*b[0]+a[1]*b[1]+a[2]*b[2]}function vcross(a,b){return[a[1]*b[2]-a[2]*b[1],a[2]*b[0]-a[0]*b[2],a[0]*b[1]-a[1]*b[0]]}function vnorm(a){const l=Math.hypot(...a)||1;return a.map(x=>x/l)} function m4mul(a,b){const o=new Array(16).fill(0);for(let r=0;r<4;r++)for(let c=0;c<4;c++)for(let k=0;k<4;k++)o[r*4+c]+=a[r*4+k]*b[k*4+c];return o} function m4persp(fovy,aspect,near,far){const f=1/Math.tan(fovy/2),nf=1/(near-far);return[f/aspect,0,0,0,0,f,0,0,0,0,(far+near)*nf,-1,0,0,2*far*near*nf,0]} function m4look(eye,center,up){const z=vnorm(vsub(eye,center)),x=vnorm(vcross(up,z)),y=vcross(z,x);return[x[0],y[0],z[0],0,x[1],y[1],z[1],0,x[2],y[2],z[2],0,-vdot(x,eye),-vdot(y,eye),-vdot(z,eye),1]} function fieldViewportProfile(){const map=$('#battleMap'),w=Math.max(1,map?.clientWidth||innerWidth),h=Math.max(1,map?.clientHeight||innerHeight),raw=w/h;if(raw>1.85&&h<500)return{aspect:Math.max(1.2,Math.min(2.28,raw)),yBase:.36,yGain:.29,kind:'short-landscape'};if(raw<.78)return{aspect:1.2,yBase:.44,yGain:.40,kind:'portrait'};return{aspect:Math.max(1.2,raw),yBase:.45,yGain:.42,kind:'standard'}} function project3(w){const c=fieldCameraState;if(!c)return null;const eye=c.eye,rx=w.x-eye[0],ry=(w.y||0)-eye[1],rz=w.z-eye[2],q=Math.SQRT1_2,cx=rx,cy=q*ry-q*rz,cz=-q*ry-q*rz;if(cz<=.08)return null;const t=Math.tan(Math.PI/10.2),nx=cx/(cz*t*c.aspect),ny=cy/(cz*t);return{x:(nx*.5+.5)*100,y:(c.yBase-ny*c.yGain)*100}} function fieldFocusNodes(){const m=state.match;if(!m)return centerIds();const s=new Set([...activeNodes(),...m.players.map(p=>p.position)]);const me=m.players[0];if(m.phase==='plan'&&me.plan?.roadId)for(const id of reachable(me,normalMoveLimitForRoad(me.plan.roadId)).keys())s.add(id);if(m.honey?.byNode)Object.keys(m.honey.byNode).filter(id=>m.honey.byNode[id]>0).forEach(id=>s.add(id));return[...s]} // R14 camera responsibility: movement selection narrows plan focus; battle resolution may independently focus the confirmed collision. // Logic/hitboxes stay on reachable()/nodeWorld()/project3; this only chooses the camera eye. function fieldMoveFocusNodes(){const m=state.match,me=m?.players?.[0];if(!m||!me||m.phase!=='plan'||!me.plan?.roadId)return null;const moveLimit=normalMoveLimitForRoad(me.plan.roadId);if(moveLimit<=0)return null;const s=new Set([me.position,...reachable(me,moveLimit).keys(),...(me.plan?.path||[])]);return[...s]} function fieldBattleFocusNodes(){const m=state.match,p=m?.battlePresentation;if(!m||m.phase!=='resolve'||!m.target||!p)return null;const attacker=m.players.find(x=>x.id===p.attackerId),defender=m.players.find(x=>x.id===p.defenderId),s=new Set();if(attacker?.position)s.add(attacker.position);if(defender?.position)s.add(defender.position);s.add(shieldId(p.defenderId,p.shield));const depth=defender?.lanes?.[p.lane]?.length||0;if(depth>0)s.add(roadId(p.defenderId,p.lane,Math.min(7,depth)));return[...s]} function fieldCamera(viewer){const profile=fieldViewportProfile(),battleFocus=fieldBattleFocusNodes(),moveFocus=fieldMoveFocusNodes(),focus=battleFocus?.length?battleFocus:moveFocus;if(focus?.length){const pts=focus.map(id=>nodeWorld(id,viewer)),xs=pts.map(p=>p.x),zs=pts.map(p=>p.z),minX=Math.min(...xs),maxX=Math.max(...xs),minZ=Math.min(...zs),maxZ=Math.max(...zs),cx=(minX+maxX)/2,cz=(minZ+maxZ)/2;let r=2.4;for(const p of pts)r=Math.max(r,Math.hypot(p.x-cx,p.z-cz)+.9);const zoom=profile.kind==='portrait'?Math.max(12,r*2.65):profile.kind==='short-landscape'?Math.max(9.5,r*2.20):Math.max(11,r*2.45),eye=[cx,zoom,cz+zoom],center=[cx,0,cz];return{eye,center,r,moveFocus:!!moveFocus?.length,battleFocus:!!battleFocus?.length,...profile}}let r=11.5;for(const id of fieldFocusNodes()){const w=nodeWorld(id,viewer);r=Math.max(r,Math.hypot(w.x,w.z)+1.1)}r=Math.min(13,r);const zoom=profile.kind==='portrait'?Math.max(24,r*1.84):profile.kind==='short-landscape'?Math.max(18,r*1.38):Math.max(22,r*1.69),eye=[0,zoom,zoom],center=[0,0,0];return{eye,center,r,moveFocus:false,battleFocus:false,...profile}} function fieldProject(id,viewer){if(fieldProjection.has(id))return fieldProjection.get(id);return null} function nodeTopSurface(id,viewer){const nw=nodeWorld(id,viewer),typ=posType(id);let size=0,topY=nw.y;if(typ==='center'){size=1.06;topY=nw.y-.015+.16}else if(typ==='shield'){size=1.05;topY=nw.y+.24}else if(typ==='road'){if(!activeNodes().has(id))return null;size=.88;topY=nw.y-."},{"line":1690,"index":10054911,"text":"With('C:')){const [,rs,cs]=id.split(':'),r=+rs,c=+cs;return{x:+cs*1.35,z:+rs*1.35,y:.24}};if(id.startsWith('S:')||id.startsWith('R:')){let owner,lane,depth=0;if(id.startsWith('S:')){[,owner,lane]=id.split(':');depth=0}else({owner,lane,depth}=parseRoad(id));const arm=projectionFor(viewer)[owner],off={L:-1.45,C:0,R:1.45}[lane],d=2.35+depth*1.25;if(arm==='N')return{x:off,z:-d,y:.24};if(arm==='S')return{x:-off,z:d,y:.24};if(arm==='W')return{x:-d,z:-off,y:.24};return{x:d,z:off,y:.24}};const k={NW:{x:-10,z:-10},NE:{x:10,z:-10},SW:{x:-10,z:10},SE:{x:10,z:10}}[id.split(':')[1]];return{k,y:.2,...k}} function vsub(a,b){return[a[0]-b[0],a[1]-b[1],a[2]-b[2]]}function vdot(a,b){return a[0]*b[0]+a[1]*b[1]+a[2]*b[2]}function vcross(a,b){return[a[1]*b[2]-a[2]*b[1],a[2]*b[0]-a[0]*b[2],a[0]*b[1]-a[1]*b[0]]}function vnorm(a){const l=Math.hypot(...a)||1;return a.map(x=>x/l)} function m4mul(a,b){const o=new Array(16).fill(0);for(let r=0;r<4;r++)for(let c=0;c<4;c++)for(let k=0;k<4;k++)o[r*4+c]+=a[r*4+k]*b[k*4+c];return o} function m4persp(fovy,aspect,near,far){const f=1/Math.tan(fovy/2),nf=1/(near-far);return[f/aspect,0,0,0,0,f,0,0,0,0,(far+near)*nf,-1,0,0,2*far*near*nf,0]} function m4look(eye,center,up){const z=vnorm(vsub(eye,center)),x=vnorm(vcross(up,z)),y=vcross(z,x);return[x[0],y[0],z[0],0,x[1],y[1],z[1],0,x[2],y[2],z[2],0,-vdot(x,eye),-vdot(y,eye),-vdot(z,eye),1]} function fieldViewportProfile(){const map=$('#battleMap'),w=Math.max(1,map?.clientWidth||innerWidth),h=Math.max(1,map?.clientHeight||innerHeight),raw=w/h;if(raw>1.85&&h<500)return{aspect:Math.max(1.2,Math.min(2.28,raw)),yBase:.36,yGain:.29,kind:'short-landscape'};if(raw<.78)return{aspect:1.2,yBase:.44,yGain:.40,kind:'portrait'};return{aspect:Math.max(1.2,raw),yBase:.45,yGain:.42,kind:'standard'}} function project3(w){const c=fieldCameraState;if(!c)return null;const eye=c.eye,rx=w.x-eye[0],ry=(w.y||0)-eye[1],rz=w.z-eye[2],q=Math.SQRT1_2,cx=rx,cy=q*ry-q*rz,cz=-q*ry-q*rz;if(cz<=.08)return null;const t=Math.tan(Math.PI/10.2),nx=cx/(cz*t*c.aspect),ny=cy/(cz*t);return{x:(nx*.5+.5)*100,y:(c.yBase-ny*c.yGain)*100}} function fieldFocusNodes(){const m=state.match;if(!m)return centerIds();const s=new Set([...activeNodes(),...m.players.map(p=>p.position)]);const me=m.players[0];if(m.phase==='plan'&&me.plan?.roadId)for(const id of reachable(me,normalMoveLimitForRoad(me.plan.roadId)).keys())s.add(id);if(m.honey?.byNode)Object.keys(m.honey.byNode).filter(id=>m.honey.byNode[id]>0).forEach(id=>s.add(id));return[...s]} // R14 camera responsibility: movement selection narrows plan focus; battle resolution may independently focus the confirmed collision. // Logic/hitboxes stay on reachable()/nodeWorld()/project3; this only chooses the camera eye. function fieldMoveFocusNodes(){const m=state.match,me=m?.players?.[0];if(!m||!me||m.phase!=='plan'||!me.plan?.roadId)return null;const moveLimit=normalMoveLimitForRoad(me.plan.roadId);if(moveLimit<=0)return null;const s=new Set([me.position,...reachable(me,moveLimit).keys(),...(me.plan?.path||[])]);return[...s]} function fieldBattleFocusNodes(){const m=state.match,p=m?.battlePresentation;if(!m||m.phase!=='resolve'||!m.target||!p)return null;const attacker=m.players.find(x=>x.id===p.attackerId),defender=m.players.find(x=>x.id===p.defenderId),s=new Set();if(attacker?.position)s.add(attacker.position);if(defender?.position)s.add(defender.position);s.add(shieldId(p.defenderId,p.shield));const depth=defender?.lanes?.[p.lane]?.length||0;if(depth>0)s.add(roadId(p.defenderId,p.lane,Math.min(7,depth)));return[...s]} function fieldCamera(viewer){const profile=fieldViewportProfile(),battleFocus=fieldBattleFocusNodes(),moveFocus=fieldMoveFocusNodes(),focus=battleFocus?.length?battleFocus:moveFocus;if(focus?.length){const pts=focus.map(id=>nodeWorld(id,viewer)),xs=pts.map(p=>p.x),zs=pts.map(p=>p.z),minX=Math.min(...xs),maxX=Math.max(...xs),minZ=Math.min(...zs),maxZ=Math.max(...zs),cx=(minX+maxX)/2,cz=(minZ+maxZ)/2;let r=2.4;for(const p of pts)r=Math.max(r,Math.hypot(p.x-cx,p.z-cz)+.9);const zoom=profile.kind==='portrait'?Math.max(12,r*2.65):profile.kind==='short-landscape'?Math.max(9.5,r*2.20):Math.max(11,r*2.45),eye=[cx,zoom,cz+zoom],center=[cx,0,cz];return{eye,center,r,moveFocus:!!moveFocus?.length,battleFocus:!!battleFocus?.length,...profile}}let r=11.5;for(const id of fieldFocusNodes()){const w=nodeWorld(id,viewer);r=Math.max(r,Math.hypot(w.x,w.z)+1.1)}r=Math.min(13,r);const zoom=profile.kind==='portrait'?Math.max(24,r*1.84):profile.kind==='short-landscape'?Math.max(18,r*1.38):Math.max(22,r*1.69),eye=[0,zoom,zoom],center=[0,0,0];return{eye,center,r,moveFocus:false,battleFocus:false,...profile}} function fieldProject(id,viewer){if(fieldProjection.has(id))return fieldProjection.get(id);return null} function nodeTopSurface(id,viewer){const nw=nodeWorld(id,viewer),typ=posType(id);let size=0,topY=nw.y;if(typ==='center'){size=1.06;topY=nw.y-.015+.16}else if(typ==='shield'){size=1.05;topY=nw.y+.24}else if(typ==='road'){if(!activeNodes().has(id))return null;size=.88;topY=nw.y-.01+.105}else return null;const h=size/2,world=[[nw.x-h,topY,nw.z-h],[nw.x+h,topY,nw.z-h],[nw.x+h,topY,nw.z+h],[nw.x-h,topY,nw.z+h]],projected=world.map(([x,y,z])=>project3({x,y,z}));return projected.every(Boolean)?projected:null} function syncRangeSurfacePath(){const m=state.match,layer=$('#rangeSurfaceLayer');if(!m||!layer)return;const path=new Set(m.players[0]?.plan?.path||[]);[...layer.children].forEach(e=>e.classList.toggle('path',path.has(e.dataset.pos)))} function renderRangeSurfaces(reach,moveLimit,viewer){const layer=$('#rangeSurfaceLayer');if(!layer)return;layer.replaceChildren();if(!reach||moveLimit<=0)return;const ns='http://www.w3.org/2000/svg';for(const [id,d] of reach){if(d>moveLimit)continue;const pts=nodeTopSurface(id,viewer);if(!pts)continue;const e=document.createElementNS(ns,'polygon');e.setAttribute('class','rangeSurface');e.setAttribute('points',pts.map(p=>`${p.x},${p.y}`).join(' '));e.dataset.pos=id;layer.appendChild(e)}syncRangeSurfacePath()} function glShader(gl,type,src){const sh=gl.createShader(type);gl.shaderSource(sh,src);gl.compileShader(sh);if(!gl.getShaderParameter(sh,gl.COMPILE_STATUS))throw Error(gl.getShaderInfoLog(sh)||'shader');return sh} function initFieldGL(){const c=$('#fieldCanvas');if(fieldGL&&fieldGL.canvas===c&&!fieldGL.isContextLost?.())return fieldGL;let gl=null;try{gl=c.getContext('webgl',{alpha:false,antialias:!state.settings.lowPerf,depth:true,preserveDrawingBuffer:false,powerPreference:state.settings.lowPerf?'low-power':'high-performance'})}catch(e){}if(!gl)return null;const vs=glShader(gl,gl.VERTEX_SHADER,'attribute vec3 p;attribute vec3 n;attribute vec3 c;uniform vec3 uEye;uniform vec3 uLight;uniform vec4 uView;varying vec3 vColor;varying float vFog;void main(){vec3 r=p-uEye;float q=.70710678;float cx=r.x;float cy=q*r.y-q*r.z;float cz=max(.08,-q*r.y-q*r.z);float nx=cx/(cz*uView.x*uView.y);float ny=cy/(cz*uView.x);float yNdc=1.0-2.0*uView.z+2.0*ny*uView.w;float z01=clamp((cz-.10)/48.0,0.0,1.0);float zNdc=z01*2.0"},{"line":1728,"index":10069874,"text":"n(a,state.settings.lowPerf);pushFieldTrails(a,viewer);const grassTufts=pushGrasslandScenery(a,viewer,state.settings.lowPerf);for(const it of items){if(it.sy<=0)continue;if(it.kind==='inactive')pushLitBox(a,it.x,it.y+.006,it.z,.52,.024,.52,[.27,.30,.24]);else if(it.kind==='roadMark')pushLitBox(a,it.x,it.y,it.z,.10,.010,.10,it.col.map(v=>v*.72));else pushLitBox(a,it.x,it.y,it.z,it.sx,it.sy,it.sz,it.col)};const data=new Float32Array(a),buf=gl.createBuffer();gl.bindBuffer(gl.ARRAY_BUFFER,buf);gl.bufferData(gl.ARRAY_BUFFER,data,gl.DYNAMIC_DRAW);gl.useProgram(fieldProgram);const stride=36,ap=gl.getAttribLocation(fieldProgram,'p'),an=gl.getAttribLocation(fieldProgram,'n'),ac=gl.getAttribLocation(fieldProgram,'c');gl.enableVertexAttribArray(ap);gl.vertexAttribPointer(ap,3,gl.FLOAT,false,stride,0);gl.enableVertexAttribArray(an);gl.vertexAttribPointer(an,3,gl.FLOAT,false,stride,12);gl.enableVertexAttribArray(ac);gl.vertexAttribPointer(ac,3,gl.FLOAT,false,stride,24);gl.uniform3fv(gl.getUniformLocation(fieldProgram,'uEye'),new Float32Array(cam.eye));gl.uniform3f(gl.getUniformLocation(fieldProgram,'uLight'),-.42,.88,.26);gl.uniform4f(gl.getUniformLocation(fieldProgram,'uView'),Math.tan(Math.PI/10.2),cam.aspect,cam.yBase,cam.yGain);gl.drawArrays(gl.TRIANGLES,0,data.length/9);gl.deleteBuffer(buf);fieldLastStats={mode:'webgl-grassland',vertices:data.length/9,grassTufts,lowPerf:!!state.settings.lowPerf}} function renderField3D(){const m=state.match,canvas=$('#fieldCanvas');if(!m||!canvas)return;const dpr=state.settings.lowPerf?1:Math.min(1.5,window.devicePixelRatio||1),w=Math.max(1,Math.floor(canvas.clientWidth*dpr)),h=Math.max(1,Math.floor(canvas.clientHeight*dpr));if(canvas.width!==w||canvas.height!==h){canvas.width=w;canvas.height=h}const viewer=m.players[0],cam=fieldCamera(viewer);fieldCameraState=cam;fieldMVP=null;fieldProjection.clear();const all=[];centerIds().forEach(id=>all.push(id));['P1','P2','P3','P4'].forEach(pid=>['L','C','R'].forEach(l=>{all.push(shieldId(pid,l));for(let d=1;d<=7;d++)all.push(roadId(pid,l,d))}));['K:NW','K:NE','K:SW','K:SE'].forEach(id=>all.push(id));all.forEach(id=>{const p=project3(nodeWorld(id,viewer));if(p)fieldProjection.set(id,p)});const items=[];const box=(x,y,z,sx,sy,sz,col,kind='box')=>items.push({x,y,z,sx,sy,sz,col,kind});const active=activeNodes();for(const id of all){const nw=nodeWorld(id,viewer),typ=posType(id);if(typ==='center'){box(nw.x,nw.y-.015,nw.z,1.06,.16,1.06,[.27,.32,.28],'center');box(nw.x,nw.y+.15,nw.z,.56,.018,.56,[.72,.62,.34],'centerMark')}else if(typ==='shield'){const owner=id.split(':')[1],pc=fieldPlayerColor(owner,true);box(nw.x,nw.y,nw.z,1.05,.24,1.05,[.25,.18,.30],'shield');box(nw.x,nw.y+.245,nw.z,.62,.025,.62,pc,'shieldMark')}else if(typ==='road'){const owner=parseRoad(id).owner,pc=fieldPlayerColor(owner,active.has(id)),base=active.has(id)?[.46,.49,.43]:[.31,.35,.31];box(nw.x,nw.y+(active.has(id)?-.01:-.045),nw.z,active.has(id)?.88:.78,active.has(id)?.105:.026,active.has(id)?.88:.78,base,active.has(id)?'road':'inactive');box(nw.x,nw.y+(active.has(id)?.10:-.017),nw.z,active.has(id)?.42:.16,.014,active.has(id)?.42:.16,pc,active.has(id)?'roadMarkActive':'roadMark')}else if(typ==='corner'){box(nw.x,nw.y-.02,nw.z,.82,.10,.82,[.40,.42,.36],'corner')}}if(m.honey?.byNode)for(const [id,n] of Object.entries(m.honey.byNode)){if(n<=0)continue;const q=nodeWorld(id,viewer);box(q.x,q.y+.18,q.z,.30,.50,.30,[.98,.61,.06],'honey')} const gl=initFieldGL();if(gl){drawGrasslandGL(gl,items,viewer,cam,w,h)}else{fieldLastStats={mode:'canvas2d-fallback',vertices:0,grassTufts:0,lowPerf:!!state.settings.lowPerf};const ctx=canvas.getContext('2d');if(ctx){ctx.setTransform(dpr,0,0,dpr,0,0);const cw=canvas.clientWidth,ch=canvas.clientHeight;const proj=(x,y,z)=>{const q=project3({x,y,z});return q?[q.x/100*cw,q.y/100*ch]:[0,0]}, rgba=(c,a)=>`rgba(${c.map(v=>Math.max(0,Math.min(255,Math.round(v*255)))).join(',')},${a})`, shade=(c,k)=>`rgb(${c.map(v=>Math.max(0,Math.min(255,Math.round(v*255*k)))).join(',')})`; /* Water and atmospheric depth are visual-only. The 109-node graph/hit areas remain separate DOM/runtime data. */ const water=ctx.createLinearGradient(0,0,0,ch);water.addColorStop(0,'#0a2b31');water.addColorStop(.36,'#0b3a37');water.addColorStop(1,'#061a1b');ctx.fillStyle=water;ctx.fillRect(0,0,cw,ch);const horizon=ctx.createLinearGradient(0,0,cw,0);horizon.addColorStop(0,'rgba(46,107,92,.02)');horizon.addColorStop(.5,'rgba(117,190,159,.15)');horizon.addColorStop(1,'rgba(46,107,92,.02)');ctx.fillStyle=horizon;ctx.fillRect(0,0,cw,ch*.48); ctx.save();ctx.globalAlpha=state.settings.lowPerf?.14:.24;ctx.strokeStyle='#4f8f87';ctx.lineWidth=1;for(let j=0;j<7;j++){const y=ch*(.18+j*.115);ctx.beginPath();for(let x=-20;x<=cw+20;x+=26){const yy=y+Math.sin((x+j*37)*.024)*2.3;const nx=x+26,ny=y+Math.sin((nx+j*37)*.024)*2.3;if(x<0)ctx.moveTo(x,yy);else ctx.quadraticCurveTo(x+13,yy+1.6,nx,ny)}ctx.stroke()}ctx.restore(); function worldPath(points,y=-.26){ctx.beginPath();points.forEach(([x,z],i)=>{const p=proj(x,y,z);if(i===0)ctx.moveTo(p[0],p[1]);else ctx.lineTo(p[0],p[1])});ctx.closePath()} const coast=[[-10,-5],[-8.7,-8.1],[-5.3,-9.7],[-1.4,-9.1],[2.4,-9.8],[6.2,-8.8],[9.1,-5.9],[9.8,-1.8],[9.0,2.3],[10.0,6.0],[6.8,9.1],[2.2,9.5],[-1.8,9.0],[-5.9,9.6],[-9.1,6.8],[-9.8,2.3],[-9.2,-1.5]];worldPath(coast);ctx.shadowColor='rgba(0,0,0,.50)';ctx.shadowBlur=state.settings.lowPerf?5:15;ctx.shadowOffsetY=7;const island=ctx.createLinearGradient(0,ch*.2,0,ch*.88);island.addColorStop(0,'#405b42');island.addColorStop(.48,'#2d4935');island.addColorStop(1,'#18322b');ctx.fillStyle=island;ctx.fill();ctx.shadowBlur=0;ctx.shadowOffsetY=0;ctx.strokeStyle='#6c8267';ctx.lineWidth=Math.max(2,cw*.0032);ctx.stroke();worldPath(coast,.01);ctx.strokeStyle='rgba(117,169,126,.24)';ctx.lineWidth=Math.max(1,cw*.0016);ctx.stroke(); const inner=[[-3.7,-3.0],[-1.8,-4.1],[1.2,-4.0],[3.8,-2.7],[4.2,.3],[3.0,3.4],[.7,4.3],[-2.3,3.8],[-4.0,1.5]];worldPath(inner,-.08);const moss=ctx.createLinearGradient(cw*.3,ch*.3,cw*.7,ch*.7);moss.addColorStop(0,'#526e50');moss.addColorStop(1,'#304b39');ctx.fillStyle=moss;ctx.fill();ctx.strokeStyle='rgba(183,205,154,.28)';ctx.lineWidth=Math.max(1.5,cw*.002);ctx.stroke();function patch(poly,fill){worldPath(poly,-.045);ctx.fillStyle=fill;ctx.fill()}patch([[-8,-3.2],[-6.1,-4.5],[-4.7,-3.8],[-5.2,-1.9],[-7.2,-1.4]],'rgba(92,116,70,.16)');patch([[4.8,2.0],[7.4,1.2],[8.3,3.3],[6.9,5.1],[5.1,4.5]],'rgba(107,126,73,.13)');patch([[-6.8,4.5],[-4.8,5.0],[-3.7,7.0],[-6.1,8.0],[-7.7,6.5]],'rgba(75,100,67,.17)');patch([[4.0,-6.8],[6.2,-7.8],[7.8,-6.2],[7.0,-4.1],[5.0,-4.4]],'rgba(76,99,70,.16)'); function lanePoints(owner,lane){const ids=[shieldId(owner,lane)];for(let d=1;d<=7;d++)ids.push(roadId(owner,lane,d));return ids.map(id=>{const q=fieldProjection.get(id);return q?[q.x/100*cw,q.y/100*ch]:null}).filter(Boolean)} ctx.save();ctx.lineCap='round';ctx.lineJoin='round';for(const owner of ['P1','P2','P3','P4'])for(const lane of ['L','C','R']){const pts=lanePoints(owner,lane);if(pts.length<2)conti"},{"line":1835,"index":10125701,"text":"ion={battlePoints:0};load();return{battlePoints:state.progression.battlePoints,storage:state.storage}}};window.GAMEROAD_3D_WALK_QA={version:'BROWSER-3D-BOARD-WALK-INTEGRATION-001',snapshot(){const m=state.match,me=m?.players?.find(p=>p.human);return{screen:state.screen,phase:m?.phase||null,position:me?.position||null,path:me?.plan?.path?[...me.plan.path]:null,avatarState:battleMount?.root?.dataset?.state||null,avatarFacing:battleMount?.facingName||null,nodeCount:document.querySelectorAll('#board .node').length,canvas:{w:$('#fieldCanvas')?.width||0,h:$('#fieldCanvas')?.height||0}}}};window.GAMEROAD_GRASSLAND3D_QA={version:'BROWSER-BOARD-GRASSLAND-3D-R4',snapshot(){const gl=$('#fieldCanvas')?.getContext('webgl');return{...fieldLastStats,webgl:!!gl,contextLost:!!gl?.isContextLost?.(),projectionCount:fieldProjection.size,nodeCount:document.querySelectorAll('#board .node').length,screen:state.screen}}}; window.GAMEROAD_BOARD_REVIEW_QA={version:'BROWSER-BOARD-PLAYTEST-REVIEW-LOOP-001',_rect(e){const b=e?.getBoundingClientRect();return b?{l:b.left,r:b.right,t:b.top,b:b.bottom,w:b.width,h:b.height}:null},_hit(a,b){return!!(a&&b&&Math.min(a.r,b.r)>Math.max(a.l,b.l)&&Math.min(a.b,b.b)>Math.max(a.t,b.t))},snapshot(){const rect=this._rect.bind(this),hit=this._hit.bind(this),nodes=[...document.querySelectorAll('#board .node')].map(e=>({id:e.dataset.pos,rect:rect(e)})),surfaces={publicHud:rect($('#publicTurnHud')),battleInfo:rect(document.querySelector('.battleInfo')),rail:rect(document.querySelector('.battleRail')),top:rect(document.querySelector('.battleTopStatus'))},collisions={};for(const[k,r]of Object.entries(surfaces))collisions[k]=nodes.filter(n=>hit(n.rect,r)).map(n=>n.id);return{screen:state.screen,phase:state.match?.phase||null,viewport:{w:innerWidth,h:innerHeight},profile:fieldCameraState?.kind||null,nodeCount:nodes.length,map:rect($('#battleMap')),collisions,criticalOverlapCount:collisions.publicHud.length+collisions.battleInfo.length,avatar:{state:battleMount?.root?.dataset?.state||null,facing:battleMount?.facingName||null}}},setupSnapshot(){const rect=this._rect.bind(this),hit=this._hit.bind(this),header=rect(document.querySelector('header.top')),controls=['[data-content=\"road_shield\"]','[data-content=\"honey_hunt\"]','[data-mode=\"2p\"]','[data-mode=\"4p\"]','[data-mode=\"2v2\"]','#startMatch'].map(sel=>({sel,rect:rect(document.querySelector(sel))}));return{viewport:{w:innerWidth,h:innerHeight},header,controls,headerCollisions:controls.filter(c=>hit(c.rect,header)).map(c=>c.sel),offscreen:controls.filter(c=>!c.rect||c.rect.t<0||c.rect.l<0||c.rect.r>innerWidth||c.rect.b>innerHeight).map(c=>c.sel)}}}; window.GAMEROAD_VISUAL_SHELL_QA={version:'BROWSER-PLAYTEST-VISUAL-SHELL-REVIEW-001',rect(sel){const e=document.querySelector(sel),r=e?.getBoundingClientRect();return r?{l:r.left,t:r.top,r:r.right,b:r.bottom,w:r.width,h:r.height}:null},inside(sel){const r=this.rect(sel);return!!(r&&r.l>=0&&r.t>=0&&r.r<=innerWidth&&r.b<=innerHeight)},snapshot(){return{screen:state.screen,viewport:{w:innerWidth,h:innerHeight},contextVisible:$('#contextHelp')?.classList.contains('on')||false,home:{primary:this.rect('.codexBattleCta'),character:null,live:this.rect('.codexHomeCenterStage')},setup:{box:this.rect('.setupBox'),start:this.rect('#startMatch'),character:this.rect('#setupRuntime'),goal:this.rect('.setupGoalCard')},result:{rank:this.rect('.resultRank'),body:this.rect('.resultBody'),buttons:this.rect('.resultBtns'),character:this.rect('.resultWorld .grtc-image')}}}}; function positionLabel(id){if(id.startsWith('C:'))return '中央';if(id.startsWith('S:'))return 'シールド境界';if(id.startsWith('R:')){const r=parseRoad(id);return `${r.owner} ${r.lane==='L'?'左':r.lane==='C'?'中央':'右'}列 ${r.depth}`;}return '盤面';}function renderPlayers(){const m=state.match,root=$('#players');root.innerHTML='';m.players.forEach(p=>{const d=document.createElement('div');d.className='player'+(p.human?' you':'')+(p.id===m.activeId?' active':'');d.innerHTML=`<b>${p.name}</b> ${m.mode==='2v2'?`<span>組${p.team}</span>`:''}<div>手札 ${p.hand.length} / 山札 ${p.deck.length} / チップ ${p.chip.length}</div><div class=\"royalUsed\">ロイヤル使用 ${royalUsedLabel(p)}</div><div>位置 ${positionLabel(p.position)}${p.battleBlockedRound===m.round?' / バトルカード提出禁止':''}${p.nextCostReduction?` / 次費用-${p.nextCostReduction}`:''}</div><div class=\"laneMini\"><i>左 ${p.lanes.L.length}</i><i>中 ${p.lanes.C.length}</i><i>右 ${p.lanes.R.length}</i></div>`;root.appendChild(d)})} function resolutionOriginLabel(origin){return({road:'ロード',active_submission:'バトル',auto_defense:'シールド',ability_active_addition:'能力追加'})[origin]||'公開札'} function laneLabel(l){return l==='L'?'左列':l==='C'?'中央列':'右列'} function resolutionPublicLogLine(r){const compare=r.mode==='2v2'?`組A ${r.teamTotals?.A??0} / 組B ${r.teamTotals?.B??0}`:r.players.map(p=>`${p.name} ${p.score}`).join(' / '),cards=r.players.map(p=>`${p.name}：${p.cards.length?p.cards.map(c=>`${resolutionOriginLabel(c.origin)} ${c.label} ${c.value}`).join('＋'):'提出なし'}`).join(' / '),gains=r.laneGains.map(g=>{const p=r.players.find(x=>x.id===g.id);return `${p?.name||g.id} ${laneLabel(g.lane)} +${g.added}枚（${g.before}→${g.after}）`}).join(' / ');return `戦闘結果 第${r.round}巡：${compare}。${cards}${gains?`。${gains}`:''}`} const BATTLE_PRESENTATION_STEP_MS=180,BATTLE_PRESENTATION_RESULT_MS=2600; const battlePresentationRuntime={fast:false,timer:null,advance:null}; const PUBLIC_CARD_PRESENTATION_SCHEMA='GAMEROAD_PUBLIC_CARD_PRESENTATION_INPUT_V1'; const FORMAL_CARD_PRESENTATION_MANIFEST=Object.freeze({schema:'GAMEROAD_FORMAL_CARD_PRESENTATION_MANIFEST_V1',entries:Object.freeze({})}); const PUBLIC_CARD_PRESENTATION_ORIGINS=new Set(['active_submission','auto_defense']); const FORBIDDEN_PRESENTATION_OUTPUT_KEYS=new Set(['hand','hands','plan','plans','battleId','roadId','shields','mana','awake','hate','secret','reservation','deck','drawPile','privateState','currentValue','score']); function presentationText(value,field){if(typeof value!=='string'||!value.length)throw Error(`invalid-${field}`);return value} function presentationRound(value){const n=Number(value);if(!Number.isInteger(n)||n<1)throw Error('invalid-round');return n} function presentationLane(value){return ['L','C','R'].includes(value)?value:null} function acceptedRevealPresentation(manifest,cardId){const entry=manifest?.entries?.[cardId];if(!entry||entry.status!=='accepted'||entry.triggerStage!=='on_reveal'||typeof entry.presentationKey!=='string'||!entry.presentationKey.length)return null;return entry.presentationKey} function assertNoForbiddenPresentationKeys(value){if(Array.isArray(value)){value.forEach(assertNoForbiddenPresentationKeys);return}if(!value||typeof value!=='object')return;for(const [key,child] of Object.entries(value)){if(FORBIDDEN_PRESENTATION_OUTPUT_KEYS.has(key))throw Error(`forbidden-presentation-output:${key}`);assertNoForbiddenPresentationKeys(child)}} function buildPublicCardPresentationInputs(reveal,acceptedManifest=FORMAL_CARD_PRESENTATION_MANIFEST){if(!reveal||typeof reveal!=='object')throw Er"},{"line":2378,"index":10232520,"text":"d.defenderId&&d.lane&&d.shield){confirm.disabled=false;delete confirm.dataset.precommitClearDisabled} grSyncBattlePrecommitClear() }); grEnsureBattlePrecommitClearButton();grSyncBattlePrecommitClear(); window.GAMEROAD_PARTNER_STATE=Object.freeze({partner:()=>{const c=partnerInfo();return{id:c.id,name:c.name}},player:()=>{const c=playerCharInfo();return{id:c.id,name:c.name}},snapshot:()=>({selectedPartnerId:state.selectedPartnerId,playerCharacterId:state.playerCharacterId,profiles:safeClone(state.partnerProfiles)})}); window.__GAMEROAD_TEST__={state,show,partnerRoles:()=>({selectedPartnerId:state.selectedPartnerId,playerCharacterId:state.playerCharacterId,profiles:safeClone(state.partnerProfiles)}),setPartnerRole:(id)=>{if(!PROD_CHARS.some(c=>c.id===id))return false;state.selectedPartnerId=id;save();return true},setPlayerCharacter:(id)=>{if(!isPlayablePlayerCharacter(id))return false;state.playerCharacterId=id;save();return true},battleResolution:()=>safeClone(state.match?.lastBattleResolution||null),battlePresentation:()=>safeClone(state.match?.battlePresentation||null),battleResolutionVisible:()=>!$('#battleResolution')?.hidden,battlePresentationFast:(v=true)=>{battlePresentationRuntime.fast=!!v;if(v)advanceBattlePresentation();return battlePresentationRuntime.fast},battlePresentationTimings:()=>({step:BATTLE_PRESENTATION_STEP_MS,read:BATTLE_PRESENTATION_STEP_MS,result:BATTLE_PRESENTATION_RESULT_MS}),publicCardPresentationBuild:(reveal,manifest=FORMAL_CARD_PRESENTATION_MANIFEST)=>safeClone(buildPublicCardPresentationInputs(reveal,manifest)),publicCardPresentationManifest:()=>safeClone(FORMAL_CARD_PRESENTATION_MANIFEST),battlePresentationRender:()=>{renderBattle();return true},battlePresentationProbe:()=>{const e=$('#battleResolution'),r=e?.getBoundingClientRect(),c=fieldCameraState;return{stage:state.match?.battlePresentation?.stage||null,phase:state.match?.phase||null,busy:!!state.match?.busy,hidden:!!e?.hidden,players:e?.querySelectorAll('.resolutionPlayer').length||0,advance:!!e?.querySelector('.resolutionAdvance'),text:e?.innerText?.slice(0,800)||'',rect:r?[r.x,r.y,r.width,r.height]:null,overflowX:e?e.scrollWidth-e.clientWidth:0,overflowY:e?e.scrollHeight-e.clientHeight:0,docOverflow:document.documentElement.scrollWidth-document.documentElement.clientWidth,camera:c?{moveFocus:!!c.moveFocus,battleFocus:!!c.battleFocus,kind:c.kind,eye:c.eye,center:c.center}:null}},targetLegalIds:(id)=>{const p=state.match?.players.find(x=>x.id===id);return p?legalOpponents(p).map(x=>x.id):[]},forceAutoTarget:(id)=>{const p=state.match?.players.find(x=>x.id===id);if(!p)throw Error('player-missing');autoTarget(p);return {...state.match.target}},start:(mode='2p',content=state.setupContent)=>{state.setupMode=mode;state.setupContent=content;startMatch();return state.match},autoRound,autoToResult,cardConservation,boardCount:()=>$('#board').children.length,visibleButtons:()=>$$('button').filter(b=>{const r=b.getBoundingClientRect(),s=getComputedStyle(b);return r.width>0&&r.height>0&&s.visibility!=='hidden'&&s.display!=='none'}).length,surfaceNames:()=>$$('.screen').map(x=>x.dataset.screen),missionProgress:()=>({matches:state.history.length,wins:historyWins(),modes:historyModes()}),packPreview:()=>{state.lastPackPreview=buildPackPreview();return state.lastPackPreview.map(x=>x.ticket?'ticket':x.id)},projectionIndependent:()=>{const p=state.match?.players?.[0];if(!p)return true;const before=JSON.stringify(state.match.players.map(x=>x.lanes));const orig=[...state.match.projection];state.match.projection.reverse();const after=JSON.stringify(state.match.players.map(x=>x.lanes));state.match.projection=orig;return before===after},save,load,deckRule:()=>safeClone(DECK_RULE),isTrumpCard:(id)=>isTrumpCard(id),isRoyalCard:(id)=>isRoyalCard(id),royalUsed:(p)=>publicRoyalUsed(p),deckValidate:(deck=state.deckDraft,opts={})=>validateDeck(deck,opts),deckSetDraft:(main,ex=[])=>{state.deckDraft={main:[...main],ex:[...ex]};return validateDeck(state.deckDraft)},deckDraftSessionKey:()=>DECK_DRAFT_SESSION_KEY,deckDraftSessionSave:saveDeckDraftSession,deckDraftSessionRecover:recoverDeckDraftSession,deckDraftSessionClear:clearDeckDraftSession,deckRestore:restoreDeck,deckDefault:defaultDeckDraft,deckCommit:commitDeck,deckSaved:()=>safeClone(state.savedDeck),deckPublic:()=>window.__CARD_DATA__.filter(publicCard).map(c=>({id:c.id,slot:deckSlot(c.id)})),makePlayerTest:(ids)=>makePlayer(0,true,ids)}; window.__V105_ABILITY_TEST__={makeParticipant,resolveAbilityBundle,effectiveAbilityCost,payAbility,participantEntries,sortAbilityEntries,abilityOrderReceiptKey,matchRnd,initRoundRuntime,clearAbilityOrderReceipts:()=>{state.match?.abilityOrderReceipts?.clear();return state.match?.abilityOrderReceipts?.size||0},abilityOrderReceiptSize:()=>state.match?.abilityOrderReceipts?.size||0,setAutoChoices:(v=true)=>{if(state.match)state.match.testAutoChoices=!!v},setAwake:(pid,n)=>{const p=state.match?.players.find(x=>x.id===pid);if(p)p.awake=Math.max(0,Math.min(p.mana.length,n));return p?.awake},setReduction:(pid,n)=>{const p=state.match?.players.find(x=>x.id===pid);if(p)p.nextCostReduction=Math.max(0,n);return p?.nextCostReduction},clearParticipants:()=>{if(state.match){state.match.participants=new Map(state.match.players.map(p=>[p.id,[]]));state.match.participantSeq=0;state.match.resolvedAbilityKeys=new Set()}},addLane:(pid,lane,id)=>{const p=state.match?.players.find(x=>x.id===pid);if(!p||!CARDS[id])return false;const ix=p.deck.indexOf(id);if(ix>=0)p.deck.splice(ix,1);else{const hi=p.hand.indexOf(id);if(hi>=0)p.hand.splice(hi,1);else return false}p.lanes[lane].push(id);return true},snapshot:()=>state.match?{round:state.match.round,players:state.match.players.map(p=>({id:p.id,awake:p.awake,reduction:p.nextCostReduction,blocked:p.battleBlockedRound,hand:[...p.hand],deck:[...p.deck],chip:[...p.chip],lanes:safeClone(p.lanes)})),participants:participantEntries().map(e=>({...e}))}:null}; window.__GAMEROAD_CARD_RUNTIME__={ normalTargetIds:window.__CARD_DATA__.map(c=>c.id), playableIds:window.__CARD_DATA__.filter(c=>c.runtime_status==='playable').map(c=>c.id), blockedIds:window.__CARD_DATA__.filter(c=>c.runtime_status!=='playable').map(c=>c.id), publicIds:window.__CARD_DATA__.filter(publicCard).map(c=>c.id), injectCard:(pid,cardId)=>{const m=state.match;if(!m||!CARDS[cardId])return false;const p=m.players.find(x=>x.id===pid);if(!p)return false;const old=p.hand[0];if(!old)return false;if(old===cardId)return true;p.hand[0]=cardId;const ix=p.deck.indexOf(cardId);if(ix>=0)p.deck[ix]=old;else if(p.deck.length)p.deck[0]=old;else return false;renderBattle();return true}, injectCardAt:(pid,cardId,slot=0)=>{const m=state.match;if(!m||!CARDS[cardId])return false;const p=m.players.find(x=>x.id===pid);if(!p||slot<0||slot>=p.hand.length)return false;const old=p.hand[slot];if(old===cardId)return true;const zones=[p.deck,p.hand,p.chip,p.lanes.L,p.lanes.C,p.lanes.R];for(const z of zones){const ix=z.indexOf(cardId);if(ix>=0){z[ix]=old;p.hand[slot]=cardId;render"}],"fieldCameraDef":[{"line":1696,"index":10056650,"text":"),-vdot(y,eye),-vdot(z,eye),1]} function fieldViewportProfile(){const map=$('#battleMap'),w=Math.max(1,map?.clientWidth||innerWidth),h=Math.max(1,map?.clientHeight||innerHeight),raw=w/h;if(raw>1.85&&h<500)return{aspect:Math.max(1.2,Math.min(2.28,raw)),yBase:.36,yGain:.29,kind:'short-landscape'};if(raw<.78)return{aspect:1.2,yBase:.44,yGain:.40,kind:'portrait'};return{aspect:Math.max(1.2,raw),yBase:.45,yGain:.42,kind:'standard'}} function project3(w){const c=fieldCameraState;if(!c)return null;const eye=c.eye,rx=w.x-eye[0],ry=(w.y||0)-eye[1],rz=w.z-eye[2],q=Math.SQRT1_2,cx=rx,cy=q*ry-q*rz,cz=-q*ry-q*rz;if(cz<=.08)return null;const t=Math.tan(Math.PI/10.2),nx=cx/(cz*t*c.aspect),ny=cy/(cz*t);return{x:(nx*.5+.5)*100,y:(c.yBase-ny*c.yGain)*100}} function fieldFocusNodes(){const m=state.match;if(!m)return centerIds();const s=new Set([...activeNodes(),...m.players.map(p=>p.position)]);const me=m.players[0];if(m.phase==='plan'&&me.plan?.roadId)for(const id of reachable(me,normalMoveLimitForRoad(me.plan.roadId)).keys())s.add(id);if(m.honey?.byNode)Object.keys(m.honey.byNode).filter(id=>m.honey.byNode[id]>0).forEach(id=>s.add(id));return[...s]} // R14 camera responsibility: movement selection narrows plan focus; battle resolution may independently focus the confirmed collision. // Logic/hitboxes stay on reachable()/nodeWorld()/project3; this only chooses the camera eye. function fieldMoveFocusNodes(){const m=state.match,me=m?.players?.[0];if(!m||!me||m.phase!=='plan'||!me.plan?.roadId)return null;const moveLimit=normalMoveLimitForRoad(me.plan.roadId);if(moveLimit<=0)return null;const s=new Set([me.position,...reachable(me,moveLimit).keys(),...(me.plan?.path||[])]);return[...s]} function fieldBattleFocusNodes(){const m=state.match,p=m?.battlePresentation;if(!m||m.phase!=='resolve'||!m.target||!p)return null;const attacker=m.players.find(x=>x.id===p.attackerId),defender=m.players.find(x=>x.id===p.defenderId),s=new Set();if(attacker?.position)s.add(attacker.position);if(defender?.position)s.add(defender.position);s.add(shieldId(p.defenderId,p.shield));const depth=defender?.lanes?.[p.lane]?.length||0;if(depth>0)s.add(roadId(p.defenderId,p.lane,Math.min(7,depth)));return[...s]} function fieldCamera(viewer){const profile=fieldViewportProfile(),battleFocus=fieldBattleFocusNodes(),moveFocus=fieldMoveFocusNodes(),focus=battleFocus?.length?battleFocus:moveFocus;if(focus?.length){const pts=focus.map(id=>nodeWorld(id,viewer)),xs=pts.map(p=>p.x),zs=pts.map(p=>p.z),minX=Math.min(...xs),maxX=Math.max(...xs),minZ=Math.min(...zs),maxZ=Math.max(...zs),cx=(minX+maxX)/2,cz=(minZ+maxZ)/2;let r=2.4;for(const p of pts)r=Math.max(r,Math.hypot(p.x-cx,p.z-cz)+.9);const zoom=profile.kind==='portrait'?Math.max(12,r*2.65):profile.kind==='short-landscape'?Math.max(9.5,r*2.20):Math.max(11,r*2.45),eye=[cx,zoom,cz+zoom],center=[cx,0,cz];return{eye,center,r,moveFocus:!!moveFocus?.length,battleFocus:!!battleFocus?.length,...profile}}let r=11.5;for(const id of fieldFocusNodes()){const w=nodeWorld(id,viewer);r=Math.max(r,Math.hypot(w.x,w.z)+1.1)}r=Math.min(13,r);const zoom=profile.kind==='portrait'?Math.max(24,r*1.84):profile.kind==='short-landscape'?Math.max(18,r*1.38):Math.max(22,r*1.69),eye=[0,zoom,zoom],center=[0,0,0];return{eye,center,r,moveFocus:false,battleFocus:false,...profile}} function fieldProject(id,viewer){if(fieldProjection.has(id))return fieldProjection.get(id);return null} function nodeTopSurface(id,viewer){const nw=nodeWorld(id,viewer),typ=posType(id);let size=0,topY=nw.y;if(typ==='center'){size=1.06;topY=nw.y-.015+.16}else if(typ==='shield'){size=1.05;topY=nw.y+.24}else if(typ==='road'){if(!activeNodes().has(id))return null;size=.88;topY=nw.y-.01+.105}else return null;const h=size/2,world=[[nw.x-h,topY,nw.z-h],[nw.x+h,topY,nw.z-h],[nw.x+h,topY,nw.z+h],[nw.x-h,topY,nw.z+h]],projected=world.map(([x,y,z])=>project3({x,y,z}));return projected.every(Boolean)?projected:null} function syncRangeSurfacePath(){const m=state.match,layer=$('#rangeSurfaceLayer');if(!m||!layer)return;const path=new Set(m.players[0]?.plan?.path||[]);[...layer.children].forEach(e=>e.classList.toggle('path',path.has(e.dataset.pos)))} function renderRangeSurfaces(reach,moveLimit,viewer){const layer=$('#rangeSurfaceLayer');if(!layer)return;layer.replaceChildren();if(!reach||moveLimit<=0)return;const ns='http://www.w3.org/2000/svg';for(const [id,d] of reach){if(d>moveLimit)continue;const pts=nodeTopSurface(id,viewer);if(!pts)continue;const e=document.createElementNS(ns,'polygon');e.setAttribute('class','rangeSurface');e.setAttribute('points',pts.map(p=>`${p.x},${p.y}`).join(' '));e.dataset.pos=id;layer.appendChild(e)}syncRangeSurfacePath()} function glShader(gl,type,src){const sh=gl.createShader(type);gl.shaderSource(sh,src);gl.compileShader(sh);if(!gl.getShaderParameter(sh,gl.COMPILE_STATUS))throw Error(gl.getShaderInfoLog(sh)||'shader');return sh} function initFieldGL(){const c=$('#fieldCanvas');if(fieldGL&&fieldGL.canvas===c&&!fieldGL.isContextLost?.())return fieldGL;let gl=null;try{gl=c.getContext('webgl',{alpha:false,antialias:!state.settings.lowPerf,depth:true,preserveDrawingBuffer:false,powerPreference:state.settings.lowPerf?'low-power':'high-performance'})}catch(e){}if(!gl)return null;const vs=glShader(gl,gl.VERTEX_SHADER,'attribute vec3 p;attribute vec3 n;attribute vec3 c;uniform vec3 uEye;uniform vec3 uLight;uniform vec4 uView;varying vec3 vColor;varying float vFog;void main(){vec3 r=p-uEye;float q=.70710678;float cx=r.x;float cy=q*r.y-q*r.z;float cz=max(.08,-q*r.y-q*r.z);float nx=cx/(cz*uView.x*uView.y);float ny=cy/(cz*uView.x);float yNdc=1.0-2.0*uView.z+2.0*ny*uView.w;float z01=clamp((cz-.10)/48.0,0.0,1.0);float zNdc=z01*2.0-1.0;gl_Position=vec4(nx*cz,yNdc*cz,zNdc*cz,cz);vec3 nn=normalize(n);float sun=max(dot(nn,normalize(uLight)),0.0);float hemi=max(nn.y,0.0);vColor=c*(.53+.39*sun+.12*hemi);vFog=smoothstep(23.0,43.0,cz);}'),fs=glShader(gl,gl.FRAGMENT_SHADER,'precision mediump float;varying vec3 vColor;varying float vFog;void main(){vec3 haze=vec3(.54,.69,.63);vec3 col=mix(clamp(vColor,0.0,1.0),haze,vFog*.52);gl_FragColor=vec4(col,1.0);}');const pr=gl.createProgram();gl.attachShader(pr,vs);gl.attachShader(pr,fs);gl.linkProgram(pr);if(!gl.getProgramParameter(pr,gl.LINK_STATUS))throw Error(gl.getProgramInfoLog(pr)||'link');fieldGL=gl;fieldProgram=pr;if(!c.dataset.grasslandGlLifecycle){c.dataset.grasslandGlLifecycle='1';c.addEventListener('webglcontextlost',e=>{e.preventDefault();fieldGL=null;fieldProgram=null});c.addEventListener('webglcontextrestored',()=>{fieldGL=null;fieldProgram=null;if(state.screen==='battle'){renderField3D();renderBoard()}})}return gl} function pushBox(arr,x,y,z,sx,sy,sz,col){const [r,g,b]=col,x0=x-sx/2,x1=x+sx/2,y0=y,y1=y+sy,z0=z-sz/2,z1=z+sz/2;const V=[[x0,y0,z0],[x1,y0,z0],[x1,y0,z1],[x0,y0,z1],[x0,y1,z0],[x1,y1,z0],[x1,y1,z1],[x0,y1,z1]],F=[[4,5,6,4,6,7],[0,2,1,0,3,2],[0,4,7,0,7,3],[1,2,6,1,6,5],[3,7,6,3,6,2],[0,1,5,0,5,4]];F.forEach((f,fi)=>{const k=fi===0?1:fi===1?.52:.72;for(const q of f){arr.push(...V[q],r*k,g*k,b*k)}})} function addDiamond(arr,w,col,scale=.42,height=.11){pushBox(arr,w.x,w.y,w.z,scale,height,scale,col)} const FIELD_PLAYER_COLORS={P1:[.72,.16,.20],P2:[.12,.38,.70],P3:[.82,.60,.10],P4:[.10,.58,.34]}; function fieldPlayerColor(owner,active=false){const c=FIELD_PLAYER_COLORS[owner]||[.22,.34,.28];return active?c:c.map(v=>v*.68+.04)} /* GRASSLAND-3D-R2: gameplay graph/hit remains DOM data; this WebGL layer owns only terrain, atmosphere and non-interactive scenery. */ function fieldRand(i){let x=(i+1)|0;x=Math.imul(x^0x9e3779b9,0x85ebca6b);x^=x>>>13;x=Math.imul(x,0xc2b2ae35);x^=x>>>16;return(x>>>0)/4294967296} function fieldTerrainHeight(x,z){const r=Math.hypot(x,z),edge=Math.max(0,Math.min(1,(r-11)/20)),ridge=edge*edge*(.92+.34*Math.sin(x*.18)+.28*Math.cos(z*.16)),mound=(mx,mz,a,rr)=>a*Math.exp(-((x-mx)*(x-mx)+(z-mz)*(z-mz))/rr);return-.31+.036*Math.sin(x*.41)+.032*Math.cos(z*.37)+.018*Math.sin((x-z)*.63)+ridge+mound(-13,-9,.42,22)+mound(14,-8,.36,28)+mound(-12,11,.34,25)+mound(13,12,.40,24)} function pushLitTri(arr,a,b,c,col){const ux=b[0]-a[0],uy=b[1]-a[1],uz=b[2]-a[2],vx=c[0]-a[0],vy=c[1]-a[1],vz=c[2]-a[2],nx=uy*vz-uz*vy,ny=uz*vx-ux*vz,nz=ux*vy-uy*vx,ln=Math.hypot(nx,ny,nz)||1,n=[nx/ln,ny/ln,nz/ln];for(const q of[a,b,c])arr.push(q[0],q[1],q[2],n[0],n[1],n[2],col[0],col[1],col[2])} function pushLitQuad(arr,a,b,c,d,col){pushLitTri(arr,a,b,c,col);pushLitTri(arr,a,c,d,col)} function fieldTerrainNormal(x,z){const e=.24,hl=fieldTerrainHeight(x-e,z),hr=fieldTerrainHeight(x+e,z),hd=fieldTerrainHeight(x,z-e),hu=fieldTerrainHeight(x,z+e),nx=hl-hr,ny=e*2,nz=hd-hu,l=Math.hypot(nx,ny,nz)||1;return[nx/l,ny/l,nz/l]} function fieldTerrainColor(x,z){const r=Math.hypot(x,z),v=.5+.25*"}],"project3Def":[{"line":1690,"index":10054882,"text":"World(id,viewer){if(id.startsWith('C:')){const [,rs,cs]=id.split(':'),r=+rs,c=+cs;return{x:+cs*1.35,z:+rs*1.35,y:.24}};if(id.startsWith('S:')||id.startsWith('R:')){let owner,lane,depth=0;if(id.startsWith('S:')){[,owner,lane]=id.split(':');depth=0}else({owner,lane,depth}=parseRoad(id));const arm=projectionFor(viewer)[owner],off={L:-1.45,C:0,R:1.45}[lane],d=2.35+depth*1.25;if(arm==='N')return{x:off,z:-d,y:.24};if(arm==='S')return{x:-off,z:d,y:.24};if(arm==='W')return{x:-d,z:-off,y:.24};return{x:d,z:off,y:.24}};const k={NW:{x:-10,z:-10},NE:{x:10,z:-10},SW:{x:-10,z:10},SE:{x:10,z:10}}[id.split(':')[1]];return{k,y:.2,...k}} function vsub(a,b){return[a[0]-b[0],a[1]-b[1],a[2]-b[2]]}function vdot(a,b){return a[0]*b[0]+a[1]*b[1]+a[2]*b[2]}function vcross(a,b){return[a[1]*b[2]-a[2]*b[1],a[2]*b[0]-a[0]*b[2],a[0]*b[1]-a[1]*b[0]]}function vnorm(a){const l=Math.hypot(...a)||1;return a.map(x=>x/l)} function m4mul(a,b){const o=new Array(16).fill(0);for(let r=0;r<4;r++)for(let c=0;c<4;c++)for(let k=0;k<4;k++)o[r*4+c]+=a[r*4+k]*b[k*4+c];return o} function m4persp(fovy,aspect,near,far){const f=1/Math.tan(fovy/2),nf=1/(near-far);return[f/aspect,0,0,0,0,f,0,0,0,0,(far+near)*nf,-1,0,0,2*far*near*nf,0]} function m4look(eye,center,up){const z=vnorm(vsub(eye,center)),x=vnorm(vcross(up,z)),y=vcross(z,x);return[x[0],y[0],z[0],0,x[1],y[1],z[1],0,x[2],y[2],z[2],0,-vdot(x,eye),-vdot(y,eye),-vdot(z,eye),1]} function fieldViewportProfile(){const map=$('#battleMap'),w=Math.max(1,map?.clientWidth||innerWidth),h=Math.max(1,map?.clientHeight||innerHeight),raw=w/h;if(raw>1.85&&h<500)return{aspect:Math.max(1.2,Math.min(2.28,raw)),yBase:.36,yGain:.29,kind:'short-landscape'};if(raw<.78)return{aspect:1.2,yBase:.44,yGain:.40,kind:'portrait'};return{aspect:Math.max(1.2,raw),yBase:.45,yGain:.42,kind:'standard'}} function project3(w){const c=fieldCameraState;if(!c)return null;const eye=c.eye,rx=w.x-eye[0],ry=(w.y||0)-eye[1],rz=w.z-eye[2],q=Math.SQRT1_2,cx=rx,cy=q*ry-q*rz,cz=-q*ry-q*rz;if(cz<=.08)return null;const t=Math.tan(Math.PI/10.2),nx=cx/(cz*t*c.aspect),ny=cy/(cz*t);return{x:(nx*.5+.5)*100,y:(c.yBase-ny*c.yGain)*100}} function fieldFocusNodes(){const m=state.match;if(!m)return centerIds();const s=new Set([...activeNodes(),...m.players.map(p=>p.position)]);const me=m.players[0];if(m.phase==='plan'&&me.plan?.roadId)for(const id of reachable(me,normalMoveLimitForRoad(me.plan.roadId)).keys())s.add(id);if(m.honey?.byNode)Object.keys(m.honey.byNode).filter(id=>m.honey.byNode[id]>0).forEach(id=>s.add(id));return[...s]} // R14 camera responsibility: movement selection narrows plan focus; battle resolution may independently focus the confirmed collision. // Logic/hitboxes stay on reachable()/nodeWorld()/project3; this only chooses the camera eye. function fieldMoveFocusNodes(){const m=state.match,me=m?.players?.[0];if(!m||!me||m.phase!=='plan'||!me.plan?.roadId)return null;const moveLimit=normalMoveLimitForRoad(me.plan.roadId);if(moveLimit<=0)return null;const s=new Set([me.position,...reachable(me,moveLimit).keys(),...(me.plan?.path||[])]);return[...s]} function fieldBattleFocusNodes(){const m=state.match,p=m?.battlePresentation;if(!m||m.phase!=='resolve'||!m.target||!p)return null;const attacker=m.players.find(x=>x.id===p.attackerId),defender=m.players.find(x=>x.id===p.defenderId),s=new Set();if(attacker?.position)s.add(attacker.position);if(defender?.position)s.add(defender.position);s.add(shieldId(p.defenderId,p.shield));const depth=defender?.lanes?.[p.lane]?.length||0;if(depth>0)s.add(roadId(p.defenderId,p.lane,Math.min(7,depth)));return[...s]} function fieldCamera(viewer){const profile=fieldViewportProfile(),battleFocus=fieldBattleFocusNodes(),moveFocus=fieldMoveFocusNodes(),focus=battleFocus?.length?battleFocus:moveFocus;if(focus?.length){const pts=focus.map(id=>nodeWorld(id,viewer)),xs=pts.map(p=>p.x),zs=pts.map(p=>p.z),minX=Math.min(...xs),maxX=Math.max(...xs),minZ=Math.min(...zs),maxZ=Math.max(...zs),cx=(minX+maxX)/2,cz=(minZ+maxZ)/2;let r=2.4;for(const p of pts)r=Math.max(r,Math.hypot(p.x-cx,p.z-cz)+.9);const zoom=profile.kind==='portrait'?Math.max(12,r*2.65):profile.kind==='short-landscape'?Math.max(9.5,r*2.20):Math.max(11,r*2.45),eye=[cx,zoom,cz+zoom],center=[cx,0,cz];return{eye,center,r,moveFocus:!!moveFocus?.length,battleFocus:!!battleFocus?.length,...profile}}let r=11.5;for(const id of fieldFocusNodes()){const w=nodeWorld(id,viewer);r=Math.max(r,Math.hypot(w.x,w.z)+1.1)}r=Math.min(13,r);const zoom=profile.kind==='portrait'?Math.max(24,r*1.84):profile.kind==='short-landscape'?Math.max(18,r*1.38):Math.max(22,r*1.69),eye=[0,zoom,zoom],center=[0,0,0];return{eye,center,r,moveFocus:false,battleFocus:false,...profile}} function fieldProject(id,viewer){if(fieldProjection.has(id))return fieldProjection.get(id);return null} function nodeTopSurface(id,viewer){const nw=nodeWorld(id,viewer),typ=posType(id);let size=0,topY=nw.y;if(typ==='center'){size=1.06;topY=nw.y-.015+.16}else if(typ==='shield'){size=1.05;topY=nw.y+.24}else if(typ==='road'){if(!activeNodes().has(id))return null;size=.88;topY=nw.y-.01+.105}else return null;const h=size/2,world=[[nw.x-h,topY,nw.z-h],[nw.x+h,topY,nw.z-h],[nw.x+h,topY,nw.z+h],[nw.x-h,topY,nw.z+h]],projected=world.map(([x,y,z])=>project3({x,y,z}));return projected.every(Boolean)?projected:null} function syncRangeSurfacePath(){const m=state.match,layer=$('#rangeSurfaceLayer');if(!m||!layer)return;const path=new Set(m.players[0]?.plan?.path||[]);[...layer.children].forEach(e=>e.classList.toggle('path',path.has(e.dataset.pos)))} function renderRangeSurfaces(reach,moveLimit,viewer){const layer=$('#rangeSurfaceLayer');if(!layer)return;layer.replaceChildren();if(!reach||moveLimit<=0)return;const ns='http://www.w3.org/2000/svg';for(const [id,d] of reach){if(d>moveLimit)continue;const pts=nodeTopSurface(id,viewer);if(!pts)continue;const e=document.createElementNS(ns,'polygon');e.setAttribute('class','rangeSurface');e.setAttribute('points',pts.map(p=>`${p.x},${p.y}`).join(' '));e.dataset.pos=id;layer.appendChild(e)}syncRangeSur"}],"renderField3DDef":[{"line":1728,"index":10069486,"text":"],[-5,16,.72],[5,-16,.76]].forEach(a=>pushBoulder(arr,...a));for(let i=0;i<34;i++){const x=(fieldRand(900+i*3)*2-1)*18,z=(fieldRand(901+i*3)*2-1)*18,r=Math.hypot(x,z);if(r<4||r>19)continue;let near=false;for(const p of reserved)if((p.x-x)*(p.x-x)+(p.z-z)*(p.z-z)<1.0){near=true;break}if(near)continue;const y=fieldTerrainHeight(x,z)+.03,c=i%3===0?[.88,.68,.18]:i%3===1?[.76,.82,.36]:[.72,.60,.78];pushCone(arr,x,y,z,.05,.16,c,5)}}else{[[-14,-12,.85],[14,12,.85],[-13,13,.72],[13,-13,.72]].forEach(a=>pushBoulder(arr,...a))}return placed} function drawGrasslandGL(gl,items,viewer,cam,w,h){gl.viewport(0,0,w,h);gl.enable(gl.DEPTH_TEST);gl.depthFunc(gl.LEQUAL);gl.disable(gl.CULL_FACE);gl.clearColor(.45,.66,.73,1);gl.clear(gl.COLOR_BUFFER_BIT|gl.DEPTH_BUFFER_BIT);const a=[];pushFieldTerrain(a,state.settings.lowPerf);pushFieldTrails(a,viewer);const grassTufts=pushGrasslandScenery(a,viewer,state.settings.lowPerf);for(const it of items){if(it.sy<=0)continue;if(it.kind==='inactive')pushLitBox(a,it.x,it.y+.006,it.z,.52,.024,.52,[.27,.30,.24]);else if(it.kind==='roadMark')pushLitBox(a,it.x,it.y,it.z,.10,.010,.10,it.col.map(v=>v*.72));else pushLitBox(a,it.x,it.y,it.z,it.sx,it.sy,it.sz,it.col)};const data=new Float32Array(a),buf=gl.createBuffer();gl.bindBuffer(gl.ARRAY_BUFFER,buf);gl.bufferData(gl.ARRAY_BUFFER,data,gl.DYNAMIC_DRAW);gl.useProgram(fieldProgram);const stride=36,ap=gl.getAttribLocation(fieldProgram,'p'),an=gl.getAttribLocation(fieldProgram,'n'),ac=gl.getAttribLocation(fieldProgram,'c');gl.enableVertexAttribArray(ap);gl.vertexAttribPointer(ap,3,gl.FLOAT,false,stride,0);gl.enableVertexAttribArray(an);gl.vertexAttribPointer(an,3,gl.FLOAT,false,stride,12);gl.enableVertexAttribArray(ac);gl.vertexAttribPointer(ac,3,gl.FLOAT,false,stride,24);gl.uniform3fv(gl.getUniformLocation(fieldProgram,'uEye'),new Float32Array(cam.eye));gl.uniform3f(gl.getUniformLocation(fieldProgram,'uLight'),-.42,.88,.26);gl.uniform4f(gl.getUniformLocation(fieldProgram,'uView'),Math.tan(Math.PI/10.2),cam.aspect,cam.yBase,cam.yGain);gl.drawArrays(gl.TRIANGLES,0,data.length/9);gl.deleteBuffer(buf);fieldLastStats={mode:'webgl-grassland',vertices:data.length/9,grassTufts,lowPerf:!!state.settings.lowPerf}} function renderField3D(){const m=state.match,canvas=$('#fieldCanvas');if(!m||!canvas)return;const dpr=state.settings.lowPerf?1:Math.min(1.5,window.devicePixelRatio||1),w=Math.max(1,Math.floor(canvas.clientWidth*dpr)),h=Math.max(1,Math.floor(canvas.clientHeight*dpr));if(canvas.width!==w||canvas.height!==h){canvas.width=w;canvas.height=h}const viewer=m.players[0],cam=fieldCamera(viewer);fieldCameraState=cam;fieldMVP=null;fieldProjection.clear();const all=[];centerIds().forEach(id=>all.push(id));['P1','P2','P3','P4'].forEach(pid=>['L','C','R'].forEach(l=>{all.push(shieldId(pid,l));for(let d=1;d<=7;d++)all.push(roadId(pid,l,d))}));['K:NW','K:NE','K:SW','K:SE'].forEach(id=>all.push(id));all.forEach(id=>{const p=project3(nodeWorld(id,viewer));if(p)fieldProjection.set(id,p)});const items=[];const box=(x,y,z,sx,sy,sz,col,kind='box')=>items.push({x,y,z,sx,sy,sz,col,kind});const active=activeNodes();for(const id of all){const nw=nodeWorld(id,viewer),typ=posType(id);if(typ==='center'){box(nw.x,nw.y-.015,nw.z,1.06,.16,1.06,[.27,.32,.28],'center');box(nw.x,nw.y+.15,nw.z,.56,.018,.56,[.72,.62,.34],'centerMark')}else if(typ==='shield'){const owner=id.split(':')[1],pc=fieldPlayerColor(owner,true);box(nw.x,nw.y,nw.z,1.05,.24,1.05,[.25,.18,.30],'shield');box(nw.x,nw.y+.245,nw.z,.62,.025,.62,pc,'shieldMark')}else if(typ==='road'){const owner=parseRoad(id).owner,pc=fieldPlayerColor(owner,active.has(id)),base=active.has(id)?[.46,.49,.43]:[.31,.35,.31];box(nw.x,nw.y+(active.has(id)?-.01:-.045),nw.z,active.has(id)?.88:.78,active.has(id)?.105:.026,active.has(id)?.88:.78,base,active.has(id)?'road':'inactive');box(nw.x,nw.y+(active.has(id)?.10:-.017),nw.z,active.has(id)?.42:.16,.014,active.has(id)?.42:.16,pc,active.has(id)?'roadMarkActive':'roadMark')}else if(typ==='corner'){box(nw.x,nw.y-.02,nw.z,.82,.10,.82,[.40,.42,.36],'corner')}}if(m.honey?.byNode)for(const [id,n] of Object.entries(m.honey.byNode)){if(n<=0)continue;const q=nodeWorld(id,viewer);box(q.x,q.y+.18,q.z,.30,.50,.30,[.98,.61,.06],'honey')} const gl=initFieldGL();if(gl){drawGrasslandGL(gl,items,viewer,cam,w,h)}else{fieldLastStats={mode:'canvas2d-fallback',vertices:0,grassTufts:0,lowPerf:!!state.settings.lowPerf};const ctx=canvas.getContext('2d');if(ctx){ctx.setTransform(dpr,0,0,dpr,0,0);const cw=canvas.clientWidth,ch=canvas.clientHeight;const proj=(x,y,z)=>{const q=project3({x,y,z});return q?[q.x/100*cw,q.y/100*ch]:[0,0]}, rgba=(c,a)=>`rgba(${c.map(v=>Math.max(0,Math.min(255,Math.round(v*255)))).join(',')},${a})`, shade=(c,k)=>`rgb(${c.map(v=>Math.max(0,Math.min(255,Math.round(v*255*k)))).join(',')})`; /* Water and atmospheric depth are visual-only. The 109-node graph/hit areas remain separate DOM/runtime data. */ const water=ctx.createLinearGradient(0,0,0,ch);water.addColorStop(0,'#0a2b31');water.addColorStop(.36,'#0b3a37');water.addColorStop(1,'#061a1b');ctx.fillStyle=water;ctx.fillRect(0,0,cw,ch);const horizon=ctx.createLinearGradient(0,0,cw,0);horizon.addColorStop(0,'rgba(46,107,92,.02)');horizon.addColorStop(.5,'rgba(117,190,159,.15)');horizon.addColorStop(1,'rgba(46,107,92,.02)');ctx.fillStyle=horizon;ctx.fillRect(0,0,cw,ch*.48); ctx.save();ctx.globalAlpha=state.settings.lowPerf?.14:.24;ctx.strokeStyle='#4f8f87';ctx.lineWidth=1;for(let j=0;j<7;j++){const y=ch*(.18+j*.115);ctx.beginPath();for(let x=-20;x<=cw+20;x+=26){const yy=y+Math.sin((x+j*37)*.024)*2.3;const nx=x+26,ny=y+Math.sin((nx+j*37)*.024)*2.3;if(x<0)ctx.moveTo(x,yy);else ctx.quadraticCurveTo(x+13,yy+1.6,nx,ny)}ctx.stroke()}ctx.restore(); function worldPath(points,y=-.26){ctx.beginPath();points.forEach(([x,z],i)=>{const p=proj(x,y,z);if(i===0)ctx.moveTo(p[0],p[1]);else ctx.lineTo(p[0],p[1])});ctx.closePath()} const coast=[[-10,-5],[-8.7,-8.1],[-5.3,-9.7],[-1.4,-9.1],[2.4,-9.8],[6.2,-8.8],[9.1,-5.9],[9.8,-1.8],[9.0,2.3],[10.0,6.0],[6.8,9.1],[2.2,9.5],[-1.8,9.0],[-5.9,9.6],[-9.1,6.8],[-9.8,2.3],[-9.2,-1.5]];worldPath(coast);ctx.shadowColor='rgba(0,0,0,.50)';ctx.shadowBlur=state.settings.lowPerf?5:15;ctx.shadowOffsetY=7;const island=ctx.createLinearGradient(0,ch*.2,0,ch*.88);island.addColorStop(0,'#405b42');island.addColorStop(.48,'#2d4935');island.addColorStop(1,'#18322b');ctx.fillStyle=island;ctx.fill();ctx.shadowBlur=0;ctx.shadowOffsetY=0;ctx.strokeStyle='#6c8267';ctx.lineWidth=Math.max(2,cw*.0032);ctx.stroke();worldPath(coast,.01);ctx.strokeStyle='rgba(117,169,126,.24)';ctx.lineWidth=Math.max(1,cw*.0016);ctx.stroke(); const inner=[[-3.7,-3.0],[-1.8,-4.1],[1.2,-4.0],[3.8,-2.7],[4.2,.3],[3.0,3.4],[.7,4.3],[-2.3,3.8],[-4.0,1.5]];worldPath(inner,-.08);const moss=ctx.createLinearGradient(cw*.3,ch*.3,cw*.7,ch*.7);moss.addColorStop(0,'#526e50');moss.addColorStop(1,'#304b39');ctx.fillStyle=moss;ctx.fill();ctx.strokeStyle='rgba(183,205,154,.28)';ctx.lineWidth=Math.max(1.5,cw*.002);ctx.stroke();function patch(poly,fill){worldPath(poly,-.045);ctx.fillStyle=fill;ctx.fill()}patch([[-8,-3.2],[-6.1,-4.5],[-4.7,-3.8],[-5.2,-1.9],[-7.2,-1.4]],'rgba(92,116,70,.16)');patch([[4.8,2.0],[7.4,1.2],[8.3,3.3],[6.9,5.1],[5.1,4.5]],'rgba(107,126,73,.13)');patch([[-6.8,4.5],[-4.8,5.0],[-3.7,7.0],[-6.1,8.0],[-7.7,6.5]],'rgba(75,100,67,.17)');patch([[4.0,-6.8],[6.2,-7.8],[7.8,-6.2],[7.0,-4.1],[5.0,-4.4]],'rgba(76,99,70,.16)'); function lanePoints(owner,lane){const ids=[shieldId(owner,lane)];for(let d=1;d<=7;d++)ids.push(roadId(owner,lane,d));return ids.map(id=>{const q=fieldProjection.get(id);return q?[q.x/100*cw,q.y/100*ch]:null}).filter(Boolean)} ctx.save();ctx.lineCap='round';ctx.lineJoin='round';for(const owner of ['P1','P2','P3','P4'])for(const lane of ['L','C','R']){const pts=lanePoints(owner,lane);if(pts.length<2)continue;ctx.beginPath();ctx.moveTo(...pts[0]);for(let i=1;i<pts.length;i++)ctx.lineTo(...pts[i]);ctx.strokeStyle='rgba(9,20,18,.62)';ctx.lineWidth=Math.max(13,Math.min(23,cw*.016));ctx.stroke();ctx.strokeStyle='rgba(104,113,96,.74)';ctx.lineWidth=Math.max(8,Math.min(15,cw*.0105));ctx.stroke();ctx.strokeStyle=rgba(fieldPlayerColor(owner,true),active.size?0.28:0.18);ctx.lineWidth=Math.max(2,Math.min(4,cw*.0028));ctx.stroke();ctx.setLineDash([2.5,7]);ctx.strokeStyle='rgba(224,227,204,.15)';ctx.lineWidth=1;ctx.stroke();ctx.setLineDash([])}ctx.restore(); const cpt=proj(0,.12,0);ctx.beginPath();ctx.ellipse(cpt[0],cpt[1]+3,Math.max(26,cw*.03),Math.max(11,ch*.022),0,0,Math.PI*2);ctx.fillStyle='rgba(9,24,20,.54)';ctx.fill();ctx.strokeStyle='rgba(216,188,112,.22)';ctx.lineWidth=2;ctx.stroke(); function tree(wx,wz,s=1){const p=proj(wx,.05,wz),base=Math.max(3,Math.min(8,cw*.0055))*s;ctx.fillStyle='rgba(0,0,0,.28)';ctx.beginPath();ctx.ellipse(p[0]+2,p[1]+base*1.3,base*1.3,base*.52,0,0,Math.PI*2);ctx.fill();ctx.strokeStyle='#4b3425';ctx.lineWidth=Math.max(1.5,base*.28);ctx.beginPath();ctx.moveTo(p[0],p[1]+base*1.1);ctx.lineTo(p[0],p[1]-base*.15);ctx.stroke();for(const [dx,dy,r,k] of [[0,-1.05,1.1,'#173f2b'],[-.7,-.55,.85,'#24583a'],[.7,-.52,.82,'#1f4d34'],[0,-.28,.92,'#2b6542']]){ctx.fillStyle=k;ctx.beginPath();ctx.arc(p[0]+dx*base,p[1]+dy*base,base*r,0,Math.PI*2);ctx.fill()}} function rock(wx,wz,s=1){const p=proj"}],"uniformCalls":[{"line":1702,"index":10059765,"text":"path',path.has(e.dataset.pos)))} function renderRangeSurfaces(reach,moveLimit,viewer){const layer=$('#rangeSurfaceLayer');if(!layer)return;layer.replaceChildren();if(!reach||moveLimit<=0)return;const ns='http://www.w3.org/2000/svg';for(const [id,d] of reach){if(d>moveLimit)continue;const pts=nodeTopSurface(id,viewer);if(!pts)continue;const e=document.createElementNS(ns,'polygon');e.setAttribute('class','rangeSurface');e.setAttribute('points',pts.map(p=>`${p.x},${p.y}`).join(' '));e.dataset.pos=id;layer.appendChild(e)}syncRangeSurfacePath()} function glShader(gl,type,src){const sh=gl.createShader(type);gl.shaderSource(sh,src);gl.compileShader(sh);if(!gl.getShaderParameter(sh,gl.COMPILE_STATUS))throw Error(gl.getShaderInfoLog(sh)||'shader');return sh} function initFieldGL(){const c=$('#fieldCanvas');if(fieldGL&&fieldGL.canvas===c&&!fieldGL.isContextLost?.())return fieldGL;let gl=null;try{gl=c.getContext('webgl',{alpha:false,antialias:!state.settings.lowPerf,depth:true,preserveDrawingBuffer:false,powerPreference:state.settings.lowPerf?'low-power':'high-performance'})}catch(e){}if(!gl)return null;const vs=glShader(gl,gl.VERTEX_SHADER,'attribute vec3 p;attribute vec3 n;attribute vec3 c;uniform vec3 uEye;uniform vec3 uLight;uniform vec4 uView;varying vec3 vColor;varying float vFog;void main(){vec3 r=p-uEye;float q=.70710678;float cx=r.x;float cy=q*r.y-q*r.z;float cz=max(.08,-q*r.y-q*r.z);float nx=cx/(cz*uView.x*uView.y);float ny=cy/(cz*uView.x);float yNdc=1.0-2.0*uView.z+2.0*ny*uView.w;float z01=clamp((cz-.10)/48.0,0.0,1.0);float zNdc=z01*2.0-1.0;gl_Position=vec4(nx*cz,yNdc*cz,zNdc*cz,cz);vec3 nn=normalize(n);float sun=max(dot(nn,normalize(uLight)),0.0);float hemi=max(nn.y,0.0);vColor=c*(.53+.39*sun+.12*hemi);vFog=smoothstep(23.0,43.0,cz);}'),fs=glShader(gl,gl.FRAGMENT_SHADER,'precision mediump float;varying vec3 vColor;varying float vFog;void main(){vec3 haze=vec3(.54,.69,.63);vec3 col=mix(clamp(vColor,0.0,1.0),haze,vFog*.52);gl_FragColor=vec4(col,1.0);}');const pr=gl.createProgram();gl.attachShader(pr,vs);gl.attachShader(pr,fs);gl.linkProgram(pr);if(!gl.getProgramParameter(pr,gl.LINK_STATUS))throw Error(gl.getProgramInfoLog(pr)||'link');fieldGL=gl;fieldProgram=pr;if(!c.dataset.grasslandGlLifecycle){c.dataset.grasslandGlLifecycle='1';c.addEventListener('webglcontextlost',e=>{e.preventDefault();fieldGL=null;fieldProgram=null});c.addEventListener('webglcontextrestored',()=>{fieldGL=null;fieldProgram=null;if(state.screen==='battle'){renderField3D();renderBoard()}})}return gl} function pushBox(arr,x,y,z,sx,sy,sz,col){const [r,g,b]=col,x0=x-sx/2,x1=x+sx/2,y0=y,y1=y+sy,z0=z-sz/2,z1=z+sz/2;const V=[[x0,y0,z0],[x1,y0,z0],[x1,y0,z1],[x0,y0,z1],[x0,y1,z0],[x1,y1,z0],[x1,y1,z1],[x0,y1,z1]],F=[[4,5,6,4,6,7],[0,2,1,0,3,2],[0,4,7,0,7,3],[1,2,6,1,6,5],[3,7,6,3,6,2],[0,1,5,0,5,4]];F.forEach((f,fi)=>{const k=fi===0?1:fi===1?.52:.72;for(const q of f){arr.push(...V[q],r*k,g*k,b*k)}})} function addDiamond(arr,w,col,scale=.42,height=.11){pushBox(arr,w.x,w.y,w.z,scale,height,scale,col)} const FIELD_PLAYER_COLORS={P1:[.72,.16,.20],P2:[.12,.38,.70],P3:[.82,.60,.10],P4:[.10,.58,.34]}; function fieldPlayerColor(owner,active=false){const c=FIELD_PLAYER_COLORS[owner]||[.22,.34,.28];return active?c:c.map(v=>v*.68+.04)} /* GRASSLAND-3D-R2: gameplay graph/hit remains DOM data; this WebGL layer owns only terrain, atmosphere and non-interactive scenery. */ function fieldRand(i){let x=(i+1)|0;x=Math.imul(x^0x9e3779b9,0x85ebca6b);x^=x>>>13;x=Math.imul(x,0xc2b2ae35);x^=x>>>16;return(x>>>0)/4294967296} function fieldTerrainHeight(x,z){const r=Math.hypot(x,z),edge=Math.max(0,Math.min(1,(r-11)/20)),ridge=edge*edge*(.92+.34*Math.sin(x*.18)+.28*Math.cos(z*.16)),mound=(mx,mz,a,rr)=>a*Math.exp(-((x-mx)*(x-mx)+(z-mz)*(z-mz))/rr);return-.31+.036*Math.sin(x*.41)+.032*Math.cos(z*.37)+.018*Math.sin((x-z)*.63)+ridge+mound(-13,-9,.42,22)+mound(14,-8,.36,28)+mound(-12,11,.34,25)+mound(13,12,.40,24)} function pushLitTri(arr,a,b,c,col){const ux=b[0]-a[0],uy=b[1]-a[1],uz=b[2]-a[2],vx=c[0]-a[0],vy=c[1]-a[1],vz=c[2]-a[2],nx=uy*vz-uz*vy,ny=uz*vx-ux*vz,nz=ux*vy-uy*vx,ln=Math.hypot(nx,ny,nz)||1,n=[nx/ln,ny/ln,nz/ln];for(const q of[a,b,c])arr.push(q[0],q[1],q[2],n[0],n[1],n[2],col[0],col[1],col[2])} function pushLitQuad(arr,a,b,c,d,col){pushLitTri(arr,a,b,c,col);pushLitTri(arr,a,c,d,col)} function fieldTerrainNormal(x,z){const e=.24,hl=fieldTerrainHeight(x-e,z),hr=fieldTerrainHeight(x+e,z),hd=fieldTerrainHeight(x,z-e),hu=fieldTerrainHeight(x,z+e),nx=hl-hr,ny=e*2,nz=hd-hu,l=Math.hypot(nx,ny,nz)||1;return[nx/l,ny/l,nz/l]} function fieldTerrainColor(x,z){const r=Math.hypot(x,z),v=.5+.25*Math.sin(x*.23+z*.17)+.25*Math.sin(x*.47-z*.31),meadow=Math.max(0,1-r/42);return[.145+.030*v+.028*meadow,.34+.060*v+.105*meadow,.12+.025*v+.018*meadow]} function pushTerrainTri(arr,a,b,c){for(const q of[a,b,c]){const n=fieldTerrainNormal(q[0],q[2]),col=fieldTerrainColor(q[0],q[2]);arr.push(q[0],q[1],q[2],n[0],n[1],n[2],col[0],col[1],col[2])}} function pushLitBox(arr,x,y,z,sx,sy,sz,col){const x0=x-sx/2,x1=x+sx/2,y0=y,y1=y+sy,z0=z-sz/2,z1=z+sz/2,V=[[x0,y0,z0],[x1,y0,z0],[x1,y0,z1],[x0,y0,z1],[x0,y1,z0],[x1,y1,z0],[x1,y1,z1],[x0,y1,z1]],Q=[[4,5,6,7],[0,3,2,1],[0,4,7,3],[1,2,6,5],[3,7,6,2],[0,1,5,4]];Q.forEach((q,i)=>{const k=i===0?1:i===1?.57:.78,cc=col.map(v=>v*k);pushLitQuad(arr,V[q[0]],V[q[1]],V[q[2]],V[q[3]],cc)})} function pushFieldTerrain(arr,lowPerf){const cells=lowPerf?26:42,extent=36"},{"line":1702,"index":10059783,"text":"ataset.pos)))} function renderRangeSurfaces(reach,moveLimit,viewer){const layer=$('#rangeSurfaceLayer');if(!layer)return;layer.replaceChildren();if(!reach||moveLimit<=0)return;const ns='http://www.w3.org/2000/svg';for(const [id,d] of reach){if(d>moveLimit)continue;const pts=nodeTopSurface(id,viewer);if(!pts)continue;const e=document.createElementNS(ns,'polygon');e.setAttribute('class','rangeSurface');e.setAttribute('points',pts.map(p=>`${p.x},${p.y}`).join(' '));e.dataset.pos=id;layer.appendChild(e)}syncRangeSurfacePath()} function glShader(gl,type,src){const sh=gl.createShader(type);gl.shaderSource(sh,src);gl.compileShader(sh);if(!gl.getShaderParameter(sh,gl.COMPILE_STATUS))throw Error(gl.getShaderInfoLog(sh)||'shader');return sh} function initFieldGL(){const c=$('#fieldCanvas');if(fieldGL&&fieldGL.canvas===c&&!fieldGL.isContextLost?.())return fieldGL;let gl=null;try{gl=c.getContext('webgl',{alpha:false,antialias:!state.settings.lowPerf,depth:true,preserveDrawingBuffer:false,powerPreference:state.settings.lowPerf?'low-power':'high-performance'})}catch(e){}if(!gl)return null;const vs=glShader(gl,gl.VERTEX_SHADER,'attribute vec3 p;attribute vec3 n;attribute vec3 c;uniform vec3 uEye;uniform vec3 uLight;uniform vec4 uView;varying vec3 vColor;varying float vFog;void main(){vec3 r=p-uEye;float q=.70710678;float cx=r.x;float cy=q*r.y-q*r.z;float cz=max(.08,-q*r.y-q*r.z);float nx=cx/(cz*uView.x*uView.y);float ny=cy/(cz*uView.x);float yNdc=1.0-2.0*uView.z+2.0*ny*uView.w;float z01=clamp((cz-.10)/48.0,0.0,1.0);float zNdc=z01*2.0-1.0;gl_Position=vec4(nx*cz,yNdc*cz,zNdc*cz,cz);vec3 nn=normalize(n);float sun=max(dot(nn,normalize(uLight)),0.0);float hemi=max(nn.y,0.0);vColor=c*(.53+.39*sun+.12*hemi);vFog=smoothstep(23.0,43.0,cz);}'),fs=glShader(gl,gl.FRAGMENT_SHADER,'precision mediump float;varying vec3 vColor;varying float vFog;void main(){vec3 haze=vec3(.54,.69,.63);vec3 col=mix(clamp(vColor,0.0,1.0),haze,vFog*.52);gl_FragColor=vec4(col,1.0);}');const pr=gl.createProgram();gl.attachShader(pr,vs);gl.attachShader(pr,fs);gl.linkProgram(pr);if(!gl.getProgramParameter(pr,gl.LINK_STATUS))throw Error(gl.getProgramInfoLog(pr)||'link');fieldGL=gl;fieldProgram=pr;if(!c.dataset.grasslandGlLifecycle){c.dataset.grasslandGlLifecycle='1';c.addEventListener('webglcontextlost',e=>{e.preventDefault();fieldGL=null;fieldProgram=null});c.addEventListener('webglcontextrestored',()=>{fieldGL=null;fieldProgram=null;if(state.screen==='battle'){renderField3D();renderBoard()}})}return gl} function pushBox(arr,x,y,z,sx,sy,sz,col){const [r,g,b]=col,x0=x-sx/2,x1=x+sx/2,y0=y,y1=y+sy,z0=z-sz/2,z1=z+sz/2;const V=[[x0,y0,z0],[x1,y0,z0],[x1,y0,z1],[x0,y0,z1],[x0,y1,z0],[x1,y1,z0],[x1,y1,z1],[x0,y1,z1]],F=[[4,5,6,4,6,7],[0,2,1,0,3,2],[0,4,7,0,7,3],[1,2,6,1,6,5],[3,7,6,3,6,2],[0,1,5,0,5,4]];F.forEach((f,fi)=>{const k=fi===0?1:fi===1?.52:.72;for(const q of f){arr.push(...V[q],r*k,g*k,b*k)}})} function addDiamond(arr,w,col,scale=.42,height=.11){pushBox(arr,w.x,w.y,w.z,scale,height,scale,col)} const FIELD_PLAYER_COLORS={P1:[.72,.16,.20],P2:[.12,.38,.70],P3:[.82,.60,.10],P4:[.10,.58,.34]}; function fieldPlayerColor(owner,active=false){const c=FIELD_PLAYER_COLORS[owner]||[.22,.34,.28];return active?c:c.map(v=>v*.68+.04)} /* GRASSLAND-3D-R2: gameplay graph/hit remains DOM data; this WebGL layer owns only terrain, atmosphere and non-interactive scenery. */ function fieldRand(i){let x=(i+1)|0;x=Math.imul(x^0x9e3779b9,0x85ebca6b);x^=x>>>13;x=Math.imul(x,0xc2b2ae35);x^=x>>>16;return(x>>>0)/4294967296} function fieldTerrainHeight(x,z){const r=Math.hypot(x,z),edge=Math.max(0,Math.min(1,(r-11)/20)),ridge=edge*edge*(.92+.34*Math.sin(x*.18)+.28*Math.cos(z*.16)),mound=(mx,mz,a,rr)=>a*Math.exp(-((x-mx)*(x-mx)+(z-mz)*(z-mz))/rr);return-.31+.036*Math.sin(x*.41)+.032*Math.cos(z*.37)+.018*Math.sin((x-z)*.63)+ridge+mound(-13,-9,.42,22)+mound(14,-8,.36,28)+mound(-12,11,.34,25)+mound(13,12,.40,24)} function pushLitTri(arr,a,b,c,col){const ux=b[0]-a[0],uy=b[1]-a[1],uz=b[2]-a[2],vx=c[0]-a[0],vy=c[1]-a[1],vz=c[2]-a[2],nx=uy*vz-uz*vy,ny=uz*vx-ux*vz,nz=ux*vy-uy*vx,ln=Math.hypot(nx,ny,nz)||1,n=[nx/ln,ny/ln,nz/ln];for(const q of[a,b,c])arr.push(q[0],q[1],q[2],n[0],n[1],n[2],col[0],col[1],col[2])} function pushLitQuad(arr,a,b,c,d,col){pushLitTri(arr,a,b,c,col);pushLitTri(arr,a,c,d,col)} function fieldTerrainNormal(x,z){const e=.24,hl=fieldTerrainHeight(x-e,z),hr=fieldTerrainHeight(x+e,z),hd=fieldTerrainHeight(x,z-e),hu=fieldTerrainHeight(x,z+e),nx=hl-hr,ny=e*2,nz=hd-hu,l=Math.hypot(nx,ny,nz)||1;return[nx/l,ny/l,nz/l]} function fieldTerrainColor(x,z){const r=Math.hypot(x,z),v=.5+.25*Math.sin(x*.23+z*.17)+.25*Math.sin(x*.47-z*.31),meadow=Math.max(0,1-r/42);return[.145+.030*v+.028*meadow,.34+.060*v+.105*meadow,.12+.025*v+.018*meadow]} function pushTerrainTri(arr,a,b,c){for(const q of[a,b,c]){const n=fieldTerrainNormal(q[0],q[2]),col=fieldTerrainColor(q[0],q[2]);arr.push(q[0],q[1],q[2],n[0],n[1],n[2],col[0],col[1],col[2])}} function pushLitBox(arr,x,y,z,sx,sy,sz,col){const x0=x-sx/2,x1=x+sx/2,y0=y,y1=y+sy,z0=z-sz/2,z1=z+sz/2,V=[[x0,y0,z0],[x1,y0,z0],[x1,y0,z1],[x0,y0,z1],[x0,y1,z0],[x1,y1,z0],[x1,y1,z1],[x0,y1,z1]],Q=[[4,5,6,7],[0,3,2,1],[0,4,7,3],[1,2,6,5],[3,7,6,2],[0,1,5,4]];Q.forEach((q,i)=>{const k=i===0?1:i===1?.57:.78,cc=col.map(v=>v*k);pushLitQuad(arr,V[q[0]],V[q[1]],V[q[2]],V[q[3]],cc)})} function pushFieldTerrain(arr,lowPerf){const cells=lowPerf?26:42,extent=36,step=extent*2/cel"},{"line":1702,"index":10059803,"text":"ion renderRangeSurfaces(reach,moveLimit,viewer){const layer=$('#rangeSurfaceLayer');if(!layer)return;layer.replaceChildren();if(!reach||moveLimit<=0)return;const ns='http://www.w3.org/2000/svg';for(const [id,d] of reach){if(d>moveLimit)continue;const pts=nodeTopSurface(id,viewer);if(!pts)continue;const e=document.createElementNS(ns,'polygon');e.setAttribute('class','rangeSurface');e.setAttribute('points',pts.map(p=>`${p.x},${p.y}`).join(' '));e.dataset.pos=id;layer.appendChild(e)}syncRangeSurfacePath()} function glShader(gl,type,src){const sh=gl.createShader(type);gl.shaderSource(sh,src);gl.compileShader(sh);if(!gl.getShaderParameter(sh,gl.COMPILE_STATUS))throw Error(gl.getShaderInfoLog(sh)||'shader');return sh} function initFieldGL(){const c=$('#fieldCanvas');if(fieldGL&&fieldGL.canvas===c&&!fieldGL.isContextLost?.())return fieldGL;let gl=null;try{gl=c.getContext('webgl',{alpha:false,antialias:!state.settings.lowPerf,depth:true,preserveDrawingBuffer:false,powerPreference:state.settings.lowPerf?'low-power':'high-performance'})}catch(e){}if(!gl)return null;const vs=glShader(gl,gl.VERTEX_SHADER,'attribute vec3 p;attribute vec3 n;attribute vec3 c;uniform vec3 uEye;uniform vec3 uLight;uniform vec4 uView;varying vec3 vColor;varying float vFog;void main(){vec3 r=p-uEye;float q=.70710678;float cx=r.x;float cy=q*r.y-q*r.z;float cz=max(.08,-q*r.y-q*r.z);float nx=cx/(cz*uView.x*uView.y);float ny=cy/(cz*uView.x);float yNdc=1.0-2.0*uView.z+2.0*ny*uView.w;float z01=clamp((cz-.10)/48.0,0.0,1.0);float zNdc=z01*2.0-1.0;gl_Position=vec4(nx*cz,yNdc*cz,zNdc*cz,cz);vec3 nn=normalize(n);float sun=max(dot(nn,normalize(uLight)),0.0);float hemi=max(nn.y,0.0);vColor=c*(.53+.39*sun+.12*hemi);vFog=smoothstep(23.0,43.0,cz);}'),fs=glShader(gl,gl.FRAGMENT_SHADER,'precision mediump float;varying vec3 vColor;varying float vFog;void main(){vec3 haze=vec3(.54,.69,.63);vec3 col=mix(clamp(vColor,0.0,1.0),haze,vFog*.52);gl_FragColor=vec4(col,1.0);}');const pr=gl.createProgram();gl.attachShader(pr,vs);gl.attachShader(pr,fs);gl.linkProgram(pr);if(!gl.getProgramParameter(pr,gl.LINK_STATUS))throw Error(gl.getProgramInfoLog(pr)||'link');fieldGL=gl;fieldProgram=pr;if(!c.dataset.grasslandGlLifecycle){c.dataset.grasslandGlLifecycle='1';c.addEventListener('webglcontextlost',e=>{e.preventDefault();fieldGL=null;fieldProgram=null});c.addEventListener('webglcontextrestored',()=>{fieldGL=null;fieldProgram=null;if(state.screen==='battle'){renderField3D();renderBoard()}})}return gl} function pushBox(arr,x,y,z,sx,sy,sz,col){const [r,g,b]=col,x0=x-sx/2,x1=x+sx/2,y0=y,y1=y+sy,z0=z-sz/2,z1=z+sz/2;const V=[[x0,y0,z0],[x1,y0,z0],[x1,y0,z1],[x0,y0,z1],[x0,y1,z0],[x1,y1,z0],[x1,y1,z1],[x0,y1,z1]],F=[[4,5,6,4,6,7],[0,2,1,0,3,2],[0,4,7,0,7,3],[1,2,6,1,6,5],[3,7,6,3,6,2],[0,1,5,0,5,4]];F.forEach((f,fi)=>{const k=fi===0?1:fi===1?.52:.72;for(const q of f){arr.push(...V[q],r*k,g*k,b*k)}})} function addDiamond(arr,w,col,scale=.42,height=.11){pushBox(arr,w.x,w.y,w.z,scale,height,scale,col)} const FIELD_PLAYER_COLORS={P1:[.72,.16,.20],P2:[.12,.38,.70],P3:[.82,.60,.10],P4:[.10,.58,.34]}; function fieldPlayerColor(owner,active=false){const c=FIELD_PLAYER_COLORS[owner]||[.22,.34,.28];return active?c:c.map(v=>v*.68+.04)} /* GRASSLAND-3D-R2: gameplay graph/hit remains DOM data; this WebGL layer owns only terrain, atmosphere and non-interactive scenery. */ function fieldRand(i){let x=(i+1)|0;x=Math.imul(x^0x9e3779b9,0x85ebca6b);x^=x>>>13;x=Math.imul(x,0xc2b2ae35);x^=x>>>16;return(x>>>0)/4294967296} function fieldTerrainHeight(x,z){const r=Math.hypot(x,z),edge=Math.max(0,Math.min(1,(r-11)/20)),ridge=edge*edge*(.92+.34*Math.sin(x*.18)+.28*Math.cos(z*.16)),mound=(mx,mz,a,rr)=>a*Math.exp(-((x-mx)*(x-mx)+(z-mz)*(z-mz))/rr);return-.31+.036*Math.sin(x*.41)+.032*Math.cos(z*.37)+.018*Math.sin((x-z)*.63)+ridge+mound(-13,-9,.42,22)+mound(14,-8,.36,28)+mound(-12,11,.34,25)+mound(13,12,.40,24)} function pushLitTri(arr,a,b,c,col){const ux=b[0]-a[0],uy=b[1]-a[1],uz=b[2]-a[2],vx=c[0]-a[0],vy=c[1]-a[1],vz=c[2]-a[2],nx=uy*vz-uz*vy,ny=uz*vx-ux*vz,nz=ux*vy-uy*vx,ln=Math.hypot(nx,ny,nz)||1,n=[nx/ln,ny/ln,nz/ln];for(const q of[a,b,c])arr.push(q[0],q[1],q[2],n[0],n[1],n[2],col[0],col[1],col[2])} function pushLitQuad(arr,a,b,c,d,col){pushLitTri(arr,a,b,c,col);pushLitTri(arr,a,c,d,col)} function fieldTerrainNormal(x,z){const e=.24,hl=fieldTerrainHeight(x-e,z),hr=fieldTerrainHeight(x+e,z),hd=fieldTerrainHeight(x,z-e),hu=fieldTerrainHeight(x,z+e),nx=hl-hr,ny=e*2,nz=hd-hu,l=Math.hypot(nx,ny,nz)||1;return[nx/l,ny/l,nz/l]} function fieldTerrainColor(x,z){const r=Math.hypot(x,z),v=.5+.25*Math.sin(x*.23+z*.17)+.25*Math.sin(x*.47-z*.31),meadow=Math.max(0,1-r/42);return[.145+.030*v+.028*meadow,.34+.060*v+.105*meadow,.12+.025*v+.018*meadow]} function pushTerrainTri(arr,a,b,c){for(const q of[a,b,c]){const n=fieldTerrainNormal(q[0],q[2]),col=fieldTerrainColor(q[0],q[2]);arr.push(q[0],q[1],q[2],n[0],n[1],n[2],col[0],col[1],col[2])}} function pushLitBox(arr,x,y,z,sx,sy,sz,col){const x0=x-sx/2,x1=x+sx/2,y0=y,y1=y+sy,z0=z-sz/2,z1=z+sz/2,V=[[x0,y0,z0],[x1,y0,z0],[x1,y0,z1],[x0,y0,z1],[x0,y1,z0],[x1,y1,z0],[x1,y1,z1],[x0,y1,z1]],Q=[[4,5,6,7],[0,3,2,1],[0,4,7,3],[1,2,6,5],[3,7,6,2],[0,1,5,4]];Q.forEach((q,i)=>{const k=i===0?1:i===1?.57:.78,cc=col.map(v=>v*k);pushLitQuad(arr,V[q[0]],V[q[1]],V[q[2]],V[q[3]],cc)})} function pushFieldTerrain(arr,lowPerf){const cells=lowPerf?26:42,extent=36,step=extent*2/cells;for(let iz=0;iz<c"},{"line":1726,"index":10069047,"text":"L(gl,items,viewer,cam,w,h){gl.viewport(0,0,w,h);gl.enable(gl.DEPTH_TEST);gl.depthFunc(gl.LEQUAL);gl.disable(gl.CULL_FACE);gl.clearColor(.45,.66,.73,1);gl.clear(gl.COLOR_BUFFER_BIT|gl.DEPTH_BUFFER_BIT);const a=[];pushFieldTerrain(a,state.settings.lowPerf);pushFieldTrails(a,viewer);const grassTufts=pushGrasslandScenery(a,viewer,state.settings.lowPerf);for(const it of items){if(it.sy<=0)continue;if(it.kind==='inactive')pushLitBox(a,it.x,it.y+.006,it.z,.52,.024,.52,[.27,.30,.24]);else if(it.kind==='roadMark')pushLitBox(a,it.x,it.y,it.z,.10,.010,.10,it.col.map(v=>v*.72));else pushLitBox(a,it.x,it.y,it.z,it.sx,it.sy,it.sz,it.col)};const data=new Float32Array(a),buf=gl.createBuffer();gl.bindBuffer(gl.ARRAY_BUFFER,buf);gl.bufferData(gl.ARRAY_BUFFER,data,gl.DYNAMIC_DRAW);gl.useProgram(fieldProgram);const stride=36,ap=gl.getAttribLocation(fieldProgram,'p'),an=gl.getAttribLocation(fieldProgram,'n'),ac=gl.getAttribLocation(fieldProgram,'c');gl.enableVertexAttribArray(ap);gl.vertexAttribPointer(ap,3,gl.FLOAT,false,stride,0);gl.enableVertexAttribArray(an);gl.vertexAttribPointer(an,3,gl.FLOAT,false,stride,12);gl.enableVertexAttribArray(ac);gl.vertexAttribPointer(ac,3,gl.FLOAT,false,stride,24);gl.uniform3fv(gl.getUniformLocation(fieldProgram,'uEye'),new Float32Array(cam.eye));gl.uniform3f(gl.getUniformLocation(fieldProgram,'uLight'),-.42,.88,.26);gl.uniform4f(gl.getUniformLocation(fieldProgram,'uView'),Math.tan(Math.PI/10.2),cam.aspect,cam.yBase,cam.yGain);gl.drawArrays(gl.TRIANGLES,0,data.length/9);gl.deleteBuffer(buf);fieldLastStats={mode:'webgl-grassland',vertices:data.length/9,grassTufts,lowPerf:!!state.settings.lowPerf}} function renderField3D(){const m=state.match,canvas=$('#fieldCanvas');if(!m||!canvas)return;const dpr=state.settings.lowPerf?1:Math.min(1.5,window.devicePixelRatio||1),w=Math.max(1,Math.floor(canvas.clientWidth*dpr)),h=Math.max(1,Math.floor(canvas.clientHeight*dpr));if(canvas.width!==w||canvas.height!==h){canvas.width=w;canvas.height=h}const viewer=m.players[0],cam=fieldCamera(viewer);fieldCameraState=cam;fieldMVP=null;fieldProjection.clear();const all=[];centerIds().forEach(id=>all.push(id));['P1','P2','P3','P4'].forEach(pid=>['L','C','R'].forEach(l=>{all.push(shieldId(pid,l));for(let d=1;d<=7;d++)all.push(roadId(pid,l,d))}));['K:NW','K:NE','K:SW','K:SE'].forEach(id=>all.push(id));all.forEach(id=>{const p=project3(nodeWorld(id,viewer));if(p)fieldProjection.set(id,p)});const items=[];const box=(x,y,z,sx,sy,sz,col,kind='box')=>items.push({x,y,z,sx,sy,sz,col,kind});const active=activeNodes();for(const id of all){const nw=nodeWorld(id,viewer),typ=posType(id);if(typ==='center'){box(nw.x,nw.y-.015,nw.z,1.06,.16,1.06,[.27,.32,.28],'center');box(nw.x,nw.y+.15,nw.z,.56,.018,.56,[.72,.62,.34],'centerMark')}else if(typ==='shield'){const owner=id.split(':')[1],pc=fieldPlayerColor(owner,true);box(nw.x,nw.y,nw.z,1.05,.24,1.05,[.25,.18,.30],'shield');box(nw.x,nw.y+.245,nw.z,.62,.025,.62,pc,'shieldMark')}else if(typ==='road'){const owner=parseRoad(id).owner,pc=fieldPlayerColor(owner,active.has(id)),base=active.has(id)?[.46,.49,.43]:[.31,.35,.31];box(nw.x,nw.y+(active.has(id)?-.01:-.045),nw.z,active.has(id)?.88:.78,active.has(id)?.105:.026,active.has(id)?.88:.78,base,active.has(id)?'road':'inactive');box(nw.x,nw.y+(active.has(id)?.10:-.017),nw.z,active.has(id)?.42:.16,.014,active.has(id)?.42:.16,pc,active.has(id)?'roadMarkActive':'roadMark')}else if(typ==='corner'){box(nw.x,nw.y-.02,nw.z,.82,.10,.82,[.40,.42,.36],'corner')}}if(m.honey?.byNode)for(const [id,n] of Object.entries(m.honey.byNode)){if(n<=0)continue;const q=nodeWorld(id,viewer);box(q.x,q.y+.18,q.z,.30,.50,.30,[.98,.61,.06],'honey')} const gl=initFieldGL();if(gl){drawGrasslandGL(gl,items,viewer,cam,w,h)}else{fieldLastStats={mode:'canvas2d-fallback',vertices:0,grassTufts:0,lowPerf:!!state.settings.lowPerf};const ctx=canvas.getContext('2d');if(ctx){ctx.setTransform(dpr,0,0,dpr,0,0);const cw=canvas.clientWidth,ch=canvas.clientHeight;const proj=(x,y,z)=>{const q=project3({x,y,z});return q?[q.x/100*cw,q.y/100*ch]:[0,0]}, rgba=(c,a)=>`rgba(${c.map(v=>Math.max(0,Math.min(255,Math.round(v*255)))).join(',')},${a})`, shade=(c,k)=>`rgb(${c.map(v=>Math.max(0,Math.min(255,Math.round(v*255*k)))).join(',')})`; /* Water and atmospheric depth are visual-only. The 109-node graph/hit areas remain separate DOM/runtime data. */ const water=ctx.createLinearGradient(0,0,0,ch);water.addColorStop(0,'#0a2b31');water.addColorStop(.36,'#0b3a37');water.addColorStop(1,'#061a1b');ctx.fillStyle=water;ctx.fillRect(0,0,cw,ch);const horizon=ctx.createLinearGradient(0,0,cw,0);horizon.addColorStop(0,'rgba(46,107,92,.02)');horizon.addColorStop(.5,'rgba(117,190,159,.15)');horizon.addColorStop(1,'rgba(46,107,92,.02)');ctx.fillStyle=horizon;ctx.fillRect(0,0,cw,ch*.48); ctx.save();ctx.globalAlpha=state.settings.lowPerf?.14:.24;ctx.strokeStyle='#4f8f87';ctx.lineWidth=1;for(let j=0;j<7;j++){const y=ch*(.18+j*.115);ctx.beginPath();for(let x=-20;x<=cw+20;x+=26){const yy=y+Math.sin((x+j*37)*.024)*2.3;const nx=x+26,ny=y+Math.sin((nx+j*37)*.024)*2.3;if(x<0)ctx.moveTo(x,yy);else ctx.quadraticCurveTo(x+13,yy+1.6,nx,ny)}ctx.stroke()}ctx.restore(); function worldPath(points,y=-.26){ctx.beginPath();points.forEach(([x,z],i)=>{const p=proj(x,y,z);if(i===0)ctx.moveTo(p[0],p[1]);else ctx.lineTo(p[0],p[1])});ctx.closePath()} const coast=[[-10,-5],[-8.7,-8.1],[-5.3,-9.7],[-1.4,-9.1],[2.4,-9.8],[6.2,-8.8],[9.1,-5.9"},{"line":1726,"index":10069131,"text":"c(gl.LEQUAL);gl.disable(gl.CULL_FACE);gl.clearColor(.45,.66,.73,1);gl.clear(gl.COLOR_BUFFER_BIT|gl.DEPTH_BUFFER_BIT);const a=[];pushFieldTerrain(a,state.settings.lowPerf);pushFieldTrails(a,viewer);const grassTufts=pushGrasslandScenery(a,viewer,state.settings.lowPerf);for(const it of items){if(it.sy<=0)continue;if(it.kind==='inactive')pushLitBox(a,it.x,it.y+.006,it.z,.52,.024,.52,[.27,.30,.24]);else if(it.kind==='roadMark')pushLitBox(a,it.x,it.y,it.z,.10,.010,.10,it.col.map(v=>v*.72));else pushLitBox(a,it.x,it.y,it.z,it.sx,it.sy,it.sz,it.col)};const data=new Float32Array(a),buf=gl.createBuffer();gl.bindBuffer(gl.ARRAY_BUFFER,buf);gl.bufferData(gl.ARRAY_BUFFER,data,gl.DYNAMIC_DRAW);gl.useProgram(fieldProgram);const stride=36,ap=gl.getAttribLocation(fieldProgram,'p'),an=gl.getAttribLocation(fieldProgram,'n'),ac=gl.getAttribLocation(fieldProgram,'c');gl.enableVertexAttribArray(ap);gl.vertexAttribPointer(ap,3,gl.FLOAT,false,stride,0);gl.enableVertexAttribArray(an);gl.vertexAttribPointer(an,3,gl.FLOAT,false,stride,12);gl.enableVertexAttribArray(ac);gl.vertexAttribPointer(ac,3,gl.FLOAT,false,stride,24);gl.uniform3fv(gl.getUniformLocation(fieldProgram,'uEye'),new Float32Array(cam.eye));gl.uniform3f(gl.getUniformLocation(fieldProgram,'uLight'),-.42,.88,.26);gl.uniform4f(gl.getUniformLocation(fieldProgram,'uView'),Math.tan(Math.PI/10.2),cam.aspect,cam.yBase,cam.yGain);gl.drawArrays(gl.TRIANGLES,0,data.length/9);gl.deleteBuffer(buf);fieldLastStats={mode:'webgl-grassland',vertices:data.length/9,grassTufts,lowPerf:!!state.settings.lowPerf}} function renderField3D(){const m=state.match,canvas=$('#fieldCanvas');if(!m||!canvas)return;const dpr=state.settings.lowPerf?1:Math.min(1.5,window.devicePixelRatio||1),w=Math.max(1,Math.floor(canvas.clientWidth*dpr)),h=Math.max(1,Math.floor(canvas.clientHeight*dpr));if(canvas.width!==w||canvas.height!==h){canvas.width=w;canvas.height=h}const viewer=m.players[0],cam=fieldCamera(viewer);fieldCameraState=cam;fieldMVP=null;fieldProjection.clear();const all=[];centerIds().forEach(id=>all.push(id));['P1','P2','P3','P4'].forEach(pid=>['L','C','R'].forEach(l=>{all.push(shieldId(pid,l));for(let d=1;d<=7;d++)all.push(roadId(pid,l,d))}));['K:NW','K:NE','K:SW','K:SE'].forEach(id=>all.push(id));all.forEach(id=>{const p=project3(nodeWorld(id,viewer));if(p)fieldProjection.set(id,p)});const items=[];const box=(x,y,z,sx,sy,sz,col,kind='box')=>items.push({x,y,z,sx,sy,sz,col,kind});const active=activeNodes();for(const id of all){const nw=nodeWorld(id,viewer),typ=posType(id);if(typ==='center'){box(nw.x,nw.y-.015,nw.z,1.06,.16,1.06,[.27,.32,.28],'center');box(nw.x,nw.y+.15,nw.z,.56,.018,.56,[.72,.62,.34],'centerMark')}else if(typ==='shield'){const owner=id.split(':')[1],pc=fieldPlayerColor(owner,true);box(nw.x,nw.y,nw.z,1.05,.24,1.05,[.25,.18,.30],'shield');box(nw.x,nw.y+.245,nw.z,.62,.025,.62,pc,'shieldMark')}else if(typ==='road'){const owner=parseRoad(id).owner,pc=fieldPlayerColor(owner,active.has(id)),base=active.has(id)?[.46,.49,.43]:[.31,.35,.31];box(nw.x,nw.y+(active.has(id)?-.01:-.045),nw.z,active.has(id)?.88:.78,active.has(id)?.105:.026,active.has(id)?.88:.78,base,active.has(id)?'road':'inactive');box(nw.x,nw.y+(active.has(id)?.10:-.017),nw.z,active.has(id)?.42:.16,.014,active.has(id)?.42:.16,pc,active.has(id)?'roadMarkActive':'roadMark')}else if(typ==='corner'){box(nw.x,nw.y-.02,nw.z,.82,.10,.82,[.40,.42,.36],'corner')}}if(m.honey?.byNode)for(const [id,n] of Object.entries(m.honey.byNode)){if(n<=0)continue;const q=nodeWorld(id,viewer);box(q.x,q.y+.18,q.z,.30,.50,.30,[.98,.61,.06],'honey')} const gl=initFieldGL();if(gl){drawGrasslandGL(gl,items,viewer,cam,w,h)}else{fieldLastStats={mode:'canvas2d-fallback',vertices:0,grassTufts:0,lowPerf:!!state.settings.lowPerf};const ctx=canvas.getContext('2d');if(ctx){ctx.setTransform(dpr,0,0,dpr,0,0);const cw=canvas.clientWidth,ch=canvas.clientHeight;const proj=(x,y,z)=>{const q=project3({x,y,z});return q?[q.x/100*cw,q.y/100*ch]:[0,0]}, rgba=(c,a)=>`rgba(${c.map(v=>Math.max(0,Math.min(255,Math.round(v*255)))).join(',')},${a})`, shade=(c,k)=>`rgb(${c.map(v=>Math.max(0,Math.min(255,Math.round(v*255*k)))).join(',')})`; /* Water and atmospheric depth are visual-only. The 109-node graph/hit areas remain separate DOM/runtime data. */ const water=ctx.createLinearGradient(0,0,0,ch);water.addColorStop(0,'#0a2b31');water.addColorStop(.36,'#0b3a37');water.addColorStop(1,'#061a1b');ctx.fillStyle=water;ctx.fillRect(0,0,cw,ch);const horizon=ctx.createLinearGradient(0,0,cw,0);horizon.addColorStop(0,'rgba(46,107,92,.02)');horizon.addColorStop(.5,'rgba(117,190,159,.15)');horizon.addColorStop(1,'rgba(46,107,92,.02)');ctx.fillStyle=horizon;ctx.fillRect(0,0,cw,ch*.48); ctx.save();ctx.globalAlpha=state.settings.lowPerf?.14:.24;ctx.strokeStyle='#4f8f87';ctx.lineWidth=1;for(let j=0;j<7;j++){const y=ch*(.18+j*.115);ctx.beginPath();for(let x=-20;x<=cw+20;x+=26){const yy=y+Math.sin((x+j*37)*.024)*2.3;const nx=x+26,ny=y+Math.sin((nx+j*37)*.024)*2.3;if(x<0)ctx.moveTo(x,yy);else ctx.quadraticCurveTo(x+13,yy+1.6,nx,ny)}ctx.stroke()}ctx.restore(); function worldPath(points,y=-.26){ctx.beginPath();points.forEach(([x,z],i)=>{const p=proj(x,y,z);if(i===0)ctx.moveTo(p[0],p[1]);else ctx.lineTo(p[0],p[1])});ctx.closePath()} const coast=[[-10,-5],[-8.7,-8.1],[-5.3,-9.7],[-1.4,-9.1],[2.4,-9.8],[6.2,-8.8],[9.1,-5.9],[9.8,-1.8],[9.0,2.3],[10.0,6.0],[6.8,9.1],[2.2,9.5],[-1.8,9.0],[-5.9,9.6],[-9.1,6."},{"line":1726,"index":10069203,"text":"ear(gl.COLOR_BUFFER_BIT|gl.DEPTH_BUFFER_BIT);const a=[];pushFieldTerrain(a,state.settings.lowPerf);pushFieldTrails(a,viewer);const grassTufts=pushGrasslandScenery(a,viewer,state.settings.lowPerf);for(const it of items){if(it.sy<=0)continue;if(it.kind==='inactive')pushLitBox(a,it.x,it.y+.006,it.z,.52,.024,.52,[.27,.30,.24]);else if(it.kind==='roadMark')pushLitBox(a,it.x,it.y,it.z,.10,.010,.10,it.col.map(v=>v*.72));else pushLitBox(a,it.x,it.y,it.z,it.sx,it.sy,it.sz,it.col)};const data=new Float32Array(a),buf=gl.createBuffer();gl.bindBuffer(gl.ARRAY_BUFFER,buf);gl.bufferData(gl.ARRAY_BUFFER,data,gl.DYNAMIC_DRAW);gl.useProgram(fieldProgram);const stride=36,ap=gl.getAttribLocation(fieldProgram,'p'),an=gl.getAttribLocation(fieldProgram,'n'),ac=gl.getAttribLocation(fieldProgram,'c');gl.enableVertexAttribArray(ap);gl.vertexAttribPointer(ap,3,gl.FLOAT,false,stride,0);gl.enableVertexAttribArray(an);gl.vertexAttribPointer(an,3,gl.FLOAT,false,stride,12);gl.enableVertexAttribArray(ac);gl.vertexAttribPointer(ac,3,gl.FLOAT,false,stride,24);gl.uniform3fv(gl.getUniformLocation(fieldProgram,'uEye'),new Float32Array(cam.eye));gl.uniform3f(gl.getUniformLocation(fieldProgram,'uLight'),-.42,.88,.26);gl.uniform4f(gl.getUniformLocation(fieldProgram,'uView'),Math.tan(Math.PI/10.2),cam.aspect,cam.yBase,cam.yGain);gl.drawArrays(gl.TRIANGLES,0,data.length/9);gl.deleteBuffer(buf);fieldLastStats={mode:'webgl-grassland',vertices:data.length/9,grassTufts,lowPerf:!!state.settings.lowPerf}} function renderField3D(){const m=state.match,canvas=$('#fieldCanvas');if(!m||!canvas)return;const dpr=state.settings.lowPerf?1:Math.min(1.5,window.devicePixelRatio||1),w=Math.max(1,Math.floor(canvas.clientWidth*dpr)),h=Math.max(1,Math.floor(canvas.clientHeight*dpr));if(canvas.width!==w||canvas.height!==h){canvas.width=w;canvas.height=h}const viewer=m.players[0],cam=fieldCamera(viewer);fieldCameraState=cam;fieldMVP=null;fieldProjection.clear();const all=[];centerIds().forEach(id=>all.push(id));['P1','P2','P3','P4'].forEach(pid=>['L','C','R'].forEach(l=>{all.push(shieldId(pid,l));for(let d=1;d<=7;d++)all.push(roadId(pid,l,d))}));['K:NW','K:NE','K:SW','K:SE'].forEach(id=>all.push(id));all.forEach(id=>{const p=project3(nodeWorld(id,viewer));if(p)fieldProjection.set(id,p)});const items=[];const box=(x,y,z,sx,sy,sz,col,kind='box')=>items.push({x,y,z,sx,sy,sz,col,kind});const active=activeNodes();for(const id of all){const nw=nodeWorld(id,viewer),typ=posType(id);if(typ==='center'){box(nw.x,nw.y-.015,nw.z,1.06,.16,1.06,[.27,.32,.28],'center');box(nw.x,nw.y+.15,nw.z,.56,.018,.56,[.72,.62,.34],'centerMark')}else if(typ==='shield'){const owner=id.split(':')[1],pc=fieldPlayerColor(owner,true);box(nw.x,nw.y,nw.z,1.05,.24,1.05,[.25,.18,.30],'shield');box(nw.x,nw.y+.245,nw.z,.62,.025,.62,pc,'shieldMark')}else if(typ==='road'){const owner=parseRoad(id).owner,pc=fieldPlayerColor(owner,active.has(id)),base=active.has(id)?[.46,.49,.43]:[.31,.35,.31];box(nw.x,nw.y+(active.has(id)?-.01:-.045),nw.z,active.has(id)?.88:.78,active.has(id)?.105:.026,active.has(id)?.88:.78,base,active.has(id)?'road':'inactive');box(nw.x,nw.y+(active.has(id)?.10:-.017),nw.z,active.has(id)?.42:.16,.014,active.has(id)?.42:.16,pc,active.has(id)?'roadMarkActive':'roadMark')}else if(typ==='corner'){box(nw.x,nw.y-.02,nw.z,.82,.10,.82,[.40,.42,.36],'corner')}}if(m.honey?.byNode)for(const [id,n] of Object.entries(m.honey.byNode)){if(n<=0)continue;const q=nodeWorld(id,viewer);box(q.x,q.y+.18,q.z,.30,.50,.30,[.98,.61,.06],'honey')} const gl=initFieldGL();if(gl){drawGrasslandGL(gl,items,viewer,cam,w,h)}else{fieldLastStats={mode:'canvas2d-fallback',vertices:0,grassTufts:0,lowPerf:!!state.settings.lowPerf};const ctx=canvas.getContext('2d');if(ctx){ctx.setTransform(dpr,0,0,dpr,0,0);const cw=canvas.clientWidth,ch=canvas.clientHeight;const proj=(x,y,z)=>{const q=project3({x,y,z});return q?[q.x/100*cw,q.y/100*ch]:[0,0]}, rgba=(c,a)=>`rgba(${c.map(v=>Math.max(0,Math.min(255,Math.round(v*255)))).join(',')},${a})`, shade=(c,k)=>`rgb(${c.map(v=>Math.max(0,Math.min(255,Math.round(v*255*k)))).join(',')})`; /* Water and atmospheric depth are visual-only. The 109-node graph/hit areas remain separate DOM/runtime data. */ const water=ctx.createLinearGradient(0,0,0,ch);water.addColorStop(0,'#0a2b31');water.addColorStop(.36,'#0b3a37');water.addColorStop(1,'#061a1b');ctx.fillStyle=water;ctx.fillRect(0,0,cw,ch);const horizon=ctx.createLinearGradient(0,0,cw,0);horizon.addColorStop(0,'rgba(46,107,92,.02)');horizon.addColorStop(.5,'rgba(117,190,159,.15)');horizon.addColorStop(1,'rgba(46,107,92,.02)');ctx.fillStyle=horizon;ctx.fillRect(0,0,cw,ch*.48); ctx.save();ctx.globalAlpha=state.settings.lowPerf?.14:.24;ctx.strokeStyle='#4f8f87';ctx.lineWidth=1;for(let j=0;j<7;j++){const y=ch*(.18+j*.115);ctx.beginPath();for(let x=-20;x<=cw+20;x+=26){const yy=y+Math.sin((x+j*37)*.024)*2.3;const nx=x+26,ny=y+Math.sin((nx+j*37)*.024)*2.3;if(x<0)ctx.moveTo(x,yy);else ctx.quadraticCurveTo(x+13,yy+1.6,nx,ny)}ctx.stroke()}ctx.restore(); function worldPath(points,y=-.26){ctx.beginPath();points.forEach(([x,z],i)=>{const p=proj(x,y,z);if(i===0)ctx.moveTo(p[0],p[1]);else ctx.lineTo(p[0],p[1])});ctx.closePath()} const coast=[[-10,-5],[-8.7,-8.1],[-5.3,-9.7],[-1.4,-9.1],[2.4,-9.8],[6.2,-8.8],[9.1,-5.9],[9.8,-1.8],[9.0,2.3],[10.0,6.0],[6.8,9.1],[2.2,9.5],[-1.8,9.0],[-5.9,9.6],[-9.1,6.8],[-9.8,2.3],[-9.2,-1.5]];worldPath(coast);ctx.shadowColor='rgba(0,0,0,"},{"line":2657,"index":10308781,"text":"_keeps_current\",\"standard_defeat_sound_disabled\",\"preview_assets_opt_in_only\"]}</script> <script id=\"gameroad-browser-first10-overlay\" type=\"application/json\">{\"schema\":\"GAMEROAD_BROWSER_FIRST10_OVERLAY_V1\",\"work_id\":\"BROWSER-FIRST10-ONBOARDING-RUNTIME-001\",\"base_drive_id\":\"1ooLZ1XkQHwkPeLgTtPPGkBBYZLn2vAa7\",\"source_contract\":\"1raK2cBU8LOAMjTf8LdfWAzk9bijJ8vkv4a8VRIksPt4\",\"state\":\"partial_candidate_current_rebase\",\"formal_account_completion\":false,\"persistent_completion\":false,\"historical_large_number_order_imported\":false,\"current_small_number_order_preserved\":true,\"rules\":[\"no_dedicated_tutorial\",\"no_long_modal\",\"manual_action_distinct_from_auto\",\"acceptance_not_click\",\"accepted_road_visible\",\"no_auto_deck\",\"no_dcg_force\",\"no_hidden_assist\",\"no_new_reward\"]}</script> <!-- GAMEROAD BROWSER ABILITY TIE UNBIASED ORDER V1: CURRENT tie-order repair; no rule text/value changes --> <script id=\"gameroad-ability-tie-order-contract\" type=\"application/json\">{\"schema\":\"GAMEROAD_BROWSER_ABILITY_TIE_UNBIASED_ORDER_V1\",\"work_id\":\"BROWSER-ABILITY-TIE-UNBIASED-ORDER-001\",\"base_sha256\":\"124c336f8a5835e5339dab5dda30a4021873608e3f3563c2439a08db74a069cf\",\"rule\":\"printed-base ascending; equal group uniform unspecified random; same bundle receipt stable\",\"non_targets\":[\"road-active-max\",\"battle-complete-tie-active-side\",\"ability-text\",\"ability-cost\",\"ability-target\"]}</script> <!-- GAMEROAD BROWSER LATEST BRANCH MERGE V1: contextual help rebased onto 2v2 target + ability tie latest --> <script id=\"gameroad-latest-branch-merge-contract\" type=\"application/json\">{\"schema\":\"GAMEROAD_BROWSER_LATEST_BRANCH_MERGE_V1\",\"work_id\":\"BROWSER-CONTEXT-HELP-LATEST-MERGE-001\",\"base_drive_id\":\"1tzCtbkFgvX-6_L6gu8nyZxj0_-zQtqA4\",\"context_source_drive_id\":\"1iUrg6C40i7COSnNqjbOO-7z-dZfSroKy\",\"merge_policy\":\"context reason layer only; target legality delegates to latest core; ability order untouched\"}</script> <!-- GAMEROAD BROWSER-BATTLE-PAYOFF-PRESENTATION-001 / separate-ID isolated derivative / rules unchanged --> <script id=\"gameroad-partner-role-integration-contract\" type=\"application/json\">{\"schema\":\"GAMEROAD_BROWSER_PARTNER_ROLE_INTEGRATION_V1\",\"work_id\":\"BROWSER-PLAYABLE-BOARD-PARTNER-COLLECTIVE-001\",\"base_drive_id\":\"1_j7U04yvQFEZUPSKCn8Bd29U6-0zSSmg\",\"state_schema\":3,\"roles\":{\"playerCharacterId\":\"board-operated-character\",\"selectedPartnerId\":\"advice-partner\"},\"legacy_migration\":\"v1-v2 selectedCharacter -> both roles once, then independent\",\"partner_profiles\":{\"equipment_slots\":\"saved-empty-until-formal-catalog\",\"approved_dialogue_manifest\":\"null-until-approved\"},\"unimplemented_policy\":\"conversation/reward/dressup-affinity-write/solo stop with reason; no fabricated rules\",\"collective\":{\"source\":\"prototype_local\",\"evidence_level\":\"L3\",\"external_send\":false,\"auto_adopt\":false},\"protected\":\"game rules/card values/victory/economy/secret planning unchanged\"}</script> <style id=\"gameroad-partner-battle-comment-log-v1\"> /* GAMEROAD_BROWSER_PARTNER_BATTLE_COMMENT_LOG_V1 — nonblocking battle comment + session log. */ .partnerBattleCommentLayer{position:absolute;left:8px;bottom:calc(18% + 18px);z-index:31;width:min(360px,38vw);display:none;grid-template-columns:minmax(0,1fr) 48px;gap:6px;align-items:end;pointer-events:none}.partnerBattleCommentLayer.on{display:grid}.partnerBattleBubble{min-width:0;border:1px solid rgba(199,234,224,.52);border-left:4px solid var(--a);border-radius:10px;background:linear-gradient(120deg,rgba(3,20,17,.94),rgba(11,41,34,.92));box-shadow:0 14px 36px rgba(0,0,0,.36);padding:7px 9px;backdrop-filter:blur(5px);pointer-events:none}.partnerBattleBubble[data-tone=\"warning\"]{border-left-color:#ff9f7a}.partnerBattleBubble[data-tone=\"result\"]{border-left-color:var(--gold)}.partnerBattleBubble .partnerCommentHead{display:flex;align-items:center;gap:6px;min-width:0;color:#c9e6dd;font-size:8px;font-weight:950;letter-spacing:.08em}.partnerBattleBubble .partnerCommentHead b{min-width:0;max-width:65%;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;color:#f5fffb;font-size:9px}.partnerBattleBubble .partnerCommentType{margin-left:auto;padding:2px 4px;border:1px solid rgba(217,242,234,.24);border-radius:4px;color:#b8d7ce}.partnerBattleBubble .partnerCommentText{margin-top:3px;color:#f4fbf8;font-size:11px;font-weight:850;line-height:1.35;overflow-wrap:anywhere}.partnerBattleBubble .partnerCommentMeta{display:none;margin-top:3px;color:#98b8af;font-size:8px;line-height:1.25}.partnerCommentFeedback{display:none;align-items:center;gap:4px;margin-top:5px;pointer-events:auto;position:relative}.partnerCommentFeedback.on{display:flex}.partnerFeedbackBtn{min-width:52px;height:44px;min-height:44px;padding:0 7px;border:1px solid rgba(216,244,233,.34);border-radius:8px;background:rgba(6,30,25,.94);color:#eefaf6;font-size:9px;font-weight:950;pointer-events:auto}.partnerFeedbackBtn[aria-pressed=\"true\"]{border-color:#d8fff0;box-shadow:0 0 0 1px rgba(216,255,240,.18) inset}.partnerFeedbackBtn[data-rating=\"good\"][aria-pressed=\"true\"]{background:#114a3c;color:#fff4fa}.partnerFeedbackBtn[data-rating=\"bad\"][aria-pressed=\"true\"]{background:#4b2926;color:#fff0e9}.partnerFeedbackHeart{position:absolute;left:20px;top:-13px;display:none;font-size:22px;line-height:1;color:#ff8eaf;text-shadow:0 2px 10px rgba(255,90,140,.55);pointer-events:none}.partnerFeedbackHeart.on{display:block;animation:gameroad-partner-good-heart .68s ease-out both}.partnerFeedbackH"}],"battleMapMarkup":[{"line":1154,"index":1102257,"text":"ード・シールド</button><button class=\"contentBtn\" data-content=\"honey_hunt\">ハニーハント</button></div><div class=\"k\">FORMAT</div><div class=\"modeRow\"><button class=\"modeBtn on\" data-mode=\"2p\">二人</button><button class=\"modeBtn\" data-mode=\"4p\">四人</button><button class=\"modeBtn\" data-mode=\"2v2\">二対二</button></div><div class=\"settingRow\"><span>相手</span><b>ボット</b></div><div class=\"settingRow setupStartPosition\" hidden><span>開始位置</span><b>中央・共通開始</b></div><div class=\"setupDeckRecovery\"><div class=\"setupDeckNote\" id=\"setupDeckNote\">札組を確認中</div><button class=\"btn\" id=\"fixDeckFromSetup\" hidden>デッキへ</button></div><button class=\"btn\" id=\"friendRoomEntry\">フレンドルーム</button><button class=\"btn primary\" id=\"startMatch\">盤面へ</button></div></section> <section class=\"screen friendroom\" data-screen=\"friendroom\"><button class=\"btn back\" data-back aria-label=\"一つ前の画面へ戻る\">← 戻る</button><div id=\"friendRoomPanel\" class=\"friendRoomPanel\"></div></section><section class=\"screen battle\" data-screen=\"battle\"><div class=\"battleMap\" id=\"battleMap\"><canvas id=\"fieldCanvas\" aria-hidden=\"true\"></canvas><svg id=\"routeSvg\" class=\"routeSvg\" aria-hidden=\"true\" viewBox=\"0 0 100 100\" preserveAspectRatio=\"none\"><g id=\"rangeSurfaceLayer\"></g><polyline id=\"routeLine\"></polyline></svg><div class=\"phaseBar\" id=\"phaseBar\"><span data-ph=\"plan\">計画</span><span data-ph=\"reveal\">公開</span><span data-ph=\"move\">移動</span><span data-ph=\"target\">対象</span><span data-ph=\"resolve\">戦闘</span></div><div class=\"battleTopStatus\"><div><span class=\"k\">ROUND <b id=\"roundNo\">1</b></span><strong id=\"phaseTitle\">行動を計画</strong></div><div class=\"resourceStrip\"><span id=\"manaMeter\">マナ 0/7</span><span id=\"honeyMeter\" hidden>ハニー 0</span></div></div><div class=\"publicTurnHud\" id=\"publicTurnHud\" aria-live=\"polite\"><div class=\"publicPlayerStrip\" id=\"publicPlayerStrip\"></div><div class=\"royalUsageStrip\" id=\"royalUsageStrip\" aria-label=\"各プレイヤーのロイヤルカード使用数\"></div></div><div class=\"board\" id=\"board\"></div><div id=\"boardPlayers\" class=\"boardPlayers\" aria-label=\"盤上参加者\"></div><div id=\"battleRuntime\" class=\"battleAvatar\" role=\"button\" aria-label=\"人物をドラッグして移動経路を予約\"></div><div id=\"originMarker\" class=\"originMarker\" aria-hidden=\"true\"></div><div class=\"quickAwardBurst\" id=\"quickAwardBurst\" aria-live=\"polite\" aria-atomic=\"true\"><strong></strong><span></span></div><section id=\"battleResolution\" class=\"battleResolution\" aria-live=\"polite\" aria-atomic=\"true\" hidden></section><div class=\"battleInfo\"><div class=\"hand\" id=\"hand\"></div><div class=\"planBox\"><div class=\"planSelect\"><label>ロード</label><select id=\"roadSelect\"></select></div><div class=\"planSelect\"><label>バトル</label><select id=\"battleSelect\"></select></div><div class=\"endpointChip\"><label>終点</label><b id=\"endpointText\">C:0:0</b></div><button class=\"iconBtn\" id=\"clearPath\" title=\"経路を戻す\" aria-label=\"経路を戻す\">↶</button><div class=\"quickReadyGroup\"><span class=\"quickCoil\" id=\"quickCoil\" aria-hidden=\"true\"><b id=\"quickRemain\">30</b></span><button class=\"btn primary\" id=\"readyPlan\"><span class=\"quickReadyText\">準備完了</span></button></div></div></div><div class=\"battleRail\"><button class=\"railBtn\" id=\"detailsBtn\" aria-label=\"対戦情報\">情報</button><button class=\"railBtn\" id=\"dangerBtn\" aria-label=\"移動可能範囲\">範囲</button><button class=\"railBtn warn\" id=\"leaveMatch\">退出</button></div><div class=\"targetBox floatingTarget\" id=\"targetBox\"><select id=\"targetPlayer\"></select><select id=\"targetLane\"><option value=\"L\">左列</option><option value=\"C\">中央列</option><option value=\"R\">右列</option></select><select id=\"targetShield\"><option value=\"L\">左シールド</option><option value=\"C\">中央シールド</option><option value=\"R\">右シールド</option></select><button class=\"btn primary\" id=\"confirmTarget\">攻撃先を決定</button></div></div><section id=\"battlePhaseSurface\" class=\"battlePhaseSurface\" aria-label=\"バトルフェイズ専用画面\" aria-live=\"polite\" hidden><div class=\"battlePhaseBackdrop\" aria-hidden=\"true\"></div><header class=\"battlePhaseHeader\"><span>BATTLE PHASE</span><b id=\"battlePhaseRound\">第1巡</b><small id=\"battlePhaseStage\">戦闘開始</small></header><div class=\"battlePhaseCutin\" id=\"battlePhaseCutin\" aria-label=\"緋累ナキ カットイン\"><div class=\"battlePhaseNaki\" id=\"battlePhaseNaki\"></div><div class=\"battlePhaseCutinCopy\"><span>PARTNER CUT-IN</span><strong>緋累ナキ</strong></div></div><div class=\"battlePhaseTarget\" id=\"battlePhaseTarget\"></div><div class=\"battlePhaseResolutionSlot\" id=\"battlePhaseResolutionSlot\"></div></section><aside class=\"battleDrawer\" id=\"battleDrawer\" aria-hidden=\"true\"><button class=\"drawerClose\" id=\"detailsClose\" aria-label=\"閉じる\">×</button><div class=\"k\">MATCH INFO</div><div class=\"players\" id=\"players\"></div><details class=\"battleLogDetails\" id=\"battleLogDetails\"><summary>対戦ログ</summary><div class=\"log\" id=\"battleLog\"></div></details></aside></section> <section class=\"screen result\" data-screen=\"result\"><div class=\"resultWorld\"></div><aside class=\"resultRank\"><div class=\"k\">MATCH RESULT</div><div class=\"grade\" id=\"resultGrade\">1</div><h2 id=\"resultHeadline\">勝利</h2><div class=\"sub\" id=\"resultSummary\"></div></aside><main class=\"resultBody\"><div class=\"resultLead\"><div><div class=\"k\">FINAL ORDER</div><strong id=\"resultLeadText\">対戦結果</strong></div><span id=\"resultLeadSub\">一列七枚を目指した最終順位</span></div><div class=\"resultMeta\"><div><span>FORMAT</span><b id=\"resultMode\">-</b></div><div><span>ROUNDS</span><b id=\"resultRounds\">-</b></div><div><span>YOUR LONGEST</span><b id=\"resultLane\">-</b></div></div><div class=\"resultPoints\" id=\"resultBattlePoints\"><div class=\"resultPointsTotal\"><span>今回のBATTLE POINTS</span><b id=\"resultPointsEarned\">+0</b></div><div class=\"resultPointsDetail\"><span>内訳</span><b id=\"resultPointsBreakdown\">通常 +0 / クイック +0</b><div class=\"resultPointsMeter\" aria-label=\"10万点宝箱進捗\"><i id=\"resultPointsMeterFill\"></i></div><span id=\"resultPointsChest\">宝箱まで 0 / 100,000</span></div></div><div id=\"resultRanking\"></div></main><details id=\"resultReplay\" class=\"safeNote\" hidden><summary>リプレイ</summary><div id=\"resultReplayEvents\"></div></details><div class=\"resultBtns\"><button class=\"btn\" data-root-go=\"home\">ホーム</button><button clas"}],"battleRailMarkup":[{"line":1154,"index":1104303,"text":"vatar\" role=\"button\" aria-label=\"人物をドラッグして移動経路を予約\"></div><div id=\"originMarker\" class=\"originMarker\" aria-hidden=\"true\"></div><div class=\"quickAwardBurst\" id=\"quickAwardBurst\" aria-live=\"polite\" aria-atomic=\"true\"><strong></strong><span></span></div><section id=\"battleResolution\" class=\"battleResolution\" aria-live=\"polite\" aria-atomic=\"true\" hidden></section><div class=\"battleInfo\"><div class=\"hand\" id=\"hand\"></div><div class=\"planBox\"><div class=\"planSelect\"><label>ロード</label><select id=\"roadSelect\"></select></div><div class=\"planSelect\"><label>バトル</label><select id=\"battleSelect\"></select></div><div class=\"endpointChip\"><label>終点</label><b id=\"endpointText\">C:0:0</b></div><button class=\"iconBtn\" id=\"clearPath\" title=\"経路を戻す\" aria-label=\"経路を戻す\">↶</button><div class=\"quickReadyGroup\"><span class=\"quickCoil\" id=\"quickCoil\" aria-hidden=\"true\"><b id=\"quickRemain\">30</b></span><button class=\"btn primary\" id=\"readyPlan\"><span class=\"quickReadyText\">準備完了</span></button></div></div></div><div class=\"battleRail\"><button class=\"railBtn\" id=\"detailsBtn\" aria-label=\"対戦情報\">情報</button><button class=\"railBtn\" id=\"dangerBtn\" aria-label=\"移動可能範囲\">範囲</button><button class=\"railBtn warn\" id=\"leaveMatch\">退出</button></div><div class=\"targetBox floatingTarget\" id=\"targetBox\"><select id=\"targetPlayer\"></select><select id=\"targetLane\"><option value=\"L\">左列</option><option value=\"C\">中央列</option><option value=\"R\">右列</option></select><select id=\"targetShield\"><option value=\"L\">左シールド</option><option value=\"C\">中央シールド</option><option value=\"R\">右シールド</option></select><button class=\"btn primary\" id=\"confirmTarget\">攻撃先を決定</button></div></div><section id=\"battlePhaseSurface\" class=\"battlePhaseSurface\" aria-label=\"バトルフェイズ専用画面\" aria-live=\"polite\" hidden><div class=\"battlePhaseBackdrop\" aria-hidden=\"true\"></div><header class=\"battlePhaseHeader\"><span>BATTLE PHASE</span><b id=\"battlePhaseRound\">第1巡</b><small id=\"battlePhaseStage\">戦闘開始</small></header><div class=\"battlePhaseCutin\" id=\"battlePhaseCutin\" aria-label=\"緋累ナキ カットイン\"><div class=\"battlePhaseNaki\" id=\"battlePhaseNaki\"></div><div class=\"battlePhaseCutinCopy\"><span>PARTNER CUT-IN</span><strong>緋累ナキ</strong></div></div><div class=\"battlePhaseTarget\" id=\"battlePhaseTarget\"></div><div class=\"battlePhaseResolutionSlot\" id=\"battlePhaseResolutionSlot\"></div></section><aside class=\"battleDrawer\" id=\"battleDrawer\" aria-hidden=\"true\"><button class=\"drawerClose\" id=\"detailsClose\" aria-label=\"閉じる\">×</button><div class=\"k\">MATCH INFO</div><div class=\"players\" id=\"players\"></div><details class=\"battleLogDetails\" id=\"battleLogDetails\"><summary>対戦ログ</summary><div class=\"log\" id=\"battleLog\"></div></details></aside></section> <section class=\"screen result\" data-screen=\"result\"><div class=\"resultWorld\"></div><aside class=\"resultRank\"><div class=\"k\">MATCH RESULT</div><div class=\"grade\" id=\"resultGrade\">1</div><h2 id=\"resultHeadline\">勝利</h2><div class=\"sub\" id=\"resultSummary\"></div></aside><main class=\"resultBody\"><div class=\"resultLead\"><div><div class=\"k\">FINAL ORDER</div><strong id=\"resultLeadText\">対戦結果</strong></div><span id=\"resultLeadSub\">一列七枚を目指した最終順位</span></div><div class=\"resultMeta\"><div><span>FORMAT</span><b id=\"resultMode\">-</b></div><div><span>ROUNDS</span><b id=\"resultRounds\">-</b></div><div><span>YOUR LONGEST</span><b id=\"resultLane\">-</b></div></div><div class=\"resultPoints\" id=\"resultBattlePoints\"><div class=\"resultPointsTotal\"><span>今回のBATTLE POINTS</span><b id=\"resultPointsEarned\">+0</b></div><div class=\"resultPointsDetail\"><span>内訳</span><b id=\"resultPointsBreakdown\">通常 +0 / クイック +0</b><div class=\"resultPointsMeter\" aria-label=\"10万点宝箱進捗\"><i id=\"resultPointsMeterFill\"></i></div><span id=\"resultPointsChest\">宝箱まで 0 / 100,000</span></div></div><div id=\"resultRanking\"></div></main><details id=\"resultReplay\" class=\"safeNote\" hidden><summary>リプレイ</summary><div id=\"resultReplayEvents\"></div></details><div class=\"resultBtns\"><button class=\"btn\" data-root-go=\"home\">ホーム</button><button class=\"btn primary\" id=\"rematch\">再戦</button></div></se"}],"renderBoardDef":[{"line":1770,"index":10091378,"text":"r=el.getBoundingClientRect();if(!hit(r,a,2)&&placed.every(x=>!hit(r,x,2))){placed.push(r);ok=true;break}}if(!ok){for(const [ox,oy] of pool){el.style.setProperty('--ox',(ox*1.45)+'px');el.style.setProperty('--oy',(oy*1.45)+'px');const r=el.getBoundingClientRect();if(!hit(r,a,2)&&placed.every(x=>!hit(r,x,2))){placed.push(r);ok=true;break}}}if(!ok)placed.push(el.getBoundingClientRect())}} function renderBoardPlayers(){const root=$('#boardPlayers'),m=state.match;if(!root||!m)return;root.innerHTML='';const viewer=m.players[0],groups=new Map();for(const p of m.players){const a=groups.get(p.position)||[];a.push(p);groups.set(p.position,a)}for(const p of m.players){const xy=nodeXY(p.position,viewer),same=groups.get(p.position)||[p],slot=same.indexOf(p),c=PROD_CHARS.find(x=>x.id===p.character)||PROD_CHARS[0],el=document.createElement('div');el.className='boardPlayerToken'+(p.human?' human':'')+(m.activeId===p.id?' active':'');el.dataset.player=p.id;el.dataset.character=c.id;el.style.left=xy[0]+'%';el.style.top=xy[1]+'%';const off=[0,0];el.dataset.groupCount=String(same.length);el.style.setProperty('--ox',off[0]+'px');el.style.setProperty('--oy',off[1]+'px');el.style.setProperty('--player-color',({P1:'#943746',P2:'#285b91',P3:'#a98520',P4:'#287253'})[p.id]||'#245b49');el.setAttribute('aria-label',`${p.id} ${p.human?'あなた':c.name}`);el.title=`${p.id} ${p.human?'あなた':c.name}`;el.innerHTML=`<b>${p.id}</b><span>${p.human?'あなた':c.name}</span>`;root.appendChild(el)}layoutBoardPlayerTokens()} function renderBoard(){const board=$('#board'),me=state.match.players[0],m=state.match;renderField3D();board.innerHTML='';const all=[];centerIds().forEach(id=>all.push(id));['P1','P2','P3','P4'].forEach(pid=>['L','C','R'].forEach(l=>{all.push(shieldId(pid,l));for(let d=1;d<=7;d++)all.push(roadId(pid,l,d))}));['K:NW','K:NE','K:SW','K:SE'].forEach(x=>all.push(x));const moveLimit=normalMoveLimitForRoad(me.plan?.roadId),reach=m.phase==='plan'&&me.plan?.roadId&&moveLimit>0?reachable(me,moveLimit):new Map();renderRangeSurfaces(reach,moveLimit,me);all.forEach(id=>{const e=document.createElement('button');e.className='node '+posType(id);e.dataset.pos=id;const xy=nodeXY(id,me);e.style.left=xy[0]+'%';e.style.top=xy[1]+'%';if(activeNodes().has(id)&&id.startsWith('R:'))e.classList.add('active');const moveDistance=reach.get(id),pathIndex=me.plan?.path?.indexOf(id)??-1;if(reach.has(id)&&moveDistance<=moveLimit){e.classList.add('reachable');e.dataset.moveDistance=String(moveDistance);e.setAttribute('aria-label',`${id} 移動可能 ${moveDistance}歩`);e.title=`移動可能 ${moveDistance}歩`}if(id===me.position){e.classList.add('currentPosition');e.dataset.decisionLabel='現在';e.setAttribute('aria-label',`${id} 現在地`);e.title='現在地'}if(moveDistance===1){e.classList.add('nextStep');e.dataset.decisionLabel='次'}if(pathIndex>=0){e.classList.add('path','pathStep');e.dataset.pathIndex=String(pathIndex);e.dataset.decisionLabel=pathIndex===0?'現在':pathIndex===me.plan.path.length-1?'終点':`経路${pathIndex}`;e.setAttribute('aria-label',`${id} ${e.dataset.decisionLabel}`);e.title=e.dataset.decisionLabel;if(pathIndex===me.plan.path.length-1&&pathIndex>0)e.classList.add('pathEnd')}if(m.players.some(p=>p.position===id))e.classList.add('person');if(id.startsWith('R:')){const q=parseRoad(id),owner=m.players.find(p=>p.id===q.owner),lane=owner?.lanes?.[q.lane]||[];if(q.depth<=lane.length){const cardId=lane[q.depth-1],card=CARDS[cardId];e.classList.add('placedCard');e.dataset.cardId=cardId;e.title=`${owner?.name||q.owner} ${q.lane}列 ${q.depth}/${lane.length}: ${card?.display_name||cardId}`;const face=document.createElement('span');face.className='placedCardFace';face.textContent=`${suitGlyphForCard(card)}${Number(card?.power)||''}`;e.appendChild(face);if(q.depth===lane.length){const prog=document.createElement('span');prog.className='placedLaneProgress';prog.textContent=`${q.lane} ${lane.length}/7`;e.appendChild(prog)}}}const honey=m.honey?.byNode?.[id]||0;if(honey>0){e.classList.add('honeyNode');e.dataset.honey=honey>1?'×'+honey:'◆'}if(!e.classList.contains('reachable'))e.disabled=true;else e.onclick=ev=>selectEndpoint(nearestProjectedNode(ev.clientX,ev.clientY,true)||id);board.appendChild(e)});renderBoardPlayers();renderRouteLine()} function battleHandFaceMarkup(id){const c=CARDS[id]||{},info=deckSuitInfo(c),glyph=info[0],rank=String(c.rank??c.power??cardRank(id)??'?'),name=String(c.display_name||id),rawType=String(c.ability_type||'Vanilla'),type=rawType==='Vanilla'?'通常':rawType;return `<span class=\"handCardTop\"><span class=\"handCardSuit\" aria-hidden=\"true\">${glyph}</span><span class=\"handCardType\">${type}</span></span><strong class=\"handCardRank\">${rank}</strong><span class=\"handCardName\">${name}</span>`} function renderHand(){const me=state.match.players[0],h=$('#hand');h.innerHTML='';me.hand.forEach(id=>{const c=CARDS[id]||{},b=document.createElement('button'),info=deckSuitInfo(c),type=String(c.ability_type||'Vanilla')==='Vanilla'?'通常':String(c.ability_type||'Vanilla');b.className='handCard';b.innerHTML=battleHandFaceMarkup(id);b.disabled=state.match.phase!=='plan';b.dataset.cardId=id;b.dataset.suit=c.suit||'DCG';b.dataset.abilityType=c.ability_type||'Vanilla';b.title=`${cardLabel(id)}｜${info[0]} ${cardRank(id)}｜${type}`;h.appendChild(b)});const rs=$('#roadSelect'),bs=$('#battleSelect');rs.innerHTML='<option value=\"\">ロードカードを選択</option>';bs.innerHTML='<option value=\"\">バトルカードを選択</option>';me.hand.forEach(id=>{if(!roadSelectable(id))return;const o=document.createElement('option');o.value=id;o.textContent=cardLabel(id);rs.appendChild(o);const ob=document.createElement('option');ob.value=id;ob.textContent=cardLabel(id);bs.appendChild(ob)});if(me.plan?.roadId)rs.value=me.plan.roadId;if(me.plan?.battleId)bs.value=me.plan.battleId;syncDirectPlanRoleUi()} function updatePlanFromSelect(){const me=state.match.players[0],rs=$('#roadSelect'),bs=$('#battleSelect'),r=rs.value,b=bs.value;me.plan=me.plan||{roadId:null,battleId:null,path:[me.position]};if(r&&b&&r===b){toast('同じ札をロードとバトルへ同時予約できません');rs.value=me.plan.roadId||'';bs.value=me.plan.battleId||'';syncDirectPlanRoleUi();return false}if(r&&!roadSelectable(r)){toast('この札は現在ロード予約に使えません');rs.value=me.plan.roadId||'';return false}if(b&&!roadSelectable(b)){toast('この札は現在バトル予約に使えません');bs.value=me.plan.battleId||'';return false}if(r!==me.plan.roadId){me.plan.roadId=r||null;me.plan.path=[me.position]}me.plan.battleId=b||null;$('#endpointText').textContent=me.plan.path.at(-1);renderBoard();syncDirectPlanRoleUi();return true} $('#roadSelect').onchange=()="}],"screenChange":[],"renderBattle":[{"line":1588,"index":10039049,"text":");show('setup');renderSetupDeckStatus();return null}const n=playerCount(snapshot.setup.mode),ps=[],diagnosticCpuDeck=[...snapshot.deck.main];for(let i=0;i<n;i++)ps.push(makePlayer(i,i===0,i===0?[...snapshot.deck.main]:diagnosticCpuDeck));if(snapshot.setup.mode==='2v2'){ps[0].team='A';ps[1].team='A';ps[2].team='B';ps[3].team='B'}state.match={id:'M'+Date.now(),mode:snapshot.setup.mode,contentId:snapshot.setup.content,round:1,phase:'plan',players:ps,activeId:null,target:null,log:[],lastResult:null,lastBattleResolution:null,battlePresentation:null,resolutionSeq:0,projection:['N','E','S','W'],abilityRng:(Date.now()^0x9e3779b9)>>>0,busy:false,deckStartSnapshot:snapshot,honey:{byNode:{},receipts:new Set(),generatedRounds:new Set()},playerDeckRule:{id:snapshot.deck.ruleId,revision:snapshot.deck.ruleRevision,mainCount:snapshot.deck.main.length,exCount:snapshot.deck.ex.length}};state.match.selectionLock=grCreateSelectionLock(state.match,'P1');grBattleReplayBegin(state.match);awardRoundStartHoney(state.match);initRoundRuntime(state.match);log(snapshot.setup.content==='honey_hunt'?'第1巡。ハニーハント：一筆移動・終点ハニー全量取得・一列七枚勝利。人物を直接ドラッグして経路を秘密予約します。':'第1巡。全員が行動を予約します。');show('battle');setupBattleDetails();renderBattle();return state.match} $('#startMatch').addEventListener('click',startMatch); function log(t){if(!state.match)return;state.match.log.unshift(t);state.match.log=state.match.log.slice(0,30)} function refill(p){while(p.hand.length<3&&p.deck.length)p.hand.push(p.deck.shift())} function laneDepth(p,l){return p.lanes[l].length} function posType(id){if(id.startsWith('C:'))return'center';if(id.startsWith('S:'))return'shield';if(id.startsWith('R:'))return'road';return'corner'} function parseRoad(id){const [,owner,lane,depth]=id.split(':');return{owner,lane,depth:+depth}} function projectionFor(viewer){const ids=['P1','P2','P3','P4'];const vi=Math.max(0,ids.indexOf(viewer.id));const arms=['N','E','S','W'];const map={};ids.forEach((id,i)=>map[id]=arms[(i-vi+4)%4]);return map} function centerIds(){const a=[];for(let r=-1;r<=1;r++)for(let c=-1;c<=1;c++)a.push(`C:${r}:${c}`);return a} function shieldId(owner,lane){return`S:${owner}:${lane}`};function roadId(owner,lane,d){return`R:${owner}:${lane}:${d}`} function activeNodes(){const s=new Set(centerIds());['K:NW','K:NE','K:SW','K:SE'].forEach(id=>s.add(id));state.match.players.forEach(p=>{['L','C','R'].forEach(l=>{s.add(shieldId(p.id,l));for(let d=1;d<=p.lanes[l].length&&d<=7;d++)s.add(roadId(p.id,l,d))})});return s} function centerPort(owner,lane,viewer){const arm=projectionFor(viewer)[owner];const laneIx={L:-1,C:0,R:1}[lane];if(arm==='N')return`C:-1:${laneIx}`;if(arm==='S')return`C:1:${-laneIx}`;if(arm==='W')return`C:${-laneIx}:-1`;return`C:${laneIx}:1`} function neighbors(id,viewer){const act=activeNodes(),out=[],proj=projectionFor(viewer),ownerFor=arm=>state.match.players.find(p=>proj[p.id]===arm)?.id,cornerDefs={'K:NW':[['N','L'],['W','R']],'K:NE':[['N','R'],['E','L']],'K:SW':[['S','R'],['W','L']],'K:SE':[['S','L'],['E','R']]},cornerRoads=k=>(cornerDefs[k]||[]).map(([arm,lane])=>{const owner=ownerFor(arm);return owner?roadId(owner,lane,1):null}).filter(Boolean);if(id.startsWith('C:')){const [,rs,cs]=id.split(':'),r=+rs,c=+cs;[[r-1,c],[r+1,c],[r,c-1],[r,c+1]].forEach(([rr,cc])=>{const x=`C:${rr}:${cc}`;if(act.has(x))out.push(x)});state.match.players.forEach(p=>['L','C','R'].forEach(l=>{const sh=shieldId(p.id,l);if(centerPort(p.id,l,viewer)===id&&act.has(sh))out.push(sh)}))}else if(id.startsWith('K:')){cornerRoads(id).forEach(x=>{if(act.has(x))out.push(x)})}else if(id.startsWith('S:')){const [,owner,lane]=id.split(':');out.push(centerPort(owner,lane,viewer));const r1=roadId(owner,lane,1);if(act.has(r1))out.push(r1)}else if(id.startsWith('R:')){const {owner,lane,depth}=parseRoad(id);if(depth===1){out.push(shieldId(owner,lane));Object.keys(cornerDefs).forEach(k=>{if(cornerRoads(k).includes(id))out.push(k)})}const a=roadId(owner,lane,depth-1),b=roadId(owner,lane,depth+1);if(depth>1&&act.has(a))out.push(a);if(depth<7&&act.has(b))out.push(b)}return out.filter(x=>act.has(x))} function shortest(start,end,max,viewer){if(start===end)return[start];const q=[[start]],seen=new Set([start]);while(q.length){const p=q.shift();if(p.length-1>=max)continue;for(const n of neighbors(p[p.length-1],viewer)){if(seen.has(n))continue;const np=[...p,n];if(n===end)return np;seen.add(n);q.push(np)}}return null} function reachable(p,"},{"line":1816,"index":10107002,"text":"uction=(p.nextCostReduction||0)+2;log(`${p.name}：${c.display_name}で1枚引き、次の能力用マナを2軽減。`);break} case'blockBattlePlay':{chosen.forEach(x=>{x.player.battleBlockedRound=m.round;log(`${x.player.name}：この巡の新しいバトルカード提出を禁止。`) });break} case'playAdditionalBattleFromHand':{const id=chosen[0].cardId;removeHand(p,id);const e=makeParticipant(p,id,'ability_active_addition');added.push(e);log(`${p.name}：${cardLabel(id)}を追加バトルカードとして公開。`);break} case'returnOwnColumnCards':{const byLane={};chosen.forEach(x=>(byLane[x.lane]||(byLane[x.lane]=[])).push(x));Object.entries(byLane).forEach(([lane,arr])=>arr.sort((a,b)=>b.index-a.index).forEach(x=>{const got=p.lanes[lane].splice(x.index,1)[0];if(got!==x.cardId)throw Error('zekuu-instance-shift');p.hand.push(got)}));log(`${p.name}：${chosen.length}枚を列から手札へ戻した。`);break} case'zeroRandomMaximum':{const foes=currentOpponentParticipants(p),max=Math.max(...foes.map(e=>e.currentValue)),cand=foes.filter(e=>e.currentValue===max).sort((a,b)=>a.key.localeCompare(b.key)),pick=cand[matchRnd(cand.length)];pick.currentValue=0;log(`${p.name}：${cardLabel(pick.cardId)}の今回戦闘中の値を0にした。`);break} default:throw Error('unaccepted-effect:'+c.effect_type+':'+entry.cardId) } renderBattle();return added } async function resolveAbilityBundle(entries,phase){let addedBundleSeq=0;const queue=[sortAbilityEntries(entries.filter(e=>CARDS[e.cardId]?.effect_type!=='none'),`${phase}:root`)];while(queue.length){const bundle=queue.shift();if(!bundle.length)continue;const newly=[];for(const e of bundle)newly.push(...await resolveAbilityEntry(e,phase));const next=newly.filter(e=>CARDS[e.cardId]?.effect_type!=='none');if(next.length)queue.push(sortAbilityEntries(next,`${phase}:add:${++addedBundleSeq}`))}} async function commitPlans(){const m=state.match;if(m.busy)return false;const me=m.players[0];updatePlanFromSelect();if(!me.plan?.roadId||!me.plan?.battleId){toast('ロードカードとバトルカードを選択してください');return false}if(me.plan.roadId===me.plan.battleId){toast('別の札を選択してください');return false}const path=me.plan.path||[me.position];if(path.length-1>normalMoveLimitForRoad(me.plan.roadId)){toast('通常移動可能歩数を超えています');return false}for(let i=1;i<path.length;i++){if(!neighbors(path[i-1],me).includes(path[i])||path.slice(0,i).includes(path[i])){toast('経路が連続していません');return false}}const quickAcceptedAt=quickNow(m);m.busy=true;try{if(!battleMount)await updateBattleAvatar();const used=new Set([path.at(-1)]);m.players.slice(1).forEach(p=>p.plan=botPlan(p,used));m.players.forEach(p=>{if(!p.hand.includes(p.plan.roadId)||!p.hand.includes(p.plan.battleId))throw Error('reservation-not-owned')});quickAcceptPlan(m,me,quickAcceptedAt);window.GAMEROAD_PARTNER_COMMENT?.emit?.({eventId:`${m.id}:set:${m.round}:road`,kind:'dialogue',speechIntent:'road_set',cardId:me.plan.roadId,loggable:false,ttlMs:9000});window.GAMEROAD_PARTNER_COMMENT?.emit?.({eventId:`${m.id}:set:${m.round}:battle`,kind:'dialogue',speechIntent:'battle_set',cardId:me.plan.battleId,loggable:false,ttlMs:9000});m.lastBattleResolution=null;m.players.forEach(p=>removeHand(p,p.plan.roadId));m.activeId=pickActive(m.players).id;const roadEntries=[];m.players.forEach(p=>roadEntries.push(makeParticipant(p,p.plan.roadId,'road')));m.phase='reveal';log('ロードカードを公開。公開時能力を小さい印刷値順で解決します。');renderBattle();await resolveAbilityBundle(roadEntries,'road');if(m.contentId==='honey_hunt'&&!m.honey.generatedRounds.has(m.round)){spawnHoneyFromRoads();log('公開された1〜6と、列上の同数字ロードカード実体を照合してハニー発生源を更新しました。')}m.phase='move';renderBattle();const order=randomOrderByRoad(m.players);for(const p of order){await animateResolvedPath(p);log(`${p.name}：${cardLabel(p.plan.roadId)} / ${p.plan.path.length-1}歩`)}const act=m.players.find(p=>p.id===m.activeId);m.phase='target';log(`${act.name}が攻撃権を取得。`);m.busy=false;renderBattle();if(!act.human){autoTarget(act);await resolveBattle()}return true}catch(e){m.busy=false;log(`処理停止：${e.message}`);renderBattle();throw e}} globalThis.GAMEROAD_READY_PLAN_COMMIT=(...args)=>commitPlans(...args); function autoTarget(active){const others=legalOpponents(active);if(!others.length){state.match.target=null;throw Error('no-legal-target')}const target=others[rndInt(others.length)];const lane=['L','C','R'][rndInt(3)],shield=['L','C','R'][rndInt(3)];state.match.target={defenderId:target.id,lane,shield}} function renderTarget(){const box=$('#targetBox'),act=state.match.players.find(p=>p.id===state.match.activeId),on=state.match.phase==='"},{"line":1819,"index":10109049,"text":".length-1>normalMoveLimitForRoad(me.plan.roadId)){toast('通常移動可能歩数を超えています');return false}for(let i=1;i<path.length;i++){if(!neighbors(path[i-1],me).includes(path[i])||path.slice(0,i).includes(path[i])){toast('経路が連続していません');return false}}const quickAcceptedAt=quickNow(m);m.busy=true;try{if(!battleMount)await updateBattleAvatar();const used=new Set([path.at(-1)]);m.players.slice(1).forEach(p=>p.plan=botPlan(p,used));m.players.forEach(p=>{if(!p.hand.includes(p.plan.roadId)||!p.hand.includes(p.plan.battleId))throw Error('reservation-not-owned')});quickAcceptPlan(m,me,quickAcceptedAt);window.GAMEROAD_PARTNER_COMMENT?.emit?.({eventId:`${m.id}:set:${m.round}:road`,kind:'dialogue',speechIntent:'road_set',cardId:me.plan.roadId,loggable:false,ttlMs:9000});window.GAMEROAD_PARTNER_COMMENT?.emit?.({eventId:`${m.id}:set:${m.round}:battle`,kind:'dialogue',speechIntent:'battle_set',cardId:me.plan.battleId,loggable:false,ttlMs:9000});m.lastBattleResolution=null;m.players.forEach(p=>removeHand(p,p.plan.roadId));m.activeId=pickActive(m.players).id;const roadEntries=[];m.players.forEach(p=>roadEntries.push(makeParticipant(p,p.plan.roadId,'road')));m.phase='reveal';log('ロードカードを公開。公開時能力を小さい印刷値順で解決します。');renderBattle();await resolveAbilityBundle(roadEntries,'road');if(m.contentId==='honey_hunt'&&!m.honey.generatedRounds.has(m.round)){spawnHoneyFromRoads();log('公開された1〜6と、列上の同数字ロードカード実体を照合してハニー発生源を更新しました。')}m.phase='move';renderBattle();const order=randomOrderByRoad(m.players);for(const p of order){await animateResolvedPath(p);log(`${p.name}：${cardLabel(p.plan.roadId)} / ${p.plan.path.length-1}歩`)}const act=m.players.find(p=>p.id===m.activeId);m.phase='target';log(`${act.name}が攻撃権を取得。`);m.busy=false;renderBattle();if(!act.human){autoTarget(act);await resolveBattle()}return true}catch(e){m.busy=false;log(`処理停止：${e.message}`);renderBattle();throw e}} globalThis.GAMEROAD_READY_PLAN_COMMIT=(...args)=>commitPlans(...args); function autoTarget(active){const others=legalOpponents(active);if(!others.length){state.match.target=null;throw Error('no-legal-target')}const target=others[rndInt(others.length)];const lane=['L','C','R'][rndInt(3)],shield=['L','C','R'][rndInt(3)];state.match.target={defenderId:target.id,lane,shield}} function renderTarget(){const box=$('#targetBox'),act=state.match.players.find(p=>p.id===state.match.activeId),on=state.match.phase==='target'&&act?.human;box.classList.toggle('on',on);$('#battleMap').classList.toggle('targetMode',!!on);if(!on)return;const t=$('#targetPlayer');t.innerHTML='';legalOpponents(act).forEach(p=>{const o=document.createElement('option');o.value=p.id;o.textContent=p.name;t.appendChild(o)})} $('#confirmTarget').onclick=async()=>{const act=state.match.players.find(p=>p.id===state.match.activeId);if(!act?.human)return;const defenderId=$('#targetPlayer').value;if(!legalOpponents(act).some(p=>p.id===defenderId)){state.match.target=null;renderBattle();return}state.match.target={defenderId,lane:$('#targetLane').value,shield:$('#targetShield').value};try{await resolveBattle()}catch(e){console.error(e)}} function sideGroups(){if(state.match.mode!=='2v2')return state.match.players.map(p=>({id:p.id,players:[p]}));return['A','B'].map(t=>({id:t,players:state.match.players.filter(p=>p.team===t)}))} async function replacementBattleCard(p,reason){const opts=p.hand.filter(id=>CARDS[id]?.runtime_status==='playable').map(cardId=>({kind:'hand',cardId,id:cardId,detail:reason}));if(!opts.length)return null;const ch=await chooseAbilityItems('バトルカードを再確認',reason,opts,1,1,p.human);return ch[0]?.cardId||null} async function resolveBattle(){const m=state.match;if(m.busy)return;if(!m.target)throw Error('target-missing');const active=m.players.find(p=>p.id===m.activeId);if(!active||!legalOpponents(active).some(p=>p.id===m.target.defenderId))throw Error('illegal-target');m.busy=true;m.phase='resolve';const defender=m.players.find(p=>p.id===m.target.defenderId);m.battlePresentation={stage:'focus',round:m.round,mode:m.mode,attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,players:[]};renderBattle();try{if(!await setBattlePresentation('focus',{attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,players:[]}))return;if(active.human)await setBattleRuntimeState('attack');const usedShield=defender.shields[m.target.shield];if(!usedShield)throw Error('shield-missing');let replenish=defender"},{"line":1819,"index":10109269,"text":");return false}}const quickAcceptedAt=quickNow(m);m.busy=true;try{if(!battleMount)await updateBattleAvatar();const used=new Set([path.at(-1)]);m.players.slice(1).forEach(p=>p.plan=botPlan(p,used));m.players.forEach(p=>{if(!p.hand.includes(p.plan.roadId)||!p.hand.includes(p.plan.battleId))throw Error('reservation-not-owned')});quickAcceptPlan(m,me,quickAcceptedAt);window.GAMEROAD_PARTNER_COMMENT?.emit?.({eventId:`${m.id}:set:${m.round}:road`,kind:'dialogue',speechIntent:'road_set',cardId:me.plan.roadId,loggable:false,ttlMs:9000});window.GAMEROAD_PARTNER_COMMENT?.emit?.({eventId:`${m.id}:set:${m.round}:battle`,kind:'dialogue',speechIntent:'battle_set',cardId:me.plan.battleId,loggable:false,ttlMs:9000});m.lastBattleResolution=null;m.players.forEach(p=>removeHand(p,p.plan.roadId));m.activeId=pickActive(m.players).id;const roadEntries=[];m.players.forEach(p=>roadEntries.push(makeParticipant(p,p.plan.roadId,'road')));m.phase='reveal';log('ロードカードを公開。公開時能力を小さい印刷値順で解決します。');renderBattle();await resolveAbilityBundle(roadEntries,'road');if(m.contentId==='honey_hunt'&&!m.honey.generatedRounds.has(m.round)){spawnHoneyFromRoads();log('公開された1〜6と、列上の同数字ロードカード実体を照合してハニー発生源を更新しました。')}m.phase='move';renderBattle();const order=randomOrderByRoad(m.players);for(const p of order){await animateResolvedPath(p);log(`${p.name}：${cardLabel(p.plan.roadId)} / ${p.plan.path.length-1}歩`)}const act=m.players.find(p=>p.id===m.activeId);m.phase='target';log(`${act.name}が攻撃権を取得。`);m.busy=false;renderBattle();if(!act.human){autoTarget(act);await resolveBattle()}return true}catch(e){m.busy=false;log(`処理停止：${e.message}`);renderBattle();throw e}} globalThis.GAMEROAD_READY_PLAN_COMMIT=(...args)=>commitPlans(...args); function autoTarget(active){const others=legalOpponents(active);if(!others.length){state.match.target=null;throw Error('no-legal-target')}const target=others[rndInt(others.length)];const lane=['L','C','R'][rndInt(3)],shield=['L','C','R'][rndInt(3)];state.match.target={defenderId:target.id,lane,shield}} function renderTarget(){const box=$('#targetBox'),act=state.match.players.find(p=>p.id===state.match.activeId),on=state.match.phase==='target'&&act?.human;box.classList.toggle('on',on);$('#battleMap').classList.toggle('targetMode',!!on);if(!on)return;const t=$('#targetPlayer');t.innerHTML='';legalOpponents(act).forEach(p=>{const o=document.createElement('option');o.value=p.id;o.textContent=p.name;t.appendChild(o)})} $('#confirmTarget').onclick=async()=>{const act=state.match.players.find(p=>p.id===state.match.activeId);if(!act?.human)return;const defenderId=$('#targetPlayer').value;if(!legalOpponents(act).some(p=>p.id===defenderId)){state.match.target=null;renderBattle();return}state.match.target={defenderId,lane:$('#targetLane').value,shield:$('#targetShield').value};try{await resolveBattle()}catch(e){console.error(e)}} function sideGroups(){if(state.match.mode!=='2v2')return state.match.players.map(p=>({id:p.id,players:[p]}));return['A','B'].map(t=>({id:t,players:state.match.players.filter(p=>p.team===t)}))} async function replacementBattleCard(p,reason){const opts=p.hand.filter(id=>CARDS[id]?.runtime_status==='playable').map(cardId=>({kind:'hand',cardId,id:cardId,detail:reason}));if(!opts.length)return null;const ch=await chooseAbilityItems('バトルカードを再確認',reason,opts,1,1,p.human);return ch[0]?.cardId||null} async function resolveBattle(){const m=state.match;if(m.busy)return;if(!m.target)throw Error('target-missing');const active=m.players.find(p=>p.id===m.activeId);if(!active||!legalOpponents(active).some(p=>p.id===m.target.defenderId))throw Error('illegal-target');m.busy=true;m.phase='resolve';const defender=m.players.find(p=>p.id===m.target.defenderId);m.battlePresentation={stage:'focus',round:m.round,mode:m.mode,attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,players:[]};renderBattle();try{if(!await setBattlePresentation('focus',{attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,players:[]}))return;if(active.human)await setBattleRuntimeState('attack');const usedShield=defender.shields[m.target.shield];if(!usedShield)throw Error('shield-missing');let replenish=defender.plan.battleId;if(!defender.hand.includes(replenish)||CARDS[replenish]?.runtime_status!=='playable')replenish=await replacementBattleCard(defender,'使用シールドの同位置補充に使う札を1枚選びます。');if(!replenish)throw Error('shield-replenish-c"},{"line":1819,"index":10109552,"text":"leId))throw Error('reservation-not-owned')});quickAcceptPlan(m,me,quickAcceptedAt);window.GAMEROAD_PARTNER_COMMENT?.emit?.({eventId:`${m.id}:set:${m.round}:road`,kind:'dialogue',speechIntent:'road_set',cardId:me.plan.roadId,loggable:false,ttlMs:9000});window.GAMEROAD_PARTNER_COMMENT?.emit?.({eventId:`${m.id}:set:${m.round}:battle`,kind:'dialogue',speechIntent:'battle_set',cardId:me.plan.battleId,loggable:false,ttlMs:9000});m.lastBattleResolution=null;m.players.forEach(p=>removeHand(p,p.plan.roadId));m.activeId=pickActive(m.players).id;const roadEntries=[];m.players.forEach(p=>roadEntries.push(makeParticipant(p,p.plan.roadId,'road')));m.phase='reveal';log('ロードカードを公開。公開時能力を小さい印刷値順で解決します。');renderBattle();await resolveAbilityBundle(roadEntries,'road');if(m.contentId==='honey_hunt'&&!m.honey.generatedRounds.has(m.round)){spawnHoneyFromRoads();log('公開された1〜6と、列上の同数字ロードカード実体を照合してハニー発生源を更新しました。')}m.phase='move';renderBattle();const order=randomOrderByRoad(m.players);for(const p of order){await animateResolvedPath(p);log(`${p.name}：${cardLabel(p.plan.roadId)} / ${p.plan.path.length-1}歩`)}const act=m.players.find(p=>p.id===m.activeId);m.phase='target';log(`${act.name}が攻撃権を取得。`);m.busy=false;renderBattle();if(!act.human){autoTarget(act);await resolveBattle()}return true}catch(e){m.busy=false;log(`処理停止：${e.message}`);renderBattle();throw e}} globalThis.GAMEROAD_READY_PLAN_COMMIT=(...args)=>commitPlans(...args); function autoTarget(active){const others=legalOpponents(active);if(!others.length){state.match.target=null;throw Error('no-legal-target')}const target=others[rndInt(others.length)];const lane=['L','C','R'][rndInt(3)],shield=['L','C','R'][rndInt(3)];state.match.target={defenderId:target.id,lane,shield}} function renderTarget(){const box=$('#targetBox'),act=state.match.players.find(p=>p.id===state.match.activeId),on=state.match.phase==='target'&&act?.human;box.classList.toggle('on',on);$('#battleMap').classList.toggle('targetMode',!!on);if(!on)return;const t=$('#targetPlayer');t.innerHTML='';legalOpponents(act).forEach(p=>{const o=document.createElement('option');o.value=p.id;o.textContent=p.name;t.appendChild(o)})} $('#confirmTarget').onclick=async()=>{const act=state.match.players.find(p=>p.id===state.match.activeId);if(!act?.human)return;const defenderId=$('#targetPlayer').value;if(!legalOpponents(act).some(p=>p.id===defenderId)){state.match.target=null;renderBattle();return}state.match.target={defenderId,lane:$('#targetLane').value,shield:$('#targetShield').value};try{await resolveBattle()}catch(e){console.error(e)}} function sideGroups(){if(state.match.mode!=='2v2')return state.match.players.map(p=>({id:p.id,players:[p]}));return['A','B'].map(t=>({id:t,players:state.match.players.filter(p=>p.team===t)}))} async function replacementBattleCard(p,reason){const opts=p.hand.filter(id=>CARDS[id]?.runtime_status==='playable').map(cardId=>({kind:'hand',cardId,id:cardId,detail:reason}));if(!opts.length)return null;const ch=await chooseAbilityItems('バトルカードを再確認',reason,opts,1,1,p.human);return ch[0]?.cardId||null} async function resolveBattle(){const m=state.match;if(m.busy)return;if(!m.target)throw Error('target-missing');const active=m.players.find(p=>p.id===m.activeId);if(!active||!legalOpponents(active).some(p=>p.id===m.target.defenderId))throw Error('illegal-target');m.busy=true;m.phase='resolve';const defender=m.players.find(p=>p.id===m.target.defenderId);m.battlePresentation={stage:'focus',round:m.round,mode:m.mode,attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,players:[]};renderBattle();try{if(!await setBattlePresentation('focus',{attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,players:[]}))return;if(active.human)await setBattleRuntimeState('attack');const usedShield=defender.shields[m.target.shield];if(!usedShield)throw Error('shield-missing');let replenish=defender.plan.battleId;if(!defender.hand.includes(replenish)||CARDS[replenish]?.runtime_status!=='playable')replenish=await replacementBattleCard(defender,'使用シールドの同位置補充に使う札を1枚選びます。');if(!replenish)throw Error('shield-replenish-card-missing');removeHand(defender,replenish);defender.shields[m.target.shield]=replenish;const battleReveals=[];m.players.forEach(p=>{if(p.id===defender.id){battleReveals.push(makeParticipant(p,usedShield,'auto_defense'));return}});for(const p of m.players){if(p.id===defender.id)con"},{"line":1819,"index":10109679,"text":"ntId:`${m.id}:set:${m.round}:road`,kind:'dialogue',speechIntent:'road_set',cardId:me.plan.roadId,loggable:false,ttlMs:9000});window.GAMEROAD_PARTNER_COMMENT?.emit?.({eventId:`${m.id}:set:${m.round}:battle`,kind:'dialogue',speechIntent:'battle_set',cardId:me.plan.battleId,loggable:false,ttlMs:9000});m.lastBattleResolution=null;m.players.forEach(p=>removeHand(p,p.plan.roadId));m.activeId=pickActive(m.players).id;const roadEntries=[];m.players.forEach(p=>roadEntries.push(makeParticipant(p,p.plan.roadId,'road')));m.phase='reveal';log('ロードカードを公開。公開時能力を小さい印刷値順で解決します。');renderBattle();await resolveAbilityBundle(roadEntries,'road');if(m.contentId==='honey_hunt'&&!m.honey.generatedRounds.has(m.round)){spawnHoneyFromRoads();log('公開された1〜6と、列上の同数字ロードカード実体を照合してハニー発生源を更新しました。')}m.phase='move';renderBattle();const order=randomOrderByRoad(m.players);for(const p of order){await animateResolvedPath(p);log(`${p.name}：${cardLabel(p.plan.roadId)} / ${p.plan.path.length-1}歩`)}const act=m.players.find(p=>p.id===m.activeId);m.phase='target';log(`${act.name}が攻撃権を取得。`);m.busy=false;renderBattle();if(!act.human){autoTarget(act);await resolveBattle()}return true}catch(e){m.busy=false;log(`処理停止：${e.message}`);renderBattle();throw e}} globalThis.GAMEROAD_READY_PLAN_COMMIT=(...args)=>commitPlans(...args); function autoTarget(active){const others=legalOpponents(active);if(!others.length){state.match.target=null;throw Error('no-legal-target')}const target=others[rndInt(others.length)];const lane=['L','C','R'][rndInt(3)],shield=['L','C','R'][rndInt(3)];state.match.target={defenderId:target.id,lane,shield}} function renderTarget(){const box=$('#targetBox'),act=state.match.players.find(p=>p.id===state.match.activeId),on=state.match.phase==='target'&&act?.human;box.classList.toggle('on',on);$('#battleMap').classList.toggle('targetMode',!!on);if(!on)return;const t=$('#targetPlayer');t.innerHTML='';legalOpponents(act).forEach(p=>{const o=document.createElement('option');o.value=p.id;o.textContent=p.name;t.appendChild(o)})} $('#confirmTarget').onclick=async()=>{const act=state.match.players.find(p=>p.id===state.match.activeId);if(!act?.human)return;const defenderId=$('#targetPlayer').value;if(!legalOpponents(act).some(p=>p.id===defenderId)){state.match.target=null;renderBattle();return}state.match.target={defenderId,lane:$('#targetLane').value,shield:$('#targetShield').value};try{await resolveBattle()}catch(e){console.error(e)}} function sideGroups(){if(state.match.mode!=='2v2')return state.match.players.map(p=>({id:p.id,players:[p]}));return['A','B'].map(t=>({id:t,players:state.match.players.filter(p=>p.team===t)}))} async function replacementBattleCard(p,reason){const opts=p.hand.filter(id=>CARDS[id]?.runtime_status==='playable').map(cardId=>({kind:'hand',cardId,id:cardId,detail:reason}));if(!opts.length)return null;const ch=await chooseAbilityItems('バトルカードを再確認',reason,opts,1,1,p.human);return ch[0]?.cardId||null} async function resolveBattle(){const m=state.match;if(m.busy)return;if(!m.target)throw Error('target-missing');const active=m.players.find(p=>p.id===m.activeId);if(!active||!legalOpponents(active).some(p=>p.id===m.target.defenderId))throw Error('illegal-target');m.busy=true;m.phase='resolve';const defender=m.players.find(p=>p.id===m.target.defenderId);m.battlePresentation={stage:'focus',round:m.round,mode:m.mode,attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,players:[]};renderBattle();try{if(!await setBattlePresentation('focus',{attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,players:[]}))return;if(active.human)await setBattleRuntimeState('attack');const usedShield=defender.shields[m.target.shield];if(!usedShield)throw Error('shield-missing');let replenish=defender.plan.battleId;if(!defender.hand.includes(replenish)||CARDS[replenish]?.runtime_status!=='playable')replenish=await replacementBattleCard(defender,'使用シールドの同位置補充に使う札を1枚選びます。');if(!replenish)throw Error('shield-replenish-card-missing');removeHand(defender,replenish);defender.shields[m.target.shield]=replenish;const battleReveals=[];m.players.forEach(p=>{if(p.id===defender.id){battleReveals.push(makeParticipant(p,usedShield,'auto_defense'));return}});for(const p of m.players){if(p.id===defender.id)continue;if(p.battleBlockedRound===m.round){log(`${p.name}：フローズンにより予約バトルカードは手札に残る。`);continue}let battle=p.plan.battleId;if(!p.han"},{"line":1823,"index":10110744,"text":"y=false;renderBattle();if(!act.human){autoTarget(act);await resolveBattle()}return true}catch(e){m.busy=false;log(`処理停止：${e.message}`);renderBattle();throw e}} globalThis.GAMEROAD_READY_PLAN_COMMIT=(...args)=>commitPlans(...args); function autoTarget(active){const others=legalOpponents(active);if(!others.length){state.match.target=null;throw Error('no-legal-target')}const target=others[rndInt(others.length)];const lane=['L','C','R'][rndInt(3)],shield=['L','C','R'][rndInt(3)];state.match.target={defenderId:target.id,lane,shield}} function renderTarget(){const box=$('#targetBox'),act=state.match.players.find(p=>p.id===state.match.activeId),on=state.match.phase==='target'&&act?.human;box.classList.toggle('on',on);$('#battleMap').classList.toggle('targetMode',!!on);if(!on)return;const t=$('#targetPlayer');t.innerHTML='';legalOpponents(act).forEach(p=>{const o=document.createElement('option');o.value=p.id;o.textContent=p.name;t.appendChild(o)})} $('#confirmTarget').onclick=async()=>{const act=state.match.players.find(p=>p.id===state.match.activeId);if(!act?.human)return;const defenderId=$('#targetPlayer').value;if(!legalOpponents(act).some(p=>p.id===defenderId)){state.match.target=null;renderBattle();return}state.match.target={defenderId,lane:$('#targetLane').value,shield:$('#targetShield').value};try{await resolveBattle()}catch(e){console.error(e)}} function sideGroups(){if(state.match.mode!=='2v2')return state.match.players.map(p=>({id:p.id,players:[p]}));return['A','B'].map(t=>({id:t,players:state.match.players.filter(p=>p.team===t)}))} async function replacementBattleCard(p,reason){const opts=p.hand.filter(id=>CARDS[id]?.runtime_status==='playable').map(cardId=>({kind:'hand',cardId,id:cardId,detail:reason}));if(!opts.length)return null;const ch=await chooseAbilityItems('バトルカードを再確認',reason,opts,1,1,p.human);return ch[0]?.cardId||null} async function resolveBattle(){const m=state.match;if(m.busy)return;if(!m.target)throw Error('target-missing');const active=m.players.find(p=>p.id===m.activeId);if(!active||!legalOpponents(active).some(p=>p.id===m.target.defenderId))throw Error('illegal-target');m.busy=true;m.phase='resolve';const defender=m.players.find(p=>p.id===m.target.defenderId);m.battlePresentation={stage:'focus',round:m.round,mode:m.mode,attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,players:[]};renderBattle();try{if(!await setBattlePresentation('focus',{attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,players:[]}))return;if(active.human)await setBattleRuntimeState('attack');const usedShield=defender.shields[m.target.shield];if(!usedShield)throw Error('shield-missing');let replenish=defender.plan.battleId;if(!defender.hand.includes(replenish)||CARDS[replenish]?.runtime_status!=='playable')replenish=await replacementBattleCard(defender,'使用シールドの同位置補充に使う札を1枚選びます。');if(!replenish)throw Error('shield-replenish-card-missing');removeHand(defender,replenish);defender.shields[m.target.shield]=replenish;const battleReveals=[];m.players.forEach(p=>{if(p.id===defender.id){battleReveals.push(makeParticipant(p,usedShield,'auto_defense'));return}});for(const p of m.players){if(p.id===defender.id)continue;if(p.battleBlockedRound===m.round){log(`${p.name}：フローズンにより予約バトルカードは手札に残る。`);continue}let battle=p.plan.battleId;if(!p.hand.includes(battle)||CARDS[battle]?.runtime_status!=='playable')battle=await replacementBattleCard(p,'予約札が不成立です。戦闘直前の合法札を1枚選びます。');if(!battle){log(`${p.name}：提出できるバトルカードがない。`);continue}removeHand(p,battle);battleReveals.push(makeParticipant(p,battle,'active_submission'))}log('バトルカードと使用シールドを公開。公開時能力を小さい印刷値順で解決します。');const publicRevealPresentation={attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,players:battlePresentationPlayers(m)};if(!await setBattlePresentation('reveal',publicRevealPresentation))return;const publicPresentationInputs=buildPublicCardPresentationInputs({round:m.round,...publicRevealPresentation});validatePublicCardPresentationInputs(publicPresentationInputs);if(publicPresentationInputs.length&&!await setBattlePresentation('read',{...publicRevealPresentation,presentationEvents:publicPresentationInputs}))return;await resolveAbilityBundle(battleReveals,'battle');const scores=new Map();m.players.forEach(p=>scores.set(p.id,(m.participants.get(p.id)||[]).reduce((a,e)=>a+e.currentValue,0)));const comparis"},{"line":1826,"index":10111923,"text":"te.match.target=null;renderBattle();return}state.match.target={defenderId,lane:$('#targetLane').value,shield:$('#targetShield').value};try{await resolveBattle()}catch(e){console.error(e)}} function sideGroups(){if(state.match.mode!=='2v2')return state.match.players.map(p=>({id:p.id,players:[p]}));return['A','B'].map(t=>({id:t,players:state.match.players.filter(p=>p.team===t)}))} async function replacementBattleCard(p,reason){const opts=p.hand.filter(id=>CARDS[id]?.runtime_status==='playable').map(cardId=>({kind:'hand',cardId,id:cardId,detail:reason}));if(!opts.length)return null;const ch=await chooseAbilityItems('バトルカードを再確認',reason,opts,1,1,p.human);return ch[0]?.cardId||null} async function resolveBattle(){const m=state.match;if(m.busy)return;if(!m.target)throw Error('target-missing');const active=m.players.find(p=>p.id===m.activeId);if(!active||!legalOpponents(active).some(p=>p.id===m.target.defenderId))throw Error('illegal-target');m.busy=true;m.phase='resolve';const defender=m.players.find(p=>p.id===m.target.defenderId);m.battlePresentation={stage:'focus',round:m.round,mode:m.mode,attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,players:[]};renderBattle();try{if(!await setBattlePresentation('focus',{attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,players:[]}))return;if(active.human)await setBattleRuntimeState('attack');const usedShield=defender.shields[m.target.shield];if(!usedShield)throw Error('shield-missing');let replenish=defender.plan.battleId;if(!defender.hand.includes(replenish)||CARDS[replenish]?.runtime_status!=='playable')replenish=await replacementBattleCard(defender,'使用シールドの同位置補充に使う札を1枚選びます。');if(!replenish)throw Error('shield-replenish-card-missing');removeHand(defender,replenish);defender.shields[m.target.shield]=replenish;const battleReveals=[];m.players.forEach(p=>{if(p.id===defender.id){battleReveals.push(makeParticipant(p,usedShield,'auto_defense'));return}});for(const p of m.players){if(p.id===defender.id)continue;if(p.battleBlockedRound===m.round){log(`${p.name}：フローズンにより予約バトルカードは手札に残る。`);continue}let battle=p.plan.battleId;if(!p.hand.includes(battle)||CARDS[battle]?.runtime_status!=='playable')battle=await replacementBattleCard(p,'予約札が不成立です。戦闘直前の合法札を1枚選びます。');if(!battle){log(`${p.name}：提出できるバトルカードがない。`);continue}removeHand(p,battle);battleReveals.push(makeParticipant(p,battle,'active_submission'))}log('バトルカードと使用シールドを公開。公開時能力を小さい印刷値順で解決します。');const publicRevealPresentation={attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,players:battlePresentationPlayers(m)};if(!await setBattlePresentation('reveal',publicRevealPresentation))return;const publicPresentationInputs=buildPublicCardPresentationInputs({round:m.round,...publicRevealPresentation});validatePublicCardPresentationInputs(publicPresentationInputs);if(publicPresentationInputs.length&&!await setBattlePresentation('read',{...publicRevealPresentation,presentationEvents:publicPresentationInputs}))return;await resolveAbilityBundle(battleReveals,'battle');const scores=new Map();m.players.forEach(p=>scores.set(p.id,(m.participants.get(p.id)||[]).reduce((a,e)=>a+e.currentValue,0)));const comparisonPlayers=battlePresentationPlayers(m);if(!await setBattlePresentation('compare',{attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,players:comparisonPlayers,teamTotals:m.mode==='2v2'?{A:m.players.filter(p=>p.team==='A').reduce((n,p)=>n+scores.get(p.id),0),B:m.players.filter(p=>p.team==='B').reduce((n,p)=>n+scores.get(p.id),0)}:null}))return;let winningPlayerIds=[];if(m.mode==='2v2'){const totals={A:0,B:0};m.players.forEach(p=>totals[p.team]+=scores.get(p.id));const max=Math.max(totals.A,totals.B),ties=['A','B'].filter(t=>totals[t]===max);let team;if(ties.length===1)team=ties[0];else{const active=m.players.find(p=>p.id===m.activeId);team=ties.includes(active.team)?active.team:ties[matchRnd(ties.length)]}winningPlayerIds=m.players.filter(p=>p.team===team).map(p=>p.id);log(`戦闘：組${team}が勝利。`)}else{const max=Math.max(...m.players.map(p=>scores.get(p.id)));const ties=m.players.filter(p=>scores.get(p.id)===max);const active=m.players.find(p=>p.id===m.activeId);const winner=ties.length===1?ties[0]:(ties.some(p=>p.id===active.id)?active:ties[matchRnd(ties.length)]);winningPlayerIds=[winner.id];log(`戦闘：${winner.name}が勝利。`)}if(!await s"}],"moduleScript":[{"line":1301,"index":3148154,"text":"ART_SNAPSHOT_CONFLICT\"); if(!existingDeckMatchStartSnapshot)Object.defineProperty(globalThis,\"GAMEROAD_CREATE_DECK_MATCH_START_SNAPSHOT\",{value:createDeckMatchStartSnapshot,enumerable:false,configurable:false,writable:false}); </script> <script src=\"./board-facility-state-core.classic.js\"></script> <script type=\"module\"> import { mountBoardFacilityRuntime } from \"./board-facility-runtime-mount.mjs\"; await mountBoardFacilityRuntime(globalThis); </script> <script type=\"module\"> import { resolveScreenNavigation, createScreenNavigationRuntimeBridge, createScreenTransitionRuntimeAdapter } from \"./screen-navigation-core.mjs\"; import { createOrientationProjectionAdapter, ORIENTATION_PROJECTIONS } from \"./ui-state-feedback-core.mjs\"; const runtimeScreenNavigationBridge=createScreenNavigationRuntimeBridge(); const existingScreenNavigationBridge=globalThis.GAMEROAD_SCREEN_NAVIGATION; if(existingScreenNavigationBridge && (existingScreenNavigationBridge.resolve!==resolveScreenNavigation || existingScreenNavigationBridge.resolveBackTarget!==runtimeScreenNavigationBridge.resolveBackTarget))throw new Error(\"GAMEROAD_SCREEN_NAVIGATION is already occupied by an incompatible bridge\"); if(!existingScreenNavigationBridge)Object.defineProperty(globalThis,\"GAMEROAD_SCREEN_NAVIGATION\",{value:runtimeScreenNavigationBridge,enumerable:false,configurable:false,writable:false}); const navigationBridge=existingScreenNavigationBridge||runtimeScreenNavigationBridge; const readUiMode=()=>globalThis.GAMEROAD_READ_UI_MODE?.()||{reducedMotion:false,lowPerf:false}; const screenTransitionRuntime=createScreenTransitionRuntimeAdapter({getCurrentScreen:()=>globalThis.GAMEROAD_GET_CURRENT_SCREEN(),applyScreen:(target,meta)=>globalThis.GAMEROAD_APPLY_SCREEN_TRANSITION(target,meta),runVisualPhase:(phase,context)=>globalThis.GAMEROAD_RUN_SCREEN_TRANSITION_PHASE(phase,context),navigationBridge,reducedMotion:()=>readUiMode().reducedMo"},{"line":1305,"index":3148312,"text":"hStartSnapshot,enumerable:false,configurable:false,writable:false}); </script> <script src=\"./board-facility-state-core.classic.js\"></script> <script type=\"module\"> import { mountBoardFacilityRuntime } from \"./board-facility-runtime-mount.mjs\"; await mountBoardFacilityRuntime(globalThis); </script> <script type=\"module\"> import { resolveScreenNavigation, createScreenNavigationRuntimeBridge, createScreenTransitionRuntimeAdapter } from \"./screen-navigation-core.mjs\"; import { createOrientationProjectionAdapter, ORIENTATION_PROJECTIONS } from \"./ui-state-feedback-core.mjs\"; const runtimeScreenNavigationBridge=createScreenNavigationRuntimeBridge(); const existingScreenNavigationBridge=globalThis.GAMEROAD_SCREEN_NAVIGATION; if(existingScreenNavigationBridge && (existingScreenNavigationBridge.resolve!==resolveScreenNavigation || existingScreenNavigationBridge.resolveBackTarget!==runtimeScreenNavigationBridge.resolveBackTarget))throw new Error(\"GAMEROAD_SCREEN_NAVIGATION is already occupied by an incompatible bridge\"); if(!existingScreenNavigationBridge)Object.defineProperty(globalThis,\"GAMEROAD_SCREEN_NAVIGATION\",{value:runtimeScreenNavigationBridge,enumerable:false,configurable:false,writable:false}); const navigationBridge=existingScreenNavigationBridge||runtimeScreenNavigationBridge; const readUiMode=()=>globalThis.GAMEROAD_READ_UI_MODE?.()||{reducedMotion:false,lowPerf:false}; const screenTransitionRuntime=createScreenTransitionRuntimeAdapter({getCurrentScreen:()=>globalThis.GAMEROAD_GET_CURRENT_SCREEN(),applyScreen:(target,meta)=>globalThis.GAMEROAD_APPLY_SCREEN_TRANSITION(target,meta),runVisualPhase:(phase,context)=>globalThis.GAMEROAD_RUN_SCREEN_TRANSITION_PHASE(phase,context),navigationBridge,reducedMotion:()=>readUiMode().reducedMotion,lowPerf:()=>readUiMode().lowPerf}); const existingScreenTransitionRuntime=globalThis.GAMEROAD_SCREEN_TRANSITION; if(existingScreenTransitionRuntime && (t"}]}
