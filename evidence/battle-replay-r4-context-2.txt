SHA256=a56c65c17463fc5748da3c5350c0b02434946659760065f7e437e1a6b680fe88
CHARS=11795593

===== 'function startMatch' HITS=1 =====

--- HIT 1 OFFSET=9992163 RANGE=9986163:9998182 ---
n_count_changed'};if(lock.playerId==='P1'&&Number(m.playerDeckRule?.exCount)!==Number(lock.exCount))return{ok:false,reason:'rule_ex_count_changed'};if(Number(p.sourceDeckSize)!==Number(lock.mainCount))return{ok:false,reason:'deck_size_changed'};if(lock.sourceDeckDigest&&Array.isArray(p.sourceDeckIds)&&p.sourceDeckIds.length&&grDeckIdentityDigest(p.sourceDeckIds)!==lock.sourceDeckDigest)return{ok:false,reason:'deck_snapshot_changed'};return{ok:true,reason:'ok'}}
function playerCount(mode){return mode==='2p'?2:4}
const QUICK_WINDOW_MS=30000,QUICK_PERFECT_REMAIN_MS=20000,QUICK_GOOD_REMAIN_MS=15000,QUICK_PERFECT_POINTS=100,QUICK_GOOD_POINTS=10,BATTLE_POINTS_CHEST_TARGET=100000;
let quickBurstTimer=null;
function quickBaseNow(){return typeof performance!=='undefined'&&Number.isFinite(performance.now())?performance.now():Date.now()}
function quickNow(m){return Number.isFinite(m?._quickQaNowMs)?m._quickQaNowMs:quickBaseNow()}
function quickStartRound(m){const now=quickBaseNow();m.quickWindow={round:m.round,startedAt:now,deadlineAt:now+QUICK_WINDOW_MS};m.quickReceipts=new Set();m.quickPointsByPlayer=m.quickPointsByPlayer||{};m.quickAwards=m.quickAwards||[];delete m._quickQaNowMs;renderQuickTimer()}
function quickRemainingMs(m,at=quickNow(m)){if(!m?.quickWindow||m.quickWindow.round!==m.round)return 0;return Math.max(0,Math.min(QUICK_WINDOW_MS,m.quickWindow.deadlineAt-at))}
function quickTierForRemaining(remainingMs){if(remainingMs>=QUICK_PERFECT_REMAIN_MS)return{tier:'PERFECT',points:QUICK_PERFECT_POINTS};if(remainingMs>=QUICK_GOOD_REMAIN_MS)return{tier:'GOOD',points:QUICK_GOOD_POINTS};return{tier:'NONE',points:0}}
function quickShowAward(awards){const live=(awards||[]).filter(a=>a.points>0),box=$('#quickAwardBurst');if(!box||!live.length)return;const tier=live.every(a=>a.tier==='PERFECT')?'PERFECT!':'GOOD',points=live.reduce((n,a)=>n+a.points,0);box.querySelector('strong').textContent=tier;box.querySelector('span').textContent=`クイック +${points}（ロード +${live.find(a=>a.stage==='road')?.points||0} / バトル +${live.find(a=>a.stage==='battle')?.points||0}）`;box.classList.remove('on');box.classList.toggle('noMotion',!!(state.settings.reduceMotion||state.settings.lowPerf));void box.offsetWidth;box.classList.add('on');clearTimeout(quickBurstTimer);quickBurstTimer=setTimeout(()=>box.classList.remove('on'),state.settings.reduceMotion||state.settings.lowPerf?900:1050)}
function quickAcceptStage(m,p,stage,acceptedAt){const key=`${m.round}:${p.id}:${stage}`;if(m.quickReceipts?.has(key))return null;m.quickReceipts?.add(key);const remainingMs=quickRemainingMs(m,acceptedAt),tier=quickTierForRemaining(remainingMs),award={seq:m.quickAwards.length+1,round:m.round,playerId:p.id,stage,tier:tier.tier,points:tier.points,remainingMs:Math.round(remainingMs),acceptedAt};m.quickAwards.push(award);m.quickPointsByPlayer[p.id]=(m.quickPointsByPlayer[p.id]||0)+tier.points;return award}
function quickAcceptPlan(m,p,acceptedAt){const awards=['road','battle'].map(stage=>quickAcceptStage(m,p,stage,acceptedAt)).filter(Boolean);quickShowAward(awards);renderQuickTimer();return awards}
function renderQuickTimer(){const m=state.match,coil=$('#quickCoil'),num=$('#quickRemain'),btn=$('#readyPlan');if(!coil||!num||!btn)return;const visible=!!(m&&state.screen==='battle'&&m.phase==='plan'&&m.quickWindow?.round===m.round);coil.hidden=!visible;if(!visible)return;const rem=quickRemainingMs(m),ratio=rem/QUICK_WINDOW_MS,tier=quickTierForRemaining(rem);coil.style.setProperty('--quick-angle',`${Math.max(0,Math.min(360,ratio*360))}deg`);num.textContent=String(Math.ceil(rem/1000));coil.classList.toggle('good',tier.tier==='GOOD');coil.classList.toggle('late',tier.tier==='NONE'&&rem>0);coil.classList.toggle('expired',rem<=0);coil.classList.toggle('lowPerf',!!state.settings.lowPerf);btn.setAttribute('aria-label',`準備完了。クイック判定 残り${Math.ceil(rem/1000)}秒${tier.tier==='PERFECT'?'、PERFECT圏内':tier.tier==='GOOD'?'、GOOD圏内':''}`)}
function settleBattlePoints(m,rank){if(m.battlePointsSettlement)return m.battlePointsSettlement;const me=m.players.find(p=>p.human),quick=Math.max(0,Math.floor(Number(m.quickPointsByPlayer?.[me?.id])||0)),base=rank===1?3000:600,before=Math.max(0,Math.floor(Number(state.progression?.battlePoints)||0)),earned=base+quick,after=before+earned,awards=(m.quickAwards||[]).filter(a=>a.playerId===me?.id&&a.points>0);state.progression={battlePoints:after};m.battlePointsSettlement={base,quick,earned,before,after,chestTarget:BATTLE_POINTS_CHEST_TARGET,chestLinesBefore:Math.floor(before/BATTLE_POINTS_CHEST_TARGET),chestLinesAfter:Math.floor(after/BATTLE_POINTS_CHEST_TARGET),awards};return m.battlePointsSettlement}
function renderResultBattlePoints(r){const p=r?.points;if(!p)return;$('#resultPointsEarned')&&($('#resultPointsEarned').textContent=`+${p.earned.toLocaleString('ja-JP')}`);const road=p.awards?.filter(a=>a.stage==='road').reduce((n,a)=>n+a.points,0)||0,battle=p.awards?.filter(a=>a.stage==='battle').reduce((n,a)=>n+a.points,0)||0;$('#resultPointsBreakdown')&&($('#resultPointsBreakdown').textContent=`通常 +${p.base.toLocaleString('ja-JP')} / クイック +${p.quick.toLocaleString('ja-JP')}（ロード +${road} / バトル +${battle}）`);const remainder=p.after%BATTLE_POINTS_CHEST_TARGET,reached=p.chestLinesAfter>p.chestLinesBefore,fill=reached?100:(remainder/BATTLE_POINTS_CHEST_TARGET)*100;$('#resultPointsMeterFill')&&($('#resultPointsMeterFill').style.width=`${fill}%`);$('#resultPointsChest')&&($('#resultPointsChest').textContent=reached?`宝箱ライン到達 / 累計 ${p.after.toLocaleString('ja-JP')} BattlePoints`:`宝箱進捗 ${remainder.toLocaleString('ja-JP')} / ${BATTLE_POINTS_CHEST_TARGET.toLocaleString('ja-JP')}`)}
const quickTicker=setInterval(()=>{if(state.screen==='battle')renderQuickTimer()},100);window.addEventListener('pagehide',()=>clearInterval(quickTicker),{once:true});
function initRoundRuntime(m){m.participants=new Map(m.players.map(p=>[p.id,[]]));m.participantSeq=0;m.resolvedAbilityKeys=new Set();m.abilityOrderReceipts=new Map();m.testAutoChoices=false;quickStartRound(m)}

function startMatch(){audioResetForNewMatch();try{if(battleMount&&window.GameRoadThreeCharRuntime?.unmount)window.GameRoadThreeCharRuntime.unmount(battleMount)}catch(e){}battleMount=null;battleMountPromise=null;const battleRuntimeEl=$('#battleRuntime');if(battleRuntimeEl)battleRuntimeEl.replaceChildren();const valid=validateDeck(state.savedDeck,{forBattle:true});if(!valid.ok){toast('札組を修正: '+valid.errors[0]);show('setup');renderSetupDeckStatus();return null}const n=playerCount(state.setupMode),ps=[],diagnosticCpuDeck=[...state.savedDeck.main];for(let i=0;i<n;i++)ps.push(makePlayer(i,i===0,i===0?state.savedDeck.main:diagnosticCpuDeck));if(state.setupMode==='2v2'){ps[0].team='A';ps[1].team='A';ps[2].team='B';ps[3].team='B'}state.match={id:'M'+Date.now(),mode:state.setupMode,contentId:state.setupContent,round:1,phase:'plan',players:ps,activeId:null,target:null,log:[],lastResult:null,lastBattleResolution:null,battlePresentation:null,resolutionSeq:0,projection:['N','E','S','W'],abilityRng:(Date.now()^0x9e3779b9)>>>0,busy:false,honey:{byNode:{},receipts:new Set(),generatedRounds:new Set()},playerDeckRule:{id:DECK_RULE.id,revision:DECK_RULE.revision,mainCount:state.savedDeck.main.length,exCount:state.savedDeck.ex.length}};state.match.selectionLock=grCreateSelectionLock(state.match,'P1');initRoundRuntime(state.match);log(state.setupContent==='honey_hunt'?'第1巡。ハニーハント：一筆移動・終点ハニー全量取得・一列七枚勝利。人物を直接ドラッグして経路を秘密予約します。':'第1巡。全員が行動を予約します。');show('battle');setupBattleDetails();renderBattle();return state.match}
$('#startMatch').addEventListener('click',startMatch);
function log(t){if(!state.match)return;state.match.log.unshift(t);state.match.log=state.match.log.slice(0,30)}
function refill(p){while(p.hand.length<3&&p.deck.length)p.hand.push(p.deck.shift())}
function laneDepth(p,l){return p.lanes[l].length}
function posType(id){if(id.startsWith('C:'))return'center';if(id.startsWith('S:'))return'shield';if(id.startsWith('R:'))return'road';return'corner'}
function parseRoad(id){const [,owner,lane,depth]=id.split(':');return{owner,lane,depth:+depth}}
function projectionFor(viewer){const ids=['P1','P2','P3','P4'];const vi=Math.max(0,ids.indexOf(viewer.id));const arms=['N','E','S','W'];const map={};ids.forEach((id,i)=>map[id]=arms[(i-vi+4)%4]);return map}
function centerIds(){const a=[];for(let r=-1;r<=1;r++)for(let c=-1;c<=1;c++)a.push(`C:${r}:${c}`);return a}
function shieldId(owner,lane){return`S:${owner}:${lane}`};function roadId(owner,lane,d){return`R:${owner}:${lane}:${d}`}
function activeNodes(){const s=new Set(centerIds());['K:NW','K:NE','K:SW','K:SE'].forEach(id=>s.add(id));state.match.players.forEach(p=>{['L','C','R'].forEach(l=>{s.add(shieldId(p.id,l));for(let d=1;d<=p.lanes[l].length&&d<=7;d++)s.add(roadId(p.id,l,d))})});return s}
function centerPort(owner,lane,viewer){const arm=projectionFor(viewer)[owner];const laneIx={L:-1,C:0,R:1}[lane];if(arm==='N')return`C:-1:${laneIx}`;if(arm==='S')return`C:1:${-laneIx}`;if(arm==='W')return`C:${-laneIx}:-1`;return`C:${laneIx}:1`}
function neighbors(id,viewer){const act=activeNodes(),out=[],proj=projectionFor(viewer),ownerFor=arm=>state.match.players.find(p=>proj[p.id]===arm)?.id,cornerDefs={'K:NW':[['N','L'],['W','R']],'K:NE':[['N','R'],['E','L']],'K:SW':[['S','R'],['W','L']],'K:SE':[['S','L'],['E','R']]},cornerRoads=k=>(cornerDefs[k]||[]).map(([arm,lane])=>{const owner=ownerFor(arm);return owner?roadId(owner,lane,1):null}).filter(Boolean);if(id.startsWith('C:')){const [,rs,cs]=id.split(':'),r=+rs,c=+cs;[[r-1,c],[r+1,c],[r,c-1],[r,c+1]].forEach(([rr,cc])=>{const x=`C:${rr}:${cc}`;if(act.has(x))out.push(x)});state.match.players.forEach(p=>['L','C','R'].forEach(l=>{const sh=shieldId(p.id,l);if(centerPort(p.id,l,viewer)===id&&act.has(sh))out.push(sh)}))}else if(id.startsWith('K:')){cornerRoads(id).forEach(x=>{if(act.has(x))out.push(x)})}else if(id.startsWith('S:')){const [,owner,lane]=id.split(':');out.push(centerPort(owner,lane,viewer));const r1=roadId(owner,lane,1);if(act.has(r1))out.push(r1)}else if(id.startsWith('R:')){const {owner,lane,depth}=parseRoad(id);if(depth===1){out.push(shieldId(owner,lane));Object.keys(cornerDefs).forEach(k=>{if(cornerRoads(k).includes(id))out.push(k)})}const a=roadId(owner,lane,depth-1),b=roadId(owner,lane,depth+1);if(depth>1&&act.has(a))out.push(a);if(depth<7&&act.has(b))out.push(b)}return out.filter(x=>act.has(x))}
function shortest(start,end,max,viewer){if(start===end)return[start];const q=[[start]],seen=new Set([start]);while(q.length){const p=q.shift();if(p.length-1>=max)continue;for(const n of neighbors(p[p.length-1],viewer)){if(seen.has(n))continue;const np=[...p,n];if(n===end)return np;seen.add(n);q.push(np)}}return null}
function reachable(p,steps){const q=[[p.position,0]],dist=new Map([[p.position,0]]);while(q.length){const [x,d]=q.shift();if(d>=steps)continue;for(const n of neighbors(x,p)){if(!dist.has(n)){dist.set(n,d+1);q.push([n,d+1])}}}return dist}
/* DOPPELGANGER-NORMAL-RULE-RUNTIME-001 R1
   Recovered user canon (2026-07-26 / 2026-08-06):
   - whole field; road/battle cards only; shields join only after becoming battle cards
   - activates when two or more cards share the same printed number
   - same-number reduction includes the Doppelganger card itself
   - continuous current value affects road movement/order/attack-right/battle value
   - zero does not destroy the card; no recursive modifier loop
   Unknowns remain fail-closed: formal card id/suit/rank, same-name self counting,
   negative values, modifier ordering beyond the recovered examples, hidden-card edge cases. */
const GR_DOPPEL_EFFECT='doppelgangerContinuous';
function grDoppelIs(cardId){return CARDS[cardId]?.effect_type===GR_DOPPEL_EFFECT}
function grDoppelEligibleParticipant(e){return !!e&&['road','active_submission','auto_defense','ability_active_addition'].includes(String(e.origin||''))}
function grDoppelFieldSnapshot(m=state.match){
 const out=[],seen=new Set(); if(!m)return out;
 const add=(key,ownerId,cardId,zone,publicNow=true)=>{const c=CARDS[cardId];if(!c||!p
--- END HIT 1 ---

===== 'async function startMatch' HITS=0 =====

===== 'state.match={' HITS=1 =====

--- HIT 1 OFFSET=9992894 RANGE=9986894:9998907 ---
){return typeof performance!=='undefined'&&Number.isFinite(performance.now())?performance.now():Date.now()}
function quickNow(m){return Number.isFinite(m?._quickQaNowMs)?m._quickQaNowMs:quickBaseNow()}
function quickStartRound(m){const now=quickBaseNow();m.quickWindow={round:m.round,startedAt:now,deadlineAt:now+QUICK_WINDOW_MS};m.quickReceipts=new Set();m.quickPointsByPlayer=m.quickPointsByPlayer||{};m.quickAwards=m.quickAwards||[];delete m._quickQaNowMs;renderQuickTimer()}
function quickRemainingMs(m,at=quickNow(m)){if(!m?.quickWindow||m.quickWindow.round!==m.round)return 0;return Math.max(0,Math.min(QUICK_WINDOW_MS,m.quickWindow.deadlineAt-at))}
function quickTierForRemaining(remainingMs){if(remainingMs>=QUICK_PERFECT_REMAIN_MS)return{tier:'PERFECT',points:QUICK_PERFECT_POINTS};if(remainingMs>=QUICK_GOOD_REMAIN_MS)return{tier:'GOOD',points:QUICK_GOOD_POINTS};return{tier:'NONE',points:0}}
function quickShowAward(awards){const live=(awards||[]).filter(a=>a.points>0),box=$('#quickAwardBurst');if(!box||!live.length)return;const tier=live.every(a=>a.tier==='PERFECT')?'PERFECT!':'GOOD',points=live.reduce((n,a)=>n+a.points,0);box.querySelector('strong').textContent=tier;box.querySelector('span').textContent=`クイック +${points}（ロード +${live.find(a=>a.stage==='road')?.points||0} / バトル +${live.find(a=>a.stage==='battle')?.points||0}）`;box.classList.remove('on');box.classList.toggle('noMotion',!!(state.settings.reduceMotion||state.settings.lowPerf));void box.offsetWidth;box.classList.add('on');clearTimeout(quickBurstTimer);quickBurstTimer=setTimeout(()=>box.classList.remove('on'),state.settings.reduceMotion||state.settings.lowPerf?900:1050)}
function quickAcceptStage(m,p,stage,acceptedAt){const key=`${m.round}:${p.id}:${stage}`;if(m.quickReceipts?.has(key))return null;m.quickReceipts?.add(key);const remainingMs=quickRemainingMs(m,acceptedAt),tier=quickTierForRemaining(remainingMs),award={seq:m.quickAwards.length+1,round:m.round,playerId:p.id,stage,tier:tier.tier,points:tier.points,remainingMs:Math.round(remainingMs),acceptedAt};m.quickAwards.push(award);m.quickPointsByPlayer[p.id]=(m.quickPointsByPlayer[p.id]||0)+tier.points;return award}
function quickAcceptPlan(m,p,acceptedAt){const awards=['road','battle'].map(stage=>quickAcceptStage(m,p,stage,acceptedAt)).filter(Boolean);quickShowAward(awards);renderQuickTimer();return awards}
function renderQuickTimer(){const m=state.match,coil=$('#quickCoil'),num=$('#quickRemain'),btn=$('#readyPlan');if(!coil||!num||!btn)return;const visible=!!(m&&state.screen==='battle'&&m.phase==='plan'&&m.quickWindow?.round===m.round);coil.hidden=!visible;if(!visible)return;const rem=quickRemainingMs(m),ratio=rem/QUICK_WINDOW_MS,tier=quickTierForRemaining(rem);coil.style.setProperty('--quick-angle',`${Math.max(0,Math.min(360,ratio*360))}deg`);num.textContent=String(Math.ceil(rem/1000));coil.classList.toggle('good',tier.tier==='GOOD');coil.classList.toggle('late',tier.tier==='NONE'&&rem>0);coil.classList.toggle('expired',rem<=0);coil.classList.toggle('lowPerf',!!state.settings.lowPerf);btn.setAttribute('aria-label',`準備完了。クイック判定 残り${Math.ceil(rem/1000)}秒${tier.tier==='PERFECT'?'、PERFECT圏内':tier.tier==='GOOD'?'、GOOD圏内':''}`)}
function settleBattlePoints(m,rank){if(m.battlePointsSettlement)return m.battlePointsSettlement;const me=m.players.find(p=>p.human),quick=Math.max(0,Math.floor(Number(m.quickPointsByPlayer?.[me?.id])||0)),base=rank===1?3000:600,before=Math.max(0,Math.floor(Number(state.progression?.battlePoints)||0)),earned=base+quick,after=before+earned,awards=(m.quickAwards||[]).filter(a=>a.playerId===me?.id&&a.points>0);state.progression={battlePoints:after};m.battlePointsSettlement={base,quick,earned,before,after,chestTarget:BATTLE_POINTS_CHEST_TARGET,chestLinesBefore:Math.floor(before/BATTLE_POINTS_CHEST_TARGET),chestLinesAfter:Math.floor(after/BATTLE_POINTS_CHEST_TARGET),awards};return m.battlePointsSettlement}
function renderResultBattlePoints(r){const p=r?.points;if(!p)return;$('#resultPointsEarned')&&($('#resultPointsEarned').textContent=`+${p.earned.toLocaleString('ja-JP')}`);const road=p.awards?.filter(a=>a.stage==='road').reduce((n,a)=>n+a.points,0)||0,battle=p.awards?.filter(a=>a.stage==='battle').reduce((n,a)=>n+a.points,0)||0;$('#resultPointsBreakdown')&&($('#resultPointsBreakdown').textContent=`通常 +${p.base.toLocaleString('ja-JP')} / クイック +${p.quick.toLocaleString('ja-JP')}（ロード +${road} / バトル +${battle}）`);const remainder=p.after%BATTLE_POINTS_CHEST_TARGET,reached=p.chestLinesAfter>p.chestLinesBefore,fill=reached?100:(remainder/BATTLE_POINTS_CHEST_TARGET)*100;$('#resultPointsMeterFill')&&($('#resultPointsMeterFill').style.width=`${fill}%`);$('#resultPointsChest')&&($('#resultPointsChest').textContent=reached?`宝箱ライン到達 / 累計 ${p.after.toLocaleString('ja-JP')} BattlePoints`:`宝箱進捗 ${remainder.toLocaleString('ja-JP')} / ${BATTLE_POINTS_CHEST_TARGET.toLocaleString('ja-JP')}`)}
const quickTicker=setInterval(()=>{if(state.screen==='battle')renderQuickTimer()},100);window.addEventListener('pagehide',()=>clearInterval(quickTicker),{once:true});
function initRoundRuntime(m){m.participants=new Map(m.players.map(p=>[p.id,[]]));m.participantSeq=0;m.resolvedAbilityKeys=new Set();m.abilityOrderReceipts=new Map();m.testAutoChoices=false;quickStartRound(m)}

function startMatch(){audioResetForNewMatch();try{if(battleMount&&window.GameRoadThreeCharRuntime?.unmount)window.GameRoadThreeCharRuntime.unmount(battleMount)}catch(e){}battleMount=null;battleMountPromise=null;const battleRuntimeEl=$('#battleRuntime');if(battleRuntimeEl)battleRuntimeEl.replaceChildren();const valid=validateDeck(state.savedDeck,{forBattle:true});if(!valid.ok){toast('札組を修正: '+valid.errors[0]);show('setup');renderSetupDeckStatus();return null}const n=playerCount(state.setupMode),ps=[],diagnosticCpuDeck=[...state.savedDeck.main];for(let i=0;i<n;i++)ps.push(makePlayer(i,i===0,i===0?state.savedDeck.main:diagnosticCpuDeck));if(state.setupMode==='2v2'){ps[0].team='A';ps[1].team='A';ps[2].team='B';ps[3].team='B'}state.match={id:'M'+Date.now(),mode:state.setupMode,contentId:state.setupContent,round:1,phase:'plan',players:ps,activeId:null,target:null,log:[],lastResult:null,lastBattleResolution:null,battlePresentation:null,resolutionSeq:0,projection:['N','E','S','W'],abilityRng:(Date.now()^0x9e3779b9)>>>0,busy:false,honey:{byNode:{},receipts:new Set(),generatedRounds:new Set()},playerDeckRule:{id:DECK_RULE.id,revision:DECK_RULE.revision,mainCount:state.savedDeck.main.length,exCount:state.savedDeck.ex.length}};state.match.selectionLock=grCreateSelectionLock(state.match,'P1');initRoundRuntime(state.match);log(state.setupContent==='honey_hunt'?'第1巡。ハニーハント：一筆移動・終点ハニー全量取得・一列七枚勝利。人物を直接ドラッグして経路を秘密予約します。':'第1巡。全員が行動を予約します。');show('battle');setupBattleDetails();renderBattle();return state.match}
$('#startMatch').addEventListener('click',startMatch);
function log(t){if(!state.match)return;state.match.log.unshift(t);state.match.log=state.match.log.slice(0,30)}
function refill(p){while(p.hand.length<3&&p.deck.length)p.hand.push(p.deck.shift())}
function laneDepth(p,l){return p.lanes[l].length}
function posType(id){if(id.startsWith('C:'))return'center';if(id.startsWith('S:'))return'shield';if(id.startsWith('R:'))return'road';return'corner'}
function parseRoad(id){const [,owner,lane,depth]=id.split(':');return{owner,lane,depth:+depth}}
function projectionFor(viewer){const ids=['P1','P2','P3','P4'];const vi=Math.max(0,ids.indexOf(viewer.id));const arms=['N','E','S','W'];const map={};ids.forEach((id,i)=>map[id]=arms[(i-vi+4)%4]);return map}
function centerIds(){const a=[];for(let r=-1;r<=1;r++)for(let c=-1;c<=1;c++)a.push(`C:${r}:${c}`);return a}
function shieldId(owner,lane){return`S:${owner}:${lane}`};function roadId(owner,lane,d){return`R:${owner}:${lane}:${d}`}
function activeNodes(){const s=new Set(centerIds());['K:NW','K:NE','K:SW','K:SE'].forEach(id=>s.add(id));state.match.players.forEach(p=>{['L','C','R'].forEach(l=>{s.add(shieldId(p.id,l));for(let d=1;d<=p.lanes[l].length&&d<=7;d++)s.add(roadId(p.id,l,d))})});return s}
function centerPort(owner,lane,viewer){const arm=projectionFor(viewer)[owner];const laneIx={L:-1,C:0,R:1}[lane];if(arm==='N')return`C:-1:${laneIx}`;if(arm==='S')return`C:1:${-laneIx}`;if(arm==='W')return`C:${-laneIx}:-1`;return`C:${laneIx}:1`}
function neighbors(id,viewer){const act=activeNodes(),out=[],proj=projectionFor(viewer),ownerFor=arm=>state.match.players.find(p=>proj[p.id]===arm)?.id,cornerDefs={'K:NW':[['N','L'],['W','R']],'K:NE':[['N','R'],['E','L']],'K:SW':[['S','R'],['W','L']],'K:SE':[['S','L'],['E','R']]},cornerRoads=k=>(cornerDefs[k]||[]).map(([arm,lane])=>{const owner=ownerFor(arm);return owner?roadId(owner,lane,1):null}).filter(Boolean);if(id.startsWith('C:')){const [,rs,cs]=id.split(':'),r=+rs,c=+cs;[[r-1,c],[r+1,c],[r,c-1],[r,c+1]].forEach(([rr,cc])=>{const x=`C:${rr}:${cc}`;if(act.has(x))out.push(x)});state.match.players.forEach(p=>['L','C','R'].forEach(l=>{const sh=shieldId(p.id,l);if(centerPort(p.id,l,viewer)===id&&act.has(sh))out.push(sh)}))}else if(id.startsWith('K:')){cornerRoads(id).forEach(x=>{if(act.has(x))out.push(x)})}else if(id.startsWith('S:')){const [,owner,lane]=id.split(':');out.push(centerPort(owner,lane,viewer));const r1=roadId(owner,lane,1);if(act.has(r1))out.push(r1)}else if(id.startsWith('R:')){const {owner,lane,depth}=parseRoad(id);if(depth===1){out.push(shieldId(owner,lane));Object.keys(cornerDefs).forEach(k=>{if(cornerRoads(k).includes(id))out.push(k)})}const a=roadId(owner,lane,depth-1),b=roadId(owner,lane,depth+1);if(depth>1&&act.has(a))out.push(a);if(depth<7&&act.has(b))out.push(b)}return out.filter(x=>act.has(x))}
function shortest(start,end,max,viewer){if(start===end)return[start];const q=[[start]],seen=new Set([start]);while(q.length){const p=q.shift();if(p.length-1>=max)continue;for(const n of neighbors(p[p.length-1],viewer)){if(seen.has(n))continue;const np=[...p,n];if(n===end)return np;seen.add(n);q.push(np)}}return null}
function reachable(p,steps){const q=[[p.position,0]],dist=new Map([[p.position,0]]);while(q.length){const [x,d]=q.shift();if(d>=steps)continue;for(const n of neighbors(x,p)){if(!dist.has(n)){dist.set(n,d+1);q.push([n,d+1])}}}return dist}
/* DOPPELGANGER-NORMAL-RULE-RUNTIME-001 R1
   Recovered user canon (2026-07-26 / 2026-08-06):
   - whole field; road/battle cards only; shields join only after becoming battle cards
   - activates when two or more cards share the same printed number
   - same-number reduction includes the Doppelganger card itself
   - continuous current value affects road movement/order/attack-right/battle value
   - zero does not destroy the card; no recursive modifier loop
   Unknowns remain fail-closed: formal card id/suit/rank, same-name self counting,
   negative values, modifier ordering beyond the recovered examples, hidden-card edge cases. */
const GR_DOPPEL_EFFECT='doppelgangerContinuous';
function grDoppelIs(cardId){return CARDS[cardId]?.effect_type===GR_DOPPEL_EFFECT}
function grDoppelEligibleParticipant(e){return !!e&&['road','active_submission','auto_defense','ability_active_addition'].includes(String(e.origin||''))}
function grDoppelFieldSnapshot(m=state.match){
 const out=[],seen=new Set(); if(!m)return out;
 const add=(key,ownerId,cardId,zone,publicNow=true)=>{const c=CARDS[cardId];if(!c||!publicNow||seen.has(key))return;seen.add(key);out.push(Object.freeze({key,ownerId,cardId,zone,printedValue:Number(c.power),name:String(c.display_name||cardId)}))};
 m.players.forEach(p=>['L','C','R'].forEach(l=>(p.lanes?.[l]||[]).forEach((id,i)=>add(`lane:${p.id}:${l}:${i}:${id}`,p.id,id,'lane',true))));
 if(m.participants)for(const e of participantEntries())if(grDoppelEligibleParticipant(e))add(`participant:${e.key}`,e.ownerId,e.cardId,e.origin,true);
 return out;
}
function grDoppelCensus(cardId,selfKey,snapshot=grDoppelFieldSnapshot(),matchBasis){
 const c=CARDS[cardId],base=Number(c?.power); if(!c||!Number.isFinite(base))return{ok:false,reason:'CARD_NOT_FOUND'};
 if(matchBasis!=='printed')return{ok:false,reason:'
--- END HIT 1 ---

===== 'lastBattleResolution' HITS=6 =====

--- HIT 1 OFFSET=9993056 RANGE=9987056:9999076 ---
aNowMs)?m._quickQaNowMs:quickBaseNow()}
function quickStartRound(m){const now=quickBaseNow();m.quickWindow={round:m.round,startedAt:now,deadlineAt:now+QUICK_WINDOW_MS};m.quickReceipts=new Set();m.quickPointsByPlayer=m.quickPointsByPlayer||{};m.quickAwards=m.quickAwards||[];delete m._quickQaNowMs;renderQuickTimer()}
function quickRemainingMs(m,at=quickNow(m)){if(!m?.quickWindow||m.quickWindow.round!==m.round)return 0;return Math.max(0,Math.min(QUICK_WINDOW_MS,m.quickWindow.deadlineAt-at))}
function quickTierForRemaining(remainingMs){if(remainingMs>=QUICK_PERFECT_REMAIN_MS)return{tier:'PERFECT',points:QUICK_PERFECT_POINTS};if(remainingMs>=QUICK_GOOD_REMAIN_MS)return{tier:'GOOD',points:QUICK_GOOD_POINTS};return{tier:'NONE',points:0}}
function quickShowAward(awards){const live=(awards||[]).filter(a=>a.points>0),box=$('#quickAwardBurst');if(!box||!live.length)return;const tier=live.every(a=>a.tier==='PERFECT')?'PERFECT!':'GOOD',points=live.reduce((n,a)=>n+a.points,0);box.querySelector('strong').textContent=tier;box.querySelector('span').textContent=`クイック +${points}（ロード +${live.find(a=>a.stage==='road')?.points||0} / バトル +${live.find(a=>a.stage==='battle')?.points||0}）`;box.classList.remove('on');box.classList.toggle('noMotion',!!(state.settings.reduceMotion||state.settings.lowPerf));void box.offsetWidth;box.classList.add('on');clearTimeout(quickBurstTimer);quickBurstTimer=setTimeout(()=>box.classList.remove('on'),state.settings.reduceMotion||state.settings.lowPerf?900:1050)}
function quickAcceptStage(m,p,stage,acceptedAt){const key=`${m.round}:${p.id}:${stage}`;if(m.quickReceipts?.has(key))return null;m.quickReceipts?.add(key);const remainingMs=quickRemainingMs(m,acceptedAt),tier=quickTierForRemaining(remainingMs),award={seq:m.quickAwards.length+1,round:m.round,playerId:p.id,stage,tier:tier.tier,points:tier.points,remainingMs:Math.round(remainingMs),acceptedAt};m.quickAwards.push(award);m.quickPointsByPlayer[p.id]=(m.quickPointsByPlayer[p.id]||0)+tier.points;return award}
function quickAcceptPlan(m,p,acceptedAt){const awards=['road','battle'].map(stage=>quickAcceptStage(m,p,stage,acceptedAt)).filter(Boolean);quickShowAward(awards);renderQuickTimer();return awards}
function renderQuickTimer(){const m=state.match,coil=$('#quickCoil'),num=$('#quickRemain'),btn=$('#readyPlan');if(!coil||!num||!btn)return;const visible=!!(m&&state.screen==='battle'&&m.phase==='plan'&&m.quickWindow?.round===m.round);coil.hidden=!visible;if(!visible)return;const rem=quickRemainingMs(m),ratio=rem/QUICK_WINDOW_MS,tier=quickTierForRemaining(rem);coil.style.setProperty('--quick-angle',`${Math.max(0,Math.min(360,ratio*360))}deg`);num.textContent=String(Math.ceil(rem/1000));coil.classList.toggle('good',tier.tier==='GOOD');coil.classList.toggle('late',tier.tier==='NONE'&&rem>0);coil.classList.toggle('expired',rem<=0);coil.classList.toggle('lowPerf',!!state.settings.lowPerf);btn.setAttribute('aria-label',`準備完了。クイック判定 残り${Math.ceil(rem/1000)}秒${tier.tier==='PERFECT'?'、PERFECT圏内':tier.tier==='GOOD'?'、GOOD圏内':''}`)}
function settleBattlePoints(m,rank){if(m.battlePointsSettlement)return m.battlePointsSettlement;const me=m.players.find(p=>p.human),quick=Math.max(0,Math.floor(Number(m.quickPointsByPlayer?.[me?.id])||0)),base=rank===1?3000:600,before=Math.max(0,Math.floor(Number(state.progression?.battlePoints)||0)),earned=base+quick,after=before+earned,awards=(m.quickAwards||[]).filter(a=>a.playerId===me?.id&&a.points>0);state.progression={battlePoints:after};m.battlePointsSettlement={base,quick,earned,before,after,chestTarget:BATTLE_POINTS_CHEST_TARGET,chestLinesBefore:Math.floor(before/BATTLE_POINTS_CHEST_TARGET),chestLinesAfter:Math.floor(after/BATTLE_POINTS_CHEST_TARGET),awards};return m.battlePointsSettlement}
function renderResultBattlePoints(r){const p=r?.points;if(!p)return;$('#resultPointsEarned')&&($('#resultPointsEarned').textContent=`+${p.earned.toLocaleString('ja-JP')}`);const road=p.awards?.filter(a=>a.stage==='road').reduce((n,a)=>n+a.points,0)||0,battle=p.awards?.filter(a=>a.stage==='battle').reduce((n,a)=>n+a.points,0)||0;$('#resultPointsBreakdown')&&($('#resultPointsBreakdown').textContent=`通常 +${p.base.toLocaleString('ja-JP')} / クイック +${p.quick.toLocaleString('ja-JP')}（ロード +${road} / バトル +${battle}）`);const remainder=p.after%BATTLE_POINTS_CHEST_TARGET,reached=p.chestLinesAfter>p.chestLinesBefore,fill=reached?100:(remainder/BATTLE_POINTS_CHEST_TARGET)*100;$('#resultPointsMeterFill')&&($('#resultPointsMeterFill').style.width=`${fill}%`);$('#resultPointsChest')&&($('#resultPointsChest').textContent=reached?`宝箱ライン到達 / 累計 ${p.after.toLocaleString('ja-JP')} BattlePoints`:`宝箱進捗 ${remainder.toLocaleString('ja-JP')} / ${BATTLE_POINTS_CHEST_TARGET.toLocaleString('ja-JP')}`)}
const quickTicker=setInterval(()=>{if(state.screen==='battle')renderQuickTimer()},100);window.addEventListener('pagehide',()=>clearInterval(quickTicker),{once:true});
function initRoundRuntime(m){m.participants=new Map(m.players.map(p=>[p.id,[]]));m.participantSeq=0;m.resolvedAbilityKeys=new Set();m.abilityOrderReceipts=new Map();m.testAutoChoices=false;quickStartRound(m)}

function startMatch(){audioResetForNewMatch();try{if(battleMount&&window.GameRoadThreeCharRuntime?.unmount)window.GameRoadThreeCharRuntime.unmount(battleMount)}catch(e){}battleMount=null;battleMountPromise=null;const battleRuntimeEl=$('#battleRuntime');if(battleRuntimeEl)battleRuntimeEl.replaceChildren();const valid=validateDeck(state.savedDeck,{forBattle:true});if(!valid.ok){toast('札組を修正: '+valid.errors[0]);show('setup');renderSetupDeckStatus();return null}const n=playerCount(state.setupMode),ps=[],diagnosticCpuDeck=[...state.savedDeck.main];for(let i=0;i<n;i++)ps.push(makePlayer(i,i===0,i===0?state.savedDeck.main:diagnosticCpuDeck));if(state.setupMode==='2v2'){ps[0].team='A';ps[1].team='A';ps[2].team='B';ps[3].team='B'}state.match={id:'M'+Date.now(),mode:state.setupMode,contentId:state.setupContent,round:1,phase:'plan',players:ps,activeId:null,target:null,log:[],lastResult:null,lastBattleResolution:null,battlePresentation:null,resolutionSeq:0,projection:['N','E','S','W'],abilityRng:(Date.now()^0x9e3779b9)>>>0,busy:false,honey:{byNode:{},receipts:new Set(),generatedRounds:new Set()},playerDeckRule:{id:DECK_RULE.id,revision:DECK_RULE.revision,mainCount:state.savedDeck.main.length,exCount:state.savedDeck.ex.length}};state.match.selectionLock=grCreateSelectionLock(state.match,'P1');initRoundRuntime(state.match);log(state.setupContent==='honey_hunt'?'第1巡。ハニーハント：一筆移動・終点ハニー全量取得・一列七枚勝利。人物を直接ドラッグして経路を秘密予約します。':'第1巡。全員が行動を予約します。');show('battle');setupBattleDetails();renderBattle();return state.match}
$('#startMatch').addEventListener('click',startMatch);
function log(t){if(!state.match)return;state.match.log.unshift(t);state.match.log=state.match.log.slice(0,30)}
function refill(p){while(p.hand.length<3&&p.deck.length)p.hand.push(p.deck.shift())}
function laneDepth(p,l){return p.lanes[l].length}
function posType(id){if(id.startsWith('C:'))return'center';if(id.startsWith('S:'))return'shield';if(id.startsWith('R:'))return'road';return'corner'}
function parseRoad(id){const [,owner,lane,depth]=id.split(':');return{owner,lane,depth:+depth}}
function projectionFor(viewer){const ids=['P1','P2','P3','P4'];const vi=Math.max(0,ids.indexOf(viewer.id));const arms=['N','E','S','W'];const map={};ids.forEach((id,i)=>map[id]=arms[(i-vi+4)%4]);return map}
function centerIds(){const a=[];for(let r=-1;r<=1;r++)for(let c=-1;c<=1;c++)a.push(`C:${r}:${c}`);return a}
function shieldId(owner,lane){return`S:${owner}:${lane}`};function roadId(owner,lane,d){return`R:${owner}:${lane}:${d}`}
function activeNodes(){const s=new Set(centerIds());['K:NW','K:NE','K:SW','K:SE'].forEach(id=>s.add(id));state.match.players.forEach(p=>{['L','C','R'].forEach(l=>{s.add(shieldId(p.id,l));for(let d=1;d<=p.lanes[l].length&&d<=7;d++)s.add(roadId(p.id,l,d))})});return s}
function centerPort(owner,lane,viewer){const arm=projectionFor(viewer)[owner];const laneIx={L:-1,C:0,R:1}[lane];if(arm==='N')return`C:-1:${laneIx}`;if(arm==='S')return`C:1:${-laneIx}`;if(arm==='W')return`C:${-laneIx}:-1`;return`C:${laneIx}:1`}
function neighbors(id,viewer){const act=activeNodes(),out=[],proj=projectionFor(viewer),ownerFor=arm=>state.match.players.find(p=>proj[p.id]===arm)?.id,cornerDefs={'K:NW':[['N','L'],['W','R']],'K:NE':[['N','R'],['E','L']],'K:SW':[['S','R'],['W','L']],'K:SE':[['S','L'],['E','R']]},cornerRoads=k=>(cornerDefs[k]||[]).map(([arm,lane])=>{const owner=ownerFor(arm);return owner?roadId(owner,lane,1):null}).filter(Boolean);if(id.startsWith('C:')){const [,rs,cs]=id.split(':'),r=+rs,c=+cs;[[r-1,c],[r+1,c],[r,c-1],[r,c+1]].forEach(([rr,cc])=>{const x=`C:${rr}:${cc}`;if(act.has(x))out.push(x)});state.match.players.forEach(p=>['L','C','R'].forEach(l=>{const sh=shieldId(p.id,l);if(centerPort(p.id,l,viewer)===id&&act.has(sh))out.push(sh)}))}else if(id.startsWith('K:')){cornerRoads(id).forEach(x=>{if(act.has(x))out.push(x)})}else if(id.startsWith('S:')){const [,owner,lane]=id.split(':');out.push(centerPort(owner,lane,viewer));const r1=roadId(owner,lane,1);if(act.has(r1))out.push(r1)}else if(id.startsWith('R:')){const {owner,lane,depth}=parseRoad(id);if(depth===1){out.push(shieldId(owner,lane));Object.keys(cornerDefs).forEach(k=>{if(cornerRoads(k).includes(id))out.push(k)})}const a=roadId(owner,lane,depth-1),b=roadId(owner,lane,depth+1);if(depth>1&&act.has(a))out.push(a);if(depth<7&&act.has(b))out.push(b)}return out.filter(x=>act.has(x))}
function shortest(start,end,max,viewer){if(start===end)return[start];const q=[[start]],seen=new Set([start]);while(q.length){const p=q.shift();if(p.length-1>=max)continue;for(const n of neighbors(p[p.length-1],viewer)){if(seen.has(n))continue;const np=[...p,n];if(n===end)return np;seen.add(n);q.push(np)}}return null}
function reachable(p,steps){const q=[[p.position,0]],dist=new Map([[p.position,0]]);while(q.length){const [x,d]=q.shift();if(d>=steps)continue;for(const n of neighbors(x,p)){if(!dist.has(n)){dist.set(n,d+1);q.push([n,d+1])}}}return dist}
/* DOPPELGANGER-NORMAL-RULE-RUNTIME-001 R1
   Recovered user canon (2026-07-26 / 2026-08-06):
   - whole field; road/battle cards only; shields join only after becoming battle cards
   - activates when two or more cards share the same printed number
   - same-number reduction includes the Doppelganger card itself
   - continuous current value affects road movement/order/attack-right/battle value
   - zero does not destroy the card; no recursive modifier loop
   Unknowns remain fail-closed: formal card id/suit/rank, same-name self counting,
   negative values, modifier ordering beyond the recovered examples, hidden-card edge cases. */
const GR_DOPPEL_EFFECT='doppelgangerContinuous';
function grDoppelIs(cardId){return CARDS[cardId]?.effect_type===GR_DOPPEL_EFFECT}
function grDoppelEligibleParticipant(e){return !!e&&['road','active_submission','auto_defense','ability_active_addition'].includes(String(e.origin||''))}
function grDoppelFieldSnapshot(m=state.match){
 const out=[],seen=new Set(); if(!m)return out;
 const add=(key,ownerId,cardId,zone,publicNow=true)=>{const c=CARDS[cardId];if(!c||!publicNow||seen.has(key))return;seen.add(key);out.push(Object.freeze({key,ownerId,cardId,zone,printedValue:Number(c.power),name:String(c.display_name||cardId)}))};
 m.players.forEach(p=>['L','C','R'].forEach(l=>(p.lanes?.[l]||[]).forEach((id,i)=>add(`lane:${p.id}:${l}:${i}:${id}`,p.id,id,'lane',true))));
 if(m.participants)for(const e of participantEntries())if(grDoppelEligibleParticipant(e))add(`participant:${e.key}`,e.ownerId,e.cardId,e.origin,true);
 return out;
}
function grDoppelCensus(cardId,selfKey,snapshot=grDoppelFieldSnapshot(),matchBasis){
 const c=CARDS[cardId],base=Number(c?.power); if(!c||!Number.isFinite(base))return{ok:false,reason:'CARD_NOT_FOUND'};
 if(matchBasis!=='printed')return{ok:false,reason:'DOPPEL_MATCH_BASIS_UNRESOLVED'};
 const self=snapshot.find(x=>x.key===selfKey&&x.cardId===cardId)||snapshot.find(x=>x.cardId===cardId);
 if(!self)return{ok:false,reason:
--- END HIT 1 ---

--- HIT 2 OFFSET=10003796 RANGE=9997796:10009816 ---
turn CARDS[cardId]?.effect_type===GR_DOPPEL_EFFECT}
function grDoppelEligibleParticipant(e){return !!e&&['road','active_submission','auto_defense','ability_active_addition'].includes(String(e.origin||''))}
function grDoppelFieldSnapshot(m=state.match){
 const out=[],seen=new Set(); if(!m)return out;
 const add=(key,ownerId,cardId,zone,publicNow=true)=>{const c=CARDS[cardId];if(!c||!publicNow||seen.has(key))return;seen.add(key);out.push(Object.freeze({key,ownerId,cardId,zone,printedValue:Number(c.power),name:String(c.display_name||cardId)}))};
 m.players.forEach(p=>['L','C','R'].forEach(l=>(p.lanes?.[l]||[]).forEach((id,i)=>add(`lane:${p.id}:${l}:${i}:${id}`,p.id,id,'lane',true))));
 if(m.participants)for(const e of participantEntries())if(grDoppelEligibleParticipant(e))add(`participant:${e.key}`,e.ownerId,e.cardId,e.origin,true);
 return out;
}
function grDoppelCensus(cardId,selfKey,snapshot=grDoppelFieldSnapshot(),matchBasis){
 const c=CARDS[cardId],base=Number(c?.power); if(!c||!Number.isFinite(base))return{ok:false,reason:'CARD_NOT_FOUND'};
 if(matchBasis!=='printed')return{ok:false,reason:'DOPPEL_MATCH_BASIS_UNRESOLVED'};
 const self=snapshot.find(x=>x.key===selfKey&&x.cardId===cardId)||snapshot.find(x=>x.cardId===cardId);
 if(!self)return{ok:false,reason:'DOPPEL_SELF_NOT_ON_PUBLIC_FIELD'};
 const sameNumber=snapshot.filter(x=>Number(x.printedValue)===base),sameName=snapshot.filter(x=>x.name===String(c.display_name||cardId));
 return{ok:true,baseValue:base,activation:sameNumber.length>=2,sameNumberCount:sameNumber.length,sameNameCount:sameName.length,selfIncludedInSameNumber:sameNumber.some(x=>x.key===self.key),fieldCount:snapshot.length,matchBasis};
}
function grResolveDoppelgangerValue(cardId,selfKey,options={}){
 const c=CARDS[cardId]; if(!c)return{ok:false,reason:'CARD_NOT_FOUND'};
 if(!grDoppelIs(cardId))return{ok:true,value:Number(c.power),applied:false,reason:'NOT_DOPPELGANGER'};
 const matchBasis=options.matchBasis??c.doppel_match_basis; const census=grDoppelCensus(cardId,selfKey,options.snapshot||grDoppelFieldSnapshot(),matchBasis); if(!census.ok)return census;
 if(!census.activation)return{ok:true,value:census.baseValue,applied:false,census,reason:'SAME_NUMBER_LT_2'};
 if(options.externalModifier!==undefined&&Number(options.externalModifier)!==0)return{ok:false,reason:'DOPPEL_MODIFIER_ORDER_UNRESOLVED',census};
 if(options.sameNameSelfIncluded!==true&&options.sameNameSelfIncluded!==false)return{ok:false,reason:'DOPPEL_SAME_NAME_SELF_POLICY_UNRESOLVED',census};
 const sameNamePenalty=options.sameNameSelfIncluded?census.sameNameCount:Math.max(0,census.sameNameCount-1);
 const candidate=census.baseValue-census.sameNumberCount-sameNamePenalty;
 if(candidate<0&&options.allowBelowZero!==true&&options.allowBelowZero!==false)return{ok:false,reason:'DOPPEL_BELOW_ZERO_POLICY_UNRESOLVED',candidate,census};
 return{ok:true,value:options.allowBelowZero?candidate:Math.max(0,candidate),applied:true,census,sameNamePenalty};
}
function grDoppelRoadCurrentValue(cardId,selfKey,options={}){const r=grResolveDoppelgangerValue(cardId,selfKey,options);if(!r.ok)throw Error(r.reason);return r.value}
function grDoppelMovementLimitFromValue(v){const n=Number(v);return Number.isFinite(n)&&n>=1&&n<=6?Math.floor(n):0}
function grDoppelPolicyReady(cardId){const c=CARDS[cardId];return !!c&&c.effect_type===GR_DOPPEL_EFFECT&&c.doppel_match_basis==='printed'&&typeof c.doppel_same_name_self_included==='boolean'&&typeof c.doppel_allow_below_zero==='boolean'&&c.doppel_modifier_order==='formal'}
window.__GAMEROAD_DOPPELGANGER_RUNTIME__={version:'DOPPEL-R1-RECOVERED-GUARD',effectType:GR_DOPPEL_EFFECT,fieldSnapshot:grDoppelFieldSnapshot,census:grDoppelCensus,resolve:grResolveDoppelgangerValue,movementLimitFromValue:grDoppelMovementLimitFromValue,formalCardRegistered:()=>window.__CARD_DATA__.some(c=>c.effect_type===GR_DOPPEL_EFFECT),policyReady:grDoppelPolicyReady,unresolved:Object.freeze(['formal_card_identity','match_basis','same_name_self_count','below_zero','modifier_order','hidden_field_edge','ui_save_reconnect_ai'])};
/* SLAYKIA-CHAOS-CODE-CROSSMEDIA-001 R2 — live R33 adapter.
   User-canon identity is registered, but the card remains unavailable to deck/gacha/runtime
   until X and the chips->rank/status contract are formally supplied. The attack-end hook is
   placed after battle cards are settled to lane/chip and before refill, so top-deck reveal is
   observed before replenishment. */
const GR_SLAYKIA_ID='DCG_SLAYKIA';
function grSlaykiaCard(){return CARDS[GR_SLAYKIA_ID]||null}
function grSlaykiaPolicyReady(){const c=grSlaykiaCard();return !!c&&c.runtime_status==='playable'&&window.GameRoadSlaykiaChaosRuntime?.validX?.(c.slaykia_chaos_x)&&Array.isArray(c.slaykia_place_registry)&&c.slaykia_place_registry.length>0&&Array.isArray(c.slaykia_target_registry)&&c.slaykia_target_registry.length>0&&Array.isArray(c.slaykia_process_registry)&&c.slaykia_process_registry.length>0&&c.slaykia_chip_rank_status_policy!=null}
function grSlaykiaEnsureState(m){if(!m.slaykiaChaos)m.slaykiaChaos={version:'SLAYKIA-R2',destroyedByOwner:{},receipts:[],specialWinnerOwnerId:null};return m.slaykiaChaos}
function grSlaykiaTopReveal(m,x){return window.GameRoadSlaykiaChaosRuntime.topReveal(m.players,x,id=>CARDS[id]?.power)}
function grSlaykiaRecordDestruction(m,ownerId,receipt){const st=grSlaykiaEnsureState(m);const before=Number(st.destroyedByOwner[ownerId]||0);if(receipt?.ok!==true)return{ok:false,reason:receipt?.reason||'DESTRUCTION_FAILED',count:before,specialWin:before>=16};const r=window.GameRoadSlaykiaChaosRuntime.destructionCount(before,1);st.destroyedByOwner[ownerId]=r.count;st.receipts.push({round:m.round,ownerId,kind:'destroy',receipt,count:r.count});if(r.specialWin)st.specialWinnerOwnerId=ownerId;return{ok:true,...r}}
function grSlaykiaWinnerTokens(m,ownerId){const p=m.players.find(x=>x.id===ownerId);if(!p)return[];return m.mode==='2v2'?[p.team]:[ownerId]}
function grSlaykiaAttackEndHook(m){
 const c=grSlaykiaCard(),rows=m?.lastBattleResolution?.players||[];
 const owners=rows.filter(r=>(r.cards||[]).some(x=>x.cardId===GR_SLAYKIA_ID)).map(r=>r.id);
 if(!owners.length)return{ok:true,active:false,specialWinnerTokens:[]};
 const st=grSlaykiaEnsureState(m);
 if(!c||c.runtime_status!=='playable'){const r={round:m.round,kind:'attack_end',ok:false,reason:'SLAYKIA_PENDING_CONTRACT',owners:[...owners]};st.receipts.push(r);return{...r,active:true,specialWinnerTokens:[]}}
 if(!grSlaykiaPolicyReady()){const r={round:m.round,kind:'attack_end',ok:false,reason:'SLAYKIA_FORMAL_POLICY_UNRESOLVED',owners:[...owners]};st.receipts.push(r);return{...r,active:true,specialWinnerTokens:[]}}
 const reveal=grSlaykiaTopReveal(m,c.slaykia_chaos_x);st.receipts.push({round:m.round,kind:'top_reveal',ok:reveal.ok,reveal});
 /* Formal registry semantics are intentionally not synthesized from their names here.
    Once canon supplies executable registry adapters, each owner calls the deterministic
    kernel and successful destruction receipts pass through grSlaykiaRecordDestruction. */
 const reason='SLAYKIA_REGISTRY_ADAPTER_PENDING';st.receipts.push({round:m.round,kind:'chaos_attempts',ok:false,reason,owners:[...owners]});
 const owner=st.specialWinnerOwnerId;return{ok:false,active:true,reason,reveal,specialWinnerTokens:owner?grSlaykiaWinnerTokens(m,owner):[]}
}
window.__GAMEROAD_SLAYKIA_CHAOS_RUNTIME__={version:'SLAYKIA-R2-LIVE-ADAPTER',cardId:GR_SLAYKIA_ID,policyReady:grSlaykiaPolicyReady,topReveal:grSlaykiaTopReveal,recordDestruction:grSlaykiaRecordDestruction,winnerTokens:grSlaykiaWinnerTokens,attackEndHook:grSlaykiaAttackEndHook,unresolved:Object.freeze(['chaos_x','chip_to_rank_status','formal_place_registry','formal_target_registry','formal_process_registry'])};
// BROWSER-DIRECT-CARD-INTERACTION-001: ロード選択適格と通常移動適格を分離する。
function roadSelectable(id){const c=CARDS[id];return !!c&&c.runtime_status==='playable'&&(!grDoppelIs(id)||grDoppelPolicyReady(id))}
function normalMoveLimitForRoad(id){if(!roadSelectable(id))return 0;const n=Number(CARDS[id].power);return Number.isFinite(n)&&n>=1&&n<=6?Math.floor(n):0}
function normalMoveLimitForPlayer(p){return normalMoveLimitForRoad(p?.plan?.roadId)}
function preferredRoadCandidates(hand){const all=(hand||[]).filter(roadSelectable),movable=all.filter(id=>normalMoveLimitForRoad(id)>0);return movable.length?movable:all}
let directPlanRole='road';
function syncDirectPlanRoleUi(){const me=state.match?.players?.[0];if(me){if(!me.plan?.roadId)directPlanRole='road';else if(!me.plan?.battleId)directPlanRole='battle'};[['road','#roadSelect'],['battle','#battleSelect']].forEach(([role,sel])=>{const e=$(sel),box=e?.closest('.planSelect');box?.classList.toggle('directRoleActive',directPlanRole===role);if(e)e.setAttribute('aria-label',`${role==='road'?'ロード':'バトル'}札。手札入力先${directPlanRole===role?'、選択中':''}`)})}
function setDirectPlanRole(role,announce=false){if(role!=='road'&&role!=='battle')return directPlanRole;directPlanRole=role;syncDirectPlanRoleUi();if(announce)toast(`${role==='road'?'ロード':'バトル'}を選び直します。手札から札を選択`);return role}
function nodeXY(id,viewer){const p=fieldProject(id,viewer);if(p)return[p.x,p.y];if(id.startsWith('C:')){const [,rs,cs]=id.split(':'),r=+rs,c=+cs;return[50+c*5.4,50+r*5.4]};if(id.startsWith('S:')||id.startsWith('R:')){let owner,lane,depth=0;if(id.startsWith('S:')){[,owner,lane]=id.split(':')}else{({owner,lane,depth}=parseRoad(id))}const arm=projectionFor(viewer)[owner],off={L:-5.7,C:0,R:5.7}[lane],base=17+depth*4.2;if(arm==='N')return[50+off,39-base+17];if(arm==='S')return[50-off,61+base-17];if(arm==='W')return[39-base+17,50-off];return[61+base-17,50+off]};return[8,8]}

let fieldGL=null,fieldProgram=null,fieldProjection=new Map(),fieldMVP=null,fieldCameraState=null,fieldViewSize={w:1,h:1},routeDrag=null,fieldResizeQueued=false,fieldLastStats={mode:'none',vertices:0,grassTufts:0,lowPerf:false};
function nodeWorld(id,viewer){if(id.startsWith('C:')){const [,rs,cs]=id.split(':'),r=+rs,c=+cs;return{x:+cs*1.35,z:+rs*1.35,y:.24}};if(id.startsWith('S:')||id.startsWith('R:')){let owner,lane,depth=0;if(id.startsWith('S:')){[,owner,lane]=id.split(':');depth=0}else({owner,lane,depth}=parseRoad(id));const arm=projectionFor(viewer)[owner],off={L:-1.45,C:0,R:1.45}[lane],d=2.35+depth*1.25;if(arm==='N')return{x:off,z:-d,y:.24};if(arm==='S')return{x:-off,z:d,y:.24};if(arm==='W')return{x:-d,z:-off,y:.24};return{x:d,z:off,y:.24}};const k={NW:{x:-10,z:-10},NE:{x:10,z:-10},SW:{x:-10,z:10},SE:{x:10,z:10}}[id.split(':')[1]];return{k,y:.2,...k}}
function vsub(a,b){return[a[0]-b[0],a[1]-b[1],a[2]-b[2]]}function vdot(a,b){return a[0]*b[0]+a[1]*b[1]+a[2]*b[2]}function vcross(a,b){return[a[1]*b[2]-a[2]*b[1],a[2]*b[0]-a[0]*b[2],a[0]*b[1]-a[1]*b[0]]}function vnorm(a){const l=Math.hypot(...a)||1;return a.map(x=>x/l)}
function m4mul(a,b){const o=new Array(16).fill(0);for(let r=0;r<4;r++)for(let c=0;c<4;c++)for(let k=0;k<4;k++)o[r*4+c]+=a[r*4+k]*b[k*4+c];return o}
function m4persp(fovy,aspect,near,far){const f=1/Math.tan(fovy/2),nf=1/(near-far);return[f/aspect,0,0,0,0,f,0,0,0,0,(far+near)*nf,-1,0,0,2*far*near*nf,0]}
function m4look(eye,center,up){const z=vnorm(vsub(eye,center)),x=vnorm(vcross(up,z)),y=vcross(z,x);return[x[0],y[0],z[0],0,x[1],y[1],z[1],0,x[2],y[2],z[2],0,-vdot(x,eye),-vdot(y,eye),-vdot(z,eye),1]}
function fieldViewportProfile(){const map=$('#battleMap'),w=Math.max(1,map?.clientWidth||innerWidth),h=Math.max(1,map?.clientHeight||innerHeight),raw=w/h;if(raw>1.85&&h<500)return{aspect:Math.max(1.2,Math.min(2.28,raw)),yBase:.36,yGain:.29,kind:'short-landscape'};if(raw<.78)return{aspect:1.2,yBase:.44,yGain:.40,kind:'portrait'};return{aspect:Math.max(1.2,raw),yBase:.45,yGain:.42,kind:'standard'}}
function project3(w){const c=fieldCameraState;if(!c)return null;const eye=c.eye,rx=w.x-eye[0],ry=(w.y||0)-eye[1],rz=w.z-eye[2],q=Math.SQRT1_2,cx=rx,cy=q*ry-q*rz,cz=-q*ry-q*rz;if(cz<=.08)return null;const t=Math.tan(Math.PI/10.2),nx=cx/(cz*t*c.aspect),ny=cy/(cz*t);return{x:(nx*.5+.5)*100,y:(c.yBase-ny*c.yGain)*100}}
function fieldFocusN
--- END HIT 2 ---

--- HIT 3 OFFSET=10062097 RANGE=10056097:10068117 ---
))}
function currentOpponentParticipants(p){const foes=new Set(legalOpponents(p).map(x=>x.id));return participantEntries().filter(e=>foes.has(e.ownerId))}
function legalLaneCards(p){const out=[];['L','C','R'].forEach(l=>p.lanes[l].forEach((id,index)=>out.push({kind:'lane',lane:l,index,cardId:id,id:`${l}:${index}:${id}`})));return out}
function abilityLogNoPay(p,c,why){log(`${p.name}：${c.display_name}は${why}ため不発。費用は消費しない。`)}
async function resolveAbilityEntry(entry,phase){const m=state.match,p=m.players.find(x=>x.id===entry.ownerId),c=CARDS[entry.cardId];if(!p||!c||c.effect_type==='none')return[];if(c.runtime_status!=='playable')throw Error('blocked-pending-contract:'+entry.cardId);const key=`${m.round}:${phase}:${entry.key}`;if(m.resolvedAbilityKeys.has(key))return[];m.resolvedAbilityKeys.add(key);const cost=effectiveAbilityCost(p,c);if(p.awake<cost.due){abilityLogNoPay(p,c,`使用可能マナ${p.awake}に対し必要${cost.due}`);return[]}
 let chosen=[],added=[];
 if(c.effect_type==='blockBattlePlay'){const opponents=legalOpponents(p),need=Math.min(c.id==='DCG_SAASUNA_FROZEN_SECOND'?2:1,opponents.length);chosen=await chooseAbilityItems(c.display_name,`${need}人を選択。この巡の新しいバトルカード提出・能力追加を止めます。`,opponents.map(player=>({kind:'player',player,id:player.id,detail:'既に公開済みの札と自動防御は残る'})),need,need,p.human);if(chosen.length<need){abilityLogNoPay(p,c,'合法な対象が足りない');return[]}}
 if(c.effect_type==='playAdditionalBattleFromHand'){if(p.battleBlockedRound===m.round){abilityLogNoPay(p,c,'バトルカード提出禁止中の');return[]}const opts=p.hand.filter(isIceTrump).map(cardId=>({kind:'hand',cardId,id:cardId,detail:'アイス・マドー / 能力なし'}));chosen=await chooseAbilityItems(c.display_name,'手札からアイストランプを1枚、別札個体として追加します。',opts,1,1,p.human);if(chosen.length!==1){abilityLogNoPay(p,c,'合法なアイストランプがない');return[]}}
 if(c.effect_type==='returnOwnColumnCards'){const opts=legalLaneCards(p);chosen=await chooseAbilityItems(c.display_name,'自分の列から1枚以上を選択し、同じ札個体のまま手札へ戻します。',opts,1,opts.length,p.human);if(!chosen.length){abilityLogNoPay(p,c,'戻せる列札がない');return[]}}
 if(c.effect_type==='zeroRandomMaximum'){const foes=currentOpponentParticipants(p);if(!foes.length){abilityLogNoPay(p,c,'相手側の戦闘参加札がない');return[]}}
 if(c.effect_type==='deckTopToChip'&&!p.deck.length){abilityLogNoPay(p,c,'山札が空の');return[]}
 const paid=payAbility(p,c);if(!paid)throw Error('ability-payment-race:'+c.id);if(p.human)window.GAMEROAD_PARTNER_COMMENT?.emit?.({eventId:`${m.id}:ability:${m.round}:${entry.key}`,kind:'dialogue',speechIntent:'ability_activation',cardId:c.id,cardName:c.display_name,loggable:false,ttlMs:9000});if(paid.base)log(`${p.name}：${c.display_name}の能力用マナ${paid.due}を支払った${paid.reduction?`（次回軽減${paid.reduction}適用）`:''}。`);
 switch(c.effect_type){
  case'wakeMana':{const before=p.awake;p.awake=Math.min(p.mana.length,p.awake+2);log(`${p.name}：${c.display_name}でマナを${p.awake-before}枚起こした。`);break}
  case'deckTopToChip':{const moved=p.deck.shift();p.chip.push(moved);log(`${p.name}：${c.display_name}で山札先頭をチップへ移した。`);break}
  case'drawAndReduceCost':{if(p.deck.length)p.hand.push(p.deck.shift());p.nextCostReduction=(p.nextCostReduction||0)+2;log(`${p.name}：${c.display_name}で1枚引き、次の能力用マナを2軽減。`);break}
  case'blockBattlePlay':{chosen.forEach(x=>{x.player.battleBlockedRound=m.round;log(`${x.player.name}：この巡の新しいバトルカード提出を禁止。`) });break}
  case'playAdditionalBattleFromHand':{const id=chosen[0].cardId;removeHand(p,id);const e=makeParticipant(p,id,'ability_active_addition');added.push(e);log(`${p.name}：${cardLabel(id)}を追加バトルカードとして公開。`);break}
  case'returnOwnColumnCards':{const byLane={};chosen.forEach(x=>(byLane[x.lane]||(byLane[x.lane]=[])).push(x));Object.entries(byLane).forEach(([lane,arr])=>arr.sort((a,b)=>b.index-a.index).forEach(x=>{const got=p.lanes[lane].splice(x.index,1)[0];if(got!==x.cardId)throw Error('zekuu-instance-shift');p.hand.push(got)}));log(`${p.name}：${chosen.length}枚を列から手札へ戻した。`);break}
  case'zeroRandomMaximum':{const foes=currentOpponentParticipants(p),max=Math.max(...foes.map(e=>e.currentValue)),cand=foes.filter(e=>e.currentValue===max).sort((a,b)=>a.key.localeCompare(b.key)),pick=cand[matchRnd(cand.length)];pick.currentValue=0;log(`${p.name}：${cardLabel(pick.cardId)}の今回戦闘中の値を0にした。`);break}
  default:throw Error('unaccepted-effect:'+c.effect_type+':'+entry.cardId)
 }
 renderBattle();return added
}
async function resolveAbilityBundle(entries,phase){let addedBundleSeq=0;const queue=[sortAbilityEntries(entries.filter(e=>CARDS[e.cardId]?.effect_type!=='none'),`${phase}:root`)];while(queue.length){const bundle=queue.shift();if(!bundle.length)continue;const newly=[];for(const e of bundle)newly.push(...await resolveAbilityEntry(e,phase));const next=newly.filter(e=>CARDS[e.cardId]?.effect_type!=='none');if(next.length)queue.push(sortAbilityEntries(next,`${phase}:add:${++addedBundleSeq}`))}}
async function commitPlans(){const m=state.match;if(m.busy)return;const me=m.players[0];updatePlanFromSelect();if(!me.plan?.roadId||!me.plan?.battleId){toast('ロードカードとバトルカードを選択してください');return}if(me.plan.roadId===me.plan.battleId){toast('別の札を選択してください');return}const path=me.plan.path||[me.position];if(path.length-1>normalMoveLimitForRoad(me.plan.roadId)){toast('通常移動可能歩数を超えています');return}for(let i=1;i<path.length;i++){if(!neighbors(path[i-1],me).includes(path[i])||path.slice(0,i).includes(path[i])){toast('経路が連続していません');return}}const quickAcceptedAt=quickNow(m);m.busy=true;try{if(!battleMount)await updateBattleAvatar();const used=new Set([path.at(-1)]);m.players.slice(1).forEach(p=>p.plan=botPlan(p,used));m.players.forEach(p=>{if(!p.hand.includes(p.plan.roadId)||!p.hand.includes(p.plan.battleId))throw Error('reservation-not-owned')});quickAcceptPlan(m,me,quickAcceptedAt);window.GAMEROAD_PARTNER_COMMENT?.emit?.({eventId:`${m.id}:set:${m.round}:road`,kind:'dialogue',speechIntent:'road_set',cardId:me.plan.roadId,loggable:false,ttlMs:9000});window.GAMEROAD_PARTNER_COMMENT?.emit?.({eventId:`${m.id}:set:${m.round}:battle`,kind:'dialogue',speechIntent:'battle_set',cardId:me.plan.battleId,loggable:false,ttlMs:9000});m.lastBattleResolution=null;m.players.forEach(p=>removeHand(p,p.plan.roadId));m.activeId=pickActive(m.players).id;const roadEntries=[];m.players.forEach(p=>roadEntries.push(makeParticipant(p,p.plan.roadId,'road')));m.phase='reveal';log('ロードカードを公開。公開時能力を小さい印刷値順で解決します。');renderBattle();await resolveAbilityBundle(roadEntries,'road');if(m.contentId==='honey_hunt'&&!m.honey.generatedRounds.has(m.round)){spawnHoneyFromRoads();log('公開された1〜6と、列上の同数字ロードカード実体を照合してハニー発生源を更新しました。')}m.phase='move';renderBattle();const order=randomOrderByRoad(m.players);for(const p of order){await animateResolvedPath(p);if(m.contentId!=='honey_hunt'&&CARDS[p.plan.roadId].effect_type==='none'){const before=p.awake;p.awake=Math.min(p.mana.length,p.awake+1);if(p.awake>before)log(`${p.name}：能力なしロードカードの成立でマナを1枚起こした。`)}log(`${p.name}：${cardLabel(p.plan.roadId)} / ${p.plan.path.length-1}歩`)}const act=m.players.find(p=>p.id===m.activeId);m.phase='target';log(`${act.name}が攻撃権を取得。`);m.busy=false;renderBattle();if(!act.human){autoTarget(act);await resolveBattle()}}catch(e){m.busy=false;log(`処理停止：${e.message}`);renderBattle();throw e}}
$('#readyPlan').onclick=()=>{commitPlans().catch(e=>console.error(e))};
function autoTarget(active){const others=legalOpponents(active);if(!others.length){state.match.target=null;throw Error('no-legal-target')}const target=others[rndInt(others.length)];const lane=['L','C','R'][rndInt(3)],shield=['L','C','R'][rndInt(3)];state.match.target={defenderId:target.id,lane,shield}}
function renderTarget(){const box=$('#targetBox'),act=state.match.players.find(p=>p.id===state.match.activeId),on=state.match.phase==='target'&&act?.human;box.classList.toggle('on',on);$('#battleMap').classList.toggle('targetMode',!!on);if(!on)return;const t=$('#targetPlayer');t.innerHTML='';legalOpponents(act).forEach(p=>{const o=document.createElement('option');o.value=p.id;o.textContent=p.name;t.appendChild(o)})}
$('#confirmTarget').onclick=async()=>{const act=state.match.players.find(p=>p.id===state.match.activeId);if(!act?.human)return;const defenderId=$('#targetPlayer').value;if(!legalOpponents(act).some(p=>p.id===defenderId)){state.match.target=null;renderBattle();return}state.match.target={defenderId,lane:$('#targetLane').value,shield:$('#targetShield').value};try{await resolveBattle()}catch(e){console.error(e)}}
function sideGroups(){if(state.match.mode!=='2v2')return state.match.players.map(p=>({id:p.id,players:[p]}));return['A','B'].map(t=>({id:t,players:state.match.players.filter(p=>p.team===t)}))}
async function replacementBattleCard(p,reason){const opts=p.hand.filter(id=>CARDS[id]?.runtime_status==='playable').map(cardId=>({kind:'hand',cardId,id:cardId,detail:reason}));if(!opts.length)return null;const ch=await chooseAbilityItems('バトルカードを再確認',reason,opts,1,1,p.human);return ch[0]?.cardId||null}
async function resolveBattle(){const m=state.match;if(m.busy)return;if(!m.target)throw Error('target-missing');const active=m.players.find(p=>p.id===m.activeId);if(!active||!legalOpponents(active).some(p=>p.id===m.target.defenderId))throw Error('illegal-target');m.busy=true;m.phase='resolve';const defender=m.players.find(p=>p.id===m.target.defenderId);m.battlePresentation={stage:'focus',round:m.round,mode:m.mode,attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,players:[]};renderBattle();try{if(!await setBattlePresentation('focus',{attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,players:[]}))return;if(active.human)await setBattleRuntimeState('attack');const usedShield=defender.shields[m.target.shield];if(!usedShield)throw Error('shield-missing');let replenish=defender.plan.battleId;if(!defender.hand.includes(replenish)||CARDS[replenish]?.runtime_status!=='playable')replenish=await replacementBattleCard(defender,'使用シールドの同位置補充に使う札を1枚選びます。');if(!replenish)throw Error('shield-replenish-card-missing');removeHand(defender,replenish);defender.shields[m.target.shield]=replenish;const battleReveals=[];m.players.forEach(p=>{if(p.id===defender.id){battleReveals.push(makeParticipant(p,usedShield,'auto_defense'));return}});for(const p of m.players){if(p.id===defender.id)continue;if(p.battleBlockedRound===m.round){log(`${p.name}：フローズンにより予約バトルカードは手札に残る。`);continue}let battle=p.plan.battleId;if(!p.hand.includes(battle)||CARDS[battle]?.runtime_status!=='playable')battle=await replacementBattleCard(p,'予約札が不成立です。戦闘直前の合法札を1枚選びます。');if(!battle){log(`${p.name}：提出できるバトルカードがない。`);continue}removeHand(p,battle);battleReveals.push(makeParticipant(p,battle,'active_submission'))}log('バトルカードと使用シールドを公開。公開時能力を小さい印刷値順で解決します。');const publicRevealPresentation={attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,players:battlePresentationPlayers(m)};if(!await setBattlePresentation('reveal',publicRevealPresentation))return;const publicPresentationInputs=buildPublicCardPresentationInputs({round:m.round,...publicRevealPresentation});validatePublicCardPresentationInputs(publicPresentationInputs);if(publicPresentationInputs.length&&!await setBattlePresentation('read',{...publicRevealPresentation,presentationEvents:publicPresentationInputs}))return;await resolveAbilityBundle(battleReveals,'battle');const scores=new Map();m.players.forEach(p=>scores.set(p.id,(m.participants.get(p.id)||[]).reduce((a,e)=>a+e.currentValue,0)));const comparisonPlayers=battlePresentationPlayers(m);if(!await setBattlePresentation('compare',{attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,players:comparisonPlayers,teamTotals:m.mode==='2v2'?{A:m.players.filter(p=>p.team==='A').reduce((n,p)=>n+scores.get(p.id),0),B:m.players.filter(p=>p.team==='B').reduce((n,p)=>n+scores.get(p.id),0)}:null}))return;let winningPlayerIds=[];if(m.mode==='2v2'){const totals={A:0,B:0};m.players.forEach(p=>totals[p.team]+=scores.get(p.id));const max=Math.max(totals.A,totals.B),ties=['A','B'].filter(t=>totals[t]===max);let team;if(ties.length===1)team=ties[0];else{const active=m.players.find(p=>
--- END HIT 3 ---

--- HIT 4 OFFSET=10070239 RANGE=10064239:10076259 ---
et=null;renderBattle();return}state.match.target={defenderId,lane:$('#targetLane').value,shield:$('#targetShield').value};try{await resolveBattle()}catch(e){console.error(e)}}
function sideGroups(){if(state.match.mode!=='2v2')return state.match.players.map(p=>({id:p.id,players:[p]}));return['A','B'].map(t=>({id:t,players:state.match.players.filter(p=>p.team===t)}))}
async function replacementBattleCard(p,reason){const opts=p.hand.filter(id=>CARDS[id]?.runtime_status==='playable').map(cardId=>({kind:'hand',cardId,id:cardId,detail:reason}));if(!opts.length)return null;const ch=await chooseAbilityItems('バトルカードを再確認',reason,opts,1,1,p.human);return ch[0]?.cardId||null}
async function resolveBattle(){const m=state.match;if(m.busy)return;if(!m.target)throw Error('target-missing');const active=m.players.find(p=>p.id===m.activeId);if(!active||!legalOpponents(active).some(p=>p.id===m.target.defenderId))throw Error('illegal-target');m.busy=true;m.phase='resolve';const defender=m.players.find(p=>p.id===m.target.defenderId);m.battlePresentation={stage:'focus',round:m.round,mode:m.mode,attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,players:[]};renderBattle();try{if(!await setBattlePresentation('focus',{attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,players:[]}))return;if(active.human)await setBattleRuntimeState('attack');const usedShield=defender.shields[m.target.shield];if(!usedShield)throw Error('shield-missing');let replenish=defender.plan.battleId;if(!defender.hand.includes(replenish)||CARDS[replenish]?.runtime_status!=='playable')replenish=await replacementBattleCard(defender,'使用シールドの同位置補充に使う札を1枚選びます。');if(!replenish)throw Error('shield-replenish-card-missing');removeHand(defender,replenish);defender.shields[m.target.shield]=replenish;const battleReveals=[];m.players.forEach(p=>{if(p.id===defender.id){battleReveals.push(makeParticipant(p,usedShield,'auto_defense'));return}});for(const p of m.players){if(p.id===defender.id)continue;if(p.battleBlockedRound===m.round){log(`${p.name}：フローズンにより予約バトルカードは手札に残る。`);continue}let battle=p.plan.battleId;if(!p.hand.includes(battle)||CARDS[battle]?.runtime_status!=='playable')battle=await replacementBattleCard(p,'予約札が不成立です。戦闘直前の合法札を1枚選びます。');if(!battle){log(`${p.name}：提出できるバトルカードがない。`);continue}removeHand(p,battle);battleReveals.push(makeParticipant(p,battle,'active_submission'))}log('バトルカードと使用シールドを公開。公開時能力を小さい印刷値順で解決します。');const publicRevealPresentation={attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,players:battlePresentationPlayers(m)};if(!await setBattlePresentation('reveal',publicRevealPresentation))return;const publicPresentationInputs=buildPublicCardPresentationInputs({round:m.round,...publicRevealPresentation});validatePublicCardPresentationInputs(publicPresentationInputs);if(publicPresentationInputs.length&&!await setBattlePresentation('read',{...publicRevealPresentation,presentationEvents:publicPresentationInputs}))return;await resolveAbilityBundle(battleReveals,'battle');const scores=new Map();m.players.forEach(p=>scores.set(p.id,(m.participants.get(p.id)||[]).reduce((a,e)=>a+e.currentValue,0)));const comparisonPlayers=battlePresentationPlayers(m);if(!await setBattlePresentation('compare',{attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,players:comparisonPlayers,teamTotals:m.mode==='2v2'?{A:m.players.filter(p=>p.team==='A').reduce((n,p)=>n+scores.get(p.id),0),B:m.players.filter(p=>p.team==='B').reduce((n,p)=>n+scores.get(p.id),0)}:null}))return;let winningPlayerIds=[];if(m.mode==='2v2'){const totals={A:0,B:0};m.players.forEach(p=>totals[p.team]+=scores.get(p.id));const max=Math.max(totals.A,totals.B),ties=['A','B'].filter(t=>totals[t]===max);let team;if(ties.length===1)team=ties[0];else{const active=m.players.find(p=>p.id===m.activeId);team=ties.includes(active.team)?active.team:ties[matchRnd(ties.length)]}winningPlayerIds=m.players.filter(p=>p.team===team).map(p=>p.id);log(`戦闘：組${team}が勝利。`)}else{const max=Math.max(...m.players.map(p=>scores.get(p.id)));const ties=m.players.filter(p=>scores.get(p.id)===max);const active=m.players.find(p=>p.id===m.activeId);const winner=ties.length===1?ties[0]:(ties.some(p=>p.id===active.id)?active:ties[matchRnd(ties.length)]);winningPlayerIds=[winner.id];log(`戦闘：${winner.name}が勝利。`)}if(!await setBattlePresentation('winner',{attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,players:battlePresentationPlayers(m,winningPlayerIds),winnerIds:[...winningPlayerIds],teamTotals:m.mode==='2v2'?{A:m.players.filter(p=>p.team==='A').reduce((n,p)=>n+scores.get(p.id),0),B:m.players.filter(p=>p.team==='B').reduce((n,p)=>n+scores.get(p.id),0)}:null}))return;const humanPlayer=m.players.find(p=>p.human);if(humanPlayer&&battleMount){if(winningPlayerIds.includes(humanPlayer.id))await setBattleRuntimeState('joy');else await setBattleRuntimeState('hit');}const resolutionPlayers=m.players.map(p=>({id:p.id,name:p.name,team:p.team||null,score:scores.get(p.id),winner:winningPlayerIds.includes(p.id),cards:(m.participants.get(p.id)||[]).map(e=>({cardId:e.cardId,label:cardLabel(e.cardId),value:e.currentValue,origin:e.origin}))}));const laneBefore=new Map(m.players.map(p=>[p.id,p.lanes[m.target.lane].length])),maxLaneBefore=new Map(m.players.map(p=>[p.id,Math.max(...Object.values(p.lanes).map(a=>a.length))]));m.players.forEach(p=>{const parts=m.participants.get(p.id)||[];if(winningPlayerIds.includes(p.id)){const lane=p.lanes[m.target.lane];parts.forEach(e=>lane.push(e.cardId));if(p.chip.length)p.chip.splice(0).forEach(id=>lane.push(id))}else parts.forEach(e=>p.chip.push(e.cardId))});const winningTeam=m.mode==='2v2'?(m.players.find(p=>winningPlayerIds.includes(p.id))?.team||null):null,teamTotals=m.mode==='2v2'?{A:m.players.filter(p=>p.team==='A').reduce((n,p)=>n+scores.get(p.id),0),B:m.players.filter(p=>p.team==='B').reduce((n,p)=>n+scores.get(p.id),0)}:null;m.lastBattleResolution={serial:++m.resolutionSeq,round:m.round,mode:m.mode,attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,winnerIds:[...winningPlayerIds],winningTeam,teamTotals,players:resolutionPlayers,laneGains:m.players.map(p=>({id:p.id,lane:m.target.lane,before:laneBefore.get(p.id),after:p.lanes[m.target.lane].length,added:p.lanes[m.target.lane].length-laneBefore.get(p.id)})).filter(x=>x.added>0),maxLaneProgress:m.players.map(p=>({id:p.id,before:maxLaneBefore.get(p.id),after:Math.max(...Object.values(p.lanes).map(a=>a.length))}))};const slaykiaAttackEnd=grSlaykiaAttackEndHook(m);m.participants=new Map(m.players.map(p=>[p.id,[]]));m.players.forEach(refill);m.battlePresentation={stage:'settle',round:m.round,mode:m.mode,attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,players:resolutionPlayers,winnerIds:[...winningPlayerIds],teamTotals};renderBattle();await battlePresentationWait(BATTLE_PRESENTATION_RESULT_MS,true);if(state.match!==m||state.screen!=='battle')return;const winners=slaykiaAttackEnd?.specialWinnerTokens?.length?[...slaykiaAttackEnd.specialWinnerTokens]:checkWin();m.battlePresentation=null;if(winners.length){m.busy=false;if(slaykiaAttackEnd?.specialWinnerTokens?.length)log('スレイキア：成功破壊16枚で特殊勝利。');else if(m.contentId==='honey_hunt')log('一列へ実札が七枚以上連続配置され、ハニーハントの勝利条件が成立しました。');return endMatch(winners)}nextRound()}catch(e){clearBattlePresentationWait();if(state.match===m){m.battlePresentation=null;m.busy=false;log(`戦闘停止：${e.message}`);renderBattle()}throw e}}
function checkWin(){if(state.match.mode==='2v2'){const teams=['A','B'].filter(t=>state.match.players.filter(p=>p.team===t).some(p=>Math.max(...Object.values(p.lanes).map(a=>a.length))>=7));return teams}return state.match.players.filter(p=>Math.max(...Object.values(p.lanes).map(a=>a.length))>=7).map(p=>p.id)}
function nextRound(){const m=state.match;m.lastBattleResolution=null;m.round++;m.phase='plan';m.activeId=null;m.target=null;m.battlePresentation=null;m.busy=false;m.players.forEach(p=>p.plan=null);initRoundRuntime(m);log(`第${m.round}巡。行動を予約します。`);renderBattle()}
function ranksFFA(){const ps=state.match.players.map(p=>({p,depth:Math.max(...Object.values(p.lanes).map(a=>a.length))})).sort((a,b)=>b.depth-a.depth);let prev=null,rank=0;return ps.map((x,i)=>{if(prev===null||x.depth!==prev)rank=i+1;prev=x.depth;return{player:x.p,rank,depth:x.depth}})}
function endMatch(winners){const m=state.match;let ranking;if(m.mode==='2v2'){const wt=winners[0];ranking=m.players.map(p=>({player:p,rank:p.team===wt?1:2,depth:Math.max(...Object.values(p.lanes).map(a=>a.length))}))}else ranking=ranksFFA();const me=ranking.find(x=>x.player.human),points=settleBattlePoints(m,me.rank);m.lastResult={ranking,headline:me.rank===1?'勝利':'結果',grade:me.rank,points};state.history.unshift({at:new Date().toISOString(),mode:m.mode,rank:me.rank,rounds:m.round,character:state.playerCharacterId,partner:state.selectedPartnerId,battlePointsEarned:points.earned,quickPoints:points.quick,battlePointsAfter:points.after});save();show('result');renderResult()}
async function renderResult(){const r=state.match?.lastResult;if(!r)return;const m=state.match,ordered=[...r.ranking].sort((a,b)=>a.rank-b.rank),me=ordered.find(x=>x.player.human),mode=({'2p':'二人','4p':'四人','2v2':'二対二'})[m.mode]||m.mode,content=m.contentId==='honey_hunt'?'ハニーハント':'ロード・シールド';$('#resultGrade').textContent=r.grade;$('#resultHeadline').textContent=r.headline;$('#resultSummary').textContent=`${m.round}巡で決着。 ${content}`;$('#resultLeadText')&&($('#resultLeadText').textContent=r.grade===1?'一位で決着':'最終順位');$('#resultLeadSub')&&($('#resultLeadSub').textContent=`${content} / ${mode}`);$('#resultMode')&&($('#resultMode').textContent=mode);$('#resultRounds')&&($('#resultRounds').textContent=`${m.round}巡`);$('#resultLane')&&($('#resultLane').textContent=`${me?.depth??0}枚`);renderResultBattlePoints(r);const box=$('#resultRanking');box.innerHTML='';ordered.forEach(x=>{const d=document.createElement('div');d.className='rankLine';d.innerHTML=`<b>${x.rank}位 ${x.player.human?'あなた':x.player.name}</b><span>最長列 ${x.depth}枚</span>`;box.appendChild(d)});resultMount=await mountChar('.resultWorld',state.playerCharacterId,r.grade===1?'joy':'defeated')}
$('#rematch').onclick=()=>startMatch();$('#leaveMatch').onclick=()=>{state.match=null;navigateRoot('home')};window.__GAMEROAD_QUICK_R14_QA__={version:'FULLGAME-QUICKTIMER-R14',constants:{windowMs:QUICK_WINDOW_MS,perfectRemainMs:QUICK_PERFECT_REMAIN_MS,goodRemainMs:QUICK_GOOD_REMAIN_MS,perfectPoints:QUICK_PERFECT_POINTS,goodPoints:QUICK_GOOD_POINTS,chestTarget:BATTLE_POINTS_CHEST_TARGET},snapshot(){const m=state.match,me=m?.players?.find(p=>p.human),rem=m?quickRemainingMs(m):null;return{screen:state.screen,round:m?.round||null,phase:m?.phase||null,remainingMs:rem,tier:m?quickTierForRemaining(rem):null,quickPoints:me?(m.quickPointsByPlayer?.[me.id]||0):0,awards:me?(m.quickAwards||[]).filter(a=>a.playerId===me.id).map(a=>({...a})):[],battlePoints:state.progression?.battlePoints||0,settlement:m?.battlePointsSettlement?safeClone(m.battlePointsSettlement):null,coilHidden:$('#quickCoil')?.hidden??null,coilText:$('#quickRemain')?.textContent||null,resultText:$('#resultBattlePoints')?.innerText||''}},setRemainingSeconds(sec){const m=state.match;if(!m?.quickWindow)return false;const v=Math.max(0,Math.min(30,Number(sec)||0));m._quickQaNowMs=m.quickWindow.deadlineAt-v*1000;renderQuickTimer();return true},clearTestClock(){const m=state.match;if(!m)return false;delete m._quickQaNowMs;renderQuickTimer();return true},setBattlePoints(n){state.progression={battlePoints:Math.max(0,Math.floor(Number(n)||0))};return state.progression.battlePoints},persistenceProbe(n){state.progression={battlePoints:Math.max(0,Math.floor(Number(n)||0))};save();state.progression={battlePoints:0};load();return{battlePoints:state.progression.battlePoints,storage:state.storage}}};window.GAMEROAD_3D_WALK_QA={version:'BROWSER-3D-BOARD-WALK-INTEGRATION-001',snapshot(){c
--- END HIT 4 ---

--- HIT 5 OFFSET=10072158 RANGE=10066158:10078178 ---
h(makeParticipant(p,usedShield,'auto_defense'));return}});for(const p of m.players){if(p.id===defender.id)continue;if(p.battleBlockedRound===m.round){log(`${p.name}：フローズンにより予約バトルカードは手札に残る。`);continue}let battle=p.plan.battleId;if(!p.hand.includes(battle)||CARDS[battle]?.runtime_status!=='playable')battle=await replacementBattleCard(p,'予約札が不成立です。戦闘直前の合法札を1枚選びます。');if(!battle){log(`${p.name}：提出できるバトルカードがない。`);continue}removeHand(p,battle);battleReveals.push(makeParticipant(p,battle,'active_submission'))}log('バトルカードと使用シールドを公開。公開時能力を小さい印刷値順で解決します。');const publicRevealPresentation={attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,players:battlePresentationPlayers(m)};if(!await setBattlePresentation('reveal',publicRevealPresentation))return;const publicPresentationInputs=buildPublicCardPresentationInputs({round:m.round,...publicRevealPresentation});validatePublicCardPresentationInputs(publicPresentationInputs);if(publicPresentationInputs.length&&!await setBattlePresentation('read',{...publicRevealPresentation,presentationEvents:publicPresentationInputs}))return;await resolveAbilityBundle(battleReveals,'battle');const scores=new Map();m.players.forEach(p=>scores.set(p.id,(m.participants.get(p.id)||[]).reduce((a,e)=>a+e.currentValue,0)));const comparisonPlayers=battlePresentationPlayers(m);if(!await setBattlePresentation('compare',{attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,players:comparisonPlayers,teamTotals:m.mode==='2v2'?{A:m.players.filter(p=>p.team==='A').reduce((n,p)=>n+scores.get(p.id),0),B:m.players.filter(p=>p.team==='B').reduce((n,p)=>n+scores.get(p.id),0)}:null}))return;let winningPlayerIds=[];if(m.mode==='2v2'){const totals={A:0,B:0};m.players.forEach(p=>totals[p.team]+=scores.get(p.id));const max=Math.max(totals.A,totals.B),ties=['A','B'].filter(t=>totals[t]===max);let team;if(ties.length===1)team=ties[0];else{const active=m.players.find(p=>p.id===m.activeId);team=ties.includes(active.team)?active.team:ties[matchRnd(ties.length)]}winningPlayerIds=m.players.filter(p=>p.team===team).map(p=>p.id);log(`戦闘：組${team}が勝利。`)}else{const max=Math.max(...m.players.map(p=>scores.get(p.id)));const ties=m.players.filter(p=>scores.get(p.id)===max);const active=m.players.find(p=>p.id===m.activeId);const winner=ties.length===1?ties[0]:(ties.some(p=>p.id===active.id)?active:ties[matchRnd(ties.length)]);winningPlayerIds=[winner.id];log(`戦闘：${winner.name}が勝利。`)}if(!await setBattlePresentation('winner',{attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,players:battlePresentationPlayers(m,winningPlayerIds),winnerIds:[...winningPlayerIds],teamTotals:m.mode==='2v2'?{A:m.players.filter(p=>p.team==='A').reduce((n,p)=>n+scores.get(p.id),0),B:m.players.filter(p=>p.team==='B').reduce((n,p)=>n+scores.get(p.id),0)}:null}))return;const humanPlayer=m.players.find(p=>p.human);if(humanPlayer&&battleMount){if(winningPlayerIds.includes(humanPlayer.id))await setBattleRuntimeState('joy');else await setBattleRuntimeState('hit');}const resolutionPlayers=m.players.map(p=>({id:p.id,name:p.name,team:p.team||null,score:scores.get(p.id),winner:winningPlayerIds.includes(p.id),cards:(m.participants.get(p.id)||[]).map(e=>({cardId:e.cardId,label:cardLabel(e.cardId),value:e.currentValue,origin:e.origin}))}));const laneBefore=new Map(m.players.map(p=>[p.id,p.lanes[m.target.lane].length])),maxLaneBefore=new Map(m.players.map(p=>[p.id,Math.max(...Object.values(p.lanes).map(a=>a.length))]));m.players.forEach(p=>{const parts=m.participants.get(p.id)||[];if(winningPlayerIds.includes(p.id)){const lane=p.lanes[m.target.lane];parts.forEach(e=>lane.push(e.cardId));if(p.chip.length)p.chip.splice(0).forEach(id=>lane.push(id))}else parts.forEach(e=>p.chip.push(e.cardId))});const winningTeam=m.mode==='2v2'?(m.players.find(p=>winningPlayerIds.includes(p.id))?.team||null):null,teamTotals=m.mode==='2v2'?{A:m.players.filter(p=>p.team==='A').reduce((n,p)=>n+scores.get(p.id),0),B:m.players.filter(p=>p.team==='B').reduce((n,p)=>n+scores.get(p.id),0)}:null;m.lastBattleResolution={serial:++m.resolutionSeq,round:m.round,mode:m.mode,attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,winnerIds:[...winningPlayerIds],winningTeam,teamTotals,players:resolutionPlayers,laneGains:m.players.map(p=>({id:p.id,lane:m.target.lane,before:laneBefore.get(p.id),after:p.lanes[m.target.lane].length,added:p.lanes[m.target.lane].length-laneBefore.get(p.id)})).filter(x=>x.added>0),maxLaneProgress:m.players.map(p=>({id:p.id,before:maxLaneBefore.get(p.id),after:Math.max(...Object.values(p.lanes).map(a=>a.length))}))};const slaykiaAttackEnd=grSlaykiaAttackEndHook(m);m.participants=new Map(m.players.map(p=>[p.id,[]]));m.players.forEach(refill);m.battlePresentation={stage:'settle',round:m.round,mode:m.mode,attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,players:resolutionPlayers,winnerIds:[...winningPlayerIds],teamTotals};renderBattle();await battlePresentationWait(BATTLE_PRESENTATION_RESULT_MS,true);if(state.match!==m||state.screen!=='battle')return;const winners=slaykiaAttackEnd?.specialWinnerTokens?.length?[...slaykiaAttackEnd.specialWinnerTokens]:checkWin();m.battlePresentation=null;if(winners.length){m.busy=false;if(slaykiaAttackEnd?.specialWinnerTokens?.length)log('スレイキア：成功破壊16枚で特殊勝利。');else if(m.contentId==='honey_hunt')log('一列へ実札が七枚以上連続配置され、ハニーハントの勝利条件が成立しました。');return endMatch(winners)}nextRound()}catch(e){clearBattlePresentationWait();if(state.match===m){m.battlePresentation=null;m.busy=false;log(`戦闘停止：${e.message}`);renderBattle()}throw e}}
function checkWin(){if(state.match.mode==='2v2'){const teams=['A','B'].filter(t=>state.match.players.filter(p=>p.team===t).some(p=>Math.max(...Object.values(p.lanes).map(a=>a.length))>=7));return teams}return state.match.players.filter(p=>Math.max(...Object.values(p.lanes).map(a=>a.length))>=7).map(p=>p.id)}
function nextRound(){const m=state.match;m.lastBattleResolution=null;m.round++;m.phase='plan';m.activeId=null;m.target=null;m.battlePresentation=null;m.busy=false;m.players.forEach(p=>p.plan=null);initRoundRuntime(m);log(`第${m.round}巡。行動を予約します。`);renderBattle()}
function ranksFFA(){const ps=state.match.players.map(p=>({p,depth:Math.max(...Object.values(p.lanes).map(a=>a.length))})).sort((a,b)=>b.depth-a.depth);let prev=null,rank=0;return ps.map((x,i)=>{if(prev===null||x.depth!==prev)rank=i+1;prev=x.depth;return{player:x.p,rank,depth:x.depth}})}
function endMatch(winners){const m=state.match;let ranking;if(m.mode==='2v2'){const wt=winners[0];ranking=m.players.map(p=>({player:p,rank:p.team===wt?1:2,depth:Math.max(...Object.values(p.lanes).map(a=>a.length))}))}else ranking=ranksFFA();const me=ranking.find(x=>x.player.human),points=settleBattlePoints(m,me.rank);m.lastResult={ranking,headline:me.rank===1?'勝利':'結果',grade:me.rank,points};state.history.unshift({at:new Date().toISOString(),mode:m.mode,rank:me.rank,rounds:m.round,character:state.playerCharacterId,partner:state.selectedPartnerId,battlePointsEarned:points.earned,quickPoints:points.quick,battlePointsAfter:points.after});save();show('result');renderResult()}
async function renderResult(){const r=state.match?.lastResult;if(!r)return;const m=state.match,ordered=[...r.ranking].sort((a,b)=>a.rank-b.rank),me=ordered.find(x=>x.player.human),mode=({'2p':'二人','4p':'四人','2v2':'二対二'})[m.mode]||m.mode,content=m.contentId==='honey_hunt'?'ハニーハント':'ロード・シールド';$('#resultGrade').textContent=r.grade;$('#resultHeadline').textContent=r.headline;$('#resultSummary').textContent=`${m.round}巡で決着。 ${content}`;$('#resultLeadText')&&($('#resultLeadText').textContent=r.grade===1?'一位で決着':'最終順位');$('#resultLeadSub')&&($('#resultLeadSub').textContent=`${content} / ${mode}`);$('#resultMode')&&($('#resultMode').textContent=mode);$('#resultRounds')&&($('#resultRounds').textContent=`${m.round}巡`);$('#resultLane')&&($('#resultLane').textContent=`${me?.depth??0}枚`);renderResultBattlePoints(r);const box=$('#resultRanking');box.innerHTML='';ordered.forEach(x=>{const d=document.createElement('div');d.className='rankLine';d.innerHTML=`<b>${x.rank}位 ${x.player.human?'あなた':x.player.name}</b><span>最長列 ${x.depth}枚</span>`;box.appendChild(d)});resultMount=await mountChar('.resultWorld',state.playerCharacterId,r.grade===1?'joy':'defeated')}
$('#rematch').onclick=()=>startMatch();$('#leaveMatch').onclick=()=>{state.match=null;navigateRoot('home')};window.__GAMEROAD_QUICK_R14_QA__={version:'FULLGAME-QUICKTIMER-R14',constants:{windowMs:QUICK_WINDOW_MS,perfectRemainMs:QUICK_PERFECT_REMAIN_MS,goodRemainMs:QUICK_GOOD_REMAIN_MS,perfectPoints:QUICK_PERFECT_POINTS,goodPoints:QUICK_GOOD_POINTS,chestTarget:BATTLE_POINTS_CHEST_TARGET},snapshot(){const m=state.match,me=m?.players?.find(p=>p.human),rem=m?quickRemainingMs(m):null;return{screen:state.screen,round:m?.round||null,phase:m?.phase||null,remainingMs:rem,tier:m?quickTierForRemaining(rem):null,quickPoints:me?(m.quickPointsByPlayer?.[me.id]||0):0,awards:me?(m.quickAwards||[]).filter(a=>a.playerId===me.id).map(a=>({...a})):[],battlePoints:state.progression?.battlePoints||0,settlement:m?.battlePointsSettlement?safeClone(m.battlePointsSettlement):null,coilHidden:$('#quickCoil')?.hidden??null,coilText:$('#quickRemain')?.textContent||null,resultText:$('#resultBattlePoints')?.innerText||''}},setRemainingSeconds(sec){const m=state.match;if(!m?.quickWindow)return false;const v=Math.max(0,Math.min(30,Number(sec)||0));m._quickQaNowMs=m.quickWindow.deadlineAt-v*1000;renderQuickTimer();return true},clearTestClock(){const m=state.match;if(!m)return false;delete m._quickQaNowMs;renderQuickTimer();return true},setBattlePoints(n){state.progression={battlePoints:Math.max(0,Math.floor(Number(n)||0))};return state.progression.battlePoints},persistenceProbe(n){state.progression={battlePoints:Math.max(0,Math.floor(Number(n)||0))};save();state.progression={battlePoints:0};load();return{battlePoints:state.progression.battlePoints,storage:state.storage}}};window.GAMEROAD_3D_WALK_QA={version:'BROWSER-3D-BOARD-WALK-INTEGRATION-001',snapshot(){const m=state.match,me=m?.players?.find(p=>p.human);return{screen:state.screen,phase:m?.phase||null,position:me?.position||null,path:me?.plan?.path?[...me.plan.path]:null,avatarState:battleMount?.root?.dataset?.state||null,avatarFacing:battleMount?.facingName||null,nodeCount:document.querySelectorAll('#board .node').length,canvas:{w:$('#fieldCanvas')?.width||0,h:$('#fieldCanvas')?.height||0}}}};window.GAMEROAD_GRASSLAND3D_QA={version:'BROWSER-BOARD-GRASSLAND-3D-R4',snapshot(){const gl=$('#fieldCanvas')?.getContext('webgl');return{...fieldLastStats,webgl:!!gl,contextLost:!!gl?.isContextLost?.(),projectionCount:fieldProjection.size,nodeCount:document.querySelectorAll('#board .node').length,screen:state.screen}}};
window.GAMEROAD_BOARD_REVIEW_QA={version:'BROWSER-BOARD-PLAYTEST-REVIEW-LOOP-001',_rect(e){const b=e?.getBoundingClientRect();return b?{l:b.left,r:b.right,t:b.top,b:b.bottom,w:b.width,h:b.height}:null},_hit(a,b){return!!(a&&b&&Math.min(a.r,b.r)>Math.max(a.l,b.l)&&Math.min(a.b,b.b)>Math.max(a.t,b.t))},snapshot(){const rect=this._rect.bind(this),hit=this._hit.bind(this),nodes=[...document.querySelectorAll('#board .node')].map(e=>({id:e.dataset.pos,rect:rect(e)})),surfaces={publicHud:rect($('#publicTurnHud')),battleInfo:rect(document.querySelector('.battleInfo')),rail:rect(document.querySelector('.battleRail')),top:rect(document.querySelector('.battleTopStatus'))},collisions={};for(const[k,r]of Object.entries(surfaces))collisions[k]=nodes.filter(n=>hit(n.rect,r)).map(n=>n.id);return{screen:state.screen,phase:state.match?.phase||null,viewport:{w:innerWidth,h:innerHeight},profile:fieldCameraState?.kind||null,nodeCount:nodes.length,map:rect($('#battleMap')),collisions,criticalOverlapCount:collisions.publicHud.length+collisions.battleInfo.length,avatar:{state:battleMount?.root?.dataset?.state||null,facing:battleMount?.facingName||null}}},setupSnapshot(){const rect=this._rect.bind(this),h
--- END HIT 5 ---

--- HIT 6 OFFSET=10088481 RANGE=10082481:10094501 ---
tionKey!=='string'||!entry.presentationKey.length)return null;return entry.presentationKey}
function assertNoForbiddenPresentationKeys(value){if(Array.isArray(value)){value.forEach(assertNoForbiddenPresentationKeys);return}if(!value||typeof value!=='object')return;for(const [key,child] of Object.entries(value)){if(FORBIDDEN_PRESENTATION_OUTPUT_KEYS.has(key))throw Error(`forbidden-presentation-output:${key}`);assertNoForbiddenPresentationKeys(child)}}
function buildPublicCardPresentationInputs(reveal,acceptedManifest=FORMAL_CARD_PRESENTATION_MANIFEST){if(!reveal||typeof reveal!=='object')throw Error('invalid-reveal');const round=presentationRound(reveal.round),players=Array.isArray(reveal.players)?reveal.players:[],context=Object.freeze({attackerId:typeof reveal.attackerId==='string'?reveal.attackerId:null,defenderId:typeof reveal.defenderId==='string'?reveal.defenderId:null,targetLane:presentationLane(reveal.lane),targetShield:presentationLane(reveal.shield)}),output=[];for(const player of players){if(!player||typeof player!=='object')continue;const ownerId=presentationText(player.id,'owner-id'),cards=Array.isArray(player.cards)?player.cards:[],occurrence=new Map();for(const card of cards){if(!card||typeof card!=='object'||!PUBLIC_CARD_PRESENTATION_ORIGINS.has(card.origin))continue;const cardId=presentationText(card.cardId,'card-id'),sourceKind=card.origin,counterKey=`${sourceKind}\u0000${cardId}`,publicOrdinal=occurrence.get(counterKey)||0;occurrence.set(counterKey,publicOrdinal+1);const presentationKey=acceptedRevealPresentation(acceptedManifest,cardId),item={schema:PUBLIC_CARD_PRESENTATION_SCHEMA,eventKey:`battle-public:${round}:${ownerId}:${sourceKind}:${cardId}:${publicOrdinal}`,round,ownerId,cardId,cardLabel:typeof card.label==='string'?card.label:cardId,sourceKind,publicOrdinal,visibility:'public',route:presentationKey?'play_formal_asset':'read_confirm_only',presentationKey:presentationKey||null,context};assertNoForbiddenPresentationKeys(item);output.push(Object.freeze(item))}}return Object.freeze(output)}
function validatePublicCardPresentationInputs(items){if(!Array.isArray(items))throw Error('presentation-output-not-array');const keys=new Set();for(const item of items){if(item?.schema!==PUBLIC_CARD_PRESENTATION_SCHEMA)throw Error('presentation-schema-mismatch');if(!PUBLIC_CARD_PRESENTATION_ORIGINS.has(item.sourceKind))throw Error('presentation-origin-not-public');if(item.visibility!=='public')throw Error('presentation-visibility-not-public');if(keys.has(item.eventKey))throw Error('presentation-duplicate-event-key');keys.add(item.eventKey);assertNoForbiddenPresentationKeys(item)}return true}
function clearBattlePresentationWait(){if(battlePresentationRuntime.timer){clearTimeout(battlePresentationRuntime.timer);battlePresentationRuntime.timer=null}battlePresentationRuntime.advance=null}
function battlePresentationWait(ms,allowAdvance=false){const wait=battlePresentationRuntime.fast?0:Math.max(0,Number(ms)||0);if(wait===0)return Promise.resolve('fast');clearBattlePresentationWait();return new Promise(resolve=>{let done=false;const finish=why=>{if(done)return;done=true;clearBattlePresentationWait();resolve(why)};battlePresentationRuntime.timer=setTimeout(()=>finish('timer'),wait);if(allowAdvance)battlePresentationRuntime.advance=()=>finish('advance')})}
function advanceBattlePresentation(){battlePresentationRuntime.advance?.()}
function battlePresentationPlayers(m,winnerIds=[]){return m.players.map(p=>({id:p.id,name:p.name,team:p.team||null,score:(m.participants.get(p.id)||[]).reduce((a,e)=>a+Number(e.currentValue||0),0),winner:winnerIds.includes(p.id),cards:(m.participants.get(p.id)||[]).map(e=>({cardId:e.cardId,label:cardLabel(e.cardId),value:e.currentValue,origin:e.origin}))}))}
async function setBattlePresentation(stage,base={},wait=BATTLE_PRESENTATION_STEP_MS){const m=state.match;if(!m)return false;m.battlePresentation={stage,round:m.round,mode:m.mode,...base};renderBattle();if(state.screen!=='battle'||state.match!==m)return false;await battlePresentationWait(wait,false);return state.match===m&&state.screen==='battle'}
function battleStageCopy(p){const target=`${laneLabel(p.lane)}・${p.shield==='L'?'左':p.shield==='C'?'中央':'右'}シールド`;return({focus:['攻撃先を固定',`${target}へ戦闘の焦点を移します。`],reveal:['バトルカードを公開','シールドと予約済みのバトルカードを公開しました。'],read:['カード読取成功','公開済みのバトルカードと使用シールドを確認しました。'],compare:['パワーを比較','公開された札の現在値を比較します。'],winner:['勝者を確定','比較結果からこの戦闘の勝者を確定しました。'],settle:['列へ反映','勝者の札とチップを同じ列へ反映しました。']})[p.stage]||['戦闘を解決','戦闘の公開情報を確認します。']}
function renderBattlePlayerCards(root,players=[],laneGains=[],presentationEvents=[]){const readKeys=new Set((presentationEvents||[]).map(e=>`${e.ownerId}\u0000${e.sourceKind}\u0000${e.cardId}\u0000${e.publicOrdinal}`));players.forEach(p=>{const gain=laneGains.find(x=>x.id===p.id),d=document.createElement('div');d.className='resolutionPlayer'+(p.winner?' win':'');const occurrence=new Map(),cards=p.cards?.length?p.cards.map(c=>{const sourceKind=c.origin,counterKey=`${sourceKind}\u0000${c.cardId}`,publicOrdinal=occurrence.get(counterKey)||0;occurrence.set(counterKey,publicOrdinal+1);const readTarget=readKeys.has(`${p.id}\u0000${sourceKind}\u0000${c.cardId}\u0000${publicOrdinal}`);return `<span class="resolutionCard ${readTarget?'readTarget':''}"><em>${resolutionOriginLabel(c.origin)}</em>${c.label}<b>${c.value}</b>${readTarget?'<small class="presentationState">読取済み</small>':''}</span>`}).join(''):'<span class="resolutionCard"><em>提出なし</em>0</span>';d.innerHTML=`<div class="resolutionPlayerTop"><b>${p.name}${p.team?` / 組${p.team}`:''}${p.winner?' / 勝者':''}</b><span class="resolutionScore">${Number.isFinite(Number(p.score))?p.score:'—'}</span></div><div class="resolutionCards">${cards}</div>${gain?`<div class="resolutionGain">→ ${laneLabel(gain.lane)} +${gain.added}枚（${gain.before}→${gain.after}）</div>`:''}`;root.appendChild(d)})}
function renderBattleResolution(){const box=$('#battleResolution'),map=$('#battleMap'),m=state.match,p=m?.battlePresentation,r=m?.lastBattleResolution;if(!box)return;const live=!!p&&m?.phase==='resolve';map?.classList.toggle('resolveMode',live);box.classList.toggle('battlePhaseLive',live);box.classList.toggle('noMotion',!!(state.settings.reduceMotion||state.settings.lowPerf));box.classList.remove('hasAdvance');box.dataset.stage=live?p.stage:'result';if(live&&p.stage!=='settle'){box.hidden=false;const [title,detail]=battleStageCopy(p),attacker=m.players.find(x=>x.id===p.attackerId),defender=m.players.find(x=>x.id===p.defenderId),compare=p.players?.length?(m.mode==='2v2'?`組A ${p.teamTotals?.A??p.players.filter(x=>x.team==='A').reduce((n,x)=>n+Number(x.score||0),0)} － ${p.teamTotals?.B??p.players.filter(x=>x.team==='B').reduce((n,x)=>n+Number(x.score||0),0)} 組B`:p.players.map(x=>`${x.name} ${x.score}`).join(' / ')):`${attacker?.name||p.attackerId} → ${defender?.name||p.defenderId}`;box.innerHTML=`<div class="resolutionHead"><div><div class="k">BATTLE PHASE / 第${p.round}巡</div><div class="resolutionWinner">${title}</div></div><div class="resolutionCompare">${detail}<br>${compare}</div></div><div class="resolutionPlayers"></div>`;const root=box.querySelector('.resolutionPlayers');if(p.players?.length)renderBattlePlayerCards(root,p.players,[],p.presentationEvents||[]);return}if(!r){box.hidden=true;box.replaceChildren();return}if(m._battleUiLoggedResolutionSerial!==r.serial){m._battleUiLoggedResolutionSerial=r.serial;log(resolutionPublicLogLine(r));$('#battleLog').textContent=m.log.join('\n');if(typeof ctxRender==='function')ctxRender()}box.hidden=false;const winner=r.mode==='2v2'?`組${r.winningTeam} 勝利`:`${r.players.find(x=>x.winner)?.name||'勝者'} 勝利`,compare=r.mode==='2v2'?`組A ${r.teamTotals?.A??0} － ${r.teamTotals?.B??0} 組B`:`${r.players.map(x=>`${x.name} ${x.score}`).join(' / ')}`;box.innerHTML=`<div class="resolutionHead"><div><div class="k">${live?'BATTLE PHASE':'BATTLE RESULT'} / 第${r.round}巡</div><div class="resolutionWinner">${live?'列へ反映・'+winner:winner}</div></div><div class="resolutionCompare">${compare}</div></div><div class="resolutionPlayers"></div>`;const root=box.querySelector('.resolutionPlayers');renderBattlePlayerCards(root,r.players,r.laneGains);if(live&&!battlePresentationRuntime.fast){box.classList.add('hasAdvance');const btn=document.createElement('button');btn.type='button';btn.className='resolutionAdvance';btn.textContent='次巡へ';btn.setAttribute('aria-label','戦闘結果を確認して次の巡へ進む');btn.onclick=advanceBattlePresentation;box.appendChild(btn)}}
function renderPhase(){const m=state.match;$$('#phaseBar span').forEach(x=>x.classList.toggle('on',x.dataset.ph===m.phase));$('#roundNo').textContent=m.round;$('#phaseTitle').textContent=({plan:'行動を計画',reveal:'ロードを公開',move:'移動を解決',target:'攻撃先を選択',resolve:'戦闘を解決'})[m.phase];const me=m.players[0];$('#manaMeter').textContent=`マナ ${me.awake}/${me.mana.length}`;const hm=$('#honeyMeter');if(m.contentId==='honey_hunt'){hm.hidden=false;const boardHoney=Object.values(m.honey.byNode||{}).reduce((a,b)=>a+b,0);hm.textContent=`盤面ハニー ${boardHoney}`}else hm.hidden=true;$('#battleLog').textContent=m.log.join('\n')}
function renderBattle(){if(!state.match)return;renderPhase();renderPlayers();renderHand();renderTarget();renderBoard();renderBattleResolution();updateBattleAvatar();setupBattleDetails();const plan=state.match.phase==='plan'&&!state.match.busy;$('#readyPlan').disabled=!plan;$('#roadSelect').disabled=!plan;$('#battleSelect').disabled=!plan;$('#clearPath').disabled=!plan}

function historyWins(){return state.history.filter(h=>Number(h.rank)===1).length}
function historyModes(){return new Set(state.history.map(h=>h.mode)).size}
function historyDcg(){return state.history.filter(h=>['2p','4p','2v2'].includes(h.mode)).length}
function renderMissions(){const matches=state.history.length,wins=historyWins(),modes=historyModes(),dcg=historyDcg();$('#missionMatches').textContent=matches;$('#missionWins').textContent=wins;$('#missionModes').textContent=modes;$('#missionSaved').textContent=state.storage==='localStorage'?matches:'起動中';const defs=[['3試合遊ぶ',matches,3],['1勝する',wins,1],['カードゲームを1試合遊ぶ',dcg,1],['10試合遊ぶ',matches,10],['5勝する',wins,5]];const root=$('#missionGrid');root.innerHTML='';defs.forEach(([label,now,target])=>{const d=document.createElement('article');d.className='missionCard'+(now>=target?' done':'');const pct=Math.min(100,Math.round(now/target*100));d.innerHTML=`<div><div class="k">PROGRESS</div><h3>${label}</h3></div><div class="progressLine"><i style="width:${pct}%"></i></div><div class="missionMeta"><span>${Math.min(now,target)} / ${target}</span><b>${now>=target?'達成':'進行中'}</b></div>`;root.appendChild(d)})}
async function renderProfile(){const c=partnerInfo();$('#profileCharName').textContent=c.name;await mountChar('#profileRuntime',c.id,'idle');const matches=state.history.length,wins=historyWins(),modes=historyModes(),avg=matches?Math.round(state.history.reduce((a,h)=>a+(Number(h.rounds)||0),0)/matches):0;$('#profileMatches').textContent=matches;$('#profileWins').textContent=wins;$('#profileModes').textContent=modes;$('#profileRounds').textContent=avg}
function renderShop(){const c=$('#shopPublicCardCount');if(c)c.textContent=`公開カード ${window.__CARD_DATA__.filter(publicCard).length}札`;}
function rarityOf(c){if(!c)return null;if(c.id.startsWith('SP_')||c.id.startsWith('HT_')||c.id.startsWith('DI_')||c.id.startsWith('CL_'))return 'common';return GACHA_RARITY[c.id]||null}
function random01(){try{const a=new Uint32Array(1);crypto.getRandomValues(a);return a[0]/4294967296}catch(e){return Math.random()}}
function weightedRarity(last=false){const x=random01()*100,rows=last?PACK_RATES.last:PACK_RATES.normal;let acc=0;for(const [r,w] of rows){acc+=w;if(x<acc)return r}return rows[rows.length-1][0]}
function pickPackCard(rarity){if(rarity==='ticket')return {ticket:true,display_name:'特別券',rarity:'ticket'};const pool=window.__CARD_DATA__.filter(c=>rarityOf(c)===rarity&&publicCard(c));if(!pool.length)return null;return pool[rndInt(pool.length)]}
function buildPackPreview(){const out=[];for(let i=0;i<7
--- END HIT 6 ---

===== 'function log(' HITS=1 =====

--- HIT 1 OFFSET=9993736 RANGE=9987736:9999749 ---
OOD',points:QUICK_GOOD_POINTS};return{tier:'NONE',points:0}}
function quickShowAward(awards){const live=(awards||[]).filter(a=>a.points>0),box=$('#quickAwardBurst');if(!box||!live.length)return;const tier=live.every(a=>a.tier==='PERFECT')?'PERFECT!':'GOOD',points=live.reduce((n,a)=>n+a.points,0);box.querySelector('strong').textContent=tier;box.querySelector('span').textContent=`クイック +${points}（ロード +${live.find(a=>a.stage==='road')?.points||0} / バトル +${live.find(a=>a.stage==='battle')?.points||0}）`;box.classList.remove('on');box.classList.toggle('noMotion',!!(state.settings.reduceMotion||state.settings.lowPerf));void box.offsetWidth;box.classList.add('on');clearTimeout(quickBurstTimer);quickBurstTimer=setTimeout(()=>box.classList.remove('on'),state.settings.reduceMotion||state.settings.lowPerf?900:1050)}
function quickAcceptStage(m,p,stage,acceptedAt){const key=`${m.round}:${p.id}:${stage}`;if(m.quickReceipts?.has(key))return null;m.quickReceipts?.add(key);const remainingMs=quickRemainingMs(m,acceptedAt),tier=quickTierForRemaining(remainingMs),award={seq:m.quickAwards.length+1,round:m.round,playerId:p.id,stage,tier:tier.tier,points:tier.points,remainingMs:Math.round(remainingMs),acceptedAt};m.quickAwards.push(award);m.quickPointsByPlayer[p.id]=(m.quickPointsByPlayer[p.id]||0)+tier.points;return award}
function quickAcceptPlan(m,p,acceptedAt){const awards=['road','battle'].map(stage=>quickAcceptStage(m,p,stage,acceptedAt)).filter(Boolean);quickShowAward(awards);renderQuickTimer();return awards}
function renderQuickTimer(){const m=state.match,coil=$('#quickCoil'),num=$('#quickRemain'),btn=$('#readyPlan');if(!coil||!num||!btn)return;const visible=!!(m&&state.screen==='battle'&&m.phase==='plan'&&m.quickWindow?.round===m.round);coil.hidden=!visible;if(!visible)return;const rem=quickRemainingMs(m),ratio=rem/QUICK_WINDOW_MS,tier=quickTierForRemaining(rem);coil.style.setProperty('--quick-angle',`${Math.max(0,Math.min(360,ratio*360))}deg`);num.textContent=String(Math.ceil(rem/1000));coil.classList.toggle('good',tier.tier==='GOOD');coil.classList.toggle('late',tier.tier==='NONE'&&rem>0);coil.classList.toggle('expired',rem<=0);coil.classList.toggle('lowPerf',!!state.settings.lowPerf);btn.setAttribute('aria-label',`準備完了。クイック判定 残り${Math.ceil(rem/1000)}秒${tier.tier==='PERFECT'?'、PERFECT圏内':tier.tier==='GOOD'?'、GOOD圏内':''}`)}
function settleBattlePoints(m,rank){if(m.battlePointsSettlement)return m.battlePointsSettlement;const me=m.players.find(p=>p.human),quick=Math.max(0,Math.floor(Number(m.quickPointsByPlayer?.[me?.id])||0)),base=rank===1?3000:600,before=Math.max(0,Math.floor(Number(state.progression?.battlePoints)||0)),earned=base+quick,after=before+earned,awards=(m.quickAwards||[]).filter(a=>a.playerId===me?.id&&a.points>0);state.progression={battlePoints:after};m.battlePointsSettlement={base,quick,earned,before,after,chestTarget:BATTLE_POINTS_CHEST_TARGET,chestLinesBefore:Math.floor(before/BATTLE_POINTS_CHEST_TARGET),chestLinesAfter:Math.floor(after/BATTLE_POINTS_CHEST_TARGET),awards};return m.battlePointsSettlement}
function renderResultBattlePoints(r){const p=r?.points;if(!p)return;$('#resultPointsEarned')&&($('#resultPointsEarned').textContent=`+${p.earned.toLocaleString('ja-JP')}`);const road=p.awards?.filter(a=>a.stage==='road').reduce((n,a)=>n+a.points,0)||0,battle=p.awards?.filter(a=>a.stage==='battle').reduce((n,a)=>n+a.points,0)||0;$('#resultPointsBreakdown')&&($('#resultPointsBreakdown').textContent=`通常 +${p.base.toLocaleString('ja-JP')} / クイック +${p.quick.toLocaleString('ja-JP')}（ロード +${road} / バトル +${battle}）`);const remainder=p.after%BATTLE_POINTS_CHEST_TARGET,reached=p.chestLinesAfter>p.chestLinesBefore,fill=reached?100:(remainder/BATTLE_POINTS_CHEST_TARGET)*100;$('#resultPointsMeterFill')&&($('#resultPointsMeterFill').style.width=`${fill}%`);$('#resultPointsChest')&&($('#resultPointsChest').textContent=reached?`宝箱ライン到達 / 累計 ${p.after.toLocaleString('ja-JP')} BattlePoints`:`宝箱進捗 ${remainder.toLocaleString('ja-JP')} / ${BATTLE_POINTS_CHEST_TARGET.toLocaleString('ja-JP')}`)}
const quickTicker=setInterval(()=>{if(state.screen==='battle')renderQuickTimer()},100);window.addEventListener('pagehide',()=>clearInterval(quickTicker),{once:true});
function initRoundRuntime(m){m.participants=new Map(m.players.map(p=>[p.id,[]]));m.participantSeq=0;m.resolvedAbilityKeys=new Set();m.abilityOrderReceipts=new Map();m.testAutoChoices=false;quickStartRound(m)}

function startMatch(){audioResetForNewMatch();try{if(battleMount&&window.GameRoadThreeCharRuntime?.unmount)window.GameRoadThreeCharRuntime.unmount(battleMount)}catch(e){}battleMount=null;battleMountPromise=null;const battleRuntimeEl=$('#battleRuntime');if(battleRuntimeEl)battleRuntimeEl.replaceChildren();const valid=validateDeck(state.savedDeck,{forBattle:true});if(!valid.ok){toast('札組を修正: '+valid.errors[0]);show('setup');renderSetupDeckStatus();return null}const n=playerCount(state.setupMode),ps=[],diagnosticCpuDeck=[...state.savedDeck.main];for(let i=0;i<n;i++)ps.push(makePlayer(i,i===0,i===0?state.savedDeck.main:diagnosticCpuDeck));if(state.setupMode==='2v2'){ps[0].team='A';ps[1].team='A';ps[2].team='B';ps[3].team='B'}state.match={id:'M'+Date.now(),mode:state.setupMode,contentId:state.setupContent,round:1,phase:'plan',players:ps,activeId:null,target:null,log:[],lastResult:null,lastBattleResolution:null,battlePresentation:null,resolutionSeq:0,projection:['N','E','S','W'],abilityRng:(Date.now()^0x9e3779b9)>>>0,busy:false,honey:{byNode:{},receipts:new Set(),generatedRounds:new Set()},playerDeckRule:{id:DECK_RULE.id,revision:DECK_RULE.revision,mainCount:state.savedDeck.main.length,exCount:state.savedDeck.ex.length}};state.match.selectionLock=grCreateSelectionLock(state.match,'P1');initRoundRuntime(state.match);log(state.setupContent==='honey_hunt'?'第1巡。ハニーハント：一筆移動・終点ハニー全量取得・一列七枚勝利。人物を直接ドラッグして経路を秘密予約します。':'第1巡。全員が行動を予約します。');show('battle');setupBattleDetails();renderBattle();return state.match}
$('#startMatch').addEventListener('click',startMatch);
function log(t){if(!state.match)return;state.match.log.unshift(t);state.match.log=state.match.log.slice(0,30)}
function refill(p){while(p.hand.length<3&&p.deck.length)p.hand.push(p.deck.shift())}
function laneDepth(p,l){return p.lanes[l].length}
function posType(id){if(id.startsWith('C:'))return'center';if(id.startsWith('S:'))return'shield';if(id.startsWith('R:'))return'road';return'corner'}
function parseRoad(id){const [,owner,lane,depth]=id.split(':');return{owner,lane,depth:+depth}}
function projectionFor(viewer){const ids=['P1','P2','P3','P4'];const vi=Math.max(0,ids.indexOf(viewer.id));const arms=['N','E','S','W'];const map={};ids.forEach((id,i)=>map[id]=arms[(i-vi+4)%4]);return map}
function centerIds(){const a=[];for(let r=-1;r<=1;r++)for(let c=-1;c<=1;c++)a.push(`C:${r}:${c}`);return a}
function shieldId(owner,lane){return`S:${owner}:${lane}`};function roadId(owner,lane,d){return`R:${owner}:${lane}:${d}`}
function activeNodes(){const s=new Set(centerIds());['K:NW','K:NE','K:SW','K:SE'].forEach(id=>s.add(id));state.match.players.forEach(p=>{['L','C','R'].forEach(l=>{s.add(shieldId(p.id,l));for(let d=1;d<=p.lanes[l].length&&d<=7;d++)s.add(roadId(p.id,l,d))})});return s}
function centerPort(owner,lane,viewer){const arm=projectionFor(viewer)[owner];const laneIx={L:-1,C:0,R:1}[lane];if(arm==='N')return`C:-1:${laneIx}`;if(arm==='S')return`C:1:${-laneIx}`;if(arm==='W')return`C:${-laneIx}:-1`;return`C:${laneIx}:1`}
function neighbors(id,viewer){const act=activeNodes(),out=[],proj=projectionFor(viewer),ownerFor=arm=>state.match.players.find(p=>proj[p.id]===arm)?.id,cornerDefs={'K:NW':[['N','L'],['W','R']],'K:NE':[['N','R'],['E','L']],'K:SW':[['S','R'],['W','L']],'K:SE':[['S','L'],['E','R']]},cornerRoads=k=>(cornerDefs[k]||[]).map(([arm,lane])=>{const owner=ownerFor(arm);return owner?roadId(owner,lane,1):null}).filter(Boolean);if(id.startsWith('C:')){const [,rs,cs]=id.split(':'),r=+rs,c=+cs;[[r-1,c],[r+1,c],[r,c-1],[r,c+1]].forEach(([rr,cc])=>{const x=`C:${rr}:${cc}`;if(act.has(x))out.push(x)});state.match.players.forEach(p=>['L','C','R'].forEach(l=>{const sh=shieldId(p.id,l);if(centerPort(p.id,l,viewer)===id&&act.has(sh))out.push(sh)}))}else if(id.startsWith('K:')){cornerRoads(id).forEach(x=>{if(act.has(x))out.push(x)})}else if(id.startsWith('S:')){const [,owner,lane]=id.split(':');out.push(centerPort(owner,lane,viewer));const r1=roadId(owner,lane,1);if(act.has(r1))out.push(r1)}else if(id.startsWith('R:')){const {owner,lane,depth}=parseRoad(id);if(depth===1){out.push(shieldId(owner,lane));Object.keys(cornerDefs).forEach(k=>{if(cornerRoads(k).includes(id))out.push(k)})}const a=roadId(owner,lane,depth-1),b=roadId(owner,lane,depth+1);if(depth>1&&act.has(a))out.push(a);if(depth<7&&act.has(b))out.push(b)}return out.filter(x=>act.has(x))}
function shortest(start,end,max,viewer){if(start===end)return[start];const q=[[start]],seen=new Set([start]);while(q.length){const p=q.shift();if(p.length-1>=max)continue;for(const n of neighbors(p[p.length-1],viewer)){if(seen.has(n))continue;const np=[...p,n];if(n===end)return np;seen.add(n);q.push(np)}}return null}
function reachable(p,steps){const q=[[p.position,0]],dist=new Map([[p.position,0]]);while(q.length){const [x,d]=q.shift();if(d>=steps)continue;for(const n of neighbors(x,p)){if(!dist.has(n)){dist.set(n,d+1);q.push([n,d+1])}}}return dist}
/* DOPPELGANGER-NORMAL-RULE-RUNTIME-001 R1
   Recovered user canon (2026-07-26 / 2026-08-06):
   - whole field; road/battle cards only; shields join only after becoming battle cards
   - activates when two or more cards share the same printed number
   - same-number reduction includes the Doppelganger card itself
   - continuous current value affects road movement/order/attack-right/battle value
   - zero does not destroy the card; no recursive modifier loop
   Unknowns remain fail-closed: formal card id/suit/rank, same-name self counting,
   negative values, modifier ordering beyond the recovered examples, hidden-card edge cases. */
const GR_DOPPEL_EFFECT='doppelgangerContinuous';
function grDoppelIs(cardId){return CARDS[cardId]?.effect_type===GR_DOPPEL_EFFECT}
function grDoppelEligibleParticipant(e){return !!e&&['road','active_submission','auto_defense','ability_active_addition'].includes(String(e.origin||''))}
function grDoppelFieldSnapshot(m=state.match){
 const out=[],seen=new Set(); if(!m)return out;
 const add=(key,ownerId,cardId,zone,publicNow=true)=>{const c=CARDS[cardId];if(!c||!publicNow||seen.has(key))return;seen.add(key);out.push(Object.freeze({key,ownerId,cardId,zone,printedValue:Number(c.power),name:String(c.display_name||cardId)}))};
 m.players.forEach(p=>['L','C','R'].forEach(l=>(p.lanes?.[l]||[]).forEach((id,i)=>add(`lane:${p.id}:${l}:${i}:${id}`,p.id,id,'lane',true))));
 if(m.participants)for(const e of participantEntries())if(grDoppelEligibleParticipant(e))add(`participant:${e.key}`,e.ownerId,e.cardId,e.origin,true);
 return out;
}
function grDoppelCensus(cardId,selfKey,snapshot=grDoppelFieldSnapshot(),matchBasis){
 const c=CARDS[cardId],base=Number(c?.power); if(!c||!Number.isFinite(base))return{ok:false,reason:'CARD_NOT_FOUND'};
 if(matchBasis!=='printed')return{ok:false,reason:'DOPPEL_MATCH_BASIS_UNRESOLVED'};
 const self=snapshot.find(x=>x.key===selfKey&&x.cardId===cardId)||snapshot.find(x=>x.cardId===cardId);
 if(!self)return{ok:false,reason:'DOPPEL_SELF_NOT_ON_PUBLIC_FIELD'};
 const sameNumber=snapshot.filter(x=>Number(x.printedValue)===base),sameName=snapshot.filter(x=>x.name===String(c.display_name||cardId));
 return{ok:true,baseValue:base,activation:sameNumber.length>=2,sameNumberCount:sameNumber.length,sameNameCount:sameName.length,selfIncludedInSameNumber:sameNumber.some(x=>x.key===self.key),fieldCount:snapshot.length,matchBasis};
}
function grResolveDoppelgangerValue(cardId,selfKey,options={}){
 const c=CARDS[cardId]; if(!c)return{ok:false,reason:'CARD_NOT_FOUND'};
 if(!grDoppelIs(cardId))return{ok:true,value:Number(c.power),applied:false,reason:'NOT_DOPPELGANGER'};
 const matchBasis=options.mat
--- END HIT 1 ---

===== 'const log=' HITS=0 =====

===== 'function show(' HITS=1 =====

--- HIT 1 OFFSET=9954152 RANGE=9948152:9960166 ---
ray.isArray(d.deck)?d.deck:{}),hasArrays=Array.isArray(deck.main)&&Array.isArray(deck.ex),main=hasArrays?deck.main:[],ex=hasArrays?deck.ex:[];return{saveRevision:Number.isInteger(d.v)&&d.v>=0?d.v:-1,ruleId:deck.ruleId??null,ruleRevision:deck.ruleRevision??null,deckSize:hasArrays?main.length:-1,deckLegal:hasArrays?validateDeck({main,ex}).ok:false}}
function classifySave(inspection){const classification=classifyDeckProjection({inspection,projection:saveProjection(inspection.parsed),authority:SAVE_AUTHORITY});saveRecoveryRuntime.inspection=inspection;saveRecoveryRuntime.classification=classification;return classification}
function markStorageReadFailure(reason){saveRecoveryRuntime.inspection={status:'unreadable',reason};saveRecoveryRuntime.classification={status:'blocked',reason};saveRecoveryRuntime.write={status:'failed',reason};state.storage='memory'}
function cloneObject(v){return v&&typeof v==='object'&&!Array.isArray(v)?JSON.parse(JSON.stringify(v)):{} }
function mergeKnownPack(base,pack){const next={...cloneObject(base),...pack};next.partner={...cloneObject(base?.partner),...pack.partner,profiles:{...cloneObject(base?.partner?.profiles),...cloneObject(pack.partner?.profiles)}};next.settings={...cloneObject(base?.settings),...pack.settings,audio:{...cloneObject(base?.settings?.audio),...cloneObject(pack.settings?.audio)}};next.progression={...cloneObject(base?.progression),...cloneObject(pack.progression)};next.deck={...pack.deck};return next}
function applyParsedSave(d){if(d.v<=2){const legacy=PROD_CHARS.some(c=>c.id===d.selectedCharacter)?d.selectedCharacter:'partner.naki';state.selectedPartnerId=legacy;state.playerCharacterId=legacy;state.partnerProfiles=normalizePartnerProfiles(null)}else{if(PROD_CHARS.some(c=>c.id===d.playerCharacterId))state.playerCharacterId=d.playerCharacterId;if(PROD_CHARS.some(c=>c.id===d.partner?.selectedId))state.selectedPartnerId=d.partner.selectedId;state.partnerProfiles=normalizePartnerProfiles(d.partner?.profiles)}if(d.settings)Object.assign(state.settings,d.settings);normalizeAudioSettings();if(Array.isArray(d.history))state.history=d.history.slice(0,30);state.progression={battlePoints:Math.max(0,Math.floor(Number(d.progression?.battlePoints)||0))};if(['2p','4p','2v2'].includes(d.setupMode))state.setupMode=d.setupMode;if(['road_shield','honey_hunt'].includes(d.setupContent))state.setupContent=d.setupContent;if((d.v===2||d.v===3)&&d.deck&&Array.isArray(d.deck.main)&&Array.isArray(d.deck.ex)){state.savedDeck={main:[...d.deck.main],ex:[...d.deck.ex]};state.savedDeckRule={id:typeof d.deck.ruleId==='string'?d.deck.ruleId:null,revision:Number.isInteger(d.deck.ruleRevision)?d.deck.ruleRevision:null}}state.deckDraft={main:[...state.savedDeck.main],ex:[...state.savedDeck.ex]}}
function prepareCurrentPackCommit(inspection,classification,pack){const nextProjection={saveRevision:3,ruleId:DECK_RULE.id,ruleRevision:DECK_RULE.revision,deckSize:pack.deck.main.length,deckLegal:validateDeck({main:pack.deck.main,ex:pack.deck.ex}).ok};const prepared=prepareExplicitDeckCommit({inspection,currentClassification:classification,path:['deck'],nextDeckRecord:pack.deck,nextProjection,authority:SAVE_AUTHORITY});if(prepared.status!=='prepared')return prepared;const nextRoot={...mergeKnownPack(prepared.nextRoot,pack),v:3};return{...prepared,nextRoot,serialized:JSON.stringify(nextRoot)}}
function save(){const pack=savePack(),read=readStorage(localStorage,SAVE_KEY);if(read.status!=='read'){memorySave=JSON.stringify(pack);markStorageReadFailure(read.reason);return false}const inspection=inspectRawSave(read.rawValue),classification=classifySave(inspection);if(classification.status!=='current'){memorySave=JSON.stringify(pack);state.storage='memory';saveRecoveryRuntime.write={status:'blocked',reason:classification.reason};$('#storageStatus')&&($('#storageStatus').textContent='この起動中に保存');return false}if(state.savedDeckRule?.id!==DECK_RULE.id||state.savedDeckRule?.revision!==DECK_RULE.revision||!validateDeck(state.savedDeck).ok){state.storage='memory';saveRecoveryRuntime.write={status:'blocked',reason:'CURRENT_RUNTIME_DECK_NOT_LEGAL'};return false}const prepared=prepareCurrentPackCommit(inspection,classification,pack);if(prepared.status!=='prepared'){state.storage='memory';saveRecoveryRuntime.write=prepared;return false}const latest=readStorage(localStorage,SAVE_KEY);if(latest.status!=='read'||latest.rawValue!==read.rawValue){state.storage='memory';saveRecoveryRuntime.write={status:'failed',reason:latest.status==='read'?'SAVE_CHANGED_DURING_WRITE':latest.reason};return false}const written=writePreparedSaveVerified(localStorage,SAVE_KEY,prepared);saveRecoveryRuntime.write=written;if(written.status!=='saved'){memorySave=prepared.serialized;state.storage='memory';$('#storageStatus')&&($('#storageStatus').textContent='この起動中に保存');return false}memorySave=prepared.serialized;state.storage='localStorage';$('#saveState')&&($('#saveState').textContent='保存済み');$('#storageStatus')&&($('#storageStatus').textContent='端末保存');return true}
function load(){const read=readStorage(localStorage,SAVE_KEY);if(read.status!=='read'){markStorageReadFailure(read.reason);state.deckDraft={main:[...state.savedDeck.main],ex:[...state.savedDeck.ex]};recoverDeckDraftSession();return saveRecoveryRuntime.classification}state.storage='localStorage';memorySave=read.rawValue;const inspection=inspectRawSave(read.rawValue),classification=classifySave(inspection);if((classification.status==='current'||classification.status==='recognized_legacy')&&inspection.parsed)applyParsedSave(inspection.parsed);normalizeAudioSettings();state.partnerProfiles=normalizePartnerProfiles(state.partnerProfiles);state.progression=state.progression&&typeof state.progression==='object'?state.progression:{battlePoints:0};recoverDeckDraftSession();if(classification.status!=='current')state.storage='memory';return classification}
window.__GAMEROAD_SAVE_RECOVERY__={snapshot:()=>JSON.parse(JSON.stringify(saveRecoveryRuntime)),authority:{...SAVE_AUTHORITY}};
function show(name){const previous=state.screen;const staleToast=$('#toast');if(staleToast?.classList.contains('on')&&staleToast.dataset.toastScreen&&staleToast.dataset.toastScreen!==name){staleToast.classList.remove('on');clearTimeout(toastTimer);delete staleToast.dataset.toastScreen}state.screen=name;document.documentElement.classList.toggle('grCodexHomeActive',name==='home');$$('.screen').forEach(s=>s.classList.toggle('active',s.dataset.screen===name));$('#topMode').textContent=({home:'ホーム',cards:'カード',characters:'パートナー',setup:'バトル準備',battle:'バトル',result:'結果',records:'記録',settings:'設定',missions:'任務',profile:'プロフィール',shop:'ショップ',gacha:'パック'})[name]||name;if(name!=='battle'&&typeof ctxClear==='function')ctxClear();if(name==='home')renderHome();if(name==='cards')renderCards();if(name==='characters')renderCharacters();if(name==='records')renderRecords();if(name==='settings')renderSettings();if(name==='missions')renderMissions();if(name==='profile')renderProfile();if(name==='shop')renderShop();if(name==='gacha')renderGacha();if(name==='setup'){renderSetupDeckStatus();renderSetupShell()}audioOnScreenTransition(previous,name);crossScreenMotionOnShow(previous,name)}
const GAMEROAD_NAV_FALLBACK_PARENT=Object.freeze({cards:'home',characters:'home',setup:'home',missions:'home',profile:'home',shop:'home',gacha:'shop',records:'home',settings:'home'});
const gameroadNav={stack:[]};
function navEntry(screen){return{screen}}
function navigateDetail(target){
  if(!target||target===state.screen)return;
  gameroadNav.stack.push(navEntry(state.screen));
  show(target);
}
function navigateRoot(target){
  gameroadNav.stack.length=0;
  show(target);
}
function navigateBack(){
  const entry=gameroadNav.stack.pop();
  const target=entry?.screen||GAMEROAD_NAV_FALLBACK_PARENT[state.screen]||'home';
  show(target);
}
$$('[data-go]').forEach(b=>b.addEventListener('click',()=>navigateDetail(b.dataset.go)));
$$('[data-root-go]').forEach(b=>b.addEventListener('click',()=>navigateRoot(b.dataset.rootGo)));
$$('[data-back]').forEach(b=>b.addEventListener('click',()=>navigateBack()));

/* CROSSSCREEN-MOTION-PRODUCTION-COVERAGE-001 R1 / native Home motion owner */
const HOME_MOTION_DESTINATIONS=Object.freeze({setup:{dir:'up'},shop:{dir:'right'},characters:{dir:'up-left'},cards:{dir:'down-left'}});
const HOME_MOTION_STATES=Object.freeze({COLLAPSED:'HOME_COLLAPSED',EXPANDED:'HOME_EXPANDED',PRESSED:'HOME_PRESSED',RECOVERING:'HOME_RECOVERING'});
const homeMotionRuntime={state:HOME_MOTION_STATES.EXPANDED,preview:'',busy:false,returnTimer:0,bridgeTimer:0,lastDirection:'',gamepadStamp:0};
function homeMotionReduced(){return !!(state?.settings?.reduceMotion||state?.settings?.lowPerf||window.matchMedia?.('(prefers-reduced-motion: reduce)')?.matches)}
function homeMotionRoot(){return document.querySelector('.screen.home')}
function homeMotionSetState(next){const root=homeMotionRoot(),center=document.getElementById('homePadCenter');homeMotionRuntime.state=next;if(root)root.dataset.homeState=next;const expanded=next!==HOME_MOTION_STATES.COLLAPSED;if(center){center.setAttribute('aria-expanded',String(expanded));center.setAttribute('aria-label',expanded?'ホームメニューを収納':'ホームメニューを展開')}return next}
function homeMotionPreview(target=''){const root=homeMotionRoot();homeMotionRuntime.preview=HOME_MOTION_DESTINATIONS[target]?target:'';if(root)root.dataset.homePreview=homeMotionRuntime.preview;document.querySelectorAll('.homePadChoice').forEach(b=>b.classList.toggle('preview',b.dataset.homeTarget===homeMotionRuntime.preview))}
function homeMotionToggle(){if(homeMotionRuntime.busy)return;homeMotionPreview('');homeMotionSetState(homeMotionRuntime.state===HOME_MOTION_STATES.COLLAPSED?HOME_MOTION_STATES.EXPANDED:HOME_MOTION_STATES.COLLAPSED)}
function crossScreenBridge(direction=''){const bridge=document.getElementById('screenMotionBridge');if(!bridge||homeMotionReduced())return;clearTimeout(homeMotionRuntime.bridgeTimer);bridge.dataset.direction=direction||'neutral';bridge.classList.remove('active');void bridge.offsetWidth;bridge.classList.add('active');homeMotionRuntime.bridgeTimer=setTimeout(()=>bridge.classList.remove('active'),Math.max(240,Number.parseInt(getComputedStyle(document.documentElement).getPropertyValue('--home-motion-ms'))||260)+60)}
function homeMotionNavigate(target){const meta=HOME_MOTION_DESTINATIONS[target];if(!meta||homeMotionRuntime.busy||state.screen!=='home')return false;if(homeMotionRuntime.state===HOME_MOTION_STATES.COLLAPSED){homeMotionSetState(HOME_MOTION_STATES.EXPANDED);return false}homeMotionRuntime.busy=true;homeMotionRuntime.lastDirection=meta.dir;homeMotionPreview(target);homeMotionSetState(HOME_MOTION_STATES.PRESSED);const button=document.querySelector(`.homePadChoice[data-home-target="${target}"]`);button?.classList.add('pressed');crossScreenBridge(meta.dir);const delay=homeMotionReduced()?0:150;setTimeout(()=>{button?.classList.remove('pressed');navigateDetail(target);homeMotionRuntime.busy=false},delay);return true}
function homeMotionOnRender(){const root=homeMotionRoot();if(!root)return;document.documentElement.classList.toggle('r10LowPerf',!!state.settings?.lowPerf);document.documentElement.classList.toggle('r10Reduced',!!state.settings?.reduceMotion);if(homeMotionRuntime.state===HOME_MOTION_STATES.PRESSED)homeMotionSetState(HOME_MOTION_STATES.EXPANDED);root.dataset.homePreview=homeMotionRuntime.preview||''}
function crossScreenMotionOnShow(previous,name){if(previous===name)return;if(previous==='home'){const d=Object.entries(HOME_MOTION_DESTINATIONS).find(([k])=>k===name)?.[1]?.dir||'neutral';crossScreenBridge(d)}else if(name==='home'){clearTimeout(homeMotionRuntime.returnTimer);homeMotionRuntime.busy=false;homeMotionPreview('');homeMotionSetState(HOME_MOTION_STATES.RECOVERING);crossScreenBridge('return');homeMotionRuntime.returnTimer=setTimeout(()=>homeMotionSetState(HOME_MOTION_STATES.EXPANDED),homeMotionReduced()?0:230)}else{crossScreenBridge('neutral')}}
function homeMotionFocusDirection(dir){const btn=document.query
--- END HIT 1 ---

===== 'id="result"' HITS=0 =====

===== 'id="rematch"' HITS=1 =====

--- HIT 1 OFFSET=1085425 RANGE=1079425:1091437 ---
T</div><div class="contentRow"><button class="contentBtn on" data-content="road_shield">ロード・シールド</button><button class="contentBtn" data-content="honey_hunt">ハニーハント</button></div><div class="k">FORMAT</div><div class="modeRow"><button class="modeBtn on" data-mode="2p">二人</button><button class="modeBtn" data-mode="4p">四人</button><button class="modeBtn" data-mode="2v2">二対二</button></div><div class="settingRow"><span>相手</span><b>ボット</b></div><div class="settingRow setupStartPosition" hidden><span>開始位置</span><b>中央・共通開始</b></div><div class="setupDeckRecovery"><div class="setupDeckNote" id="setupDeckNote">札組を確認中</div><button class="btn" id="fixDeckFromSetup" hidden>デッキへ</button></div><button class="btn" id="friendRoomEntry">フレンドルーム</button><button class="btn primary" id="startMatch">盤面へ</button></div></section>
<section class="screen friendroom" data-screen="friendroom"><button class="btn back" data-back aria-label="一つ前の画面へ戻る">← 戻る</button><div id="friendRoomPanel" class="friendRoomPanel"></div></section><section class="screen battle" data-screen="battle"><div class="battleMap" id="battleMap"><canvas id="fieldCanvas" aria-hidden="true"></canvas><svg id="routeSvg" class="routeSvg" aria-hidden="true" viewBox="0 0 100 100" preserveAspectRatio="none"><g id="rangeSurfaceLayer"></g><polyline id="routeLine"></polyline></svg><div class="phaseBar" id="phaseBar"><span data-ph="plan">計画</span><span data-ph="reveal">公開</span><span data-ph="move">移動</span><span data-ph="target">対象</span><span data-ph="resolve">戦闘</span></div><div class="battleTopStatus"><div><span class="k">ROUND <b id="roundNo">1</b></span><strong id="phaseTitle">行動を計画</strong></div><div class="resourceStrip"><span id="manaMeter">マナ 0/7</span><span id="honeyMeter" hidden>ハニー 0</span></div></div><div class="publicTurnHud" id="publicTurnHud" aria-live="polite"><div class="publicPlayerStrip" id="publicPlayerStrip"></div><div class="royalUsageStrip" id="royalUsageStrip" aria-label="各プレイヤーのロイヤルカード使用数"></div></div><div class="board" id="board"></div><div id="boardPlayers" class="boardPlayers" aria-label="盤上参加者"></div><div id="battleRuntime" class="battleAvatar" role="button" aria-label="人物をドラッグして移動経路を予約"></div><div id="originMarker" class="originMarker" aria-hidden="true"></div><div class="quickAwardBurst" id="quickAwardBurst" aria-live="polite" aria-atomic="true"><strong></strong><span></span></div><section id="battleResolution" class="battleResolution" aria-live="polite" aria-atomic="true" hidden></section><div class="battleInfo"><div class="hand" id="hand"></div><div class="planBox"><div class="planSelect"><label>ロード</label><select id="roadSelect"></select></div><div class="planSelect"><label>バトル</label><select id="battleSelect"></select></div><div class="endpointChip"><label>終点</label><b id="endpointText">C:0:0</b></div><button class="iconBtn" id="clearPath" title="経路を戻す" aria-label="経路を戻す">↶</button><div class="quickReadyGroup"><span class="quickCoil" id="quickCoil" aria-hidden="true"><b id="quickRemain">30</b></span><button class="btn primary" id="readyPlan"><span class="quickReadyText">準備完了</span></button></div></div></div><div class="battleRail"><button class="railBtn" id="detailsBtn" aria-label="対戦情報">情報</button><button class="railBtn" id="dangerBtn" aria-label="移動可能範囲">範囲</button><button class="railBtn warn" id="leaveMatch">退出</button></div><div class="targetBox floatingTarget" id="targetBox"><select id="targetPlayer"></select><select id="targetLane"><option value="L">左列</option><option value="C">中央列</option><option value="R">右列</option></select><select id="targetShield"><option value="L">左シールド</option><option value="C">中央シールド</option><option value="R">右シールド</option></select><button class="btn primary" id="confirmTarget">攻撃先を決定</button></div></div><section id="battlePhaseSurface" class="battlePhaseSurface" aria-label="バトルフェイズ専用画面" aria-live="polite" hidden><div class="battlePhaseBackdrop" aria-hidden="true"></div><header class="battlePhaseHeader"><span>BATTLE PHASE</span><b id="battlePhaseRound">第1巡</b><small id="battlePhaseStage">戦闘開始</small></header><div class="battlePhaseCutin" id="battlePhaseCutin" aria-label="緋累ナキ カットイン"><div class="battlePhaseNaki" id="battlePhaseNaki"></div><div class="battlePhaseCutinCopy"><span>PARTNER CUT-IN</span><strong>緋累ナキ</strong></div></div><div class="battlePhaseTarget" id="battlePhaseTarget"></div><div class="battlePhaseResolutionSlot" id="battlePhaseResolutionSlot"></div></section><aside class="battleDrawer" id="battleDrawer" aria-hidden="true"><button class="drawerClose" id="detailsClose" aria-label="閉じる">×</button><div class="k">MATCH INFO</div><div class="players" id="players"></div><details class="battleLogDetails" id="battleLogDetails"><summary>対戦ログ</summary><div class="log" id="battleLog"></div></details></aside></section>
<section class="screen result" data-screen="result"><div class="resultWorld"></div><aside class="resultRank"><div class="k">MATCH RESULT</div><div class="grade" id="resultGrade">1</div><h2 id="resultHeadline">勝利</h2><div class="sub" id="resultSummary"></div></aside><main class="resultBody"><div class="resultLead"><div><div class="k">FINAL ORDER</div><strong id="resultLeadText">対戦結果</strong></div><span id="resultLeadSub">一列七枚を目指した最終順位</span></div><div class="resultMeta"><div><span>FORMAT</span><b id="resultMode">-</b></div><div><span>ROUNDS</span><b id="resultRounds">-</b></div><div><span>YOUR LONGEST</span><b id="resultLane">-</b></div></div><div class="resultPoints" id="resultBattlePoints"><div class="resultPointsTotal"><span>今回のBATTLE POINTS</span><b id="resultPointsEarned">+0</b></div><div class="resultPointsDetail"><span>内訳</span><b id="resultPointsBreakdown">通常 +0 / クイック +0</b><div class="resultPointsMeter" aria-label="10万点宝箱進捗"><i id="resultPointsMeterFill"></i></div><span id="resultPointsChest">宝箱まで 0 / 100,000</span></div></div><div id="resultRanking"></div></main><div class="resultBtns"><button class="btn" data-root-go="home">ホーム</button><button class="btn primary" id="rematch">再戦</button></div></section>

<section class="screen simple" data-screen="missions"><button class="btn back" data-back aria-label="一つ前の画面へ戻る">← 戻る</button><div class="simplePanel"><div class="surfaceHead"><div><div class="k">MISSIONS</div><div class="h">任務</div></div></div><div class="metricGrid"><div class="metric"><b id="missionMatches">0</b><span>完了した対戦</span></div><div class="metric"><b id="missionWins">0</b><span>勝利</span></div><div class="metric"><b id="missionModes">0</b><span>遊んだ対戦形式</span></div><div class="metric"><b id="missionSaved">0</b><span>保存済み記録</span></div></div><div class="missionGrid" id="missionGrid"></div><div class="safeNote" style="margin-top:10px">報酬受取：利用不可</div></div></section>
<section class="screen simple" data-screen="profile"><button class="btn back" data-back aria-label="一つ前の画面へ戻る">← 戻る</button><div class="simplePanel"><div class="surfaceHead"><div><div class="k">PROFILE</div><div class="h">プロフィール</div></div></div><div class="profileLayout"><div class="profileStage"><div id="profileRuntime"></div><div class="profileName"><div class="k">PARTNER</div><h2 id="profileCharName">緋累ナキ</h2></div></div><div class="profileStats"><div class="metricGrid"><div class="metric"><b id="profileMatches">0</b><span>対戦</span></div><div class="metric"><b id="profileWins">0</b><span>勝利</span></div><div class="metric"><b id="profileModes">0</b><span>対戦形式</span></div><div class="metric"><b id="profileRounds">0</b><span>平均巡</span></div></div><div class="profileActions"><button class="btn primary" data-go="characters">パートナーを変更</button><button class="btn" data-go="records">対戦記録</button><button class="btn" data-go="settings">表示設定</button></div></div></div></div></section>
<section class="screen simple" data-screen="shop"><button class="btn back" data-back aria-label="一つ前の画面へ戻る">← 戻る</button><div class="simplePanel"><div class="surfaceHead"><div><div class="k">SHOP</div><div class="h">ショップ</div></div></div><div class="shopGrid"><article class="shopCard"><h2>カードパック</h2><button class="btn primary" data-go="gacha">パックを見る</button></article><article class="shopCard"><span class="statusChip">人物 8人</span><h2>人物・見た目</h2><button class="btn" data-go="characters">人物を見る</button></article><article class="shopCard"><span class="statusChip" id="shopPublicCardCount">公開カード</span><h2>カード・札組</h2><button class="btn" data-go="cards">カードを見る</button></article></div><div class="safeNote" style="margin-top:10px">購入：利用不可</div></div></section>
<section class="screen gachaSurface" data-screen="gacha"><button class="btn back" data-back aria-label="一つ前の画面へ戻る">← 戻る</button><div class="gachaLayout"><div class="gachaStage" id="gachaStage"><video id="gachaVideo" playsinline preload="auto" muted></video><div class="gachaIdle" id="gachaIdle"><div class="gachaCompanion"><div class="gachaGuideRuntime" id="gachaGuideRuntime" aria-label="現在のパートナー"></div><div class="gachaCompanionLabel"><div class="k">CURRENT PARTNER</div><b id="gachaPartnerName">現在のパートナー</b></div></div><div class="gachaOffer"><div class="offerKicker">GAMEROAD CARD PACK</div><div class="gachaCardFan" id="gachaCardFan" aria-label="提供カードの例"></div></div><div class="gachaStageBand"></div></div><div class="gachaResults hidden" id="gachaResultsView"><div class="gachaResultsHead"><b>開封結果</b></div><div class="packResults" id="packResults"></div></div><div class="gachaFocus hidden" id="gachaFocus"><div class="gachaFocusCard" id="gachaFocusCard"></div><div class="gachaFocusCopy"><div class="k">PACK RESULT</div><h3 id="gachaFocusName">カード詳細</h3><p id="gachaFocusRules"></p><div class="gachaFocusActions"><button class="btn primary" id="gachaFocusToCards">カード一覧で見る</button><button class="btn" id="gachaFocusBack">7枚に戻る</button></div></div></div></div><aside class="gachaControls"><div class="k">CARD PACK</div><h2>7枠パック</h2><div class="gachaRateSummary"><b>7枚開封</b><br>1〜6枠は通常抽選 / 7枠は希少以上</div><div class="rateList"><span>第1〜6枠 通常</span><b>70.95%</b><span>希少</span><b>20%</b><span>上位</span><b>7%</b><span>最高</span><b>2%</b><span>特別券</span><b>0.05%</b><span>第7枠</span><b>希少以上</b></div><div class="safeNote">所持追加：なし</div><div class="packActions"><button class="btn primary" id="openPack">7枚を開封</button><button class="btn hidden" id="skipPack">演出をスキップ</button></div></aside></div></section>
<section class="screen simple" data-screen="records"><button class="btn back" data-back aria-label="一つ前の画面へ戻る">← 戻る</button><div class="simplePanel"><div class="k">MATCH HISTORY</div><div class="h" style="font-size:42px">記録</div><div id="recordsList"></div></div></section>
<section class="screen simple" data-screen="settings"><button class="btn back" data-back aria-label="一つ前の画面へ戻る">← 戻る</button><div class="simplePanel"><div class="k">SETTINGS</div><div class="h" style="font-size:42px">設定</div><div class="settingRow"><span>動き軽減</span><button class="btn" id="reduceMotion">OFF</button></div><div class="settingRow"><span>低性能表示</span><button class="btn" id="lowPerf">OFF</button></div><div class="audioDevBox" id="audioDevBox"><div class="k">音響</div><div class="settingRow"><span>音楽音量 <b id="musicVolumeLabel">70</b></span><input id="musicVolume" type="range" min="0" max="100" step="1" value="70"></div><div class="settingRow"><span>効果音量 <b id="sfxVolumeLabel">80</b></span><input id="sfxVolume" type="range" min="0" max="100" step="1" value="80"></div><div class="settingRow"><span>パートナー音声音量 <b id="partnerVoiceVolumeLabel">80</b></span><input id="partnerVoiceVolume" type="range" min="0" max="100" step="1" value="80"></div><div class="settingRow"><span>個別消音</span><span><button class="btn" id="musicMute">音楽 OFF</button> <button class="btn" id="sfxMute">SFX OFF</button> <button class="btn" id="partnerVoiceMute">パートナー音声 OFF</button></span></div><div class="settingRow"><span>開発試聴パック</span><select id="audioPreviewPack"><option value="none">無効（正式既定）</option><option value="unity_candidate_v1">Unity隔離候補を試聴</option></select></div><div class="settingRow"><span>対戦BGM</span><select id="battleMusicKey"><option value="battle_musi
--- END HIT 1 ---

===== 'resultRanking' HITS=3 =====

--- HIT 1 OFFSET=58111 RANGE=52111:64124 ---
ght:97%;filter:drop-shadow(22px 28px 28px rgba(0,0,0,.5))}
.homeCopy.withChar{left:36%;top:14%;width:34%}
.homeCopy .h{font-size:clamp(60px,7.2vw,112px);line-height:.78;letter-spacing:-.07em;text-shadow:0 10px 38px rgba(0,0,0,.36)}
.homeCopy .sub{max-width:360px;font-size:12px;margin-top:14px;color:#c0d6cf}
.homeRail{right:2.5%;top:12%;bottom:13%;width:33%;gap:5px}
.homeCmd{min-height:58px;padding:10px 20px 10px 28px;background:linear-gradient(95deg,rgba(13,48,39,.95),rgba(5,20,17,.94));border-color:rgba(176,229,212,.2);transition:transform .12s ease,border-color .12s ease,background .12s ease}
.homeCmd:hover,.homeCmd:focus-visible{transform:translateX(-7px);border-color:rgba(165,244,217,.75);background:linear-gradient(95deg,rgba(24,78,63,.98),rgba(7,29,23,.96))}
.homeCmd.main{min-height:132px;font-size:31px;padding-left:34px;background:linear-gradient(100deg,rgba(38,104,84,.98),rgba(8,31,25,.97));box-shadow:0 24px 54px rgba(0,0,0,.24)}
.homeCmd.main:before{content:"PLAY";position:absolute;right:20px;top:12px;font-size:10px;letter-spacing:.18em;color:var(--a)}
.homeCmd small{font-size:10px;line-height:1.35}
.homeLiveCard{position:absolute;left:36%;bottom:10%;z-index:6;width:32%;display:grid;grid-template-columns:1.3fr 1fr;gap:6px;padding:10px;border-left:3px solid var(--a);background:linear-gradient(90deg,rgba(4,25,20,.88),rgba(4,19,16,.48));backdrop-filter:blur(5px)}
.homeLiveCard b{display:block;font-size:15px}.homeLiveCard span{display:block;font-size:8px;color:var(--muted);letter-spacing:.08em;margin-bottom:3px}.homeLiveCard .homeLiveWide{grid-column:1/-1;color:#d9ebe5;font-size:9px}
.homeFoot{left:4%;bottom:3.3%;font-size:9px;color:#b6cec6}
.homeDock{right:2.5%;width:33%;bottom:2.4%;gap:4px}.homeDock .dockBtn{background:rgba(3,17,14,.94)}

.setup{background:
  radial-gradient(circle at 26% 45%,rgba(154,240,213,.13),transparent 27%),
  linear-gradient(112deg,#092820 0 51%,#041713 51% 100%);overflow:hidden}
.setup:before{content:"";position:absolute;left:-5%;top:30%;width:58%;height:44%;background:repeating-linear-gradient(105deg,rgba(65,139,111,.2) 0 20px,transparent 20px 42px);clip-path:polygon(0 35%,77% 0,100% 38%,72% 100%,0 82%);pointer-events:none}
.setupHero{left:3%;top:9%;bottom:7%;width:48%;display:block;overflow:hidden}
.setupHeroCopy{position:absolute;left:3%;top:4%;z-index:5;width:72%}
.setupHeroCopy .h{font-size:clamp(52px,6.4vw,96px);line-height:.82;letter-spacing:-.055em}.setupHeroCopy .sub{max-width:430px;margin-top:12px;color:#c1d8d0}
.setupRuntime{position:absolute;left:-7%;right:18%;top:26%;bottom:0;display:flex;align-items:flex-end;justify-content:center;z-index:2}
.setupRuntime .grtc-root,.setupRuntime .grtc-facing{height:100%;width:100%}.setupRuntime .grtc-image{height:96%;max-width:100%;object-fit:contain;filter:drop-shadow(22px 28px 24px rgba(0,0,0,.48))}
.setupGoalCard{position:absolute;left:49%;right:1%;bottom:3%;z-index:6;padding:11px 13px;background:rgba(3,20,16,.9);border-left:3px solid var(--gold);box-shadow:0 18px 36px rgba(0,0,0,.25)}
.setupGoalCard .k{color:var(--gold)}.setupGoalCard b{display:block;font-size:16px;margin:3px 0}.setupGoalCard p{margin:0;color:#b8d0c7;font-size:9px;line-height:1.45}
.setupBox{right:3%;top:11%;bottom:8%;width:45%;padding:22px 24px;align-content:center;gap:10px;background:linear-gradient(145deg,rgba(6,29,23,.97),rgba(3,17,14,.96));border-color:rgba(176,229,212,.25);box-shadow:0 28px 64px rgba(0,0,0,.3)}
.contentRow{gap:7px}.contentBtn,.modeBtn{position:relative;overflow:hidden}.contentBtn{min-height:62px}.contentBtn.on,.modeBtn.on{background:linear-gradient(135deg,#246a56,#123d32);box-shadow:0 0 0 1px rgba(159,239,212,.22) inset}
.contentBtn.on:after,.modeBtn.on:after{content:"SELECTED";position:absolute;right:7px;top:5px;font-size:6px;letter-spacing:.12em;color:var(--a)}
.setupBox #startMatch{min-height:64px;font-size:18px;letter-spacing:.04em;background:linear-gradient(90deg,#286f5b,#174b3e);box-shadow:0 16px 30px rgba(0,0,0,.22)}
.setupBox #startMatch:after{content:"  ▶"}.settingRow{min-height:42px;padding:9px 0}.setupDeckNote{font-size:9px;padding:8px}.setupNote{font-size:9px;line-height:1.45}.setupChoiceSummary{font-size:10px;color:#dcece7;border-top:1px solid var(--line);padding-top:8px}

.result{background:linear-gradient(112deg,#08261f 0 45%,#03120f 45% 100%)}
.resultWorld{inset:0 50% 0 0;background:
  radial-gradient(circle at 38% 39%,rgba(255,215,139,.21),transparent 24%),
  linear-gradient(148deg,#194d3e,#061812)}
.resultWorld:after{content:"";position:absolute;inset:46% -6% 0 0;background:repeating-linear-gradient(105deg,rgba(70,143,114,.18) 0 22px,transparent 22px 46px);clip-path:polygon(0 32%,72% 0,100% 26%,100% 100%,0 100%);pointer-events:none}
.resultWorld .grtc-root{inset:10% 2% 1% 5%;justify-content:flex-start}.resultWorld .grtc-image{height:88%;max-width:93%;object-fit:contain;filter:drop-shadow(26px 28px 24px rgba(0,0,0,.5))}
.resultRank{left:3.5%;top:11%;width:36%}.resultRank .k{font-size:10px}.grade{font-size:clamp(116px,17vw,260px);line-height:.72;letter-spacing:-.08em;text-shadow:0 18px 44px rgba(0,0,0,.38)}
.resultRank h2{font-size:clamp(30px,3.4vw,56px);margin:18px 0 3px}.resultRank .sub{font-size:11px;color:#c0d6ce}
.resultBody{left:52%;right:3%;top:12%;bottom:20%;padding:18px 20px;background:linear-gradient(145deg,rgba(7,32,26,.98),rgba(3,18,15,.97));border-color:rgba(176,229,212,.24);box-shadow:0 24px 58px rgba(0,0,0,.28);display:grid;grid-template-rows:auto auto 1fr;gap:10px;overflow:hidden}
.resultLead{display:flex;align-items:end;justify-content:space-between;gap:10px;border-bottom:1px solid var(--line);padding-bottom:11px}.resultLead strong{font-size:25px}.resultLead span{font-size:9px;color:var(--muted);text-align:right}
.resultMeta{display:grid;grid-template-columns:repeat(3,1fr);gap:5px}.resultMeta div{padding:8px 9px;background:#071d18;border:1px solid rgba(176,229,212,.16)}.resultMeta span{display:block;font-size:7px;letter-spacing:.1em;color:var(--muted)}.resultMeta b{font-size:13px}
#resultRanking{overflow:auto}.rankLine{min-height:58px;align-items:center;padding:10px 12px;background:rgba(9,37,30,.52);border:1px solid rgba(176,229,212,.12);margin-bottom:5px}.rankLine:first-child{border-color:rgba(255,212,126,.55);background:linear-gradient(90deg,rgba(86,69,27,.35),rgba(9,37,30,.6))}.rankLine b{font-size:15px}.rankLine span{font-size:10px;color:#c2d6cf}
.resultBtns{right:3%;bottom:6%;gap:7px}.resultBtns .btn{min-width:130px;min-height:54px}.resultBtns .primary{min-width:190px;font-size:16px}

@media(max-width:900px){
 .homeCopy.withChar{left:35%;width:34%}.homeCopy .h{font-size:54px}.homeLiveCard{left:35%;width:33%;bottom:9%}.homeCmd.main{min-height:88px;font-size:23px}
 .setupHero{left:2%;width:47%}.setupHeroCopy .h{font-size:46px}.setupBox{right:2%;width:49%;padding:12px 13px}.setupRuntime{right:10%}.setupGoalCard{left:39%;right:1%;padding:7px 8px}.setupGoalCard b{font-size:12px}
 .resultRank{left:2.5%;width:39%}.resultBody{left:50%;right:2%;padding:10px}.resultLead strong{font-size:19px}.resultMeta div{padding:5px}.resultBtns{right:2%;bottom:3%}
}
@media(max-height:470px) and (orientation:landscape){
 .homeCopy.withChar{top:10%;left:33%}.homeCopy .h{font-size:39px}.homeCopy .sub{font-size:8px;margin-top:5px}.heroRuntime{width:34%}.homeRail{top:5%;bottom:23%;width:35%}.homeCmd{min-height:34px;padding:4px 12px 4px 18px}.homeCmd.main{min-height:56px;font-size:17px}.homeLiveCard{left:33%;bottom:6%;width:31%;padding:5px}.homeLiveCard b{font-size:10px}.homeLiveCard span,.homeLiveCard .homeLiveWide{font-size:6px}.homeDock{width:35%}
 .setup{padding-top:48px}.setupHero{top:7%;bottom:2%;width:47%}.setupHeroCopy{top:1%}.setupHeroCopy .h{font-size:30px}.setupHeroCopy .sub{font-size:7px;margin-top:3px}.setupRuntime{left:-4%;right:26%;top:24%}.setupGoalCard{left:43%;bottom:1%;padding:4px 6px}.setupGoalCard b{font-size:9px}.setupGoalCard p{display:none}.setupBox{top:50px;bottom:4px;padding:6px 8px;gap:3px;overflow:auto}.contentBtn{min-height:38px;font-size:8px}.modeBtn{min-height:35px}.settingRow{min-height:24px;padding:3px 0;font-size:8px}.setupBox #startMatch{min-height:38px;font-size:11px}.setupChoiceSummary{font-size:7px;padding-top:3px}.setupNote{font-size:6px}
 .resultWorld{right:54%}.resultRank{top:9%;width:35%}.grade{font-size:90px}.resultRank h2{font-size:22px;margin:7px 0 2px}.resultRank .sub{font-size:8px}.resultBody{left:48%;top:11%;bottom:18%;padding:7px;gap:4px}.resultLead{padding-bottom:4px}.resultLead strong{font-size:14px}.resultLead span{font-size:6px}.resultMeta{gap:3px}.resultMeta div{padding:3px 4px}.resultMeta b{font-size:9px}.resultMeta span{font-size:5px}.rankLine{min-height:29px;margin-bottom:2px;padding:3px 6px}.rankLine b{font-size:8px}.rankLine span{font-size:6px}.resultBtns{bottom:2%}.resultBtns .btn{min-height:36px;min-width:80px}.resultBtns .primary{min-width:110px;font-size:10px}
}
@media(max-width:540px) and (orientation:portrait){
 .homeScene{width:100%;height:62%}.heroRuntime{left:-8%;top:15%;width:55%;height:46%}.homeCopy.withChar{left:41%;top:12%;width:54%}.homeCopy .h{font-size:46px}.homeCopy .sub{font-size:9px}.homeLiveCard{left:40%;bottom:auto;top:42%;width:55%;grid-template-columns:1fr 1fr}.homeRail{left:4%;right:4%;top:auto;bottom:14%;width:auto;height:34%;display:grid;grid-template-columns:1.4fr 1fr 1fr;align-content:end}.homeCmd{min-height:50px;transform:none;clip-path:none;padding:7px 9px}.homeCmd.main{grid-row:span 2;min-height:108px;font-size:20px;transform:none}.homeCmd.main:before{display:none}.homeDock{left:4%;right:4%;bottom:2%;width:auto;grid-template-columns:repeat(4,1fr)}
 .setupHero{left:0;top:48px;width:100%;height:39%;bottom:auto}.setupHeroCopy{left:4%;top:6%;width:60%}.setupHeroCopy .h{font-size:38px}.setupHeroCopy .sub{font-size:8px}.setupRuntime{left:40%;right:1%;top:4%;bottom:0}.setupGoalCard{left:4%;right:42%;bottom:3%;padding:5px}.setupGoalCard b{font-size:10px}.setupGoalCard p{display:none}.setupBox{left:3%;right:3%;top:41%;bottom:3%;width:auto;padding:8px 10px;gap:5px;overflow:auto}.contentBtn{min-height:48px}.modeBtn{min-height:46px}.settingRow{min-height:30px;padding:5px 0;font-size:9px}.setupBox #startMatch{min-height:48px}
 .resultWorld{inset:0 0 49% 0}.resultWorld .grtc-root{inset:5% 2% 0 33%;justify-content:center}.resultWorld .grtc-image{height:92%}.resultRank{left:4%;top:8%;width:43%}.grade{font-size:95px}.resultRank h2{font-size:25px;margin:10px 0 2px}.resultRank .sub{font-size:8px}.resultBody{left:3%;right:3%;top:52%;bottom:15%;padding:8px}.resultLead strong{font-size:17px}.resultMeta div{padding:5px}.rankLine{min-height:34px;padding:4px 7px}.rankLine b{font-size:10px}.rankLine span{font-size:8px}.resultBtns{left:3%;right:3%;bottom:3%;justify-content:stretch}.resultBtns .btn{flex:1;min-width:0}.resultBtns .primary{min-width:0}
}


@media (min-width:541px){
  .charStage .charRuntime{left:48%;right:3%;top:5%;bottom:3%;justify-content:flex-end}
  .charStage .charName{left:5%;bottom:8%;width:40%;max-width:40%}
  .charStage .charName h2{font-size:clamp(27px,3.4vw,48px);line-height:1.05}
  .charStage .charName .sub{font-size:clamp(9px,.85vw,12px);line-height:1.45}
}

/* Gacha R1 viewport-specific composition: preserve a visible collectible/character at short landscape without shrinking primary actions below 44px. */
@media(max-width:900px){.gachaLayout{grid-template-columns:minmax(0,1.55fr) minmax(230px,.72fr)}.gachaControls{padding:8px}.gachaControls h2{font-size:20px}.gachaIdle{grid-template-columns:.82fr 1.18fr}.gachaCompanionLabel{left:10px;bottom:10px}.gachaOffer{padding:5%}.gachaOffer h2{font-size:clamp(24px,4.3vw,38px)}.gachaCardFan{min-height:120px;margin:8px 0}.packResults{gap:4px}.packCard{min-height:104px;padding:5px}}
@media(max-height:470px) and (orientation:landscape){.gachaResults .packCard{aspect-ratio:auto;height:56%;min-height:118px}.gachaControls .gachaLead{display:none}.gachaRateSummary{margin:5px 0 3px;padding:4px 6px;font-size:7px}.gachaControls .safeNote{display:none}.rateList{margin:3px 0 5px;padding
--- END HIT 1 ---

--- HIT 2 OFFSET=1085293 RANGE=1079293:1091306 ---
 class="setupGoalCard"><div class="k">勝利</div><b id="setupGoalTitle">一列7枚</b></div></div><div class="setupBox"><div class="k">CONTENT</div><div class="contentRow"><button class="contentBtn on" data-content="road_shield">ロード・シールド</button><button class="contentBtn" data-content="honey_hunt">ハニーハント</button></div><div class="k">FORMAT</div><div class="modeRow"><button class="modeBtn on" data-mode="2p">二人</button><button class="modeBtn" data-mode="4p">四人</button><button class="modeBtn" data-mode="2v2">二対二</button></div><div class="settingRow"><span>相手</span><b>ボット</b></div><div class="settingRow setupStartPosition" hidden><span>開始位置</span><b>中央・共通開始</b></div><div class="setupDeckRecovery"><div class="setupDeckNote" id="setupDeckNote">札組を確認中</div><button class="btn" id="fixDeckFromSetup" hidden>デッキへ</button></div><button class="btn" id="friendRoomEntry">フレンドルーム</button><button class="btn primary" id="startMatch">盤面へ</button></div></section>
<section class="screen friendroom" data-screen="friendroom"><button class="btn back" data-back aria-label="一つ前の画面へ戻る">← 戻る</button><div id="friendRoomPanel" class="friendRoomPanel"></div></section><section class="screen battle" data-screen="battle"><div class="battleMap" id="battleMap"><canvas id="fieldCanvas" aria-hidden="true"></canvas><svg id="routeSvg" class="routeSvg" aria-hidden="true" viewBox="0 0 100 100" preserveAspectRatio="none"><g id="rangeSurfaceLayer"></g><polyline id="routeLine"></polyline></svg><div class="phaseBar" id="phaseBar"><span data-ph="plan">計画</span><span data-ph="reveal">公開</span><span data-ph="move">移動</span><span data-ph="target">対象</span><span data-ph="resolve">戦闘</span></div><div class="battleTopStatus"><div><span class="k">ROUND <b id="roundNo">1</b></span><strong id="phaseTitle">行動を計画</strong></div><div class="resourceStrip"><span id="manaMeter">マナ 0/7</span><span id="honeyMeter" hidden>ハニー 0</span></div></div><div class="publicTurnHud" id="publicTurnHud" aria-live="polite"><div class="publicPlayerStrip" id="publicPlayerStrip"></div><div class="royalUsageStrip" id="royalUsageStrip" aria-label="各プレイヤーのロイヤルカード使用数"></div></div><div class="board" id="board"></div><div id="boardPlayers" class="boardPlayers" aria-label="盤上参加者"></div><div id="battleRuntime" class="battleAvatar" role="button" aria-label="人物をドラッグして移動経路を予約"></div><div id="originMarker" class="originMarker" aria-hidden="true"></div><div class="quickAwardBurst" id="quickAwardBurst" aria-live="polite" aria-atomic="true"><strong></strong><span></span></div><section id="battleResolution" class="battleResolution" aria-live="polite" aria-atomic="true" hidden></section><div class="battleInfo"><div class="hand" id="hand"></div><div class="planBox"><div class="planSelect"><label>ロード</label><select id="roadSelect"></select></div><div class="planSelect"><label>バトル</label><select id="battleSelect"></select></div><div class="endpointChip"><label>終点</label><b id="endpointText">C:0:0</b></div><button class="iconBtn" id="clearPath" title="経路を戻す" aria-label="経路を戻す">↶</button><div class="quickReadyGroup"><span class="quickCoil" id="quickCoil" aria-hidden="true"><b id="quickRemain">30</b></span><button class="btn primary" id="readyPlan"><span class="quickReadyText">準備完了</span></button></div></div></div><div class="battleRail"><button class="railBtn" id="detailsBtn" aria-label="対戦情報">情報</button><button class="railBtn" id="dangerBtn" aria-label="移動可能範囲">範囲</button><button class="railBtn warn" id="leaveMatch">退出</button></div><div class="targetBox floatingTarget" id="targetBox"><select id="targetPlayer"></select><select id="targetLane"><option value="L">左列</option><option value="C">中央列</option><option value="R">右列</option></select><select id="targetShield"><option value="L">左シールド</option><option value="C">中央シールド</option><option value="R">右シールド</option></select><button class="btn primary" id="confirmTarget">攻撃先を決定</button></div></div><section id="battlePhaseSurface" class="battlePhaseSurface" aria-label="バトルフェイズ専用画面" aria-live="polite" hidden><div class="battlePhaseBackdrop" aria-hidden="true"></div><header class="battlePhaseHeader"><span>BATTLE PHASE</span><b id="battlePhaseRound">第1巡</b><small id="battlePhaseStage">戦闘開始</small></header><div class="battlePhaseCutin" id="battlePhaseCutin" aria-label="緋累ナキ カットイン"><div class="battlePhaseNaki" id="battlePhaseNaki"></div><div class="battlePhaseCutinCopy"><span>PARTNER CUT-IN</span><strong>緋累ナキ</strong></div></div><div class="battlePhaseTarget" id="battlePhaseTarget"></div><div class="battlePhaseResolutionSlot" id="battlePhaseResolutionSlot"></div></section><aside class="battleDrawer" id="battleDrawer" aria-hidden="true"><button class="drawerClose" id="detailsClose" aria-label="閉じる">×</button><div class="k">MATCH INFO</div><div class="players" id="players"></div><details class="battleLogDetails" id="battleLogDetails"><summary>対戦ログ</summary><div class="log" id="battleLog"></div></details></aside></section>
<section class="screen result" data-screen="result"><div class="resultWorld"></div><aside class="resultRank"><div class="k">MATCH RESULT</div><div class="grade" id="resultGrade">1</div><h2 id="resultHeadline">勝利</h2><div class="sub" id="resultSummary"></div></aside><main class="resultBody"><div class="resultLead"><div><div class="k">FINAL ORDER</div><strong id="resultLeadText">対戦結果</strong></div><span id="resultLeadSub">一列七枚を目指した最終順位</span></div><div class="resultMeta"><div><span>FORMAT</span><b id="resultMode">-</b></div><div><span>ROUNDS</span><b id="resultRounds">-</b></div><div><span>YOUR LONGEST</span><b id="resultLane">-</b></div></div><div class="resultPoints" id="resultBattlePoints"><div class="resultPointsTotal"><span>今回のBATTLE POINTS</span><b id="resultPointsEarned">+0</b></div><div class="resultPointsDetail"><span>内訳</span><b id="resultPointsBreakdown">通常 +0 / クイック +0</b><div class="resultPointsMeter" aria-label="10万点宝箱進捗"><i id="resultPointsMeterFill"></i></div><span id="resultPointsChest">宝箱まで 0 / 100,000</span></div></div><div id="resultRanking"></div></main><div class="resultBtns"><button class="btn" data-root-go="home">ホーム</button><button class="btn primary" id="rematch">再戦</button></div></section>

<section class="screen simple" data-screen="missions"><button class="btn back" data-back aria-label="一つ前の画面へ戻る">← 戻る</button><div class="simplePanel"><div class="surfaceHead"><div><div class="k">MISSIONS</div><div class="h">任務</div></div></div><div class="metricGrid"><div class="metric"><b id="missionMatches">0</b><span>完了した対戦</span></div><div class="metric"><b id="missionWins">0</b><span>勝利</span></div><div class="metric"><b id="missionModes">0</b><span>遊んだ対戦形式</span></div><div class="metric"><b id="missionSaved">0</b><span>保存済み記録</span></div></div><div class="missionGrid" id="missionGrid"></div><div class="safeNote" style="margin-top:10px">報酬受取：利用不可</div></div></section>
<section class="screen simple" data-screen="profile"><button class="btn back" data-back aria-label="一つ前の画面へ戻る">← 戻る</button><div class="simplePanel"><div class="surfaceHead"><div><div class="k">PROFILE</div><div class="h">プロフィール</div></div></div><div class="profileLayout"><div class="profileStage"><div id="profileRuntime"></div><div class="profileName"><div class="k">PARTNER</div><h2 id="profileCharName">緋累ナキ</h2></div></div><div class="profileStats"><div class="metricGrid"><div class="metric"><b id="profileMatches">0</b><span>対戦</span></div><div class="metric"><b id="profileWins">0</b><span>勝利</span></div><div class="metric"><b id="profileModes">0</b><span>対戦形式</span></div><div class="metric"><b id="profileRounds">0</b><span>平均巡</span></div></div><div class="profileActions"><button class="btn primary" data-go="characters">パートナーを変更</button><button class="btn" data-go="records">対戦記録</button><button class="btn" data-go="settings">表示設定</button></div></div></div></div></section>
<section class="screen simple" data-screen="shop"><button class="btn back" data-back aria-label="一つ前の画面へ戻る">← 戻る</button><div class="simplePanel"><div class="surfaceHead"><div><div class="k">SHOP</div><div class="h">ショップ</div></div></div><div class="shopGrid"><article class="shopCard"><h2>カードパック</h2><button class="btn primary" data-go="gacha">パックを見る</button></article><article class="shopCard"><span class="statusChip">人物 8人</span><h2>人物・見た目</h2><button class="btn" data-go="characters">人物を見る</button></article><article class="shopCard"><span class="statusChip" id="shopPublicCardCount">公開カード</span><h2>カード・札組</h2><button class="btn" data-go="cards">カードを見る</button></article></div><div class="safeNote" style="margin-top:10px">購入：利用不可</div></div></section>
<section class="screen gachaSurface" data-screen="gacha"><button class="btn back" data-back aria-label="一つ前の画面へ戻る">← 戻る</button><div class="gachaLayout"><div class="gachaStage" id="gachaStage"><video id="gachaVideo" playsinline preload="auto" muted></video><div class="gachaIdle" id="gachaIdle"><div class="gachaCompanion"><div class="gachaGuideRuntime" id="gachaGuideRuntime" aria-label="現在のパートナー"></div><div class="gachaCompanionLabel"><div class="k">CURRENT PARTNER</div><b id="gachaPartnerName">現在のパートナー</b></div></div><div class="gachaOffer"><div class="offerKicker">GAMEROAD CARD PACK</div><div class="gachaCardFan" id="gachaCardFan" aria-label="提供カードの例"></div></div><div class="gachaStageBand"></div></div><div class="gachaResults hidden" id="gachaResultsView"><div class="gachaResultsHead"><b>開封結果</b></div><div class="packResults" id="packResults"></div></div><div class="gachaFocus hidden" id="gachaFocus"><div class="gachaFocusCard" id="gachaFocusCard"></div><div class="gachaFocusCopy"><div class="k">PACK RESULT</div><h3 id="gachaFocusName">カード詳細</h3><p id="gachaFocusRules"></p><div class="gachaFocusActions"><button class="btn primary" id="gachaFocusToCards">カード一覧で見る</button><button class="btn" id="gachaFocusBack">7枚に戻る</button></div></div></div></div><aside class="gachaControls"><div class="k">CARD PACK</div><h2>7枠パック</h2><div class="gachaRateSummary"><b>7枚開封</b><br>1〜6枠は通常抽選 / 7枠は希少以上</div><div class="rateList"><span>第1〜6枠 通常</span><b>70.95%</b><span>希少</span><b>20%</b><span>上位</span><b>7%</b><span>最高</span><b>2%</b><span>特別券</span><b>0.05%</b><span>第7枠</span><b>希少以上</b></div><div class="safeNote">所持追加：なし</div><div class="packActions"><button class="btn primary" id="openPack">7枚を開封</button><button class="btn hidden" id="skipPack">演出をスキップ</button></div></aside></div></section>
<section class="screen simple" data-screen="records"><button class="btn back" data-back aria-label="一つ前の画面へ戻る">← 戻る</button><div class="simplePanel"><div class="k">MATCH HISTORY</div><div class="h" style="font-size:42px">記録</div><div id="recordsList"></div></div></section>
<section class="screen simple" data-screen="settings"><button class="btn back" data-back aria-label="一つ前の画面へ戻る">← 戻る</button><div class="simplePanel"><div class="k">SETTINGS</div><div class="h" style="font-size:42px">設定</div><div class="settingRow"><span>動き軽減</span><button class="btn" id="reduceMotion">OFF</button></div><div class="settingRow"><span>低性能表示</span><button class="btn" id="lowPerf">OFF</button></div><div class="audioDevBox" id="audioDevBox"><div class="k">音響</div><div class="settingRow"><span>音楽音量 <b id="musicVolumeLabel">70</b></span><input id="musicVolume" type="range" min="0" max="100" step="1" value="70"></div><div class="settingRow"><span>効果音量 <b id="sfxVolumeLabel">80</b></span><input id="sfxVolume" type="range" min="0" max="100" step="1" value="80"></div><div class="settingRow"><span>パートナー音声音量 <b id="partnerVoiceVolumeLabel">80</b></span><input id="partnerVoiceVolume" type="range" min="0" max="100" step="1" value="80"></div><div class="settingRow"><span>個別消音</span><span><button class="btn" id="musicMute">音楽 OFF</button> <button class="btn" id="sfxMute">SFX OFF</button> <button class="btn" id="partnerVoiceMute">パートナー音声 OFF</button></span></div><div class="settingRow"><span>開発試聴パック</span><select id="audioPreviewPack"><option value="none">無効（正式既定）</option><option value="unity_candidate_v1">U
--- END HIT 2 ---

--- HIT 3 OFFSET=10074174 RANGE=10068174:10080187 ---
.team:ties[matchRnd(ties.length)]}winningPlayerIds=m.players.filter(p=>p.team===team).map(p=>p.id);log(`戦闘：組${team}が勝利。`)}else{const max=Math.max(...m.players.map(p=>scores.get(p.id)));const ties=m.players.filter(p=>scores.get(p.id)===max);const active=m.players.find(p=>p.id===m.activeId);const winner=ties.length===1?ties[0]:(ties.some(p=>p.id===active.id)?active:ties[matchRnd(ties.length)]);winningPlayerIds=[winner.id];log(`戦闘：${winner.name}が勝利。`)}if(!await setBattlePresentation('winner',{attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,players:battlePresentationPlayers(m,winningPlayerIds),winnerIds:[...winningPlayerIds],teamTotals:m.mode==='2v2'?{A:m.players.filter(p=>p.team==='A').reduce((n,p)=>n+scores.get(p.id),0),B:m.players.filter(p=>p.team==='B').reduce((n,p)=>n+scores.get(p.id),0)}:null}))return;const humanPlayer=m.players.find(p=>p.human);if(humanPlayer&&battleMount){if(winningPlayerIds.includes(humanPlayer.id))await setBattleRuntimeState('joy');else await setBattleRuntimeState('hit');}const resolutionPlayers=m.players.map(p=>({id:p.id,name:p.name,team:p.team||null,score:scores.get(p.id),winner:winningPlayerIds.includes(p.id),cards:(m.participants.get(p.id)||[]).map(e=>({cardId:e.cardId,label:cardLabel(e.cardId),value:e.currentValue,origin:e.origin}))}));const laneBefore=new Map(m.players.map(p=>[p.id,p.lanes[m.target.lane].length])),maxLaneBefore=new Map(m.players.map(p=>[p.id,Math.max(...Object.values(p.lanes).map(a=>a.length))]));m.players.forEach(p=>{const parts=m.participants.get(p.id)||[];if(winningPlayerIds.includes(p.id)){const lane=p.lanes[m.target.lane];parts.forEach(e=>lane.push(e.cardId));if(p.chip.length)p.chip.splice(0).forEach(id=>lane.push(id))}else parts.forEach(e=>p.chip.push(e.cardId))});const winningTeam=m.mode==='2v2'?(m.players.find(p=>winningPlayerIds.includes(p.id))?.team||null):null,teamTotals=m.mode==='2v2'?{A:m.players.filter(p=>p.team==='A').reduce((n,p)=>n+scores.get(p.id),0),B:m.players.filter(p=>p.team==='B').reduce((n,p)=>n+scores.get(p.id),0)}:null;m.lastBattleResolution={serial:++m.resolutionSeq,round:m.round,mode:m.mode,attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,winnerIds:[...winningPlayerIds],winningTeam,teamTotals,players:resolutionPlayers,laneGains:m.players.map(p=>({id:p.id,lane:m.target.lane,before:laneBefore.get(p.id),after:p.lanes[m.target.lane].length,added:p.lanes[m.target.lane].length-laneBefore.get(p.id)})).filter(x=>x.added>0),maxLaneProgress:m.players.map(p=>({id:p.id,before:maxLaneBefore.get(p.id),after:Math.max(...Object.values(p.lanes).map(a=>a.length))}))};const slaykiaAttackEnd=grSlaykiaAttackEndHook(m);m.participants=new Map(m.players.map(p=>[p.id,[]]));m.players.forEach(refill);m.battlePresentation={stage:'settle',round:m.round,mode:m.mode,attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,players:resolutionPlayers,winnerIds:[...winningPlayerIds],teamTotals};renderBattle();await battlePresentationWait(BATTLE_PRESENTATION_RESULT_MS,true);if(state.match!==m||state.screen!=='battle')return;const winners=slaykiaAttackEnd?.specialWinnerTokens?.length?[...slaykiaAttackEnd.specialWinnerTokens]:checkWin();m.battlePresentation=null;if(winners.length){m.busy=false;if(slaykiaAttackEnd?.specialWinnerTokens?.length)log('スレイキア：成功破壊16枚で特殊勝利。');else if(m.contentId==='honey_hunt')log('一列へ実札が七枚以上連続配置され、ハニーハントの勝利条件が成立しました。');return endMatch(winners)}nextRound()}catch(e){clearBattlePresentationWait();if(state.match===m){m.battlePresentation=null;m.busy=false;log(`戦闘停止：${e.message}`);renderBattle()}throw e}}
function checkWin(){if(state.match.mode==='2v2'){const teams=['A','B'].filter(t=>state.match.players.filter(p=>p.team===t).some(p=>Math.max(...Object.values(p.lanes).map(a=>a.length))>=7));return teams}return state.match.players.filter(p=>Math.max(...Object.values(p.lanes).map(a=>a.length))>=7).map(p=>p.id)}
function nextRound(){const m=state.match;m.lastBattleResolution=null;m.round++;m.phase='plan';m.activeId=null;m.target=null;m.battlePresentation=null;m.busy=false;m.players.forEach(p=>p.plan=null);initRoundRuntime(m);log(`第${m.round}巡。行動を予約します。`);renderBattle()}
function ranksFFA(){const ps=state.match.players.map(p=>({p,depth:Math.max(...Object.values(p.lanes).map(a=>a.length))})).sort((a,b)=>b.depth-a.depth);let prev=null,rank=0;return ps.map((x,i)=>{if(prev===null||x.depth!==prev)rank=i+1;prev=x.depth;return{player:x.p,rank,depth:x.depth}})}
function endMatch(winners){const m=state.match;let ranking;if(m.mode==='2v2'){const wt=winners[0];ranking=m.players.map(p=>({player:p,rank:p.team===wt?1:2,depth:Math.max(...Object.values(p.lanes).map(a=>a.length))}))}else ranking=ranksFFA();const me=ranking.find(x=>x.player.human),points=settleBattlePoints(m,me.rank);m.lastResult={ranking,headline:me.rank===1?'勝利':'結果',grade:me.rank,points};state.history.unshift({at:new Date().toISOString(),mode:m.mode,rank:me.rank,rounds:m.round,character:state.playerCharacterId,partner:state.selectedPartnerId,battlePointsEarned:points.earned,quickPoints:points.quick,battlePointsAfter:points.after});save();show('result');renderResult()}
async function renderResult(){const r=state.match?.lastResult;if(!r)return;const m=state.match,ordered=[...r.ranking].sort((a,b)=>a.rank-b.rank),me=ordered.find(x=>x.player.human),mode=({'2p':'二人','4p':'四人','2v2':'二対二'})[m.mode]||m.mode,content=m.contentId==='honey_hunt'?'ハニーハント':'ロード・シールド';$('#resultGrade').textContent=r.grade;$('#resultHeadline').textContent=r.headline;$('#resultSummary').textContent=`${m.round}巡で決着。 ${content}`;$('#resultLeadText')&&($('#resultLeadText').textContent=r.grade===1?'一位で決着':'最終順位');$('#resultLeadSub')&&($('#resultLeadSub').textContent=`${content} / ${mode}`);$('#resultMode')&&($('#resultMode').textContent=mode);$('#resultRounds')&&($('#resultRounds').textContent=`${m.round}巡`);$('#resultLane')&&($('#resultLane').textContent=`${me?.depth??0}枚`);renderResultBattlePoints(r);const box=$('#resultRanking');box.innerHTML='';ordered.forEach(x=>{const d=document.createElement('div');d.className='rankLine';d.innerHTML=`<b>${x.rank}位 ${x.player.human?'あなた':x.player.name}</b><span>最長列 ${x.depth}枚</span>`;box.appendChild(d)});resultMount=await mountChar('.resultWorld',state.playerCharacterId,r.grade===1?'joy':'defeated')}
$('#rematch').onclick=()=>startMatch();$('#leaveMatch').onclick=()=>{state.match=null;navigateRoot('home')};window.__GAMEROAD_QUICK_R14_QA__={version:'FULLGAME-QUICKTIMER-R14',constants:{windowMs:QUICK_WINDOW_MS,perfectRemainMs:QUICK_PERFECT_REMAIN_MS,goodRemainMs:QUICK_GOOD_REMAIN_MS,perfectPoints:QUICK_PERFECT_POINTS,goodPoints:QUICK_GOOD_POINTS,chestTarget:BATTLE_POINTS_CHEST_TARGET},snapshot(){const m=state.match,me=m?.players?.find(p=>p.human),rem=m?quickRemainingMs(m):null;return{screen:state.screen,round:m?.round||null,phase:m?.phase||null,remainingMs:rem,tier:m?quickTierForRemaining(rem):null,quickPoints:me?(m.quickPointsByPlayer?.[me.id]||0):0,awards:me?(m.quickAwards||[]).filter(a=>a.playerId===me.id).map(a=>({...a})):[],battlePoints:state.progression?.battlePoints||0,settlement:m?.battlePointsSettlement?safeClone(m.battlePointsSettlement):null,coilHidden:$('#quickCoil')?.hidden??null,coilText:$('#quickRemain')?.textContent||null,resultText:$('#resultBattlePoints')?.innerText||''}},setRemainingSeconds(sec){const m=state.match;if(!m?.quickWindow)return false;const v=Math.max(0,Math.min(30,Number(sec)||0));m._quickQaNowMs=m.quickWindow.deadlineAt-v*1000;renderQuickTimer();return true},clearTestClock(){const m=state.match;if(!m)return false;delete m._quickQaNowMs;renderQuickTimer();return true},setBattlePoints(n){state.progression={battlePoints:Math.max(0,Math.floor(Number(n)||0))};return state.progression.battlePoints},persistenceProbe(n){state.progression={battlePoints:Math.max(0,Math.floor(Number(n)||0))};save();state.progression={battlePoints:0};load();return{battlePoints:state.progression.battlePoints,storage:state.storage}}};window.GAMEROAD_3D_WALK_QA={version:'BROWSER-3D-BOARD-WALK-INTEGRATION-001',snapshot(){const m=state.match,me=m?.players?.find(p=>p.human);return{screen:state.screen,phase:m?.phase||null,position:me?.position||null,path:me?.plan?.path?[...me.plan.path]:null,avatarState:battleMount?.root?.dataset?.state||null,avatarFacing:battleMount?.facingName||null,nodeCount:document.querySelectorAll('#board .node').length,canvas:{w:$('#fieldCanvas')?.width||0,h:$('#fieldCanvas')?.height||0}}}};window.GAMEROAD_GRASSLAND3D_QA={version:'BROWSER-BOARD-GRASSLAND-3D-R4',snapshot(){const gl=$('#fieldCanvas')?.getContext('webgl');return{...fieldLastStats,webgl:!!gl,contextLost:!!gl?.isContextLost?.(),projectionCount:fieldProjection.size,nodeCount:document.querySelectorAll('#board .node').length,screen:state.screen}}};
window.GAMEROAD_BOARD_REVIEW_QA={version:'BROWSER-BOARD-PLAYTEST-REVIEW-LOOP-001',_rect(e){const b=e?.getBoundingClientRect();return b?{l:b.left,r:b.right,t:b.top,b:b.bottom,w:b.width,h:b.height}:null},_hit(a,b){return!!(a&&b&&Math.min(a.r,b.r)>Math.max(a.l,b.l)&&Math.min(a.b,b.b)>Math.max(a.t,b.t))},snapshot(){const rect=this._rect.bind(this),hit=this._hit.bind(this),nodes=[...document.querySelectorAll('#board .node')].map(e=>({id:e.dataset.pos,rect:rect(e)})),surfaces={publicHud:rect($('#publicTurnHud')),battleInfo:rect(document.querySelector('.battleInfo')),rail:rect(document.querySelector('.battleRail')),top:rect(document.querySelector('.battleTopStatus'))},collisions={};for(const[k,r]of Object.entries(surfaces))collisions[k]=nodes.filter(n=>hit(n.rect,r)).map(n=>n.id);return{screen:state.screen,phase:state.match?.phase||null,viewport:{w:innerWidth,h:innerHeight},profile:fieldCameraState?.kind||null,nodeCount:nodes.length,map:rect($('#battleMap')),collisions,criticalOverlapCount:collisions.publicHud.length+collisions.battleInfo.length,avatar:{state:battleMount?.root?.dataset?.state||null,facing:battleMount?.facingName||null}}},setupSnapshot(){const rect=this._rect.bind(this),hit=this._hit.bind(this),header=rect(document.querySelector('header.top')),controls=['[data-content="road_shield"]','[data-content="honey_hunt"]','[data-mode="2p"]','[data-mode="4p"]','[data-mode="2v2"]','#startMatch'].map(sel=>({sel,rect:rect(document.querySelector(sel))}));return{viewport:{w:innerWidth,h:innerHeight},header,controls,headerCollisions:controls.filter(c=>hit(c.rect,header)).map(c=>c.sel),offscreen:controls.filter(c=>!c.rect||c.rect.t<0||c.rect.l<0||c.rect.r>innerWidth||c.rect.b>innerHeight).map(c=>c.sel)}}};
window.GAMEROAD_VISUAL_SHELL_QA={version:'BROWSER-PLAYTEST-VISUAL-SHELL-REVIEW-001',rect(sel){const e=document.querySelector(sel),r=e?.getBoundingClientRect();return r?{l:r.left,t:r.top,r:r.right,b:r.bottom,w:r.width,h:r.height}:null},inside(sel){const r=this.rect(sel);return!!(r&&r.l>=0&&r.t>=0&&r.r<=innerWidth&&r.b<=innerHeight)},snapshot(){return{screen:state.screen,viewport:{w:innerWidth,h:innerHeight},contextVisible:$('#contextHelp')?.classList.contains('on')||false,home:{primary:this.rect('.codexBattleCta'),character:null,live:this.rect('.codexHomeCenterStage')},setup:{box:this.rect('.setupBox'),start:this.rect('#startMatch'),character:this.rect('#setupRuntime'),goal:this.rect('.setupGoalCard')},result:{rank:this.rect('.resultRank'),body:this.rect('.resultBody'),buttons:this.rect('.resultBtns'),character:this.rect('.resultWorld .grtc-image')}}}};
function positionLabel(id){if(id.startsWith('C:'))return '中央';if(id.startsWith('S:'))return 'シールド境界';if(id.startsWith('R:')){const r=parseRoad(id);return `${r.owner} ${r.lane==='L'?'左':r.lane==='C'?'中央':'右'}列 ${r.depth}`;}return '盤面';}function renderPlayers(){const m=state.match,root=$('#players');root.innerHTML='';m.players.forEach(p=>{const d=document.createElement('div');d.className='player'+(p.human?' you':'')+(p.id===m.activeId?' active':'');d.innerHTML=`<b>${p.name}</b> ${m.mode==='2v2'?`<span>組${p.team}</span>`:''}<div>手札 ${p.hand.length} / 山札 ${p.deck.length} / チップ ${p.chip.length}</div><div class="r
--- END HIT 3 ---

===== "show('result')" HITS=3 =====

--- HIT 1 OFFSET=10073315 RANGE=10067315:10079329 ---
');const scores=new Map();m.players.forEach(p=>scores.set(p.id,(m.participants.get(p.id)||[]).reduce((a,e)=>a+e.currentValue,0)));const comparisonPlayers=battlePresentationPlayers(m);if(!await setBattlePresentation('compare',{attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,players:comparisonPlayers,teamTotals:m.mode==='2v2'?{A:m.players.filter(p=>p.team==='A').reduce((n,p)=>n+scores.get(p.id),0),B:m.players.filter(p=>p.team==='B').reduce((n,p)=>n+scores.get(p.id),0)}:null}))return;let winningPlayerIds=[];if(m.mode==='2v2'){const totals={A:0,B:0};m.players.forEach(p=>totals[p.team]+=scores.get(p.id));const max=Math.max(totals.A,totals.B),ties=['A','B'].filter(t=>totals[t]===max);let team;if(ties.length===1)team=ties[0];else{const active=m.players.find(p=>p.id===m.activeId);team=ties.includes(active.team)?active.team:ties[matchRnd(ties.length)]}winningPlayerIds=m.players.filter(p=>p.team===team).map(p=>p.id);log(`戦闘：組${team}が勝利。`)}else{const max=Math.max(...m.players.map(p=>scores.get(p.id)));const ties=m.players.filter(p=>scores.get(p.id)===max);const active=m.players.find(p=>p.id===m.activeId);const winner=ties.length===1?ties[0]:(ties.some(p=>p.id===active.id)?active:ties[matchRnd(ties.length)]);winningPlayerIds=[winner.id];log(`戦闘：${winner.name}が勝利。`)}if(!await setBattlePresentation('winner',{attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,players:battlePresentationPlayers(m,winningPlayerIds),winnerIds:[...winningPlayerIds],teamTotals:m.mode==='2v2'?{A:m.players.filter(p=>p.team==='A').reduce((n,p)=>n+scores.get(p.id),0),B:m.players.filter(p=>p.team==='B').reduce((n,p)=>n+scores.get(p.id),0)}:null}))return;const humanPlayer=m.players.find(p=>p.human);if(humanPlayer&&battleMount){if(winningPlayerIds.includes(humanPlayer.id))await setBattleRuntimeState('joy');else await setBattleRuntimeState('hit');}const resolutionPlayers=m.players.map(p=>({id:p.id,name:p.name,team:p.team||null,score:scores.get(p.id),winner:winningPlayerIds.includes(p.id),cards:(m.participants.get(p.id)||[]).map(e=>({cardId:e.cardId,label:cardLabel(e.cardId),value:e.currentValue,origin:e.origin}))}));const laneBefore=new Map(m.players.map(p=>[p.id,p.lanes[m.target.lane].length])),maxLaneBefore=new Map(m.players.map(p=>[p.id,Math.max(...Object.values(p.lanes).map(a=>a.length))]));m.players.forEach(p=>{const parts=m.participants.get(p.id)||[];if(winningPlayerIds.includes(p.id)){const lane=p.lanes[m.target.lane];parts.forEach(e=>lane.push(e.cardId));if(p.chip.length)p.chip.splice(0).forEach(id=>lane.push(id))}else parts.forEach(e=>p.chip.push(e.cardId))});const winningTeam=m.mode==='2v2'?(m.players.find(p=>winningPlayerIds.includes(p.id))?.team||null):null,teamTotals=m.mode==='2v2'?{A:m.players.filter(p=>p.team==='A').reduce((n,p)=>n+scores.get(p.id),0),B:m.players.filter(p=>p.team==='B').reduce((n,p)=>n+scores.get(p.id),0)}:null;m.lastBattleResolution={serial:++m.resolutionSeq,round:m.round,mode:m.mode,attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,winnerIds:[...winningPlayerIds],winningTeam,teamTotals,players:resolutionPlayers,laneGains:m.players.map(p=>({id:p.id,lane:m.target.lane,before:laneBefore.get(p.id),after:p.lanes[m.target.lane].length,added:p.lanes[m.target.lane].length-laneBefore.get(p.id)})).filter(x=>x.added>0),maxLaneProgress:m.players.map(p=>({id:p.id,before:maxLaneBefore.get(p.id),after:Math.max(...Object.values(p.lanes).map(a=>a.length))}))};const slaykiaAttackEnd=grSlaykiaAttackEndHook(m);m.participants=new Map(m.players.map(p=>[p.id,[]]));m.players.forEach(refill);m.battlePresentation={stage:'settle',round:m.round,mode:m.mode,attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,players:resolutionPlayers,winnerIds:[...winningPlayerIds],teamTotals};renderBattle();await battlePresentationWait(BATTLE_PRESENTATION_RESULT_MS,true);if(state.match!==m||state.screen!=='battle')return;const winners=slaykiaAttackEnd?.specialWinnerTokens?.length?[...slaykiaAttackEnd.specialWinnerTokens]:checkWin();m.battlePresentation=null;if(winners.length){m.busy=false;if(slaykiaAttackEnd?.specialWinnerTokens?.length)log('スレイキア：成功破壊16枚で特殊勝利。');else if(m.contentId==='honey_hunt')log('一列へ実札が七枚以上連続配置され、ハニーハントの勝利条件が成立しました。');return endMatch(winners)}nextRound()}catch(e){clearBattlePresentationWait();if(state.match===m){m.battlePresentation=null;m.busy=false;log(`戦闘停止：${e.message}`);renderBattle()}throw e}}
function checkWin(){if(state.match.mode==='2v2'){const teams=['A','B'].filter(t=>state.match.players.filter(p=>p.team===t).some(p=>Math.max(...Object.values(p.lanes).map(a=>a.length))>=7));return teams}return state.match.players.filter(p=>Math.max(...Object.values(p.lanes).map(a=>a.length))>=7).map(p=>p.id)}
function nextRound(){const m=state.match;m.lastBattleResolution=null;m.round++;m.phase='plan';m.activeId=null;m.target=null;m.battlePresentation=null;m.busy=false;m.players.forEach(p=>p.plan=null);initRoundRuntime(m);log(`第${m.round}巡。行動を予約します。`);renderBattle()}
function ranksFFA(){const ps=state.match.players.map(p=>({p,depth:Math.max(...Object.values(p.lanes).map(a=>a.length))})).sort((a,b)=>b.depth-a.depth);let prev=null,rank=0;return ps.map((x,i)=>{if(prev===null||x.depth!==prev)rank=i+1;prev=x.depth;return{player:x.p,rank,depth:x.depth}})}
function endMatch(winners){const m=state.match;let ranking;if(m.mode==='2v2'){const wt=winners[0];ranking=m.players.map(p=>({player:p,rank:p.team===wt?1:2,depth:Math.max(...Object.values(p.lanes).map(a=>a.length))}))}else ranking=ranksFFA();const me=ranking.find(x=>x.player.human),points=settleBattlePoints(m,me.rank);m.lastResult={ranking,headline:me.rank===1?'勝利':'結果',grade:me.rank,points};state.history.unshift({at:new Date().toISOString(),mode:m.mode,rank:me.rank,rounds:m.round,character:state.playerCharacterId,partner:state.selectedPartnerId,battlePointsEarned:points.earned,quickPoints:points.quick,battlePointsAfter:points.after});save();show('result');renderResult()}
async function renderResult(){const r=state.match?.lastResult;if(!r)return;const m=state.match,ordered=[...r.ranking].sort((a,b)=>a.rank-b.rank),me=ordered.find(x=>x.player.human),mode=({'2p':'二人','4p':'四人','2v2':'二対二'})[m.mode]||m.mode,content=m.contentId==='honey_hunt'?'ハニーハント':'ロード・シールド';$('#resultGrade').textContent=r.grade;$('#resultHeadline').textContent=r.headline;$('#resultSummary').textContent=`${m.round}巡で決着。 ${content}`;$('#resultLeadText')&&($('#resultLeadText').textContent=r.grade===1?'一位で決着':'最終順位');$('#resultLeadSub')&&($('#resultLeadSub').textContent=`${content} / ${mode}`);$('#resultMode')&&($('#resultMode').textContent=mode);$('#resultRounds')&&($('#resultRounds').textContent=`${m.round}巡`);$('#resultLane')&&($('#resultLane').textContent=`${me?.depth??0}枚`);renderResultBattlePoints(r);const box=$('#resultRanking');box.innerHTML='';ordered.forEach(x=>{const d=document.createElement('div');d.className='rankLine';d.innerHTML=`<b>${x.rank}位 ${x.player.human?'あなた':x.player.name}</b><span>最長列 ${x.depth}枚</span>`;box.appendChild(d)});resultMount=await mountChar('.resultWorld',state.playerCharacterId,r.grade===1?'joy':'defeated')}
$('#rematch').onclick=()=>startMatch();$('#leaveMatch').onclick=()=>{state.match=null;navigateRoot('home')};window.__GAMEROAD_QUICK_R14_QA__={version:'FULLGAME-QUICKTIMER-R14',constants:{windowMs:QUICK_WINDOW_MS,perfectRemainMs:QUICK_PERFECT_REMAIN_MS,goodRemainMs:QUICK_GOOD_REMAIN_MS,perfectPoints:QUICK_PERFECT_POINTS,goodPoints:QUICK_GOOD_POINTS,chestTarget:BATTLE_POINTS_CHEST_TARGET},snapshot(){const m=state.match,me=m?.players?.find(p=>p.human),rem=m?quickRemainingMs(m):null;return{screen:state.screen,round:m?.round||null,phase:m?.phase||null,remainingMs:rem,tier:m?quickTierForRemaining(rem):null,quickPoints:me?(m.quickPointsByPlayer?.[me.id]||0):0,awards:me?(m.quickAwards||[]).filter(a=>a.playerId===me.id).map(a=>({...a})):[],battlePoints:state.progression?.battlePoints||0,settlement:m?.battlePointsSettlement?safeClone(m.battlePointsSettlement):null,coilHidden:$('#quickCoil')?.hidden??null,coilText:$('#quickRemain')?.textContent||null,resultText:$('#resultBattlePoints')?.innerText||''}},setRemainingSeconds(sec){const m=state.match;if(!m?.quickWindow)return false;const v=Math.max(0,Math.min(30,Number(sec)||0));m._quickQaNowMs=m.quickWindow.deadlineAt-v*1000;renderQuickTimer();return true},clearTestClock(){const m=state.match;if(!m)return false;delete m._quickQaNowMs;renderQuickTimer();return true},setBattlePoints(n){state.progression={battlePoints:Math.max(0,Math.floor(Number(n)||0))};return state.progression.battlePoints},persistenceProbe(n){state.progression={battlePoints:Math.max(0,Math.floor(Number(n)||0))};save();state.progression={battlePoints:0};load();return{battlePoints:state.progression.battlePoints,storage:state.storage}}};window.GAMEROAD_3D_WALK_QA={version:'BROWSER-3D-BOARD-WALK-INTEGRATION-001',snapshot(){const m=state.match,me=m?.players?.find(p=>p.human);return{screen:state.screen,phase:m?.phase||null,position:me?.position||null,path:me?.plan?.path?[...me.plan.path]:null,avatarState:battleMount?.root?.dataset?.state||null,avatarFacing:battleMount?.facingName||null,nodeCount:document.querySelectorAll('#board .node').length,canvas:{w:$('#fieldCanvas')?.width||0,h:$('#fieldCanvas')?.height||0}}}};window.GAMEROAD_GRASSLAND3D_QA={version:'BROWSER-BOARD-GRASSLAND-3D-R4',snapshot(){const gl=$('#fieldCanvas')?.getContext('webgl');return{...fieldLastStats,webgl:!!gl,contextLost:!!gl?.isContextLost?.(),projectionCount:fieldProjection.size,nodeCount:document.querySelectorAll('#board .node').length,screen:state.screen}}};
window.GAMEROAD_BOARD_REVIEW_QA={version:'BROWSER-BOARD-PLAYTEST-REVIEW-LOOP-001',_rect(e){const b=e?.getBoundingClientRect();return b?{l:b.left,r:b.right,t:b.top,b:b.bottom,w:b.width,h:b.height}:null},_hit(a,b){return!!(a&&b&&Math.min(a.r,b.r)>Math.max(a.l,b.l)&&Math.min(a.b,b.b)>Math.max(a.t,b.t))},snapshot(){const rect=this._rect.bind(this),hit=this._hit.bind(this),nodes=[...document.querySelectorAll('#board .node')].map(e=>({id:e.dataset.pos,rect:rect(e)})),surfaces={publicHud:rect($('#publicTurnHud')),battleInfo:rect(document.querySelector('.battleInfo')),rail:rect(document.querySelector('.battleRail')),top:rect(document.querySelector('.battleTopStatus'))},collisions={};for(const[k,r]of Object.entries(surfaces))collisions[k]=nodes.filter(n=>hit(n.rect,r)).map(n=>n.id);return{screen:state.screen,phase:state.match?.phase||null,viewport:{w:innerWidth,h:innerHeight},profile:fieldCameraState?.kind||null,nodeCount:nodes.length,map:rect($('#battleMap')),collisions,criticalOverlapCount:collisions.publicHud.length+collisions.battleInfo.length,avatar:{state:battleMount?.root?.dataset?.state||null,facing:battleMount?.facingName||null}}},setupSnapshot(){const rect=this._rect.bind(this),hit=this._hit.bind(this),header=rect(document.querySelector('header.top')),controls=['[data-content="road_shield"]','[data-content="honey_hunt"]','[data-mode="2p"]','[data-mode="4p"]','[data-mode="2v2"]','#startMatch'].map(sel=>({sel,rect:rect(document.querySelector(sel))}));return{viewport:{w:innerWidth,h:innerHeight},header,controls,headerCollisions:controls.filter(c=>hit(c.rect,header)).map(c=>c.sel),offscreen:controls.filter(c=>!c.rect||c.rect.t<0||c.rect.l<0||c.rect.r>innerWidth||c.rect.b>innerHeight).map(c=>c.sel)}}};
window.GAMEROAD_VISUAL_SHELL_QA={version:'BROWSER-PLAYTEST-VISUAL-SHELL-REVIEW-001',rect(sel){const e=document.querySelector(sel),r=e?.getBoundingClientRect();return r?{l:r.left,t:r.top,r:r.right,b:r.bottom,w:r.width,h:r.height}:null},inside(sel){const r=this.rect(sel);return!!(r&&r.l>=0&&r.t>=0&&r.r<=innerWidth&&r.b<=innerHeight)},snapshot(){return{screen:state.screen,viewport:{w:innerWidth,h:innerHeight},contextVisible:$('#contextHelp')?.classList.contains('on')||false,home:{primary:this.rect('.codexBattleCta'),character:null,live:this.rect('.codexHomeCenterStage')},setup:{box:this.rect('.setupBox'),start:this.re
--- END HIT 1 ---

--- HIT 2 OFFSET=10210062 RANGE=10204062:10216076 ---
(FR.localPartnerRule)){rule=FR.localPartnerRule;const pd=ensurePartnerRuntime(state.match);if(pd)pd.rule=rule}else if(rule)FR.localPartnerRule=rule}if(FR.role==='host'){FR.partnerRules.P1=rule;if(state.match?.players?.find(p=>p.id==='P1')?.hateExploded)queueMicrotask(()=>friendForcedDecision('P1','rule-update').catch(e=>{FR.hateBlockers.P1=String(e?.message||e)}));return true}if(FR.role==='guest'&&FR.assignedPid){send({type:'partner_rule',rule});renderPartnerRuntime();return true}return false}
function hostStart(){if(FR.role!=='host'||FR.roomState!=='lobby'||!allReady())return false;FR.roomState='playing';FR.matchNo++;FR.planValues={};FR.baseProgression=Number(state.progression?.battlePoints||0);FR.baseHistoryLength=state.history.length;state.setupMode=FR.playerCount===4?'4p':'2p';state.setupContent=FR.contentId||'road_shield';const m=startMatch();if(!m){FR.roomState='lobby';return false}m.friendRoom={build:BUILD,code:FR.code,playerCount:FR.playerCount,localPid:'P1',host:true,resultPersistenceEnabled:false,noBotFill:true};m.players.forEach(p=>{p.roomHuman=true;p.roomConnected=true;if(p.id!=='P1'){p.name=p.id;p.human=false}});FR.partnerRules={P1:FRIEND_PARTNER_RULES.has(currentPartnerRule())?currentPartnerRule():null};FR.hateBlockers={};friendResetHateClocks(performance.now());document.body.classList.add('friend-room-match');const pts=document.getElementById('resultBattlePoints');if(pts)pts.hidden=true;broadcastSnapshots();return true}
function publicPlayer(p,viewer){return{id:p.id,name:p.id===viewer?'あなた':p.name,human:p.id===viewer,roomHuman:true,character:p.character,team:p.team||null,position:p.position,awake:Number(p.awake)||0,mana:Array.from({length:p.mana?.length||0},(_,i)=>'M'+i),chip:Array.from({length:p.chip?.length||0},()=> '__PUBLIC_COUNT__'),lanes:clone(p.lanes||{L:[],C:[],R:[]}),deck:Array.from({length:p.deck?.length||0},()=> '__HIDDEN__'),hand:p.id===viewer?[...p.hand]:Array.from({length:p.hand?.length||0},()=> '__HIDDEN__'),shields:p.id===viewer?clone(p.shields):{L:'__HIDDEN__',C:'__HIDDEN__',R:'__HIDDEN__'},plan:p.id===viewer?clone(FR.planValues[p.id]||p.plan||null):null,nextCostReduction:Number(p.nextCostReduction)||0,battleBlockedRound:Number(p.battleBlockedRound)||0,royalUsed:publicRoyalUsed(p),sourceDeckIds:[],sourceDeckSize:Number(p.sourceDeckSize)||0}}
function project(viewer){const m=state.match,p=m?.players?.find(x=>x.id===viewer);if(!m||!p)return null;const ordered=[p,...m.players.filter(x=>x.id!==viewer)].map(x=>publicPlayer(x,viewer));return{schema:'GAMEROAD_FRIEND_ROOM_R2_PROJECTION',build:BUILD,viewer,code:FR.code,matchInstanceId:m.id,roomState:FR.roomState,playerCount:FR.playerCount,round:m.round,phase:m.phase,activeId:m.activeId,target:clone(m.target),mode:m.mode,contentId:m.contentId,log:[...m.log],players:ordered,busy:!!m.busy,honey:{byNode:clone(m.honey?.byNode||{}),receipts:[],generatedRounds:[]},lastBattleResolution:clone(m.lastBattleResolution),battlePresentation:clone(m.battlePresentation),resolutionSeq:Number(m.resolutionSeq)||0,result:FR.roomState==='result'?clone(m.lastResult):null,participants:participantsHost(),selectionContract:grPublicSelectionContract(m,viewer),private:{pid:viewer,planSubmitted:!!FR.planValues[viewer],hateTime:Number(p.hateTime)||0,hateExploded:!!p.hateExploded,hateExplosionCount:Number(p.hateExplosionCount)||0}}}
function sendSnapshot(to){const s=slotByClient(to);if(!s?.connected)return;send({type:'snapshot',to,projection:project(s.pid)})}
function broadcastSnapshots(){if(FR.role!=='host'||!state.match)return;for(const s of Object.values(FR.slots))if(s.connected)sendSnapshot(s.clientId);renderFriendHostBattleState()}
function projectionToMatch(pr){const m={id:'FRIEND-'+pr.code+':'+String(pr.matchInstanceId||'legacy'),mode:pr.mode,contentId:pr.contentId,round:pr.round,phase:pr.phase,players:pr.players.map(x=>clone(x)),activeId:pr.activeId,target:clone(pr.target),log:[...pr.log],lastResult:null,lastBattleResolution:clone(pr.lastBattleResolution),battlePresentation:clone(pr.battlePresentation),resolutionSeq:pr.resolutionSeq,projection:['N','E','S','W'],abilityRng:1,busy:!!pr.busy,honey:{byNode:clone(pr.honey?.byNode||{}),receipts:new Set(),generatedRounds:new Set()},participants:new Map(pr.players.map(p=>[p.id,[]])),participantSeq:0,resolvedAbilityKeys:new Set(),abilityOrderReceipts:new Map(),testAutoChoices:false,quickWindow:null,quickPointsByPlayer:{},quickAwards:[],playerDeckRule:{id:pr.selectionContract?.regulationId||DECK_RULE.id,revision:pr.selectionContract?.regulationRevision||DECK_RULE.revision,mainCount:Number(pr.selectionContract?.mainCount)||Number(pr.players?.find(x=>x.id===pr.viewer)?.sourceDeckSize)||0,exCount:Number(pr.selectionContract?.exCount)||0},friendRoom:{build:BUILD,code:pr.code,playerCount:pr.playerCount,localPid:pr.viewer,host:false,resultPersistenceEnabled:false,noBotFill:true}};m.selectionLock=Object.freeze({version:1,playerId:pr.viewer,mode:m.mode,contentId:m.contentId,regulationId:m.playerDeckRule.id,regulationRevision:m.playerDeckRule.revision,mainCount:m.playerDeckRule.mainCount,exCount:m.playerDeckRule.exCount,sourceDeckDigest:null});const me=m.players.find(x=>x.id===pr.viewer);if(me){me.hateTime=Math.max(0,Math.min(1000,Number(pr.private?.hateTime)||0));me.hateExploded=!!pr.private?.hateExploded;me.hateExplosionCount=Math.max(0,Number(pr.private?.hateExplosionCount)||0)}if(pr.result){const ranking=(pr.result.ranking||[]).map(r=>{const pp=m.players.find(x=>x.id===r.player?.id);return{player:pp,rank:r.rank,depth:r.depth}}).filter(r=>r.player);const me=ranking.find(r=>r.player.id===pr.viewer);m.lastResult={ranking,headline:me?.rank===1?'勝利':'結果',grade:me?.rank||0,points:null,friendRoom:true}}return m}
function applyProjection(pr){if(!pr||pr.viewer!==FR.assignedPid)return;FR.snapshot=pr;FR.roomState=pr.roomState;state.match=projectionToMatch(pr);document.body.classList.add('friend-room-match');const pts=document.getElementById('resultBattlePoints');if(pts)pts.hidden=true;if(pr.roomState==='result'){show('result');renderResult();if(pts)pts.hidden=true;const h=document.getElementById('resultPointsEarned');if(h)h.textContent='フレンド対戦・報酬なし'}else{show('battle');renderBattle();renderRemoteWait();syncFriendPartnerRule()} }
function renderRemoteWait(){if(FR.role!=='guest'||state.screen!=='battle')return;const b=document.getElementById('readyPlan'),pr=FR.snapshot;if(b&&pr?.private?.planSubmitted){b.disabled=true;b.querySelector('.quickReadyText')&&(b.querySelector('.quickReadyText').textContent='予約済み・全員待ち')}const act=state.match?.players?.find(p=>p.id===state.match.activeId);if(state.match?.phase==='target'&&act?.id!==FR.assignedPid){document.getElementById('targetBox')?.classList.remove('on')}}
function renderFriendHostBattleState(){if(FR.role!=='host'||state.screen!=='battle'||!state.match?.friendRoom)return;const b=document.getElementById('readyPlan');if(b&&FR.planValues.P1){b.disabled=true;b.querySelector('.quickReadyText')&&(b.querySelector('.quickReadyText').textContent='予約済み・全員待ち')}}
function legalPlan(p,plan){if(!p||!plan?.roadId||!plan?.battleId)return{ok:false,reason:'plan_missing'};if(plan.roadId===plan.battleId)return{ok:false,reason:'same_card'};if(!p.hand.includes(plan.roadId)||!p.hand.includes(plan.battleId))return{ok:false,reason:'reservation_not_owned'};if(!roadSelectable(plan.roadId)||!roadSelectable(plan.battleId))return{ok:false,reason:'card_not_selectable'};const path=Array.isArray(plan.path)&&plan.path.length?plan.path:[p.position];if(path[0]!==p.position)return{ok:false,reason:'path_origin_mismatch'};const lim=normalMoveLimitForRoad(plan.roadId);if(path.length-1>lim)return{ok:false,reason:'move_limit'};for(let i=1;i<path.length;i++)if(!neighbors(path[i-1],p).includes(path[i])||path.slice(0,i).includes(path[i]))return{ok:false,reason:'path_invalid'};return{ok:true,plan:{roadId:plan.roadId,battleId:plan.battleId,path:[...path]}}}
async function submitHostPlan(){const m=state.match,p=m?.players?.find(x=>x.id==='P1');if(!m?.friendRoom||FR.role!=='host'||m.phase!=='plan'||m.busy)return false;if(p?.hateExploded){queueMicrotask(()=>friendForcedDecision('P1','manual-block').catch(e=>{FR.hateBlockers.P1=String(e?.message||e)}));return toast('ヘイト爆発中は必須判断を手動変更できません')}friendTickHate(performance.now());updatePlanFromSelect();const v=legalPlan(p,p.plan);if(!v.ok){toast('予約内容を確認してください');return false}if(FR.planValues.P1)return false;FR.planValues.P1=v.plan;window.GAMEROAD_PARTNER_COMMENT?.emit?.({eventId:`${m.id}:set:${m.round}:road:${p.id}`,kind:'dialogue',speechIntent:'road_set',cardId:v.plan.roadId,loggable:false,ttlMs:9000});window.GAMEROAD_PARTNER_COMMENT?.emit?.({eventId:`${m.id}:set:${m.round}:battle:${p.id}`,kind:'dialogue',speechIntent:'battle_set',cardId:v.plan.battleId,loggable:false,ttlMs:9000});friendSyncHateClocks(performance.now());broadcastSnapshots();await maybeResolvePlans();return true}
function submitGuestPlan(){const m=state.match,p=m?.players?.find(x=>x.id===FR.assignedPid);if(FR.role!=='guest'||!m?.friendRoom||m.phase!=='plan'||m.busy||FR.snapshot?.private?.planSubmitted)return false;if(FR.snapshot?.private?.hateExploded)return toast('ヘイト爆発中は必須判断を手動変更できません');updatePlanFromSelect();const v=legalPlan(p,p.plan);if(!v.ok){toast('予約内容を確認してください');return false}window.GAMEROAD_PARTNER_COMMENT?.emit?.({eventId:`${m.id}:set:${m.round}:road:${p.id}`,kind:'dialogue',speechIntent:'road_set',cardId:v.plan.roadId,loggable:false,ttlMs:9000});window.GAMEROAD_PARTNER_COMMENT?.emit?.({eventId:`${m.id}:set:${m.round}:battle:${p.id}`,kind:'dialogue',speechIntent:'battle_set',cardId:v.plan.battleId,loggable:false,ttlMs:9000});send({type:'plan',round:m.round,phase:m.phase,payload:v.plan});return true}
async function maybeResolvePlans(){if(FR.role!=='host'||!state.match||state.match.phase!=='plan')return;const need=pidList();if(!need.every(pid=>FR.planValues[pid]))return;const plans=Object.fromEntries(need.map(pid=>[pid,clone(FR.planValues[pid])]));FR.planValues={};await resolveHumanPlans(plans)}
async function resolveHumanPlans(plans){const m=state.match;if(m.busy)return;m.busy=true;try{if(!battleMount)await updateBattleAvatar();for(const p of m.players){const v=legalPlan(p,plans[p.id]);if(!v.ok)throw Error(p.id+':'+v.reason);p.plan=v.plan}m.lastBattleResolution=null;m.players.forEach(p=>removeHand(p,p.plan.roadId));m.activeId=pickActive(m.players).id;const roadEntries=m.players.map(p=>makeParticipant(p,p.plan.roadId,'road'));m.phase='reveal';log('ロードカードを公開。公開時能力を小さい印刷値順で解決します。');renderBattle();broadcastSnapshots();await resolveAbilityBundle(roadEntries,'road');if(m.contentId==='honey_hunt'&&!m.honey.generatedRounds.has(m.round)){spawnHoneyFromRoads();log('公開された1〜6と、列上の同数字ロードカード実体を照合してハニー発生源を更新しました。')}m.phase='move';renderBattle();broadcastSnapshots();const order=randomOrderByRoad(m.players);for(const p of order){await animateResolvedPath(p);if(m.contentId!=='honey_hunt'&&CARDS[p.plan.roadId].effect_type==='none'){const before=p.awake;p.awake=Math.min(p.mana.length,p.awake+1);if(p.awake>before)log(`${p.name}：能力なしロードカードの成立でマナを1枚起こした。`)}log(`${p.name}：${cardLabel(p.plan.roadId)} / ${p.plan.path.length-1}歩`)}const act=m.players.find(p=>p.id===m.activeId);m.phase='target';m.busy=false;friendSyncHateClocks(performance.now());log(`${act.name}が攻撃権を取得。`);renderBattle();broadcastSnapshots()}catch(e){m.busy=false;log(`処理停止：${e.message}`);renderBattle();broadcastSnapshots();throw e}}
async function submitGuestTarget(){const m=state.match,act=m?.players?.find(p=>p.id===m.activeId);if(FR.role!=='guest'||m?.phase!=='target'||act?.id!==FR.assignedPid)return false;if(FR.snapshot?.private?.hateExploded)return toast('ヘイト爆発中は必須判断を手動変更できません');const defenderId=document.getElementById('targetPlayer').value;if(!legalOpponents(act).some(p=>p.id===defenderId))return false;send({type:'target',round:m.round,phase:m.phase,payload:{defenderId,lane:document.getElementById('targetLane').value,shield:document.getElementById('targetShield').value}});return true}
function choicePayload(items){return items.map((it,i)=>({i,label:abil
--- END HIT 2 ---

--- HIT 3 OFFSET=10227079 RANGE=10221079:10233093 ---
eject(cid,'hate_forced');queueMicrotask(()=>friendForcedDecision(s.pid,'manual-block').catch(e=>{FR.hateBlockers[s.pid]=String(e?.message||e);broadcastSnapshots()}));return}const t=m.payload||{},def=String(t.defenderId||'');if(!legalOpponents(act).some(p=>p.id===def)||!['L','C','R'].includes(t.lane)||!['L','C','R'].includes(t.shield)){reject(cid,'illegal_target');return}mm.target={defenderId:def,lane:t.lane,shield:t.shield};await resolveBattle();broadcastSnapshots();return}if(m.type==='choice_response'){const w=FR.choiceWait.get(String(m.choiceId||''));if(!w||w.pid!==s.pid){reject(cid,'choice_not_actor');return}friendTickHate(performance.now());const actor=state.match?.players?.find(p=>p.id===s.pid);if(actor?.hateExploded){reject(cid,'hate_forced');queueMicrotask(()=>friendForcedDecision(s.pid,'manual-block').catch(e=>{FR.hateBlockers[s.pid]=String(e?.message||e);broadcastSnapshots()}));return}const idx=[...(m.indices||[])].map(Number).filter(i=>Number.isInteger(i)&&i>=0&&i<w.items.length);w.resolve([...new Set(idx)]);return}}
function handleGuestMessage(m){if(m.to&&m.to!==FR.clientId)return;if(m.type==='accept'){FR.assignedPid=m.pid;FR.authToken=m.authToken||'';FR.playerCount=Number(m.playerCount)===4?4:2;FR.contentId=m.contentId||'road_shield';FR.roomState=m.roomState||'lobby';store(clientKey(FR.code),FR.clientId);store(authKey(FR.code),FR.authToken);renderLobby();send({type:'sync'});return}if(m.type==='reject'){FR.lastReject=m.reason||'reject';toast('参加できません: '+FR.lastReject);renderLobby();return}if(m.type==='host_close'){FR.roomState='closed';toast('部屋主が部屋を閉じました');show('friendroom');renderLobby();return}if(m.type==='lobby'){FR.participants=clone(m.lobby?.participants||[]);FR.roomState=m.lobby?.roomState||'lobby';FR.playerCount=Number(m.lobby?.playerCount)===4?4:2;FR.contentId=m.lobby?.contentId||FR.contentId;FR.guestReady=!!FR.participants.find(p=>p.pid===FR.assignedPid)?.ready;if(state.screen!=='friendroom')show('friendroom');renderLobby();return}if(m.type==='snapshot'){applyProjection(m.projection);return}if(m.type==='choice_request'){showRemoteChoice(m.choice);return}}
function renderLobby(){const root=document.getElementById('friendRoomPanel');if(!root)return;const tm=document.getElementById('topMode');if(state.screen==='friendroom'&&tm)tm.textContent='フレンドルーム';const idle=!FR.role,parts=FR.role==='host'?participantsHost():(FR.participants.length?FR.participants:pidList().map(pid=>({pid,connected:pid===FR.assignedPid,ready:false}))),role=FR.role==='host'?'部屋主':FR.role==='guest'?'参加者':'未参加',canReady=FR.roomState==='lobby'&&((FR.role==='host')||(FR.role==='guest'&&FR.assignedPid)),ownReady=FR.role==='host'?FR.hostReady:FR.guestReady;root.innerHTML=`<div class="friendRoomHero"><div><div class="k">FRIEND ROOM</div><div class="h">フレンドルーム</div></div><div class="friendCode"><span>合言葉</span><b>${FR.code||'-------'}</b><small>${role} / ${FR.playerCount}人 / ${FR.transport==='ws_relay_recipient_r21'?'遠隔WS':'同一アドレス'}</small></div></div><div class="friendRoomGrid"><div class="friendRoomCard"><h3>部屋</h3>${idle?`<div class="friendCreateRow"><button class="btn primary" id="friendCreate2">二人部屋を作る</button><button class="btn" id="friendCreate4">四人部屋を作る</button></div><label class="friendJoinLabel">七文字の合言葉<input id="friendJoinCode" maxlength="9" autocomplete="off" inputmode="text" placeholder="ABCDEFG"></label><button class="btn" id="friendJoinBtn">合言葉で入る</button>`:`<div class="friendStatus">状態: ${FR.roomState==='lobby'?'準備中':FR.roomState==='playing'?'対戦中':FR.roomState==='result'?'対戦終了':FR.roomState}</div><button class="btn danger" id="friendLeaveBtn">部屋を出る</button>`}${FR.lastReject?`<div class="friendReject">${FR.lastReject}</div>`:''}</div><div class="friendRoomCard"><h3>参加者</h3><div class="friendParticipants">${parts.map(p=>`<div class="friendParticipant ${p.connected?'connected':''} ${p.ready?'ready':''}"><b>${p.pid}</b><span>${p.connected?'接続':'空き'}</span><em>${p.ready?'準備完了':'待機'}</em></div>`).join('')}</div>${!idle?`<div class="friendActions"><button class="btn ${ownReady?'on':''}" id="friendReadyBtn" ${canReady?'':'disabled'}>${ownReady?'準備を戻す':'準備完了'}</button>${FR.role==='host'?`<button class="btn primary" id="friendStartBtn" ${allReady()?'':'disabled'}>部屋主が開始</button>`:''}</div>`:''}</div></div>`;document.getElementById('friendCreate2')?.addEventListener('click',()=>createRoom(2));document.getElementById('friendCreate4')?.addEventListener('click',()=>createRoom(4));document.getElementById('friendJoinBtn')?.addEventListener('click',()=>joinRoom(document.getElementById('friendJoinCode')?.value));document.getElementById('friendReadyBtn')?.addEventListener('click',toggleReady);document.getElementById('friendStartBtn')?.addEventListener('click',hostStart);document.getElementById('friendLeaveBtn')?.addEventListener('click',()=>{grClearPartnerMatchSession(state.match);reset(true);state.match=null;show('setup')})}
function returnLobby(){if(FR.role!=='host'||!['result','playing'].includes(FR.roomState))return false;FR.roomState='lobby';FR.hostReady=false;for(const s of Object.values(FR.slots))s.ready=false;FR.planValues={};FR.choiceWait.forEach(x=>x.reject?.(Error('rematch_reset')));FR.choiceWait.clear();grClearPartnerMatchSession(state.match);state.match=null;document.body.classList.remove('friend-room-match');show('friendroom');sendAllLobby();return true}
function friendEndMatch(winners){const m=state.match;let ranking;if(m.mode==='2v2'){const wt=winners[0];ranking=m.players.map(p=>({player:p,rank:p.team===wt?1:2,depth:Math.max(...Object.values(p.lanes).map(a=>a.length))}))}else ranking=ranksFFA();const me=ranking.find(x=>x.player.id==='P1');m.lastResult={ranking,headline:me?.rank===1?'勝利':'結果',grade:me?.rank||0,points:null,friendRoom:true};FR.roomState='result';if(FR.baseProgression!=null)state.progression={battlePoints:FR.baseProgression};if(FR.baseHistoryLength!=null&&state.history.length>FR.baseHistoryLength)state.history.splice(0,state.history.length-FR.baseHistoryLength);broadcastSnapshots();show('result');renderResult();const pts=document.getElementById('resultBattlePoints');if(pts)pts.hidden=true;return m.lastResult}
const baseResolveAbilityEntry=resolveAbilityEntry;
resolveAbilityEntry=async function(entry,phase){if(FR.role==='host'&&state.match?.friendRoom){FR.choiceActorPid=entry.ownerId;try{return await baseResolveAbilityEntry(entry,phase)}finally{FR.choiceActorPid=null}}return baseResolveAbilityEntry(entry,phase)};
const baseChooseAbilityItems=chooseAbilityItems;
chooseAbilityItems=async function(title,hint,items,min,max,human){const pid=FR.choiceActorPid;if(FR.role==='host'&&state.match?.friendRoom&&pid&&pid!=='P1'&&slotByPid(pid)?.connected)return requestRemoteChoice(pid,title,hint,items,min,max);return baseChooseAbilityItems(title,hint,items,min,max,human)};
const baseReplacementBattleCard=replacementBattleCard;
replacementBattleCard=async function(p,reason){if(FR.role==='host'&&state.match?.friendRoom&&p?.id!=='P1'&&p?.roomHuman){const opts=p.hand.filter(id=>CARDS[id]?.runtime_status==='playable').map(cardId=>({kind:'hand',cardId,id:cardId,detail:reason}));if(!opts.length)return null;const ch=await requestRemoteChoice(p.id,'バトルカードを再確認',reason,opts,1,1);return ch[0]?.cardId||null}return baseReplacementBattleCard(p,reason)};
const baseNextRound=nextRound;
nextRound=function(){const r=baseNextRound();if(FR.role==='host'&&state.match?.friendRoom){FR.planValues={};friendSyncHateClocks(performance.now());broadcastSnapshots()}return r};
const baseEndMatch=endMatch;
endMatch=function(winners){if(FR.role==='host'&&state.match?.friendRoom)return friendEndMatch(winners);return baseEndMatch(winners)};
const baseRenderBattle=renderBattle;
renderBattle=function(){const r=baseRenderBattle();if(FR.role==='guest'&&state.match?.friendRoom)renderRemoteWait();else if(FR.role==='host'&&state.match?.friendRoom)renderFriendHostBattleState();return r};
const ready=document.getElementById('readyPlan'),baseReady=ready?.onclick;
if(ready)ready.onclick=()=>{if(state.match?.friendRoom){const p=FR.role==='host'?submitHostPlan():submitGuestPlan();return Promise.resolve(p).catch(console.error)}return baseReady?.()};
const target=document.getElementById('confirmTarget'),baseTarget=target?.onclick;
if(target)target.onclick=async()=>{if(state.match?.friendRoom&&FR.role==='guest')return submitGuestTarget();if(FR.role==='host'&&state.match?.friendRoom&&state.match.players?.find(p=>p.id==='P1')?.hateExploded){queueMicrotask(()=>friendForcedDecision('P1','manual-block').catch(e=>{FR.hateBlockers.P1=String(e?.message||e)}));return toast('ヘイト爆発中は必須判断を手動変更できません')}friendTickHate(performance.now());const r=await baseTarget?.();if(FR.role==='host'&&state.match?.friendRoom){friendSyncHateClocks(performance.now());broadcastSnapshots()}return r};
const rem=document.getElementById('rematch'),baseRem=rem?.onclick;
if(rem)rem.onclick=()=>{if(state.match?.friendRoom||FR.roomState==='result'){if(FR.role==='host')return returnLobby();show('friendroom');renderLobby();return}return baseRem?.()};
const leave=document.getElementById('leaveMatch'),baseLeave=leave?.onclick;
if(leave)leave.onclick=()=>{if(state.match?.friendRoom){reset(true);state.match=null;document.body.classList.remove('friend-room-match');show('setup');return}return baseLeave?.()};
document.getElementById('partnerRule')?.addEventListener('change',()=>syncFriendPartnerRule(true));
document.getElementById('friendRoomEntry')?.addEventListener('click',()=>{show('friendroom');renderLobby()});
window.addEventListener('beforeunload',()=>closeChannel(true));
const auto=validCode(new URLSearchParams(location.search).get('friend')||'');if(auto)setTimeout(()=>joinRoom(auto),50);
function qaState(){return{build:BUILD,role:FR.role,code:FR.code,pid:FR.assignedPid,clientId:FR.clientId,transport:FR.transport,playerCount:FR.playerCount,roomState:FR.roomState,ready:FR.role==='host'?FR.hostReady:FR.guestReady,participants:FR.role==='host'?participantsHost():clone(FR.participants),lastReject:FR.lastReject,planSubmitted:FR.role==='host'?Object.keys(FR.planValues):FR.snapshot?.private?.planSubmitted||false,hateAuthority:FR.role==='host'?{values:Object.fromEntries((state.match?.players||[]).filter(p=>p.roomHuman).map(p=>[p.id,{hateTime:Number(p.hateTime)||0,hateExploded:!!p.hateExploded,hateExplosionCount:Number(p.hateExplosionCount)||0,signature:friendHateSignature(p.id),rule:friendRule(p.id),blocker:FR.hateBlockers[p.id]||null}])),clockOwner:'host'}:{private:clone(FR.snapshot?.private||null),clockOwner:'host_snapshot'},match:state.match?{mode:state.match.mode,phase:state.match.phase,round:state.match.round,activeId:state.match.activeId,friend:!!state.match.friendRoom,players:state.match.players.map(p=>({id:p.id,human:p.human,roomHuman:!!p.roomHuman,hand:p.hand.length,position:p.position})),historyLength:state.history.length,battlePoints:Number(state.progression?.battlePoints||0)}:null}}
window.GAMEROAD_FRIEND_ROOM_R2={BUILD,create:createRoom,join:joinRoom,ready:toggleReady,start:hostStart,leave:()=>reset(true),returnLobby,status:qaState,projectFor:pid=>FR.role==='host'?project(pid):null,rawSend:o=>send(o),submitPlan:()=>FR.role==='host'?submitHostPlan():submitGuestPlan(),submitTarget:submitGuestTarget,forceSnapshot:()=>{if(FR.role==='host')broadcastSnapshots()},qaSetFast:()=>{battlePresentationRuntime.fast=true;return true},qaSetNoEffectHands:()=>{if(FR.role!=='host'||!state.match?.friendRoom)return null;const out={};for(const p of state.match.players){const pool=[...p.hand,...p.deck],picked=[];for(const id of pool){if(roadSelectable(id)&&CARDS[id]?.runtime_status==='playable'&&CARDS[id]?.effect_type==='none'&&!picked.includes(id)){picked.push(id);if(picked.length===3)break}}if(picked.length<2)throw Error('not-enough-noeffect-'+p.id);const remain=[...pool];for(const id of picked){const i=remain.indexOf(id);if(i>=0)remain.splice(i,1)}p.hand=picked;p.deck=remain;out[p.id]=[...picked]}broadcastSnapshots();renderBattle();return out},qaPlans:()=>state.match?{activeId:state.match.activeId,phas
--- END HIT 3 ---

===== 'renderResult()' HITS=4 =====

--- HIT 1 OFFSET=10073330 RANGE=10067330:10079344 ---
=new Map();m.players.forEach(p=>scores.set(p.id,(m.participants.get(p.id)||[]).reduce((a,e)=>a+e.currentValue,0)));const comparisonPlayers=battlePresentationPlayers(m);if(!await setBattlePresentation('compare',{attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,players:comparisonPlayers,teamTotals:m.mode==='2v2'?{A:m.players.filter(p=>p.team==='A').reduce((n,p)=>n+scores.get(p.id),0),B:m.players.filter(p=>p.team==='B').reduce((n,p)=>n+scores.get(p.id),0)}:null}))return;let winningPlayerIds=[];if(m.mode==='2v2'){const totals={A:0,B:0};m.players.forEach(p=>totals[p.team]+=scores.get(p.id));const max=Math.max(totals.A,totals.B),ties=['A','B'].filter(t=>totals[t]===max);let team;if(ties.length===1)team=ties[0];else{const active=m.players.find(p=>p.id===m.activeId);team=ties.includes(active.team)?active.team:ties[matchRnd(ties.length)]}winningPlayerIds=m.players.filter(p=>p.team===team).map(p=>p.id);log(`戦闘：組${team}が勝利。`)}else{const max=Math.max(...m.players.map(p=>scores.get(p.id)));const ties=m.players.filter(p=>scores.get(p.id)===max);const active=m.players.find(p=>p.id===m.activeId);const winner=ties.length===1?ties[0]:(ties.some(p=>p.id===active.id)?active:ties[matchRnd(ties.length)]);winningPlayerIds=[winner.id];log(`戦闘：${winner.name}が勝利。`)}if(!await setBattlePresentation('winner',{attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,players:battlePresentationPlayers(m,winningPlayerIds),winnerIds:[...winningPlayerIds],teamTotals:m.mode==='2v2'?{A:m.players.filter(p=>p.team==='A').reduce((n,p)=>n+scores.get(p.id),0),B:m.players.filter(p=>p.team==='B').reduce((n,p)=>n+scores.get(p.id),0)}:null}))return;const humanPlayer=m.players.find(p=>p.human);if(humanPlayer&&battleMount){if(winningPlayerIds.includes(humanPlayer.id))await setBattleRuntimeState('joy');else await setBattleRuntimeState('hit');}const resolutionPlayers=m.players.map(p=>({id:p.id,name:p.name,team:p.team||null,score:scores.get(p.id),winner:winningPlayerIds.includes(p.id),cards:(m.participants.get(p.id)||[]).map(e=>({cardId:e.cardId,label:cardLabel(e.cardId),value:e.currentValue,origin:e.origin}))}));const laneBefore=new Map(m.players.map(p=>[p.id,p.lanes[m.target.lane].length])),maxLaneBefore=new Map(m.players.map(p=>[p.id,Math.max(...Object.values(p.lanes).map(a=>a.length))]));m.players.forEach(p=>{const parts=m.participants.get(p.id)||[];if(winningPlayerIds.includes(p.id)){const lane=p.lanes[m.target.lane];parts.forEach(e=>lane.push(e.cardId));if(p.chip.length)p.chip.splice(0).forEach(id=>lane.push(id))}else parts.forEach(e=>p.chip.push(e.cardId))});const winningTeam=m.mode==='2v2'?(m.players.find(p=>winningPlayerIds.includes(p.id))?.team||null):null,teamTotals=m.mode==='2v2'?{A:m.players.filter(p=>p.team==='A').reduce((n,p)=>n+scores.get(p.id),0),B:m.players.filter(p=>p.team==='B').reduce((n,p)=>n+scores.get(p.id),0)}:null;m.lastBattleResolution={serial:++m.resolutionSeq,round:m.round,mode:m.mode,attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,winnerIds:[...winningPlayerIds],winningTeam,teamTotals,players:resolutionPlayers,laneGains:m.players.map(p=>({id:p.id,lane:m.target.lane,before:laneBefore.get(p.id),after:p.lanes[m.target.lane].length,added:p.lanes[m.target.lane].length-laneBefore.get(p.id)})).filter(x=>x.added>0),maxLaneProgress:m.players.map(p=>({id:p.id,before:maxLaneBefore.get(p.id),after:Math.max(...Object.values(p.lanes).map(a=>a.length))}))};const slaykiaAttackEnd=grSlaykiaAttackEndHook(m);m.participants=new Map(m.players.map(p=>[p.id,[]]));m.players.forEach(refill);m.battlePresentation={stage:'settle',round:m.round,mode:m.mode,attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,players:resolutionPlayers,winnerIds:[...winningPlayerIds],teamTotals};renderBattle();await battlePresentationWait(BATTLE_PRESENTATION_RESULT_MS,true);if(state.match!==m||state.screen!=='battle')return;const winners=slaykiaAttackEnd?.specialWinnerTokens?.length?[...slaykiaAttackEnd.specialWinnerTokens]:checkWin();m.battlePresentation=null;if(winners.length){m.busy=false;if(slaykiaAttackEnd?.specialWinnerTokens?.length)log('スレイキア：成功破壊16枚で特殊勝利。');else if(m.contentId==='honey_hunt')log('一列へ実札が七枚以上連続配置され、ハニーハントの勝利条件が成立しました。');return endMatch(winners)}nextRound()}catch(e){clearBattlePresentationWait();if(state.match===m){m.battlePresentation=null;m.busy=false;log(`戦闘停止：${e.message}`);renderBattle()}throw e}}
function checkWin(){if(state.match.mode==='2v2'){const teams=['A','B'].filter(t=>state.match.players.filter(p=>p.team===t).some(p=>Math.max(...Object.values(p.lanes).map(a=>a.length))>=7));return teams}return state.match.players.filter(p=>Math.max(...Object.values(p.lanes).map(a=>a.length))>=7).map(p=>p.id)}
function nextRound(){const m=state.match;m.lastBattleResolution=null;m.round++;m.phase='plan';m.activeId=null;m.target=null;m.battlePresentation=null;m.busy=false;m.players.forEach(p=>p.plan=null);initRoundRuntime(m);log(`第${m.round}巡。行動を予約します。`);renderBattle()}
function ranksFFA(){const ps=state.match.players.map(p=>({p,depth:Math.max(...Object.values(p.lanes).map(a=>a.length))})).sort((a,b)=>b.depth-a.depth);let prev=null,rank=0;return ps.map((x,i)=>{if(prev===null||x.depth!==prev)rank=i+1;prev=x.depth;return{player:x.p,rank,depth:x.depth}})}
function endMatch(winners){const m=state.match;let ranking;if(m.mode==='2v2'){const wt=winners[0];ranking=m.players.map(p=>({player:p,rank:p.team===wt?1:2,depth:Math.max(...Object.values(p.lanes).map(a=>a.length))}))}else ranking=ranksFFA();const me=ranking.find(x=>x.player.human),points=settleBattlePoints(m,me.rank);m.lastResult={ranking,headline:me.rank===1?'勝利':'結果',grade:me.rank,points};state.history.unshift({at:new Date().toISOString(),mode:m.mode,rank:me.rank,rounds:m.round,character:state.playerCharacterId,partner:state.selectedPartnerId,battlePointsEarned:points.earned,quickPoints:points.quick,battlePointsAfter:points.after});save();show('result');renderResult()}
async function renderResult(){const r=state.match?.lastResult;if(!r)return;const m=state.match,ordered=[...r.ranking].sort((a,b)=>a.rank-b.rank),me=ordered.find(x=>x.player.human),mode=({'2p':'二人','4p':'四人','2v2':'二対二'})[m.mode]||m.mode,content=m.contentId==='honey_hunt'?'ハニーハント':'ロード・シールド';$('#resultGrade').textContent=r.grade;$('#resultHeadline').textContent=r.headline;$('#resultSummary').textContent=`${m.round}巡で決着。 ${content}`;$('#resultLeadText')&&($('#resultLeadText').textContent=r.grade===1?'一位で決着':'最終順位');$('#resultLeadSub')&&($('#resultLeadSub').textContent=`${content} / ${mode}`);$('#resultMode')&&($('#resultMode').textContent=mode);$('#resultRounds')&&($('#resultRounds').textContent=`${m.round}巡`);$('#resultLane')&&($('#resultLane').textContent=`${me?.depth??0}枚`);renderResultBattlePoints(r);const box=$('#resultRanking');box.innerHTML='';ordered.forEach(x=>{const d=document.createElement('div');d.className='rankLine';d.innerHTML=`<b>${x.rank}位 ${x.player.human?'あなた':x.player.name}</b><span>最長列 ${x.depth}枚</span>`;box.appendChild(d)});resultMount=await mountChar('.resultWorld',state.playerCharacterId,r.grade===1?'joy':'defeated')}
$('#rematch').onclick=()=>startMatch();$('#leaveMatch').onclick=()=>{state.match=null;navigateRoot('home')};window.__GAMEROAD_QUICK_R14_QA__={version:'FULLGAME-QUICKTIMER-R14',constants:{windowMs:QUICK_WINDOW_MS,perfectRemainMs:QUICK_PERFECT_REMAIN_MS,goodRemainMs:QUICK_GOOD_REMAIN_MS,perfectPoints:QUICK_PERFECT_POINTS,goodPoints:QUICK_GOOD_POINTS,chestTarget:BATTLE_POINTS_CHEST_TARGET},snapshot(){const m=state.match,me=m?.players?.find(p=>p.human),rem=m?quickRemainingMs(m):null;return{screen:state.screen,round:m?.round||null,phase:m?.phase||null,remainingMs:rem,tier:m?quickTierForRemaining(rem):null,quickPoints:me?(m.quickPointsByPlayer?.[me.id]||0):0,awards:me?(m.quickAwards||[]).filter(a=>a.playerId===me.id).map(a=>({...a})):[],battlePoints:state.progression?.battlePoints||0,settlement:m?.battlePointsSettlement?safeClone(m.battlePointsSettlement):null,coilHidden:$('#quickCoil')?.hidden??null,coilText:$('#quickRemain')?.textContent||null,resultText:$('#resultBattlePoints')?.innerText||''}},setRemainingSeconds(sec){const m=state.match;if(!m?.quickWindow)return false;const v=Math.max(0,Math.min(30,Number(sec)||0));m._quickQaNowMs=m.quickWindow.deadlineAt-v*1000;renderQuickTimer();return true},clearTestClock(){const m=state.match;if(!m)return false;delete m._quickQaNowMs;renderQuickTimer();return true},setBattlePoints(n){state.progression={battlePoints:Math.max(0,Math.floor(Number(n)||0))};return state.progression.battlePoints},persistenceProbe(n){state.progression={battlePoints:Math.max(0,Math.floor(Number(n)||0))};save();state.progression={battlePoints:0};load();return{battlePoints:state.progression.battlePoints,storage:state.storage}}};window.GAMEROAD_3D_WALK_QA={version:'BROWSER-3D-BOARD-WALK-INTEGRATION-001',snapshot(){const m=state.match,me=m?.players?.find(p=>p.human);return{screen:state.screen,phase:m?.phase||null,position:me?.position||null,path:me?.plan?.path?[...me.plan.path]:null,avatarState:battleMount?.root?.dataset?.state||null,avatarFacing:battleMount?.facingName||null,nodeCount:document.querySelectorAll('#board .node').length,canvas:{w:$('#fieldCanvas')?.width||0,h:$('#fieldCanvas')?.height||0}}}};window.GAMEROAD_GRASSLAND3D_QA={version:'BROWSER-BOARD-GRASSLAND-3D-R4',snapshot(){const gl=$('#fieldCanvas')?.getContext('webgl');return{...fieldLastStats,webgl:!!gl,contextLost:!!gl?.isContextLost?.(),projectionCount:fieldProjection.size,nodeCount:document.querySelectorAll('#board .node').length,screen:state.screen}}};
window.GAMEROAD_BOARD_REVIEW_QA={version:'BROWSER-BOARD-PLAYTEST-REVIEW-LOOP-001',_rect(e){const b=e?.getBoundingClientRect();return b?{l:b.left,r:b.right,t:b.top,b:b.bottom,w:b.width,h:b.height}:null},_hit(a,b){return!!(a&&b&&Math.min(a.r,b.r)>Math.max(a.l,b.l)&&Math.min(a.b,b.b)>Math.max(a.t,b.t))},snapshot(){const rect=this._rect.bind(this),hit=this._hit.bind(this),nodes=[...document.querySelectorAll('#board .node')].map(e=>({id:e.dataset.pos,rect:rect(e)})),surfaces={publicHud:rect($('#publicTurnHud')),battleInfo:rect(document.querySelector('.battleInfo')),rail:rect(document.querySelector('.battleRail')),top:rect(document.querySelector('.battleTopStatus'))},collisions={};for(const[k,r]of Object.entries(surfaces))collisions[k]=nodes.filter(n=>hit(n.rect,r)).map(n=>n.id);return{screen:state.screen,phase:state.match?.phase||null,viewport:{w:innerWidth,h:innerHeight},profile:fieldCameraState?.kind||null,nodeCount:nodes.length,map:rect($('#battleMap')),collisions,criticalOverlapCount:collisions.publicHud.length+collisions.battleInfo.length,avatar:{state:battleMount?.root?.dataset?.state||null,facing:battleMount?.facingName||null}}},setupSnapshot(){const rect=this._rect.bind(this),hit=this._hit.bind(this),header=rect(document.querySelector('header.top')),controls=['[data-content="road_shield"]','[data-content="honey_hunt"]','[data-mode="2p"]','[data-mode="4p"]','[data-mode="2v2"]','#startMatch'].map(sel=>({sel,rect:rect(document.querySelector(sel))}));return{viewport:{w:innerWidth,h:innerHeight},header,controls,headerCollisions:controls.filter(c=>hit(c.rect,header)).map(c=>c.sel),offscreen:controls.filter(c=>!c.rect||c.rect.t<0||c.rect.l<0||c.rect.r>innerWidth||c.rect.b>innerHeight).map(c=>c.sel)}}};
window.GAMEROAD_VISUAL_SHELL_QA={version:'BROWSER-PLAYTEST-VISUAL-SHELL-REVIEW-001',rect(sel){const e=document.querySelector(sel),r=e?.getBoundingClientRect();return r?{l:r.left,t:r.top,r:r.right,b:r.bottom,w:r.width,h:r.height}:null},inside(sel){const r=this.rect(sel);return!!(r&&r.l>=0&&r.t>=0&&r.r<=innerWidth&&r.b<=innerHeight)},snapshot(){return{screen:state.screen,viewport:{w:innerWidth,h:innerHeight},contextVisible:$('#contextHelp')?.classList.contains('on')||false,home:{primary:this.rect('.codexBattleCta'),character:null,live:this.rect('.codexHomeCenterStage')},setup:{box:this.rect('.setupBox'),start:this.rect('#startMatch
--- END HIT 1 ---

--- HIT 2 OFFSET=10073361 RANGE=10067361:10079375 ---
>scores.set(p.id,(m.participants.get(p.id)||[]).reduce((a,e)=>a+e.currentValue,0)));const comparisonPlayers=battlePresentationPlayers(m);if(!await setBattlePresentation('compare',{attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,players:comparisonPlayers,teamTotals:m.mode==='2v2'?{A:m.players.filter(p=>p.team==='A').reduce((n,p)=>n+scores.get(p.id),0),B:m.players.filter(p=>p.team==='B').reduce((n,p)=>n+scores.get(p.id),0)}:null}))return;let winningPlayerIds=[];if(m.mode==='2v2'){const totals={A:0,B:0};m.players.forEach(p=>totals[p.team]+=scores.get(p.id));const max=Math.max(totals.A,totals.B),ties=['A','B'].filter(t=>totals[t]===max);let team;if(ties.length===1)team=ties[0];else{const active=m.players.find(p=>p.id===m.activeId);team=ties.includes(active.team)?active.team:ties[matchRnd(ties.length)]}winningPlayerIds=m.players.filter(p=>p.team===team).map(p=>p.id);log(`戦闘：組${team}が勝利。`)}else{const max=Math.max(...m.players.map(p=>scores.get(p.id)));const ties=m.players.filter(p=>scores.get(p.id)===max);const active=m.players.find(p=>p.id===m.activeId);const winner=ties.length===1?ties[0]:(ties.some(p=>p.id===active.id)?active:ties[matchRnd(ties.length)]);winningPlayerIds=[winner.id];log(`戦闘：${winner.name}が勝利。`)}if(!await setBattlePresentation('winner',{attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,players:battlePresentationPlayers(m,winningPlayerIds),winnerIds:[...winningPlayerIds],teamTotals:m.mode==='2v2'?{A:m.players.filter(p=>p.team==='A').reduce((n,p)=>n+scores.get(p.id),0),B:m.players.filter(p=>p.team==='B').reduce((n,p)=>n+scores.get(p.id),0)}:null}))return;const humanPlayer=m.players.find(p=>p.human);if(humanPlayer&&battleMount){if(winningPlayerIds.includes(humanPlayer.id))await setBattleRuntimeState('joy');else await setBattleRuntimeState('hit');}const resolutionPlayers=m.players.map(p=>({id:p.id,name:p.name,team:p.team||null,score:scores.get(p.id),winner:winningPlayerIds.includes(p.id),cards:(m.participants.get(p.id)||[]).map(e=>({cardId:e.cardId,label:cardLabel(e.cardId),value:e.currentValue,origin:e.origin}))}));const laneBefore=new Map(m.players.map(p=>[p.id,p.lanes[m.target.lane].length])),maxLaneBefore=new Map(m.players.map(p=>[p.id,Math.max(...Object.values(p.lanes).map(a=>a.length))]));m.players.forEach(p=>{const parts=m.participants.get(p.id)||[];if(winningPlayerIds.includes(p.id)){const lane=p.lanes[m.target.lane];parts.forEach(e=>lane.push(e.cardId));if(p.chip.length)p.chip.splice(0).forEach(id=>lane.push(id))}else parts.forEach(e=>p.chip.push(e.cardId))});const winningTeam=m.mode==='2v2'?(m.players.find(p=>winningPlayerIds.includes(p.id))?.team||null):null,teamTotals=m.mode==='2v2'?{A:m.players.filter(p=>p.team==='A').reduce((n,p)=>n+scores.get(p.id),0),B:m.players.filter(p=>p.team==='B').reduce((n,p)=>n+scores.get(p.id),0)}:null;m.lastBattleResolution={serial:++m.resolutionSeq,round:m.round,mode:m.mode,attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,winnerIds:[...winningPlayerIds],winningTeam,teamTotals,players:resolutionPlayers,laneGains:m.players.map(p=>({id:p.id,lane:m.target.lane,before:laneBefore.get(p.id),after:p.lanes[m.target.lane].length,added:p.lanes[m.target.lane].length-laneBefore.get(p.id)})).filter(x=>x.added>0),maxLaneProgress:m.players.map(p=>({id:p.id,before:maxLaneBefore.get(p.id),after:Math.max(...Object.values(p.lanes).map(a=>a.length))}))};const slaykiaAttackEnd=grSlaykiaAttackEndHook(m);m.participants=new Map(m.players.map(p=>[p.id,[]]));m.players.forEach(refill);m.battlePresentation={stage:'settle',round:m.round,mode:m.mode,attackerId:active.id,defenderId:defender.id,lane:m.target.lane,shield:m.target.shield,players:resolutionPlayers,winnerIds:[...winningPlayerIds],teamTotals};renderBattle();await battlePresentationWait(BATTLE_PRESENTATION_RESULT_MS,true);if(state.match!==m||state.screen!=='battle')return;const winners=slaykiaAttackEnd?.specialWinnerTokens?.length?[...slaykiaAttackEnd.specialWinnerTokens]:checkWin();m.battlePresentation=null;if(winners.length){m.busy=false;if(slaykiaAttackEnd?.specialWinnerTokens?.length)log('スレイキア：成功破壊16枚で特殊勝利。');else if(m.contentId==='honey_hunt')log('一列へ実札が七枚以上連続配置され、ハニーハントの勝利条件が成立しました。');return endMatch(winners)}nextRound()}catch(e){clearBattlePresentationWait();if(state.match===m){m.battlePresentation=null;m.busy=false;log(`戦闘停止：${e.message}`);renderBattle()}throw e}}
function checkWin(){if(state.match.mode==='2v2'){const teams=['A','B'].filter(t=>state.match.players.filter(p=>p.team===t).some(p=>Math.max(...Object.values(p.lanes).map(a=>a.length))>=7));return teams}return state.match.players.filter(p=>Math.max(...Object.values(p.lanes).map(a=>a.length))>=7).map(p=>p.id)}
function nextRound(){const m=state.match;m.lastBattleResolution=null;m.round++;m.phase='plan';m.activeId=null;m.target=null;m.battlePresentation=null;m.busy=false;m.players.forEach(p=>p.plan=null);initRoundRuntime(m);log(`第${m.round}巡。行動を予約します。`);renderBattle()}
function ranksFFA(){const ps=state.match.players.map(p=>({p,depth:Math.max(...Object.values(p.lanes).map(a=>a.length))})).sort((a,b)=>b.depth-a.depth);let prev=null,rank=0;return ps.map((x,i)=>{if(prev===null||x.depth!==prev)rank=i+1;prev=x.depth;return{player:x.p,rank,depth:x.depth}})}
function endMatch(winners){const m=state.match;let ranking;if(m.mode==='2v2'){const wt=winners[0];ranking=m.players.map(p=>({player:p,rank:p.team===wt?1:2,depth:Math.max(...Object.values(p.lanes).map(a=>a.length))}))}else ranking=ranksFFA();const me=ranking.find(x=>x.player.human),points=settleBattlePoints(m,me.rank);m.lastResult={ranking,headline:me.rank===1?'勝利':'結果',grade:me.rank,points};state.history.unshift({at:new Date().toISOString(),mode:m.mode,rank:me.rank,rounds:m.round,character:state.playerCharacterId,partner:state.selectedPartnerId,battlePointsEarned:points.earned,quickPoints:points.quick,battlePointsAfter:points.after});save();show('result');renderResult()}
async function renderResult(){const r=state.match?.lastResult;if(!r)return;const m=state.match,ordered=[...r.ranking].sort((a,b)=>a.rank-b.rank),me=ordered.find(x=>x.player.human),mode=({'2p':'二人','4p':'四人','2v2':'二対二'})[m.mode]||m.mode,content=m.contentId==='honey_hunt'?'ハニーハント':'ロード・シールド';$('#resultGrade').textContent=r.grade;$('#resultHeadline').textContent=r.headline;$('#resultSummary').textContent=`${m.round}巡で決着。 ${content}`;$('#resultLeadText')&&($('#resultLeadText').textContent=r.grade===1?'一位で決着':'最終順位');$('#resultLeadSub')&&($('#resultLeadSub').textContent=`${content} / ${mode}`);$('#resultMode')&&($('#resultMode').textContent=mode);$('#resultRounds')&&($('#resultRounds').textContent=`${m.round}巡`);$('#resultLane')&&($('#resultLane').textContent=`${me?.depth??0}枚`);renderResultBattlePoints(r);const box=$('#resultRanking');box.innerHTML='';ordered.forEach(x=>{const d=document.createElement('div');d.className='rankLine';d.innerHTML=`<b>${x.rank}位 ${x.player.human?'あなた':x.player.name}</b><span>最長列 ${x.depth}枚</span>`;box.appendChild(d)});resultMount=await mountChar('.resultWorld',state.playerCharacterId,r.grade===1?'joy':'defeated')}
$('#rematch').onclick=()=>startMatch();$('#leaveMatch').onclick=()=>{state.match=null;navigateRoot('home')};window.__GAMEROAD_QUICK_R14_QA__={version:'FULLGAME-QUICKTIMER-R14',constants:{windowMs:QUICK_WINDOW_MS,perfectRemainMs:QUICK_PERFECT_REMAIN_MS,goodRemainMs:QUICK_GOOD_REMAIN_MS,perfectPoints:QUICK_PERFECT_POINTS,goodPoints:QUICK_GOOD_POINTS,chestTarget:BATTLE_POINTS_CHEST_TARGET},snapshot(){const m=state.match,me=m?.players?.find(p=>p.human),rem=m?quickRemainingMs(m):null;return{screen:state.screen,round:m?.round||null,phase:m?.phase||null,remainingMs:rem,tier:m?quickTierForRemaining(rem):null,quickPoints:me?(m.quickPointsByPlayer?.[me.id]||0):0,awards:me?(m.quickAwards||[]).filter(a=>a.playerId===me.id).map(a=>({...a})):[],battlePoints:state.progression?.battlePoints||0,settlement:m?.battlePointsSettlement?safeClone(m.battlePointsSettlement):null,coilHidden:$('#quickCoil')?.hidden??null,coilText:$('#quickRemain')?.textContent||null,resultText:$('#resultBattlePoints')?.innerText||''}},setRemainingSeconds(sec){const m=state.match;if(!m?.quickWindow)return false;const v=Math.max(0,Math.min(30,Number(sec)||0));m._quickQaNowMs=m.quickWindow.deadlineAt-v*1000;renderQuickTimer();return true},clearTestClock(){const m=state.match;if(!m)return false;delete m._quickQaNowMs;renderQuickTimer();return true},setBattlePoints(n){state.progression={battlePoints:Math.max(0,Math.floor(Number(n)||0))};return state.progression.battlePoints},persistenceProbe(n){state.progression={battlePoints:Math.max(0,Math.floor(Number(n)||0))};save();state.progression={battlePoints:0};load();return{battlePoints:state.progression.battlePoints,storage:state.storage}}};window.GAMEROAD_3D_WALK_QA={version:'BROWSER-3D-BOARD-WALK-INTEGRATION-001',snapshot(){const m=state.match,me=m?.players?.find(p=>p.human);return{screen:state.screen,phase:m?.phase||null,position:me?.position||null,path:me?.plan?.path?[...me.plan.path]:null,avatarState:battleMount?.root?.dataset?.state||null,avatarFacing:battleMount?.facingName||null,nodeCount:document.querySelectorAll('#board .node').length,canvas:{w:$('#fieldCanvas')?.width||0,h:$('#fieldCanvas')?.height||0}}}};window.GAMEROAD_GRASSLAND3D_QA={version:'BROWSER-BOARD-GRASSLAND-3D-R4',snapshot(){const gl=$('#fieldCanvas')?.getContext('webgl');return{...fieldLastStats,webgl:!!gl,contextLost:!!gl?.isContextLost?.(),projectionCount:fieldProjection.size,nodeCount:document.querySelectorAll('#board .node').length,screen:state.screen}}};
window.GAMEROAD_BOARD_REVIEW_QA={version:'BROWSER-BOARD-PLAYTEST-REVIEW-LOOP-001',_rect(e){const b=e?.getBoundingClientRect();return b?{l:b.left,r:b.right,t:b.top,b:b.bottom,w:b.width,h:b.height}:null},_hit(a,b){return!!(a&&b&&Math.min(a.r,b.r)>Math.max(a.l,b.l)&&Math.min(a.b,b.b)>Math.max(a.t,b.t))},snapshot(){const rect=this._rect.bind(this),hit=this._hit.bind(this),nodes=[...document.querySelectorAll('#board .node')].map(e=>({id:e.dataset.pos,rect:rect(e)})),surfaces={publicHud:rect($('#publicTurnHud')),battleInfo:rect(document.querySelector('.battleInfo')),rail:rect(document.querySelector('.battleRail')),top:rect(document.querySelector('.battleTopStatus'))},collisions={};for(const[k,r]of Object.entries(surfaces))collisions[k]=nodes.filter(n=>hit(n.rect,r)).map(n=>n.id);return{screen:state.screen,phase:state.match?.phase||null,viewport:{w:innerWidth,h:innerHeight},profile:fieldCameraState?.kind||null,nodeCount:nodes.length,map:rect($('#battleMap')),collisions,criticalOverlapCount:collisions.publicHud.length+collisions.battleInfo.length,avatar:{state:battleMount?.root?.dataset?.state||null,facing:battleMount?.facingName||null}}},setupSnapshot(){const rect=this._rect.bind(this),hit=this._hit.bind(this),header=rect(document.querySelector('header.top')),controls=['[data-content="road_shield"]','[data-content="honey_hunt"]','[data-mode="2p"]','[data-mode="4p"]','[data-mode="2v2"]','#startMatch'].map(sel=>({sel,rect:rect(document.querySelector(sel))}));return{viewport:{w:innerWidth,h:innerHeight},header,controls,headerCollisions:controls.filter(c=>hit(c.rect,header)).map(c=>c.sel),offscreen:controls.filter(c=>!c.rect||c.rect.t<0||c.rect.l<0||c.rect.r>innerWidth||c.rect.b>innerHeight).map(c=>c.sel)}}};
window.GAMEROAD_VISUAL_SHELL_QA={version:'BROWSER-PLAYTEST-VISUAL-SHELL-REVIEW-001',rect(sel){const e=document.querySelector(sel),r=e?.getBoundingClientRect();return r?{l:r.left,t:r.top,r:r.right,b:r.bottom,w:r.width,h:r.height}:null},inside(sel){const r=this.rect(sel);return!!(r&&r.l>=0&&r.t>=0&&r.r<=innerWidth&&r.b<=innerHeight)},snapshot(){return{screen:state.screen,viewport:{w:innerWidth,h:innerHeight},contextVisible:$('#contextHelp')?.classList.contains('on')||false,home:{primary:this.rect('.codexBattleCta'),character:null,live:this.rect('.codexHomeCenterStage')},setup:{box:this.rect('.setupBox'),start:this.rect('#startMatch'),character:this.rect('#setupR
--- END HIT 2 ---

--- HIT 3 OFFSET=10210077 RANGE=10204077:10216091 ---
rRule)){rule=FR.localPartnerRule;const pd=ensurePartnerRuntime(state.match);if(pd)pd.rule=rule}else if(rule)FR.localPartnerRule=rule}if(FR.role==='host'){FR.partnerRules.P1=rule;if(state.match?.players?.find(p=>p.id==='P1')?.hateExploded)queueMicrotask(()=>friendForcedDecision('P1','rule-update').catch(e=>{FR.hateBlockers.P1=String(e?.message||e)}));return true}if(FR.role==='guest'&&FR.assignedPid){send({type:'partner_rule',rule});renderPartnerRuntime();return true}return false}
function hostStart(){if(FR.role!=='host'||FR.roomState!=='lobby'||!allReady())return false;FR.roomState='playing';FR.matchNo++;FR.planValues={};FR.baseProgression=Number(state.progression?.battlePoints||0);FR.baseHistoryLength=state.history.length;state.setupMode=FR.playerCount===4?'4p':'2p';state.setupContent=FR.contentId||'road_shield';const m=startMatch();if(!m){FR.roomState='lobby';return false}m.friendRoom={build:BUILD,code:FR.code,playerCount:FR.playerCount,localPid:'P1',host:true,resultPersistenceEnabled:false,noBotFill:true};m.players.forEach(p=>{p.roomHuman=true;p.roomConnected=true;if(p.id!=='P1'){p.name=p.id;p.human=false}});FR.partnerRules={P1:FRIEND_PARTNER_RULES.has(currentPartnerRule())?currentPartnerRule():null};FR.hateBlockers={};friendResetHateClocks(performance.now());document.body.classList.add('friend-room-match');const pts=document.getElementById('resultBattlePoints');if(pts)pts.hidden=true;broadcastSnapshots();return true}
function publicPlayer(p,viewer){return{id:p.id,name:p.id===viewer?'あなた':p.name,human:p.id===viewer,roomHuman:true,character:p.character,team:p.team||null,position:p.position,awake:Number(p.awake)||0,mana:Array.from({length:p.mana?.length||0},(_,i)=>'M'+i),chip:Array.from({length:p.chip?.length||0},()=> '__PUBLIC_COUNT__'),lanes:clone(p.lanes||{L:[],C:[],R:[]}),deck:Array.from({length:p.deck?.length||0},()=> '__HIDDEN__'),hand:p.id===viewer?[...p.hand]:Array.from({length:p.hand?.length||0},()=> '__HIDDEN__'),shields:p.id===viewer?clone(p.shields):{L:'__HIDDEN__',C:'__HIDDEN__',R:'__HIDDEN__'},plan:p.id===viewer?clone(FR.planValues[p.id]||p.plan||null):null,nextCostReduction:Number(p.nextCostReduction)||0,battleBlockedRound:Number(p.battleBlockedRound)||0,royalUsed:publicRoyalUsed(p),sourceDeckIds:[],sourceDeckSize:Number(p.sourceDeckSize)||0}}
function project(viewer){const m=state.match,p=m?.players?.find(x=>x.id===viewer);if(!m||!p)return null;const ordered=[p,...m.players.filter(x=>x.id!==viewer)].map(x=>publicPlayer(x,viewer));return{schema:'GAMEROAD_FRIEND_ROOM_R2_PROJECTION',build:BUILD,viewer,code:FR.code,matchInstanceId:m.id,roomState:FR.roomState,playerCount:FR.playerCount,round:m.round,phase:m.phase,activeId:m.activeId,target:clone(m.target),mode:m.mode,contentId:m.contentId,log:[...m.log],players:ordered,busy:!!m.busy,honey:{byNode:clone(m.honey?.byNode||{}),receipts:[],generatedRounds:[]},lastBattleResolution:clone(m.lastBattleResolution),battlePresentation:clone(m.battlePresentation),resolutionSeq:Number(m.resolutionSeq)||0,result:FR.roomState==='result'?clone(m.lastResult):null,participants:participantsHost(),selectionContract:grPublicSelectionContract(m,viewer),private:{pid:viewer,planSubmitted:!!FR.planValues[viewer],hateTime:Number(p.hateTime)||0,hateExploded:!!p.hateExploded,hateExplosionCount:Number(p.hateExplosionCount)||0}}}
function sendSnapshot(to){const s=slotByClient(to);if(!s?.connected)return;send({type:'snapshot',to,projection:project(s.pid)})}
function broadcastSnapshots(){if(FR.role!=='host'||!state.match)return;for(const s of Object.values(FR.slots))if(s.connected)sendSnapshot(s.clientId);renderFriendHostBattleState()}
function projectionToMatch(pr){const m={id:'FRIEND-'+pr.code+':'+String(pr.matchInstanceId||'legacy'),mode:pr.mode,contentId:pr.contentId,round:pr.round,phase:pr.phase,players:pr.players.map(x=>clone(x)),activeId:pr.activeId,target:clone(pr.target),log:[...pr.log],lastResult:null,lastBattleResolution:clone(pr.lastBattleResolution),battlePresentation:clone(pr.battlePresentation),resolutionSeq:pr.resolutionSeq,projection:['N','E','S','W'],abilityRng:1,busy:!!pr.busy,honey:{byNode:clone(pr.honey?.byNode||{}),receipts:new Set(),generatedRounds:new Set()},participants:new Map(pr.players.map(p=>[p.id,[]])),participantSeq:0,resolvedAbilityKeys:new Set(),abilityOrderReceipts:new Map(),testAutoChoices:false,quickWindow:null,quickPointsByPlayer:{},quickAwards:[],playerDeckRule:{id:pr.selectionContract?.regulationId||DECK_RULE.id,revision:pr.selectionContract?.regulationRevision||DECK_RULE.revision,mainCount:Number(pr.selectionContract?.mainCount)||Number(pr.players?.find(x=>x.id===pr.viewer)?.sourceDeckSize)||0,exCount:Number(pr.selectionContract?.exCount)||0},friendRoom:{build:BUILD,code:pr.code,playerCount:pr.playerCount,localPid:pr.viewer,host:false,resultPersistenceEnabled:false,noBotFill:true}};m.selectionLock=Object.freeze({version:1,playerId:pr.viewer,mode:m.mode,contentId:m.contentId,regulationId:m.playerDeckRule.id,regulationRevision:m.playerDeckRule.revision,mainCount:m.playerDeckRule.mainCount,exCount:m.playerDeckRule.exCount,sourceDeckDigest:null});const me=m.players.find(x=>x.id===pr.viewer);if(me){me.hateTime=Math.max(0,Math.min(1000,Number(pr.private?.hateTime)||0));me.hateExploded=!!pr.private?.hateExploded;me.hateExplosionCount=Math.max(0,Number(pr.private?.hateExplosionCount)||0)}if(pr.result){const ranking=(pr.result.ranking||[]).map(r=>{const pp=m.players.find(x=>x.id===r.player?.id);return{player:pp,rank:r.rank,depth:r.depth}}).filter(r=>r.player);const me=ranking.find(r=>r.player.id===pr.viewer);m.lastResult={ranking,headline:me?.rank===1?'勝利':'結果',grade:me?.rank||0,points:null,friendRoom:true}}return m}
function applyProjection(pr){if(!pr||pr.viewer!==FR.assignedPid)return;FR.snapshot=pr;FR.roomState=pr.roomState;state.match=projectionToMatch(pr);document.body.classList.add('friend-room-match');const pts=document.getElementById('resultBattlePoints');if(pts)pts.hidden=true;if(pr.roomState==='result'){show('result');renderResult();if(pts)pts.hidden=true;const h=document.getElementById('resultPointsEarned');if(h)h.textContent='フレンド対戦・報酬なし'}else{show('battle');renderBattle();renderRemoteWait();syncFriendPartnerRule()} }
function renderRemoteWait(){if(FR.role!=='guest'||state.screen!=='battle')return;const b=document.getElementById('readyPlan'),pr=FR.snapshot;if(b&&pr?.private?.planSubmitted){b.disabled=true;b.querySelector('.quickReadyText')&&(b.querySelector('.quickReadyText').textContent='予約済み・全員待ち')}const act=state.match?.players?.find(p=>p.id===state.match.activeId);if(state.match?.phase==='target'&&act?.id!==FR.assignedPid){document.getElementById('targetBox')?.classList.remove('on')}}
function renderFriendHostBattleState(){if(FR.role!=='host'||state.screen!=='battle'||!state.match?.friendRoom)return;const b=document.getElementById('readyPlan');if(b&&FR.planValues.P1){b.disabled=true;b.querySelector('.quickReadyText')&&(b.querySelector('.quickReadyText').textContent='予約済み・全員待ち')}}
function legalPlan(p,plan){if(!p||!plan?.roadId||!plan?.battleId)return{ok:false,reason:'plan_missing'};if(plan.roadId===plan.battleId)return{ok:false,reason:'same_card'};if(!p.hand.includes(plan.roadId)||!p.hand.includes(plan.battleId))return{ok:false,reason:'reservation_not_owned'};if(!roadSelectable(plan.roadId)||!roadSelectable(plan.battleId))return{ok:false,reason:'card_not_selectable'};const path=Array.isArray(plan.path)&&plan.path.length?plan.path:[p.position];if(path[0]!==p.position)return{ok:false,reason:'path_origin_mismatch'};const lim=normalMoveLimitForRoad(plan.roadId);if(path.length-1>lim)return{ok:false,reason:'move_limit'};for(let i=1;i<path.length;i++)if(!neighbors(path[i-1],p).includes(path[i])||path.slice(0,i).includes(path[i]))return{ok:false,reason:'path_invalid'};return{ok:true,plan:{roadId:plan.roadId,battleId:plan.battleId,path:[...path]}}}
async function submitHostPlan(){const m=state.match,p=m?.players?.find(x=>x.id==='P1');if(!m?.friendRoom||FR.role!=='host'||m.phase!=='plan'||m.busy)return false;if(p?.hateExploded){queueMicrotask(()=>friendForcedDecision('P1','manual-block').catch(e=>{FR.hateBlockers.P1=String(e?.message||e)}));return toast('ヘイト爆発中は必須判断を手動変更できません')}friendTickHate(performance.now());updatePlanFromSelect();const v=legalPlan(p,p.plan);if(!v.ok){toast('予約内容を確認してください');return false}if(FR.planValues.P1)return false;FR.planValues.P1=v.plan;window.GAMEROAD_PARTNER_COMMENT?.emit?.({eventId:`${m.id}:set:${m.round}:road:${p.id}`,kind:'dialogue',speechIntent:'road_set',cardId:v.plan.roadId,loggable:false,ttlMs:9000});window.GAMEROAD_PARTNER_COMMENT?.emit?.({eventId:`${m.id}:set:${m.round}:battle:${p.id}`,kind:'dialogue',speechIntent:'battle_set',cardId:v.plan.battleId,loggable:false,ttlMs:9000});friendSyncHateClocks(performance.now());broadcastSnapshots();await maybeResolvePlans();return true}
function submitGuestPlan(){const m=state.match,p=m?.players?.find(x=>x.id===FR.assignedPid);if(FR.role!=='guest'||!m?.friendRoom||m.phase!=='plan'||m.busy||FR.snapshot?.private?.planSubmitted)return false;if(FR.snapshot?.private?.hateExploded)return toast('ヘイト爆発中は必須判断を手動変更できません');updatePlanFromSelect();const v=legalPlan(p,p.plan);if(!v.ok){toast('予約内容を確認してください');return false}window.GAMEROAD_PARTNER_COMMENT?.emit?.({eventId:`${m.id}:set:${m.round}:road:${p.id}`,kind:'dialogue',speechIntent:'road_set',cardId:v.plan.roadId,loggable:false,ttlMs:9000});window.GAMEROAD_PARTNER_COMMENT?.emit?.({eventId:`${m.id}:set:${m.round}:battle:${p.id}`,kind:'dialogue',speechIntent:'battle_set',cardId:v.plan.battleId,loggable:false,ttlMs:9000});send({type:'plan',round:m.round,phase:m.phase,payload:v.plan});return true}
async function maybeResolvePlans(){if(FR.role!=='host'||!state.match||state.match.phase!=='plan')return;const need=pidList();if(!need.every(pid=>FR.planValues[pid]))return;const plans=Object.fromEntries(need.map(pid=>[pid,clone(FR.planValues[pid])]));FR.planValues={};await resolveHumanPlans(plans)}
async function resolveHumanPlans(plans){const m=state.match;if(m.busy)return;m.busy=true;try{if(!battleMount)await updateBattleAvatar();for(const p of m.players){const v=legalPlan(p,plans[p.id]);if(!v.ok)throw Error(p.id+':'+v.reason);p.plan=v.plan}m.lastBattleResolution=null;m.players.forEach(p=>removeHand(p,p.plan.roadId));m.activeId=pickActive(m.players).id;const roadEntries=m.players.map(p=>makeParticipant(p,p.plan.roadId,'road'));m.phase='reveal';log('ロードカードを公開。公開時能力を小さい印刷値順で解決します。');renderBattle();broadcastSnapshots();await resolveAbilityBundle(roadEntries,'road');if(m.contentId==='honey_hunt'&&!m.honey.generatedRounds.has(m.round)){spawnHoneyFromRoads();log('公開された1〜6と、列上の同数字ロードカード実体を照合してハニー発生源を更新しました。')}m.phase='move';renderBattle();broadcastSnapshots();const order=randomOrderByRoad(m.players);for(const p of order){await animateResolvedPath(p);if(m.contentId!=='honey_hunt'&&CARDS[p.plan.roadId].effect_type==='none'){const before=p.awake;p.awake=Math.min(p.mana.length,p.awake+1);if(p.awake>before)log(`${p.name}：能力なしロードカードの成立でマナを1枚起こした。`)}log(`${p.name}：${cardLabel(p.plan.roadId)} / ${p.plan.path.length-1}歩`)}const act=m.players.find(p=>p.id===m.activeId);m.phase='target';m.busy=false;friendSyncHateClocks(performance.now());log(`${act.name}が攻撃権を取得。`);renderBattle();broadcastSnapshots()}catch(e){m.busy=false;log(`処理停止：${e.message}`);renderBattle();broadcastSnapshots();throw e}}
async function submitGuestTarget(){const m=state.match,act=m?.players?.find(p=>p.id===m.activeId);if(FR.role!=='guest'||m?.phase!=='target'||act?.id!==FR.assignedPid)return false;if(FR.snapshot?.private?.hateExploded)return toast('ヘイト爆発中は必須判断を手動変更できません');const defenderId=document.getElementById('targetPlayer').value;if(!legalOpponents(act).some(p=>p.id===defenderId))return false;send({type:'target',round:m.round,phase:m.phase,payload:{defenderId,lane:document.getElementById('targetLane').value,shield:document.getElementById('targetShield').value}});return true}
function choicePayload(items){return items.map((it,i)=>({i,label:abilityOptionLabel(
--- END HIT 3 ---

--- HIT 4 OFFSET=10227094 RANGE=10221094:10233108 ---
_forced');queueMicrotask(()=>friendForcedDecision(s.pid,'manual-block').catch(e=>{FR.hateBlockers[s.pid]=String(e?.message||e);broadcastSnapshots()}));return}const t=m.payload||{},def=String(t.defenderId||'');if(!legalOpponents(act).some(p=>p.id===def)||!['L','C','R'].includes(t.lane)||!['L','C','R'].includes(t.shield)){reject(cid,'illegal_target');return}mm.target={defenderId:def,lane:t.lane,shield:t.shield};await resolveBattle();broadcastSnapshots();return}if(m.type==='choice_response'){const w=FR.choiceWait.get(String(m.choiceId||''));if(!w||w.pid!==s.pid){reject(cid,'choice_not_actor');return}friendTickHate(performance.now());const actor=state.match?.players?.find(p=>p.id===s.pid);if(actor?.hateExploded){reject(cid,'hate_forced');queueMicrotask(()=>friendForcedDecision(s.pid,'manual-block').catch(e=>{FR.hateBlockers[s.pid]=String(e?.message||e);broadcastSnapshots()}));return}const idx=[...(m.indices||[])].map(Number).filter(i=>Number.isInteger(i)&&i>=0&&i<w.items.length);w.resolve([...new Set(idx)]);return}}
function handleGuestMessage(m){if(m.to&&m.to!==FR.clientId)return;if(m.type==='accept'){FR.assignedPid=m.pid;FR.authToken=m.authToken||'';FR.playerCount=Number(m.playerCount)===4?4:2;FR.contentId=m.contentId||'road_shield';FR.roomState=m.roomState||'lobby';store(clientKey(FR.code),FR.clientId);store(authKey(FR.code),FR.authToken);renderLobby();send({type:'sync'});return}if(m.type==='reject'){FR.lastReject=m.reason||'reject';toast('参加できません: '+FR.lastReject);renderLobby();return}if(m.type==='host_close'){FR.roomState='closed';toast('部屋主が部屋を閉じました');show('friendroom');renderLobby();return}if(m.type==='lobby'){FR.participants=clone(m.lobby?.participants||[]);FR.roomState=m.lobby?.roomState||'lobby';FR.playerCount=Number(m.lobby?.playerCount)===4?4:2;FR.contentId=m.lobby?.contentId||FR.contentId;FR.guestReady=!!FR.participants.find(p=>p.pid===FR.assignedPid)?.ready;if(state.screen!=='friendroom')show('friendroom');renderLobby();return}if(m.type==='snapshot'){applyProjection(m.projection);return}if(m.type==='choice_request'){showRemoteChoice(m.choice);return}}
function renderLobby(){const root=document.getElementById('friendRoomPanel');if(!root)return;const tm=document.getElementById('topMode');if(state.screen==='friendroom'&&tm)tm.textContent='フレンドルーム';const idle=!FR.role,parts=FR.role==='host'?participantsHost():(FR.participants.length?FR.participants:pidList().map(pid=>({pid,connected:pid===FR.assignedPid,ready:false}))),role=FR.role==='host'?'部屋主':FR.role==='guest'?'参加者':'未参加',canReady=FR.roomState==='lobby'&&((FR.role==='host')||(FR.role==='guest'&&FR.assignedPid)),ownReady=FR.role==='host'?FR.hostReady:FR.guestReady;root.innerHTML=`<div class="friendRoomHero"><div><div class="k">FRIEND ROOM</div><div class="h">フレンドルーム</div></div><div class="friendCode"><span>合言葉</span><b>${FR.code||'-------'}</b><small>${role} / ${FR.playerCount}人 / ${FR.transport==='ws_relay_recipient_r21'?'遠隔WS':'同一アドレス'}</small></div></div><div class="friendRoomGrid"><div class="friendRoomCard"><h3>部屋</h3>${idle?`<div class="friendCreateRow"><button class="btn primary" id="friendCreate2">二人部屋を作る</button><button class="btn" id="friendCreate4">四人部屋を作る</button></div><label class="friendJoinLabel">七文字の合言葉<input id="friendJoinCode" maxlength="9" autocomplete="off" inputmode="text" placeholder="ABCDEFG"></label><button class="btn" id="friendJoinBtn">合言葉で入る</button>`:`<div class="friendStatus">状態: ${FR.roomState==='lobby'?'準備中':FR.roomState==='playing'?'対戦中':FR.roomState==='result'?'対戦終了':FR.roomState}</div><button class="btn danger" id="friendLeaveBtn">部屋を出る</button>`}${FR.lastReject?`<div class="friendReject">${FR.lastReject}</div>`:''}</div><div class="friendRoomCard"><h3>参加者</h3><div class="friendParticipants">${parts.map(p=>`<div class="friendParticipant ${p.connected?'connected':''} ${p.ready?'ready':''}"><b>${p.pid}</b><span>${p.connected?'接続':'空き'}</span><em>${p.ready?'準備完了':'待機'}</em></div>`).join('')}</div>${!idle?`<div class="friendActions"><button class="btn ${ownReady?'on':''}" id="friendReadyBtn" ${canReady?'':'disabled'}>${ownReady?'準備を戻す':'準備完了'}</button>${FR.role==='host'?`<button class="btn primary" id="friendStartBtn" ${allReady()?'':'disabled'}>部屋主が開始</button>`:''}</div>`:''}</div></div>`;document.getElementById('friendCreate2')?.addEventListener('click',()=>createRoom(2));document.getElementById('friendCreate4')?.addEventListener('click',()=>createRoom(4));document.getElementById('friendJoinBtn')?.addEventListener('click',()=>joinRoom(document.getElementById('friendJoinCode')?.value));document.getElementById('friendReadyBtn')?.addEventListener('click',toggleReady);document.getElementById('friendStartBtn')?.addEventListener('click',hostStart);document.getElementById('friendLeaveBtn')?.addEventListener('click',()=>{grClearPartnerMatchSession(state.match);reset(true);state.match=null;show('setup')})}
function returnLobby(){if(FR.role!=='host'||!['result','playing'].includes(FR.roomState))return false;FR.roomState='lobby';FR.hostReady=false;for(const s of Object.values(FR.slots))s.ready=false;FR.planValues={};FR.choiceWait.forEach(x=>x.reject?.(Error('rematch_reset')));FR.choiceWait.clear();grClearPartnerMatchSession(state.match);state.match=null;document.body.classList.remove('friend-room-match');show('friendroom');sendAllLobby();return true}
function friendEndMatch(winners){const m=state.match;let ranking;if(m.mode==='2v2'){const wt=winners[0];ranking=m.players.map(p=>({player:p,rank:p.team===wt?1:2,depth:Math.max(...Object.values(p.lanes).map(a=>a.length))}))}else ranking=ranksFFA();const me=ranking.find(x=>x.player.id==='P1');m.lastResult={ranking,headline:me?.rank===1?'勝利':'結果',grade:me?.rank||0,points:null,friendRoom:true};FR.roomState='result';if(FR.baseProgression!=null)state.progression={battlePoints:FR.baseProgression};if(FR.baseHistoryLength!=null&&state.history.length>FR.baseHistoryLength)state.history.splice(0,state.history.length-FR.baseHistoryLength);broadcastSnapshots();show('result');renderResult();const pts=document.getElementById('resultBattlePoints');if(pts)pts.hidden=true;return m.lastResult}
const baseResolveAbilityEntry=resolveAbilityEntry;
resolveAbilityEntry=async function(entry,phase){if(FR.role==='host'&&state.match?.friendRoom){FR.choiceActorPid=entry.ownerId;try{return await baseResolveAbilityEntry(entry,phase)}finally{FR.choiceActorPid=null}}return baseResolveAbilityEntry(entry,phase)};
const baseChooseAbilityItems=chooseAbilityItems;
chooseAbilityItems=async function(title,hint,items,min,max,human){const pid=FR.choiceActorPid;if(FR.role==='host'&&state.match?.friendRoom&&pid&&pid!=='P1'&&slotByPid(pid)?.connected)return requestRemoteChoice(pid,title,hint,items,min,max);return baseChooseAbilityItems(title,hint,items,min,max,human)};
const baseReplacementBattleCard=replacementBattleCard;
replacementBattleCard=async function(p,reason){if(FR.role==='host'&&state.match?.friendRoom&&p?.id!=='P1'&&p?.roomHuman){const opts=p.hand.filter(id=>CARDS[id]?.runtime_status==='playable').map(cardId=>({kind:'hand',cardId,id:cardId,detail:reason}));if(!opts.length)return null;const ch=await requestRemoteChoice(p.id,'バトルカードを再確認',reason,opts,1,1);return ch[0]?.cardId||null}return baseReplacementBattleCard(p,reason)};
const baseNextRound=nextRound;
nextRound=function(){const r=baseNextRound();if(FR.role==='host'&&state.match?.friendRoom){FR.planValues={};friendSyncHateClocks(performance.now());broadcastSnapshots()}return r};
const baseEndMatch=endMatch;
endMatch=function(winners){if(FR.role==='host'&&state.match?.friendRoom)return friendEndMatch(winners);return baseEndMatch(winners)};
const baseRenderBattle=renderBattle;
renderBattle=function(){const r=baseRenderBattle();if(FR.role==='guest'&&state.match?.friendRoom)renderRemoteWait();else if(FR.role==='host'&&state.match?.friendRoom)renderFriendHostBattleState();return r};
const ready=document.getElementById('readyPlan'),baseReady=ready?.onclick;
if(ready)ready.onclick=()=>{if(state.match?.friendRoom){const p=FR.role==='host'?submitHostPlan():submitGuestPlan();return Promise.resolve(p).catch(console.error)}return baseReady?.()};
const target=document.getElementById('confirmTarget'),baseTarget=target?.onclick;
if(target)target.onclick=async()=>{if(state.match?.friendRoom&&FR.role==='guest')return submitGuestTarget();if(FR.role==='host'&&state.match?.friendRoom&&state.match.players?.find(p=>p.id==='P1')?.hateExploded){queueMicrotask(()=>friendForcedDecision('P1','manual-block').catch(e=>{FR.hateBlockers.P1=String(e?.message||e)}));return toast('ヘイト爆発中は必須判断を手動変更できません')}friendTickHate(performance.now());const r=await baseTarget?.();if(FR.role==='host'&&state.match?.friendRoom){friendSyncHateClocks(performance.now());broadcastSnapshots()}return r};
const rem=document.getElementById('rematch'),baseRem=rem?.onclick;
if(rem)rem.onclick=()=>{if(state.match?.friendRoom||FR.roomState==='result'){if(FR.role==='host')return returnLobby();show('friendroom');renderLobby();return}return baseRem?.()};
const leave=document.getElementById('leaveMatch'),baseLeave=leave?.onclick;
if(leave)leave.onclick=()=>{if(state.match?.friendRoom){reset(true);state.match=null;document.body.classList.remove('friend-room-match');show('setup');return}return baseLeave?.()};
document.getElementById('partnerRule')?.addEventListener('change',()=>syncFriendPartnerRule(true));
document.getElementById('friendRoomEntry')?.addEventListener('click',()=>{show('friendroom');renderLobby()});
window.addEventListener('beforeunload',()=>closeChannel(true));
const auto=validCode(new URLSearchParams(location.search).get('friend')||'');if(auto)setTimeout(()=>joinRoom(auto),50);
function qaState(){return{build:BUILD,role:FR.role,code:FR.code,pid:FR.assignedPid,clientId:FR.clientId,transport:FR.transport,playerCount:FR.playerCount,roomState:FR.roomState,ready:FR.role==='host'?FR.hostReady:FR.guestReady,participants:FR.role==='host'?participantsHost():clone(FR.participants),lastReject:FR.lastReject,planSubmitted:FR.role==='host'?Object.keys(FR.planValues):FR.snapshot?.private?.planSubmitted||false,hateAuthority:FR.role==='host'?{values:Object.fromEntries((state.match?.players||[]).filter(p=>p.roomHuman).map(p=>[p.id,{hateTime:Number(p.hateTime)||0,hateExploded:!!p.hateExploded,hateExplosionCount:Number(p.hateExplosionCount)||0,signature:friendHateSignature(p.id),rule:friendRule(p.id),blocker:FR.hateBlockers[p.id]||null}])),clockOwner:'host'}:{private:clone(FR.snapshot?.private||null),clockOwner:'host_snapshot'},match:state.match?{mode:state.match.mode,phase:state.match.phase,round:state.match.round,activeId:state.match.activeId,friend:!!state.match.friendRoom,players:state.match.players.map(p=>({id:p.id,human:p.human,roomHuman:!!p.roomHuman,hand:p.hand.length,position:p.position})),historyLength:state.history.length,battlePoints:Number(state.progression?.battlePoints||0)}:null}}
window.GAMEROAD_FRIEND_ROOM_R2={BUILD,create:createRoom,join:joinRoom,ready:toggleReady,start:hostStart,leave:()=>reset(true),returnLobby,status:qaState,projectFor:pid=>FR.role==='host'?project(pid):null,rawSend:o=>send(o),submitPlan:()=>FR.role==='host'?submitHostPlan():submitGuestPlan(),submitTarget:submitGuestTarget,forceSnapshot:()=>{if(FR.role==='host')broadcastSnapshots()},qaSetFast:()=>{battlePresentationRuntime.fast=true;return true},qaSetNoEffectHands:()=>{if(FR.role!=='host'||!state.match?.friendRoom)return null;const out={};for(const p of state.match.players){const pool=[...p.hand,...p.deck],picked=[];for(const id of pool){if(roadSelectable(id)&&CARDS[id]?.runtime_status==='playable'&&CARDS[id]?.effect_type==='none'&&!picked.includes(id)){picked.push(id);if(picked.length===3)break}}if(picked.length<2)throw Error('not-enough-noeffect-'+p.id);const remain=[...pool];for(const id of picked){const i=remain.indexOf(id);if(i>=0)remain.splice(i,1)}p.hand=picked;p.deck=remain;out[p.id]=[...picked]}broadcastSnapshots();renderBattle();return out},qaPlans:()=>state.match?{activeId:state.match.activeId,phase:state.match.p
--- END HIT 4 ---