html_bytes=11831149
html_sha256=ae0aeb66374cbaf2e5f7c3edb58aff8cb43651e6cfb437cd3ab8e13a845a99e0

=== function replayFor ===
-- hit 1 index=10127615 --
ng(c.candidateId),c]));return (ids||[]).map(id=>partnerPublicCandidate(map.get(String(id)))).filter(Boolean)}
function partnerAdviceEnvelope({m=state.match,rule=null,kind='generic',status='ready',recommendation=null,alternatives=[],reason=null,reversalCondition=null,publicEvidence=null,error=null}){return Object.freeze({schema:'GAMEROAD_PARTNER_ADVICE_ENVELOPE_V1',status,kind,rule,stateVersion:partnerAdviceStateVersion(m),recommendation,alternatives,reason,reversalCondition,publicEvidence,error})}
function partnerAdviceStaleEnvelope(m=state.match){return partnerAdviceEnvelope({m,rule:currentPartnerRule(),kind:'stale',status:'blocked',error:'STALE_STATE',reason:'盤面状態が助言作成時から変わりました。',reversalCondition:'現在の状態から助言を再取得してください。',publicEvidence:null})}
window.__GAMEROAD_PARTNER_ADVICE_STATE_VERSION__=partnerAdviceStateVersion;
window.__GAMEROAD_PARTNER_ADVICE_STALE__=partnerAdviceStaleEnvelope;
function replayFor(candidates,rule=currentPartnerRule()){if(!rule)return{ok:false,error:'PARTNER_RULE_NOT_SELECTED'};return partnerReplay({rule,candidates})}
function partnerAdviceNow(){const m=state.match,pd=ensurePartnerRuntime();if(!m||!pd)return null;const rule=pd.rule;if(!rule){pd.advice='作戦規則が未選択です。左端・右端・最大・最小の正式4規則から選んでください。';pd.adviceEnvelope=partnerAdviceEnvelope({m,kind:'rule',status:'blocked',error:'PARTNER_RULE_NOT_SELECTED',reason:'正式4規則のいずれかを明示選択する必要があります。'});renderPartnerRuntime();toast('作戦規則を選んでください');return null}
  if(m.phase==='plan'){
    const roadCandidates=planCardCandidates('road'),road=replayFor(roadCandidates,rule);let battleCandidates=[],battle={ok:false,error:'ROAD_UNRESOLVED'};
    if(road.ok&&road.selected){battleCandidates=planCardCandidates('battle').filter(c=>c.candidateId!==road.selected.candidateId);battle=replayFor(battleCandidates,rule)}
    if(!road.ok||!battle.ok){pd.advice=`アドバイスできません：${road.error||battle.error}`;pd.adviceEnvelope=partnerAdviceEnvelope({m,rule,kind:'plan',status:'blocked',error:road.error||battle.error,reason:'合法候補の再生に失敗したため、カードを勝手に選びません。',publicEvidence:{roadCandidates:roadCandidates.map(partnerPublicCandidate),battleCandidates:battleCandidates.map(partnerPublicCandidate)}});renderPartnerRuntime();return null}
    const roadSelected=partnerPublicCandidate(road.selected),battleSelected=partnerPublicCandidate(battle.selected),roadAlternatives=partnerAdviceCandidates(road.ordered.slice(1,3),roadCandidates),battleAlternatives=partnerAdviceCandidates(battle.ordered.slice(1,3),battleCandidates);
    pd.adviceEnvelope=partnerAdviceEnvelope({m
count_shown=1

=== replayFor( ===
-- hit 1 index=10127624 --
idateId),c]));return (ids||[]).map(id=>partnerPublicCandidate(map.get(String(id)))).filter(Boolean)}
function partnerAdviceEnvelope({m=state.match,rule=null,kind='generic',status='ready',recommendation=null,alternatives=[],reason=null,reversalCondition=null,publicEvidence=null,error=null}){return Object.freeze({schema:'GAMEROAD_PARTNER_ADVICE_ENVELOPE_V1',status,kind,rule,stateVersion:partnerAdviceStateVersion(m),recommendation,alternatives,reason,reversalCondition,publicEvidence,error})}
function partnerAdviceStaleEnvelope(m=state.match){return partnerAdviceEnvelope({m,rule:currentPartnerRule(),kind:'stale',status:'blocked',error:'STALE_STATE',reason:'盤面状態が助言作成時から変わりました。',reversalCondition:'現在の状態から助言を再取得してください。',publicEvidence:null})}
window.__GAMEROAD_PARTNER_ADVICE_STATE_VERSION__=partnerAdviceStateVersion;
window.__GAMEROAD_PARTNER_ADVICE_STALE__=partnerAdviceStaleEnvelope;
function replayFor(candidates,rule=currentPartnerRule()){if(!rule)return{ok:false,error:'PARTNER_RULE_NOT_SELECTED'};return partnerReplay({rule,candidates})}
function partnerAdviceNow(){const m=state.match,pd=ensurePartnerRuntime();if(!m||!pd)return null;const rule=pd.rule;if(!rule){pd.advice='作戦規則が未選択です。左端・右端・最大・最小の正式4規則から選んでください。';pd.adviceEnvelope=partnerAdviceEnvelope({m,kind:'rule',status:'blocked',error:'PARTNER_RULE_NOT_SELECTED',reason:'正式4規則のいずれかを明示選択する必要があります。'});renderPartnerRuntime();toast('作戦規則を選んでください');return null}
  if(m.phase==='plan'){
    const roadCandidates=planCardCandidates('road'),road=replayFor(roadCandidates,rule);let battleCandidates=[],battle={ok:false,error:'ROAD_UNRESOLVED'};
    if(road.ok&&road.selected){battleCandidates=planCardCandidates('battle').filter(c=>c.candidateId!==road.selected.candidateId);battle=replayFor(battleCandidates,rule)}
    if(!road.ok||!battle.ok){pd.advice=`アドバイスできません：${road.error||battle.error}`;pd.adviceEnvelope=partnerAdviceEnvelope({m,rule,kind:'plan',status:'blocked',error:road.error||battle.error,reason:'合法候補の再生に失敗したため、カードを勝手に選びません。',publicEvidence:{roadCandidates:roadCandidates.map(partnerPublicCandidate),battleCandidates:battleCandidates.map(partnerPublicCandidate)}});renderPartnerRuntime();return null}
    const roadSelected=partnerPublicCandidate(road.selected),battleSelected=partnerPublicCandidate(battle.selected),roadAlternatives=partnerAdviceCandidates(road.ordered.slice(1,3),roadCandidates),battleAlternatives=partnerAdviceCandidates(battle.ordered.slice(1,3),battleCandidates);
    pd.adviceEnvelope=partnerAdviceEnvelope({m,
-- hit 2 index=10128232 --
'stale',status:'blocked',error:'STALE_STATE',reason:'盤面状態が助言作成時から変わりました。',reversalCondition:'現在の状態から助言を再取得してください。',publicEvidence:null})}
window.__GAMEROAD_PARTNER_ADVICE_STATE_VERSION__=partnerAdviceStateVersion;
window.__GAMEROAD_PARTNER_ADVICE_STALE__=partnerAdviceStaleEnvelope;
function replayFor(candidates,rule=currentPartnerRule()){if(!rule)return{ok:false,error:'PARTNER_RULE_NOT_SELECTED'};return partnerReplay({rule,candidates})}
function partnerAdviceNow(){const m=state.match,pd=ensurePartnerRuntime();if(!m||!pd)return null;const rule=pd.rule;if(!rule){pd.advice='作戦規則が未選択です。左端・右端・最大・最小の正式4規則から選んでください。';pd.adviceEnvelope=partnerAdviceEnvelope({m,kind:'rule',status:'blocked',error:'PARTNER_RULE_NOT_SELECTED',reason:'正式4規則のいずれかを明示選択する必要があります。'});renderPartnerRuntime();toast('作戦規則を選んでください');return null}
  if(m.phase==='plan'){
    const roadCandidates=planCardCandidates('road'),road=replayFor(roadCandidates,rule);let battleCandidates=[],battle={ok:false,error:'ROAD_UNRESOLVED'};
    if(road.ok&&road.selected){battleCandidates=planCardCandidates('battle').filter(c=>c.candidateId!==road.selected.candidateId);battle=replayFor(battleCandidates,rule)}
    if(!road.ok||!battle.ok){pd.advice=`アドバイスできません：${road.error||battle.error}`;pd.adviceEnvelope=partnerAdviceEnvelope({m,rule,kind:'plan',status:'blocked',error:road.error||battle.error,reason:'合法候補の再生に失敗したため、カードを勝手に選びません。',publicEvidence:{roadCandidates:roadCandidates.map(partnerPublicCandidate),battleCandidates:battleCandidates.map(partnerPublicCandidate)}});renderPartnerRuntime();return null}
    const roadSelected=partnerPublicCandidate(road.selected),battleSelected=partnerPublicCandidate(battle.selected),roadAlternatives=partnerAdviceCandidates(road.ordered.slice(1,3),roadCandidates),battleAlternatives=partnerAdviceCandidates(battle.ordered.slice(1,3),battleCandidates);
    pd.adviceEnvelope=partnerAdviceEnvelope({m,rule,kind:'plan',recommendation:{road:roadSelected,battle:battleSelected},alternatives:{road:roadAlternatives,battle:battleAlternatives},reason:{road:road.reason,battle:battle.reason},reversalCondition:{road:roadAlternatives.length?`公開手札のpositionOrderが変わり、${roadSelected.candidateId}より先に合法となった札が現れるとロード推奨が変わります。`:'ロード候補が増えると推奨を再計算します。',battle:battleAlternatives.length?`公開powerの大小または同値tie-breakが変わるとバトル推奨が変わります。`:'バトル候補が増えると推奨を再計算します。'},publicEvidence:{roadCandidates:roadCandidates.map(partnerPublicCandidate),battleCandidates:battleCandidates.map(partnerPublicCandidate)}});
    pd.advice=`${rule.toUpperCa
-- hit 3 index=10128467 --
TNER_ADVICE_STALE__=partnerAdviceStaleEnvelope;
function replayFor(candidates,rule=currentPartnerRule()){if(!rule)return{ok:false,error:'PARTNER_RULE_NOT_SELECTED'};return partnerReplay({rule,candidates})}
function partnerAdviceNow(){const m=state.match,pd=ensurePartnerRuntime();if(!m||!pd)return null;const rule=pd.rule;if(!rule){pd.advice='作戦規則が未選択です。左端・右端・最大・最小の正式4規則から選んでください。';pd.adviceEnvelope=partnerAdviceEnvelope({m,kind:'rule',status:'blocked',error:'PARTNER_RULE_NOT_SELECTED',reason:'正式4規則のいずれかを明示選択する必要があります。'});renderPartnerRuntime();toast('作戦規則を選んでください');return null}
  if(m.phase==='plan'){
    const roadCandidates=planCardCandidates('road'),road=replayFor(roadCandidates,rule);let battleCandidates=[],battle={ok:false,error:'ROAD_UNRESOLVED'};
    if(road.ok&&road.selected){battleCandidates=planCardCandidates('battle').filter(c=>c.candidateId!==road.selected.candidateId);battle=replayFor(battleCandidates,rule)}
    if(!road.ok||!battle.ok){pd.advice=`アドバイスできません：${road.error||battle.error}`;pd.adviceEnvelope=partnerAdviceEnvelope({m,rule,kind:'plan',status:'blocked',error:road.error||battle.error,reason:'合法候補の再生に失敗したため、カードを勝手に選びません。',publicEvidence:{roadCandidates:roadCandidates.map(partnerPublicCandidate),battleCandidates:battleCandidates.map(partnerPublicCandidate)}});renderPartnerRuntime();return null}
    const roadSelected=partnerPublicCandidate(road.selected),battleSelected=partnerPublicCandidate(battle.selected),roadAlternatives=partnerAdviceCandidates(road.ordered.slice(1,3),roadCandidates),battleAlternatives=partnerAdviceCandidates(battle.ordered.slice(1,3),battleCandidates);
    pd.adviceEnvelope=partnerAdviceEnvelope({m,rule,kind:'plan',recommendation:{road:roadSelected,battle:battleSelected},alternatives:{road:roadAlternatives,battle:battleAlternatives},reason:{road:road.reason,battle:battle.reason},reversalCondition:{road:roadAlternatives.length?`公開手札のpositionOrderが変わり、${roadSelected.candidateId}より先に合法となった札が現れるとロード推奨が変わります。`:'ロード候補が増えると推奨を再計算します。',battle:battleAlternatives.length?`公開powerの大小または同値tie-breakが変わるとバトル推奨が変わります。`:'バトル候補が増えると推奨を再計算します。'},publicEvidence:{roadCandidates:roadCandidates.map(partnerPublicCandidate),battleCandidates:battleCandidates.map(partnerPublicCandidate)}});
    pd.advice=`${rule.toUpperCase()}：ロード ${cardLabel(road.selected.candidateId)}／バトル ${cardLabel(battle.selected.candidateId)}。移動は現在予約済み経路を優先します。`;renderPartnerRuntime();return{road,battle,envelope:pd.adviceEnvelope};
  }
  if(m.phase==='target'){
    const targetCa
-- hit 4 index=10130116 --
AdviceEnvelope({m,rule,kind:'plan',recommendation:{road:roadSelected,battle:battleSelected},alternatives:{road:roadAlternatives,battle:battleAlternatives},reason:{road:road.reason,battle:battle.reason},reversalCondition:{road:roadAlternatives.length?`公開手札のpositionOrderが変わり、${roadSelected.candidateId}より先に合法となった札が現れるとロード推奨が変わります。`:'ロード候補が増えると推奨を再計算します。',battle:battleAlternatives.length?`公開powerの大小または同値tie-breakが変わるとバトル推奨が変わります。`:'バトル候補が増えると推奨を再計算します。'},publicEvidence:{roadCandidates:roadCandidates.map(partnerPublicCandidate),battleCandidates:battleCandidates.map(partnerPublicCandidate)}});
    pd.advice=`${rule.toUpperCase()}：ロード ${cardLabel(road.selected.candidateId)}／バトル ${cardLabel(battle.selected.candidateId)}。移動は現在予約済み経路を優先します。`;renderPartnerRuntime();return{road,battle,envelope:pd.adviceEnvelope};
  }
  if(m.phase==='target'){
    const targetCandidatesNow=targetPlayerCandidates(),r=replayFor(targetCandidatesNow,rule);if(!r.ok){pd.advice=`攻撃先のアドバイスを停止：${r.error}。最大/最小の対象評価値は未裁定です。`;pd.adviceEnvelope=partnerAdviceEnvelope({m,rule,kind:'target',status:'blocked',error:r.error,reason:'対象評価値が正式裁定されていないため、推奨を捏造しません。',publicEvidence:{targetCandidates:targetCandidatesNow.map(partnerPublicCandidate)}});renderPartnerRuntime();return null}
    const selected=partnerPublicCandidate(r.selected),alternatives=partnerAdviceCandidates(r.ordered.slice(1,3),targetCandidatesNow);pd.adviceEnvelope=partnerAdviceEnvelope({m,rule,kind:'target',recommendation:selected,alternatives,reason:r.reason,reversalCondition:alternatives.length?'合法な相手のpositionOrderが変わると攻撃先推奨が変わります。':'合法な相手が増えると推奨を再計算します。',publicEvidence:{targetCandidates:targetCandidatesNow.map(partnerPublicCandidate)}});pd.advice=`${rule.toUpperCase()}：攻撃相手 ${r.selected?.payload?.player?.name||r.selected?.candidateId||'なし'}。列・シールドは現在の明示選択を変更しません。`;renderPartnerRuntime();return{...r,envelope:pd.adviceEnvelope};
  }
  if(pd.ability){pd.advice='能力選択中です。正式候補だけで判断します。';pd.adviceEnvelope=partnerAdviceEnvelope({m,rule,kind:'ability',status:'blocked',error:'ABILITY_SELECTION_PENDING',reason:'能力候補の正式選択が完了するまで自動判断しません。'});renderPartnerRuntime();return null}
  pd.advice='現在は本人へ必須判断を要求していないため、ヘイト計測もアドバイスも停止中です。';pd.adviceEnvelope=partnerAdviceEnvelope({m,rule,kind:'idle',status:'blocked',error:'NO_ACTIVE_DECISION',reason:'現在は必須判断の対象外です。'});renderPartnerRuntime();return null;
}
async function autoPlanByPartner(reason){const m=state.match,p=currentHuman(),pd=ensurePartnerRuntime();if(!m||m.phase!=='plan'||!p)return false;const rule=pd.rule;if(
-- hit 5 index=10131792 --
date)}});pd.advice=`${rule.toUpperCase()}：攻撃相手 ${r.selected?.payload?.player?.name||r.selected?.candidateId||'なし'}。列・シールドは現在の明示選択を変更しません。`;renderPartnerRuntime();return{...r,envelope:pd.adviceEnvelope};
  }
  if(pd.ability){pd.advice='能力選択中です。正式候補だけで判断します。';pd.adviceEnvelope=partnerAdviceEnvelope({m,rule,kind:'ability',status:'blocked',error:'ABILITY_SELECTION_PENDING',reason:'能力候補の正式選択が完了するまで自動判断しません。'});renderPartnerRuntime();return null}
  pd.advice='現在は本人へ必須判断を要求していないため、ヘイト計測もアドバイスも停止中です。';pd.adviceEnvelope=partnerAdviceEnvelope({m,rule,kind:'idle',status:'blocked',error:'NO_ACTIVE_DECISION',reason:'現在は必須判断の対象外です。'});renderPartnerRuntime();return null;
}
async function autoPlanByPartner(reason){const m=state.match,p=currentHuman(),pd=ensurePartnerRuntime();if(!m||m.phase!=='plan'||!p)return false;const rule=pd.rule;if(!rule)throw Error('作戦規則の初期値が未裁定です。パートナー規則を明示選択してください');const road=replayFor(planCardCandidates('road'),rule);if(!road.ok||!road.selected)throw Error('ロード判断:'+String(road.error||'候補なし'));const battles=planCardCandidates('battle').filter(c=>c.candidateId!==road.selected.candidateId),battle=replayFor(battles,rule);if(!battle.ok||!battle.selected)throw Error('バトル判断:'+String(battle.error||'候補なし'));p.plan=p.plan||{roadId:null,battleId:null,path:[p.position]};p.plan.roadId=road.selected.candidateId;p.plan.battleId=battle.selected.candidateId;if(!Array.isArray(p.plan.path)||!p.plan.path.length)p.plan.path=[p.position];$('#roadSelect').value=p.plan.roadId;$('#battleSelect').value=p.plan.battleId;$('#endpointText').textContent=p.plan.path.at(-1);pd.advice=`${reason}：${cardLabel(p.plan.roadId)} / ${cardLabel(p.plan.battleId)} / ${p.plan.path.length-1}歩を確定します。`;renderBattle();await commitPlans();return true}
async function autoTargetByPartner(reason){const m=state.match,pd=ensurePartnerRuntime(),act=m?.players.find(p=>p.id===m.activeId);if(!m||m.phase!=='target'||!act?.human)return false;const rule=pd.rule;if(!rule)throw Error('作戦規則の初期値が未裁定です');const t=replayFor(targetPlayerCandidates(),rule);if(!t.ok||!t.selected)throw Error('攻撃対象:'+String(t.error||'候補なし'));if(rule==='max'||rule==='min')throw Error('攻撃対象の最大/最小評価値が未裁定です');const lane=replayFor(fixedLaneCandidates('lane'),rule),shield=replayFor(fixedLaneCandidates('shield'),rule);if(!lane.ok||!shield.ok)throw Error('列/シールド判断:'+String(lane.error||shield.error));m.target={defenderId:t.selected.candidateId,lane:lane.selected.candidateId,shield:shield.selected.candidateId};pd.advice=`${reason}：${t.selected.payload.p
-- hit 6 index=10132015 --
{pd.advice='能力選択中です。正式候補だけで判断します。';pd.adviceEnvelope=partnerAdviceEnvelope({m,rule,kind:'ability',status:'blocked',error:'ABILITY_SELECTION_PENDING',reason:'能力候補の正式選択が完了するまで自動判断しません。'});renderPartnerRuntime();return null}
  pd.advice='現在は本人へ必須判断を要求していないため、ヘイト計測もアドバイスも停止中です。';pd.adviceEnvelope=partnerAdviceEnvelope({m,rule,kind:'idle',status:'blocked',error:'NO_ACTIVE_DECISION',reason:'現在は必須判断の対象外です。'});renderPartnerRuntime();return null;
}
async function autoPlanByPartner(reason){const m=state.match,p=currentHuman(),pd=ensurePartnerRuntime();if(!m||m.phase!=='plan'||!p)return false;const rule=pd.rule;if(!rule)throw Error('作戦規則の初期値が未裁定です。パートナー規則を明示選択してください');const road=replayFor(planCardCandidates('road'),rule);if(!road.ok||!road.selected)throw Error('ロード判断:'+String(road.error||'候補なし'));const battles=planCardCandidates('battle').filter(c=>c.candidateId!==road.selected.candidateId),battle=replayFor(battles,rule);if(!battle.ok||!battle.selected)throw Error('バトル判断:'+String(battle.error||'候補なし'));p.plan=p.plan||{roadId:null,battleId:null,path:[p.position]};p.plan.roadId=road.selected.candidateId;p.plan.battleId=battle.selected.candidateId;if(!Array.isArray(p.plan.path)||!p.plan.path.length)p.plan.path=[p.position];$('#roadSelect').value=p.plan.roadId;$('#battleSelect').value=p.plan.battleId;$('#endpointText').textContent=p.plan.path.at(-1);pd.advice=`${reason}：${cardLabel(p.plan.roadId)} / ${cardLabel(p.plan.battleId)} / ${p.plan.path.length-1}歩を確定します。`;renderBattle();await commitPlans();return true}
async function autoTargetByPartner(reason){const m=state.match,pd=ensurePartnerRuntime(),act=m?.players.find(p=>p.id===m.activeId);if(!m||m.phase!=='target'||!act?.human)return false;const rule=pd.rule;if(!rule)throw Error('作戦規則の初期値が未裁定です');const t=replayFor(targetPlayerCandidates(),rule);if(!t.ok||!t.selected)throw Error('攻撃対象:'+String(t.error||'候補なし'));if(rule==='max'||rule==='min')throw Error('攻撃対象の最大/最小評価値が未裁定です');const lane=replayFor(fixedLaneCandidates('lane'),rule),shield=replayFor(fixedLaneCandidates('shield'),rule);if(!lane.ok||!shield.ok)throw Error('列/シールド判断:'+String(lane.error||shield.error));m.target={defenderId:t.selected.candidateId,lane:lane.selected.candidateId,shield:shield.selected.candidateId};pd.advice=`${reason}：${t.selected.payload.player.name} / ${m.target.lane}列 / ${m.target.shield}シールドを確定します。`;renderPartnerRuntime();await resolveBattle();return true}
function grPartnerSelectionGuard(reason='delegate'){const m=state.match;if(!m)return false;if(!m.sel
-- hit 7 index=10132885 --
.selected.candidateId),battle=replayFor(battles,rule);if(!battle.ok||!battle.selected)throw Error('バトル判断:'+String(battle.error||'候補なし'));p.plan=p.plan||{roadId:null,battleId:null,path:[p.position]};p.plan.roadId=road.selected.candidateId;p.plan.battleId=battle.selected.candidateId;if(!Array.isArray(p.plan.path)||!p.plan.path.length)p.plan.path=[p.position];$('#roadSelect').value=p.plan.roadId;$('#battleSelect').value=p.plan.battleId;$('#endpointText').textContent=p.plan.path.at(-1);pd.advice=`${reason}：${cardLabel(p.plan.roadId)} / ${cardLabel(p.plan.battleId)} / ${p.plan.path.length-1}歩を確定します。`;renderBattle();await commitPlans();return true}
async function autoTargetByPartner(reason){const m=state.match,pd=ensurePartnerRuntime(),act=m?.players.find(p=>p.id===m.activeId);if(!m||m.phase!=='target'||!act?.human)return false;const rule=pd.rule;if(!rule)throw Error('作戦規則の初期値が未裁定です');const t=replayFor(targetPlayerCandidates(),rule);if(!t.ok||!t.selected)throw Error('攻撃対象:'+String(t.error||'候補なし'));if(rule==='max'||rule==='min')throw Error('攻撃対象の最大/最小評価値が未裁定です');const lane=replayFor(fixedLaneCandidates('lane'),rule),shield=replayFor(fixedLaneCandidates('shield'),rule);if(!lane.ok||!shield.ok)throw Error('列/シールド判断:'+String(lane.error||shield.error));m.target={defenderId:t.selected.candidateId,lane:lane.selected.candidateId,shield:shield.selected.candidateId};pd.advice=`${reason}：${t.selected.payload.player.name} / ${m.target.lane}列 / ${m.target.shield}シールドを確定します。`;renderPartnerRuntime();await resolveBattle();return true}
function grPartnerSelectionGuard(reason='delegate'){const m=state.match;if(!m)return false;if(!m.selectionLock){const local=(m.players||[]).find(p=>p.human)||m.players?.[0];m.selectionLock=grCreateSelectionLock(m,local?.id||'P1')}const check=grVerifySelectionLock(m);if(check.ok)return true;const pd=ensurePartnerRuntime(m);if(pd){const msg=`試合開始時の札組・モード保持に失敗(${check.reason})`;if(pd.blocker!==msg){pd.blocker=msg;log(`相棒委任停止：${msg}`)}renderPartnerRuntime()}return false}
async function attemptPartnerAutoDecision(reason='delegate'){
  const m=state.match,pd=ensurePartnerRuntime();if(!m||!partnerDelegated())return false;if(m.friendRoom&&partnerForced())return false;if(!grPartnerSelectionGuard(reason))return false;if(pd.ability){if(typeof pd.ability.autoResolve==='function')return pd.ability.autoResolve(reason);return false}if(m.phase==='plan'&&!m.busy)return autoPlanByPartner(reason);const act=m.players.find(p=>p.id===m.activeId);if(m.phase==='target'&&!m.busy&&
-- hit 8 index=10133069 --
[p.position]};p.plan.roadId=road.selected.candidateId;p.plan.battleId=battle.selected.candidateId;if(!Array.isArray(p.plan.path)||!p.plan.path.length)p.plan.path=[p.position];$('#roadSelect').value=p.plan.roadId;$('#battleSelect').value=p.plan.battleId;$('#endpointText').textContent=p.plan.path.at(-1);pd.advice=`${reason}：${cardLabel(p.plan.roadId)} / ${cardLabel(p.plan.battleId)} / ${p.plan.path.length-1}歩を確定します。`;renderBattle();await commitPlans();return true}
async function autoTargetByPartner(reason){const m=state.match,pd=ensurePartnerRuntime(),act=m?.players.find(p=>p.id===m.activeId);if(!m||m.phase!=='target'||!act?.human)return false;const rule=pd.rule;if(!rule)throw Error('作戦規則の初期値が未裁定です');const t=replayFor(targetPlayerCandidates(),rule);if(!t.ok||!t.selected)throw Error('攻撃対象:'+String(t.error||'候補なし'));if(rule==='max'||rule==='min')throw Error('攻撃対象の最大/最小評価値が未裁定です');const lane=replayFor(fixedLaneCandidates('lane'),rule),shield=replayFor(fixedLaneCandidates('shield'),rule);if(!lane.ok||!shield.ok)throw Error('列/シールド判断:'+String(lane.error||shield.error));m.target={defenderId:t.selected.candidateId,lane:lane.selected.candidateId,shield:shield.selected.candidateId};pd.advice=`${reason}：${t.selected.payload.player.name} / ${m.target.lane}列 / ${m.target.shield}シールドを確定します。`;renderPartnerRuntime();await resolveBattle();return true}
function grPartnerSelectionGuard(reason='delegate'){const m=state.match;if(!m)return false;if(!m.selectionLock){const local=(m.players||[]).find(p=>p.human)||m.players?.[0];m.selectionLock=grCreateSelectionLock(m,local?.id||'P1')}const check=grVerifySelectionLock(m);if(check.ok)return true;const pd=ensurePartnerRuntime(m);if(pd){const msg=`試合開始時の札組・モード保持に失敗(${check.reason})`;if(pd.blocker!==msg){pd.blocker=msg;log(`相棒委任停止：${msg}`)}renderPartnerRuntime()}return false}
async function attemptPartnerAutoDecision(reason='delegate'){
  const m=state.match,pd=ensurePartnerRuntime();if(!m||!partnerDelegated())return false;if(m.friendRoom&&partnerForced())return false;if(!grPartnerSelectionGuard(reason))return false;if(pd.ability){if(typeof pd.ability.autoResolve==='function')return pd.ability.autoResolve(reason);return false}if(m.phase==='plan'&&!m.busy)return autoPlanByPartner(reason);const act=m.players.find(p=>p.id===m.activeId);if(m.phase==='target'&&!m.busy&&act?.human)return autoTargetByPartner(reason);return false
}
function makeAbilityPartnerCandidates(items){return normalizePartnerCandidates(items.map((item,i)=>({ ...item,id:partnerCan
-- hit 9 index=10133120 --
Id;p.plan.battleId=battle.selected.candidateId;if(!Array.isArray(p.plan.path)||!p.plan.path.length)p.plan.path=[p.position];$('#roadSelect').value=p.plan.roadId;$('#battleSelect').value=p.plan.battleId;$('#endpointText').textContent=p.plan.path.at(-1);pd.advice=`${reason}：${cardLabel(p.plan.roadId)} / ${cardLabel(p.plan.battleId)} / ${p.plan.path.length-1}歩を確定します。`;renderBattle();await commitPlans();return true}
async function autoTargetByPartner(reason){const m=state.match,pd=ensurePartnerRuntime(),act=m?.players.find(p=>p.id===m.activeId);if(!m||m.phase!=='target'||!act?.human)return false;const rule=pd.rule;if(!rule)throw Error('作戦規則の初期値が未裁定です');const t=replayFor(targetPlayerCandidates(),rule);if(!t.ok||!t.selected)throw Error('攻撃対象:'+String(t.error||'候補なし'));if(rule==='max'||rule==='min')throw Error('攻撃対象の最大/最小評価値が未裁定です');const lane=replayFor(fixedLaneCandidates('lane'),rule),shield=replayFor(fixedLaneCandidates('shield'),rule);if(!lane.ok||!shield.ok)throw Error('列/シールド判断:'+String(lane.error||shield.error));m.target={defenderId:t.selected.candidateId,lane:lane.selected.candidateId,shield:shield.selected.candidateId};pd.advice=`${reason}：${t.selected.payload.player.name} / ${m.target.lane}列 / ${m.target.shield}シールドを確定します。`;renderPartnerRuntime();await resolveBattle();return true}
function grPartnerSelectionGuard(reason='delegate'){const m=state.match;if(!m)return false;if(!m.selectionLock){const local=(m.players||[]).find(p=>p.human)||m.players?.[0];m.selectionLock=grCreateSelectionLock(m,local?.id||'P1')}const check=grVerifySelectionLock(m);if(check.ok)return true;const pd=ensurePartnerRuntime(m);if(pd){const msg=`試合開始時の札組・モード保持に失敗(${check.reason})`;if(pd.blocker!==msg){pd.blocker=msg;log(`相棒委任停止：${msg}`)}renderPartnerRuntime()}return false}
async function attemptPartnerAutoDecision(reason='delegate'){
  const m=state.match,pd=ensurePartnerRuntime();if(!m||!partnerDelegated())return false;if(m.friendRoom&&partnerForced())return false;if(!grPartnerSelectionGuard(reason))return false;if(pd.ability){if(typeof pd.ability.autoResolve==='function')return pd.ability.autoResolve(reason);return false}if(m.phase==='plan'&&!m.busy)return autoPlanByPartner(reason);const act=m.players.find(p=>p.id===m.activeId);if(m.phase==='target'&&!m.busy&&act?.human)return autoTargetByPartner(reason);return false
}
function makeAbilityPartnerCandidates(items){return normalizePartnerCandidates(items.map((item,i)=>({ ...item,id:partnerCandidateId(item,i),positionOrder:i,comparisonValue:it
-- hit 10 index=10135074 --
ced())return false;if(!grPartnerSelectionGuard(reason))return false;if(pd.ability){if(typeof pd.ability.autoResolve==='function')return pd.ability.autoResolve(reason);return false}if(m.phase==='plan'&&!m.busy)return autoPlanByPartner(reason);const act=m.players.find(p=>p.id===m.activeId);if(m.phase==='target'&&!m.busy&&act?.human)return autoTargetByPartner(reason);return false
}
function makeAbilityPartnerCandidates(items){return normalizePartnerCandidates(items.map((item,i)=>({ ...item,id:partnerCandidateId(item,i),positionOrder:i,comparisonValue:item?.cardId&&CARDS[item.cardId]?Number(CARDS[item.cardId].power):null,assetAction:PARTNER_ASSET_NONE,publicScope:true,legal:true})),'ability')}
function resolvePartnerMulti(items,min,max,rule){if(min!==max)return{ok:false,error:'VARIABLE_COUNT_UNRESOLVED'};let remain=makeAbilityPartnerCandidates(items),picked=[];for(let n=0;n<min;n++){const r=replayFor(remain,rule);if(!r.ok||!r.selected)return{ok:false,error:r.error||'NO_CANDIDATE'};picked.push(r.selected.payload);remain=remain.filter(c=>c.candidateId!==r.selected.candidateId)}return{ok:true,picked}}
const __grBaseChooseAbilityItems=chooseAbilityItems;
chooseAbilityItems=async function(title,hint,items,min,max,human){if(!human)return __grBaseChooseAbilityItems(title,hint,items,min,max,human);const m=state.match,pd=ensurePartnerRuntime();if(!m)return __grBaseChooseAbilityItems(title,hint,items,min,max,human);const ctxId=`${m.round}:${title}:${++pd.sessionSeq}`;return await new Promise(resolve=>{const veil=$('#abilityVeil'),root=$('#abilityChoices'),confirm=$('#abilityConfirm'),count=$('#abilityCount');$('#abilityTitle').textContent=title;$('#abilityHint').textContent=hint;root.innerHTML='';const selected=new Set(),finish=vals=>{veil.classList.remove('on');veil.setAttribute('aria-hidden','true');confirm.onclick=null;pd.ability=null;syncPartnerDecisionSession();renderPartnerRuntime();resolve(vals)},refresh=()=>{count.textContent=min===max?`選択 ${selected.size} / ${max}`:`選択 ${selected.size}枚 / 必須${min}枚以上・最大${max}枚`;confirm.disabled=selected.size<min||selected.size>max};items.forEach((item,i)=>{const b=document.createElement('button');b.className='abilityChoice';b.innerHTML=`${abilityOptionLabel(item)}${item.detail?`<small>${item.detail}</small>`:''}`;b.onclick=()=>{if(partnerForced())return toast('ヘイト爆発中は必須判断を手動変更できません');if(selected.has(i)){selected.delete(i);b.classList.remove('on')}else if(selected.size<max){selected.add(i);b.classList.add('on')}refresh()};root.appendChild
count_shown=10

=== partnerAdviceNow ===
-- hit 1 index=10127782 --
ind='generic',status='ready',recommendation=null,alternatives=[],reason=null,reversalCondition=null,publicEvidence=null,error=null}){return Object.freeze({schema:'GAMEROAD_PARTNER_ADVICE_ENVELOPE_V1',status,kind,rule,stateVersion:partnerAdviceStateVersion(m),recommendation,alternatives,reason,reversalCondition,publicEvidence,error})}
function partnerAdviceStaleEnvelope(m=state.match){return partnerAdviceEnvelope({m,rule:currentPartnerRule(),kind:'stale',status:'blocked',error:'STALE_STATE',reason:'盤面状態が助言作成時から変わりました。',reversalCondition:'現在の状態から助言を再取得してください。',publicEvidence:null})}
window.__GAMEROAD_PARTNER_ADVICE_STATE_VERSION__=partnerAdviceStateVersion;
window.__GAMEROAD_PARTNER_ADVICE_STALE__=partnerAdviceStaleEnvelope;
function replayFor(candidates,rule=currentPartnerRule()){if(!rule)return{ok:false,error:'PARTNER_RULE_NOT_SELECTED'};return partnerReplay({rule,candidates})}
function partnerAdviceNow(){const m=state.match,pd=ensurePartnerRuntime();if(!m||!pd)return null;const rule=pd.rule;if(!rule){pd.advice='作戦規則が未選択です。左端・右端・最大・最小の正式4規則から選んでください。';pd.adviceEnvelope=partnerAdviceEnvelope({m,kind:'rule',status:'blocked',error:'PARTNER_RULE_NOT_SELECTED',reason:'正式4規則のいずれかを明示選択する必要があります。'});renderPartnerRuntime();toast('作戦規則を選んでください');return null}
  if(m.phase==='plan'){
    const roadCandidates=planCardCandidates('road'),road=replayFor(roadCandidates,rule);let battleCandidates=[],battle={ok:false,error:'ROAD_UNRESOLVED'};
    if(road.ok&&road.selected){battleCandidates=planCardCandidates('battle').filter(c=>c.candidateId!==road.selected.candidateId);battle=replayFor(battleCandidates,rule)}
    if(!road.ok||!battle.ok){pd.advice=`アドバイスできません：${road.error||battle.error}`;pd.adviceEnvelope=partnerAdviceEnvelope({m,rule,kind:'plan',status:'blocked',error:road.error||battle.error,reason:'合法候補の再生に失敗したため、カードを勝手に選びません。',publicEvidence:{roadCandidates:roadCandidates.map(partnerPublicCandidate),battleCandidates:battleCandidates.map(partnerPublicCandidate)}});renderPartnerRuntime();return null}
    const roadSelected=partnerPublicCandidate(road.selected),battleSelected=partnerPublicCandidate(battle.selected),roadAlternatives=partnerAdviceCandidates(road.ordered.slice(1,3),roadCandidates),battleAlternatives=partnerAdviceCandidates(battle.ordered.slice(1,3),battleCandidates);
    pd.adviceEnvelope=partnerAdviceEnvelope({m,rule,kind:'plan',recommendation:{road:roadSelected,battle:battleSelected},alternatives:{road:roadAlternatives,battle:battleAlternatives},reason:{road:road.reason,ba
-- hit 2 index=10139531 --
</div><div class="sub">最大/最小の対象評価と任意枚数能力は未裁定なら自動化しません。</div>';drawer.insertBefore(box,$('#players'))}const st=document.createElement('style');st.textContent='#partnerDecisionBox{padding:8px;border:1px solid var(--line);background:#071d18;margin-bottom:6px}.partnerRuleLabel{display:grid;grid-template-columns:auto 1fr;gap:6px;align-items:center;font-size:9px}.partnerRuleLabel select{height:44px;background:#0b231d;border:1px solid var(--line);color:inherit}.partnerDecisionStatus{font-size:9px;line-height:1.4;margin-top:6px;white-space:pre-wrap}#hateMeter.hot{border-color:#ff9c86;color:#ffd5ca}#partnerDelegateBtn.forced{border-color:#ff7b66;background:#52251e}';document.head.appendChild(st);$('#partnerRule').onchange=()=>{const pd=ensurePartnerRuntime();pd.rule=$('#partnerRule').value||null;pd.blocker=null;grPersistPartnerMatchSession();renderPartnerRuntime()};$('#partnerAdviceBtn').onclick=partnerAdviceNow;$('#partnerDelegateBtn').onclick=()=>{const pd=ensurePartnerRuntime();if(partnerForced())return toast('ヘイト爆発中は対局終了まで手動へ戻せません');if(!pd.rule)return toast('作戦規則を選んでください');if(!pd.voluntary&&!grPartnerSelectionGuard('voluntary-toggle'))return toast('試合開始時の札組・モードを保持できないため委任できません');pd.voluntary=!pd.voluntary;pd.advice=pd.voluntary?'任意委任を開始。未確定の次判断から同じ合法候補契約を使います。':'次の未確定判断から手動へ戻します。';grPersistPartnerMatchSession();renderPartnerRuntime();if(pd.voluntary)attemptPartnerAutoDecision('voluntary-delegate').catch(e=>{pd.blocker=e.message;renderPartnerRuntime()})}}
function renderPartnerRuntime(){
const publicBox=$('#partnerDecisionBox');if(publicBox){const k=publicBox.querySelector('.k');if(k)k.textContent='パートナーの判断';const label=publicBox.querySelector('.partnerRuleLabel');if(label?.firstChild)label.firstChild.nodeValue='選び方 ';const sub=publicBox.querySelector('.sub');if(sub)sub.textContent='最大・最小を選べない場面では、パートナーは勝手に決めません。'}const telemetry=$('#hudTelemetryBox');if(telemetry){telemetry.hidden=true;telemetry.setAttribute('aria-hidden','true');telemetry.style.display='none'}
installPartnerUi();const m=state.match,p=currentHuman(),pd=m?ensurePartnerRuntime(m):null;if(!p||!pd)return;const h=$('#hateMeter');if(h){h.textContent=`ヘイト ${p.hateTime}/1000`;h.classList.toggle('hot',p.hateTime>=800)}const rule=$('#partnerRule');if(rule&&rule.value!==(pd.rule||''))rule.value=pd.rule||'';const del=$('#partnerDelegateBtn');if(del){del.classList.toggle('forced',partnerForced());del.textContent=partnerForced()?'強制委任':(pd.voluntary?'まかせろ':'まかせた')}const stat=$('#partnerDecisionStatus');if(stat){stat.textCon
-- hit 3 index=10143249 --
state.match.partnerDecision.session=null;return r};
const __grOriginalConfirmTarget=$('#confirmTarget').onclick;
$('#confirmTarget').onclick=async()=>{if(partnerForced())return toast('ヘイト爆発中は必須判断を手動変更できません');return __grOriginalConfirmTarget()};
installPartnerUi();
window.__GAMEROAD_HATE_PARTNER_TEST__={partnerReplay,normalizePartnerCandidates,ensure:ensurePartnerRuntime,tick:(ms)=>{const pd=ensurePartnerRuntime();if(pd){pd.lastTick-=Number(ms)||0;tickPartnerHate(performance.now())}return currentHuman()?{hate:currentHuman().hateTime,exploded:currentHuman().hateExploded,count:currentHuman().hateExplosionCount}:null},setRule:r=>{const pd=ensurePartnerRuntime();pd.rule=r||null;grPersistPartnerMatchSession();renderPartnerRuntime();return pd.rule},setVoluntary:v=>{const pd=ensurePartnerRuntime();pd.voluntary=!!v;grPersistPartnerMatchSession();renderPartnerRuntime();return pd.voluntary},advice:partnerAdviceNow,adviceEnvelope:()=>ensurePartnerRuntime()?.adviceEnvelope||null,adviceStateVersion:()=>partnerAdviceStateVersion(state.match),validateAdviceEnvelope:env=>{const now=partnerAdviceStateVersion(state.match);return env?.stateVersion===now?{ok:true,stateVersion:now}:{ok:false,error:'STALE_STATE',stateVersion:now}},attempt:attemptPartnerAutoDecision,session:()=>ensurePartnerRuntime()?.session||null,status:()=>{const p=currentHuman(),pd=ensurePartnerRuntime();return p&&pd?{hate:p.hateTime,exploded:p.hateExploded,explosionCount:p.hateExplosionCount,rule:pd.rule,voluntary:pd.voluntary,forced:pd.forced,blocker:pd.blocker,advice:pd.advice,adviceEnvelope:pd.adviceEnvelope,session:pd.session?.signature||null}:null},targetCandidates:targetPlayerCandidates,planCandidates:planCardCandidates,selectionLock:()=>state.match?.selectionLock||null,selectionCheck:()=>grVerifySelectionLock(state.match),selectionGuard:grPartnerSelectionGuard,deckDigest:grDeckIdentityDigest,persistSelection:grPersistPartnerMatchSession,restoreSelection:()=>{const m=state.match,pd=ensurePartnerRuntime(m),p=currentHuman();return grRestorePartnerMatchSession(m,pd,p)},clearSelection:()=>grClearPartnerMatchSession(state.match)};


// GAMEROAD_BROWSER_FIRST10_RUNTIME_V1
// Rebased onto the CURRENT browser reservation flow. The July 22 historical "large number first" observation is NOT imported.
const FIRST10_RUNTIME_CONTRACT=Object.freeze({
  schema:'GAMEROAD_BROWSER_FIRST10_RUNTIME_V1',
  workId:'BROWSER-FIRST10-ONBOARDING-RUNTIME-001',
  source:'FIRST10-001 1raK2cBU8LOAMjTf8LdfWAzk9bijJ8vkv4a8VRIksPt4',
  mode:'current-
count_shown=3

=== partnerReplay ===
-- hit 1 index=10117820 --
ht','max','min']);
const PARTNER_ASSET_NONE='NONE';
function partnerClone(v){return JSON.parse(JSON.stringify(v))}
function partnerCandidateId(v,i){return String(v?.candidateId||v?.id||v?.cardId||v?.player?.id||`IDX${i}`)}
function normalizePartnerCandidates(items,kind='generic'){
  return items.map((item,i)=>{
    const id=partnerCandidateId(item,i), cardId=item?.cardId||((typeof item==='string'&&CARDS[item])?item:null);
    const power=cardId&&CARDS[cardId]?Number(CARDS[cardId].power):null;
    return {candidateId:id,kind:item?.kind||kind,positionOrder:Number.isFinite(item?.positionOrder)?Number(item.positionOrder):i,comparisonValue:Number.isFinite(item?.comparisonValue)?Number(item.comparisonValue):(Number.isFinite(power)?power:null),legal:item?.legal!==false,publicScope:item?.publicScope!==false,assetAction:String(item?.assetAction||PARTNER_ASSET_NONE),payload:item};
  })
}
function partnerReplay(input){
  const before=JSON.stringify(input), rule=String(input?.rule||'');
  if(!PARTNER_REPLAY_RULES.has(rule))return{ok:false,error:'UNKNOWN_RULE'};
  if(!Array.isArray(input?.candidates))return{ok:false,error:'CANDIDATES_REQUIRED'};
  const seen=new Set(), legal=[];
  for(const raw of input.candidates){
    const c={...raw},id=String(c.candidateId||'');
    if(!id||seen.has(id))return{ok:false,error:'DUPLICATE_OR_MISSING_ID'}; seen.add(id);
    if(c.assetAction!==PARTNER_ASSET_NONE)return{ok:false,error:'ASSET_ACTION_BLOCKED'};
    if(c.publicScope===false)return{ok:false,error:'HIDDEN_INFO_BLOCKED'};
    if(c.legal===false)continue;
    if(!Number.isFinite(Number(c.positionOrder)))return{ok:false,error:'POSITION_REQUIRED'};
    if((rule==='max'||rule==='min')&&(c.comparisonValue===null||c.comparisonValue===undefined||c.comparisonValue===''||!Number.isFinite(Number(c.comparisonValue))))return{ok:false,error:'COMPARISON_VALUE_UNRESOLVED'};
    legal.push(c);
  }
  if(!legal.length)return{ok:true,selected:null,ordered:[],next:null,reason:'NO_LEGAL_CANDIDATE',inputUnchanged:before===JSON.stringify(input)};
  const byId=(a,b)=>String(a.candidateId).localeCompare(String(b.candidateId),'en');
  legal.sort((a,b)=>{
    if(rule==='left'){const d=Number(a.positionOrder)-Number(b.positionOrder);return d||byId(a,b)}
    if(rule==='right'){const d=Number(b.positionOrder)-Number(a.positionOrder);return d||byId(a,b)}
    if(rule==='max'){const d=Number(b.comparisonValue)-Number(a.comparisonValue);return d||byId(a,b)}
    const d=Number(a.comparisonValue)-Number(b.comparisonValue);re
-- hit 2 index=10127739 --
erAdviceEnvelope({m=state.match,rule=null,kind='generic',status='ready',recommendation=null,alternatives=[],reason=null,reversalCondition=null,publicEvidence=null,error=null}){return Object.freeze({schema:'GAMEROAD_PARTNER_ADVICE_ENVELOPE_V1',status,kind,rule,stateVersion:partnerAdviceStateVersion(m),recommendation,alternatives,reason,reversalCondition,publicEvidence,error})}
function partnerAdviceStaleEnvelope(m=state.match){return partnerAdviceEnvelope({m,rule:currentPartnerRule(),kind:'stale',status:'blocked',error:'STALE_STATE',reason:'盤面状態が助言作成時から変わりました。',reversalCondition:'現在の状態から助言を再取得してください。',publicEvidence:null})}
window.__GAMEROAD_PARTNER_ADVICE_STATE_VERSION__=partnerAdviceStateVersion;
window.__GAMEROAD_PARTNER_ADVICE_STALE__=partnerAdviceStaleEnvelope;
function replayFor(candidates,rule=currentPartnerRule()){if(!rule)return{ok:false,error:'PARTNER_RULE_NOT_SELECTED'};return partnerReplay({rule,candidates})}
function partnerAdviceNow(){const m=state.match,pd=ensurePartnerRuntime();if(!m||!pd)return null;const rule=pd.rule;if(!rule){pd.advice='作戦規則が未選択です。左端・右端・最大・最小の正式4規則から選んでください。';pd.adviceEnvelope=partnerAdviceEnvelope({m,kind:'rule',status:'blocked',error:'PARTNER_RULE_NOT_SELECTED',reason:'正式4規則のいずれかを明示選択する必要があります。'});renderPartnerRuntime();toast('作戦規則を選んでください');return null}
  if(m.phase==='plan'){
    const roadCandidates=planCardCandidates('road'),road=replayFor(roadCandidates,rule);let battleCandidates=[],battle={ok:false,error:'ROAD_UNRESOLVED'};
    if(road.ok&&road.selected){battleCandidates=planCardCandidates('battle').filter(c=>c.candidateId!==road.selected.candidateId);battle=replayFor(battleCandidates,rule)}
    if(!road.ok||!battle.ok){pd.advice=`アドバイスできません：${road.error||battle.error}`;pd.adviceEnvelope=partnerAdviceEnvelope({m,rule,kind:'plan',status:'blocked',error:road.error||battle.error,reason:'合法候補の再生に失敗したため、カードを勝手に選びません。',publicEvidence:{roadCandidates:roadCandidates.map(partnerPublicCandidate),battleCandidates:battleCandidates.map(partnerPublicCandidate)}});renderPartnerRuntime();return null}
    const roadSelected=partnerPublicCandidate(road.selected),battleSelected=partnerPublicCandidate(battle.selected),roadAlternatives=partnerAdviceCandidates(road.ordered.slice(1,3),roadCandidates),battleAlternatives=partnerAdviceCandidates(battle.ordered.slice(1,3),battleCandidates);
    pd.adviceEnvelope=partnerAdviceEnvelope({m,rule,kind:'plan',recommendation:{road:roadSelected,battle:battleSelected},alternatives:{road:roadAlternatives,battle:b
-- hit 3 index=10142653 --
ion();renderPartnerRuntime();if(partnerDelegated())queueMicrotask(()=>attemptPartnerAutoDecision(partnerForced()?'hate-forced':'voluntary-delegate').catch(e=>{const pd=ensurePartnerRuntime();pd.blocker=e.message;renderPartnerRuntime()}))}};
const __grBaseEndMatch=endMatch;
endMatch=function(winners){const m=state.match;if(m?.partnerDecision){m.partnerDecision.session=null;m.partnerDecision.forced=false;m.partnerDecision.voluntary=false}return __grBaseEndMatch(winners)};
const __grBaseShow=show;
show=function(name){const r=__grBaseShow(name);if(name!=='battle'&&state.match?.partnerDecision)state.match.partnerDecision.session=null;return r};
const __grOriginalConfirmTarget=$('#confirmTarget').onclick;
$('#confirmTarget').onclick=async()=>{if(partnerForced())return toast('ヘイト爆発中は必須判断を手動変更できません');return __grOriginalConfirmTarget()};
installPartnerUi();
window.__GAMEROAD_HATE_PARTNER_TEST__={partnerReplay,normalizePartnerCandidates,ensure:ensurePartnerRuntime,tick:(ms)=>{const pd=ensurePartnerRuntime();if(pd){pd.lastTick-=Number(ms)||0;tickPartnerHate(performance.now())}return currentHuman()?{hate:currentHuman().hateTime,exploded:currentHuman().hateExploded,count:currentHuman().hateExplosionCount}:null},setRule:r=>{const pd=ensurePartnerRuntime();pd.rule=r||null;grPersistPartnerMatchSession();renderPartnerRuntime();return pd.rule},setVoluntary:v=>{const pd=ensurePartnerRuntime();pd.voluntary=!!v;grPersistPartnerMatchSession();renderPartnerRuntime();return pd.voluntary},advice:partnerAdviceNow,adviceEnvelope:()=>ensurePartnerRuntime()?.adviceEnvelope||null,adviceStateVersion:()=>partnerAdviceStateVersion(state.match),validateAdviceEnvelope:env=>{const now=partnerAdviceStateVersion(state.match);return env?.stateVersion===now?{ok:true,stateVersion:now}:{ok:false,error:'STALE_STATE',stateVersion:now}},attempt:attemptPartnerAutoDecision,session:()=>ensurePartnerRuntime()?.session||null,status:()=>{const p=currentHuman(),pd=ensurePartnerRuntime();return p&&pd?{hate:p.hateTime,exploded:p.hateExploded,explosionCount:p.hateExplosionCount,rule:pd.rule,voluntary:pd.voluntary,forced:pd.forced,blocker:pd.blocker,advice:pd.advice,adviceEnvelope:pd.adviceEnvelope,session:pd.session?.signature||null}:null},targetCandidates:targetPlayerCandidates,planCandidates:planCardCandidates,selectionLock:()=>state.match?.selectionLock||null,selectionCheck:()=>grVerifySelectionLock(state.match),selectionGuard:grPartnerSelectionGuard,deckDigest:grDeckIdentityDigest,persistSelection:grPersistPartnerMatch
-- hit 4 index=10204992 --
 sig=friendHateSignature(p.id),c=FR.hateClocks[p.id]||(FR.hateClocks[p.id]={signature:sig,lastTick:at,remainderMs:0});if(c.signature!==sig){c.signature=sig;c.remainderMs=0;c.lastTick=at}else if(!sig){c.remainderMs=0;c.lastTick=at}}FR.hateLastTick=at}
function friendPlanCandidatesFor(p,kind){return normalizePartnerCandidates(p.hand.map((id,i)=>({kind,cardId:id,id,positionOrder:i,comparisonValue:Number(CARDS[id]?.power),legal:roadSelectable(id),assetAction:PARTNER_ASSET_NONE})).filter(x=>x.legal),kind)}
function friendTargetCandidatesFor(p){const all=state.match?.players||[],legal=legalOpponents(p);return normalizePartnerCandidates(legal.map(x=>({kind:'player',player:x,id:x.id,positionOrder:all.findIndex(y=>y.id===x.id),comparisonValue:null,assetAction:PARTNER_ASSET_NONE})),'player')}
function friendReplay(candidates,rule){if(!rule)return{ok:false,error:'PARTNER_RULE_NOT_SELECTED'};return partnerReplay({rule,candidates})}
function friendForcedChoiceIndices(wait,rule){if(!wait)return{ok:false,error:'CHOICE_NOT_FOUND'};if(Number(wait.min)!==Number(wait.max))return{ok:false,error:'VARIABLE_COUNT_UNRESOLVED'};let remain=makeAbilityPartnerCandidates(wait.items),picked=[];for(let n=0;n<Number(wait.min);n++){const r=friendReplay(remain,rule);if(!r.ok||!r.selected)return{ok:false,error:r.error||'NO_CANDIDATE'};picked.push(Number(r.selected.positionOrder));remain=remain.filter(c=>c.candidateId!==r.selected.candidateId)}return{ok:true,indices:picked}}
async function friendForcedDecision(pid,reason='hate-forced'){const m=state.match,p=m?.players?.find(x=>x.id===pid);if(FR.role!=='host'||!m?.friendRoom||!p?.roomHuman||!p.hateExploded)return false;const rule=friendRule(pid);if(!rule){FR.hateBlockers[pid]='PARTNER_RULE_NOT_SELECTED';if(pid==='P1'){const pd=ensurePartnerRuntime(m);pd.blocker='作戦規則の初期値が未裁定です';renderPartnerRuntime()}broadcastSnapshots();return false}delete FR.hateBlockers[pid];if(m.phase==='plan'&&!m.busy&&!FR.planValues[pid]){const road=friendReplay(friendPlanCandidatesFor(p,'road'),rule);if(!road.ok||!road.selected)throw Error('forced-road:'+(road.error||'NO_LEGAL_CANDIDATE'));const battle=friendReplay(friendPlanCandidatesFor(p,'battle').filter(c=>c.candidateId!==road.selected.candidateId),rule);if(!battle.ok||!battle.selected)throw Error('forced-battle:'+(battle.error||'NO_LEGAL_CANDIDATE'));const q=legalPlan(p,{roadId:road.selected.candidateId,battleId:battle.selected.candidateId,path:[p.position]});if(!q.ok)throw Error('forced-plan:'+q.reason);FR.planValues[pid]=q.p
count_shown=4

=== partner-legal-action-adapter ===
count_shown=0

=== selectPartnerManifestOrRuleCandidate ===
count_shown=0