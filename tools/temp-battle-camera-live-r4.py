from pathlib import Path
import json, re

SCRIPT = Path('tools/temp-battle-camera-live-r4.py')
MARK = '# CAMERA_NARROW_PROBE='
if any(line.startswith(MARK) for line in SCRIPT.read_text(encoding='utf-8').splitlines()):
    raise SystemExit(0)
text = Path('browser/GAMEROAD.html').read_text(encoding='utf-8')
build = Path('deploy/cloudflare/scripts/build.mjs').read_text(encoding='utf-8')

def ctx(needle, before=900, after=2200, limit=4):
    out=[]; pos=0
    while len(out)<limit:
        i=text.find(needle,pos)
        if i<0: break
        out.append({'line':text.count('\n',0,i)+1,'index':i,'text':re.sub(r'\s+',' ',text[max(0,i-before):min(len(text),i+len(needle)+after)])})
        pos=i+len(needle)
    return out

result={
  'fieldCamera':ctx('function fieldCamera(',1200,4200,2),
  'battleMapPointer':ctx("$('#battleMap')",900,2600,6),
  'pointerdown':ctx("addEventListener('pointerdown'",800,2600,6),
  'wheel':ctx("addEventListener('wheel'",800,2400,6),
  'renderField3DCalls':ctx('renderField3D();',650,1500,8),
  'renderBoardDef':ctx('function renderBoard(',1000,2600,2),
  'battleMapMarkup':ctx('id="battleMap"',700,1800,2),
  'fieldCanvasMarkup':ctx('id="fieldCanvas"',700,1800,2),
  'battleRuntimeMarkup':ctx('id="battleRuntime"',700,1800,2),
  'buildCameraControl':build.count('battle-camera-control-core.mjs'),
  'buildCameraRouter':build.count('battle-camera-input-router.mjs'),
}
raw=json.dumps(result,ensure_ascii=False,separators=(',',':'))
with SCRIPT.open('a',encoding='utf-8') as f:
    f.write('\n'+MARK+raw+'\n')
print(raw)

# CAMERA_NARROW_PROBE={"fieldCamera":[{"line":1696,"index":10056650,"text":"me.plan.roadId)).keys())s.add(id);if(m.honey?.byNode)Object.keys(m.honey.byNode).filter(id=>m.honey.byNode[id]>0).forEach(id=>s.add(id));return[...s]} // R14 camera responsibility: movement selection narrows plan focus; battle resolution may independently focus the confirmed collision. // Logic/hitboxes stay on reachable()/nodeWorld()/project3; this only chooses the camera eye. function fieldMoveFocusNodes(){const m=state.match,me=m?.players?.[0];if(!m||!me||m.phase!=='plan'||!me.plan?.roadId)return null;const moveLimit=normalMoveLimitForRoad(me.plan.roadId);if(moveLimit<=0)return null;const s=new Set([me.position,...reachable(me,moveLimit).keys(),...(me.plan?.path||[])]);return[...s]} function fieldBattleFocusNodes(){const m=state.match,p=m?.battlePresentation;if(!m||m.phase!=='resolve'||!m.target||!p)return null;const attacker=m.players.find(x=>x.id===p.attackerId),defender=m.players.find(x=>x.id===p.defenderId),s=new Set();if(attacker?.position)s.add(attacker.position);if(defender?.position)s.add(defender.position);s.add(shieldId(p.defenderId,p.shield));const depth=defender?.lanes?.[p.lane]?.length||0;if(depth>0)s.add(roadId(p.defenderId,p.lane,Math.min(7,depth)));return[...s]} function fieldCamera(viewer){const profile=fieldViewportProfile(),battleFocus=fieldBattleFocusNodes(),moveFocus=fieldMoveFocusNodes(),focus=battleFocus?.length?battleFocus:moveFocus;if(focus?.length){const pts=focus.map(id=>nodeWorld(id,viewer)),xs=pts.map(p=>p.x),zs=pts.map(p=>p.z),minX=Math.min(...xs),maxX=Math.max(...xs),minZ=Math.min(...zs),maxZ=Math.max(...zs),cx=(minX+maxX)/2,cz=(minZ+maxZ)/2;let r=2.4;for(const p of pts)r=Math.max(r,Math.hypot(p.x-cx,p.z-cz)+.9);const zoom=profile.kind==='portrait'?Math.max(12,r*2.65):profile.kind==='short-landscape'?Math.max(9.5,r*2.20):Math.max(11,r*2.45),eye=[cx,zoom,cz+zoom],center=[cx,0,cz];return{eye,center,r,moveFocus:!!moveFocus?.length,battleFocus:!!battleFocus?.length,...profile}}let r=11.5;for(const id of fieldFocusNodes()){const w=nodeWorld(id,viewer);r=Math.max(r,Math.hypot(w.x,w.z)+1.1)}r=Math.min(13,r);const zoom=profile.kind==='portrait'?Math.max(24,r*1.84):profile.kind==='short-landscape'?Math.max(18,r*1.38):Math.max(22,r*1.69),eye=[0,zoom,zoom],center=[0,0,0];return{eye,center,r,moveFocus:false,battleFocus:false,...profile}} function fieldProject(id,viewer){if(fieldProjection.has(id))return fieldProjection.get(id);return null} function nodeTopSurface(id,viewer){const nw=nodeWorld(id,viewer),typ=posType(id);let size=0,topY=nw.y;if(typ==='center'){size=1.06;topY=nw.y-.015+.16}else if(typ==='shield'){size=1.05;topY=nw.y+.24}else if(typ==='road'){if(!activeNodes().has(id))return null;size=.88;topY=nw.y-.01+.105}else return null;const h=size/2,world=[[nw.x-h,topY,nw.z-h],[nw.x+h,topY,nw.z-h],[nw.x+h,topY,nw.z+h],[nw.x-h,topY,nw.z+h]],projected=world.map(([x,y,z])=>project3({x,y,z}));return projected.every(Boolean)?projected:null} function syncRangeSurfacePath(){const m=state.match,layer=$('#rangeSurfaceLayer');if(!m||!layer)return;const path=new Set(m.players[0]?.plan?.path||[]);[...layer.children].forEach(e=>e.classList.toggle('path',path.has(e.dataset.pos)))} function renderRangeSurfaces(reach,moveLimit,viewer){const layer=$('#rangeSurfaceLayer');if(!layer)return;layer.replaceChildren();if(!reach||moveLimit<=0)return;const ns='http://www.w3.org/2000/svg';for(const [id,d] of reach){if(d>moveLimit)continue;const pts=nodeTopSurface(id,viewer);if(!pts)continue;const e=document.createElementNS(ns,'polygon');e.setAttribute('class','rangeSurface');e.setAttribute('points',pts.map(p=>`${p.x},${p.y}`).join(' '));e.dataset.pos=id;layer.appendChild(e)}syncRangeSurfacePath()} function glShader(gl,type,src){const sh=gl.createShader(type);gl.shaderSource(sh,src);gl.compileShader(sh);if(!gl.getShaderParameter(sh,gl.COMPILE_STATUS))throw Error(gl.getShaderInfoLog(sh)||'shader');return sh} function initFieldGL(){const c=$('#fieldCanvas');if(fieldGL&&fieldGL.canvas===c&&!fieldGL.isContextLost?.())return fieldGL;let gl=null;try{gl=c.getContext('webgl',{alpha:false,antialias:!state.settings.lowPerf,depth:true,preserveDrawingBuffer:false,powerPreference:state.settings.lowPerf?'low-power':'high-performance'})}catch(e){}if(!gl)return null;const vs=glShader(gl,gl.VERTEX_SHADER,'attribute vec3 p;attribute vec3 n;attribute vec3 c;uniform vec3 uEye;uniform vec3 uLight;uniform vec4 uView;varying vec3 vColor;varying float vFog;void main(){vec3 r=p-uEye;float q=.70710678;float cx=r.x;float cy=q*r.y-q*r.z;float cz=max(.08,-q*r.y-q*r.z);float nx=cx/(cz*uView.x*uView.y);float ny=cy/(cz*uView.x);float yNdc=1.0-2.0*uView.z+2.0*ny*uView.w;float z01=clamp((cz-.10)/48.0,0.0,1.0);float zNdc=z01*2.0-1.0;gl_Position=vec4(nx*cz,yNdc*cz,zNdc*cz,cz);vec3 nn=normalize(n);float sun=max(dot(nn,normalize(uLight)),0.0);float hemi=max(nn.y,0.0);vColor=c*(.53+.39*sun+.12*hemi);vFog=smoothstep(23.0,43.0,cz);}'),fs=glShader(gl,gl.FRAGMENT_SHADER,'precision mediump float;varying vec3 vColor;varying float vFog;void main(){vec3 haze=vec3(.54,.69,.63);vec3 col=mix(clamp(vColor,0.0,1.0),haze,vFog*.52);gl_FragColor=vec4(col,1.0);}');const pr=gl.createProgram();gl.attachShader(pr,vs);gl.attachShader(pr,fs);gl.linkProgram(pr);if(!gl.getProgramParameter(pr,gl.LINK_STATUS))throw Error(gl.getProgramInfoLog(pr)||'link');fieldGL=gl;fieldProgram=pr;if(!c.dataset.grasslandGlLifecycle){c.dataset.grasslandGlLifecycle='1';c.addEventListener('webglcontextlost'"}],"battleMapPointer":[{"line":1689,"index":10054524,"text":":{x:10,z:-10},SW:{x:-10,z:10},SE:{x:10,z:10}}[id.split(':')[1]];return{k,y:.2,...k}} function vsub(a,b){return[a[0]-b[0],a[1]-b[1],a[2]-b[2]]}function vdot(a,b){return a[0]*b[0]+a[1]*b[1]+a[2]*b[2]}function vcross(a,b){return[a[1]*b[2]-a[2]*b[1],a[2]*b[0]-a[0]*b[2],a[0]*b[1]-a[1]*b[0]]}function vnorm(a){const l=Math.hypot(...a)||1;return a.map(x=>x/l)} function m4mul(a,b){const o=new Array(16).fill(0);for(let r=0;r<4;r++)for(let c=0;c<4;c++)for(let k=0;k<4;k++)o[r*4+c]+=a[r*4+k]*b[k*4+c];return o} function m4persp(fovy,aspect,near,far){const f=1/Math.tan(fovy/2),nf=1/(near-far);return[f/aspect,0,0,0,0,f,0,0,0,0,(far+near)*nf,-1,0,0,2*far*near*nf,0]} function m4look(eye,center,up){const z=vnorm(vsub(eye,center)),x=vnorm(vcross(up,z)),y=vcross(z,x);return[x[0],y[0],z[0],0,x[1],y[1],z[1],0,x[2],y[2],z[2],0,-vdot(x,eye),-vdot(y,eye),-vdot(z,eye),1]} function fieldViewportProfile(){const map=$('#battleMap'),w=Math.max(1,map?.clientWidth||innerWidth),h=Math.max(1,map?.clientHeight||innerHeight),raw=w/h;if(raw>1.85&&h<500)return{aspect:Math.max(1.2,Math.min(2.28,raw)),yBase:.36,yGain:.29,kind:'short-landscape'};if(raw<.78)return{aspect:1.2,yBase:.44,yGain:.40,kind:'portrait'};return{aspect:Math.max(1.2,raw),yBase:.45,yGain:.42,kind:'standard'}} function project3(w){const c=fieldCameraState;if(!c)return null;const eye=c.eye,rx=w.x-eye[0],ry=(w.y||0)-eye[1],rz=w.z-eye[2],q=Math.SQRT1_2,cx=rx,cy=q*ry-q*rz,cz=-q*ry-q*rz;if(cz<=.08)return null;const t=Math.tan(Math.PI/10.2),nx=cx/(cz*t*c.aspect),ny=cy/(cz*t);return{x:(nx*.5+.5)*100,y:(c.yBase-ny*c.yGain)*100}} function fieldFocusNodes(){const m=state.match;if(!m)return centerIds();const s=new Set([...activeNodes(),...m.players.map(p=>p.position)]);const me=m.players[0];if(m.phase==='plan'&&me.plan?.roadId)for(const id of reachable(me,normalMoveLimitForRoad(me.plan.roadId)).keys())s.add(id);if(m.honey?.byNode)Object.keys(m.honey.byNode).filter(id=>m.honey.byNode[id]>0).forEach(id=>s.add(id));return[...s]} // R14 camera responsibility: movement selection narrows plan focus; battle resolution may independently focus the confirmed collision. // Logic/hitboxes stay on reachable()/nodeWorld()/project3; this only chooses the camera eye. function fieldMoveFocusNodes(){const m=state.match,me=m?.players?.[0];if(!m||!me||m.phase!=='plan'||!me.plan?.roadId)return null;const moveLimit=normalMoveLimitForRoad(me.plan.roadId);if(moveLimit<=0)return null;const s=new Set([me.position,...reachable(me,moveLimit).keys(),...(me.plan?.path||[])]);return[...s]} function fieldBattleFocusNodes(){const m=state.match,p=m?.battlePresentation;if(!m||m.phase!=='resolve'||!m.target||!p)return null;const attacker=m.players.find(x=>x.id===p.attackerId),defender=m.players.find(x=>x.id===p.defenderId),s=new Set();if(attacker?.position)s.add(attacker.position);if(defender?.position)s.add(defender.position);s.add(shieldId(p.defenderId,p.shield));const depth=defender?.lanes?.[p.lane]?.length||0;if(depth>0)s.add(roadId(p.defenderId,p.lane,Math.min(7,depth)));return[...s]} function fieldCamera(viewer){const profile=fieldViewportProfile(),battleFocus=fieldBattleFocusNodes(),moveFocus=fieldMoveFocusNodes(),focus=battleFocus?.length?battleFocus:moveFocus;if(focus?.length){const pts=focus.map(id=>nodeWorld(id,viewer)),xs=pts.map(p=>p.x),zs=pts.map(p=>p.z),minX=Math.min(...xs),maxX=Math.max(...xs),minZ=Math.min(...zs),maxZ=Math.max(...zs),cx=(minX+maxX)/2,cz=(minZ+maxZ)/2;let r=2.4;for(const p of pts)r=Math.max(r,Math.hypot(p.x-cx,p.z-cz)+.9);const zoom=prof"},{"line":1746,"index":10079908,"text":"or='#ffc43d';ctx.shadowBlur=12;ctx.fill();ctx.shadowBlur=0}} const vignette=ctx.createRadialGradient(cw*.5,ch*.47,Math.min(cw,ch)*.20,cw*.5,ch*.47,Math.max(cw,ch)*.70);vignette.addColorStop(0,'rgba(0,0,0,0)');vignette.addColorStop(1,'rgba(0,9,9,.48)');ctx.fillStyle=vignette;ctx.fillRect(0,0,cw,ch);}}fieldViewSize={w:canvas.clientWidth,h:canvas.clientHeight}} function renderRouteLine(){const m=state.match;if(!m)return;const me=m.players[0],path=me.plan?.path||[me.position],pts=path.map(id=>fieldProjection.get(id)).filter(Boolean).map(p=>`${p.x},${p.y}`).join(' ');const line=$('#routeLine');line.setAttribute('points',pts);line.setAttribute('vector-effect','non-scaling-stroke');const origin=fieldProjection.get(me.position);if(origin){$('#originMarker').style.left=origin.x+'%';$('#originMarker').style.top=origin.y+'%'}} function nearestProjectedNode(clientX,clientY,onlyReach=true){const map=$('#battleMap'),r=map.getBoundingClientRect(),me=state.match?.players?.[0];if(!me)return null;const reach=me.plan?.roadId?reachable(me,normalMoveLimitForRoad(me.plan.roadId)):new Map();let best=null,bd=1e9;for(const [id,p] of fieldProjection){if(onlyReach&&!reach.has(id))continue;const x=r.left+p.x/100*r.width,y=r.top+p.y/100*r.height,d=Math.hypot(clientX-x,clientY-y);if(d<bd){bd=d;best=id}}return bd<=Math.max(27,Math.min(44,r.width*.055))?best:null} function refreshDragVisual(){const me=state.match.players[0],end=me.plan?.path?.at(-1)||me.position,p=fieldProjection.get(end);if(p){const e=$('#battleRuntime');e.style.left=p.x+'%';e.style.top=p.y+'%'}layoutBoardPlayerTokens();renderRouteLine();syncRangeSurfacePath();$$('#board .node').forEach(e=>e.classList.toggle('path',me.plan?.path?.includes(e.dataset.pos)));$('#endpointText').textContent=end;window.__GAMEROAD_BATTLE_FOCUS_R1__?.decorate?.()} function beginRouteDrag(ev){const m=state.match,me=m?.players?.[0];if(!m||m.phase!=='plan'||m.busy||!me.plan?.roadId)return;const power=normalMoveLimitForRoad(me.plan.roadId);if(power<1)return;routeDrag={pointerId:ev.pointerId,before:[...(me.plan.path||[me.position])],path:[me.position]};me.plan.path=[me.position];$('#battleRuntime').classList.add('dragging');try{$('#battleRuntime').setPointerCapture(ev.pointerId)}catch(e){}refreshDragVisual();ev.preventDefault()} function moveRouteDrag(ev){if(!routeDrag||ev.pointerId!==routeDrag.pointerId)return;const me=state.match.players[0],map=$('#battleMap'),rect=map.getBoundingClientRect(),av=$('#battleRuntime');const px=Math.max(2,Math.min(98,(ev.clientX-rect.left)/Math.max(1,rect.width)*100)),py=Math.max(3,Math.min(97,(ev.clientY-rect.top)/Math.max(1,rect.height)*100));av.style.left=px+'%';av.style.top=py+'%';const id=nearestProjectedNode(ev.clientX,ev.clientY,true);if(!id){ev.preventDefault();return}let path=routeDrag.path,last=path.at(-1),prev=path.length>1?path[path.length-2]:null;if(id!==last){if(id===prev){path.pop()}else if(path.length-1<normalMoveLimitForRoad(me.plan.roadId)&&neighbors(last,me).includes(id)&&!path.includes(id)){path.push(id)}me.plan.path=[...path];renderRouteLine();syncRangeSurfacePath();$$('#board .node').forEach(e=>e.classList.toggle('path',me.plan?.path?.includes(e.dataset.pos)));$('#endpointText').textContent=path.at(-1)||me.position;window.__GAMEROAD_BATTLE_FOCUS_R1__?.decorate?.()}ev.preventDefault()} function endRouteDrag(ev){if(!routeDrag||ev.pointerId!==routeDrag.pointerId)return;const me=state.match.players[0],drop=nearestProjectedNode(ev.clientX,ev.clientY,true),last=routeDrag.path.at(-1),tapOnly=routeD"},{"line":1749,"index":10081404,"text":"$('#battleRuntime');e.style.left=p.x+'%';e.style.top=p.y+'%'}layoutBoardPlayerTokens();renderRouteLine();syncRangeSurfacePath();$$('#board .node').forEach(e=>e.classList.toggle('path',me.plan?.path?.includes(e.dataset.pos)));$('#endpointText').textContent=end;window.__GAMEROAD_BATTLE_FOCUS_R1__?.decorate?.()} function beginRouteDrag(ev){const m=state.match,me=m?.players?.[0];if(!m||m.phase!=='plan'||m.busy||!me.plan?.roadId)return;const power=normalMoveLimitForRoad(me.plan.roadId);if(power<1)return;routeDrag={pointerId:ev.pointerId,before:[...(me.plan.path||[me.position])],path:[me.position]};me.plan.path=[me.position];$('#battleRuntime').classList.add('dragging');try{$('#battleRuntime').setPointerCapture(ev.pointerId)}catch(e){}refreshDragVisual();ev.preventDefault()} function moveRouteDrag(ev){if(!routeDrag||ev.pointerId!==routeDrag.pointerId)return;const me=state.match.players[0],map=$('#battleMap'),rect=map.getBoundingClientRect(),av=$('#battleRuntime');const px=Math.max(2,Math.min(98,(ev.clientX-rect.left)/Math.max(1,rect.width)*100)),py=Math.max(3,Math.min(97,(ev.clientY-rect.top)/Math.max(1,rect.height)*100));av.style.left=px+'%';av.style.top=py+'%';const id=nearestProjectedNode(ev.clientX,ev.clientY,true);if(!id){ev.preventDefault();return}let path=routeDrag.path,last=path.at(-1),prev=path.length>1?path[path.length-2]:null;if(id!==last){if(id===prev){path.pop()}else if(path.length-1<normalMoveLimitForRoad(me.plan.roadId)&&neighbors(last,me).includes(id)&&!path.includes(id)){path.push(id)}me.plan.path=[...path];renderRouteLine();syncRangeSurfacePath();$$('#board .node').forEach(e=>e.classList.toggle('path',me.plan?.path?.includes(e.dataset.pos)));$('#endpointText').textContent=path.at(-1)||me.position;window.__GAMEROAD_BATTLE_FOCUS_R1__?.decorate?.()}ev.preventDefault()} function endRouteDrag(ev){if(!routeDrag||ev.pointerId!==routeDrag.pointerId)return;const me=state.match.players[0],drop=nearestProjectedNode(ev.clientX,ev.clientY,true),last=routeDrag.path.at(-1),tapOnly=routeDrag.path.length===1;if(tapOnly&&drop&&drop!==last){routeDrag=null;$('#battleRuntime').classList.remove('dragging');selectEndpoint(drop);refreshDragVisual();ev.preventDefault();return}if(!drop||drop!==last){me.plan.path=routeDrag.before;toast('移動可能マスへ置くと経路を予約します')}else me.plan.path=[...routeDrag.path];routeDrag=null;$('#battleRuntime').classList.remove('dragging');refreshDragVisual();ev.preventDefault()} function bindRouteDrag(){const e=$('#battleRuntime'),map=$('#battleMap');if(!e||e.dataset.dragBound)return;e.dataset.dragBound='1';e.addEventListener('pointerdown',beginRouteDrag);map.addEventListener('pointermove',moveRouteDrag,{passive:false});map.addEventListener('pointerup',endRouteDrag,{passive:false});map.addEventListener('pointercancel',endRouteDrag,{passive:false})} function awakeManaFromHoney(p,count){const before=p.awake,wake=Math.max(0,Math.min(count,p.mana.length-p.awake));p.awake+=wake;return{before,wake,after:p.awake,excess:count-wake}} function honeySourceNodesForPower(power){if(!state.match||power<1||power>6)return[];const out=[];for(const p of state.match.players){for(const l of ['L','C','R']){p.lanes[l].forEach((cid,ix)=>{if(CARDS[cid]?.power===power)out.push(roadId(p.id,l,ix+1))})}}return out} function spawnHoneyFromRoads(){const m=state.match;if(m?.contentId!=='honey_hunt')return;for(const p of m.players){const power=CARDS[p.plan?.roadId]?.power||0;if(power<1||power>6)continue;for(const id of honeySourceNodesForPower(power))m.honey.byNode[id]=(m.honey.b"},{"line":1751,"index":10082987,"text":");$$('#board .node').forEach(e=>e.classList.toggle('path',me.plan?.path?.includes(e.dataset.pos)));$('#endpointText').textContent=path.at(-1)||me.position;window.__GAMEROAD_BATTLE_FOCUS_R1__?.decorate?.()}ev.preventDefault()} function endRouteDrag(ev){if(!routeDrag||ev.pointerId!==routeDrag.pointerId)return;const me=state.match.players[0],drop=nearestProjectedNode(ev.clientX,ev.clientY,true),last=routeDrag.path.at(-1),tapOnly=routeDrag.path.length===1;if(tapOnly&&drop&&drop!==last){routeDrag=null;$('#battleRuntime').classList.remove('dragging');selectEndpoint(drop);refreshDragVisual();ev.preventDefault();return}if(!drop||drop!==last){me.plan.path=routeDrag.before;toast('移動可能マスへ置くと経路を予約します')}else me.plan.path=[...routeDrag.path];routeDrag=null;$('#battleRuntime').classList.remove('dragging');refreshDragVisual();ev.preventDefault()} function bindRouteDrag(){const e=$('#battleRuntime'),map=$('#battleMap');if(!e||e.dataset.dragBound)return;e.dataset.dragBound='1';e.addEventListener('pointerdown',beginRouteDrag);map.addEventListener('pointermove',moveRouteDrag,{passive:false});map.addEventListener('pointerup',endRouteDrag,{passive:false});map.addEventListener('pointercancel',endRouteDrag,{passive:false})} function awakeManaFromHoney(p,count){const before=p.awake,wake=Math.max(0,Math.min(count,p.mana.length-p.awake));p.awake+=wake;return{before,wake,after:p.awake,excess:count-wake}} function honeySourceNodesForPower(power){if(!state.match||power<1||power>6)return[];const out=[];for(const p of state.match.players){for(const l of ['L','C','R']){p.lanes[l].forEach((cid,ix)=>{if(CARDS[cid]?.power===power)out.push(roadId(p.id,l,ix+1))})}}return out} function spawnHoneyFromRoads(){const m=state.match;if(m?.contentId!=='honey_hunt')return;for(const p of m.players){const power=CARDS[p.plan?.roadId]?.power||0;if(power<1||power>6)continue;for(const id of honeySourceNodesForPower(power))m.honey.byNode[id]=(m.honey.byNode[id]||0)+1}m.honey.generatedRounds.add(m.round)} function collectEndpointHoney(p){const m=state.match;if(m?.contentId!=='honey_hunt')return{status:'not-honey'};const id=p.position,count=m.honey.byNode[id]||0,key=`${m.round}:${p.id}:${id}`;if(m.honey.receipts.has(key))return{status:'duplicate',count:0};m.honey.receipts.add(key);if(count<=0)return{status:'none',count:0};m.honey.byNode[id]=0;const w=awakeManaFromHoney(p,count);const excess=w.excess>0?`／起床上限超過${w.excess}は持ち越さない`:'';log(`${p.name}：終点のハニー${count}個を全量取得。寝ているマナを${w.wake}枚起こした${excess}。`);if(p.human)window.GAMEROAD_PARTNER_COMMENT?.emit?.({eventId:`${m.id}:honey:${m.round}:${p.id}:${id}`,kind:'dialogue',speechIntent:'honey_collected',count,loggable:false,ttlMs:9000});return{status:'collected-all',count,...w}} const grControlledCharacter4pLiveState={warningShown:false}; function grControlledCharacter4pSource(){const m=state.match;if(!m||!Array.isArray(m.players)||m.players.length!==4)return null;const participants=m.players.map(p=>({participantId:p.id,characterId:String(p.character||''),positionKey:p.position}));if(participants.some(row=>!row.participantId||!row.characterId))return null;return{matchId:String(m.id||''),participants,reducedMotion:!!state.settings.reduceMotion,lowPerformance:!!state.settings.lowPerf}} window.__GAMEROAD_CONTROLLED_CHARACTER_4P_SOURCE__=grControlledCharacter4pSource; function grControlledCharacter4pEvent(p,type,detail={}){const emit=window.__GAMEROAD_CONTROLLED_CHARACTER_4P_EVENT__;if(typeof emit!=='function')return null;try{return emit(p?.id,{type,...detail})}catch("},{"line":1763,"index":10087822,"text":"path[i-1],to=path[i],nextFacing=movementFacing(from,to,p);p.position=to;if(human&&nextFacing!==facing)await setBattleRuntimeState('move',nextFacing);facing=nextFacing;grControlledCharacter4pEvent(p,'MOVE_ACCEPTED',{toPositionKey:to,facing});renderField3D();renderBoard();if(human)await updateBattleAvatar();if(dur)await new Promise(r=>setTimeout(r,dur))}collectEndpointHoney(p);grControlledCharacter4pEvent(p,'SETTLE',{positionKey:p.position});if(human)await setBattleRuntimeState('idle',facing);else{renderField3D();renderBoard()}} function setupBattleDetails(){const btn=$('#detailsBtn'),close=$('#detailsClose'),drawer=$('#battleDrawer');if(btn&&!btn.dataset.bound){btn.dataset.bound='1';btn.onclick=()=>{drawer.classList.add('on');drawer.setAttribute('aria-hidden','false')};close.onclick=()=>{drawer.classList.remove('on');drawer.setAttribute('aria-hidden','true')};$('#dangerBtn').onclick=()=>{$('#battleMap').classList.toggle('showRange');toast($('#battleMap').classList.contains('showRange')?'移動可能範囲を表示':'範囲表示を戻しました')}}} window.addEventListener('resize',()=>{if(fieldResizeQueued)return;fieldResizeQueued=true;requestAnimationFrame(()=>{fieldResizeQueued=false;if(state.screen==='battle'){renderField3D();renderBoard();updateBattleAvatar()}})}); async function updateBattleAvatar(){if(!state.match)return;const me=state.match.players[0],visual=(state.match.phase==='plan'&&me.plan?.path?.length)?me.plan.path.at(-1):me.position;const xy=nodeXY(visual,me),el=$('#battleRuntime');el.style.left=xy[0]+'%';el.style.top=xy[1]+'%';if(!battleMount){if(!battleMountPromise)battleMountPromise=mountChar('#battleRuntime',state.playerCharacterId,'idle').then(x=>(battleMount=x,x)).finally(()=>{battleMountPromise=null});battleMount=await battleMountPromise}layoutBoardPlayerTokens();renderRouteLine();bindRouteDrag()} function suitGlyphForCard(c){const raw=String(c?.suit||'').trim(),u=raw.toUpperCase(),s=raw.toLowerCase(),m={SP:'♠',HT:'♥',DI:'♦',CL:'♣',DCG:'◆'};return m[u]||(s.startsWith('spade')?'♠':s.startsWith('heart')?'♥':s.startsWith('diamond')?'♦':s.startsWith('club')?'♣':'•')} function layoutBoardPlayerTokens(){const root=$('#boardPlayers'),actor=$('#battleRuntime');if(!root||!actor)return;const a=actor.getBoundingClientRect(),els=[...root.children],placed=[],narrow=innerWidth<=540,mid=innerWidth<=900,rx=narrow?46:(mid?42:54),ry=narrow?30:(mid?28:34),hit=(r,t,g=3)=>Math.min(r.right+g,t.right)>Math.max(r.left-g,t.left)&&Math.min(r.bottom+g,t.bottom)>Math.max(r.top-g,t.top);for(const el of els){const id=el.dataset.player,count=Number(el.dataset.groupCount)||1,q={P1:[-1,-1],P2:[1,-1],P3:[-1,1],P4:[1,1]}[id]||[0,-1],solo=[[0,-ry],[q[0]*rx,q[1]*ry],[-rx,-ry],[rx,-ry],[-rx,0],[rx,0],[-rx,ry],[rx,ry],[0,ry]],group=[[q[0]*rx,q[1]*ry],[q[0]*rx,0],[0,q[1]*ry],[-q[0]*rx,q[1]*ry],[q[0]*rx,-q[1]*ry]],pool=count>1?group:solo;let ok=false;for(const [ox,oy] of pool){el.style.setProperty('--ox',ox+'px');el.style.setProperty('--oy',oy+'px');const r=el.getBoundingClientRect();if(!hit(r,a,2)&&placed.every(x=>!hit(r,x,2))){placed.push(r);ok=true;break}}if(!ok){for(const [ox,oy] of pool){el.style.setProperty('--ox',(ox*1.45)+'px');el.style.setProperty('--oy',(oy*1.45)+'px');const r=el.getBoundingClientRect();if(!hit(r,a,2)&&placed.every(x=>!hit(r,x,2))){placed.push(r);ok=true;break}}}if(!ok)placed.push(el.getBoundingClientRect())}} function renderBoardPlayers(){const root=$('#boardPlayers'),m=state.match;if(!root||!m)return;root.innerHTML='';const viewer=m.players[0],groups=new Map();for(const p of "},{"line":1763,"index":10087874,"text":"to,p);p.position=to;if(human&&nextFacing!==facing)await setBattleRuntimeState('move',nextFacing);facing=nextFacing;grControlledCharacter4pEvent(p,'MOVE_ACCEPTED',{toPositionKey:to,facing});renderField3D();renderBoard();if(human)await updateBattleAvatar();if(dur)await new Promise(r=>setTimeout(r,dur))}collectEndpointHoney(p);grControlledCharacter4pEvent(p,'SETTLE',{positionKey:p.position});if(human)await setBattleRuntimeState('idle',facing);else{renderField3D();renderBoard()}} function setupBattleDetails(){const btn=$('#detailsBtn'),close=$('#detailsClose'),drawer=$('#battleDrawer');if(btn&&!btn.dataset.bound){btn.dataset.bound='1';btn.onclick=()=>{drawer.classList.add('on');drawer.setAttribute('aria-hidden','false')};close.onclick=()=>{drawer.classList.remove('on');drawer.setAttribute('aria-hidden','true')};$('#dangerBtn').onclick=()=>{$('#battleMap').classList.toggle('showRange');toast($('#battleMap').classList.contains('showRange')?'移動可能範囲を表示':'範囲表示を戻しました')}}} window.addEventListener('resize',()=>{if(fieldResizeQueued)return;fieldResizeQueued=true;requestAnimationFrame(()=>{fieldResizeQueued=false;if(state.screen==='battle'){renderField3D();renderBoard();updateBattleAvatar()}})}); async function updateBattleAvatar(){if(!state.match)return;const me=state.match.players[0],visual=(state.match.phase==='plan'&&me.plan?.path?.length)?me.plan.path.at(-1):me.position;const xy=nodeXY(visual,me),el=$('#battleRuntime');el.style.left=xy[0]+'%';el.style.top=xy[1]+'%';if(!battleMount){if(!battleMountPromise)battleMountPromise=mountChar('#battleRuntime',state.playerCharacterId,'idle').then(x=>(battleMount=x,x)).finally(()=>{battleMountPromise=null});battleMount=await battleMountPromise}layoutBoardPlayerTokens();renderRouteLine();bindRouteDrag()} function suitGlyphForCard(c){const raw=String(c?.suit||'').trim(),u=raw.toUpperCase(),s=raw.toLowerCase(),m={SP:'♠',HT:'♥',DI:'♦',CL:'♣',DCG:'◆'};return m[u]||(s.startsWith('spade')?'♠':s.startsWith('heart')?'♥':s.startsWith('diamond')?'♦':s.startsWith('club')?'♣':'•')} function layoutBoardPlayerTokens(){const root=$('#boardPlayers'),actor=$('#battleRuntime');if(!root||!actor)return;const a=actor.getBoundingClientRect(),els=[...root.children],placed=[],narrow=innerWidth<=540,mid=innerWidth<=900,rx=narrow?46:(mid?42:54),ry=narrow?30:(mid?28:34),hit=(r,t,g=3)=>Math.min(r.right+g,t.right)>Math.max(r.left-g,t.left)&&Math.min(r.bottom+g,t.bottom)>Math.max(r.top-g,t.top);for(const el of els){const id=el.dataset.player,count=Number(el.dataset.groupCount)||1,q={P1:[-1,-1],P2:[1,-1],P3:[-1,1],P4:[1,1]}[id]||[0,-1],solo=[[0,-ry],[q[0]*rx,q[1]*ry],[-rx,-ry],[rx,-ry],[-rx,0],[rx,0],[-rx,ry],[rx,ry],[0,ry]],group=[[q[0]*rx,q[1]*ry],[q[0]*rx,0],[0,q[1]*ry],[-q[0]*rx,q[1]*ry],[q[0]*rx,-q[1]*ry]],pool=count>1?group:solo;let ok=false;for(const [ox,oy] of pool){el.style.setProperty('--ox',ox+'px');el.style.setProperty('--oy',oy+'px');const r=el.getBoundingClientRect();if(!hit(r,a,2)&&placed.every(x=>!hit(r,x,2))){placed.push(r);ok=true;break}}if(!ok){for(const [ox,oy] of pool){el.style.setProperty('--ox',(ox*1.45)+'px');el.style.setProperty('--oy',(oy*1.45)+'px');const r=el.getBoundingClientRect();if(!hit(r,a,2)&&placed.every(x=>!hit(r,x,2))){placed.push(r);ok=true;break}}}if(!ok)placed.push(el.getBoundingClientRect())}} function renderBoardPlayers(){const root=$('#boardPlayers'),m=state.match;if(!root||!m)return;root.innerHTML='';const viewer=m.players[0],groups=new Map();for(const p of m.players){const a=groups.get(p.position)||[];a.push"}],"pointerdown":[{"line":1536,"index":10019338,"text":"Count(){return $('#r4TrayCount')||$('#deckCount')||$('#r4DeckTotal')} function grDeckSwipePresentSuccess(sourceElement,cardId){const target=grDeckSwipeTarget();if(!grDeckSwipePresentation||!sourceElement||!target)return;try{grDeckSwipePresentation.playSuccess({sourceElement,targetElement:target,countElement:grDeckSwipeCount(),cardId})}catch(_){}} function grDeckSwipePresentReject(sourceElement,reason){const target=grDeckSwipeTarget();if(!grDeckSwipePresentation||!sourceElement)return;try{grDeckSwipePresentation.playReject({sourceElement,targetElement:target,reason})}catch(_){}} function grDeckSwipeResetVisual(e){if(!e)return;e.style.transform='';e.style.filter='';e.style.boxShadow='';delete e.dataset.swipeAdd} function bindCollectionSwipeAdd(e,c,inside){ e.style.touchAction='pan-y'; e.addEventListener('pointerdown',ev=>{if(ev.pointerType==='mouse'&&ev.button!==0)return;grDeckSwipeGesture={pointerId:ev.pointerId,id:c.id,startX:ev.clientX,startY:ev.clientY,lastX:ev.clientX,lastY:ev.clientY,el:e};e.dataset.swipeAdd='tracking';try{e.setPointerCapture(ev.pointerId)}catch(_){}}); e.addEventListener('pointermove',ev=>{const g=grDeckSwipeGesture;if(!g||g.pointerId!==ev.pointerId||g.el!==e)return;g.lastX=ev.clientX;g.lastY=ev.clientY;const dx=g.lastX-g.startX,dy=g.lastY-g.startY;if(Math.abs(dy)>16&&Math.abs(dy)>=Math.abs(dx)){grDeckSwipeGesture=null;grDeckSwipeResetVisual(e);return}if(dx>6&&dx>Math.abs(dy)*1.05){const ready=dx>=GR_DECK_SWIPE_ADD_THRESHOLD_PX&&dx>=Math.abs(dy)*GR_DECK_SWIPE_ADD_AXIS_RATIO;e.dataset.swipeAdd=ready?'ready':'tracking';e.style.transform=`translateX(${Math.min(dx,64)}px) scale(${ready?1.025:1.01})`;e.style.filter=ready?'brightness(1.09)':'';e.style.boxShadow=ready?'0 0 0 2px rgba(110,225,255,.72),0 10px 26px rgba(4,10,20,.36)':''}else if(dx<=0){grDeckSwipeResetVisual(e)}}); const finish=(ev,cancelled)=>{const g=grDeckSwipeGesture;if(!g||g.pointerId!==ev.pointerId||g.el!==e)return;grDeckSwipeGesture=null;const dx=ev.clientX-g.startX,dy=ev.clientY-g.startY;const committed=!cancelled&&dx>=GR_DECK_SWIPE_ADD_THRESHOLD_PX&&dx>=Math.abs(dy)*GR_DECK_SWIPE_ADD_AXIS_RATIO;grDeckSwipeResetVisual(e);if(!committed)return;grDeckSwipeSuppressClickUntil=performance.now()+420;ev.preventDefault();const r=addDeckCard(c.id);if(!r?.ok){grDeckSwipePresentReject(e,r?.error||'rejected');toast(r?.error||'追加できません');return}grDeckSwipePresentSuccess(e,c.id);recentDeckCardId=c.id;r4SetInspector(false);renderCards();focusRecentDeckCard();toast(inside?'札組登録済み':'札組へ追加しました')}; e.addEventListener('pointerup',ev=>finish(ev,false)); e.addEventListener('pointercancel',ev=>finish(ev,true)); e.addEventListener('lostpointercapture',()=>{if(grDeckSwipeGesture?.el===e){grDeckSwipeGesture=null;grDeckSwipeResetVisual(e)}}); e.onclick=ev=>{if(performance.now()<grDeckSwipeSuppressClickUntil){ev.preventDefault();return}state.selectedCardId=c.id;r4SetInspector(true);renderCards()}; } function renderCards(){renderDeckPicker();renderDeckSlots('#deckSlots','main');renderDeckSlots('#exDeckSlots','ex');const v=validateDeck(state.deckDraft),st=v.stats,elig=deckEligibility(state.deckDraft);$('#deckCount').textContent=st.main;$('#exDeckCount').textContent=st.ex;const sv=$('#deckSaveState');sv.textContent=deckDirty()?'未保存':'保存済み';sv.className=deckDirty()?'deckDirty':'deckSaved';const dv=$('#deckValidation');dv.className='deckValidation '+(elig.usable?'ok':'bad');dv.textContent=elig.usable?"},{"line":1751,"index":10083063,"text":"TLE_FOCUS_R1__?.decorate?.()}ev.preventDefault()} function endRouteDrag(ev){if(!routeDrag||ev.pointerId!==routeDrag.pointerId)return;const me=state.match.players[0],drop=nearestProjectedNode(ev.clientX,ev.clientY,true),last=routeDrag.path.at(-1),tapOnly=routeDrag.path.length===1;if(tapOnly&&drop&&drop!==last){routeDrag=null;$('#battleRuntime').classList.remove('dragging');selectEndpoint(drop);refreshDragVisual();ev.preventDefault();return}if(!drop||drop!==last){me.plan.path=routeDrag.before;toast('移動可能マスへ置くと経路を予約します')}else me.plan.path=[...routeDrag.path];routeDrag=null;$('#battleRuntime').classList.remove('dragging');refreshDragVisual();ev.preventDefault()} function bindRouteDrag(){const e=$('#battleRuntime'),map=$('#battleMap');if(!e||e.dataset.dragBound)return;e.dataset.dragBound='1';e.addEventListener('pointerdown',beginRouteDrag);map.addEventListener('pointermove',moveRouteDrag,{passive:false});map.addEventListener('pointerup',endRouteDrag,{passive:false});map.addEventListener('pointercancel',endRouteDrag,{passive:false})} function awakeManaFromHoney(p,count){const before=p.awake,wake=Math.max(0,Math.min(count,p.mana.length-p.awake));p.awake+=wake;return{before,wake,after:p.awake,excess:count-wake}} function honeySourceNodesForPower(power){if(!state.match||power<1||power>6)return[];const out=[];for(const p of state.match.players){for(const l of ['L','C','R']){p.lanes[l].forEach((cid,ix)=>{if(CARDS[cid]?.power===power)out.push(roadId(p.id,l,ix+1))})}}return out} function spawnHoneyFromRoads(){const m=state.match;if(m?.contentId!=='honey_hunt')return;for(const p of m.players){const power=CARDS[p.plan?.roadId]?.power||0;if(power<1||power>6)continue;for(const id of honeySourceNodesForPower(power))m.honey.byNode[id]=(m.honey.byNode[id]||0)+1}m.honey.generatedRounds.add(m.round)} function collectEndpointHoney(p){const m=state.match;if(m?.contentId!=='honey_hunt')return{status:'not-honey'};const id=p.position,count=m.honey.byNode[id]||0,key=`${m.round}:${p.id}:${id}`;if(m.honey.receipts.has(key))return{status:'duplicate',count:0};m.honey.receipts.add(key);if(count<=0)return{status:'none',count:0};m.honey.byNode[id]=0;const w=awakeManaFromHoney(p,count);const excess=w.excess>0?`／起床上限超過${w.excess}は持ち越さない`:'';log(`${p.name}：終点のハニー${count}個を全量取得。寝ているマナを${w.wake}枚起こした${excess}。`);if(p.human)window.GAMEROAD_PARTNER_COMMENT?.emit?.({eventId:`${m.id}:honey:${m.round}:${p.id}:${id}`,kind:'dialogue',speechIntent:'honey_collected',count,loggable:false,ttlMs:9000});return{status:'collected-all',count,...w}} const grControlledCharacter4pLiveState={warningShown:false}; function grControlledCharacter4pSource(){const m=state.match;if(!m||!Array.isArray(m.players)||m.players.length!==4)return null;const participants=m.players.map(p=>({participantId:p.id,characterId:String(p.character||''),positionKey:p.position}));if(participants.some(row=>!row.participantId||!row.characterId))return null;return{matchId:String(m.id||''),participants,reducedMotion:!!state.settings.reduceMotion,lowPerformance:!!state.settings.lowPerf}} window.__GAMEROAD_CONTROLLED_CHARACTER_4P_SOURCE__=grControlledCharacter4pSource; function grControlledCharacter4pEvent(p,type,detail={}){const emit=window.__GAMEROAD_CONTROLLED_CHARACTER_4P_EVENT__;if(typeof emit!=='function')return null;try{return emit(p?.id,{type,...detail})}catch(e){if(!grControlledCharacter4pLiveState.warningShown){grControlledCharacter4pLiveState.warn"},{"line":1952,"index":10156898,"text":"')}save();renderSettings();await audioPrimeUnlock()}; $('#previewBgm').onclick=async()=>{await audioPrimeUnlock();const a=normalizeAudioSettings();const r=await audioRequestMusic('settings_preview',a.battleMusicKey,'settings-preview');if(!r.ok)audioSetStatus(`BGM試聴不可: ${r.error}`)}; $('#previewMatchFound').onclick=async()=>{await audioPrimeUnlock();const r=await audioEmitSfx('match_found','');if(!r.ok)audioSetStatus(`相手発見音試聴不可: ${r.error}`)}; $('#previewBattleStart').onclick=async()=>{await audioPrimeUnlock();const r=await audioEmitSfx('battle_start','');if(!r.ok)audioSetStatus(`対戦開始音試聴不可: ${r.error}`)}; $('#previewComplete').onclick=async()=>{await audioPrimeUnlock();const r=await audioEmitSfx('all_players_completed','');if(!r.ok)audioSetStatus(`完了音試聴不可: ${r.error}`)}; } document.addEventListener('pointerdown',()=>{if(audioPreviewAllowed()&&!audioRuntime.unlocked)void audioPrimeUnlock()},{passive:true}); function renderSettings(){const a=normalizeAudioSettings();$('#reduceMotion').textContent=state.settings.reduceMotion?'ON':'OFF';$('#lowPerf').textContent=state.settings.lowPerf?'ON':'OFF';$('#storageStatus').textContent=state.storage==='localStorage'?'端末保存':'この起動中に保存';$('#musicVolume').value=Math.round(a.musicVolume*100);$('#sfxVolume').value=Math.round(a.sfxVolume*100);$('#partnerVoiceVolume').value=Math.round(a.partnerVoiceVolume*100);$('#musicVolumeLabel').textContent=$('#musicVolume').value;$('#sfxVolumeLabel').textContent=$('#sfxVolume').value;$('#partnerVoiceVolumeLabel').textContent=$('#partnerVoiceVolume').value;$('#musicMute').textContent=a.musicMuted?'音楽消音 ON':'音楽消音 OFF';$('#sfxMute').textContent=a.sfxMuted?'SFX消音 ON':'SFX消音 OFF';$('#partnerVoiceMute').textContent=a.partnerVoiceMuted?'パートナー音声消音 ON':'パートナー音声消音 OFF';$('#audioPreviewPack').value=a.previewPack;$('#battleMusicKey').value=a.battleMusicKey;$('#battleMusicKey').disabled=a.previewPack==='none';$('#audioStatus').textContent=audioRuntime.status} $('#reduceMotion').onclick=()=>{state.settings.reduceMotion=!state.settings.reduceMotion;document.documentElement.style.setProperty('--motion',state.settings.reduceMotion?'0':'1');save();renderSettings()};$('#lowPerf').onclick=()=>{state.settings.lowPerf=!state.settings.lowPerf;save();renderSettings()};$('#resetSave').onclick=()=>{if(!window.confirm('端末のGAMEROAD保存を初期化します。よろしいですか？'))return false;const rr=resetExplicitSaveKeys(localStorage,[SAVE_KEY],{confirmed:true});saveRecoveryRuntime.reset=rr;if(rr.status!=='reset'){state.storage='memory';renderSettings();toast('保存の初期化に失敗しました。元の保存データを維持します');return false}memorySave=null;clearDeckDraftSession();state.history=[];state.progression={battlePoints:0};state.setupMode='2p';state.setupContent='road_shield';state.selectedPartnerId='partner.naki';state.playerCharacterId='partner.naki';state.characterSelectionRole='partner';state.partnerProfiles=normalizePartnerProfiles(null);state.settings={reduceMotion:false,lowPerf:false,audio:{...AUDIO_DEFAULTS}};state.lastPackPreview=[];state.gachaBusy=false;state.savedDeck={main:[...DEFAULT_DECK],ex:[]};state.savedDeckRule={id:null,revision:null};state.deckDraft={main:[...DEFAULT_DECK],ex:[]};state.storage='memory';audioStopMusic('reset');audioRuntime.seenEvents.clear();renderSettings();renderCards();renderSetupDeckStatus();toast('保存を初期化しました');return true};wireAudioSettings(); function cardConservation(){if(!state.match)return true;for(const p of state.match.players){con"}],"wheel":[],"renderField3DCalls":[{"line":1702,"index":10061034,"text":" float;varying vec3 vColor;varying float vFog;void main(){vec3 haze=vec3(.54,.69,.63);vec3 col=mix(clamp(vColor,0.0,1.0),haze,vFog*.52);gl_FragColor=vec4(col,1.0);}');const pr=gl.createProgram();gl.attachShader(pr,vs);gl.attachShader(pr,fs);gl.linkProgram(pr);if(!gl.getProgramParameter(pr,gl.LINK_STATUS))throw Error(gl.getProgramInfoLog(pr)||'link');fieldGL=gl;fieldProgram=pr;if(!c.dataset.grasslandGlLifecycle){c.dataset.grasslandGlLifecycle='1';c.addEventListener('webglcontextlost',e=>{e.preventDefault();fieldGL=null;fieldProgram=null});c.addEventListener('webglcontextrestored',()=>{fieldGL=null;fieldProgram=null;if(state.screen==='battle'){renderField3D();renderBoard()}})}return gl} function pushBox(arr,x,y,z,sx,sy,sz,col){const [r,g,b]=col,x0=x-sx/2,x1=x+sx/2,y0=y,y1=y+sy,z0=z-sz/2,z1=z+sz/2;const V=[[x0,y0,z0],[x1,y0,z0],[x1,y0,z1],[x0,y0,z1],[x0,y1,z0],[x1,y1,z0],[x1,y1,z1],[x0,y1,z1]],F=[[4,5,6,4,6,7],[0,2,1,0,3,2],[0,4,7,0,7,3],[1,2,6,1,6,5],[3,7,6,3,6,2],[0,1,5,0,5,4]];F.forEach((f,fi)=>{const k=fi===0?1:fi===1?.52:.72;for(const q of f){arr.push(...V[q],r*k,g*k,b*k)}})} function addDiamond(arr,w,col,scale=.42,height=.11){pushBox(arr,w.x,w.y,w.z,scale,height,scale,col)} const FIELD_PLAYER_COLORS={P1:[.72,.16,.20],P2:[.12,.38,.70],P3:[.82,.60,.10],P4:[.10,.58,.34]}; function fieldPlayerColor(owner,active=false){const c=FIELD_PLAYER_COLORS[owner]||[.22,.34,.28];return active?c:c.map(v=>v*.68+.04)} /* GRASSLAND-3D-R2: gameplay graph/hit remains DOM data; this WebGL layer owns only terrain, atmosphere and non-interactive scenery. */ function fieldRand(i){let x=(i+1)|0;x=Math.imul(x^0x9e3779b9,0x85ebca6b);x^=x>>>13;x=Math.imul(x,0xc2b2ae35);x^=x>>>16;return(x>>>0)/4294967296} function fieldTerrainHeight(x,z){const r=Math.hypot(x,z),edge=Math.max(0,Math.min(1,(r-11)/20)),ridge=edge*edge*(.92+.34*Math.sin(x*.18)+.28*Math.cos(z*.16)),mound=(mx,mz,a,rr)=>a*Math.exp(-((x-mx)*(x-mx)+(z-mz)*(z-mz))/rr);return-.31+.036*Math.sin(x*.41)+.032*Math.cos(z*.37)+.018*Math.sin((x-z)*.63)+ridge+mound(-13,-9,.42,22)+mound(14,-8,.36,28)+mound(-12,11,.34,25)+mound(13,12,.40,24)} function pushLitTri(arr,a,b,c,col){const ux=b[0]-a[0],uy=b[1]-a[1],u"},{"line":1762,"index":10086705,"text":"leMount||!window.GameRoadThreeCharRuntime)return;try{await window.GameRoadThreeCharRuntime.setState(battleMount,name,{facing:facing||battleMount.facingName||'right',performance:(state.settings.lowPerf||state.settings.reduceMotion)?'low':'normal'})}catch(e){console.warn('battle-runtime-state',name,e)}} async function animateResolvedPath(p){const path=p.plan?.path||[p.position],human=p.human,dur=(state.settings.reduceMotion||state.settings.lowPerf)?0:145;if(path.length<2){p.position=path.at(-1)||p.position;collectEndpointHoney(p);grControlledCharacter4pEvent(p,'SETTLE',{positionKey:p.position});if(human)await setBattleRuntimeState('idle');else{renderField3D();renderBoard()}return}let facing=battleMount?.facingName||'right';if(human){facing=movementFacing(path[0],path[1],p);await setBattleRuntimeState('move',facing)}for(let i=1;i<path.length;i++){const from=path[i-1],to=path[i],nextFacing=movementFacing(from,to,p);p.position=to;if(human&&nextFacing!==facing)await setBattleRuntimeState('move',nextFacing);facing=nextFacing;grControlledCharacter4pEvent(p,'MOVE_ACCEPTED',{toPositionKey:to,facing});renderField3D();renderBoard();if(human)await updateBattleAvatar();if(dur)await new Promise(r=>setTimeout(r,dur))}collectEndpointHoney(p);grControlledCharacter4pEvent(p,'SETTLE',{positionKey:p.position});if(human)await setBattleRuntimeState('idle',facing);else{renderField3D();renderBoard()}} function setupBattleDetails(){const btn=$('#detailsBtn'),close=$('#detailsClose'),drawer=$('#battleDrawer');if(btn&&!btn.dataset.bound){btn.dataset.bound='1';btn.onclick=()=>{drawer.classList.add('on');drawer.setAttribute('aria-hidden','false')};close.onclick=()=>{drawer.classList.remove('on');drawer.setAttribute('aria-hidden','true')};$('#dangerBtn').onclick=()=>{$('#battleMap').classList.toggle('showRange');toast($('#battleMap').classList.contains('showRange')?'移動可能範囲を表示':'範囲表示を戻しました')}}} window.addEventListener('resize',()=>{if(fieldResizeQueued)return;fieldResizeQueued=true;requestAnimationFrame(()=>{fieldResizeQueued=false;if(state.screen==='battle'){renderField3D();renderBoard();updateBattleAvatar()}})}); async function updateBattleAvatar(){if(!stat"},{"line":1762,"index":10087163,"text":"f(path.length<2){p.position=path.at(-1)||p.position;collectEndpointHoney(p);grControlledCharacter4pEvent(p,'SETTLE',{positionKey:p.position});if(human)await setBattleRuntimeState('idle');else{renderField3D();renderBoard()}return}let facing=battleMount?.facingName||'right';if(human){facing=movementFacing(path[0],path[1],p);await setBattleRuntimeState('move',facing)}for(let i=1;i<path.length;i++){const from=path[i-1],to=path[i],nextFacing=movementFacing(from,to,p);p.position=to;if(human&&nextFacing!==facing)await setBattleRuntimeState('move',nextFacing);facing=nextFacing;grControlledCharacter4pEvent(p,'MOVE_ACCEPTED',{toPositionKey:to,facing});renderField3D();renderBoard();if(human)await updateBattleAvatar();if(dur)await new Promise(r=>setTimeout(r,dur))}collectEndpointHoney(p);grControlledCharacter4pEvent(p,'SETTLE',{positionKey:p.position});if(human)await setBattleRuntimeState('idle',facing);else{renderField3D();renderBoard()}} function setupBattleDetails(){const btn=$('#detailsBtn'),close=$('#detailsClose'),drawer=$('#battleDrawer');if(btn&&!btn.dataset.bound){btn.dataset.bound='1';btn.onclick=()=>{drawer.classList.add('on');drawer.setAttribute('aria-hidden','false')};close.onclick=()=>{drawer.classList.remove('on');drawer.setAttribute('aria-hidden','true')};$('#dangerBtn').onclick=()=>{$('#battleMap').classList.toggle('showRange');toast($('#battleMap').classList.contains('showRange')?'移動可能範囲を表示':'範囲表示を戻しました')}}} window.addEventListener('resize',()=>{if(fieldResizeQueued)return;fieldResizeQueued=true;requestAnimationFrame(()=>{fieldResizeQueued=false;if(state.screen==='battle'){renderField3D();renderBoard();updateBattleAvatar()}})}); async function updateBattleAvatar(){if(!state.match)return;const me=state.match.players[0],visual=(state.match.phase==='plan'&&me.plan?.path?.length)?me.plan.path.at(-1):me.position;const xy=nodeXY(visual,me),el=$('#battleRuntime');el.style.left=xy[0]+'%';el.style.top=xy[1]+'%';if(!battleMount){if(!battleMountPromise)battleMountPromise=mountChar('#battleRuntime',state.playerCharacterId,'idle').then(x=>(battleMount=x,x)).finally(()=>{battleMountPromise=null});battleMount=await battleMountPromise}la"},{"line":1762,"index":10087423,"text":"ame||'right';if(human){facing=movementFacing(path[0],path[1],p);await setBattleRuntimeState('move',facing)}for(let i=1;i<path.length;i++){const from=path[i-1],to=path[i],nextFacing=movementFacing(from,to,p);p.position=to;if(human&&nextFacing!==facing)await setBattleRuntimeState('move',nextFacing);facing=nextFacing;grControlledCharacter4pEvent(p,'MOVE_ACCEPTED',{toPositionKey:to,facing});renderField3D();renderBoard();if(human)await updateBattleAvatar();if(dur)await new Promise(r=>setTimeout(r,dur))}collectEndpointHoney(p);grControlledCharacter4pEvent(p,'SETTLE',{positionKey:p.position});if(human)await setBattleRuntimeState('idle',facing);else{renderField3D();renderBoard()}} function setupBattleDetails(){const btn=$('#detailsBtn'),close=$('#detailsClose'),drawer=$('#battleDrawer');if(btn&&!btn.dataset.bound){btn.dataset.bound='1';btn.onclick=()=>{drawer.classList.add('on');drawer.setAttribute('aria-hidden','false')};close.onclick=()=>{drawer.classList.remove('on');drawer.setAttribute('aria-hidden','true')};$('#dangerBtn').onclick=()=>{$('#battleMap').classList.toggle('showRange');toast($('#battleMap').classList.contains('showRange')?'移動可能範囲を表示':'範囲表示を戻しました')}}} window.addEventListener('resize',()=>{if(fieldResizeQueued)return;fieldResizeQueued=true;requestAnimationFrame(()=>{fieldResizeQueued=false;if(state.screen==='battle'){renderField3D();renderBoard();updateBattleAvatar()}})}); async function updateBattleAvatar(){if(!state.match)return;const me=state.match.players[0],visual=(state.match.phase==='plan'&&me.plan?.path?.length)?me.plan.path.at(-1):me.position;const xy=nodeXY(visual,me),el=$('#battleRuntime');el.style.left=xy[0]+'%';el.style.top=xy[1]+'%';if(!battleMount){if(!battleMountPromise)battleMountPromise=mountChar('#battleRuntime',state.playerCharacterId,'idle').then(x=>(battleMount=x,x)).finally(()=>{battleMountPromise=null});battleMount=await battleMountPromise}layoutBoardPlayerTokens();renderRouteLine();bindRouteDrag()} function suitGlyphForCard(c){const raw=String(c?.suit||'').trim(),u=raw.toUpperCase(),s=raw.toLowerCase(),m={SP:'♠',HT:'♥',DI:'♦',CL:'♣',DCG:'◆'};return m[u]||(s.startsWith('spade')?'♠':s.startsWith('h"},{"line":1764,"index":10088119,"text":"BattleDetails(){const btn=$('#detailsBtn'),close=$('#detailsClose'),drawer=$('#battleDrawer');if(btn&&!btn.dataset.bound){btn.dataset.bound='1';btn.onclick=()=>{drawer.classList.add('on');drawer.setAttribute('aria-hidden','false')};close.onclick=()=>{drawer.classList.remove('on');drawer.setAttribute('aria-hidden','true')};$('#dangerBtn').onclick=()=>{$('#battleMap').classList.toggle('showRange');toast($('#battleMap').classList.contains('showRange')?'移動可能範囲を表示':'範囲表示を戻しました')}}} window.addEventListener('resize',()=>{if(fieldResizeQueued)return;fieldResizeQueued=true;requestAnimationFrame(()=>{fieldResizeQueued=false;if(state.screen==='battle'){renderField3D();renderBoard();updateBattleAvatar()}})}); async function updateBattleAvatar(){if(!state.match)return;const me=state.match.players[0],visual=(state.match.phase==='plan'&&me.plan?.path?.length)?me.plan.path.at(-1):me.position;const xy=nodeXY(visual,me),el=$('#battleRuntime');el.style.left=xy[0]+'%';el.style.top=xy[1]+'%';if(!battleMount){if(!battleMountPromise)battleMountPromise=mountChar('#battleRuntime',state.playerCharacterId,'idle').then(x=>(battleMount=x,x)).finally(()=>{battleMountPromise=null});battleMount=await battleMountPromise}layoutBoardPlayerTokens();renderRouteLine();bindRouteDrag()} function suitGlyphForCard(c){const raw=String(c?.suit||'').trim(),u=raw.toUpperCase(),s=raw.toLowerCase(),m={SP:'♠',HT:'♥',DI:'♦',CL:'♣',DCG:'◆'};return m[u]||(s.startsWith('spade')?'♠':s.startsWith('heart')?'♥':s.startsWith('diamond')?'♦':s.startsWith('club')?'♣':'•')} function layoutBoardPlayerTokens(){const root=$('#boardPlayers'),actor=$('#battleRuntime');if(!root||!actor)return;const a=actor.getBoundingClientRect(),els=[...root.children],placed=[],narrow=innerWidth<=540,mid=innerWidth<=900,rx=narrow?46:(mid?42:54),ry=narrow?30:(mid?28:34),hit=(r,t,g=3)=>Math.min(r.right+g,t.right)>Math.max(r.left-g,t.left)&&Math.min(r.bottom+g,t.bottom)>Math.max(r.top-g,t.top);for(const el of els){const id=el.dataset.player,count=Number(el.dataset.groupCount)||1,q={P1:[-1,-1],P2:[1,-1],P3:[-1,1],P4:[1,1]}[id]||[0,-1],solo=[[0,-ry],[q[0]*rx,q[1]*ry],[-rx,-ry],[rx,-ry],[-rx,0],[rx,0],[-rx,ry],[rx,r"},{"line":1770,"index":10091465,"text":"aset.player=p.id;el.dataset.character=c.id;el.style.left=xy[0]+'%';el.style.top=xy[1]+'%';const off=[0,0];el.dataset.groupCount=String(same.length);el.style.setProperty('--ox',off[0]+'px');el.style.setProperty('--oy',off[1]+'px');el.style.setProperty('--player-color',({P1:'#943746',P2:'#285b91',P3:'#a98520',P4:'#287253'})[p.id]||'#245b49');el.setAttribute('aria-label',`${p.id} ${p.human?'あなた':c.name}`);el.title=`${p.id} ${p.human?'あなた':c.name}`;el.innerHTML=`<b>${p.id}</b><span>${p.human?'あなた':c.name}</span>`;root.appendChild(el)}layoutBoardPlayerTokens()} function renderBoard(){const board=$('#board'),me=state.match.players[0],m=state.match;renderField3D();board.innerHTML='';const all=[];centerIds().forEach(id=>all.push(id));['P1','P2','P3','P4'].forEach(pid=>['L','C','R'].forEach(l=>{all.push(shieldId(pid,l));for(let d=1;d<=7;d++)all.push(roadId(pid,l,d))}));['K:NW','K:NE','K:SW','K:SE'].forEach(x=>all.push(x));const moveLimit=normalMoveLimitForRoad(me.plan?.roadId),reach=m.phase==='plan'&&me.plan?.roadId&&moveLimit>0?reachable(me,moveLimit):new Map();renderRangeSurfaces(reach,moveLimit,me);all.forEach(id=>{const e=document.createElement('button');e.className='node '+posType(id);e.dataset.pos=id;const xy=nodeXY(id,me);e.style.left=xy[0]+'%';e.style.top=xy[1]+'%';if(activeNodes().has(id)&&id.startsWith('R:'))e.classList.add('active');const moveDistance=reach.get(id),pathIndex=me.plan?.path?.indexOf(id)??-1;if(reach.has(id)&&moveDistance<=moveLimit){e.classList.add('reachable');e.dataset.moveDistance=String(moveDistance);e.setAttribute('aria-label',`${id} 移動可能 ${moveDistance}歩`);e.title=`移動可能 ${moveDistance}歩`}if(id===me.position){e.classList.add('currentPosition');e.dataset.decisionLabel='現在';e.setAttribute('aria-label',`${id} 現在地`);e.title='現在地'}if(moveDistance===1){e.classList.add('nextStep');e.dataset.decisionLabel='次'}if(pathIndex>=0){e.classList.add('path','pathStep');e.dataset.pathIndex=String(pathIndex);e.dataset.decisionLabel=pathIndex===0?'現在':pathIndex===me.plan.path.length-1?'終点':`経路${pathIndex}`;e.setAttribute('aria-label',`${id} ${e.dataset.decisionLabel}`);e.title=e.dataset.decisionLabel;if(pathIndex===me.plan.pat"}],"renderBoardDef":[{"line":1770,"index":10091378,"text":";const viewer=m.players[0],groups=new Map();for(const p of m.players){const a=groups.get(p.position)||[];a.push(p);groups.set(p.position,a)}for(const p of m.players){const xy=nodeXY(p.position,viewer),same=groups.get(p.position)||[p],slot=same.indexOf(p),c=PROD_CHARS.find(x=>x.id===p.character)||PROD_CHARS[0],el=document.createElement('div');el.className='boardPlayerToken'+(p.human?' human':'')+(m.activeId===p.id?' active':'');el.dataset.player=p.id;el.dataset.character=c.id;el.style.left=xy[0]+'%';el.style.top=xy[1]+'%';const off=[0,0];el.dataset.groupCount=String(same.length);el.style.setProperty('--ox',off[0]+'px');el.style.setProperty('--oy',off[1]+'px');el.style.setProperty('--player-color',({P1:'#943746',P2:'#285b91',P3:'#a98520',P4:'#287253'})[p.id]||'#245b49');el.setAttribute('aria-label',`${p.id} ${p.human?'あなた':c.name}`);el.title=`${p.id} ${p.human?'あなた':c.name}`;el.innerHTML=`<b>${p.id}</b><span>${p.human?'あなた':c.name}</span>`;root.appendChild(el)}layoutBoardPlayerTokens()} function renderBoard(){const board=$('#board'),me=state.match.players[0],m=state.match;renderField3D();board.innerHTML='';const all=[];centerIds().forEach(id=>all.push(id));['P1','P2','P3','P4'].forEach(pid=>['L','C','R'].forEach(l=>{all.push(shieldId(pid,l));for(let d=1;d<=7;d++)all.push(roadId(pid,l,d))}));['K:NW','K:NE','K:SW','K:SE'].forEach(x=>all.push(x));const moveLimit=normalMoveLimitForRoad(me.plan?.roadId),reach=m.phase==='plan'&&me.plan?.roadId&&moveLimit>0?reachable(me,moveLimit):new Map();renderRangeSurfaces(reach,moveLimit,me);all.forEach(id=>{const e=document.createElement('button');e.className='node '+posType(id);e.dataset.pos=id;const xy=nodeXY(id,me);e.style.left=xy[0]+'%';e.style.top=xy[1]+'%';if(activeNodes().has(id)&&id.startsWith('R:'))e.classList.add('active');const moveDistance=reach.get(id),pathIndex=me.plan?.path?.indexOf(id)??-1;if(reach.has(id)&&moveDistance<=moveLimit){e.classList.add('reachable');e.dataset.moveDistance=String(moveDistance);e.setAttribute('aria-label',`${id} 移動可能 ${moveDistance}歩`);e.title=`移動可能 ${moveDistance}歩`}if(id===me.position){e.classList.add('currentPosition');e.dataset.decisionLabel='現在';e.setAttribute('aria-label',`${id} 現在地`);e.title='現在地'}if(moveDistance===1){e.classList.add('nextStep');e.dataset.decisionLabel='次'}if(pathIndex>=0){e.classList.add('path','pathStep');e.dataset.pathIndex=String(pathIndex);e.dataset.decisionLabel=pathIndex===0?'現在':pathIndex===me.plan.path.length-1?'終点':`経路${pathIndex}`;e.setAttribute('aria-label',`${id} ${e.dataset.decisionLabel}`);e.title=e.dataset.decisionLabel;if(pathIndex===me.plan.path.length-1&&pathIndex>0)e.classList.add('pathEnd')}if(m.players.some(p=>p.position===id))e.classList.add('person');if(id.startsWith('R:')){const q=parseRoad(id),owner=m.players.find(p=>p.id===q.owner),lane=owner?.lanes?.[q.lane]||[];if(q.depth<=lane.length){const cardId=lane[q.depth-1],card=CARDS[cardId];e.classList.add('placedCard');e.dataset.cardId=cardId;e.title=`${owner?.name||q.owner} ${q.lane}列 ${q.depth}/${lane.length}: ${card?.display_name||cardId}`;const face=document.createElement('span');face.className='placedCardFace';face.textContent=`${suitGlyphForCard(card)}${Number(card?.power)||''}`;e.appendChild(face);if(q.depth===lane.length){const prog=document.createElement('span');prog.className='placedLaneProgress';prog.textContent=`${q.lane} ${lane.length}/7`;e.appendChild(prog)}}}const honey=m.honey?.byNode?.[id]||0;if(honey>0){e.classList.add('honeyNode');e.dataset.honey=honey>1?'×'+honey:'◆'}if(!e.classList.contains('reachable'))e.disabled=true;else e.onclick=ev=>selectEndpoint(nearestProjecte"}],"battleMapMarkup":[{"line":1154,"index":1102257,"text":"div class=\"settingRow\"><span>相手</span><b>ボット</b></div><div class=\"settingRow setupStartPosition\" hidden><span>開始位置</span><b>中央・共通開始</b></div><div class=\"setupDeckRecovery\"><div class=\"setupDeckNote\" id=\"setupDeckNote\">札組を確認中</div><button class=\"btn\" id=\"fixDeckFromSetup\" hidden>デッキへ</button></div><button class=\"btn\" id=\"friendRoomEntry\">フレンドルーム</button><button class=\"btn primary\" id=\"startMatch\">盤面へ</button></div></section> <section class=\"screen friendroom\" data-screen=\"friendroom\"><button class=\"btn back\" data-back aria-label=\"一つ前の画面へ戻る\">← 戻る</button><div id=\"friendRoomPanel\" class=\"friendRoomPanel\"></div></section><section class=\"screen battle\" data-screen=\"battle\"><div class=\"battleMap\" id=\"battleMap\"><canvas id=\"fieldCanvas\" aria-hidden=\"true\"></canvas><svg id=\"routeSvg\" class=\"routeSvg\" aria-hidden=\"true\" viewBox=\"0 0 100 100\" preserveAspectRatio=\"none\"><g id=\"rangeSurfaceLayer\"></g><polyline id=\"routeLine\"></polyline></svg><div class=\"phaseBar\" id=\"phaseBar\"><span data-ph=\"plan\">計画</span><span data-ph=\"reveal\">公開</span><span data-ph=\"move\">移動</span><span data-ph=\"target\">対象</span><span data-ph=\"resolve\">戦闘</span></div><div class=\"battleTopStatus\"><div><span class=\"k\">ROUND <b id=\"roundNo\">1</b></span><strong id=\"phaseTitle\">行動を計画</strong></div><div class=\"resourceStrip\"><span id=\"manaMeter\">マナ 0/7</span><span id=\"honeyMeter\" hidden>ハニー 0</span></div></div><div class=\"publicTurnHud\" id=\"publicTurnHud\" aria-live=\"polite\"><div class=\"publicPlayerStrip\" id=\"publicPlayerStrip\"></div><div class=\"royalUsageStrip\" id=\"royalUsageStrip\" aria-label=\"各プレイヤーのロイヤルカード使用数\"></div></div><div class=\"board\" id=\"board\"></div><div id=\"boardPlayers\" class=\"boardPlayers\" aria-label=\"盤上参加者\"></div><div id=\"battleRuntime\" class=\"battleAvatar\" role=\"button\" aria-label=\"人物をドラッグして移動経路を予約\"></div><div id=\"originMarker\" class=\"originMarker\" aria-hidden=\"true\"></div><div class=\"quickAwardBurst\" id=\"quickAwardBurst\" aria-live=\"polite\" aria-atomic=\"true\"><strong></strong><span></span></div><section id=\"battleResolution\" class=\"battleResolution\" aria-live=\"polite\" aria-atomic=\"true\" hidden></section><div class=\"battleInfo\"><div class=\"hand\" id=\"hand\"></div><div class=\"planBox\"><div class=\"planSelect\"><label>ロード</label><select id=\"roadSelect\"></select></div><div class=\"planSelect\"><label>バトル</label><select id=\"battleSelect\"></select></div><div class=\"endpointChip\"><label>終点</label><b id=\"endpointText\">C:0:0</b></div><button class=\"iconBtn\" id=\"clearPath\" title=\"経路を戻す\" aria-label=\"経路を戻す\">↶</button><di"}],"fieldCanvasMarkup":[{"line":1154,"index":1102280,"text":"<span>相手</span><b>ボット</b></div><div class=\"settingRow setupStartPosition\" hidden><span>開始位置</span><b>中央・共通開始</b></div><div class=\"setupDeckRecovery\"><div class=\"setupDeckNote\" id=\"setupDeckNote\">札組を確認中</div><button class=\"btn\" id=\"fixDeckFromSetup\" hidden>デッキへ</button></div><button class=\"btn\" id=\"friendRoomEntry\">フレンドルーム</button><button class=\"btn primary\" id=\"startMatch\">盤面へ</button></div></section> <section class=\"screen friendroom\" data-screen=\"friendroom\"><button class=\"btn back\" data-back aria-label=\"一つ前の画面へ戻る\">← 戻る</button><div id=\"friendRoomPanel\" class=\"friendRoomPanel\"></div></section><section class=\"screen battle\" data-screen=\"battle\"><div class=\"battleMap\" id=\"battleMap\"><canvas id=\"fieldCanvas\" aria-hidden=\"true\"></canvas><svg id=\"routeSvg\" class=\"routeSvg\" aria-hidden=\"true\" viewBox=\"0 0 100 100\" preserveAspectRatio=\"none\"><g id=\"rangeSurfaceLayer\"></g><polyline id=\"routeLine\"></polyline></svg><div class=\"phaseBar\" id=\"phaseBar\"><span data-ph=\"plan\">計画</span><span data-ph=\"reveal\">公開</span><span data-ph=\"move\">移動</span><span data-ph=\"target\">対象</span><span data-ph=\"resolve\">戦闘</span></div><div class=\"battleTopStatus\"><div><span class=\"k\">ROUND <b id=\"roundNo\">1</b></span><strong id=\"phaseTitle\">行動を計画</strong></div><div class=\"resourceStrip\"><span id=\"manaMeter\">マナ 0/7</span><span id=\"honeyMeter\" hidden>ハニー 0</span></div></div><div class=\"publicTurnHud\" id=\"publicTurnHud\" aria-live=\"polite\"><div class=\"publicPlayerStrip\" id=\"publicPlayerStrip\"></div><div class=\"royalUsageStrip\" id=\"royalUsageStrip\" aria-label=\"各プレイヤーのロイヤルカード使用数\"></div></div><div class=\"board\" id=\"board\"></div><div id=\"boardPlayers\" class=\"boardPlayers\" aria-label=\"盤上参加者\"></div><div id=\"battleRuntime\" class=\"battleAvatar\" role=\"button\" aria-label=\"人物をドラッグして移動経路を予約\"></div><div id=\"originMarker\" class=\"originMarker\" aria-hidden=\"true\"></div><div class=\"quickAwardBurst\" id=\"quickAwardBurst\" aria-live=\"polite\" aria-atomic=\"true\"><strong></strong><span></span></div><section id=\"battleResolution\" class=\"battleResolution\" aria-live=\"polite\" aria-atomic=\"true\" hidden></section><div class=\"battleInfo\"><div class=\"hand\" id=\"hand\"></div><div class=\"planBox\"><div class=\"planSelect\"><label>ロード</label><select id=\"roadSelect\"></select></div><div class=\"planSelect\"><label>バトル</label><select id=\"battleSelect\"></select></div><div class=\"endpointChip\"><label>終点</label><b id=\"endpointText\">C:0:0</b></div><button class=\"iconBtn\" id=\"clearPath\" title=\"経路を戻す\" aria-label=\"経路を戻す\">↶</button><div class=\"quickReadyGroup\""}],"battleRuntimeMarkup":[{"line":1154,"index":1103270,"text":"an data-ph=\"reveal\">公開</span><span data-ph=\"move\">移動</span><span data-ph=\"target\">対象</span><span data-ph=\"resolve\">戦闘</span></div><div class=\"battleTopStatus\"><div><span class=\"k\">ROUND <b id=\"roundNo\">1</b></span><strong id=\"phaseTitle\">行動を計画</strong></div><div class=\"resourceStrip\"><span id=\"manaMeter\">マナ 0/7</span><span id=\"honeyMeter\" hidden>ハニー 0</span></div></div><div class=\"publicTurnHud\" id=\"publicTurnHud\" aria-live=\"polite\"><div class=\"publicPlayerStrip\" id=\"publicPlayerStrip\"></div><div class=\"royalUsageStrip\" id=\"royalUsageStrip\" aria-label=\"各プレイヤーのロイヤルカード使用数\"></div></div><div class=\"board\" id=\"board\"></div><div id=\"boardPlayers\" class=\"boardPlayers\" aria-label=\"盤上参加者\"></div><div id=\"battleRuntime\" class=\"battleAvatar\" role=\"button\" aria-label=\"人物をドラッグして移動経路を予約\"></div><div id=\"originMarker\" class=\"originMarker\" aria-hidden=\"true\"></div><div class=\"quickAwardBurst\" id=\"quickAwardBurst\" aria-live=\"polite\" aria-atomic=\"true\"><strong></strong><span></span></div><section id=\"battleResolution\" class=\"battleResolution\" aria-live=\"polite\" aria-atomic=\"true\" hidden></section><div class=\"battleInfo\"><div class=\"hand\" id=\"hand\"></div><div class=\"planBox\"><div class=\"planSelect\"><label>ロード</label><select id=\"roadSelect\"></select></div><div class=\"planSelect\"><label>バトル</label><select id=\"battleSelect\"></select></div><div class=\"endpointChip\"><label>終点</label><b id=\"endpointText\">C:0:0</b></div><button class=\"iconBtn\" id=\"clearPath\" title=\"経路を戻す\" aria-label=\"経路を戻す\">↶</button><div class=\"quickReadyGroup\"><span class=\"quickCoil\" id=\"quickCoil\" aria-hidden=\"true\"><b id=\"quickRemain\">30</b></span><button class=\"btn primary\" id=\"readyPlan\"><span class=\"quickReadyText\">準備完了</span></button></div></div></div><div class=\"battleRail\"><button class=\"railBtn\" id=\"detailsBtn\" aria-label=\"対戦情報\">情報</button><button class=\"railBtn\" id=\"dangerBtn\" aria-label=\"移動可能範囲\">範囲</button><button class=\"railBtn warn\" id=\"leaveMatch\">退出</button></div><div class=\"targetBox floatingTarget\" id=\"targetBox\"><select id=\"targetPlayer\"></select><select id=\"targetLane\"><option value=\"L\">左列</option><option value=\"C\">中央列</option><option value=\"R\">右列</option></select><select id=\"targetShield\"><option value=\"L\">左シールド</option><option value=\"C\">中央シールド</option><option value=\"R\">右シールド</option></select><button class=\"btn primary\" id=\"confirmTarget\">攻撃先を決定</button></div></div><section id=\"battlePhaseSurface\" class=\"battlePhaseSurface\" aria-label=\"バトルフェイズ専用画面\" aria-live=\"polite\" hidden><div class=\"battlePhaseBackdrop\" aria-h"}],"buildCameraControl":0,"buildCameraRouter":0}
