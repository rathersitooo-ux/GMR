name: zz-temp-battle-camera-html-r5c
on:
  push:
    branches:
      - sol/battle-camera-html-r5c-20260910
permissions:
  contents: read
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run focused Battle camera tests
        run: |
          set -euo pipefail
          node --test tests/battle-camera-input-router.test.mjs
          node --test tests/battle-camera-live-runtime.test.mjs
          node --test tests/battle-camera-live-integration.test.mjs
